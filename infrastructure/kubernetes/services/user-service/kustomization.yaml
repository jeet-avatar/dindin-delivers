apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml

commonLabels:
  app.kubernetes.io/name: user-service
  app.kubernetes.io/component: microservice
  app.kubernetes.io/part-of: dollor-platform

images:
  - name: ${ECR_REGISTRY}/dollor/user-service
    newTag: latest
