# Dollor.ai Ride Service - Staging Overlay
# =========================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
  - ../../

patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: ride-service
      spec:
        replicas: 2
        template:
          spec:
            containers:
              - name: ride-service
                resources:
                  requests:
                    cpu: 100m
                    memory: 128Mi
                  limits:
                    cpu: 500m
                    memory: 512Mi

configMapGenerator:
  - name: ride-service-config
    behavior: merge
    literals:
      - ENVIRONMENT=staging
      - SERVICE_PORT=8014
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector.observability:4317

images:
  - name: dollor/ride-service
    newTag: staging-latest
