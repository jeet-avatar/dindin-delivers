# Dollor.ai Negotiation Service - ArgoCD Application (Staging)
# ==============================================================
# Deploys negotiation-service to staging environment

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: negotiation-service-staging
  namespace: argocd
  labels:
    app.kubernetes.io/name: negotiation-service
    app.kubernetes.io/component: negotiation-management
    app.kubernetes.io/part-of: dollor-platform
    platform: dollor-ai
    environment: staging
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: dollor-staging

  source:
    repoURL: https://github.com/dollor-ai/eatfair-ios.git
    targetRevision: HEAD
    path: infrastructure/kubernetes/services/negotiation-service/overlays/staging

  destination:
    server: https://kubernetes.default.svc
    namespace: staging

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas

  info:
    - name: Description
      value: "Negotiation management service for Dollor.ai platform (Staging)"
    - name: Port
      value: "8017"
    - name: Endpoints
      value: "/api/negotiation/*"
