# Dollor.ai TechCloudPro AI Video Service - ArgoCD Application (Dev)
# =====================================================================
# Deploys TechCloudPro AI video generation service to development environment

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: techcloudpro-employee-dev
  namespace: argocd
  labels:
    app.kubernetes.io/name: techcloudpro-employee
    app.kubernetes.io/component: ai-video-generation
    app.kubernetes.io/part-of: dollor-platform
    environment: dev
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: dollor-dev

  source:
    repoURL: https://github.com/dollor-ai/eatfair-ios.git
    targetRevision: HEAD
    path: infrastructure/kubernetes/services/techcloudpro-employee/overlays/dev

  destination:
    server: https://kubernetes.default.svc
    namespace: dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  # Health checks
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas

  info:
    - name: Description
      value: "AI-powered video generation service using PyTorch and diffusers"
    - name: Port
      value: "5000"
    - name: Technology
      value: "Flask, PyTorch, Transformers, Diffusers"
    - name: GPU
      value: "CPU only (dev)"
