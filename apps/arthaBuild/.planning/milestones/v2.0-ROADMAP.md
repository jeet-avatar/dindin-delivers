# ArthaBuild Roadmap
## Milestone 1: v1.0 — First Customer Ready

**Goal:** One customer can deploy ArthaBuild in their AWS VPC, connect their NetSuite account via TBA, ask the AI a NetSuite question, generate a SuiteScript, and deploy it to their NetSuite — all without any data leaving their infrastructure.

---

### Phase 1: Foundation & Auth Backend
**Goal:** Users can register, log in, reset passwords, and receive JWT tokens — all running inside the customer's VPC with no external auth dependencies.

**Requirements:** FR-AUTH-01, FR-AUTH-02, FR-AUTH-03, FR-AUTH-04, FR-AUTH-05, FR-AUTH-06

**Plans:** 5/5 plans executed — PHASE COMPLETE

Plans:
- [x] 01-01-PLAN.md — Extract source + remove hardcoded keys + port fix + .env setup (DONE 2026-04-07)
- [x] 01-02-PLAN.md — SQLite database foundation (database.py, models.py, Alembic migration) (DONE 2026-04-07)
- [x] 01-03-PLAN.md — Core auth endpoints: /api/user/register, /api/auth/check-user, /api/auth/login (DONE 2026-04-07)
- [x] 01-04-PLAN.md — Password reset + JWT refresh: forgot-password, reset-password, refresh (DONE 2026-04-07)
- [x] 01-05-PLAN.md — Wire into rawapi.py, startup validation, health endpoint, smoke tests (DONE 2026-04-07 — 22/22 tests)

---

### Phase 2: NetSuite TBA Session Management
**Goal:** Users can connect their NetSuite account using TBA credentials within a session. Credentials are held only in server memory and destroyed on logout/expiry. All SuiteCloud CLI operations use session credentials.

**Requirements:** FR-NS-01, FR-NS-02, FR-NS-03

**Plans:** 1/1 plans executed — PHASE COMPLETE
- [x] 02-01-PLAN.md — session_store.py, netsuite router (auth/status/logout), deploy router, frontend TBA modal (DONE 2026-04-08 — 57/57 tests)

Tasks:
- Create in-memory session credential store (keyed by user session JWT)
- Implement POST /api/netsuite/connect — accepts Account ID, Consumer Key/Secret, Token ID/Secret
- Validate credentials by running suitecloud account:setup CI mode
- Return connection status + account environment (sandbox vs production)
- Implement GET /api/netsuite/status — check if session is connected
- Implement POST /api/netsuite/disconnect — wipe credentials from memory
- Wire SuiteCloud CLI to use session credentials per request (not global config)
- Add frontend NetSuite credentials modal in ChatHeader (replace dead arthalicht.com URL)
- Show connection status indicator (green/red dot) in chat header
- Guard all SuiteScript deploy/fetch endpoints with NetSuite connection check
- Clear credentials automatically on JWT expiry and logout

---

### Phase 3: LLM Migration — OpenAI to Ollama
**Goal:** All AI inference runs locally via Ollama. Zero OpenAI API calls. FAISS vectorstore rebuilt with local nomic-embed-text embeddings (768-dim). Intent classification runs on local LLM.

**Requirements:** FR-LLM-01, FR-LLM-02, FR-LLM-03

**Plans:** 1/1 plans executed — PHASE COMPLETE
- [x] 03-01-PLAN.md — Ollama install, model pull, model_utils.py rewrite, FAISS rebuild script (768-dim) (DONE 2026-04-09)

Tasks:
- Install langchain-ollama, replace all langchain-openai imports
- Replace OpenAIEmbeddings with OllamaEmbeddings(model="nomic-embed-text")
- Replace init_chat_model OpenAI references with Ollama (llama3.1:8b)
- Replace OpenAI intent classifier (infer_intent) with local Ollama call
- Rebuild FAISS vectorstore: extract all source docs from index.pkl, re-embed with nomic-embed-text (768-dim), save new index
- Add OLLAMA_BASE_URL, OLLAMA_MODEL, OLLAMA_EMBED_MODEL to .env
- Add startup health check: verify Ollama running + models pulled
- grep -r "sk-proj" . must return zero matches
- grep -r "openai_api_key" . must return zero matches in production code
- Test full RAG pipeline with local LLM end-to-end

---

### Phase 4: Frontend Wiring to Real Backend
**Goal:** The React frontend communicates with the real FastAPI backend for all operations. Mock mode fully disabled. Chat sends messages to /api/chatbot/process and streams responses back.

**Requirements:** FR-CHAT-01, FR-CHAT-02, FR-CHAT-03, FR-CHAT-04

**Plans:**
- [x] 04-01-PLAN.md — .env port fix, api.ts Axios+JWT, authService real endpoints, chatService, E2E smoke test

Tasks:
- Fix VITE_API_URL in .env from 8080 to 8000
- Set VITE_ENABLE_MOCK_DATA=false permanently
- Wire Chat.tsx handleSend to POST /api/chatbot/process (replace local QA matching)
- Implement response streaming (EventSource / chunked response) in backend + frontend
- Wire auth service to real backend endpoints (already structured, just needs URL fix)
- Wire NetSuite connect modal to POST /api/netsuite/connect
- Handle loading states and error messages from real API responses
- Test full user flow: register → login → connect NetSuite → ask question → get AI answer

---

### Phase 5: Docker Compose Deployment Package
**Goal:** Customer runs docker-compose up -d on a fresh AWS EC2 and ArthaBuild is fully running within 10 minutes.

**Requirements:** FR-DEPLOY-01, FR-DEPLOY-02

**Plans:**
- [x] 05-01-PLAN.md — Dockerfile, nginx.conf, docker-compose.yml (GPU), Terraform EC2+EBS, deploy.sh, .env.example

Tasks:
- Write Dockerfile for FastAPI backend (includes SuiteCloud CLI, FAISS vectorstore, Python deps)
- Write nginx.conf for React frontend (SPA routing, proxy /api to backend)
- Write docker-compose.yml with 3 services: frontend, backend, ollama
- Ollama model auto-pull on first startup (entrypoint script)
- Write .env.example with all required + optional variables documented
- Add Docker volume mounts for SQLite DB and FAISS vectorstore persistence
- Add CORS configuration for same-origin production
- Test: docker-compose up on clean machine → all services healthy
- Test: docker-compose down && up → data persists

---

### Phase 6: Testing & Hardening
**Goal:** All 86 test cases pass. Security hardened (rate limiting, secret audit). Performance verified.

**Requirements:** NFR-SEC-01, NFR-PERF-01, NFR-ISO-01

**Plans:**
- [x] 06-01-PLAN.md — conftest.py, test_auth.py (86 TCs), test_netsuite.py, slowapi rate limits, security audit

Tasks:
- Build license server FastAPI app (TechCloudPro AWS)
- License model: key, customer_id, tier, expiry, active
- POST /api/license/validate endpoint (receives: license_key, version, account_id — returns: valid, expiry, tier)
- Add license check to ArthaBuild backend startup
- Implement 72-hour grace period with cached validation
- Implement demo mode (read-only, no NetSuite connection) when license invalid
- Add license banner in frontend when in demo mode
- Deploy license server to TechCloudPro AWS (separate ECS service)

---

### Phase 7: License System
**Goal:** Deployments validate license key on startup. 7-day offline cache. 72-hour grace period. No customer data sent to license server.

**Status: COMPLETE — 2026-04-10**

**Requirements:** FR-LICENSE-01

**Plans:** 1/1 plans executed — PHASE COMPLETE
- [x] 07-01-PLAN.md — LicenseCache + ScriptDeployment models, Alembic migration, license router (validate/quota/record), license-server with instance-lock, LicenseBanner UI, PrivacyPolicy, TermsOfService, enterprise landing (zero pricing, Get a Demo CTAs), NetSuite auto-index, customer-index RAG priority, ARCHITECTURE.md v1.7 (DONE 2026-04-10 — 59/59 tests)

Tasks:
- Write Terraform module: EC2 instance (g4dn.xlarge), security group, EBS volume, Elastic IP
- User_data script: installs Docker, pulls ArthaBuild compose file, starts services
- Variables: aws_region, vpc_id, subnet_id, instance_type, license_key
- Outputs: public_ip, ssh_instructions, arthaBuild_url
- Test: terraform apply on fresh AWS account → ArthaBuild accessible
- Write customer deployment guide (README)

### Phase 07.1: Frontend Section Verification (INSERTED)

**Goal:** Verify all new frontend sections added in Phase 7 work correctly end-to-end. LicenseBanner wired into Chat layout, all routes render, build passes, 59/59 tests confirmed.
**Depends on:** Phase 7
**Requirements:** NFR-UI-01
**Status: COMPLETE — 2026-04-10**
**Plans:** 1/1 plans executed — PHASE COMPLETE

Plans:
- [x] 07.1-01-PLAN.md — Wire LicenseBanner into Chat.tsx, verify build + routes + components, run 59/59 tests, update test-report.html (DONE 2026-04-10 — 59/59 tests)

---

### Phase 9: RBAC, Team Management and Chat Persistence

**Goal:** Users see only their own chats (persisted to DB). Admins see all team chats. Admin role can add team members. Chat history survives server restarts and works across devices. Reuse Dollor.ai auth_utils patterns for RBAC. PLUS: full user dashboard + full admin panel UI.

**Depends on:** Phase 7.1
**Requirements:** RBAC-01, CHAT-01, TEAM-01
**Plans:** 3/3 plans complete

What gets built:
- `role` field on User model (admin/user enum), `team_id` FK, `ChatSession` + `ChatMessage` DB tables — Alembic migration
- RBAC deps: `require_admin()`, `require_user()` — port from Dollor.ai auth_utils pattern
- Role added to JWT claims, jti token blacklist for logout
- Backend: POST /api/chats, GET /api/chats, GET /api/chats/{id}/messages — persists to SQLite
- Backend: /api/chatbot/process saves messages to DB after AI response
- Backend: Admin endpoints — GET /api/admin/chats (all team), POST /api/admin/team/invite
- Frontend: chatService.ts localStorage → real API calls, Dashboard page, AdminPanel
- First-user-is-admin bootstrapping, default team creation

Plans:
- [x] 09-01-PLAN.md — Alembic migration (Team/ChatSession/ChatMessage/TeamInvite + role/team_id on User), auth_utils RBAC Depends, chats router, admin router, rawapi DB persistence, 80+ tests (DONE 2026-04-10)
- [x] 09-02-PLAN.md — authService role bug fix, User type update, chatService localStorage→API, useChat async, Chat.tsx chat_session_id, Dashboard page, /dashboard route, Sidebar admin link (DONE 2026-04-10)
- [x] 09-03-PLAN.md — AdminPanel.tsx (team members + invite + all team chats), /admin route with AdminProtected, ARCHITECTURE.md v1.8, test-report.html Phase 9 rows (completed 2026-04-10)

---

### Phase 10: Admin Panel — Enterprise Team Management UI

**Goal:** Admin has a dedicated panel (like BrandMonkz admin) where they can: view all team members, invite new members, see team chat activity, manage roles, remove members. Enterprise-level UI — not just an API, a full polished interface.

**Depends on:** Phase 9
**Requirements:** CASE-173, CASE-174, CASE-175, CASE-176, CASE-177, CASE-178, CASE-179, CASE-180
**Status: COMPLETE** — 3/3 plans executed

What was built:
- AuditLog + SystemConfig DB models (Alembic migration b3c4d5e6f7a8)
- 8 new backend endpoints: stats, users, role change, soft-delete, audit, config, license, teams
- POST /api/user/accept-invite — invite token → new user created under correct team
- AdminPanel.tsx extended from 3 tabs to 5 tabs (Stats + Audit Log added, Promote button, deleteUser)
- AcceptInvite.tsx — public page at /accept-invite?token=xxx
- adminService.ts extended to 9 functions (getStats, listUsers, changeRole, deleteUser, getAuditLog)
- ARCHITECTURE.md v1.9 — Phase 10 section 11.1–11.6 complete
- test-report.html — 13 Phase 10 Plan 03 acceptance checks all PASS

Plans:
- [x] 10-01-PLAN.md — AuditLog + SystemConfig models, Alembic migration, 8 new admin endpoints in routers/admin.py
- [x] 10-02-PLAN.md — POST /api/user/accept-invite endpoint + AcceptInvite.tsx page + /accept-invite public route
- [x] 10-03-PLAN.md — adminService.ts 5 new functions, AdminPanel.tsx 5-tab extension, ARCHITECTURE.md v1.9, test-report.html Phase 10 rows

---

### Phase 11: Password Management — Enterprise Email Flow

**Goal:** Password forgot/reset flow works reliably with professional email templates, proper token expiry UX, and enterprise-grade feedback. Backend already has endpoints (auth.py:82-161) and frontend pages exist — this phase upgrades the email templates, improves UX flows, and ensures it matches BrandMonkz enterprise quality.

**Depends on:** Phase 10
**Plans:** 3/3 plans complete

What gets built:
- Professional HTML email template for password reset (branded, not plain-text)
- Improved error states: clear messages for expired links, already-used tokens
- Email verification on signup (optional — confirm email before first login)
- Rate limiting on forgot-password (already exists, verify it holds under load)
- Admin can reset any team member's password from the admin panel
- Frontend UX: loading states, countdown timer on "check your email" page

Plans:
- [x] 11-01-PLAN.md — Backend: HTML email templates, token expiry fix (15min), EmailVerificationToken model + migration, new user endpoints (change-password/verify-email/resend/me), admin send-reset, auth_utils enforcement, pytest CASE-181/184/185/186/187 (DONE 2026-04-11 — 96/96 tests)
- [x] 11-02-PLAN.md — Frontend: ForgotPassword check-email state, ResetFailed inline resend (60s cooldown), ResetPassword policy validation, Profile change-password form, EmailVerificationBanner, Chat.tsx wiring, AdminPanel send-reset button (DONE 2026-04-11 — build clean, 9 files)
- [x] 11-03-PLAN.md — Integration + docs: mark CASEs DONE/DEFERRED (5 DONE, 2 DEFERRED), architecture-diagram.html Phase 11 Frontend Components section, test-report.html CASE-182/183 DEFERRED rows (DONE 2026-04-10)

---

### Phase 12: Security Hardening and SOC2 Readiness

**Goal:** Platform is hardened to enterprise security standards. Key SOC2 controls documented and implemented: audit logging, access control, data encryption at rest, session management, incident response runbook. Security review passes with zero critical findings.

**Depends on:** Phase 11
**Plans:** 3/3 plans complete

What gets built:
- Audit log expansion: 15 event types (auth + user + admin), write_audit_event() shared helper, Alembic migration d5e6f7a8b9ca
- HTTP security headers (HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, CSP-Report-Only) in nginx.prod.conf
- TLS 1.2/1.3 only — Mozilla Intermediate cipher profile — disabled SSLv3/TLS1.0/TLS1.1
- HTTPS redirect (port 80 → 443) in nginx.prod.conf (dev nginx.conf unchanged)
- CORS: ALLOWED_ORIGINS env var, comma-separated, no wildcard
- EBS encryption at rest: encrypted = true in infra/terraform/main.tf
- 4 SOC2 documents: SECURITY_CONTROLS.md, INCIDENT_RESPONSE.md, DATA_CLASSIFICATION.md, DEPLOYMENT_SECURITY.md
- SECURITY.md at repo root (GitHub vulnerability disclosure convention)
- pip-audit + OWASP ZAP scan documented
- 19 static analysis + async security tests
- ARCHITECTURE.md v2.1 + test-report.html CASE-188 through CASE-195

Plans:
- [x] 12-01-PLAN.md — audit_utils.py shared helper, AuditLog model expansion, Alembic migration d5e6f7a8b9ca, auth/user/admin router hooks (15 events), GET /api/admin/audit pagination, 5 security tests
- [x] 12-02-PLAN.md — nginx.prod.conf (HTTPS redirect + TLS + security headers), CORS tightening, Terraform EBS encryption, 14 static analysis tests — COMPLETE 2026-04-10 (110/110 tests pass)
- [x] 12-03-PLAN.md — pip-audit + ZAP scan report, 5 SOC2 docs, SECURITY.md, ARCHITECTURE.md v2.1, test-report.html Phase 12 rows (completed 2026-04-11)

---

### Phase 8: Launch Readiness *(FINAL — execute last)*

**Goal:** All test cases pass. Customer deployment guide complete. Smoke tests pass. v1.0.0 tagged. Architecture diagrams final. CHANGELOG complete.

**Depends on:** Phase 12
**Requirements:** NFR-SEC-01 through NFR-ISO-04
**Status: COMPLETE — 2026-04-10**

**Plans:** 1/1 plans complete
- [x] 08-01-PLAN.md — benchmark.sh, smoke_test.sh (10 checks), CUSTOMER_DEPLOYMENT.md (641 lines), TROUBLESHOOTING.md (10 issues), CHANGELOG.md, test-report.html/architecture-diagram.html updated, security audit clean (0 critical findings), v1.0.0 git tag created (DONE 2026-04-10)

**Milestone achieved: ArthaBuild v1.0.0 — all 13 phases complete. First customer ready.**

### Phase 08.1: Pre-Staging Case Resolution bug fixes backend tests static analysis (INSERTED)

**Goal:** Resolve all 90 non-passing cases before staging: 21 bug fixes, 34 new tests, static analysis, docs update.
**Depends on:** Phase 8
**Status: COMPLETE — 2026-04-11**

**Plans:** 3/3 plans executed — PHASE COMPLETE
- [x] 08.1-01-PLAN.md — 21 bug fixes (Vite port, NOCASE email, health split, persist_chat return, license env vars, CORS_EXTRA_ORIGINS, dead code removal) (DONE 2026-04-11)
- [x] 08.1-02-PLAN.md — 34 new pytest tests: test_user/auth/migrations/netsuite/chats/rbac/license/infrastructure/security files — 149/150 passing (DONE 2026-04-11)
- [x] 08.1-03-PLAN.md — Static analysis, ROADMAP fix, 90 case files updated (44 DONE, 23 PASS, 21 DEFERRED), ARCHITECTURE.md v2.2, architecture-diagram.html v2.2, test-report.html updated (DONE 2026-04-11)

---

## Milestone 2: v2.0 — Enterprise Ready

**Goal:** ArthaBuild is safe to sell to Fortune-500 procurement. It passes SSO/SAML integration, GDPR data requests, SOC2 Type I audit evidence, production backup/DR drills, versioned API contracts, and guided onboarding — all without external cloud dependencies.

**Depends on:** Milestone 1 complete (v1.0.0 tagged at d3cfca6b)

**Execution order:** 13 → 14 → 15 → 16 → 17 → (M2 launch readiness)

---

### Phase 13: Identity & Access Management

**Goal:** Enterprise teams can authenticate via their corporate IdP (SAML 2.0 / OIDC SSO). Individual users can enable MFA/TOTP. Idle sessions expire after a configurable timeout. Admin can restrict login to approved IP ranges.

**Depends on:** Phase 12 (security hardening + SOC2 readiness)
**Requirements:** IAM-01 (SSO/SAML), IAM-02 (MFA/TOTP), IAM-03 (idle timeout), IAM-04 (IP allowlist)

**Status:** ● Complete (2026-04-13) — MFA login gate + SSO test-connection deferred to future phase by owner decision
**Plans:** 1/1 plans complete
- [x] 13-01-PLAN.md — SAML/OIDC SSO router, pyotp TOTP endpoints, session idle timeout middleware, IP allowlist config + admin UI (2026-04-13, commits 9b37581e 97c21b9f c0502ae5)

---

### Phase 14: Compliance & Data Governance

**Goal:** Users can export or erase all their personal data on request (GDPR Art. 15/17). Audit log entries are immutable and exportable as CSV. SOC2 evidence package is generated automatically from existing controls.

**Depends on:** Phase 13
**Requirements:** GDPR-01 (data export), GDPR-02 (right to erasure), AUDIT-01 (immutability), SOC2-01 (evidence package)

**Plans:** 1/1 plans complete — PHASE COMPLETE (2026-04-13)
- [x] 14-01-PLAN.md — /api/user/export-data, /api/user/erase endpoint, audit log hash-chaining for immutability, audit CSV export endpoint, SOC2 evidence generator script (2026-04-13, commits c9b8fb1b 808ad18f)

---

### Phase 15: Operations & Reliability

**Goal:** Automated daily SQLite backup to S3. Sentry error monitoring wired to all unhandled exceptions. Graceful shutdown drains in-flight requests before SIGTERM. /health/detail returns real dependency status (Ollama, DB, license, disk).

**Depends on:** Phase 14
**Requirements:** OPS-01 (backup/DR), OPS-02 (error monitoring), OPS-03 (graceful shutdown), OPS-04 (health depth)

**Plans:** 1/1 plans complete — PHASE COMPLETE (2026-04-13)
- [x] 15-01-PLAN.md — backup.sh cron script (SQLite → S3), Sentry SDK wired in rawapi.py, SIGTERM handler, /health/detail extended with disk/latency metrics (2026-04-13, commits a9d34c23 8abfa5b3)

---

### Phase 16: API Platform

**Goal:** Third-party integrations can authenticate with API keys (not user JWTs). All endpoints are served under /api/v1/ with backward-compatible versioning. Webhook delivery notifies external systems on key events (chat completed, script deployed). All responses follow a standard envelope: {data, error, meta}.

**Depends on:** Phase 15
**Requirements:** API-01 (API key auth), API-02 (versioning), API-03 (webhooks), API-04 (response standard)

**Plans:** 1/1 plans complete
- [x] 16-01-PLAN.md — APIKey model + Alembic migration, /api/v1/ router prefix, webhook delivery worker, response envelope middleware

---

### Phase 17: Onboarding UX

**Goal:** A new admin sees a first-run wizard on first login (connect NetSuite → invite team → verify license). License key can be entered and validated from the UI (no .env editing). In-app notifications surface warnings (license expiry, Ollama down, disk full). Empty states guide users when no chats/scripts exist yet.

**Depends on:** Phase 16
**Requirements:** UX-01 (first-run wizard), UX-02 (license UI), UX-03 (in-app notifications), UX-04 (empty states)

**Plans:** 1/1 plans complete
- [x] 17-01-PLAN.md — OnboardingWizard.tsx (3-step modal), LicenseSettings.tsx admin tab, NotificationBanner.tsx (poll /health/detail), EmptyState.tsx component for Chat/History — **DONE** 2026-04-14 (commits d300d9ba, 4f7a65a5)

---

### Phase 18: Security Hardening + Cloudflare Analytics — PHASE COMPLETE (2026-04-14)

**Goal:** Full Cloudflare integration for security and traffic visibility — matching TechCloudPro standards. WAF rules block common attacks. DDoS protection active at edge. Traffic analytics dashboard shows pageviews, unique visitors, geographic breakdown, top pages, and bot traffic. All HTTP is force-redirected to HTTPS at the Cloudflare layer (not just nginx).

**Depends on:** Phase 17
**Requirements:**
- SEC-CF-01: Cloudflare proxy enabled for artha.build (orange-cloud DNS)
- SEC-CF-02: WAF ruleset active (OWASP Core Rule Set + Cloudflare Managed Rules)
- SEC-CF-03: DDoS protection L3/L4/L7 (Cloudflare default under Free/Pro plan)
- SEC-CF-04: Cloudflare Analytics dashboard wired — pageviews, unique IPs, country breakdown, top paths, bot score
- SEC-CF-05: Cloudflare Workers rate-limiter at edge (backs up SlowAPI in-app limiter)
- SEC-CF-06: Security headers enforced via Cloudflare Transform Rules (CSP, HSTS, X-Frame-Options)
- SEC-CF-07: Page Rules: force HTTPS, cache static assets (JS/CSS/images), bypass cache for /api/*
- SEC-CF-08: Email obfuscation + hotlink protection enabled
- SEC-CF-09: SSL/TLS mode = Full (Strict) — Cloudflare ↔ origin uses verified cert

**Plans:** 2/2 plans complete

Plans:
- [x] 18-01-PLAN.md — DNS proxy, SSL Full Strict, zone settings, HTTPS redirect rule, cache rules (SEC-CF-01, SEC-CF-03, SEC-CF-07, SEC-CF-08, SEC-CF-09) (DONE 2026-04-14)
- [x] 18-02-PLAN.md — WAF managed rules, security headers Transform Rules, WAF rate limiting, analytics verification, ARCHITECTURE.md v2.8 (SEC-CF-02, SEC-CF-04, SEC-CF-05, SEC-CF-06) (DONE 2026-04-14)

**Note:** This phase is infrastructure/DNS config — no backend code changes required. Parallels exactly what was done for TechCloudPro (techcloudpro.com Cloudflare setup).
