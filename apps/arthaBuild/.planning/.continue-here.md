---
phase: ad-hoc-bug-fixes-and-staging-setup
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-04-15T02:16:44.595Z
---

<current_state>
Paused mid-session on ArthaBuild ONLY (user explicitly restricted scope). Two bug-fix deploys
completed and live on EC2 44.194.34.223 (artha.build). Staging environment setup is 80% done —
nginx.conf and deploy script are written, but the Cloudflare origin certificate has NOT been
provided by the user yet. The activate-staging.sh script is ready to run the moment the user
pastes the cert.

Production URL: https://artha.build (= EC2 44.194.34.223 = the ONLY server — no staging yet)
Staging URL target: https://staging.artha.build (same EC2, same backend — waiting on CF cert)
</current_state>

<completed_work>

## Deploy 1 — OAuth + Route Bugs (bundle index-BfNEGEtw.js)
- **Root cause found and fixed**: `Chat.tsx`, `Guide.tsx`, `History.tsx` all called `useChat()`
  in component body BUT `ChatProvider` only exists INSIDE `ChatLayout` (which those components
  render as children). `useChat()` always threw "must be used inside ChatProvider" → blank page.
  Fix: extracted inner components (`ChatInner`, `GuideInner`, `HistoryInner`) rendered inside
  ChatLayout so they're children of ChatProvider.
- **Files changed**: `Chat.tsx`, `Guide.tsx`, `History.tsx`
- **Also fixed in prior deploy (index-CWXjNCKO.js)**:
  - `OAuthCallback.tsx`: removed broken `useAuth().login(user)` call (was sending wrong
    payload to /api/auth/login — causing silent 422)
  - `auth.py` google_callback: added `user.is_verified = True` for existing OAuth users
  - `SignUpSuccess.tsx`: `nav("/auth")` → `nav("/log-in")` (broken redirect)
  - `AdminPanel.tsx`: `PUT /api/admin/system-config` → `PUT /api/admin/config`

## Deploy 2 — Build timestamp indicator (bundle index-Dy_fVqK9.js)
- `vite.config.ts`: added `define: { __BUILD_TS__: JSON.stringify(new Date().toISOString()) }`
- `env.d.ts`: added `declare const __BUILD_TS__: string`
- `Auth.tsx`: small badge bottom-right of login page showing deploy timestamp
  (e.g. "artha.build · Apr 14, 3:45 PM EDT") — answers "how do I know what's deployed"
- This DEPLOYED and LIVE. Each new build auto-stamps itself.

## Staging Config Prepared (NOT yet deployed — waiting on CF cert)
- `nginx/nginx.conf`: rewritten with TWO server blocks (artha.build + staging.artha.build).
  Both use `/etc/nginx/ssl/origin.crt` + `/etc/nginx/ssl/origin.key` (CF wildcard origin cert).
  Also adds `staging.artha.build` to port 80 redirect block.
- `scripts/activate-staging.sh`: one-shot deploy script that SCPs cert, uploads nginx.conf,
  updates ALLOWED_ORIGINS in .env, force-recreates nginx container.
- `nginx/ssl/` directory: exists locally, empty — waiting for cert files.
</completed_work>

<remaining_work>

## Task 3 — Complete staging activation (BLOCKED on user action)
User needs to do 2 things in Cloudflare dashboard (artha.build zone):

**Step 1 — DNS record** (DNS tab):
```
Type: A
Name: staging
IPv4: 44.194.34.223
Proxy: ON (orange cloud)
```

**Step 2 — Origin certificate** (SSL/TLS → Origin Server → Create Certificate):
- Hostnames: `*.artha.build, artha.build`
- Validity: 15 years
- Format: PEM
- Download → copy Certificate text → save as `nginx/ssl/origin.crt`
- Copy Private Key text → save as `nginx/ssl/origin.key`

**Then run:**
```bash
cd /Users/jeet/doordash-p2p/apps/arthaBuild
bash scripts/activate-staging.sh
```

**After staging is live, also needed:**
- Add `https://staging.artha.build/api/auth/google/callback` to Google Cloud Console as an
  authorized OAuth redirect URI (so Google OAuth works on staging)
- Update backend `.env` on EC2: `APP_BASE_URL` stays as `artha.build` for prod, but staging
  users who hit OAuth will be redirected to artha.build OAuth callback (acceptable for now)

## Future work (not this session)
- Spin up NEW production EC2 when ready
- Point `artha.build` DNS → new prod EC2
- `staging.artha.build` stays on current EC2 (44.194.34.223)
- That completes the staging/prod split
</remaining_work>

<decisions_made>

- **Cloudflare origin cert over Let's Encrypt expansion**: Existing cert only covers
  `artha.build, www.artha.build`. Expanding via certbot HTTP-01 challenge is blocked by CF
  "Always Use HTTPS" redirect at edge. Origin cert covers `*.artha.build` wildcard, valid 15
  years, no renewal, works perfectly with CF proxy (Full Strict mode). Cleaner solution.

- **Same backend for staging + prod (for now)**: Both artha.build and staging.artha.build
  proxy to the same backend:8000. They share the same DB and data. Acceptable for initial
  staging setup. Only nginx routing differs. Future: separate docker-compose per environment.

- **ChatProvider bug fix approach**: Extracted inner components (ChatInner, GuideInner,
  HistoryInner) rather than moving ChatProvider up the tree. Minimal change, preserves all
  existing component interfaces.

- **ArthaBuild ONLY this session**: User explicitly restricted scope — do NOT touch
  BrandMonkz, Dollor.ai, TechCloudPro, LaunchOS (Phase 22), or any other app.

- **Current branch**: `gsd/phase-22-launchos-smb-platform` — ArthaBuild work is committed
  to this branch (it was the active branch). Not ideal but acceptable.
</decisions_made>

<blockers>

- **Staging cert BLOCKED on user**: Cloudflare origin cert must be generated manually in CF
  dashboard. Cannot be done programmatically without CF API token. User has not yet done this.

- **Google OAuth on staging**: Will redirect to artha.build callback URL (since APP_BASE_URL
  is artha.build). OAuth won't complete on staging.artha.build until Google Cloud Console gets
  the staging redirect URI added. Low priority for now.
</blockers>

<context>
ArthaBuild has NO staging environment — it has always been deploy-directly-to-production.
The staging plan was deferred in Phase 08.1 decision AB-081-006 (needed user to provide
registrar + staging host details). Those details are now known:
- Domain: GoDaddy registrar, Cloudflare DNS
- Staging host: SAME EC2 as production (44.194.34.223), different subdomain

The staging/prod split plan:
- NOW: staging.artha.build = current EC2 (testing ground)
- LATER: artha.build = new EC2 (real production), staging.artha.build stays on current EC2

The three bug fixes (Chat/Guide/History blank pages) are all deployed and live. The
useChat()/ChatProvider structural bug was the root cause of ALL blank pages in the app —
not just OAuth. Every logged-in page was broken. Now fixed.

Build timestamp on login page gives instant visual confirmation of what's deployed.
Every deploy changes the bundle hash AND the timestamp badge.
</context>

<next_action>
Resume with: "Has the user added the Cloudflare DNS record and origin certificate?"
- If YES: run `bash apps/arthaBuild/scripts/activate-staging.sh` then verify with
  `curl -I https://staging.artha.build/`
- If NO: remind user of the 2 Cloudflare steps (DNS A record + origin cert download)
  and wait. Do not proceed without the cert — it would break production nginx.

Files ready on local machine (NOT yet on EC2):
- `apps/arthaBuild/nginx/nginx.conf` (updated with staging block)
- `apps/arthaBuild/scripts/activate-staging.sh` (deploy automation)
- `apps/arthaBuild/nginx/ssl/` (empty — needs origin.crt + origin.key)
</next_action>
