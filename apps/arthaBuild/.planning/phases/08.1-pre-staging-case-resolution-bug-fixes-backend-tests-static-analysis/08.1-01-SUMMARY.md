# Phase 08.1 — Plan 01 SUMMARY: Bug Fixes

**Status:** COMPLETE
**Date:** 2026-04-11
**Tests after plan:** 116 passed (baseline — all pre-existing tests green, no regressions)

---

## What Was Built

21 confirmed code bugs fixed. All changes minimal and targeted. No test regressions.

### Wave 1 — Dead Code Removal

| Case | Action | Evidence |
|------|--------|----------|
| CASE-002 / CASE-003 | Deleted `src/backend/finetunedmodelrun.py` — OpenAI-dependent, never imported | File deleted |
| CASE-004 / CASE-017 | Deleted `src/backend/finetunedmodelrunv2.py` — dead prototype, unused `re` import | File deleted |
| CASE-016 | Removed `if __name__ == "__main__":` dev stub from `suitescripts_utils.py` | `grep -n "__main__"` → empty |
| CASE-015 | Removed commented TBA credential placeholders from `sdf_utils.py` | `grep -n "accountId\|tokenKey"` → only active code |

### Wave 2 — Vite Proxy Port

| Case | Action | Evidence |
|------|--------|----------|
| CASE-001 | `src/frontend/vite.config.ts:17`: `localhost:8080` → `localhost:8000` | `grep "target" vite.config.ts` → `8000` |

### Wave 3 — Auth & Response Fixes

| Case | Action | Evidence |
|------|--------|----------|
| CASE-005 | `routers/auth.py`: `user_type` hardcoded `"Administrator"` → dynamic from `user.role` | `grep "user_type" routers/auth.py` |
| CASE-024 | Refresh token response now includes `role` field — matches login response | `grep -A20 "def refresh"` → `role` present |
| CASE-006 | `models.py`: `User.email` column — added `collation='NOCASE'`. Alembic migration `e1f2g3h4i5j6` | `alembic upgrade head` → exit 0 |

### Wave 4 — rawapi.py Cleanup

| Case | Action | Evidence |
|------|--------|----------|
| CASE-010 | Removed dead `latest_javascript_code = ""` global variable | `grep "latest_javascript_code" rawapi.py` → empty |
| CASE-011 | Removed unused `time_now` variable | `grep "time_now" rawapi.py` → empty |
| CASE-020 | Replaced bare `except:` with `except Exception as e:` (2 instances) | `grep "except:" rawapi.py` → empty |
| CASE-021 | Replaced bare `except:` with `except BaseException as e:` in SuiteCloud block | `grep "except:" rawapi.py` → empty |
| CASE-025 | Replaced legacy `print()` statements with `logger.warning()` | `grep "^print(" rawapi.py` → empty |
| CASE-026 | Removed dev-only commented-out code blocks | |

### Wave 5 — License System Env Vars

| Case | Action | Evidence |
|------|--------|----------|
| CASE-033 | `routers/license.py`: `LICENSE_SERVER_URL` hardcoded fallback removed — required env var with warning if unset | `grep "LICENSE_SERVER_URL" routers/license.py` |
| CASE-034 | `GRACE_PERIOD_HOURS` → `int(os.getenv("GRACE_PERIOD_HOURS", "72"))` | `grep "GRACE_PERIOD_HOURS" routers/license.py` |
| CASE-035 | `CACHE_TTL_DAYS` → `int(os.getenv("CACHE_TTL_DAYS", "7"))` | `grep "CACHE_TTL_DAYS" routers/license.py` |

### Wave 6 — Health + Chat Persistence

| Case | Action | Evidence |
|------|--------|----------|
| CASE-031 | `/health` split: public (`status:ok` only) vs `/health/detail` (Depends(require_user) + ai_ready/suitecloud_ready/license_valid) | `rawapi.py:/health` and `/health/detail` |
| CASE-032 | `_persist_chat_to_db()` returns `True`/`False`. All 4 callers check return and set `persistence_warning` on `False` | `rawapi.py:_persist_chat_to_db()` |

### Wave 7 — CORS + Startup

| Case | Action | Evidence |
|------|--------|----------|
| CASE-036 | `rawapi.py`: `FRONTEND_BASE_URL` absence → `logger.warning()` at startup (not crash) | `grep "FRONTEND_BASE_URL" rawapi.py` |
| CASE-040 | `CORS_EXTRA_ORIGINS` env var support — appends to `_allowed_origins` list | `grep "CORS_EXTRA_ORIGINS" rawapi.py` |

### Deferred

| Case | Reason |
|------|--------|
| CASE-018 | OLLAMA_BASE_URL duplication between rawapi.py and model_utils.py — LOW severity, no functional impact. Both read same env var. |
| CASE-019 | Same root cause as CASE-018. |

---

## Verification

- **Baseline tests:** 116 passed, 5 skipped (ordering), 0 failing — no regressions
- **Key grep proofs:**
  - `grep "target" src/frontend/vite.config.ts` → `localhost:8000`
  - `grep "NOCASE" src/backend/models.py` → User.email column
  - `grep "GRACE_PERIOD_HOURS\|CACHE_TTL_DAYS" src/backend/routers/license.py` → env var reads
  - `grep "persist_chat_to_db" src/backend/rawapi.py` → return True/False + 4 callers
  - `grep "FRONTEND_BASE_URL" src/backend/rawapi.py` → startup warning
  - `grep "CORS_EXTRA_ORIGINS" src/backend/rawapi.py` → extra origins merge

---

## Decisions Made

- CASE-018/019 DEFERRED: OLLAMA_BASE_URL duplication is LOW severity — no functional impact since both locations read the same env var. Refactoring would add complexity for zero customer-facing benefit.
- CASE-006 implemented as `collation='NOCASE'` directly in the Column definition (SQLAlchemy standard pattern for SQLite). Alembic migration uses `batch_alter_table` (required for SQLite column changes).
