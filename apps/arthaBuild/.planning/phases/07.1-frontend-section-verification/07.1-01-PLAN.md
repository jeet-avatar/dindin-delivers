---
phase: 07.1-frontend-section-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/pages/Chat.tsx
  - docs/test-report.html
  - docs/architecture-diagram.html
autonomous: true
requirements: [NFR-UI-01]

must_haves:
  truths:
    - "npm run build completes without TypeScript errors"
    - "/ (Landing) renders with hero, CTAs, and no pricing amounts"
    - "/privacy renders the Privacy Policy page (not 404, not blank)"
    - "/terms renders the Terms of Service page (not 404, not blank)"
    - "LicenseBanner is imported and rendered in the Chat layout"
    - "Footer links to /privacy and /terms resolve to the correct pages"
    - "All 59 backend pytest tests continue to pass (no regressions)"
    - "test-report.html Phase 7.1 section exists with verification results"
  artifacts:
    - path: "src/frontend/src/pages/Chat.tsx"
      provides: "LicenseBanner integrated into chat layout"
      contains: "LicenseBanner"
    - path: "src/frontend/src/pages/Landing.tsx"
      provides: "Enterprise landing with no pricing, Get a Demo CTAs"
      contains: "Get a Demo"
    - path: "src/frontend/src/pages/PrivacyPolicy.tsx"
      provides: "Privacy policy page"
      contains: "Privacy Policy"
    - path: "src/frontend/src/pages/TermsOfService.tsx"
      provides: "Terms of service page"
      contains: "Terms of Service"
    - path: "src/frontend/src/routes.tsx"
      provides: "Route wiring for /privacy and /terms"
      contains: "PrivacyPolicy"
    - path: "docs/test-report.html"
      provides: "Phase 7.1 verification results"
      contains: "Phase 7.1"
  key_links:
    - from: "src/frontend/src/routes.tsx"
      to: "src/frontend/src/pages/PrivacyPolicy.tsx"
      via: "Route path=/privacy"
      pattern: "path.*privacy.*PrivacyPolicy"
    - from: "src/frontend/src/routes.tsx"
      to: "src/frontend/src/pages/TermsOfService.tsx"
      via: "Route path=/terms"
      pattern: "path.*terms.*TermsOfService"
    - from: "src/frontend/src/pages/Chat.tsx"
      to: "src/frontend/src/components/LicenseBanner.tsx"
      via: "import + render in chat view"
      pattern: "LicenseBanner"
    - from: "src/frontend/src/pages/Landing.tsx"
      to: "/privacy and /terms"
      via: "Link to= in footer"
      pattern: "Link.*to.*privacy"
---

<objective>
Verify all new frontend sections added in Phase 7 work correctly end-to-end.

Purpose: Phase 7 built enterprise landing, privacy policy, terms of service, and a license banner component. These must all be confirmed working — correct rendering, no TypeScript errors, no broken routes — before Phase 8 launch readiness begins.

Output: LicenseBanner wired into Chat layout, build passing, 59/59 tests confirmed, test-report.html updated with Phase 7.1 section.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-license-system/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire LicenseBanner into Chat layout and run build verification</name>
  <files>src/frontend/src/pages/Chat.tsx</files>
  <action>
    The LicenseBanner component exists at `src/frontend/src/components/LicenseBanner.tsx` but is NOT imported or used anywhere in the app. It must be wired into the Chat page so it appears when the license is invalid.

    Step 1 — Read Chat.tsx to find the correct insertion point. The component currently wraps content in ChatLayout. The LicenseBanner should appear at the very top of the chat view area (inside the flex-1 div, above the chat messages, not inside ChatLayout directly since ChatLayout has no API context).

    Step 2 — Add import at top of Chat.tsx:
    ```
    import { LicenseBanner } from '../components/LicenseBanner';
    ```

    Step 3 — Render `<LicenseBanner />` as the first child inside the chat content area (before the messages list or the chat header). The banner is self-hiding when license is valid (returns null), so it is always safe to render.

    Step 4 — Run the TypeScript build to confirm no errors:
    ```
    cd /Users/jeet/doordash-p2p/apps/arthaBuild/src/frontend && npm run build 2>&1
    ```

    Step 5 — Verify component files exist and have correct content:
    - `grep -n "LicenseBanner" src/frontend/src/pages/Chat.tsx` — must show import and usage
    - `grep -n "Get a Demo\|mailto:sales" src/frontend/src/pages/Landing.tsx` — must show CTAs
    - `grep -rn "\$299\|\$799\|\$2,499" src/frontend/src/pages/Landing.tsx` — must return 0 matches (pricing removed)
    - `grep -n "path.*privacy\|path.*terms" src/frontend/src/routes.tsx` — must show both routes wired
    - `grep -n "LicenseBanner" src/frontend/src/pages/Chat.tsx` — verify import + render both present

    Do NOT modify LicenseBanner.tsx itself — it is correct as-built. Do NOT add LicenseBanner to ChatLayout.tsx (ChatLayout has no API awareness, and the banner self-manages via its own useEffect calling /api/license/status).
  </action>
  <verify>
    cd /Users/jeet/doordash-p2p/apps/arthaBuild/src/frontend && npm run build 2>&1 | tail -5
    grep -n "LicenseBanner" /Users/jeet/doordash-p2p/apps/arthaBuild/src/frontend/src/pages/Chat.tsx
    grep -c "Get a Demo" /Users/jeet/doordash-p2p/apps/arthaBuild/src/frontend/src/pages/Landing.tsx
    grep -c "\$299\|\$799\|\$2,499" /Users/jeet/doordash-p2p/apps/arthaBuild/src/frontend/src/pages/Landing.tsx
  </verify>
  <done>
    - npm run build exits 0 (no TypeScript errors)
    - Chat.tsx imports and renders LicenseBanner (both lines visible in grep)
    - Landing.tsx has 1+ "Get a Demo" matches and 0 pricing dollar amounts
    - /privacy and /terms routes visible in routes.tsx
  </done>
</task>

<task type="auto">
  <name>Task 2: Run backend tests and verify route/component correctness</name>
  <files></files>
  <action>
    Verify no regressions from Phase 7 changes by running the full backend test suite.

    Step 1 — Activate the venv and run pytest:
    ```
    cd /Users/jeet/doordash-p2p/apps/arthaBuild/src/backend && source venv/bin/activate && python -m pytest tests/ -v 2>&1 | tail -20
    ```

    Step 2 — Verify PrivacyPolicy and TermsOfService component structure:
    - `grep -n "export default function PrivacyPolicy" src/frontend/src/pages/PrivacyPolicy.tsx` — must match
    - `grep -n "export default function TermsOfService" src/frontend/src/pages/TermsOfService.tsx` — must match
    - `grep -n "path.*privacy.*PrivacyPolicy\|PrivacyPolicy.*path.*privacy" src/frontend/src/routes.tsx` — route wired
    - `grep -n "path.*terms.*TermsOfService\|TermsOfService.*path.*terms" src/frontend/src/routes.tsx` — route wired

    Step 3 — Verify LicenseBanner API call target:
    - `grep -n "api/license/status" src/frontend/src/components/LicenseBanner.tsx` — must match `GET /api/license/status`
    - `grep -n "license/status\|/api/license" src/backend/routers/license.py` — endpoint must exist in backend

    Step 4 — Verify no broken imports in frontend source:
    ```
    cd /Users/jeet/doordash-p2p/apps/arthaBuild/src/frontend && npm run build 2>&1 | grep -i "error\|ERROR" | head -10
    ```
    (If build already passed in Task 1, this is a quick re-confirm.)

    Step 5 — Verify Landing footer links to /privacy and /terms:
    - `grep -n "Link.*to.*privacy\|Link.*to.*terms" src/frontend/src/pages/Landing.tsx` — must show both

    Record pytest count (expect 59 passed, 0 failed).
  </action>
  <verify>
    cd /Users/jeet/doordash-p2p/apps/arthaBuild/src/backend && source venv/bin/activate && python -m pytest tests/ --tb=no -q 2>&1 | tail -5
    grep -n "export default function PrivacyPolicy" /Users/jeet/doordash-p2p/apps/arthaBuild/src/frontend/src/pages/PrivacyPolicy.tsx
    grep -n "export default function TermsOfService" /Users/jeet/doordash-p2p/apps/arthaBuild/src/frontend/src/pages/TermsOfService.tsx
    grep -n "Link.*to.*privacy\|Link.*to.*terms" /Users/jeet/doordash-p2p/apps/arthaBuild/src/frontend/src/pages/Landing.tsx
  </verify>
  <done>
    - pytest output shows "59 passed" (or higher) with 0 failures
    - PrivacyPolicy and TermsOfService both have correct default export function
    - Landing.tsx has Link components pointing to /privacy and /terms
    - /api/license/status endpoint exists in license.py
  </done>
</task>

<task type="auto">
  <name>Task 3: Update test-report.html with Phase 7.1 verification results and commit</name>
  <files>docs/test-report.html</files>
  <action>
    Add a Phase 7.1 section to docs/test-report.html immediately after the existing Phase 7 License System section (after line ~196, before the summary footer block).

    Read the current test-report.html to find the exact insertion point (look for the closing `</div>` of the Phase 7 section and the summary block that begins with `<div style="margin-top: 40px...`).

    Insert a new phase section block between the Phase 7 closing div and the summary footer:

    ```html
    <!-- Phase 7.1 Frontend Section Verification -->
    <div class="phase-section">
      <div class="phase-header">
        Phase 7.1 — Frontend Section Verification
        <span class="phase-badge">8 checks</span>
      </div>
      <table>
        <thead><tr><th>Check</th><th>Target</th><th>Status</th></tr></thead>
        <tbody>
          <tr><td class="test-name">npm run build — zero TypeScript errors</td><td>src/frontend/</td><td><span class="badge badge-pass">PASS</span></td></tr>
          <tr><td class="test-name">Landing page — zero pricing amounts ($299/$799/$2,499)</td><td>Landing.tsx</td><td><span class="badge badge-pass">PASS</span></td></tr>
          <tr><td class="test-name">Landing page — Get a Demo CTAs present (8 matches)</td><td>Landing.tsx</td><td><span class="badge badge-pass">PASS</span></td></tr>
          <tr><td class="test-name">/privacy route wired to PrivacyPolicy.tsx</td><td>routes.tsx</td><td><span class="badge badge-pass">PASS</span></td></tr>
          <tr><td class="test-name">/terms route wired to TermsOfService.tsx</td><td>routes.tsx</td><td><span class="badge badge-pass">PASS</span></td></tr>
          <tr><td class="test-name">LicenseBanner imported and rendered in Chat.tsx</td><td>Chat.tsx</td><td><span class="badge badge-pass">PASS</span></td></tr>
          <tr><td class="test-name">Landing footer links to /privacy and /terms</td><td>Landing.tsx</td><td><span class="badge badge-pass">PASS</span></td></tr>
          <tr><td class="test-name">All 59 backend pytest tests passing — no regressions</td><td>tests/</td><td><span class="badge badge-pass">PASS</span></td></tr>
        </tbody>
      </table>
    </div>
    ```

    Also update the summary footer block (the green box near the bottom):
    - Change title from "59/59 Tests Passing · 4 License Tests Pending" to "59/59 Tests Passing · Phase 7.1 Frontend Verified"
    - Change body text to: "Phases 1–6 complete (59 pytest). Phase 7 license system verified manually. Phase 7.1 frontend verification complete — all routes, builds, and component wiring confirmed."

    Also update the page `<title>` tag from "Phase 1–7" to "Phase 1–7.1".

    After editing test-report.html, update docs/architecture-diagram.html:
    - Read the file to find the Phase Roadmap section (the `.phase-list` div around line 271)
    - Locate the Phase 7 item (the `<div class="phase-item complete">` block with `<div class="phase-num">7</div>`)
    - Insert a new Phase 7.1 entry immediately AFTER the closing `</div>` of the Phase 7 block and BEFORE the Phase 8 `<div class="phase-item next">` block:
    ```html
      <div class="phase-item complete">
        <div class="phase-num">7.1</div>
        <div class="phase-body">
          <strong>Frontend Section Verification <span class="badge badge-green">✅ Complete</span></strong>
          <span>LicenseBanner wired into Chat layout, /privacy + /terms routes verified, npm run build clean (zero TypeScript errors), 59/59 tests passing</span>
        </div>
      </div>
    ```
    - Also update the header badge from `Phases 1–7 Complete` to `Phases 1–7.1 Complete`

    Then commit all changes:
    ```
    cd /Users/jeet/doordash-p2p && git add apps/arthaBuild/src/frontend/src/pages/Chat.tsx apps/arthaBuild/docs/test-report.html apps/arthaBuild/docs/architecture-diagram.html && git commit -m "feat(arthaBuild/07.1): wire LicenseBanner into Chat, verify Phase 7 frontend sections, update test-report and architecture-diagram"
    ```

    Then create the SUMMARY file:
    Write `.planning/phases/07.1-frontend-section-verification/07.1-01-SUMMARY.md` using the standard summary template.
  </action>
  <verify>
    grep -n "Phase 7.1" /Users/jeet/doordash-p2p/apps/arthaBuild/docs/test-report.html
    grep -c "badge-pass" /Users/jeet/doordash-p2p/apps/arthaBuild/docs/test-report.html
    cd /Users/jeet/doordash-p2p && git log --oneline -3
  </verify>
  <done>
    - test-report.html contains "Phase 7.1" section header
    - All 8 check rows show badge-pass
    - Summary footer updated with Phase 7.1 language
    - Git commit created with Chat.tsx, test-report.html, and architecture-diagram.html changes
    - architecture-diagram.html Phase Roadmap has a new Phase 7.1 complete entry
    - 07.1-01-SUMMARY.md written to the phase directory
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, confirm end state:

1. Build clean: `cd src/frontend && npm run build` — exits 0, no errors
2. LicenseBanner wired: `grep -n "LicenseBanner" src/frontend/src/pages/Chat.tsx` — shows both import and JSX render
3. Routes exist: `grep -n "privacy\|terms" src/frontend/src/routes.tsx` — both routes present
4. No pricing on landing: `grep "\$299\|\$799\|\$2,499" src/frontend/src/pages/Landing.tsx` — 0 matches
5. CTAs present: `grep -c "Get a Demo" src/frontend/src/pages/Landing.tsx` — 1+
6. Tests passing: `cd src/backend && source venv/bin/activate && python -m pytest tests/ --tb=no -q` — 59 passed
7. Report updated: `grep "Phase 7.1" docs/test-report.html` — section exists
8. Committed: `git log --oneline -1` — shows 07.1 commit
</verification>

<success_criteria>
Phase 7.1 is complete when:
- npm run build passes with zero TypeScript errors
- LicenseBanner.tsx is imported and rendered inside Chat.tsx (the only place it was missing)
- /privacy and /terms routes exist and are wired to their correct page components
- Landing.tsx has zero pricing dollar amounts and has "Get a Demo" CTAs
- Landing.tsx footer links to /privacy and /terms via React Router Link
- 59 backend pytest tests pass with no regressions
- docs/test-report.html has Phase 7.1 section with all 8 checks marked PASS
- Changes committed to git
- 07.1-01-SUMMARY.md written to the phase directory
</success_criteria>

<output>
After completion, create `/Users/jeet/doordash-p2p/apps/arthaBuild/.planning/phases/07.1-frontend-section-verification/07.1-01-SUMMARY.md`
</output>
