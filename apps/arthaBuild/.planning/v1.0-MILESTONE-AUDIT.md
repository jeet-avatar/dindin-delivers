---
milestone: v1.0
audited: 2026-04-11T00:00:00Z
status: gaps_found
scores:
  requirements: 18/22
  phases: 12/13
  integration: 7/9
  flows: 7/9
gaps:
  requirements:
    - id: "FR-AUTH-01 (password reset)"
      status: "partial"
      phase: "01-foundation-and-auth-backend"
      issue: "auth.py:175 generates ?token= query param; ResetPassword.tsx reads useParams() path param — token arrives as undefined, reset fails silently"
    - id: "CASE-186 (unverified user blocked)"
      status: "partial"
      phase: "11-password-management-enterprise-email-flow"
      issue: "POST /api/chatbot/process has no require_user Depends — any unauthenticated caller can invoke the LLM pipeline"
    - id: "Phase 9 JTI blacklist"
      status: "partial"
      phase: "09-rbac-team-management-and-chat-persistence"
      issue: "authService.logout() never calls POST /api/auth/logout — JTI blacklist has zero frontend consumers"
    - id: "CASE-188/189/195 (TLS + security headers)"
      status: "partial"
      phase: "12-security-hardening-and-soc2-readiness"
      issue: "docker-compose.yml mounts nginx.conf (port 80 only), not nginx.prod.conf — TLS and security headers never served in standard deployment"
  integration:
    - "POST /api/chatbot/process — no JWT enforcement, license check only"
    - "GET /api/license/status — public endpoint, leaks license plan/expiry to unauthenticated callers"
    - "POST /reset — not proxied by nginx, no auth, no frontend consumer — dead in production"
    - "docker-compose.yml mounts nginx.conf instead of nginx.prod.conf for port 443"
  flows:
    - "Password reset: token URL format mismatch (query param vs path param) — reset fails silently"
    - "Frontend logout: client-only, never calls POST /api/auth/logout — JTI blacklist unused"
tech_debt:
  - phase: "01-foundation-and-auth-backend"
    items:
      - "rawapi.py:192,213 — @app.on_event('startup') deprecated, migrate to lifespan"
      - "rawapi.py:339,350 — bare print() in exception handlers, should be logger.error()"
      - "JWT_SECRET_KEY=changeme placeholder in .env — no startup check that it has been changed"
  - phase: "03-ollama-rag-pipeline"
    items:
      - "finetunedmodelrun.py — dead file, contains lazy OpenAI import (line 152), never imported"
      - "finetunedmodelrunv2.py — dead prototype FastAPI app, never imported"
  - phase: "04-frontend-integration"
    items:
      - "src/frontend/public/landing-preview.html — internal design file served publicly by nginx"
      - "src/frontend/public/3d-image-samples.html — internal design file served publicly by nginx"
  - phase: "05-docker-aws-terraform"
    items:
      - "Dockerfile — single-stage build, no test/production stage separation"
  - phase: "10-admin-panel-enterprise-team-management-ui"
    items:
      - "adminService.ts AuditEntry interface missing Phase 12 fields: actor_role, result, ip_address, target"
      - "AdminPanel.tsx audit tab only renders actor_email — 4 of 5 Phase 12 SOC2 fields not displayed"
  - phase: "12-security-hardening-and-soc2-readiness"
    items:
      - "CASE_BOARD.html — CASE-188 through CASE-195 show status=PENDING in embedded JSON data"
      - "architecture-diagram.html Phase 6 test count shows 59/59, actual is 115/120"
      - "suitescripts_utils.py:168-188 — dead __main__ block referencing non-existent handle_suitescript_execution"
      - "CSP in report-only mode — should be enforced before GA (deferred by CONTEXT.md decision)"
unexecuted_phases:
  - phase: "08-launch-readiness"
    issue: "Phase 8 has 08-01-PLAN.md but no SUMMARY.md — never executed. Contains: customer deployment guide, smoke test scripts, final 86-test pass confirmation, human checkpoint."
---

# ArthaBuild v1.0 — Milestone Audit Report

**Audited:** 2026-04-11
**Status:** gaps_found → fixing inline

## Requirements Coverage

| Source | Score |
|--------|-------|
| Phase VERIFICATIONs | 5 files: 02 ✓, 07.1 ✓, 09 ✓, 10 ✓, 11 ✓, 12 ✓ |
| Phases without VERIFICATION | 01, 03, 04, 05, 06, 07, 08 (pre-verifier phases) |
| E2E Flows | 7/9 complete, 2 broken |

## Critical Fixes Required (being applied now)

1. **Password reset URL mismatch** — `auth.py:175` `?token=` → `/{raw_token}` (path param)
2. **Chatbot endpoint unprotected** — add `Depends(require_user)` to `POST /api/chatbot/process`
3. **License status public** — add auth to `GET /api/license/status`
4. **Reset endpoint dead** — remove `POST /reset` (unreachable via nginx, no consumers)
5. **Frontend logout incomplete** — add `POST /api/auth/logout` call in `authService.logout()`
6. **Admin audit tab** — display all Phase 12 audit fields in `AdminPanel.tsx`
7. **docker-compose nginx** — create `docker-compose.prod.yml` mounting `nginx.prod.conf`
8. **Dead code** — delete `finetunedmodelrun.py`, `finetunedmodelrunv2.py`, suitescripts_utils __main__
9. **Public design files** — remove `landing-preview.html`, `3d-image-samples.html` from public/
10. **CASE_BOARD.html** — update CASE-188→195 to DONE
11. **Architecture diagram** — fix Phase 6 test count to 115/120
12. **Dockerfile** — add multi-stage build (test/production)
13. **print() → logger** — replace bare prints in rawapi.py
14. **@on_event → lifespan** — migrate FastAPI startup handlers
