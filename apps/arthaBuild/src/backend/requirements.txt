# ArthaBuild Backend -- Phase 1 Dependencies
# Generated: 2026-04-07
# Pinned to exact versions for reproducible customer deployments.
# DO NOT edit version numbers manually -- run: pip freeze | grep <package> to verify.
# Phase 3 will replace langchain-openai + openai with langchain-ollama equivalents.
# Note: langchain-core resolved to 0.3.63 (langchain-community 0.3.8 requires >=0.3.21)
#       openai resolved to 1.109.1 (langchain-openai 0.2.9 requires >=1.54.0)
#       SQLAlchemy resolved to 2.0.35 (langchain deps required)

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Auth
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3

# Database
SQLAlchemy==2.0.35
aiosqlite==0.20.0
alembic==1.13.3
greenlet==3.3.2          # required by SQLAlchemy async (greenlet_spawn)

# Rate limiting
slowapi==0.1.9

# Email
fastapi-mail==1.4.1

# HTTP / forms
python-multipart==0.0.12
httpx==0.27.2

# Config
python-dotenv==1.0.1

# Data validation
pydantic[email]==2.9.2
email-validator==2.2.0

# Existing chatbot deps (Phase 3: switched to Ollama -- no OpenAI)
langchain-core==0.3.63
langchain-community==0.3.8
langchain-ollama==0.2.3
faiss-cpu==1.9.0
langgraph==0.2.38

# Phase 13: Identity & Access (SSO + MFA)
pyotp==2.9.0          # TOTP MFA secret generation and verification
authlib==1.3.2         # OIDC/OAuth2 SSO flows

# Phase 15: Operational reliability (error monitoring)
sentry-sdk>=2.0.0     # Sentry error monitoring — optional (requires SENTRY_DSN env var)
