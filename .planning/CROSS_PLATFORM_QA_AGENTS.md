# GSD Cross-Platform QA Agent System

> **Version**: 4.1.0
> **Date**: February 8, 2026
> **Scope**: iOS, Android, Web - Full Cross-Platform Validation
> **Generated by**: GSD (Get Stuff Done) Framework
> **Backend**: v1.0.11 | **Staging**: d3kuu45w6kl8hr.cloudfront.net | **Production**: api.dollor.ai

---

## Executive Summary

24 specialized QA agents that validate the Dollor.ai platform across **iOS**, **Android**, and **Web**.
All agents are **READ-ONLY** and verify cross-platform consistency, API contracts, and timing.

---

## 24 Agent Overview

| # | Agent Name | Focus | Platforms | Critical |
|---|------------|-------|-----------|----------|
| 1 | API Endpoint Validator | All API endpoints exist and return correct status | All | Yes |
| 2 | UI/Code Quality | Hardcoded values, patterns, accessibility | iOS/Android | Yes |
| 3 | E2E Workflow | Complete user journeys work correctly | All | Yes |
| 4 | Dead Code Detection | Unused code, orphan files | iOS/Android/Backend | No |
| 5 | Security (OWASP) | Secrets, injection, auth | All | Yes |
| 6 | Test Runner | Unit/integration tests pass | All | Yes |
| 7 | Database Health | Connectivity, schema, integrity | Backend | Yes |
| 8 | Performance | Response times < thresholds | All | Yes |
| 9 | Dependency Audit | Package versions, vulnerabilities | All | No |
| 10 | Frontend Data Validation | API response field presence | iOS/Android | Yes |
| 11 | Frontend Display Validation | No hardcoded display values | iOS/Android | Yes |
| 12 | Field Mapping Validator | iOS/Android struct matches backend | All | Yes |
| 13 | Driver App Validator | 4 tabs, all flows work | iOS/Android | Yes |
| 14 | Customer App Validator | 4-5 tabs, all flows work | iOS/Android | Yes |
| 15 | Restaurant App Validator | 4 tabs, order management | iOS/Android | Yes |
| 16 | Order Lifecycle Validator | Complete order flow | All | Yes |
| 17 | API Documentation | Endpoints documented correctly | All | No |
| 18 | Driver Details Flow | Photo, rating, vehicle pass-through | All | Yes |
| 19 | Deployment Readiness | Health checks, version, database | Production | Yes |
| 20 | TestFlight/Play Store | Build configs, signing | iOS/Android | Yes |
| 21 | API Contract Validator | iOS vs Android vs Backend match | All | Yes |
| 22 | Data Type Validator | Types consistent across platforms | All | Yes |
| 23 | QA Challenger (FINAL GATE) | Demands evidence for all passes | All | Yes |
| 24 | Cross-Platform Validator | Button actions, timing, paths | All | Yes |
| 25 | Error Message Consistency | User-friendly patterns, no raw errors | iOS/Android | Yes |
| 26 | Logger Compliance | os.Logger usage, no print(), subsystem consistency | iOS | Yes |
| 27 | Bid Negotiation Flow | Multi-round counter-offers, max rounds | All | Yes |
| 28 | Push Notification | Bid notifications, order updates | All | Yes |
| 29 | Smart Error UX | Blocking errors with navigation | iOS/Android | Yes |
| 30 | UI Consistency | Visual elements, spacing, colors, fonts | iOS | Yes |
| 31 | UX Flow | User journeys, friction points, feedback | iOS | Yes |
| 32 | Cross-App Integration | Customer↔Driver↔Restaurant flows | iOS | Yes |
| 33 | Button Action Validator | All buttons trigger correct actions | iOS | Yes |
| 34 | Navigation Flow | Screen transitions, back buttons, deep links | iOS | Yes |

---

## Platform-Specific Paths

### Critical API Path Differences

| Action | iOS Path | Android Path | Web Path | Backend |
|--------|----------|--------------|----------|---------|
| Customer Login | `/api/auth/customer/login` | `/api/auth/customer/login` | `/api/auth/customer/login` | Same |
| Apple Auth | `/api/customer/apple-auth` | N/A | N/A | iOS only |
| Create Order | `/api/erp/orders/create` | `/api/orders/create` | `/api/orders/create` | Aliased |
| Update Profile | PUT `/api/customer/{id}` | POST `/api/customer/{id}` | PUT | Different! |
| Track Ride | `/api/erp/rides/{id}/track` | `/api/rides/{id}/track` | `/api/rides/{id}/track` | Aliased |
| Cancel Ride | `/api/erp/rides/{id}/cancel` | `/api/rides/request/{id}/cancel` | Same as Android | Different! |
| Add Favorite | POST body `{customer_id, vendor_id}` | Path params | Path params | Different! |
| Payment Intent | `/payments/ride/create-intent` | `/payments/create-intent` | Same as Android | Different! |
| Negotiate Fare | `/erp/rides/{id}/customer-negotiate` | `/api/rides/{id}/negotiate` | Same as Android | Aliased |

---

## Agent Definitions

### Agent 1: API Endpoint Validator

**Purpose**: Verify all API endpoints exist and return expected status codes

**Platforms**: iOS, Android, Web, Backend

**Test Matrix**:
```
| Endpoint Category | iOS | Android | Web | Expected |
|-------------------|-----|---------|-----|----------|
| Health Check | /health | /health | /health | 200 |
| Customer Login | /api/auth/customer/login | Same | Same | 200 |
| Driver Login | /api/auth/driver/login | Same | Same | 200 |
| Vendor Login | /api/auth/vendor/login | Same | Same | 200 |
| Restaurants | /api/vendors/published | Same | Same | 200 |
| Menu | /api/vendors/{id}/menu | Same | Same | 200 |
| Customer Orders | /api/customer/orders | Same | Same | 200 |
| Driver Dashboard | /api/v5/driver/{id}/dashboard | Same | Same | 200 |
| Vendor Orders | /api/erp/orders/vendor/{id} | Same | Same | 200 |
```

**Validation Commands**:
```bash
# Test all critical endpoints
curl -s -w "%{http_code}" "$API/health"
curl -s -w "%{http_code}" "$API/api/vendors/published"
curl -s -w "%{http_code}" "$API/api/v5/driver/48/dashboard"
```

**Output**: `QA_REPORT_API.md`

---

### Agent 2: UI/Code Quality Validator

**Purpose**: Detect hardcoded values, patterns, accessibility issues

**Platforms**: iOS (Swift), Android (Kotlin)

**Checks**:
| Check | iOS Pattern | Android Pattern |
|-------|-------------|-----------------|
| Hardcoded URLs | `"http://` or `"https://` in code | Same |
| Hardcoded Prices | `"$X.XX"` literals | Same |
| Hardcoded Colors | `#[0-9A-F]{6}` not in theme | Same |
| Force Unwraps | `!` usage (iOS only) | `!!` usage (Kotlin) |
| Missing Accessibility | No `accessibilityLabel` | No `contentDescription` |

**Output**: `QA_REPORT_UI.md`

---

### Agent 3: E2E Workflow Validator

**Purpose**: Complete user journeys work across all platforms

**Workflows Tested**:

#### Customer Order Flow
```
1. Login (all platforms)
2. Browse restaurants
3. View menu
4. Add to cart
5. Checkout
6. Track order
7. Rate driver/restaurant
```

#### Driver Delivery Flow
```
1. Login
2. Go online
3. Accept delivery
4. Navigate to restaurant
5. Mark picked up
6. Navigate to customer
7. Mark delivered
8. View earnings
```

#### Restaurant Order Flow
```
1. Login
2. Receive order notification
3. Accept order (3-min window)
4. Start preparing
5. Mark ready
6. Accept/decline delivery
7. Complete
```

**Output**: `QA_REPORT_E2E.md`

---

### Agent 4: Dead Code Detection

**Purpose**: Identify unused code across all platforms

**Scope**:
- iOS: Unused Swift classes, functions, imports
- Android: Unused Kotlin classes, functions, imports
- Backend: Unused Python functions, endpoints, imports

**Output**: `QA_REPORT_DEADCODE.md`

---

### Agent 5: Security Validator (OWASP)

**Purpose**: Identify security vulnerabilities

**Checks**:
| Check | Description | All Platforms |
|-------|-------------|---------------|
| Hardcoded Secrets | API keys, tokens in code | Yes |
| Insecure Storage | Tokens in plain storage | Yes |
| HTTP Usage | Non-HTTPS URLs | Yes |
| SQL Injection | Raw queries without params | Backend |
| XSS | Unescaped user input | Web |
| Certificate Pinning | SSL pinning configured | iOS/Android |

**Output**: `QA_REPORT_SECURITY.md`

---

### Agent 6: Test Runner

**Purpose**: Execute and verify all automated tests

**Test Suites**:
| Platform | Framework | Command |
|----------|-----------|---------|
| iOS | XCTest | `xcodebuild test` |
| Android | JUnit/Espresso | `./gradlew test` |
| Backend | pytest | `pytest tests/` |

**Output**: `QA_REPORT_TESTS.md`

---

### Agent 7: Database Health Validator

**Purpose**: Verify database connectivity and integrity

**Checks**:
- Connection to PostgreSQL
- Schema migrations applied
- Foreign key constraints valid
- Indexes present for common queries
- No orphan records

**Output**: `QA_REPORT_DATABASE.md`

---

### Agent 8: Performance Validator

**Purpose**: Verify response times meet thresholds

**Thresholds**:
| Endpoint Type | Max Time | Action |
|---------------|----------|--------|
| Health check | 500ms | Critical |
| Auth endpoints | 2s | Warning |
| Read endpoints | 2s | Warning |
| Write endpoints | 3s | Warning |
| File uploads | 10s | Info |

**Output**: `QA_REPORT_PERFORMANCE.md`

---

### Agent 9: Dependency Audit

**Purpose**: Check package versions and vulnerabilities

**Scope**:
| Platform | File | Tool |
|----------|------|------|
| iOS | Package.swift, Podfile | swift-outdated |
| Android | build.gradle | dependencyUpdates |
| Backend | requirements.txt | pip-audit |

**Output**: `QA_REPORT_DEPENDENCIES.md`

---

### Agent 10: Frontend Data Validation

**Purpose**: Verify API response fields are present

**Critical Fields**:
```
FareNegotiationResponse:
  - platform_fee_driver (REQUIRED - was missing!)
  - platform_fee_customer (REQUIRED - was missing!)

CustomerRideBidsResponse:
  - request_id, bids, total_bids, bidding_open, bidding_ends_at

ActiveOrdersResponse:
  - orders[] (wrapper, not raw array!)
  - items (array or string?)
```

**Output**: `QA_REPORT_FRONTEND_DATA.md`

---

### Agent 11: Frontend Display Validation

**Purpose**: No hardcoded display values

**Checks**:
- No hardcoded prices ("$X.XX")
- No hardcoded names
- No Lorem ipsum text
- Dynamic data bindings present

**Output**: `QA_REPORT_FRONTEND_DISPLAY.md`

---

### Agent 12: Field Mapping Validator

**Purpose**: iOS/Android structs match backend response

**Critical Mappings**:
```swift
// iOS P2PCustomerOrder
items: String  // Backend returns: list (MISMATCH!)

// iOS expects raw array for active-orders
// Backend returns: {"orders": [...]} (MISMATCH!)
```

**Output**: `QA_REPORT_FIELD_MAPPING.md`

---

### Agent 13: Driver App Validator

**Purpose**: Validate all 4 Driver app tabs

**Tabs**:
1. **Delivery** - Available orders, accept, pickup, deliver
2. **Rideshare** - Available rides, bid, accept, complete
3. **Active** - Current order/ride details
4. **Earnings** - Dashboard, today/week/month stats

**Output**: `QA_REPORT_DRIVER_APP.md`

---

### Agent 14: Customer App Validator

**Purpose**: Validate all Customer app tabs

**iOS Tabs (4)**:
1. Home - Restaurants, categories
2. Search - Search, filters
3. Orders - History, tracking
4. Profile - Settings, payments

**Android Tabs (5)**:
1. Home
2. Search
3. Deals (Android only!)
4. Orders
5. Profile

**Output**: `QA_REPORT_CUSTOMER_APP.md`

---

### Agent 15: Restaurant App Validator

**Purpose**: Validate all Restaurant app tabs

**Tabs**:
1. Orders - Incoming, preparing, ready
2. Menu - Items, categories, availability
3. Analytics - Revenue, orders, popular items
4. Settings - Hours, profile, notifications

**Output**: `QA_REPORT_RESTAURANT_APP.md`

---

### Agent 16: Order Lifecycle Validator

**Purpose**: Complete order status flow works

**Status Flow**:
```
pending_payment → confirmed → pending_restaurant → preparing
→ ready_for_pickup → pending_delivery_decision
→ [assigned_to_driver | restaurant_will_deliver]
→ out_for_delivery → delivered
```

**Output**: `QA_REPORT_ORDER_LIFECYCLE.md`

---

### Agent 17: API Documentation Validator

**Purpose**: All endpoints documented

**Checks**:
- All endpoints in main_new.py documented
- All aliases documented
- Request/response formats specified
- iOS vs Android differences noted

**Output**: `QA_REPORT_API_DOCS.md`

---

### Agent 18: Driver Details Flow Validator

**Purpose**: Driver info passes through correctly

**Fields Traced**:
- Driver photo URL
- Driver rating
- Vehicle make/model/color
- License plate
- Vehicle photo URL

**Output**: `QA_REPORT_DRIVER_DETAILS_FLOW.md`

---

### Agent 19: Deployment Readiness Validator

**Purpose**: Production health checks

**Checks**:
- /health returns 200
- Database connected
- Version matches expected
- All critical endpoints respond
- SSL certificate valid

**Output**: `QA_REPORT_DEPLOYMENT.md`

---

### Agent 20: TestFlight/Play Store Validator

**Purpose**: Build configs correct

**iOS Checks**:
- Bundle ID correct
- Provisioning profile valid
- Build number incremented
- Signing identity correct

**Android Checks**:
- Package name correct
- Signing config valid
- Version code incremented
- ProGuard enabled

**Output**: `QA_REPORT_TESTFLIGHT.md`

---

### Agent 21: API Contract Validator

**Purpose**: iOS vs Android vs Backend contracts match

**Known Mismatches**:
| Issue | iOS | Android | Backend |
|-------|-----|---------|---------|
| Active Orders | `[P2PCustomerOrder]` | Wrapped | `{"orders": [...]}` |
| Items field | `String` | Array | `list` |
| Create Order | `/erp/orders/create` | `/orders/create` | Both aliased |
| Update Profile | PUT | POST | PUT expected |

**Output**: `QA_REPORT_API_CONTRACT.md`

---

### Agent 22: Data Type Validator

**Purpose**: Types consistent across platforms

**Checks**:
| Field | iOS Type | Android Type | Backend Type |
|-------|----------|--------------|--------------|
| order_id | Int | Int | int |
| total_amount | Double | Double | float |
| items | String | List | list/string |
| created_at | String | String | datetime ISO |

**Output**: `QA_REPORT_DATA_TYPE.md`

---

### Agent 23: QA Challenger (FINAL GATE)

**Purpose**: Question every pass, demand evidence

**Rules**:
1. Every "PASS" must have evidence (API response, code location)
2. No assumptions - verify with actual data
3. Block deployment if any test lacks justification
4. Creates audit trail

**Script**: `.claude/agents/qa-challenger-agent.sh`

**Output**: `.planning/qa-challenger-reports/`

---

### Agent 24: Cross-Platform Validator

**Purpose**: Button actions, timing, path consistency

**Validation Matrix**:
| Action | iOS | Android | Web | Timing |
|--------|-----|---------|-----|--------|
| Login | /auth/customer/login | Same | Same | < 3s |
| Get Restaurants | /vendors/published | Same | Same | < 2s |
| Create Order | /erp/orders/create | /orders/create | /orders/create | < 3s |
| Track Order | /erp/orders/{id}/full-tracking | Same | Same | < 2s |
| Rate Driver | /customer/orders/{id}/rate-driver | Same | Same | < 2s |

**Output**: `.claude/agents/agent-24-cross-platform-validator.md`

---

## Execution Commands

### Run All 24 Agents
```bash
./scripts/qa-runner.sh staging pre-deploy
```

### Run Individual Agent
```bash
./scripts/qa-runner.sh staging pre-deploy --agent=1  # API
./scripts/qa-runner.sh staging pre-deploy --agent=24 # Cross-Platform
```

### Run Cross-Platform Comparison
```bash
./scripts/qa-runner.sh staging cross-platform
```

---

## Current Issues Found (Feb 2026)

### CRITICAL

None - All critical issues resolved.

### RESOLVED

| Issue | Agent | Resolution | Date |
|-------|-------|------------|------|
| iOS ERP path mismatch | 1, 21, 24 | Added /api/erp/rides/* aliases | 2026-02-08 |
| FareNegotiationResponse missing fields | 12 | Backend v1.0.10 | 2026-02-06 |
| Negotiate endpoint 404 | 1, 24 | Backend alias added | 2026-02-06 |
| Active orders decode fails | 21, 12 | Backend aliased | 2026-02-07 |
| Items field type mismatch | 21, 12 | Backend returns correct format | 2026-02-07 |

## Latest QA Run (February 8, 2026)

| Agent | Status | Evidence |
|-------|--------|----------|
| 1 - API Endpoint | ✅ PASS | All endpoints return 200 |
| 7 - Database | ✅ PASS | connected, v1.0.11 |
| 8 - Performance | ✅ PASS | All < 0.3s |
| 10 - Frontend Data | ✅ PASS | platform_fee_driver/customer present |
| 12 - Field Mapping | ✅ PASS | CustomerRideBidsResponse valid |
| 13 - Driver App | ✅ PASS | today/this_week/this_month present |
| 14 - Customer App | ✅ PASS | 15 restaurants, all fields |
| 15 - Restaurant App | ✅ PASS | 84 orders, all fields |
| 16 - Order Lifecycle | ✅ PASS | All statuses present |
| 18 - Driver Details | ✅ PASS | Endpoint working |
| 19 - Deployment | ✅ PASS | healthy, connected |
| 20 - TestFlight | ✅ PASS | Customer 1053, Driver 154, Restaurant 128 |
| 21 - API Contract | ✅ PASS | iOS/Android paths working |
| 23 - QA Challenger | ✅ PASS | All evidence verified |
| 24 - Cross-Platform | ✅ PASS | iOS=Android paths equivalent |

---

## Demo Credentials

| App | Email | Password | ID |
|-----|-------|----------|-----|
| Customer | demo.customer@dollor.ai | DemoCustomer2025! | 74 |
| Driver | demo.driver@dollor.ai | DemoDriver2025! | 48 |
| Restaurant | demo.restaurant@dollor.ai | DemoRestaurant2025! | 40 |

---

## File Locations

| Component | Path |
|-----------|------|
| iOS API | `/apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift` |
| Android API | `/Users/jeet/StudioProjects/eatfair-android/shared/.../DollorApiService.kt` |
| Backend | `/apps/web/p2p-platform/backend/main_new.py` |
| QA Runner | `/scripts/qa-runner.sh` |
| QA Knowledge Base | `/.claude/agents/QA_KNOWLEDGE_BASE.md` |

---

## Quick Validation

```bash
# Health check all platforms
curl -s "https://d3kuu45w6kl8hr.cloudfront.net/health" | jq '.version'
curl -s "https://api.dollor.ai/health" | jq '.version'

# Active orders format check (THE BUG)
curl -s "https://api.dollor.ai/api/customer/74/active-orders" | python3 -c "
import json,sys
d=json.load(sys.stdin)
print('Format:', 'WRAPPED' if isinstance(d, dict) else 'RAW ARRAY')
print('Orders:', len(d.get('orders',d)))
"

# Cross-platform endpoint check
for path in "/api/erp/orders/create" "/api/orders/create"; do
  echo "$path: $(curl -s -o /dev/null -w '%{http_code}' -X POST "https://api.dollor.ai$path")"
done
```

---

### Agent 25: Error Message Consistency Validator

**Purpose**: Verify all error messages are user-friendly, not raw technical errors

**Platforms**: iOS (Swift), Android (Kotlin)

**Pattern Detection**:
| Pattern Type | Example | Rating |
|--------------|---------|--------|
| USER-FRIENDLY | "Please enter a valid email address" | 10/10 |
| USER-FRIENDLY | "Failed to submit bid: {context}" | 9/10 |
| TECHNICAL | `error.localizedDescription` raw | 5/10 |
| TECHNICAL | "Error: \(error)" | 3/10 |

**Validation Rules**:
```swift
// GOOD: User-friendly patterns
"Please [action]"                    // Validation
"[State]. [Suggestion]"              // State info
"Failed to [action]: [context]"      // Context + detail

// BAD: Technical patterns
error.localizedDescription           // Raw passthrough
"Error: \(error)"                    // No context
"Network error"                      // Too vague
```

**Files to Audit**:
- All ViewModels in customer/, delivery/, restaurant/
- Look for `errorMessage =` assignments
- Count user-friendly vs technical patterns

**Thresholds**:
| Rating | % User-Friendly |
|--------|-----------------|
| PASS | ≥ 95% |
| WARN | 90-94% |
| FAIL | < 90% |

**Output**: `QA_REPORT_ERROR_MESSAGES.md`

---

### Agent 26: Logger Compliance Validator

**Purpose**: Verify proper os.Logger usage across all iOS apps

**Platforms**: iOS only

**Compliance Checks**:
| Check | Pattern | Required |
|-------|---------|----------|
| Logger import | `import os` | Yes |
| Logger declaration | `private let logger = Logger(...)` | Yes |
| Subsystem format | `com.dollorai.{app}` | Yes |
| No print() | `print(...)` | Must be 0 |
| DEBUG-only print | `#if DEBUG print(...) #endif` | Acceptable |

**Subsystem Standards**:
| App | Subsystem |
|-----|-----------|
| Customer | `com.dollorai.customer` |
| Driver | `com.dollorai.delivery` |
| Restaurant | `com.dollorai.restaurant` |
| Shared | `com.dollorai.shared` |

**Bad Patterns**:
```swift
// WRONG: Inconsistent subsystem naming
Logger(subsystem: "ai.dollor.customer", ...)     // Wrong prefix
Logger(subsystem: Bundle.main.bundleIdentifier!, ...) // Dynamic (risky)
Logger(subsystem: "com.dollor.delivery", ...)    // Typo (missing 'ai')
```

**Validation Commands**:
```bash
# Count Logger declarations
grep -r "Logger(subsystem" apps/ios/*/

# Count print() statements
grep -r "print(" apps/ios/*/*.swift | grep -v "#if DEBUG"

# Check subsystem consistency
grep -ro "subsystem: \"[^\"]*\"" apps/ios/
```

**Output**: `QA_REPORT_LOGGER_COMPLIANCE.md`

---

### Agent 27: Bid Negotiation Flow Validator

**Purpose**: Verify complete multi-round bid negotiation flow

**Platforms**: All (iOS, Android, Backend)

**Negotiation Rules**:
| Rule | Value | Enforcement |
|------|-------|-------------|
| Max rounds per bid | 2 | Backend enforced |
| Max customer counters per ride | 3 | Backend enforced |
| Bid expiry | 10 minutes | Auto-expired |
| Low bid warning | 40-60% of suggested | UI warning |
| Bid rejection | < 40% of suggested | Backend rejects |

**Flow Validation**:
```
1. Customer creates ride request
   ↓
2. Driver submits bid (proposed_price, message, eta)
   ↓
3. Customer sees bids in DriverBidsSheet
   ↓
4. Customer can: ACCEPT | REJECT | COUNTER
   ↓ (if COUNTER)
5. Driver sees counter in pendingCounterOffers
   ↓
6. Driver can: ACCEPT | REJECT | COUNTER (via /driver-counter)
   ↓ (if COUNTER round 2)
7. Customer receives final offer (is_final_round=true)
   ↓
8. Customer must ACCEPT or REJECT (no more counters)
```

**API Endpoints**:
| Step | Endpoint | Method |
|------|----------|--------|
| Submit bid | `/api/rides/request/{id}/bid` | POST |
| Get bids | `/api/rides/request/{id}/bids` | GET |
| Customer respond | `/api/rides/bid/{id}/respond` | POST |
| Driver counter | `/api/rides/bid/{id}/driver-counter` | POST |

**Test Scenarios**:
1. Happy path: Bid → Accept
2. Counter flow: Bid → Counter → Accept
3. Double counter: Bid → Counter → Driver Counter → Accept
4. Max rounds: Verify is_final_round=true after 2 rounds
5. Rejection: Bid → Reject → Bid removed
6. Expiry: Bid → Wait 10 min → Bid expired

**Output**: `QA_REPORT_BID_NEGOTIATION.md`

---

### Agent 28: Push Notification Validator

**Purpose**: Verify push notifications trigger correctly for all events

**Platforms**: iOS, Android, Backend

**Notification Events**:
| Event | Trigger | Recipients |
|-------|---------|------------|
| New bid | Driver submits bid | Customer |
| Bid accepted | Customer accepts | Driver |
| Counter-offer | Customer/Driver counters | Other party |
| Bid rejected | Customer rejects | Driver |
| Order ready | Restaurant marks ready | Customer + Driver |
| Order delivered | Driver marks delivered | Customer |
| Ride started | Driver starts ride | Customer |
| Ride completed | Driver completes ride | Customer |

**Backend Implementation** (v1.0.15+):
```python
# bid_routes.py - push notification on new bid
await send_push_notification(
    customer_id=ride_request.customer_id,
    title="New Bid Received",
    body=f"${bid.proposed_price:.2f} from {driver.name}"
)
```

**Validation Checks**:
1. APNs/FCM tokens stored correctly
2. Notification payload format correct
3. Deep links work (tap opens correct screen)
4. Badge counts update correctly
5. Notification permissions requested properly

**Output**: `QA_REPORT_PUSH_NOTIFICATIONS.md`

---

### Agent 29: Smart Error UX Validator

**Purpose**: Verify blocking errors show smart alerts with navigation

**Platforms**: iOS, Android

**Smart Error Pattern**:
```swift
// WORLD-CLASS: Smart detection + navigation
if message.contains("active ride") {
    alertTitle = "Complete Active Ride First"
    primaryButton = "View Active Work" → navigates to Active tab
}
```

**Required Blocking Errors**:
| Error Condition | Alert Title | Primary Action |
|-----------------|-------------|----------------|
| Active ride exists | "Complete Active Ride First" | Navigate to Active tab |
| Active delivery exists | "Complete Delivery First" | Navigate to Active tab |
| Max bids reached | "Maximum Bids Reached" | Show alternative rides |
| Bidding closed | "Bidding Closed" | Dismiss |

**Implementation Checklist**:
- [ ] ViewModel detects blocking keywords with `.contains()`
- [ ] View computes `isBlockingError` boolean
- [ ] Alert shows context-aware title
- [ ] Primary button has navigation action
- [ ] Secondary button is "OK" for dismissal

**Files to Audit**:
| File | Smart Error |
|------|-------------|
| RideBiddingViewModel.swift:200-205 | Smart detection |
| RideshareDashboardView.swift | hasActiveRide/hasActiveDelivery |
| AvailableRideRequestsView.swift | isBlockingError + navigation |

**Backend Contract Dependency**:
iOS smart alerts depend on backend error messages containing exact keywords:
- "active ride" (bid_routes.py:781)
- "active delivery" (bid_routes.py:795)

**Change Coordination Required**: If backend changes these error strings, iOS must be updated simultaneously.

**Output**: `QA_REPORT_SMART_ERROR_UX.md`

---

---

### Agent 30: UI Consistency Validator

**Purpose**: Verify visual consistency across all screens in each app

**Platforms**: iOS (Swift/SwiftUI)

**Checks**:
| Check | Pattern | Standard |
|-------|---------|----------|
| Theme Colors | `Theme.brandColor`, `Theme.primary` | Consistent per app |
| Font Usage | `.font(.system(...))` or Theme fonts | No hardcoded sizes |
| Spacing | `padding()`, `spacing:` | Consistent spacing values |
| Corner Radius | `.cornerRadius()` | 8, 12, 16 standard |
| Button Styles | `ButtonStyle` | Consistent across app |
| Card Styles | Card views | Consistent shadow/border |

**Files to Audit**:
- All Views/*.swift in each app
- Theme.swift for each app
- Shared components

**Thresholds**:
| Rating | Consistency |
|--------|-------------|
| PASS | ≥ 95% consistent |
| WARN | 85-94% |
| FAIL | < 85% |

**Output**: `QA_REPORT_UI_CONSISTENCY.md`

---

### Agent 31: UX Flow Validator

**Purpose**: Verify user experience quality - smooth flows, clear feedback, minimal friction

**Platforms**: iOS

**UX Checks**:
| Check | Description | Requirement |
|-------|-------------|-------------|
| Loading States | `isLoading` shown to user | Every API call shows loading |
| Error Feedback | Errors shown clearly | All errors have user message |
| Success Feedback | Confirmations shown | Actions confirmed visually |
| Empty States | Empty list handling | Helpful message + action |
| Input Validation | Real-time validation | Immediate feedback |
| Undo/Cancel | Reversible actions | Cancel option where needed |

**User Journeys to Validate**:

**Customer App:**
1. Browse → Add to Cart → Checkout → Track → Rate
2. Request Ride → See Bids → Accept → Track → Pay → Rate

**Driver App:**
1. Go Online → See Orders → Accept → Navigate → Pickup → Deliver → Earnings
2. See Rides → Bid → Wait → Accept Counter → Start → Complete

**Restaurant App:**
1. See Order → Accept → Prepare → Mark Ready → Assign Driver/Self-Deliver

**Friction Points to Check**:
- Excessive taps to complete action
- Unclear next steps
- Missing back/cancel options
- Confusing error messages
- No loading indicators

**Output**: `QA_REPORT_UX_FLOW.md`

---

### Agent 32: Cross-App Integration Validator

**Purpose**: Verify data flows correctly between Customer, Driver, and Restaurant apps

**Platforms**: All 3 iOS apps + Backend

**Integration Flows**:

**Flow 1: Food Order Lifecycle**
```
Customer creates order
    ↓ (order_id)
Restaurant sees order in list
    ↓ (accept)
Restaurant marks ready
    ↓ (status update)
Driver sees available order
    ↓ (accept delivery)
Customer sees driver assigned
    ↓ (driver_id, eta)
Driver marks picked up
    ↓ (status update)
Customer sees "Out for Delivery"
    ↓
Driver marks delivered
    ↓ (status update)
Customer can rate driver
Restaurant sees completed order
```

**Flow 2: Ride Request Lifecycle**
```
Customer creates ride request
    ↓ (request_id)
Driver sees in available rides
    ↓ (bid)
Customer sees bid in list
    ↓ (accept/counter)
Driver sees acceptance/counter
    ↓ (respond)
Customer sees driver details
    ↓ (navigate)
Driver starts ride
    ↓ (status)
Customer sees "In Progress"
    ↓
Driver completes ride
    ↓ (status)
Customer can rate + tip
```

**Data Consistency Checks**:
| Field | Customer App | Driver App | Restaurant App |
|-------|--------------|------------|----------------|
| order_id | Shows | Shows | Shows |
| order_status | Real-time | Real-time | Real-time |
| driver_name | Shows | N/A | Shows |
| driver_photo | Shows | N/A | Shows |
| eta | Calculated | Calculated | Shows |
| items | Shows | Shows | Shows |
| total | Shows | Shows | Shows |

**Output**: `QA_REPORT_CROSS_APP.md`

---

### Agent 33: Button Action Validator

**Purpose**: Verify every button triggers the correct action and provides feedback

**Platforms**: iOS

**Validation Matrix**:
| Button Type | Expected Action | Feedback Required |
|-------------|-----------------|-------------------|
| Submit | API call + navigate | Loading → Success/Error |
| Cancel | Dismiss/reset | Immediate |
| Toggle | State change | Visual change |
| Navigation | Push/present view | Animation |
| Delete | Confirmation + action | Alert → Loading → Done |
| Refresh | Reload data | Loading indicator |

**Checks per App**:

**Customer App (261 buttons)**:
- Login/Register buttons → Auth flow
- Add to Cart → Cart update
- Checkout → Order creation
- Track → Navigation to tracking
- Rate → Rating submission
- Request Ride → Ride creation
- Accept Bid → Bid acceptance

**Driver App (256 buttons)**:
- Go Online → Status change
- Accept Order → Order assignment
- Navigate → Maps opening
- Mark Picked Up → Status update
- Mark Delivered → Order completion
- Submit Bid → Bid creation
- Accept Counter → Bid acceptance

**Restaurant App (98 buttons)**:
- Accept Order → Order status
- Mark Preparing → Status update
- Mark Ready → Status update
- Self-Deliver → Delivery assignment
- Menu toggle → Availability change

**Output**: `QA_REPORT_BUTTON_ACTIONS.md`

---

### Agent 34: Navigation Flow Validator

**Purpose**: Verify all navigation paths work correctly

**Platforms**: iOS

**Navigation Checks**:
| Check | Description |
|-------|-------------|
| Tab Navigation | All tabs accessible |
| Push Navigation | Forward navigation works |
| Back Navigation | Back buttons return correctly |
| Modal Dismiss | Sheets/modals dismiss properly |
| Deep Links | URL schemes work |
| State Preservation | State maintained on back |

**Tab Structures**:

**Customer (4 tabs):**
- Home → Restaurant → Menu → Item → Cart → Checkout
- Search → Results → Restaurant
- Orders → Order Detail → Track → Rate
- Profile → Settings → Edit

**Driver (5 tabs):**
- Delivery → Order Detail → Navigate → Complete
- Rideshare → Bid → Active Ride → Complete
- Active → Current Work Detail
- Messages → Chat
- Profile → Settings → Documents

**Restaurant (5 tabs):**
- Orders → Order Detail → Actions
- Menu → Item Edit
- Analytics → Period Select
- AI → Insights
- Settings → Various

**Edge Cases**:
- Double-tap tab (scroll to top/refresh)
- Swipe back gesture
- Hardware back button (if applicable)
- App backgrounding/foregrounding

**Output**: `QA_REPORT_NAVIGATION.md`

---

*Generated by GSD Framework v1.18.0*
*Dollor.ai Cross-Platform QA System - 34 Agents*
*Last Updated: February 11, 2026*
