---
phase: 02-api-endpoint-standardization
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift
autonomous: true
requirements: [API-01, API-05]

must_haves:
  truths:
    - "iOS vendor delete account calls DELETE /api/vendors/{vendorId} (no /delete suffix)"
    - "iOS order chat calls GET/POST /api/customer/orders/{orderId}/chat (correct path)"
    - "iOS completeRide() duplicate function removed or aligned with canonical path"
    - "No broken iOS API paths remain in P2PAPIService.swift"
  artifacts:
    - path: "apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift"
      provides: "Corrected API paths for vendor delete and order chat"
      contains: "vendors/\\(vendorId)\""
  key_links:
    - from: "P2PAPIService.swift deleteVendorAccount"
      to: "main_new.py DELETE /api/vendors/{vendor_id}"
      via: "URLSession HTTP DELETE"
      pattern: "vendors/.*vendorId"
    - from: "P2PAPIService.swift getOrderChat/sendOrderChat"
      to: "main_new.py GET/POST /api/customer/orders/{order_id}/chat"
      via: "URLSession HTTP GET/POST"
      pattern: "customer/orders.*chat"
---

<objective>
Fix broken iOS API paths in P2PAPIService.swift so vendor delete, order chat, and ride completion work correctly.

Purpose: BUG-01 (vendor delete) is an App Store compliance issue -- Apple requires working account deletion. BUG-02 (order chat) prevents customers from chatting about food orders. Divergence #17 (duplicate completeRide) causes ride completion to hit the wrong endpoint. All are real user-facing failures caused by wrong URL paths in iOS.

Output: P2PAPIService.swift with corrected paths and duplicate completeRide() resolved. Backend aliases from Plan 01 provide backward compatibility.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-api-endpoint-standardization/02-RESEARCH.md
@.planning/phases/02-api-endpoint-standardization/02-01-SUMMARY.md
@apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix iOS vendor delete, order chat paths, and completeRide duplicate</name>
  <files>apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift</files>
  <action>
**BUG-01 FIX: Vendor delete account path.**
At line ~6730 in P2PAPIService.swift, find:
```swift
guard let url = URL(string: "\(baseURL)/vendors/\(vendorId)/delete") else {
```
Change to:
```swift
guard let url = URL(string: "\(baseURL)/vendors/\(vendorId)") else {
```
The HTTP method is already DELETE, so removing the `/delete` suffix is the only change needed. Backend has `DELETE /api/vendors/{vendor_id}` at main_new.py:11347.

**BUG-02 FIX: Order chat paths.**
At line ~11687, find:
```swift
guard let url = URL(string: "\(baseURL)/orders/\(orderId)/chat") else {
```
Change to:
```swift
guard let url = URL(string: "\(baseURL)/customer/orders/\(orderId)/chat") else {
```

At line ~11743, find the same pattern for POST and make the same change:
```swift
guard let url = URL(string: "\(baseURL)/orders/\(orderId)/chat") else {
```
Change to:
```swift
guard let url = URL(string: "\(baseURL)/customer/orders/\(orderId)/chat") else {
```

Note: Plan 01 also adds backend aliases at `/api/orders/{id}/chat` for backward compatibility, but fixing the iOS path to the canonical `/api/customer/orders/{id}/chat` is the correct long-term fix.

**DIVERGENCE #17 FIX: Remove duplicate completeRide() function.**
There are TWO ride completion functions in P2PAPIService.swift:

1. `completeRide()` at line ~4953 -- calls `PUT /api/erp/orders/{rideId}/complete-delivery`. This is a FOOD DELIVERY endpoint being misused for rides. It is the WRONG path for rideshare completion.

2. `completeRideRequest()` at line ~5833 -- calls `POST /api/rides/request/{rideRequestId}/complete`. This is the CORRECT canonical rideshare completion endpoint (bid_routes.py).

Action:
- Grep P2PAPIService.swift for ALL callers of `completeRide(` (with opening paren, to distinguish from `completeRideRequest`). Check if any caller outside this file references it.
- If `completeRide()` is called from other iOS files, update those callers to use `completeRideRequest()` instead, adjusting parameter names as needed (both take an ID).
- After confirming no remaining callers, remove the `completeRide()` function entirely (lines ~4953-4990 approximately). If callers exist that cannot be easily updated, instead change `completeRide()` to delegate to `completeRideRequest()` internally.
- The goal is: only ONE ride completion function remains, calling the canonical `POST /api/rides/request/{id}/complete` path.

**Verify no other broken paths.** After making the fixes, search P2PAPIService.swift for any other paths that might be wrong. Specifically:
- `grep` for `/delete"` patterns to find any other endpoints with stale `/delete` suffix
- Confirm that ride-related paths use the canonical bid_routes.py paths (already verified as correct in research)
  </action>
  <verify>
Run: `grep -n "vendors.*vendorId.*delete\|vendors.*delete\"" /Users/jeet/doordash-p2p/apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift` -- should return NO matches (the /delete suffix is gone).

Run: `grep -n "orders.*orderId.*chat" /Users/jeet/doordash-p2p/apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift` -- should show `customer/orders/\(orderId)/chat` (with customer prefix).

Run: `grep -c "baseURL.*orders.*chat" /Users/jeet/doordash-p2p/apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift` -- should return 2 (GET and POST).

Run: `grep -n "func completeRide(" /Users/jeet/doordash-p2p/apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift` -- should return NO matches (duplicate removed). Only `completeRideRequest(` should remain.

Run: `grep -c "complete-delivery" /Users/jeet/doordash-p2p/apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift` -- should return 1 (the food delivery completion function only, NOT a ride completion function).
  </verify>
  <done>
iOS vendor delete calls `DELETE /api/vendors/{vendorId}` (matching backend). iOS order chat calls `GET/POST /api/customer/orders/{orderId}/chat` (matching backend). Duplicate `completeRide()` function removed -- only `completeRideRequest()` remains for rideshare completion via canonical `POST /api/rides/request/{id}/complete`. No more 404s for these operations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy backend and smoke test staging</name>
  <files>.github/workflows/deploy-staging.yml</files>
  <action>
Deploy the backend changes from Plan 01 (new routes/aliases) plus the iOS client fixes to staging, then verify. This task does not modify any files -- it deploys and smoke tests.

**Step 1: Commit all changes.**
Stage and commit both main_new.py (Plan 01 backend changes) and P2PAPIService.swift (this plan's iOS fixes) if not already committed:
```bash
git add apps/web/p2p-platform/backend/main_new.py apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift
git commit -m "fix(02): add backend aliases + fix iOS paths for vendor delete, order chat, demo-login, and Android financial endpoints"
```

**Step 2: Push to remote.**
```bash
git push origin main
```

**Step 3: Deploy to staging via CI/CD.**
```bash
gh workflow run deploy-staging.yml --ref main
```
Wait for the workflow to complete:
```bash
gh run list --workflow=deploy-staging.yml --limit 3
gh run watch <run-id>
```

**Step 4: Smoke test staging.**
Test the new/fixed endpoints against staging (`https://d34u5ixl0bulv4.cloudfront.net`):

1. Order chat alias: `curl -s -o /dev/null -w "%{http_code}" "https://d34u5ixl0bulv4.cloudfront.net/api/orders/1/chat" -H "Authorization: Bearer test"` -- should return 401 (auth required, NOT 404)
2. Driver balance: `curl -s -o /dev/null -w "%{http_code}" "https://d34u5ixl0bulv4.cloudfront.net/api/drivers/1/balance" -H "Authorization: Bearer test"` -- should return 401 (NOT 404)
3. Health check: `curl -s "https://d34u5ixl0bulv4.cloudfront.net/api/health"` -- should return 200

The key verification is that new endpoints return 401 (auth middleware working) instead of 404 (route not found). A 404 means the route alias was not deployed.

**IMPORTANT:** Follow CI/CD rules from CLAUDE.md. NEVER run manual docker/ecs commands.
  </action>
  <verify>
Run: `gh run list --workflow=deploy-staging.yml --limit 1` -- latest run should show "completed" status.

Smoke test: `curl -s -o /dev/null -w "%{http_code}" "https://d34u5ixl0bulv4.cloudfront.net/api/orders/1/chat"` -- should NOT return 404.

Smoke test: `curl -s -o /dev/null -w "%{http_code}" "https://d34u5ixl0bulv4.cloudfront.net/api/drivers/1/balance"` -- should NOT return 404.

Smoke test: `curl -s "https://d34u5ixl0bulv4.cloudfront.net/api/health"` -- should return `{"status": "healthy"}`.
  </verify>
  <done>
Backend deployed to staging via CI/CD. All new routes respond (not 404). Health check passes. Staging is ready for production promotion.
  </done>
</task>

</tasks>

<verification>
1. iOS P2PAPIService.swift has no broken vendor delete or order chat paths
2. Duplicate completeRide() removed, only completeRideRequest() remains for rides
3. Backend changes deployed to staging ECS
4. Staging smoke tests confirm new routes are live (401, not 404)
5. Health check passes on staging
</verification>

<success_criteria>
- grep confirms no `/delete` suffix in vendor delete path
- grep confirms order chat uses `/customer/orders/` prefix
- grep confirms only ONE ride completion function (completeRideRequest) for rideshare
- staging deploy completed successfully via CI/CD
- staging smoke tests show 401 (not 404) for new auth-protected routes
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-endpoint-standardization/02-02-SUMMARY.md`
</output>
