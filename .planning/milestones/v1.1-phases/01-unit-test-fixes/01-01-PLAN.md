---
phase: 01-unit-test-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/p2p-platform/backend/email_service.py
  - apps/web/p2p-platform/backend/tests/unit/test_api_config.py
  - apps/web/p2p-platform/backend/tests/unit/test_dollor_pricing_model.py
  - apps/web/p2p-platform/backend/tests/unit/test_email_service.py
  - apps/web/p2p-platform/backend/tests/unit/test_models.py
  - apps/web/p2p-platform/backend/tests/unit/test_stripe_integration.py
autonomous: false

must_haves:
  truths:
    - "All 17 previously-failing unit tests now pass in CI"
    - "CI pipeline on main is green (run-tests job succeeds)"
    - "No regressions in the other 985 tests"
    - "deploy-dollar-ai.yml no longer blocked by test failures"
  artifacts:
    - path: "apps/web/p2p-platform/backend/email_service.py"
      provides: "Demo email allowlist in _validate_recipient_in_db()"
      contains: "DEMO_EMAILS_ALLOWLIST"
    - path: "apps/web/p2p-platform/backend/tests/unit/test_api_config.py"
      provides: "Tax rate assertion aligned to 6%"
      contains: "0.06"
    - path: "apps/web/p2p-platform/backend/tests/unit/test_dollor_pricing_model.py"
      provides: "Tax rate assertion aligned to 6%"
      contains: "0.06"
    - path: "apps/web/p2p-platform/backend/tests/unit/test_email_service.py"
      provides: "IS_PRODUCTION patches, SMTP timeout, approval email assertions"
      contains: "IS_PRODUCTION"
    - path: "apps/web/p2p-platform/backend/tests/unit/test_models.py"
      provides: "Updated enum counts (OrderStatus=14, DriverStatus=6)"
      contains: "PENDING_DELIVERY_PROOF"
    - path: "apps/web/p2p-platform/backend/tests/unit/test_stripe_integration.py"
      provides: "Order prefix accepts DOLL or ORD-, IS_PRODUCTION patch"
      contains: "DOLL"
  key_links:
    - from: "deploy-dollar-ai.yml"
      to: "pytest tests/unit/"
      via: "run-tests job gates deploy jobs"
      pattern: "needs: run-tests"
    - from: "deploy-staging.yml"
      to: "pytest tests/unit/"
      via: "run-tests job gates deploy jobs"
      pattern: "needs: run-tests"
---

<objective>
Commit the 6 locally-modified files that fix all 17 unit test failures, push to main, and verify the CI pipeline turns green.

Purpose: The CI/CD pipeline (deploy-dollar-ai.yml and deploy-staging.yml) runs `pytest tests/unit/ -v --tb=short` as a hard gate. 17 stale test assertions block ALL deployments. The fixes are already applied locally -- this plan commits, pushes, and verifies.

Output: Green CI on main, unblocked deployment pipeline, Phase 01 complete.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-unit-test-fixes/01-RESEARCH.md
@.planning/phases/01-unit-test-fixes/VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run local test suite and commit the 6 fixed files</name>
  <files>
    apps/web/p2p-platform/backend/email_service.py
    apps/web/p2p-platform/backend/tests/unit/test_api_config.py
    apps/web/p2p-platform/backend/tests/unit/test_dollor_pricing_model.py
    apps/web/p2p-platform/backend/tests/unit/test_email_service.py
    apps/web/p2p-platform/backend/tests/unit/test_models.py
    apps/web/p2p-platform/backend/tests/unit/test_stripe_integration.py
  </files>
  <action>
    1. Run the unit tests locally to confirm all 356 pass before committing:
       ```
       cd apps/web/p2p-platform/backend
       python -m pytest tests/unit/test_api_config.py tests/unit/test_dollor_pricing_model.py tests/unit/test_email_service.py tests/unit/test_models.py tests/unit/test_stripe_integration.py -v --tb=short
       ```
       Expect: 356 passed, 0 failed. If any fail, STOP and diagnose.

    2. Stage ONLY these 6 files (do NOT stage other uncommitted changes):
       ```
       git add apps/web/p2p-platform/backend/email_service.py \
              apps/web/p2p-platform/backend/tests/unit/test_api_config.py \
              apps/web/p2p-platform/backend/tests/unit/test_dollor_pricing_model.py \
              apps/web/p2p-platform/backend/tests/unit/test_email_service.py \
              apps/web/p2p-platform/backend/tests/unit/test_models.py \
              apps/web/p2p-platform/backend/tests/unit/test_stripe_integration.py
       ```

    3. Commit with descriptive message:
       ```
       git commit -m "fix(tests): align 17 unit test assertions with production code

       - Tax rate 9% -> 6% (test_api_config, test_dollor_pricing_model)
       - OrderStatus count 13->14 (PENDING_DELIVERY_PROOF), DriverStatus 5->6 (ONLINE)
       - SMTP timeout=30 assertion, IS_PRODUCTION=False patches (11 test methods)
       - Driver approval email: subject, code display, login link assertions
       - Order number prefix DOLL (was ORD-)
       - Demo email allowlist in email_service.py for App Store review accounts"
       ```

    4. Push to origin/main:
       ```
       git push origin main
       ```
       This push modifies files under `apps/web/p2p-platform/**` which triggers deploy-dollar-ai.yml.
  </action>
  <verify>
    - `git log --oneline -1` shows the fix commit on main
    - `git status` shows the 6 files are no longer in modified state
    - `git diff HEAD -- apps/web/p2p-platform/backend/email_service.py` returns empty (committed)
  </verify>
  <done>
    6 files committed to main and pushed to origin. The push triggers deploy-dollar-ai.yml CI workflow.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify CI pipeline passes on GitHub</name>
  <files>none (remote verification only)</files>
  <action>
    Run `gh run list --workflow=deploy-dollar-ai.yml --limit=1` to check the CI status.
    If the run is still in progress, wait and re-check with `gh run watch` or poll.
    The run-tests job should complete in 2-3 minutes.
  </action>
  <verify>
    1. Check GitHub Actions for the latest workflow run:
       `gh run list --workflow=deploy-dollar-ai.yml --limit=1`
       OR visit: https://github.com/jeet-avatar/doordash-p2p/actions/workflows/deploy-dollar-ai.yml

    2. Wait for the "Run Tests" job to complete (typically 2-3 minutes).

    3. Verify the run-tests job shows green (success). The key output line should be:
       `XX passed` with 0 failures in the pytest output.

    4. If the run-tests job passes, the deploy-frontend and deploy-backend jobs will proceed.

    NOTE: The deploy jobs may fail for infrastructure reasons (AWS credentials, etc.) -- that is OUT OF SCOPE for this phase. The goal is ONLY that run-tests passes.

    If run-tests FAILS: Check the pytest output. The 1 known flaky test (test_send_email_smtp_connection_error) may appear -- this is a pre-existing test-ordering issue that passes in isolation and is not part of the 17 fixes.
  </verify>
  <done>
    CI run-tests job passes. Phase 01 is complete. Phase 02 (Security Auth Fix) is unblocked for deployment.
  </done>
</task>

</tasks>

<verification>
1. Local: `pytest tests/unit/ -v --tb=short` passes 356/356 in the 5 target test files
2. Remote: GitHub Actions `deploy-dollar-ai.yml` run-tests job succeeds
3. No regressions: Other 646 tests (10 unmodified files) still pass
4. Deployment unblocked: deploy-frontend and deploy-backend jobs can proceed past the test gate
</verification>

<success_criteria>
- The commit is on origin/main
- GitHub Actions run-tests job passes with 0 test failures in the 5 fixed files
- Phase 01 is complete -- Phase 02 (Security Auth Fix) is unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/01-unit-test-fixes/01-01-SUMMARY.md`
</output>
