---
phase: 04-docs-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - apps/ios/Config/Staging.xcconfig
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "Staging URL in CLAUDE.md is d34u5ixl0bulv4.cloudfront.net"
    - "Admin email in CLAUDE.md is support@dollor.ai"
    - "Android build commands use assembleDebug/assembleRelease (no product flavors)"
    - "iOS build commands are documented with xcodebuild examples"
    - "Doc file references only list files that actually exist"
    - "Microservices count says 16"
    - "auth_utils.py and global auth middleware are documented"
    - "iOS Staging.xcconfig uses correct staging URL"
    - "Last Updated date reflects current date"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Corrected project instructions"
      contains: "d34u5ixl0bulv4.cloudfront.net"
    - path: "apps/ios/Config/Staging.xcconfig"
      provides: "Correct iOS staging environment config"
      contains: "d34u5ixl0bulv4.cloudfront.net"
  key_links:
    - from: "CLAUDE.md"
      to: ".claude/docs/"
      via: "doc file references"
      pattern: "GROUND_TRUTH.md|API_ENDPOINTS.md"
    - from: "apps/ios/Config/Staging.xcconfig"
      to: "production staging infrastructure"
      via: "API_BASE_URL"
      pattern: "d34u5ixl0bulv4"
---

<objective>
Fix all wrong, missing, and stale information in CLAUDE.md and iOS Staging.xcconfig.

Purpose: CLAUDE.md is the primary instruction file read by every Claude session. Wrong URLs, wrong emails, and references to non-existent files cause hallucination and wasted effort. The iOS Staging.xcconfig points to the old production CloudFront URL, meaning iOS staging builds hit production.

Output: Corrected CLAUDE.md with accurate URLs, emails, iOS build commands, security docs, and file references. Corrected Staging.xcconfig with real staging URL.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@apps/ios/Config/Staging.xcconfig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all wrong information in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Read CLAUDE.md, then apply ALL of the following corrections:

**1. Fix Staging URL (line 48):**
Change `https://d3kuu45w6kl8hr.cloudfront.net` to `https://d34u5ixl0bulv4.cloudfront.net`

**2. Fix Admin email (line 219):**
Change `admin.invoice@dollor.ai` to `support@dollor.ai`

**3. Fix Microservices count (line 83):**
Change `Microservices (18 total)` to `Microservices (16 total)`

**4. Fix Detailed Documentation table (lines 165-177):**
Remove ALL doc file references that do not exist. Only these 2 files exist in `.claude/docs/`:
- `API_ENDPOINTS.md` — API endpoints, auth patterns, demo credentials
- `GROUND_TRUTH.md` — Backend-verified facts with file:line refs (anti-hallucination)

Remove references to: `01-BUSINESS_MODEL.md`, `02-ARCHITECTURE.md`, `03-API_ENDPOINTS.md` (note: the actual file is just `API_ENDPOINTS.md`, not `03-API_ENDPOINTS.md`), `04-DEVELOPMENT.md`, `05-DEPLOYMENT.md`, `06-APP_STORE.md`, `07-EVENT_ARCHITECTURE.md`, `99-CHANGELOG.md`

**5. Add iOS Build Commands section (NEW — after Android commands):**
Add a new section:
```
### Build iOS Apps
```bash
cd /Users/jeet/doordash-p2p

# Customer App
xcodebuild -workspace apps/ios/EatFair.xcworkspace \
  -scheme eatfaircustomer -configuration Staging \
  -destination 'generic/platform=iOS' build

# Driver App
xcodebuild -workspace apps/ios/EatFair.xcworkspace \
  -scheme eatffairdelivery -configuration Staging \
  -destination 'generic/platform=iOS' build

# Restaurant App (NOTE: scheme may not be shared in workspace;
# if "scheme not found", use -project apps/ios/restaurant/eatffairrestaurant.xcodeproj instead of -workspace)
xcodebuild -workspace apps/ios/EatFair.xcworkspace \
  -scheme eatffairrestaurant -configuration Staging \
  -destination 'generic/platform=iOS' build
```

Configs: `apps/ios/Config/` (Development, Staging, Production)
Bundle IDs: `com.dollorai.customer`, `com.dollorai.delivery`, `com.dollorai.restaurant`
```

**6. Add Security Architecture section (NEW — after Common Commands):**
Add a new section documenting the auth system:
```
## SECURITY ARCHITECTURE

### Authentication Stack (Phase 02 — Feb 2026)

| Layer | File | Purpose |
|-------|------|---------|
| Global middleware | `main_new.py:367` (`require_auth_middleware`) | Safety net — blocks unauthenticated requests to non-allowlisted paths |
| Admin middleware | `main_new.py:196` (`admin_auth_middleware`) | All `/api/admin/*` endpoints require admin JWT or ADMIN_SECRET_KEY |
| Auth utilities | `auth_utils.py` | Reusable Depends() functions for router/endpoint-level auth |
| iOS guard-let | `P2PAPIService.swift` | Hard-fail without token (no silent fallback) |

### Auth Utility Functions (`auth_utils.py`)

| Function | Returns | Use Case |
|----------|---------|----------|
| `require_any_auth` | JWT payload dict | Router-level — any valid JWT accepted |
| `require_customer` | Customer ORM object | Customer-only endpoints |
| `require_driver` | Driver ORM object | Driver-only endpoints |
| `require_vendor` | Vendor ORM object | Vendor-only endpoints |
| `require_admin` | User ORM object | Admin-only endpoints (403 if wrong role) |

### Required Environment Variables

| Variable | Purpose | Crash Behavior |
|----------|---------|----------------|
| `JWT_SECRET_KEY` | JWT signing/verification | `RuntimeError` at startup (`main_new.py:848`) |
| `STRIPE_SECRET_KEY` | Payment processing | Stripe calls fail |
| `ADMIN_SECRET_KEY` | Admin API access | Admin auth fallback disabled |
| `DATABASE_URL` | PostgreSQL connection | App won't start |
```

**7. Verify Android Build Commands are ALREADY CORRECT — do NOT change them:**
The existing Android commands (`assembleDebug`, `assembleRelease`, `bundleRelease`, `testDebugUnitTest`) are correct. There are NO product flavors (Staging/Production) in the Android build.gradle.kts. Do NOT add `assembleStagingDebug`, `assembleProductionRelease`, or `testStagingDebugUnitTest` — these Gradle tasks do not exist.

**8. Update Last Updated date (line 231):**
Change `February 15, 2026` to `February 20, 2026`
  </action>
  <verify>
Run these checks after writing:
1. `grep "d3kuu45w6kl8hr" CLAUDE.md` should return NO results
2. `grep "d34u5ixl0bulv4" CLAUDE.md` should return exactly 1 result (staging URL)
3. `grep "admin.invoice" CLAUDE.md` should return NO results
4. `grep "support@dollor.ai" CLAUDE.md` should return at least 1 result
5. `grep "18 total" CLAUDE.md` should return NO results
6. `grep "16 total" CLAUDE.md` should return 1 result
7. `grep "01-BUSINESS_MODEL" CLAUDE.md` should return NO results
8. `grep "auth_utils.py" CLAUDE.md` should return at least 1 result
9. `grep "JWT_SECRET_KEY" CLAUDE.md` should return at least 1 result
10. `grep "assembleStagingDebug" CLAUDE.md` should return NO results (these tasks do not exist)
11. `grep "assembleDebug" CLAUDE.md` should return at least 1 result (existing correct commands)
12. `grep "xcodebuild" CLAUDE.md` should return at least 1 result
13. `grep "February 20, 2026" CLAUDE.md` should return 1 result
  </verify>
  <done>
CLAUDE.md contains zero wrong URLs, zero wrong emails, zero references to non-existent doc files, existing Android build commands preserved unchanged (assembleDebug/assembleRelease — no product flavors), iOS build commands with scheme limitation noted for restaurant app, security architecture documentation, and correct microservices count.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix iOS Staging.xcconfig URL</name>
  <files>apps/ios/Config/Staging.xcconfig</files>
  <action>
Read `apps/ios/Config/Staging.xcconfig`, then replace ALL occurrences of the old staging URL with the correct one:

Change `d3kuu45w6kl8hr.cloudfront.net` to `d34u5ixl0bulv4.cloudfront.net` on these 3 lines:
- Line 6: `API_BASE_URL = https:/$()/d3kuu45w6kl8hr.cloudfront.net` -> `API_BASE_URL = https:/$()/d34u5ixl0bulv4.cloudfront.net`
- Line 38: `WEBSOCKET_URL = wss:/$()/d3kuu45w6kl8hr.cloudfront.net` -> `WEBSOCKET_URL = wss:/$()/d34u5ixl0bulv4.cloudfront.net`
- Line 39: `CDN_URL = https:/$()/d3kuu45w6kl8hr.cloudfront.net` -> `CDN_URL = https:/$()/d34u5ixl0bulv4.cloudfront.net`

Also update the comment on line 37 to reference the correct domain:
`// Server URLs (Using CloudFront - staging-ws.dollor.ai DNS not configured)` -- keep as-is (comment is fine)

IMPORTANT: Preserve the xcconfig `/$()` escape syntax exactly. Do NOT change `https:/$()/` to `https://`.
  </action>
  <verify>
1. `grep "d3kuu45w6kl8hr" apps/ios/Config/Staging.xcconfig` should return NO results
2. `grep "d34u5ixl0bulv4" apps/ios/Config/Staging.xcconfig` should return exactly 3 results (API, WS, CDN)
3. `grep 'https:/\$()/' apps/ios/Config/Staging.xcconfig` should return 2 results (API + CDN) -- confirms escape syntax preserved
  </verify>
  <done>
iOS Staging.xcconfig points to the real staging CloudFront distribution (d34u5ixl0bulv4), not the production one. All 3 URLs (API, WebSocket, CDN) updated.
  </done>
</task>

</tasks>

<verification>
- `grep -r "d3kuu45w6kl8hr" CLAUDE.md apps/ios/Config/Staging.xcconfig` returns NO matches
- `grep "admin.invoice" CLAUDE.md` returns NO matches
- `grep "18 total" CLAUDE.md` returns NO matches
- `grep "01-BUSINESS_MODEL\|04-DEVELOPMENT\|05-DEPLOYMENT\|06-APP_STORE\|07-EVENT_ARCHITECTURE\|99-CHANGELOG" CLAUDE.md` returns NO matches
- `grep "auth_utils\|require_auth_middleware\|JWT_SECRET_KEY" CLAUDE.md` returns matches for all 3
- `grep "assembleStagingDebug\|assembleProductionRelease\|testStagingDebugUnitTest" CLAUDE.md` returns NO matches (these tasks do not exist)
</verification>

<success_criteria>
1. Zero wrong URLs in CLAUDE.md and Staging.xcconfig
2. Zero wrong emails in CLAUDE.md
3. Zero references to non-existent doc files in CLAUDE.md
4. Android build commands unchanged — assembleDebug/assembleRelease preserved (no product flavors exist)
5. iOS build commands documented with correct workspace, schemes, configs, and restaurant scheme limitation noted
6. Security architecture (auth_utils.py, global middleware, env vars) documented
7. Microservices count is 16
</success_criteria>

<output>
After completion, create `.planning/phases/04-docs-overhaul/04-01-SUMMARY.md`
</output>
