---
phase: 05-android-distribution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/StudioProjects/eatfair-android/partner/build.gradle.kts
autonomous: true
requirements:
  - DIST-06

must_haves:
  truths:
    - "Partner app versionCode is 20 and versionName is 1.0.19"
    - "Partner APK is signed with the release keystore"
    - "Partner APK is uploaded to Firebase App Distribution for tester jeetnair.in@gmail.com"
    - "Partner app points to production API (api.dollor.ai)"
  artifacts:
    - path: "/Users/jeet/StudioProjects/eatfair-android/partner/build.gradle.kts"
      provides: "Bumped version numbers"
      contains: "versionCode = 20"
    - path: "/Users/jeet/StudioProjects/eatfair-android/partner/build/outputs/apk/release/partner-release.apk"
      provides: "Signed release APK"
  key_links:
    - from: "partner/build.gradle.kts"
      to: "Firebase App Distribution"
      via: "firebase appdistribution:distribute CLI"
      pattern: "partner-release.apk"
---

<objective>
Bump Android Partner (Restaurant) app version, build release APK, and upload to Firebase App Distribution.

Purpose: Distribute the latest Partner app (with all API verification fixes from Phase 03) to testers via Firebase.
Output: Partner app vC=20/1.0.19 on Firebase App Distribution.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@/Users/jeet/StudioProjects/eatfair-android/partner/build.gradle.kts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump Partner app version and build release APK</name>
  <files>/Users/jeet/StudioProjects/eatfair-android/partner/build.gradle.kts</files>
  <action>
In `/Users/jeet/StudioProjects/eatfair-android/partner/build.gradle.kts`, update the defaultConfig block:
- Change `versionCode = 19` to `versionCode = 20`
- Change `versionName = "1.0.18"` to `versionName = "1.0.19"`

Verify the file already has `API_BASE_URL` set to `"https://api.dollor.ai/api"` and `IS_PRODUCTION` set to `true`. Do NOT change these -- they are already correct.

Then build the release APK:
```bash
cd /Users/jeet/StudioProjects/eatfair-android && ./gradlew :partner:assembleRelease
```

The APK will be at: `partner/build/outputs/apk/release/partner-release.apk`

If the build fails due to signing, check that `local.properties` has `RELEASE_KEYSTORE_PATH`, `RELEASE_KEYSTORE_PASSWORD`, `RELEASE_KEY_ALIAS`, `RELEASE_KEY_PASSWORD` (or `RELEASE_KEY_ALIAS_PARTNER` for partner-specific key alias) set. Note that partner's build.gradle.kts checks for `RELEASE_KEY_ALIAS_PARTNER` first, then falls back to `RELEASE_KEY_ALIAS`.
  </action>
  <verify>
Run: `ls -la /Users/jeet/StudioProjects/eatfair-android/partner/build/outputs/apk/release/partner-release.apk` -- file must exist.
Run: `grep "versionCode = 20" /Users/jeet/StudioProjects/eatfair-android/partner/build.gradle.kts` -- must match.
Run: `grep 'versionName = "1.0.19"' /Users/jeet/StudioProjects/eatfair-android/partner/build.gradle.kts` -- must match.
  </verify>
  <done>Partner app build.gradle.kts has versionCode=20/versionName=1.0.19, and partner-release.apk exists in build outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Upload Partner APK to Firebase App Distribution</name>
  <files>/Users/jeet/StudioProjects/eatfair-android/partner/build/outputs/apk/release/partner-release.apk</files>
  <action>
Upload the built APK to Firebase App Distribution:
```bash
cd /Users/jeet/StudioProjects/eatfair-android && firebase appdistribution:distribute partner/build/outputs/apk/release/partner-release.apk \
  --app "1:65740760476:android:8591cc17fa4f8d4c42d459" \
  --testers "jeetnair.in@gmail.com" \
  --release-notes "Partner v1.0.19 (vC=20) - API verification fixes from Phase 03" \
  --project dollorai-production
```

Firebase App ID for Partner: `1:65740760476:android:8591cc17fa4f8d4c42d459`
Tester: `jeetnair.in@gmail.com`
Project: `dollorai-production`

If firebase CLI is not authenticated, run `firebase login` first.
  </action>
  <verify>
The firebase command output must show "Distribution complete" or equivalent success message. No error output.
  </verify>
  <done>Partner APK v1.0.19 (vC=20) is uploaded to Firebase App Distribution and tester jeetnair.in@gmail.com has been notified.</done>
</task>

</tasks>

<verification>
1. `grep "versionCode = 20" /Users/jeet/StudioProjects/eatfair-android/partner/build.gradle.kts` returns a match
2. `grep 'api.dollor.ai' /Users/jeet/StudioProjects/eatfair-android/partner/build.gradle.kts` confirms production API URL
3. Firebase App Distribution shows Partner v1.0.19 available for jeetnair.in@gmail.com
</verification>

<success_criteria>
- Partner app versionCode is 20 and versionName is 1.0.19 in build.gradle.kts
- Release APK was built successfully (signed with release keystore)
- APK uploaded to Firebase App Distribution for project dollorai-production
- Tester jeetnair.in@gmail.com notified of new build
</success_criteria>

<output>
After completion, create `.planning/phases/05-android-distribution/05-03-SUMMARY.md`
</output>
