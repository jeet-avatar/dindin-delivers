---
phase: 01-customer-driver-endpoint-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/web/p2p-platform/backend/main_new.py
autonomous: true
requirements:
  - AUTH-02

must_haves:
  truths:
    - "Every driver endpoint in main_new.py rejects requests without a valid driver JWT (401)"
    - "Driver endpoints with driver_id in path verify authenticated driver owns the resource (403 on mismatch)"
    - "Shared ride endpoints (called by both customer and driver apps) use require_any_auth with participant verification"
    - "No driver endpoint uses get_current_user, get_current_driver, or manual oauth2_scheme patterns"
  artifacts:
    - path: "apps/web/p2p-platform/backend/main_new.py"
      provides: "All driver + shared ride endpoints converted to proper auth"
      contains: "Depends(require_driver)"
  key_links:
    - from: "apps/web/p2p-platform/backend/main_new.py"
      to: "apps/web/p2p-platform/backend/auth_utils.py"
      via: "Depends(require_driver) and Depends(require_any_auth) on all driver and shared endpoints"
      pattern: "driver.*Depends\\(require_driver\\)"
---

<objective>
Convert all driver endpoints and shared ride endpoints in main_new.py to standardized auth patterns. Driver-specific endpoints get `Depends(require_driver)`, shared ride endpoints (called by both customer and driver apps) get `Depends(require_any_auth)`. This covers 19 driver endpoints and ~16 shared ride endpoints.

Purpose: Enforce role-specific auth on driver endpoints so customer/vendor JWTs cannot access driver data, and ensure shared ride endpoints have explicit per-endpoint auth instead of relying only on global middleware.
Output: main_new.py with all driver and shared ride endpoints using proper Depends() auth.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-customer-driver-endpoint-auth/01-RESEARCH.md
@.planning/phases/01-customer-driver-endpoint-auth/01-01-SUMMARY.md
@apps/web/p2p-platform/backend/auth_utils.py
@apps/web/p2p-platform/backend/main_new.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert driver endpoints from ad-hoc auth to Depends(require_driver)</name>
  <files>apps/web/p2p-platform/backend/main_new.py</files>
  <action>
Convert all driver-specific endpoints to use `Depends(require_driver)` from auth_utils.py. The import already exists at line 33.

**Group A: get_current_user -> require_driver (4 endpoints, MEDIUM risk)**
Replace `current_user: User = Depends(get_current_user)` with `driver: Driver = Depends(require_driver)`. Update ALL references from `current_user` to `driver`. For endpoints that look up driver by email (`current_user.email`), use `driver` directly since require_driver already returns the Driver object.

- Line ~16192: GET `/api/driver/bids` -- replace auth, use `driver.id` directly instead of deriving driver_id from user email lookup
- Line ~19851: POST `/api/driver/location` -- replace auth, verify driver_id in request body matches `driver.id`; add ownership check: `if data.driver_id != driver.id: raise HTTPException(403, "Access denied")`
- Line ~19879: GET `/api/v2/driver/deliveries/available` -- replace auth, use `driver` directly
- Line ~19969: GET `/api/erp/driver/{driver_id}/deliveries` -- replace auth, add `if driver.id != driver_id: raise HTTPException(403, "Access denied")`

**Group B: get_current_driver -> require_driver (1 endpoint, LOW risk)**
Simple parameter swap -- both return Driver objects.

- Line ~7527: GET `/api/driver/earnings` -- replace `Depends(get_current_driver)` with `Depends(require_driver)`. Rename variable if needed.

**Group C: Manual oauth2_scheme -> require_driver (9 endpoints, HIGH risk)**
Replace `token: str = Depends(oauth2_scheme)` + manual `jwt.decode()` + try/except block with `driver: Driver = Depends(require_driver)`. Remove the entire JWT decode boilerplate. Use `driver.id` for ownership checks instead of `payload.get("driver_id")`.

IMPORTANT: These endpoints currently allow admin access via `if payload.get("role") != "admin"` checks. Per the research recommendation, REMOVE admin-or-owner checks -- admins should use admin-specific endpoints. Convert to pure `require_driver` with ownership check only.

- Line ~3546: DELETE `/api/drivers/{driver_id}/delete` -- add `if driver.id != driver_id: raise HTTPException(403, "Access denied")`
- Line ~4817: POST `/api/drivers/{driver_id}/status` -- add ownership check
- Line ~4860: POST `/api/drivers/{driver_id}/stripe/connect` -- add `if driver.id != driver_id: raise HTTPException(403, "You can only manage your own Stripe account")`
- Line ~4933: GET `/api/drivers/{driver_id}/stripe/onboarding-link` -- add ownership check
- Line ~5012: GET `/api/drivers/{driver_id}/stripe/status` -- add ownership check
- Line ~5096: POST `/api/drivers/{driver_id}/stripe/dashboard-link` -- add ownership check
- Line ~5912: GET `/api/drivers/{driver_id}/payout-history` -- add ownership check
- Line ~14815: GET `/api/drivers/{driver_id}/active-order` -- add ownership check
- Line ~20148: GET `/api/drivers/{driver_id}/earnings` -- add ownership check

For each Group C endpoint:
1. Replace `token: str = Depends(oauth2_scheme)` with `driver: Driver = Depends(require_driver)` in signature
2. Delete the entire `try: payload = jwt.decode(...)` block (5-10 lines)
3. Delete the `driver = db.query(Driver).filter(...)` lookup since require_driver already provides it
4. Add simple ownership check: `if driver.id != driver_id: raise HTTPException(status_code=403, detail="Access denied")`
5. Update function body to use `driver` directly instead of re-querying from DB

**Group D: require_any_auth -- KEEP AS IS (1 endpoint)**
- Line ~4795: GET `/api/drivers/{driver_id}/status` -- keep `Depends(require_any_auth)` since customer apps may check driver status during rides

**ERP proxy endpoints: require_any_auth -> require_driver (2 endpoints)**
- Line ~18185: PUT `/api/erp/drivers/{driver_id}/status` -- replace `Depends(require_any_auth)` with `Depends(require_driver)`, add ownership check
- Line ~18194: PUT `/api/erp/drivers/{driver_id}/location` -- replace `Depends(require_any_auth)` with `Depends(require_driver)`, add ownership check

**FCM token endpoints: get_current_user -> require_driver (2 endpoints)**
- Line ~18240: POST `/api/erp/drivers/{driver_id}/fcm-token` -- replace auth, add ownership check
- Line ~18312: DELETE `/api/erp/drivers/{driver_id}/fcm-token` -- replace auth, add ownership check

DO NOT touch customer endpoints (already done in Plan 01).
DO NOT touch bid_routes.py (Plan 03).
DO NOT remove `get_current_user` or `get_current_driver` function definitions -- they may still be used by vendor/admin endpoints for Phase 02.
  </action>
  <verify>
1. `grep -n "Depends(get_current_driver)" apps/web/p2p-platform/backend/main_new.py | grep -v "def get_current_driver"` should return zero lines
2. Count of `Depends(require_driver)` should increase by ~18 from the 14 already present
3. `cd apps/web/p2p-platform/backend && python -c "from main_new import app; print('Import OK')"` succeeds
  </verify>
  <done>All 18 driver-specific endpoints use Depends(require_driver) with ownership checks where driver_id appears in path. No driver endpoint uses get_current_user, get_current_driver, or manual oauth2_scheme. Admin-or-owner JWT checks removed from driver Stripe/payout endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Convert shared ride endpoints to Depends(require_any_auth)</name>
  <files>apps/web/p2p-platform/backend/main_new.py</files>
  <action>
Convert all shared ride endpoints (called by BOTH customer and driver apps) to use `Depends(require_any_auth)`. These cannot use role-specific guards because both customer and driver apps call the same paths.

**Shared ride endpoints currently using get_current_user (11 endpoints):**
Replace `current_user: User = Depends(get_current_user)` with `_auth: dict = Depends(require_any_auth)`. Since require_any_auth returns a JWT payload dict (not a User object), update any references to `current_user.email` to use `_auth.get("sub")` and `current_user.id` to look up the appropriate record.

For participant verification on rides: use `_auth.get("customer_id")` or `_auth.get("driver_id")` from the JWT payload to verify the caller is a participant of the ride.

- Line ~15653: GET `/api/rides/{ride_id}/track` -- replace auth. Add participant check: verify caller's customer_id or driver_id matches ride's customer_id or driver_id.
- Line ~15801: POST `/api/rides/{ride_id}/cancel` -- replace auth. Verify caller is ride's customer.
- Line ~16018: POST `/api/rides/{ride_id}/rate` -- replace auth. Verify caller is ride's customer (only customers rate).
- Line ~16093: POST `/api/rides/{ride_id}/tip` -- replace auth. Verify caller is ride's customer (only customers tip).
- Line ~15090: POST `/api/erp/rides/{ride_id}/cancel` -- replace auth. Verify caller is participant.
- Line ~15177: POST `/api/erp/rides/{ride_id}/accept-fare` -- replace auth. Verify caller is participant.
- Line ~15209: POST/GET `/api/rides/{ride_id}/negotiate` -- replace auth. Verify caller is participant.
- Line ~15248: POST/GET `/api/erp/rides/{ride_id}/customer-accept-fare` -- replace auth. Verify caller is customer.
- Line ~15275: GET `/api/erp/rides/{ride_id}/negotiation-status` -- replace auth. Verify caller is participant.

**Shared ride endpoints currently using manual oauth2_scheme (5 endpoints):**
Replace `token: str = Depends(oauth2_scheme)` + manual JWT decode with `_auth: dict = Depends(require_any_auth)`. Remove the try/except JWT block.

- Line ~14990: GET `/api/erp/rides/available` -- replace auth. This is driver-only in practice but uses generic path; use `_auth: dict = Depends(require_any_auth)`.
- Line ~15005: POST `/api/erp/rides/{ride_id}/accept` -- replace auth. Use `_auth.get("driver_id")` for the accepting driver.
- Line ~15028: POST `/api/erp/rides/{ride_id}/picked-up` -- replace auth.
- Line ~15041: POST `/api/erp/rides/{ride_id}/start` -- replace auth.
- Line ~15062: GET `/api/erp/rides/{ride_id}/track` (ERP) -- replace auth.
- Line ~15074: GET `/api/erp/rides/{ride_id}/status` (ERP) -- replace auth.
- Line ~15098: POST `/api/erp/rides/{ride_id}/negotiate` (ERP) -- replace auth.

For each endpoint with manual JWT:
1. Replace `token: str = Depends(oauth2_scheme)` with `_auth: dict = Depends(require_any_auth)` in signature
2. Delete the `try: payload = jwt.decode(...)` block
3. Replace `payload.get("driver_id")` with `_auth.get("driver_id")`
4. Replace `payload.get("sub")` with `_auth.get("sub")`

NOTE: Some of these endpoints may already have the manual JWT decode serving a functional purpose (extracting driver_id from token to assign to ride). Preserve that logic by using `_auth.get("driver_id")` or `_auth.get("customer_id")`.

After ALL conversions, verify that `get_current_user` usage in main_new.py is ONLY on vendor/admin endpoints (which are Phase 02 scope). Run: `grep -n "Depends(get_current_user)" main_new.py` and verify every remaining usage is on a non-customer, non-driver path.
  </action>
  <verify>
1. `grep -n "Depends(get_current_user)" apps/web/p2p-platform/backend/main_new.py | grep -v "def get_current_user"` -- should show ONLY vendor/admin endpoints remaining (Phase 02 scope)
2. `grep -n "Depends(oauth2_scheme)" apps/web/p2p-platform/backend/main_new.py` -- should show zero remaining usages (all converted)
3. `cd apps/web/p2p-platform/backend && python -c "from main_new import app; print('Import OK')"` succeeds
4. `cd apps/web/p2p-platform/backend && pytest tests/test_api_contracts.py -x -q 2>&1 | tail -5` -- no new failures
  </verify>
  <done>All ~16 shared ride endpoints use Depends(require_any_auth). No shared ride endpoint uses get_current_user or manual oauth2_scheme. Participant verification added where applicable. The only remaining get_current_user usages in main_new.py are on vendor/admin endpoints (Phase 02 scope).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -n "Depends(require_driver)" apps/web/p2p-platform/backend/main_new.py | wc -l` should show ~32+ lines (14 existing + 18 new)
2. `grep -n "Depends(require_any_auth)" apps/web/p2p-platform/backend/main_new.py | wc -l` should show ~16+ new shared endpoint usages
3. No driver endpoint path (`/api/driver/`, `/api/drivers/`, `/api/erp/drivers/`) uses `get_current_user`, `get_current_driver`, or `oauth2_scheme`
4. `cd apps/web/p2p-platform/backend && python -c "from main_new import app; print('Import OK')"` succeeds
5. `cd apps/web/p2p-platform/backend && pytest tests/ -x -q --timeout=30 2>&1 | tail -10` -- no new failures
</verification>

<success_criteria>
- All 18 driver-specific endpoints in main_new.py use Depends(require_driver)
- All 16 shared ride endpoints use Depends(require_any_auth)
- All driver endpoints with driver_id path parameters have ownership checks
- Manual JWT decode blocks fully removed from all driver endpoints
- Admin-or-owner fallback removed from Stripe/payout endpoints (cleaner security model)
- Only vendor/admin endpoints still use get_current_user (deferred to Phase 02)
- No syntax errors, no NameError from unrenamed variables
</success_criteria>

<output>
After completion, create `.planning/phases/01-customer-driver-endpoint-auth/01-02-SUMMARY.md`
</output>
