---
phase: 02-vendor-admin-endpoint-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/p2p-platform/backend/main_new.py
autonomous: true
requirements: [AUTH-04]
must_haves:
  truths:
    - "Every admin endpoint rejects requests without an admin JWT or ADMIN_SECRET_KEY (returns 401/403)"
    - "Admin endpoints using get_current_user + manual role check are replaced with Depends(require_admin)"
    - "Admin endpoints using manual jwt.decode() are replaced with Depends(require_admin)"
    - "Endpoints with NO auth at all (/api/admin/cleanup-expired-bids, /api/admin/api/duplicates) now require admin auth"
    - "ADMIN_SECRET_KEY-protected endpoints remain unchanged (backfill-payouts, migrate, set-document-status)"
  artifacts:
    - path: "apps/web/p2p-platform/backend/main_new.py"
      provides: "25 admin endpoints with Depends(require_admin)"
      contains: "Depends(require_admin)"
  key_links:
    - from: "main_new.py admin endpoints"
      to: "auth_utils.require_admin"
      via: "Depends(require_admin)"
      pattern: "admin.*Depends\\(require_admin\\)"
---

<objective>
Convert all 25 admin endpoints in main_new.py from ad-hoc auth (get_current_user + role check, manual JWT decode, NO auth) to standardized Depends(require_admin).

Purpose: Satisfy AUTH-04 -- every admin endpoint has per-endpoint Depends(require_admin) role checks, preventing non-admin access at the endpoint level (not just middleware).
Output: main_new.py with all admin endpoints using require_admin from auth_utils.py.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vendor-admin-endpoint-auth/02-RESEARCH.md
@apps/web/p2p-platform/backend/auth_utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert admin endpoints using get_current_user + role check to Depends(require_admin)</name>
  <files>apps/web/p2p-platform/backend/main_new.py</files>
  <action>
Convert all Group B1 admin endpoints (13-15 endpoints) from `get_current_user` + manual admin role check to `Depends(require_admin)`.

**Pattern for each endpoint:**
1. Replace `current_user: User = Depends(get_current_user)` with `admin: User = Depends(require_admin)` in function signature
2. Remove the `if current_user.role != UserRole.ADMIN: raise HTTPException(403, ...)` check -- `require_admin` already handles this
3. Replace any remaining `current_user` references in the function body with `admin`
4. Keep all other function logic unchanged

**Specific endpoints (from research Group B1):**
- Line ~1623: DELETE /api/auth/admin/legacy-cleanup
- Line ~11484: POST /api/admin/vendors/{id}/documents/{type}/approve
- Line ~11533: POST /api/admin/vendors/{id}/documents/{type}/reject
- Line ~11579: POST /api/admin/vendors/{id}/documents/upload
- Line ~11954: POST /api/admin/menu/{id}/approve
- Line ~11988: POST /api/admin/menu/{id}/reject
- Line ~12025: POST /api/admin/menu/{id}/flag
- Line ~12576: POST /api/admin/vendors/{id}/verify-menu
- Line ~12630: POST /api/admin/vendors/{id}/publish
- Line ~12808: POST /api/admin/vendors/{id}/unpublish
- Line ~12852: GET /api/admin/vendors/{id}/publish-checklist
- Line ~12924: GET /api/admin/vendors/all-documents
- Line ~20378: GET /api/admin/drivers
- Line ~20474: POST /api/admin/drivers/{id}/set-documents
- Line ~20529: POST /api/admin/drivers/{id}/verify

**NOTE:** These endpoints are ALSO protected by admin_auth_middleware as a defense-in-depth layer. Adding Depends(require_admin) is the explicit per-endpoint auth required by AUTH-04 and AUTH-06. Both layers can coexist -- this is by design.

**WARNING:** Line numbers are approximate from research. Grep for the endpoint path to find the exact current line before editing.
  </action>
  <verify>
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && grep -n "Depends(require_admin)" main_new.py | wc -l` -- should be >= 20 (existing + new).
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && python -c "import main_new; print('Import OK')"` -- should succeed.
  </verify>
  <done>All 13-15 Group B1 admin endpoints use Depends(require_admin). Manual role checks removed. Function body references updated from current_user to admin.</done>
</task>

<task type="auto">
  <name>Task 2: Convert admin endpoints using manual JWT decode and fix unauthed endpoints</name>
  <files>apps/web/p2p-platform/backend/main_new.py</files>
  <action>
Convert Group B2 (5 endpoints with manual JWT decode), B3 (2 endpoints with missing/incomplete auth), and B4 unauthed endpoint to Depends(require_admin).

**Group B2 -- manual JWT decode (5 endpoints):**
Pattern: Replace `token: str = Depends(oauth2_scheme)` with `admin: User = Depends(require_admin)`. Remove the entire `try: payload = jwt.decode(...)` block (typically 6-10 lines) including the manual User query and role check. Keep function body logic after the auth block.

- Line ~20183: GET /api/admin/rideshare/requests -- remove JWT decode block
- Line ~20281: GET /api/admin/rideshare/active -- remove JWT decode block
- Line ~20572: POST /api/admin/cleanup/pending-orders -- remove JWT decode block, replace `admin_user` variable with `admin`
- Line ~20641: POST /api/admin/cleanup/all-incomplete -- remove JWT decode block, replace `admin_user` variable with `admin`
- Line ~20728: GET /api/admin/database/schema -- remove JWT decode block

**Group B3 -- incomplete/missing auth (2 endpoints):**
- Line ~20782: GET /api/admin/api/routes -- has JWT decode but NO role check. Replace `token: str = Depends(oauth2_scheme)` with `admin: User = Depends(require_admin)`, remove JWT decode block.
- Line ~20828: GET /api/admin/api/duplicates -- has NO AUTH AT ALL. Add `admin: User = Depends(require_admin)` to function signature.

**Group B4 unauthed endpoint:**
- Line ~19213: POST /api/admin/cleanup-expired-bids -- has NO AUTH AT ALL (only protected by admin middleware). Add `admin: User = Depends(require_admin)` to function signature.

**ADMIN_SECRET_KEY endpoints -- DO NOT MODIFY:**
- Line ~523: POST /api/admin/backfill-payouts -- uses ADMIN_SECRET_KEY, keep as-is
- Line ~569: POST /api/admin/migrate -- uses ADMIN_SECRET_KEY, keep as-is
- Line ~11672: POST /api/admin/set-document-status -- uses body-based admin_secret, keep as-is

**IMPORTANT:** For B2 endpoints, the manual JWT decode block is typically:
```python
try:
    payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
    admin_user = db.query(User).filter(User.email == payload.get("sub")).first()
    if not admin_user or admin_user.role != UserRole.ADMIN:
        raise HTTPException(status_code=403, detail="Admin access required")
except JWTError:
    raise HTTPException(status_code=401, ...)
```
This entire block gets replaced by `admin: User = Depends(require_admin)` in the signature. Any references to `admin_user` in the remaining body become `admin`.
  </action>
  <verify>
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && grep -n "token.*Depends(oauth2_scheme)" main_new.py` -- should only show ADMIN_SECRET_KEY endpoints (backfill-payouts, migrate) and possibly public endpoints.
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && grep -n "cleanup-expired-bids\|api/duplicates\|api/routes" main_new.py | head -10` -- verify these endpoints now have require_admin.
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && python -c "import main_new; print('Import OK')"` -- should succeed.
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && pytest tests/ -x -q 2>&1 | tail -5` -- no new failures.
  </verify>
  <done>All 8 remaining admin endpoints (B2+B3+B4) use Depends(require_admin). Manual JWT decode blocks removed. Previously unauthed endpoints (cleanup-expired-bids, api/duplicates) now require admin auth. ADMIN_SECRET_KEY endpoints remain unchanged.</done>
</task>

</tasks>

<verification>
1. `grep -c "Depends(require_admin)" main_new.py` -- count increased by ~23 from pre-Phase-02 baseline
2. No admin endpoint uses manual `jwt.decode()` in its function body (except ADMIN_SECRET_KEY endpoints)
3. POST /api/admin/cleanup-expired-bids and GET /api/admin/api/duplicates now have explicit auth
4. `python -c "import main_new"` succeeds
5. `pytest tests/ -x -q` passes with no new failures
</verification>

<success_criteria>
- All 25 admin endpoints converted to Depends(require_admin)
- Manual JWT decode blocks removed from 7 endpoints
- 3 previously unauthed endpoints now require admin auth
- ADMIN_SECRET_KEY endpoints unchanged
- Defense-in-depth: admin_auth_middleware + per-endpoint Depends(require_admin) coexist
- Tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-vendor-admin-endpoint-auth/02-02-SUMMARY.md`
</output>
