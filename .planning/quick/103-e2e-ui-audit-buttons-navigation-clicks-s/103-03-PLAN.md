---
phase: quick-103
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/103-e2e-ui-audit-buttons-navigation-clicks-s/UI_AUDIT_ANDROID.md
autonomous: true
requirements: [QUICK-103]

must_haves:
  truths:
    - "Every button and onClick handler in the Android Customer app maps to a real action"
    - "Every button and onClick handler in the Android Driver app maps to a real action"
    - "Every button and onClick handler in the Android Partner app maps to a real action"
    - "Every bottom nav item in all 3 Android apps navigates to the correct screen"
    - "Phase 10 Android features (OrderChat, LiveChat) are correctly wired"
  artifacts:
    - path: ".planning/quick/103-e2e-ui-audit-buttons-navigation-clicks-s/UI_AUDIT_ANDROID.md"
      provides: "Audit findings for all 3 Android apps"
      contains: "## Android Customer App"
  key_links:
    - from: "Android Customer onClick handlers"
      to: "Backend API endpoints"
      via: "ApiService Retrofit calls"
      pattern: "@(GET|POST|PUT|DELETE).*api/"
    - from: "Android bottom nav items"
      to: "Screen composables"
      via: "NavGraph routes"
      pattern: "composable\\("
---

<objective>
Audit every button, navigation link, and data flow in all 3 Android apps (Customer ~30 screens, Driver ~20 screens, Partner ~20 screens) in the separate eatfair-android repo.

Purpose: Ensure no dead buttons, broken navigation, or disconnected API calls exist in the Android apps before next Play Store submission.
Output: Audit report for all 3 Android apps. NOTE: This plan does NOT modify Android source files -- any fixes found will be handled by a follow-up quick task in the eatfair-android repo.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static code audit of Android Customer app</name>
  <files>
    .planning/quick/103-e2e-ui-audit-buttons-navigation-clicks-s/UI_AUDIT_ANDROID.md
  </files>
  <action>
Audit the Android Customer app in the separate repo at `/Users/jeet/StudioProjects/eatfair-android/app/`.

1. Read MainScreen.kt (or equivalent) for bottom navigation structure.
2. For each screen composable in `app/src/main/java/.../customer/`, trace every:
   - Button onClick
   - IconButton onClick
   - clickable modifier
   - LaunchedEffect API calls
   - ViewModel function calls that trigger API requests
3. Cross-reference ApiService.kt (Retrofit interface) for each endpoint called -- verify it exists in the backend:
   ```bash
   grep -rn "the/endpoint/path" /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend/main_new.py /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend/routers/*.py
   ```
4. Check Phase 10 features: OrderChatScreen, LiveChatScreen, AI feature hiding (SHOW_AI_FEATURES).

**Categorize as:** DEAD, MISSING, WRONG_TARGET, or OK.

Write findings to the **Android Customer** section of UI_AUDIT_ANDROID.md.
  </action>
  <verify>
    grep -c "## Android Customer" .planning/quick/103-e2e-ui-audit-buttons-navigation-clicks-s/UI_AUDIT_ANDROID.md shows the section exists with findings.
  </verify>
  <done>
    Android Customer app section complete in audit report with every button/navigation/form traced. Each finding has file path, line number, and category.
  </done>
</task>

<task type="auto">
  <name>Task 2: Static code audit of Android Driver and Partner apps</name>
  <files>
    .planning/quick/103-e2e-ui-audit-buttons-navigation-clicks-s/UI_AUDIT_ANDROID.md
  </files>
  <action>
Audit the Android Driver and Partner apps at `/Users/jeet/StudioProjects/eatfair-android/`.

**Android Driver App** (`driver/src/main/java/.../driver/` — ~20 screens):
1. Read DriverNavGraph.kt for route definitions.
2. For each screen composable, trace every Button onClick, IconButton onClick, clickable modifier, LaunchedEffect, ViewModel calls.
3. Verify delivery flow: accept -> pickup -> deliver (correct PUT endpoints in ApiService).
4. Verify rideshare flow: view available rides -> bid -> start -> complete.
5. Verify swipe-to-confirm patterns, ride chat, order chat, delivery proof.
6. Cross-reference each ApiService endpoint against backend (grep to verify existence).

**Android Partner App** (`partner/src/main/java/.../partner/` — ~20 screens):
1. Read PartnerNavGraph.kt for route definitions.
2. For each screen composable, trace every handler.
3. Verify order management: accept -> preparing -> ready.
4. Verify menu management: add/edit/delete items.
5. Check AI feature visibility: SHOW_AI_FEATURES flag.
6. Cross-reference each ApiService endpoint against backend.

**Categorize as:** DEAD, MISSING, WRONG_TARGET, or OK.

Append findings to the **Android Driver** and **Android Partner** sections of UI_AUDIT_ANDROID.md. Add a final summary table at the top of the file covering all 3 Android apps.

**NOTE:** This is a READ-ONLY audit of the Android repo. Do NOT modify any Android source files. If issues are found, they will be listed in the report for a follow-up quick task.
  </action>
  <verify>
    grep -c "## Android Driver\|## Android Partner" .planning/quick/103-e2e-ui-audit-buttons-navigation-clicks-s/UI_AUDIT_ANDROID.md shows both sections exist. grep -c "DEAD\|MISSING\|WRONG_TARGET\|OK" on the report shows categorized findings.
  </verify>
  <done>
    Audit report covers all 3 Android apps with every button/navigation/form traced. Summary table at top shows findings per app and per category. Any issues are documented for follow-up -- no Android source files modified.
  </done>
</task>

</tasks>

<verification>
1. UI_AUDIT_ANDROID.md exists with sections for all 3 Android apps
2. Each finding has file path, line number, and category (OK/DEAD/MISSING/WRONG_TARGET)
3. Summary table shows total findings across all 3 apps
4. No Android source files were modified (read-only audit)
</verification>

<success_criteria>
- Complete audit report for all 3 Android apps with file:line references
- Clear categorization of every button, tab, navigation, and form handler
- Issues documented for follow-up quick task (no source modifications in this plan)
</success_criteria>

<output>
After completion, create `.planning/quick/103-e2e-ui-audit-buttons-navigation-clicks-s/103-03-SUMMARY.md`
</output>
