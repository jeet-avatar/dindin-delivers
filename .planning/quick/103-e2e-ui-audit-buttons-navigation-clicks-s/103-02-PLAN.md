---
phase: quick-103
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/103-e2e-ui-audit-buttons-navigation-clicks-s/UI_AUDIT_IOS_DRIVER_RESTAURANT.md
  - apps/web/p2p-platform/backend/tests/e2e/test_driver_vendor_ui_wiring_e2e.py
autonomous: true
requirements: [QUICK-103]

must_haves:
  truths:
    - "Every button and tap handler in the iOS Driver app maps to a real action"
    - "Every button and tap handler in the iOS Restaurant app maps to a real action"
    - "Every tab bar item in Driver and Restaurant apps navigates to the correct screen"
    - "Driver delivery flow (accept/pickup/deliver) and rideshare flow (bid/accept/navigate) are correctly wired"
    - "Restaurant order management (accept/prepare/ready) and menu management are correctly wired"
    - "Backend E2E tests cover the driver and vendor user journeys"
  artifacts:
    - path: ".planning/quick/103-e2e-ui-audit-buttons-navigation-clicks-s/UI_AUDIT_IOS_DRIVER_RESTAURANT.md"
      provides: "Audit findings for iOS Driver and Restaurant apps"
      contains: "## iOS Driver App"
    - path: "apps/web/p2p-platform/backend/tests/e2e/test_driver_vendor_ui_wiring_e2e.py"
      provides: "E2E tests covering driver and vendor user flows"
      exports: ["TestDriverFullFlow", "TestVendorFullFlow", "TestRideshareDriverFlow"]
  key_links:
    - from: "iOS Driver button handlers"
      to: "Backend API endpoints"
      via: "P2PAPIService HTTP calls"
      pattern: "POST|GET|PUT.*api/"
    - from: "iOS Restaurant button handlers"
      to: "Backend API endpoints"
      via: "P2PAPIService HTTP calls"
      pattern: "POST|GET|PUT.*api/"
---

<objective>
Audit every button, navigation link, and data flow in the iOS Driver app (~17 views) and iOS Restaurant app (~12 views), then write backend E2E tests covering the driver and vendor user journeys.

Purpose: Ensure no dead buttons, broken navigation, or disconnected API calls exist in the driver and restaurant apps before next App Store submission.
Output: Audit report for iOS Driver + Restaurant + E2E test suite for driver/vendor flows.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/ios/delivery/eatffairdelivery/Views/
@apps/ios/restaurant/eatffairrestaurant/Views/
@apps/web/p2p-platform/backend/tests/e2e/test_critical_flows.py
@apps/web/p2p-platform/backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static code audit of iOS Driver and Restaurant apps</name>
  <files>
    .planning/quick/103-e2e-ui-audit-buttons-navigation-clicks-s/UI_AUDIT_IOS_DRIVER_RESTAURANT.md
  </files>
  <action>
Audit the iOS Driver and Restaurant apps by reading every view/screen file and tracing button handlers, navigation links, and API calls.

**iOS Driver App** (`apps/ios/delivery/eatffairdelivery/Views/` — ~17 views):
1. Read main view for tab structure.
2. For each view file, trace every Button action, NavigationLink destination, .sheet/.fullScreenCover, .onTapGesture, .swipeActions, .refreshable, .onSubmit handler.
3. Verify delivery flow wiring: accept order -> pickup -> deliver (correct PUT endpoints).
4. Verify rideshare flow wiring: view available rides -> bid -> accept -> start -> complete (correct POST/PUT endpoints).
5. Verify OrderChatView wiring: calls /api/orders/{id}/chat/messages GET and POST.
6. Verify driver status toggle, earnings view, location update calls.

**iOS Restaurant App** (`apps/ios/restaurant/eatffairrestaurant/Views/` — ~12 views):
1. Read main view for tab/navigation structure.
2. For each view file, trace every handler.
3. Verify order management wiring: accept -> preparing -> ready (correct PUT endpoints).
4. Verify menu management: add/edit/delete menu items (correct POST/PUT/DELETE endpoints).
5. Verify KOT settings, dashboard stats, earnings view.
6. Check AI feature hiding: #if ENABLE_AI_EMPLOYEES guard on unreleased features.

**For every P2PAPIService method called, verify the backend endpoint exists:**
```bash
grep -rn "the/endpoint/path" apps/web/p2p-platform/backend/main_new.py apps/web/p2p-platform/backend/routers/*.py
```

**Categorize as:** DEAD, MISSING, WRONG_TARGET, or OK (same criteria as Plan 01).

Write to UI_AUDIT_IOS_DRIVER_RESTAURANT.md with summary table at top.
  </action>
  <verify>
    The audit report exists and contains sections for both Driver and Restaurant apps with file:line references. Run: grep -c "DEAD\|MISSING\|WRONG_TARGET\|OK" .planning/quick/103-e2e-ui-audit-buttons-navigation-clicks-s/UI_AUDIT_IOS_DRIVER_RESTAURANT.md
  </verify>
  <done>
    Audit report covers both iOS Driver (~17 views) and Restaurant (~12 views) apps with every button/navigation/form traced. Each finding has file path, line number, and category. Summary table shows totals per app and per category.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write backend E2E tests covering driver and vendor user journeys</name>
  <files>
    apps/web/p2p-platform/backend/tests/e2e/test_driver_vendor_ui_wiring_e2e.py
  </files>
  <action>
Using the audit report from Task 1 and existing test patterns, write E2E tests that exercise every backend API endpoint that the iOS Driver and Restaurant apps call. Use FastAPI TestClient.

**Test classes:**

1. **TestDriverFullFlow**: Register driver -> login -> go online (PUT /api/drivers/me/status) -> view available orders (GET /api/deliveries/available) -> accept delivery (POST /api/deliveries/{id}/accept) -> pickup (PUT /api/deliveries/{id}/pickup) -> deliver (PUT /api/deliveries/{id}/deliver) -> view earnings (GET /api/drivers/me/earnings) -> order chat (GET+POST /api/orders/{id}/chat/messages)

2. **TestRideshareDriverFlow**: Go online -> view available rides (GET /api/rides/available) -> bid (POST /api/rides/{id}/bid) -> start ride (PUT /api/rides/{id}/start) -> complete ride (PUT /api/rides/{id}/complete) -> view ride earnings

3. **TestVendorFullFlow**: Register vendor -> login -> view dashboard (GET /api/vendors/me/dashboard) -> manage menu (GET+POST+PUT /api/vendors/me/menu) -> accept order (PUT /api/orders/{id}/accept) -> mark preparing -> mark ready -> view earnings (GET /api/vendors/me/earnings)

**IMPORTANT: Before writing ANY test endpoint call, verify the endpoint exists:**
```bash
grep -rn "the/endpoint/path" apps/web/p2p-platform/backend/main_new.py apps/web/p2p-platform/backend/routers/*.py
```

Use conftest.py fixtures. Each test asserts correct status codes, response fields, state persistence, auth requirements.

Target: 12-18 test cases covering driver+rideshare+vendor flows.
  </action>
  <verify>
    cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && python -m pytest tests/e2e/test_driver_vendor_ui_wiring_e2e.py -v --tb=short 2>&1 | tail -30
  </verify>
  <done>
    E2E test file has 12+ test cases covering driver delivery, rideshare driver, and vendor flows. All tests pass (or failures documented as backend bugs). Every API endpoint called by the iOS Driver and Restaurant apps has at least one test.
  </done>
</task>

</tasks>

<verification>
1. UI_AUDIT_IOS_DRIVER_RESTAURANT.md exists with categorized findings for both apps
2. test_driver_vendor_ui_wiring_e2e.py has 12+ passing test cases
3. Existing backend test suite still passes: pytest tests/ -v (no regressions)
</verification>

<success_criteria>
- Complete audit report for iOS Driver and Restaurant apps with file:line references
- Backend E2E tests exercising every API endpoint those two apps call
- Zero regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/quick/103-e2e-ui-audit-buttons-navigation-clicks-s/103-02-SUMMARY.md`
</output>
