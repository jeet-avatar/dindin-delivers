---
phase: quick-128 (not started)
task: 0
total_tasks: 3
status: not_started
last_updated: 2026-03-10T05:59:33.402Z
---

<current_state>
Session completed 3 quick tasks (125-127) + ticketing system setup. Quick-128 was initialized (directory created) but NO work done — no plan, no execution. Resume by running the full quick-128 task.
</current_state>

<completed_work>

### This Session (2026-03-10)

- **Quick-125 [pre-ticketing]**: Wire promotion system into payment flow
  - Added `discount_amount`, `promo_code`, `promo_type` to Order model
  - `CreateOrderRequest` accepts `promo_code`, validates against built-in + DB promos
  - Discount subtracted from total; vendor absorbs discount, platform keeps $2 flat
  - Customer receipt email shows discount line; vendor email shows payout breakdown
  - New driver earnings email (`send_delivery_completed_driver_email`)
  - Featured deals from real Promotion table (not fake templates)
  - Deployed staging + production. 1489 tests green.
  - Commits: `c4b60252`, `35cebeb9`

- **Quick-126 [CR-0002]**: Test promotion system E2E + deploy staging
  - 1489 passed, 0 failed. Promo math verified. Staging deployed (CI/CD run 22888129870).
  - Commit: `5024e8ef`

- **Quick-127 [CR-0003]**: Fix 3 restaurant self-delivery gaps
  - `leaveAtDoor: Bool?` added to P2PVendorOrder + Order model
  - MapKit MapView with restaurant + customer pins + "Navigate to Customer" button
  - Orange "LEAVE AT DOOR" badge + blue delivery instructions callout
  - Restaurant app BUILD SUCCEEDED (simulator). NOT yet on TestFlight.
  - Commits: `0a38c974`, `fdb99ad0`

- **Ticketing system setup** (`9f2af04b`):
  - `branching_strategy: "phase"` in .planning/config.json
  - `.agents/skills/ticketed-task/SKILL.md` — GSD executors auto-create CR tickets
  - CLAUDE.md updated with mandatory CR ticket rule
  - `.planning/TASK_PROMPT_TEMPLATE.md` for reusable prompts

</completed_work>

<remaining_work>

### Quick-128: Clean stale orders + build restaurant apps + audit parity (NOT STARTED)

Directory: `.planning/quick/128-clean-stale-orders-build-restaurant-apps/`

1. **Create CR ticket** via admin API (per ticketed-task skill)
2. **Clean stale pending orders** — cancel orders stuck >24h in pending states
3. **Check latest build versions** (MEMORY.md says iOS 185, Android vC=29 from Mar 6)
4. **Build iOS restaurant** → archive + TestFlight (new build 186+)
5. **Build Android partner** → assembleRelease + Firebase distribute (vC=30+)
6. **Audit iOS vs Android feature parity** for restaurant app
   - iOS has: MapView self-delivery, leave-at-door badge, instructions callout (quick-127)
   - Android likely MISSING these → need to add for parity
   - Also check: order chat, support, KOT, earnings, menu management
7. **Deploy backend** if any changes needed (`gh workflow run deploy-staging.yml --ref main`)
8. **Transition CR** through stages: In Progress → Staging → Production → Verified

### MUST follow for build/release/deploy:
- Create CR ticket BEFORE starting (`.agents/skills/ticketed-task/SKILL.md`)
- iOS upload: ExportOptions.plist handles upload (no separate xcrun altool)
- Android distribute: Firebase App Distribution to jeetnair.in@gmail.com
- Deploy: ONLY via `gh workflow run` — NEVER manual docker/ecs
- Post-build: notify jeetnair.in@gmail.com as tester

</remaining_work>

<decisions_made>
- Vendor absorbs promo discount; platform keeps $2/order flat
- Branching: "phase" for full phases (branches/PRs); quick tasks commit to main with CR tickets
- iOS Customer app (build 1111) APPROVED — DO NOT touch
- Restaurant app can be rebuilt freely
- SourceKit "No such module EatFairShared" is pre-existing IDE diagnostic, not real error
</decisions_made>

<blockers>
None.
</blockers>

<context>
This session wired promotions into the real money flow and fixed restaurant self-delivery UX. The user caught us committing without tickets — we set up CR ticketing (CR-0002, CR-0003 created successfully). Next task is getting fresh restaurant builds out with the self-delivery improvements and ensuring Android parity.

Current build versions in MEMORY.md (Mar 6): iOS Restaurant 185, Android Partner vC=29. Both need new builds with latest changes.
</context>

<next_action>
Resume with:
```
/gsd:quick Clean up stale pending orders on restaurant, build new iOS restaurant to TestFlight + Android partner to Firebase, audit iOS vs Android feature parity. MUST create CR ticket first.
```

First steps:
1. Create CR ticket via admin API
2. Query + cancel stale pending orders on production
3. Build iOS restaurant: archive + TestFlight upload
4. Build Android partner: assembleRelease + Firebase distribute
5. Audit feature parity, add missing features to Android
6. Transition CR through deploy stages
</next_action>
