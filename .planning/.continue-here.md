---
phase: quick-142 (pending)
task: 0
total_tasks: 3
status: not_started
last_updated: 2026-03-10T22:19:34.183Z
---

<current_state>
Quick tasks 138-140 COMPLETE. Quick-142 (self-delivery navigation flow) is PLANNED but NOT executed. The plan and deep research are ready. Order 269 (DOLL2026269) is live on production at Apple Restaurant for testing the flow.
</current_state>

<completed_work>

### This Session (2026-03-10, continued)

- **Quick-138 [CR-0011]**: Fix 5 delivery notification gaps (GAP-1 through GAP-5)
  - GAP-1: Customer push at order placement (order_flow.py)
  - GAP-2: Customer push "Your food is ready!" at ready_for_pickup
  - GAP-3: Customer push "Out for delivery!" in both update_order_status endpoints
  - GAP-4: "Your Order Has Arrived" vs "Driver Has Arrived" for self-delivery
  - GAP-5 backend: New `POST /api/erp/orders/{id}/vendor-arrived-at-delivery` endpoint
  - GAP-5 iOS: `markArrivedAtDelivery()` in P2PAPIService + OrdersViewModel + "I've Arrived" button
  - GAP-5 Android: `vendorArrivedAtDelivery()` in DollorApiService + DollorRepository + OrdersViewModel + "I've Arrived" button
  - Backend tests: 1490 passed, 0 failed
  - Commits: `7d808b60`, `08c8cad3` (android), `140f31a1`, `f14e320c`

- **Quick-139**: Full backend test suite verification
  - 1490 passed, 0 failed, 11 skipped, 18 warnings
  - Quick-138 notification fixes: zero regressions
  - Commits: `dcd9c962`, `656c9bcf`

- **Quick-140**: Build iOS Restaurant 187 + Android Partner vC=31
  - iOS Restaurant build 187 → TestFlight UPLOADED
  - Android Partner vC=31 (v1.0.30) → Firebase DISTRIBUTED to jeetnair.in@gmail.com
  - Commits: `b8aeeb43`, `2d20b3db`, `c1a9c0d6`, `fb940628` (android)

- **Test order created**: Order 269 (DOLL2026269) at Apple Restaurant
  - Address: 12 Teberry, Rancho Santa Margarita, CA 92688
  - Coords: 33.6409, -117.6031
  - Status: `pending_restaurant` (waiting for restaurant acceptance)
  - Customer: Demo Customer (demo.customer@dollor.ai)

</completed_work>

<remaining_work>

### Quick-142: Self-Delivery Navigation Flow (READY TO EXECUTE)

Plan exists at: `.planning/quick/142-self-delivery-navigation-flow-reusing-dr/142-PLAN.md`

**Deep research completed** on both iOS Driver and Android Driver delivery flows. Key findings:

#### iOS Restaurant changes needed:
1. **Split `restaurant_will_deliver` section** in EnhancedDashboardView.swift into two phases:
   - `restaurant_will_deliver` status: Show "Start Delivery" button + address preview only
   - `out_for_delivery` status (new else-if): Show map + Navigate + I've Arrived + Photo & Delivered
2. **Add `startDelivery(_ order:)` method** to OrdersViewModel.swift — calls `updateOrderStatus("out_for_delivery")`
3. **Add `onStartDelivery` callback** to EnhancedOrderCard
4. **Navigate button**: Add Google Maps fallback like driver app:
   ```swift
   let googleMapsURL = URL(string: "comgooglemaps://?daddr=\(lat),\(lon)&directionsmode=driving")
   // Try Google Maps first, fall back to Apple Maps
   ```
5. **Add "comgooglemaps" to LSApplicationQueriesSchemes** in Info.plist

#### Android Partner changes needed:
1. **Add map + navigation** to the `isOutForDelivery` section in OrdersScreen.kt
2. **"Navigate to Customer" button** using Google Maps intent:
   ```kotlin
   val gmmUri = Uri.parse("google.navigation:q=${lat},${lng}")
   context.startActivity(Intent(Intent.ACTION_VIEW, gmmUri).apply { setPackage("com.google.android.apps.maps") })
   ```
3. **Show customer address + delivery instructions** prominently during delivery
4. Check VendorOrder model for delivery lat/lng fields

#### After code changes:
- Build iOS Restaurant 188 → TestFlight
- Build Android Partner vC=32 → Firebase
- Test with order 269 on production

### Other pending items:
- Deploy backend to staging + production (Quick-138 notification fixes still not deployed)
- iOS Customer build 1111 still PENDING_DEVELOPER_RELEASE on App Store
- Accept order 269 from vendor side to test delivery flow

</remaining_work>

<decisions_made>
- Created separate `vendor-arrived-at-delivery` endpoint (not reusing `driver-arrived-at-delivery`) because vendor auth != driver auth
- Self-delivery flow should match driver delivery flow: sequential Start → Navigate → Arrived → Photo → Complete
- Reuse driver app navigation patterns (Google Maps → Apple Maps fallback on iOS, Google Maps intent on Android)
- No location tracking for restaurant self-delivery (unlike driver continuous GPS updates)
- Backend already has all needed endpoints — this is purely a client-side UI enhancement
</decisions_made>

<blockers>
- None — plan and research are complete, ready to execute
</blockers>

<context>
User tested the self-delivery flow and noticed there's no navigation/directions to help the restaurant reach the customer's address. The iOS Restaurant app currently shows all buttons at once (Navigate, I've Arrived, Photo & Delivered) with no sequential flow. Android Partner has buttons but no map or navigation at all.

The fix is to create a proper step-by-step delivery flow matching what drivers see:
1. Food ready → "Start Delivery" button
2. Driving → Map + Navigate button (opens Apple Maps/Google Maps for turn-by-turn directions)
3. At customer → "I've Arrived" button (sends customer push notification)
4. Handoff → Take photo + "Complete Delivery"

Full research on driver delivery flows (iOS + Android) completed with line-by-line analysis. The driver apps have ActiveDeliveryDetailView (iOS) and ActiveDeliveryScreen (Android) with maps, polylines, navigate buttons, swipe-to-confirm, and photo proof — all ready to reference for the restaurant implementation.

Build versions entering next session:
- iOS: Customer 1113, Driver 215, Restaurant 187 (TestFlight)
- Android: Customer vC=37, Driver vC=33, Partner vC=31 (Firebase)
</context>

<next_action>
Resume with:
```
/gsd:quick Execute Quick-142: Self-delivery navigation flow. Plan at .planning/quick/142-self-delivery-navigation-flow-reusing-dr/142-PLAN.md. Research complete. Split iOS restaurant_will_deliver into Start Delivery + out_for_delivery phases. Add map+navigation to Android Partner. Build iOS Restaurant 188 + Android Partner vC=32. Only restaurant apps — no customer or driver changes.
```

First steps:
1. Read iOS EnhancedDashboardView.swift lines 1036-1215 (current restaurant_will_deliver section)
2. Split into two phases: restaurant_will_deliver → Start Delivery button, out_for_delivery → map+navigate+arrived+photo
3. Add startDelivery() to OrdersViewModel
4. Read Android OrdersScreen.kt lines 1095-1290 (current self-delivery section)
5. Add map + Navigate button + address card to isOutForDelivery section
6. Commit, build, distribute both apps
7. Test with order 269 on production
</next_action>
