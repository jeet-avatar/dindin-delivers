---
phase: 35-editable-cad-drawings-part-management
plan: 05
type: execute
wave: 4
depends_on: ["35-04", "35-02"]
files_modified:
  - /Users/jeet/turion-space-demo/satellite/part.html
  - /Users/jeet/turion-space-demo/satellite/instance.html
autonomous: true
requirements: [DrawingEditor]
must_haves:
  truths:
    - "part.html has an 'Edit drawing' control near the .cad-frame (2D mode) that opens window.svgEditor with the part's drawing_svg; Save → PATCH /api/parts/:id/drawing → the 2D view re-renders with the new SVG (no full reload needed)"
    - "part.html has a 'Revert to generated' affordance (confirm → POST /api/parts/:id/drawing/regenerate → re-render the 2D view from the returned drawing_svg)"
    - "instance.html links/launches into part.html's edit mode (deep-link part.html?edit=drawing) — there's a reachable path from the instance page"
    - "part.html shows a '⚠ retired part' banner when GET /api/parts/:id returns retired_at"
    - "All new controls are wired via addEventListener (zero inline onclick); button audit stays 0 violations"
  artifacts:
    - path: /Users/jeet/turion-space-demo/satellite/part.html
      provides: "Edit-drawing control + Revert-to-generated + svgEditor mount + ?edit=drawing auto-open + retired banner"
      contains: "svgEditor"
    - path: /Users/jeet/turion-space-demo/satellite/instance.html
      provides: "link/launch into part.html?edit=drawing"
      contains: "edit=drawing"
  key_links:
    - from: /Users/jeet/turion-space-demo/satellite/part.html
      to: /Users/jeet/turion-space-demo/satellite/svg-editor.js
      via: "window.svgEditor.open(drawing_svg, {onSave, onRevert})"
      pattern: "svgEditor\\.open"
    - from: /Users/jeet/turion-space-demo/satellite/part.html
      to: "/api/parts/:id/drawing"
      via: "satelliteApi.patch on Save"
      pattern: "drawing"
---

<objective>
Wire the drawing editor into the part page (and make it reachable from the instance page): an "Edit drawing" control on `part.html`'s 2D `.cad-frame` that opens `window.svgEditor`, saves via `PATCH /api/parts/:id/drawing`, and re-renders the 2D view; a "Revert to generated" affordance that calls the regenerate route; a retired-part banner; and a deep-link from `instance.html`.

Purpose: This is requirement DrawingEditor's user-facing half. The 3D viewer is untouched — `mount3DViewer` re-derives from `dimensions_mm` only and never parses `drawing_svg`, so editing the SVG body changes nothing 3D-side (no re-mount needed; only a *dimensions* edit, which is 35-06, triggers a `location.reload()`).
Output: `part.html` + `instance.html` edits.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-editable-cad-drawings-part-management/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Edit-drawing + Revert-to-generated controls to part.html</name>
  <files>/Users/jeet/turion-space-demo/satellite/part.html</files>
  <action>
Read `satellite/part.html` carefully (the `.cad-frame` block: `#cadFrame`, `#toggleCallouts`, `#viewToggle`/`#viewToggle` chips, `#viewer3d`, `.cad-hud`; the inline IIFE that renders `drawing.drawing_svg` into `#cadFrame`; the `mount3DViewer(document.getElementById('viewer3d'), part, {...})` call; the page's data-load — `GET /api/parts/:id` and `GET /api/parts/:id/drawing` or similar). Add a `<script src="/satellite/svg-editor.js"></script>` line (before the inline IIFE, after `satellite-api.js`/`satellite-render.js`). Then in the inline IIFE:

1. Add an `#editDrawingBtn` chip/button alongside the existing `.cad-frame` controls (only visible/enabled in 2D mode — if the page is in 3D mode, either disable it or have the click first switch to 2D via the existing toggle). Wire with `addEventListener('click', ...)`: it calls `window.svgEditor.open(currentDrawingSvg, { onSave, onRevert })` where:
   - `currentDrawingSvg` = the SVG string the page currently has loaded for this part.
   - `onSave: async (svgString) => { try { await satelliteApi.patch('/api/parts/' + partId + '/drawing', { drawing_svg: svgString }); /* re-render */ document.getElementById('cadFrame').innerHTML = svgString; r.toast('Drawing saved'); } catch(e){ r.toast('Save failed: ' + (e && e.status || 'error')); } }` — (re-injecting the SVG string into `#cadFrame` is acceptable here since it's our own freshly-saved markup; if `part.html` already has a `renderDrawing(svg)` helper, call that instead).
   - `onRevert: async () => { if (!confirm('Revert to the auto-generated drawing? Your edits will be lost.')) return; try { const res = await satelliteApi.post('/api/parts/' + partId + '/drawing/regenerate', {}); document.getElementById('cadFrame').innerHTML = res.drawing_svg; currentDrawingSvg = res.drawing_svg; r.toast('Reverted to generated'); } catch(e){ r.toast('Regenerate failed'); } }`
2. Add a standalone `#revertDrawingBtn` near the `.cad-frame` too (same `onRevert` behavior) for users who want to revert without opening the editor — `addEventListener('click', ...)`.
3. `?edit=drawing` auto-open: if `r.getQueryParam('edit') === 'drawing'` (after the drawing has loaded), programmatically click `#editDrawingBtn` (or call `svgEditor.open(...)` directly). This is the target the instance-page deep-link uses.
4. Retired banner: when `GET /api/parts/:id` returns a non-null `retired_at`, render a visible "⚠ This part is retired" banner above the part header (`createElement` + `textContent`); also disable `#editDrawingBtn`/`#revertDrawingBtn` in that case (don't edit a retired part).

ALL wiring `addEventListener`; no inline `onclick=`. Use `textContent` for the banner. `node --check` doesn't apply to HTML, but a JS syntax error in the inline `<script>` will break the page — keep it tidy.
  </action>
  <verify>`grep -n "svgEditor.open\|editDrawingBtn\|revertDrawingBtn\|retired" satellite/part.html` shows the additions; `grep -c "onclick=" satellite/part.html` did not increase (existing allowlisted onclicks only); `node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` → `violations: 0`.</verify>
  <done>part.html: #editDrawingBtn opens svgEditor (2D mode), Save PATCHes /api/parts/:id/drawing + re-renders #cadFrame, #revertDrawingBtn + onRevert POST /api/parts/:id/drawing/regenerate + re-render, ?edit=drawing auto-opens, retired_at → banner + disabled controls; all addEventListener; audit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Add the deep-link from instance.html into part.html's edit mode</name>
  <files>/Users/jeet/turion-space-demo/satellite/instance.html</files>
  <action>
Read `satellite/instance.html` (it already has the `.cad-frame` 2D/3D toggle and a link to the part page — find where it renders the part identity / the "Manufacturing / Procurement" panel from 29260a0). Add an "✏ Edit drawing" link/button near the `.cad-frame` (or in the part-identity area) that navigates to `part.html?id=<partDefId>&edit=drawing` (preserve any `?sat=` param if `part.html` uses it). Wire with `addEventListener('click', () => { location.href = 'part.html?id=' + partDefId + '&edit=drawing' + (satId ? '&sat=' + satId : ''); })` — NOT an inline `onclick`, and NOT a bare `<a href>` with a templated href built at render time unless that's already the page's pattern (an `<a>` whose `href` is set via `el.href = ...` in JS is fine and audit-clean). Also: if `GET /api/parts/:id` for this instance's definition returns `retired_at`, show a "⚠ retired part" badge on the instance page (the kanban/tree do the same per 35-03). `node`/HTML tidiness as above.
  </action>
  <verify>`grep -n "edit=drawing\|retired" satellite/instance.html` shows the link + badge; `grep -c "onclick=" satellite/instance.html` did not increase; `node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` → `violations: 0`.</verify>
  <done>instance.html has an "Edit drawing" affordance navigating to part.html?id=…&edit=drawing, and a retired-part badge when applicable; addEventListener-wired; audit 0.</done>
</task>

</tasks>

<verification>
- `node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` → `violations: 0`.
- `grep -c "onclick=" satellite/part.html satellite/instance.html` unchanged from before this plan.
- Visual/functional check is deferred to the W6 headless-substitute checkpoint (per Phases 27-34).
</verification>

<success_criteria>
- `part.html`: Edit-drawing control opens `svgEditor`, Save → `PATCH /api/parts/:id/drawing` → 2D re-render; Revert → `POST /api/parts/:id/drawing/regenerate` → re-render; `?edit=drawing` auto-opens; retired banner; all `addEventListener`.
- `instance.html`: deep-link into `part.html?id=…&edit=drawing`; retired badge.
- Button audit 0 violations.
- Committed in turion-space-demo with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar"` (not pushed).
</success_criteria>

<output>
After completion, create `.planning/phases/35-editable-cad-drawings-part-management/35-05-SUMMARY.md`
</output>
