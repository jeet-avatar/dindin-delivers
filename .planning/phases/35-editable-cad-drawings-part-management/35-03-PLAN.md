---
phase: 35-editable-cad-drawings-part-management
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - /Users/jeet/turion-satellite/backend/src/routes/parts.ts
  - /Users/jeet/turion-satellite/backend/src/routes/bom.ts
  - /Users/jeet/turion-satellite/backend/src/routes/kanban.ts
  - /Users/jeet/turion-satellite/backend/tests/parts.test.ts
  - /Users/jeet/turion-satellite/backend/tests/bom.delete.test.ts
autonomous: true
requirements: [PartRetire, BomLineDelete]
must_haves:
  truths:
    - "DELETE /api/parts/:id soft-retires (sets retired_at=now()); returns 409 if live part_instances exist UNLESS ?force=1"
    - "DELETE /api/satellites/:satId/bom/:lineId deletes only the bom_lines row; returns 200 {ok:true}; refuses 409 if the child instance has its own sub-lines"
    - "GET /api/parts (list) and GET /api/parts/:partDefId/children HIDE retired parts (WHERE pd.retired_at IS NULL)"
    - "GET /api/parts/:id, GET /bom/tree nodes, and the kanban instance query SURFACE retired_at (badge, not hide) — they do NOT hard-filter on retired_at"
    - "All new routes require auth (401 without a JWT) and never leak err.message"
  artifacts:
    - path: /Users/jeet/turion-satellite/backend/src/routes/parts.ts
      provides: "DELETE /:id (soft-retire, ?force=1) + retired_at IS NULL on the list + children SELECTs + retired_at field on GET /:id"
      contains: "retired_at"
    - path: /Users/jeet/turion-satellite/backend/src/routes/bom.ts
      provides: "DELETE /:lineId (delete bom_lines row, refuse-on-sub-lines, 200 {ok}) + retired_at surfaced on /tree nodes"
      contains: "DELETE"
    - path: /Users/jeet/turion-satellite/backend/tests/bom.delete.test.ts
      provides: "vitest+supertest coverage for DELETE bom line incl. 401, 404, 409-on-sub-lines"
      contains: "DELETE"
  key_links:
    - from: /Users/jeet/turion-satellite/backend/src/routes/parts.ts
      to: "turion_satellite.part_instances"
      via: "EXISTS check before soft-retire"
      pattern: "part_instances"
    - from: /Users/jeet/turion-satellite/backend/src/routes/bom.ts
      to: "turion_satellite.bom_lines"
      via: "DELETE FROM bom_lines WHERE id=$1"
      pattern: "DELETE FROM .*bom_lines"
---

<objective>
Add the retire + bom-line-delete backend routes, and do the careful `retired_at` filter sweep: hard-filter ONLY the parts list + the part-children/picker endpoints; everywhere else (part detail, BOM tree, kanban) surface `retired_at` so the UI can badge a retired part WITHOUT amputating sub-assemblies or hiding in-flight work.

Purpose: This is the destructive-operations half of the backend. The filter-sweep decision is the single most likely thing a reviewer "fixes" the wrong way — the plan states it explicitly.
Output: `DELETE /api/parts/:id`, `DELETE /api/satellites/:satId/bom/:lineId`, the `retired_at` sweep across `parts.ts`/`bom.ts`/`kanban.ts`, + tests.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-editable-cad-drawings-part-management/35-RESEARCH.md
@.planning/phases/35-editable-cad-drawings-part-management/35-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DELETE /api/parts/:id (soft-retire) + the parts.ts retired_at sweep</name>
  <files>/Users/jeet/turion-satellite/backend/src/routes/parts.ts</files>
  <action>
Read `backend/src/routes/parts.ts` first. Add `DELETE /api/parts/:id` (requireAuth, hardened catch): read `?force=1` from `req.query`. If NOT force: `queryOne('SELECT 1 FROM part_instances WHERE part_definition_id=$1 LIMIT 1')` — if a row exists → `res.status(409).json({ error: 'Part has live instances; pass ?force=1 to retire anyway' })`. Else (force OR no instances): `UPDATE part_definitions SET retired_at = now() WHERE id=$1 AND retired_at IS NULL RETURNING id` — 404 if no row. Optionally audit-log `'retire_part_definition'`. Return `200 { ok: true, id, retired_at }`.

The retired_at sweep in this file (REQUIRED edits):
- `GET /api/parts` (the list endpoint `parts.html` consumes): add `AND pd.retired_at IS NULL` to its WHERE. (HIDE retired from the list.)
- `GET /api/parts/:partDefId/children`: add `AND c_pd.retired_at IS NULL` to the child-rows filter. (HIDE retired from the picker/children view.)
- `GET /api/parts/:id`: do NOT 404 a retired part — instead ADD `pd.retired_at` to the SELECT's column list so the response carries it (the part page renders a "⚠ retired" banner). (SURFACE, don't hide.)

Document this decision as a comment in the file: `// Phase 35: retired_at hard-filters ONLY the list + children/picker; everywhere else (detail, /bom/tree, kanban) surfaces the field so the UI badges — filtering the tree's recursive walk would amputate sub-assemblies. Do not "fix" this.`
  </action>
  <verify>`cd backend && npx tsc --noEmit` clean; `node backend/scripts/audit-satellite-buttons.mjs` → `violations: 0`; `grep -n "retired_at IS NULL" backend/src/routes/parts.ts` shows the list + children edits; `grep -n "retired_at" backend/src/routes/parts.ts` shows it on the GET /:id SELECT.</verify>
  <done>DELETE /api/parts/:id soft-retires with ?force=1 override + 409-on-live-instances; GET /api/parts and GET /:partDefId/children filter retired; GET /:id surfaces retired_at; the decision is documented inline.</done>
</task>

<task type="auto">
  <name>Task 2: DELETE /api/satellites/:satId/bom/:lineId + surface retired_at on /bom/tree and kanban</name>
  <files>/Users/jeet/turion-satellite/backend/src/routes/bom.ts, /Users/jeet/turion-satellite/backend/src/routes/kanban.ts</files>
  <action>
Read `backend/src/routes/bom.ts` (note: `Router({ mergeParams: true })`, mounted at `/api/satellites/:satId/bom` from `satellites.ts`, so the path here is `/:lineId`) and `backend/src/routes/kanban.ts`.

In `bom.ts` add `router.delete('/:lineId', requireAuth, ...)` (hardened catch): `queryOne('SELECT id, child_part_instance_id FROM bom_lines WHERE id=$1 AND satellite_id=$2')` — 404 if no row (verifies the line belongs to this satellite). Then check the child instance isn't itself a parent in any bom_line on this satellite: `queryOne('SELECT 1 FROM bom_lines WHERE parent_part_instance_id=$1 AND satellite_id=$2 LIMIT 1', [child_part_instance_id, satId])` — if a row exists → `res.status(409).json({ error: 'This line has sub-lines; delete those first' })` (no `?cascade=1` in v1). Else `query('DELETE FROM bom_lines WHERE id=$1')`. Return `200 { ok: true }` (NOT 204 — `satelliteApi.del()`'s `res.json()` would throw on an empty body). The orphaned `part_instance` survives — `/bom/tree`'s roots CTE re-roots it (no extra work). Optionally audit-log `'delete_bom_line'`.

`/bom/tree` retired_at: in `GET /bom/tree`, ADD `pd.retired_at` (and/or `c_pd.retired_at`) to the node payload column lists — but do NOT add a `retired_at IS NULL` filter to the roots CTE or the recursive child join (filtering there amputates sub-assemblies under a force-retired part — the research doc Pitfall 3 is explicit). Comment: `// Phase 35: surface retired_at on tree nodes for badging; do NOT filter — filtering amputates sub-assemblies.`

`kanban.ts`: the kanban groups `part_instances` by stage. ADD `pd.retired_at` to the instance payload (join through to `part_definitions` if not already joined) so a card can show a "⚠ retired part" badge — do NOT add a `retired_at IS NULL` filter (hides in-flight work).
  </action>
  <verify>`cd backend && npx tsc --noEmit` clean; `node backend/scripts/audit-satellite-buttons.mjs` → `violations: 0`; `grep -n "router.delete" backend/src/routes/bom.ts` shows the new route; `grep -n "retired_at" backend/src/routes/bom.ts backend/src/routes/kanban.ts` shows it surfaced (no `IS NULL` filter in either).</verify>
  <done>DELETE /api/satellites/:satId/bom/:lineId deletes the bom_lines row, refuses 409 on sub-lines, returns 200 {ok:true}; /bom/tree nodes + kanban cards carry retired_at WITHOUT a hard filter.</done>
</task>

<task type="auto">
  <name>Task 3: Tests — bom.delete.test.ts + extend parts.test.ts for retire</name>
  <files>/Users/jeet/turion-satellite/backend/tests/bom.delete.test.ts, /Users/jeet/turion-satellite/backend/tests/parts.test.ts</files>
  <action>
Create `backend/tests/bom.delete.test.ts` (model on the existing bom/parts test pattern — mock `../src/db`, ES256 JWT, supertest `request(app)`). Cover `DELETE /api/satellites/:satId/bom/:lineId`:
- 401 without auth.
- 404 when the line lookup → undefined.
- 409 when the sub-lines check → a row (`{ error: /sub-lines/ }`).
- 200 `{ok:true}` happy path: line lookup → `{id, child_part_instance_id}`, sub-lines check → undefined, DELETE → ok.

Extend `backend/tests/parts.test.ts` (or add to `parts.write.test.ts` if that's cleaner) with `DELETE /api/parts/:id`:
- 401 without auth.
- 409 when `EXISTS part_instances` → a row and no `?force=1` (`{ error: /live instances/ }`).
- 200 when `?force=1` (skips the EXISTS check; UPDATE RETURNING → `{id}`).
- 200 when no instances exist (EXISTS → undefined; UPDATE RETURNING → `{id}`).
- 404 when the UPDATE RETURNING → undefined (already retired / not found).
Also a quick assertion that `GET /api/parts` includes `retired_at IS NULL` is hard to unit-test (it's in the SQL string) — instead just confirm `GET /api/parts/:id` returns `retired_at` in the body when the mocked row includes it.

Run `cd backend && npx vitest run tests/bom.delete.test.ts tests/parts.test.ts` — green.
  </action>
  <verify>`cd backend && npx vitest run tests/bom.delete.test.ts tests/parts.test.ts` — all pass; `npx vitest run` full suite green.</verify>
  <done>bom.delete.test.ts + the parts.test.ts retire cases green; full backend suite green.</done>
</task>

</tasks>

<verification>
- `cd backend && npx tsc --noEmit` clean; `npx vitest run` full suite green.
- `node backend/scripts/audit-satellite-buttons.mjs` → `violations: 0`.
- `grep -rn "retired_at IS NULL" backend/src/routes/` shows it ONLY in `parts.ts` (the list + children SELECTs) — NOT in `bom.ts` or `kanban.ts`.
</verification>

<success_criteria>
- `DELETE /api/parts/:id` (soft-retire, `?force=1`, 409-on-live-instances) + `DELETE /api/satellites/:satId/bom/:lineId` (delete row, refuse-on-sub-lines, 200 {ok}).
- The `retired_at` sweep: hard-filter ONLY `GET /api/parts` + `GET /api/parts/:partDefId/children`; surface (no filter) on `GET /api/parts/:id`, `GET /bom/tree`, kanban — documented inline.
- Tests green; full backend suite green; button audit 0 violations.
- Committed in turion-satellite with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar"` (not pushed).
</success_criteria>

<output>
After completion, create `.planning/phases/35-editable-cad-drawings-part-management/35-03-SUMMARY.md`
</output>
