---
phase: 35-editable-cad-drawings-part-management
plan: 07
type: execute
wave: 6
depends_on: ["35-05", "35-06"]
files_modified:
  - /Users/jeet/doordash-p2p/.planning/STATE.md
  - /Users/jeet/doordash-p2p/.planning/ROADMAP.md
autonomous: false
requirements: [DrawingEditor, DrawingRegenerate, PartCreate, PartEdit, PartRetire, BomLineDelete]
must_haves:
  truths:
    - "turion-satellite Lambda redeployed via build-and-push.sh (old→new CodeSha256 recorded); migration 022 already applied (35-01)"
    - "turion-space-demo redeployed via deploy-frontend.sh with the F6 pre-flight stash/restore; CloudFront /* invalidation issued + Completed"
    - "Curl smoke passes: the 6 new routes are 401-gated unauth + 404 on bogus id; an authed (or DB-direct) round-trip creates a test part → regenerates its drawing → PATCHes its drawing → retires it (409 w/ live instance, 200 w/ ?force=1) → restores it → deletes the test part row + its bom line"
    - "node scripts/audit-satellite-buttons.mjs → 0 violations in BOTH repos"
    - "Phase 27-34 regression intact: existing drawings render, 3D viewer mounts, Add-BOM modal works, the Phase-33 wizard works, the bom/kanban satellite picker works, the chat widget loads"
    - "STATE.md + ROADMAP.md updated for Phase 35"
  artifacts:
    - path: /Users/jeet/doordash-p2p/.planning/STATE.md
      provides: "narrative update: Phase 35 complete, the 6 routes live, the editor live, the deploy hashes/invalidation"
      contains: "Phase 35"
    - path: /Users/jeet/doordash-p2p/.planning/ROADMAP.md
      provides: "Phase 35 marked done with the plan checklist"
      contains: "Phase 35"
  key_links:
    - from: "build-and-push.sh"
      to: "Lambda turion-satellite-api"
      via: "aws lambda update-function-code --image-uri ...:latest"
      pattern: "turion-satellite-api"
    - from: "deploy-frontend.sh"
      to: "S3 turion-demo-static + CloudFront E37R9PT8IL44L2"
      via: "aws s3 sync + create-invalidation"
      pattern: "turion-demo-static|E37R9PT8IL44L2"
---

<objective>
Ship Phase 35: push all six plans' commits, redeploy the Lambda and the static frontend, smoke-test the new routes + the editor, run the button audit in both repos, regression-check Phases 27-34, get the headless-substitute human-verify checkpoint approved, and update STATE/ROADMAP.

Purpose: Nothing from 35-01..35-06 was pushed or deployed — this plan does it as a planned deploy (never ad-hoc, never the dollor.ai CI/CD — Turion uses its own scripts).
Output: live Lambda + live frontend + verification record + STATE/ROADMAP updates.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-editable-cad-drawings-part-management/35-RESEARCH.md
@.planning/phases/35-editable-cad-drawings-part-management/35-01-PLAN.md
@.planning/phases/35-editable-cad-drawings-part-management/35-02-PLAN.md
@.planning/phases/35-editable-cad-drawings-part-management/35-03-PLAN.md
@.planning/phases/35-editable-cad-drawings-part-management/35-04-PLAN.md
@.planning/phases/35-editable-cad-drawings-part-management/35-05-PLAN.md
@.planning/phases/35-editable-cad-drawings-part-management/35-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push commits + redeploy the Lambda</name>
  <files>(no .planning files — backend deploy)</files>
  <action>
1. In `/Users/jeet/turion-satellite`: confirm the working tree has the 35-01/35-02/35-03/35-04 commits (migration 022, `backend/src/cad-templates/*`, `cad-generator.test.ts`, `parts.write.test.ts`, `bom.delete.test.ts`, the `parts.ts`/`bom.ts`/`kanban.ts` route edits, the `audit-satellite-buttons.mjs` regex tweak). Run `cd backend && npx tsc --noEmit && npx vitest run` — must be all green. Then `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" push origin main` (or `HEAD:main`).
2. `cd /Users/jeet/turion-satellite && ./build-and-push.sh` — this runs `npm run build` (tsc), `docker build --platform linux/arm64 -f backend/lambda-build backend/`, ECR push, `aws lambda update-function-code --function-name turion-satellite-api --image-uri ...:latest`, `aws lambda wait function-updated`. Record the old → new `CodeSha256` (from `aws lambda get-function-configuration --function-name turion-satellite-api --query CodeSha256` before/after). Confirm the wait succeeds.
3. Sanity: `curl -s https://rjydekliee.execute-api.us-east-1.amazonaws.com/<health-path>` returns ok (use whatever the existing health route is — check `app.ts`).
  </action>
  <verify>`aws lambda get-function-configuration --function-name turion-satellite-api --query CodeSha256` changed; the health curl returns 200; `git log --oneline -8` shows the Phase-35 backend commits pushed.</verify>
  <done>turion-satellite main pushed; Lambda redeployed (CodeSha256 recorded); health ok.</done>
</task>

<task type="auto">
  <name>Task 2: Push + redeploy the frontend with the F6 pre-flight; smoke-test the new routes</name>
  <files>(no .planning files — frontend deploy + smoke)</files>
  <action>
1. In `/Users/jeet/turion-space-demo`: confirm the 35-04/35-05/35-06 commits (svg-editor.js, satellite-api.js +del, part.html, instance.html, bom.html, parts.html). Then `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" push origin main`.
2. **F6 pre-flight** (mandatory — `deploy-frontend.sh` does `aws s3 sync . --delete` over the working tree, excluding only `.git/* .vercel/* .DS_Store backend/* *.md *.sh deploy-*`, so dirty WIP HTML would get published): `git stash push -u -- about-this-demo.html agent-sales-cash.html dashboard-cio.html` (only if those are dirty/untracked — check `git status` first; stash whatever of them is dirty); `git stash push -u -- backend/` (if `backend/*` is dirty); `mv .superpowers .superpowers.bak 2>/dev/null || true`. Wrap the deploy in something that ALWAYS restores: run `./deploy-frontend.sh`; then UNCONDITIONALLY (even on failure) `git stash pop` (for each stash, in reverse order) and `mv .superpowers.bak .superpowers 2>/dev/null || true`.
3. `./deploy-frontend.sh` issues a CloudFront invalidation `/*` on dist `E37R9PT8IL44L2` — confirm it reaches `Completed` (`aws cloudfront wait invalidation-completed --distribution-id E37R9PT8IL44L2 --id <id>`), record the id.
4. **Curl smoke** against `https://rjydekliee.execute-api.us-east-1.amazonaws.com`:
   - Unauth: `POST /api/parts`, `PATCH /api/parts/00000000-0000-0000-0000-000000000000`, `PATCH /api/parts/.../drawing`, `POST /api/parts/.../drawing/regenerate`, `DELETE /api/parts/...`, `DELETE /api/satellites/<sat>/bom/...` — each returns 401.
   - With a valid JWT (mint a short-lived test ES256 JWT using the same keypair pattern the tests use, OR — simpler — do this round-trip directly against the prod DB with `psql "$PROD_URL"` to avoid needing a real token): `POST /api/parts {part_number:'TEST-DELETE-ME-35', description:'phase35 smoke', subsystem_id:<a real subsystem id>, default_make_buy:'make'}` → 201, capture `id` + assert `drawing_rev=1` and `drawing_svg` starts `<svg`; `POST /api/parts/<id>/drawing/regenerate` → 200, `drawing_rev=2`; `PATCH /api/parts/<id>/drawing {drawing_svg:'<svg ...>...</svg>'}` → 200, `drawing_rev=3`; create one `part_instance` for it + a `bom_line` (via the routes or psql) then `DELETE /api/parts/<id>` → 409 (live instance) → `DELETE /api/parts/<id>?force=1` → 200 → `POST /api/parts/<id>/restore` → 200; `DELETE /api/satellites/<sat>/bom/<lineId>` → 200 {ok:true}. Then CLEAN UP: `psql "$PROD_URL" -c "DELETE FROM turion_satellite.bom_lines WHERE ...; DELETE FROM turion_satellite.part_instances WHERE part_definition_id IN (SELECT id FROM turion_satellite.part_definitions WHERE part_number='TEST-DELETE-ME-35'); DELETE FROM turion_satellite.part_revisions WHERE part_def_id IN (...); DELETE FROM turion_satellite.part_definitions WHERE part_number='TEST-DELETE-ME-35';"` so prod is left exactly as found.
   - `curl -sI https://turionspace.zietra.com/satellite/svg-editor.js` → 200; `curl -s https://turionspace.zietra.com/satellite/part.html | grep -c "svg-editor.js"` → 1.
5. Button audit BOTH repos: `node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` → `violations: 0` — confirm it picks up the new `.del()` calls (the `satelliteApi calls scanned:` count should have gone up).
  </action>
  <verify>The 6 routes 401 unauth; the create→regenerate→PATCH-drawing→retire(409→force)→restore→delete-bom-line round-trip all returned the expected statuses; prod DB cleaned (no `TEST-DELETE-ME-35` rows remain); `svg-editor.js` 200 + linked on `part.html`; CF invalidation Completed; button audit 0 violations in both repos.</verify>
  <done>Frontend pushed + deployed (F6 pre-flight done + restored); CF invalidated; new routes smoke-tested + prod cleaned; audit 0 both repos.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 27-34 regression smoke</name>
  <files>(no files — verification)</files>
  <action>
Quick regression of the prior phases (curl/HEAD smoke + spot checks, per the Phases 27-34 convention):
- `curl -s https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/parts/<a-known-partDefId>/drawing` (or whatever the existing drawing endpoint is) → 200 with a `<svg` body (Phase 27 generator corpus intact — migration 022 didn't touch `drawing_svg`).
- `curl -sI https://turionspace.zietra.com/satellite/bom.html` → 200; `curl -s .../bom.html | grep -c "addBomLineBtn"` → 1 (Phase 29 modal still there).
- `curl -sI https://turionspace.zietra.com/satellite/satellite-3d.js` → 200; `curl -s .../part.html | grep -c "mount3DViewer"` → 1 (Phase 30/31 3D viewer wiring intact).
- `curl -sI https://turionspace.zietra.com/satellite/sat.html` (or the Phase-33 program/wizard page) → 200; spot-check the bom/kanban satellite-picker dropdown is still present (`grep` for it in the HTML).
- `curl -sI https://turionspace.zietra.com/satellite/satellite-chat.js` → 200; `curl -s .../bom.html | grep -c "satellite-chat.js"` → 1 (Phase 34 chat widget).
- `curl -s https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/satellites/<SAT-003-uuid>/bom/tree` → 200 with a recursive tree (Phase 28/33 BOM hierarchy intact; confirm node payloads now also carry `retired_at` but no nodes were dropped — count is the same as before).
Record results.
  </action>
  <verify>All prior-phase smoke checks return the expected statuses/content; the BOM tree node count is unchanged (no amputation from the retired_at sweep).</verify>
  <done>Phases 27-34 verified non-regressed: drawings render, 3D viewer mounts, Add-BOM modal present, wizard/satellite-picker present, chat widget loads, BOM tree intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Headless-substitute human-verify checkpoint</name>
  <action>Present the what-built summary and the how-to-verify steps below to the user and pause for approval (per the Phases 27-34 headless-substitute convention — curl/HEAD smoke + the button audit have already passed in Tasks 1-3; a full browser walk is the user's call). On "approved", continue to Task 5.</action>
  <what-built>
Phase 35 shipped: (A) a vanilla in-browser SVG drawing editor on part.html (reachable from instance.html via ?edit=drawing) — select/move/resize/rotate/delete/add-primitives/undo-redo, Save → PATCH /api/parts/:id/drawing, "Revert to generated" → POST /api/parts/:id/drawing/regenerate (server-side Phase-27 generator, now ported into the Lambda); (B) part management — create a new part inline from the Add-BOM-line modal (3-call chain), edit a part's fields (dimensions edit → regenerate prompt → reload re-mounts the 3D viewer), retire a part (soft-delete, ?force=1 override, restore), delete a BOM line; (C) migration 022 (drawing_rev + retired_at + part_revisions) applied to prod; Lambda redeployed; static frontend redeployed with the F6 pre-flight. Button audit 0 violations in both repos; Phases 27-34 regression-checked headlessly (curl/HEAD smoke + audit — a full browser walk is noted as pending, per the Phases 27-34 convention).
  </what-built>
  <how-to-verify>
1. Visit https://turionspace.zietra.com/satellite/part.html?id=<a real partDefId>&edit=drawing — the SVG editor modal should open with the part's drawing; try select/move/add-rect/undo, then Save → the 2D view should refresh; reopen and click "Revert to generated" → it should regenerate.
2. On https://turionspace.zietra.com/satellite/bom.html for SAT-003 (Cygnus) — "+ Add BOM line" → "➕ Create new part" tab → fill it → Save → the new part should appear in the tree; then click a tree row's 🗑 → confirm → the line should disappear (try one with sub-lines → should warn "delete those first").
3. On https://turionspace.zietra.com/satellite/parts.html — a 🗑 on a row → retire a throwaway part → it should drop off the list; deep-link to that part's page → it should show "⚠ retired" + a "♻ Restore" button → restore → reappears in the list.
4. On a part page, "✏ Edit part" → change a dimension → Save → answer "yes" to regenerate → page reloads → the 3D viewer should reflect the new dimensions and the 2D drawing should be regenerated.
5. Confirm the existing 3D viewer, the chat widget, and the existing CAD drawings on other parts still work.
Reply "approved" if it all works, or describe what's broken.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Update STATE.md + ROADMAP.md for Phase 35</name>
  <files>/Users/jeet/doordash-p2p/.planning/STATE.md, /Users/jeet/doordash-p2p/.planning/ROADMAP.md</files>
  <action>
STATE.md is narrative-format (gsd-tools can't parse it — edit manually, consistent with Phases 27-34). Add a Phase-35 entry: the new routes (`PATCH /api/parts/:id`, `PATCH /api/parts/:id/drawing`, `POST /api/parts/:id/drawing/regenerate`, `POST /api/parts`, `POST /api/parts/:id/restore`, `DELETE /api/parts/:id`, `DELETE /api/satellites/:satId/bom/:lineId`), migration 022 (drawing_rev + retired_at + part_revisions), the ported `backend/src/cad-templates/` generator, `satellite/svg-editor.js`, the `satelliteApi.del()` + audit-regex tweak, the part-management UI, the Lambda CodeSha256 old→new, the CF invalidation id, the `retired_at`-sweep decision (hard-filter only the parts list + children/picker; badge elsewhere), and that the prod smoke used a `TEST-DELETE-ME-35` throwaway part that was cleaned up.

ROADMAP.md: in the Phase 35 entry, change `**Plans:** 0 plans` (or `**Plans:** 7 plans`) and replace the plan checklist with all `- [x]` lines + one-line outcomes, and add a "Phase 35 complete" line to the footer/last-updated.

Also append a one-line index entry to `/Users/jeet/.claude/projects/-Users-jeet-doordash-p2p/memory/MEMORY.md` (keep it ≤200 chars) for Phase 35.

Commit the .planning changes in `/Users/jeet/doordash-p2p` with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit` (the project's GSD repo — this one DOES get committed; the dollor.ai CI/CD is irrelevant, this is just docs).
  </action>
  <verify>`grep -n "Phase 35" .planning/STATE.md .planning/ROADMAP.md` shows the updates; `git -C /Users/jeet/doordash-p2p log --oneline -1` shows the docs commit.</verify>
  <done>STATE.md + ROADMAP.md + MEMORY.md updated for Phase 35; committed in the doordash-p2p repo.</done>
</task>

</tasks>

<verification>
- Lambda CodeSha256 changed; health ok; the 6 routes 401 unauth; the prod round-trip (create→regenerate→PATCH-drawing→retire 409→force→restore→delete-bom-line) passed and prod was cleaned.
- `node backend/scripts/audit-satellite-buttons.mjs` → 0 violations in BOTH repos; the `satelliteApi calls scanned:` count went up.
- Phases 27-34 smoke all green; BOM tree node count unchanged.
- Headless-substitute checkpoint approved.
- STATE.md + ROADMAP.md committed.
</verification>

<success_criteria>
- Lambda redeployed (`build-and-push.sh`), frontend redeployed (`deploy-frontend.sh` + F6 pre-flight + CF invalidation Completed), all pushes done with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar"`.
- All 6 new routes live + smoke-tested; prod left exactly as found (throwaway test data cleaned).
- Button audit 0 violations in both repos; Phases 27-34 non-regressed.
- Checkpoint approved; STATE/ROADMAP/MEMORY updated + committed.
</success_criteria>

<output>
After completion, create `.planning/phases/35-editable-cad-drawings-part-management/35-07-SUMMARY.md`
</output>
