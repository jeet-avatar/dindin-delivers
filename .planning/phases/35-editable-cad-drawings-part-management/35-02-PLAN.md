---
phase: 35-editable-cad-drawings-part-management
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - /Users/jeet/turion-satellite/backend/src/routes/parts.ts
  - /Users/jeet/turion-satellite/backend/tests/parts.write.test.ts
autonomous: true
requirements: [DrawingRegenerate, PartCreate, PartEdit]
must_haves:
  truths:
    - "POST /api/parts creates a part_definition with a generated drawing_svg and drawing_rev=1, returns 201; duplicate part_number returns 409"
    - "PATCH /api/parts/:id updates description/subsystem_id/default_make_buy/itar_flag/part_number/dimensions_mm subsets; 404 if not found"
    - "PATCH /api/parts/:id/drawing replaces drawing_svg, bumps drawing_rev, inserts a part_revisions row"
    - "POST /api/parts/:id/drawing/regenerate regenerates the SVG from generateDrawingSvg, bumps drawing_rev, inserts a part_revisions row"
    - "POST /api/parts/:id/restore clears retired_at"
    - "All new routes require auth (401 without a JWT) and never leak err.message"
  artifacts:
    - path: /Users/jeet/turion-satellite/backend/src/routes/parts.ts
      provides: "POST /, PATCH /:id, PATCH /:id/drawing, POST /:id/drawing/regenerate, POST /:id/restore — all requireAuth, hardened catch"
      contains: "drawing/regenerate"
    - path: /Users/jeet/turion-satellite/backend/tests/parts.write.test.ts
      provides: "vitest+supertest coverage for the 5 new routes incl. 401 + 409-on-dup"
      contains: "POST /api/parts"
  key_links:
    - from: /Users/jeet/turion-satellite/backend/src/routes/parts.ts
      to: /Users/jeet/turion-satellite/backend/src/cad-templates/index.ts
      via: "import { generateDrawingSvg }"
      pattern: "generateDrawingSvg"
    - from: /Users/jeet/turion-satellite/backend/src/routes/parts.ts
      to: "turion_satellite.part_revisions"
      via: "INSERT on drawing change"
      pattern: "part_revisions"
---

<objective>
Add the parts-CRUD + drawing-mutation backend routes to the existing `parts` router: create a part (with an auto-generated drawing), edit its fields, replace its drawing, regenerate its drawing server-side, and restore a retired part.

Purpose: These are the API surface the drawing editor (35-05) and the part-management UI (35-06) call. Mounts in the already-mounted `parts` router → no `app.ts` change → the button audit picks them up automatically.
Output: 5 new routes in `routes/parts.ts` + `tests/parts.write.test.ts`.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-editable-cad-drawings-part-management/35-RESEARCH.md
@.planning/phases/35-editable-cad-drawings-part-management/35-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add POST /api/parts, PATCH /api/parts/:id, POST /api/parts/:id/restore</name>
  <files>/Users/jeet/turion-satellite/backend/src/routes/parts.ts</files>
  <action>
Read `backend/src/routes/parts.ts` first to match the existing style (`Router()`, `requireAuth` from `../middleware/auth`, `query`/`queryOne` from `../db`, hardened `catch` — return `{ error: '<generic message>' }`, never `err.message`). Also read `backend/src/routes/instances.ts` for the `POST /api/satellites/:satId/instances` request shape (35-06 needs it documented; not built here). Add:

`POST /api/parts` (requireAuth): body `{ part_number, description, subsystem_id, default_make_buy, itar_flag?, dimensions_mm? }`. Validate: `part_number` non-empty string; `description` string; `subsystem_id` — `queryOne('SELECT id FROM subsystems WHERE id=$1')` must return a row (else 400); `default_make_buy ∈ {'make','buy'}` (else 400); `itar_flag` boolean default false. Build `specifications = dimensions_mm ? jsonb_build_object('dimensions_mm', $::jsonb) : '{}'::jsonb`. Generate `drawing_svg` via `generateDrawingSvg({ part_number, subsystem_code: <the subsystem's code — fetch it>, specifications: { dimensions_mm } })`. INSERT into `part_definitions` (`part_number, description, subsystem_id, default_make_buy, itar_flag, specifications, drawing_svg, drawing_rev` = 1, `flagged_for_review` = false, `created_by` = `req.user.id`) RETURNING the row. On PG error code `23505` → `res.status(409).json({ error: 'A part with that part number already exists' })`. Else `res.status(201).json(row)`. Optionally `query('INSERT INTO audit_log(...) VALUES (...,'create_part_definition',...)')` if the audit_log shape is obvious from migration 021 — else skip (acceptable).

`PATCH /api/parts/:id` (requireAuth): accept any subset of `{ description, subsystem_id, default_make_buy, itar_flag, part_number, dimensions_mm }`. Validate provided keys (subsystem_id must exist; default_make_buy enum; part_number non-empty). Build a parametrized dynamic `SET` clause from the provided keys. For `dimensions_mm`: `specifications = jsonb_set(coalesce(specifications,'{}'::jsonb), '{dimensions_mm}', $::jsonb)`. `WHERE id=$1 AND retired_at IS NULL RETURNING <row>` — if no row → 404. Does NOT auto-regenerate the drawing (the frontend prompts + calls regenerate). On `23505` (part_number collision) → 409. Else 200 with the updated row.

`POST /api/parts/:id/restore` (requireAuth): `UPDATE part_definitions SET retired_at = NULL WHERE id=$1 RETURNING id` — 404 if no row. Return `200 { ok: true, id }`.

Hardened catch on all three.
  </action>
  <verify>`cd backend && npx tsc --noEmit` clean. `node backend/scripts/audit-satellite-buttons.mjs` still reports `violations: 0` (new routes auto-allowlisted).</verify>
  <done>POST /api/parts, PATCH /api/parts/:id, POST /api/parts/:id/restore exist in routes/parts.ts, requireAuth, hardened, 409-on-dup, 404-on-missing.</done>
</task>

<task type="auto">
  <name>Task 2: Add PATCH /api/parts/:id/drawing and POST /api/parts/:id/drawing/regenerate</name>
  <files>/Users/jeet/turion-satellite/backend/src/routes/parts.ts</files>
  <action>
`import { generateDrawingSvg } from '../cad-templates';` (match the import-extension convention from 35-01).

`PATCH /api/parts/:id/drawing` (requireAuth): body `{ drawing_svg, expected_rev? }`. Validate: `drawing_svg` is a string, `startsWith('<svg')` (after trim), `includes('</svg>')`, `length < 500_000` (defensive; `express.json` limit is the hard ceiling). If `expected_rev` provided: `SELECT drawing_rev FROM part_definitions WHERE id=$1 AND retired_at IS NULL` — if no row → 404; if `drawing_rev !== expected_rev` → 409 `{ error: 'Drawing was modified by someone else; reload and retry', current_rev }`. `UPDATE part_definitions SET drawing_svg=$1, drawing_rev=drawing_rev+1 WHERE id=$2 RETURNING id, drawing_rev` — 404 if no row. Then `INSERT INTO part_revisions(part_def_id, rev, drawing_svg, edited_by) VALUES ($1,$2,$3,$4)` with the new `drawing_rev`. Return `200 { part_id, drawing_rev }`.

`POST /api/parts/:id/drawing/regenerate` (requireAuth): `queryOne('SELECT pd.id, pd.part_number, pd.specifications, s.code AS subsystem_code FROM part_definitions pd LEFT JOIN subsystems s ON s.id = pd.subsystem_id WHERE pd.id=$1 AND pd.retired_at IS NULL')` — 404 if no row. `let svg; try { svg = generateDrawingSvg(part); } catch { return res.status(500).json({ error: 'Drawing generation failed' }); }`. `UPDATE part_definitions SET drawing_svg=$1, drawing_rev=drawing_rev+1 WHERE id=$2 RETURNING id, drawing_rev`. `INSERT INTO part_revisions(...)`. Return `200 { part_id, drawing_rev, drawing_svg: svg }` (frontend re-renders the 2D view from the returned svg).

Hardened catch on both. Do NOT re-run any migration-017 bulk backfill — these routes touch ONE part each.
  </action>
  <verify>`cd backend && npx tsc --noEmit` clean. `node backend/scripts/audit-satellite-buttons.mjs` → `violations: 0`.</verify>
  <done>PATCH /api/parts/:id/drawing (+optional expected_rev 409) and POST /api/parts/:id/drawing/regenerate exist, both bump drawing_rev + insert a part_revisions row, both requireAuth + hardened.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend/tests/parts.write.test.ts</name>
  <files>/Users/jeet/turion-satellite/backend/tests/parts.write.test.ts</files>
  <action>
Model on `backend/tests/parts.test.ts` (mock `../src/db` `query`/`queryOne`, mint an ES256 JWT via `crypto.generateKeyPairSync('ec',{namedCurve:'P-256'})` + `process.env.SUPABASE_JWT_PUBLIC_KEY`, `request(app)` from supertest). Cover:
- `POST /api/parts`: 401 without auth; 400 on bad `default_make_buy`; 400 on unknown `subsystem_id` (mock subsystem lookup → undefined); 201 happy path (mock subsystem lookup → `{id, code}`, mock insert RETURNING → `{id:'pd-new', part_number:'NEW-001', drawing_rev:1}`) and assert the response body; 409 when the insert mock rejects with `{ code: '23505' }`.
- `PATCH /api/parts/:id`: 401 without auth; 404 when the UPDATE RETURNING mock → undefined; 200 happy path with a partial body (e.g. `{description:'X'}`); 200 with `{dimensions_mm:{length:50,width:40,height:30}}` (assert it didn't error).
- `PATCH /api/parts/:id/drawing`: 401; 400 on a non-SVG string; 404 when UPDATE → undefined; 200 happy path (mock UPDATE RETURNING → `{id, drawing_rev:2}`, mock the part_revisions INSERT → ok) asserting body `{drawing_rev:2}`; 409 when `expected_rev` mismatches the mocked current rev.
- `POST /api/parts/:id/drawing/regenerate`: 401; 404 when the part lookup → undefined; 200 happy path (mock lookup → `{id, part_number:'ADCS-ASSY', specifications:{}, subsystem_code:'ADCS'}`, mock UPDATE RETURNING → `{id, drawing_rev:3}`, mock part_revisions INSERT) asserting body has `drawing_svg` (a string starting `<svg`).
- `POST /api/parts/:id/restore`: 401; 404 when UPDATE → undefined; 200 `{ok:true}` happy path.
Run `cd backend && npx vitest run tests/parts.write.test.ts` — all green.
  </action>
  <verify>`cd backend && npx vitest run tests/parts.write.test.ts` — all pass; `npx vitest run` full suite green.</verify>
  <done>parts.write.test.ts covers all 5 new routes incl. 401, 404, 409-on-dup, 409-on-rev-mismatch — green.</done>
</task>

</tasks>

<verification>
- `cd backend && npx tsc --noEmit` clean; `npx vitest run` full suite green.
- `node backend/scripts/audit-satellite-buttons.mjs` → `violations: 0`.
- Routes confirmed mounted: `grep -n "router.post\|router.patch" backend/src/routes/parts.ts` shows the 5 new routes.
</verification>

<success_criteria>
- 5 new routes in `routes/parts.ts`: `POST /`, `PATCH /:id`, `PATCH /:id/drawing`, `POST /:id/drawing/regenerate`, `POST /:id/restore` — all requireAuth, hardened catch.
- `tests/parts.write.test.ts` green; full backend suite green; button audit 0 violations.
- Committed in turion-satellite with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar"` (not pushed).
</success_criteria>

<output>
After completion, create `.planning/phases/35-editable-cad-drawings-part-management/35-02-SUMMARY.md`
</output>
