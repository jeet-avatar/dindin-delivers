---
phase: 35-editable-cad-drawings-part-management
plan: 06
type: execute
wave: 5
depends_on: ["35-04", "35-02", "35-03"]
files_modified:
  - /Users/jeet/turion-space-demo/satellite/bom.html
  - /Users/jeet/turion-space-demo/satellite/part.html
  - /Users/jeet/turion-space-demo/satellite/parts.html
autonomous: true
requirements: [PartCreate, PartEdit, PartRetire, BomLineDelete]
must_haves:
  truths:
    - "bom.html's '+ Add BOM line' modal has a '➕ create new part' sub-form: POST /api/parts → POST /api/satellites/:satId/instances {part_definition_id} → POST /api/satellites/:satId/bom {parent_part_instance_id, child_part_instance_id, qty} — then the tree reloads"
    - "part.html has an 'Edit part' form → PATCH /api/parts/:id (description/subsystem/dimensions/make-buy/itar/part_number); if dimensions changed, prompt to regenerate the drawing → POST /api/parts/:id/drawing/regenerate → location.reload()"
    - "part.html (and parts.html) has a 'Retire part' control with a confirm dialog → DELETE /api/parts/:id; on 409 (live instances) it offers to retry with ?force=1"
    - "bom.html tree rows have a delete (🗑) control → DELETE /api/satellites/:satId/bom/:lineId; on 409 (sub-lines) it tells the user to delete sub-lines first; on success the tree reloads"
    - "Retired parts show a '⚠ retired' badge wherever surfaced (parts.html list excludes them; tree/kanban badge them)"
    - "All new controls are addEventListener-wired (zero inline onclick); button audit stays 0 violations"
  artifacts:
    - path: /Users/jeet/turion-space-demo/satellite/bom.html
      provides: "extended Add-BOM modal w/ create-new-part sub-form + delete control on tree rows + retired badge"
      contains: "create new part"
    - path: /Users/jeet/turion-space-demo/satellite/part.html
      provides: "Edit-part form + Retire-part control"
      contains: "Retire"
    - path: /Users/jeet/turion-space-demo/satellite/parts.html
      provides: "Retire affordance + retired-aware list (list endpoint already excludes them)"
      contains: "Retire"
  key_links:
    - from: /Users/jeet/turion-space-demo/satellite/bom.html
      to: "/api/parts and /api/satellites/:satId/instances and /api/satellites/:satId/bom"
      via: "the 3-call create-new-part chain"
      pattern: "/api/parts"
    - from: /Users/jeet/turion-space-demo/satellite/bom.html
      to: "/api/satellites/:satId/bom/:lineId"
      via: "satelliteApi.del on the row delete control"
      pattern: "satelliteApi\\.del"
    - from: /Users/jeet/turion-space-demo/satellite/part.html
      to: "/api/parts/:id"
      via: "satelliteApi.patch (edit) / satelliteApi.del (retire)"
      pattern: "satelliteApi\\.(patch|del)"
---

<objective>
Wire the part-management UI: add a brand-new part inline from the Add-BOM-line modal, edit an existing part's fields, retire a part, and delete a BOM line — all via the routes from 35-02/35-03, all `addEventListener`-wired so the button audit stays clean.

Purpose: This is the user-facing half of PartCreate / PartEdit / PartRetire / BomLineDelete.
Output: `bom.html` + `part.html` + `parts.html` edits.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-editable-cad-drawings-part-management/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend bom.html — create-new-part sub-form in the Add-BOM modal + delete control on tree rows</name>
  <files>/Users/jeet/turion-space-demo/satellite/bom.html</files>
  <action>
Read `satellite/bom.html` (the `#addBomLineBtn` / `openAddBomLineModal()` flow, `#modalSave` → `POST /api/satellites/:satId/bom`, the `<details>` recursive tree render, the tree-flatten helper, the existing allowlisted `.remove()` onclicks, `r.toast` + `location.reload()` after success). Also read `backend/src/routes/instances.ts` to confirm the `POST /api/satellites/:satId/instances` body shape (the research says it checks `part_definition_id` — confirm and use exactly that shape; also note what subsystems lookup the modal needs — the "create new part" sub-form needs a subsystem `<select>` populated from `GET /api/subsystems` if that endpoint exists, else from whatever list endpoint the page already uses).

1. Add a tab/toggle in the Add-BOM-line modal: "Pick existing part" (the current behavior) vs "➕ Create new part". The create sub-form has inputs: `part_number` (text, required), `description` (text), `subsystem` (`<select>` from the subsystems list), `dimensions_mm` (three number inputs L/W/H, optional), `default_make_buy` (`<select>` make|buy), `itar_flag` (checkbox), and a `qty` input (shared with the pick-existing path). On Save (when the create tab is active): `addEventListener` handler does the 3-call chain:
   - `const pd = await satelliteApi.post('/api/parts', { part_number, description, subsystem_id, default_make_buy, itar_flag, dimensions_mm: hasDims ? {length, width, height} : undefined });`
   - `const inst = await satelliteApi.post('/api/satellites/' + satId + '/instances', { part_definition_id: pd.id });` (use the exact body shape from `instances.ts`)
   - `await satelliteApi.post('/api/satellites/' + satId + '/bom', { parent_part_instance_id: parentInstId, child_part_instance_id: inst.id /* or whatever instances.ts returns */, qty });` (use the exact body shape `bom.ts POST /` expects — read it).
   - On success: close modal, `r.toast('Part created and added')`, `location.reload()`. On any error: `r.toast('Failed: ' + (e && e.status || 'error'))` and leave the modal open. Catch a 409 on the `POST /api/parts` step specifically → "A part with that part number already exists".
   ALL `addEventListener` — the modal's existing `onclick="...remove()"` for the close button is allowlisted; the new Save handler must NOT be inline.

2. Delete control on tree rows: in the `<details>` recursive render, add a small `🗑` button per BOM-line row (each row knows its `lineId` — the `bom_lines.id`). `addEventListener('click', async (e) => { e.stopPropagation(); if (!confirm('Remove this BOM line? (the part instance stays on the satellite)')) return; try { await satelliteApi.del('/api/satellites/' + satId + '/bom/' + lineId); r.toast('BOM line removed'); location.reload(); } catch(e){ if (e && e.status === 409) { alert('This line has sub-lines — delete those first.'); } else { r.toast('Delete failed'); } } })`. Since the tree is `<details>`, `e.stopPropagation()` keeps the click from toggling the disclosure. Wire each row's button when rendering (delegate or per-element `addEventListener` — per-element is fine given the tree is re-rendered wholesale).

3. Retired badge: when a tree node's payload carries `retired_at` (35-03 surfaces it on `/bom/tree`), render a small "⚠ retired" badge next to that node's label (`textContent`).
  </action>
  <verify>`grep -n "create new part\|satelliteApi.del\|/api/parts\|retired" satellite/bom.html` shows all four additions; `grep -c "onclick=" satellite/bom.html` did not increase (only the pre-existing allowlisted `.remove()` onclicks); `node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` → `violations: 0`.</verify>
  <done>bom.html: Add-BOM modal has a create-new-part sub-form doing the 3-call chain; tree rows have a 🗑 delete control hitting DELETE .../bom/:lineId with 409 handling; retired nodes badged; all addEventListener; audit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Edit-part form + Retire-part control to part.html</name>
  <files>/Users/jeet/turion-space-demo/satellite/part.html</files>
  <action>
Read `satellite/part.html` again (the part-identity / specifications area; the `partId`, the loaded `part` object incl. `description`, `subsystem_id`/`subsystem_code`, `specifications.dimensions_mm`, `default_make_buy`, `itar_flag`, `retired_at`). NOTE: 35-05 already added the drawing-editor controls + the `<script src="/satellite/svg-editor.js">` line + the retired banner — this task ADDS the field-editing + retire controls; don't duplicate the banner.

1. "✏ Edit part" button near the part header → opens an inline form (or a small modal — `createElement`) with: `part_number` (text), `description` (text), `subsystem` (`<select>` from the subsystems list), `dimensions_mm` (L/W/H number inputs prefilled from `specifications.dimensions_mm`), `default_make_buy` (`<select>`), `itar_flag` (checkbox). On Save (`addEventListener`): build a body of ONLY the changed fields; `await satelliteApi.patch('/api/parts/' + partId, body)`. Track whether `dimensions_mm` changed — if so, after the PATCH succeeds: `if (confirm('Dimensions changed. Regenerate the 2D drawing to match? (the 3D view updates either way)')) { await satelliteApi.post('/api/parts/' + partId + '/drawing/regenerate', {}); } location.reload();` — the reload re-mounts `mount3DViewer` from the fresh `dimensions_mm` and reloads the SVG. If only non-dimension fields changed: `location.reload()` (or just update the DOM in place + `r.toast`). On error: `r.toast` + leave the form open; 409 on `part_number` collision → "A part with that part number already exists".
2. "🗑 Retire part" button near the part header (disabled if `retired_at` is already set — instead show a "♻ Restore" button that POSTs `/api/parts/:id/restore` → reload). On click (`addEventListener`): `if (!confirm('Retire this part? It will be hidden from the parts list and pickers (kept for audit).')) return; try { await satelliteApi.del('/api/parts/' + partId); r.toast('Part retired'); location.reload(); } catch(e){ if (e && e.status === 409) { if (confirm('This part still has live instances. Retire anyway? (orphan instances stay visible with a badge)')) { await satelliteApi.del('/api/parts/' + partId + '?force=1'); r.toast('Part retired (forced)'); location.reload(); } } else { r.toast('Retire failed'); } }`.
ALL `addEventListener`; no inline `onclick=`.
  </action>
  <verify>`grep -n "Edit part\|Retire\|Restore\|satelliteApi.del\|drawing/regenerate" satellite/part.html` shows the additions; `grep -c "onclick=" satellite/part.html` did not increase; `node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` → `violations: 0`.</verify>
  <done>part.html: Edit-part form → PATCH /api/parts/:id (+dimensions-changed → regenerate prompt → reload); Retire button → DELETE /api/parts/:id (409 → offer ?force=1); Restore button when already retired; all addEventListener; audit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Add Retire/Restore affordances to parts.html</name>
  <files>/Users/jeet/turion-space-demo/satellite/parts.html</files>
  <action>
Read `satellite/parts.html` (the parts list — it consumes `GET /api/parts`, which 35-03 made exclude retired parts, so the list won't show retired ones at all; the `?subsystem=` filter from Phase 29). Add a small "🗑" retire control per row → `addEventListener('click', async (e) => { e.stopPropagation(); if (!confirm('Retire part ' + partNumber + '? It will disappear from this list (kept for audit).')) return; try { await satelliteApi.del('/api/parts/' + partDefId); r.toast('Retired'); location.reload(); } catch(e){ if (e && e.status === 409) { if (confirm('Has live instances — retire anyway (force)?')) { await satelliteApi.del('/api/parts/' + partDefId + '?force=1'); location.reload(); } } else { r.toast('Retire failed'); } } })`. (No "Edit part" here — `part.html` is the canonical place; a row click already navigates there. No restore here either since retired parts aren't listed — restore lives on `part.html` via the deep link.) ALL `addEventListener`; no inline `onclick=`.
  </action>
  <verify>`grep -n "Retire\|satelliteApi.del" satellite/parts.html` shows the addition; `grep -c "onclick=" satellite/parts.html` did not increase; `node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` → `violations: 0`.</verify>
  <done>parts.html rows have a 🗑 retire control hitting DELETE /api/parts/:id with 409→force handling; addEventListener-wired; audit 0.</done>
</task>

</tasks>

<verification>
- `node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` → `violations: 0` (and the `satelliteApi calls scanned:` count went UP — the new `.del()` calls are now validated, per the 35-04 regex tweak).
- `grep -c "onclick=" satellite/bom.html satellite/part.html satellite/parts.html` unchanged vs before this plan.
- Visual/functional check deferred to the W6 headless-substitute checkpoint.
</verification>

<success_criteria>
- `bom.html`: Add-BOM modal create-new-part sub-form (3-call chain) + tree-row 🗑 delete (DELETE .../bom/:lineId, 409 handling) + retired badge.
- `part.html`: Edit-part form (PATCH, dimensions→regenerate prompt→reload) + Retire/Restore controls.
- `parts.html`: row retire control.
- All `addEventListener`; button audit 0 violations and the DELETE calls are actually validated.
- Committed in turion-space-demo with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar"` (not pushed).
</success_criteria>

<output>
After completion, create `.planning/phases/35-editable-cad-drawings-part-management/35-06-SUMMARY.md`
</output>
