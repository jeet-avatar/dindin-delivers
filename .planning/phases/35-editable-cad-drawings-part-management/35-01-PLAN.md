---
phase: 35-editable-cad-drawings-part-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/turion-satellite/migrations/022_part_revisions_and_retire.sql
  - /Users/jeet/turion-satellite/backend/src/cad-templates/primitives.ts
  - /Users/jeet/turion-satellite/backend/src/cad-templates/palettes.ts
  - /Users/jeet/turion-satellite/backend/src/cad-templates/assembly.ts
  - /Users/jeet/turion-satellite/backend/src/cad-templates/subassembly.ts
  - /Users/jeet/turion-satellite/backend/src/cad-templates/cylindrical.ts
  - /Users/jeet/turion-satellite/backend/src/cad-templates/lens-optical.ts
  - /Users/jeet/turion-satellite/backend/src/cad-templates/antenna-dish.ts
  - /Users/jeet/turion-satellite/backend/src/cad-templates/solar-cell.ts
  - /Users/jeet/turion-satellite/backend/src/cad-templates/fastener.ts
  - /Users/jeet/turion-satellite/backend/src/cad-templates/plate.ts
  - /Users/jeet/turion-satellite/backend/src/cad-templates/index.ts
  - /Users/jeet/turion-satellite/backend/tests/cad-generator.test.ts
autonomous: true
requirements: [DrawingRegenerate]
must_haves:
  truths:
    - "Migration 022 applies cleanly to prod (and a second time = no-op): part_definitions has drawing_rev and retired_at; part_revisions table exists"
    - "backend/src/cad-templates/index.ts exports generateDrawingSvg(part) and ships in the Lambda build (dist/cad-templates/ exists after npm run build)"
    - "generateDrawingSvg byte-reproduces a known migration-017 part's drawing_svg"
  artifacts:
    - path: /Users/jeet/turion-satellite/migrations/022_part_revisions_and_retire.sql
      provides: "drawing_rev + retired_at columns + part_revisions table + (optional) widened audit_log.action CHECK, idempotent"
      contains: "ADD COLUMN IF NOT EXISTS drawing_rev"
    - path: /Users/jeet/turion-satellite/backend/src/cad-templates/index.ts
      provides: "generateDrawingSvg(part) — chooseTemplate dispatch + invoke matched template fn"
      contains: "generateDrawingSvg"
    - path: /Users/jeet/turion-satellite/backend/tests/cad-generator.test.ts
      provides: "byte-equality test vs a migration-017 SVG literal + ported __tests__ cases"
      contains: "generateDrawingSvg"
  key_links:
    - from: /Users/jeet/turion-satellite/backend/src/cad-templates/index.ts
      to: /Users/jeet/turion-satellite/backend/src/cad-templates/primitives.ts
      via: "import of cabinetBox/normalizeDims/makePrefix/etc."
      pattern: "from ['\"]\\./primitives"
    - from: /Users/jeet/turion-satellite/migrations/022_part_revisions_and_retire.sql
      to: "turion_satellite.part_definitions"
      via: "ALTER TABLE ADD COLUMN IF NOT EXISTS"
      pattern: "part_definitions"
---

<objective>
Lay the Phase-35 foundation: migration 022 (the schema changes every downstream plan needs) applied to prod, and the Phase-27 SVG generator ported into the Lambda-compiled `backend/src/` tree so "Revert to generated" can run server-side.

Purpose: Without `drawing_rev`/`retired_at`/`part_revisions` the CRUD/retire routes can't be written; without the ported generator the regenerate route 500s in prod. This plan is the W1 root that blocks 35-02, 35-03, 35-05.
Output: `migrations/022_part_revisions_and_retire.sql` (applied), `backend/src/cad-templates/*` (compiled into the image), `backend/tests/cad-generator.test.ts`.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-editable-cad-drawings-part-management/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 022 and apply it to prod</name>
  <files>/Users/jeet/turion-satellite/migrations/022_part_revisions_and_retire.sql</files>
  <action>
Create `migrations/022_part_revisions_and_retire.sql` modeled on `migrations/021_expand_audit_log_actions_phase33.sql` (idempotent style, `SET search_path TO turion_satellite, public;`, `current_database()` guard `IN ('postgres')`). It must:
1. `ALTER TABLE turion_satellite.part_definitions ADD COLUMN IF NOT EXISTS drawing_rev int NOT NULL DEFAULT 1;`
2. `ALTER TABLE turion_satellite.part_definitions ADD COLUMN IF NOT EXISTS retired_at timestamptz;`
3. `CREATE INDEX IF NOT EXISTS idx_part_definitions_retired_at ON turion_satellite.part_definitions(retired_at);`
4. `CREATE TABLE IF NOT EXISTS turion_satellite.part_revisions ( id uuid PRIMARY KEY DEFAULT gen_random_uuid(), part_def_id uuid NOT NULL REFERENCES turion_satellite.part_definitions(id) ON DELETE CASCADE, rev int NOT NULL, drawing_svg text NOT NULL, edited_by uuid, edited_at timestamptz NOT NULL DEFAULT now(), UNIQUE (part_def_id, rev) );`
5. Widen the `audit_log.action` CHECK (drop-IF-EXISTS both `chk_audit_log_action` and `audit_log_action_check`, re-add `chk_audit_log_action`) — copy the existing action list from migration 021 verbatim and APPEND: `'create_part_definition','edit_part_definition','retire_part_definition','restore_part_definition','edit_part_drawing','delete_bom_line'`. (If migration 021's exact action list is hard to reproduce, read it from the file first — do NOT guess.)
NO data backfill (no `part_revisions` rows, no `drawing_rev` UPDATE) — `DEFAULT 1` handles existing rows; `part_revisions` is lazily populated by the routes in 35-02.

Then apply to prod:
`PROD_URL=$(aws secretsmanager get-secret-value --secret-id turion-satellite/production/database-url-NCbgX6 --query SecretString --output text | python3 -c 'import sys,json;print(json.load(sys.stdin)["DATABASE_URL"].split("?")[0])')` (strip the `?schema=` suffix), then `psql "$PROD_URL" -f /Users/jeet/turion-satellite/migrations/022_part_revisions_and_retire.sql` TWICE — both runs must succeed with no errors (second run = all IF-NOT-EXISTS / IF-EXISTS no-ops). Verify: `psql "$PROD_URL" -c "\d turion_satellite.part_definitions"` shows `drawing_rev` and `retired_at`; `psql "$PROD_URL" -c "\d turion_satellite.part_revisions"` shows the table.
  </action>
  <verify>`psql "$PROD_URL" -f migrations/022_part_revisions_and_retire.sql` succeeds twice; `\d turion_satellite.part_definitions` lists drawing_rev + retired_at; `\d turion_satellite.part_revisions` exists.</verify>
  <done>Migration file committed (not pushed); prod schema has the two new columns + the part_revisions table + widened audit CHECK; double-apply proven clean.</done>
</task>

<task type="auto">
  <name>Task 2: Port the Phase-27 SVG generator into backend/src/cad-templates/</name>
  <files>/Users/jeet/turion-satellite/backend/src/cad-templates/primitives.ts, /Users/jeet/turion-satellite/backend/src/cad-templates/palettes.ts, /Users/jeet/turion-satellite/backend/src/cad-templates/assembly.ts, /Users/jeet/turion-satellite/backend/src/cad-templates/subassembly.ts, /Users/jeet/turion-satellite/backend/src/cad-templates/cylindrical.ts, /Users/jeet/turion-satellite/backend/src/cad-templates/lens-optical.ts, /Users/jeet/turion-satellite/backend/src/cad-templates/antenna-dish.ts, /Users/jeet/turion-satellite/backend/src/cad-templates/solar-cell.ts, /Users/jeet/turion-satellite/backend/src/cad-templates/fastener.ts, /Users/jeet/turion-satellite/backend/src/cad-templates/plate.ts, /Users/jeet/turion-satellite/backend/src/cad-templates/index.ts</files>
  <action>
First read `/Users/jeet/turion-satellite/backend/tsconfig.json` and an existing multi-file area (e.g. `backend/src/lib/` or `backend/src/routes/`) to learn the import-extension convention (`./foo.js` ESM specifiers if `moduleResolution: NodeNext`, bare `./foo` if not). Match it.

Copy the PURE template files from `/Users/jeet/turion-satellite/scripts/cad-templates/` (`primitives.ts`, `palettes.ts`, `assembly.ts`, `subassembly.ts`, `cylindrical.ts`, `lens-optical.ts`, `antenna-dish.ts`, `solar-cell.ts`, `fastener.ts`, `plate.ts`) into `backend/src/cad-templates/`, rewriting only the import-extension specifiers to match `backend/`'s convention. These files have NO DB / `fs` dependency — they're pure functions of `{part_number, subsystem_code?, specifications?}`. Do NOT port `scripts/generate-cad-svgs.ts`'s `pg.Client`/`fs` machinery.

Create `backend/src/cad-templates/index.ts` exporting `export function generateDrawingSvg(part: { part_number: string; subsystem_code?: string | null; specifications?: any }): string` — copy the `chooseTemplate(part)` dispatch table VERBATIM from `scripts/generate-cad-svgs.ts` (including all comments — the regex ORDER matters: the W5-fix comment about `SOLAR-*` before `-PANEL-` plate must carry over), then call the matched template fn and return its string.

Run `cd /Users/jeet/turion-satellite/backend && npm run build` — confirm `dist/cad-templates/index.js` and all the template `.js` files exist (so they ship in the `npm ci --omit=dev` Lambda image). Run `npx tsc --noEmit` — clean.
  </action>
  <verify>`cd backend && npm run build` succeeds; `ls dist/cad-templates/` shows index.js + 10 template files; `npx tsc --noEmit` clean.</verify>
  <done>backend/src/cad-templates/ has the 10 pure template files + index.ts (generateDrawingSvg); compiles; ships in dist/.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend/tests/cad-generator.test.ts (byte-equality + dispatch coverage)</name>
  <files>/Users/jeet/turion-satellite/backend/tests/cad-generator.test.ts</files>
  <action>
Create `backend/tests/cad-generator.test.ts` (vitest). It must:
1. Import `generateDrawingSvg` from `../src/cad-templates`.
2. Extract a known part's `drawing_svg` literal from `/Users/jeet/turion-satellite/migrations/017_redraw_cad_phase27.sql` (the `$svg$...$svg$` dollar-quoted body for a stable part like `ADCS-ASSY` — pick one whose template is deterministic; read the file to get the exact part_number + the exact SVG string), and assert `generateDrawingSvg({ part_number: 'ADCS-ASSY', subsystem_code: <its code from migration 001/017>, specifications: <its dims if any, else undefined> })` equals that string byte-for-byte. (If pinning the inline literal is unwieldy, store the expected SVG as a fixture file under `backend/tests/fixtures/cad-017-adcs-assy.svg` and read it.)
3. Port the dispatch-table assertions from `scripts/cad-templates/__tests__/` (or write equivalent ones) — at minimum: `SOLAR-*` routes to the solar template (NOT plate), `*-ASSY` to assembly, a fastener part to fastener, a lens/optical part to lens-optical, a plate/panel part to plate.
4. A determinism check: calling `generateDrawingSvg` twice with the same input returns the identical string.
Run `cd backend && npx vitest run tests/cad-generator.test.ts` — all green.
  </action>
  <verify>`cd backend && npx vitest run tests/cad-generator.test.ts` — all tests pass.</verify>
  <done>cad-generator.test.ts proves the port reproduces a migration-017 row byte-for-byte + the dispatch table is intact + deterministic.</done>
</task>

</tasks>

<verification>
- `psql "$PROD_URL" -f migrations/022_part_revisions_and_retire.sql` succeeds on first and second run (idempotent); prod `\d part_definitions` shows drawing_rev + retired_at; `\d part_revisions` exists.
- `cd backend && npm run build && npx tsc --noEmit` clean; `dist/cad-templates/` shipped.
- `cd backend && npx vitest run` — full suite green (no regressions from the new files), cad-generator.test.ts passes.
</verification>

<success_criteria>
- Migration 022 in `migrations/`, applied to prod, double-apply clean.
- `backend/src/cad-templates/` has 10 pure template files + index.ts exporting `generateDrawingSvg`; compiles into `dist/`.
- `backend/tests/cad-generator.test.ts` green (byte-equality vs migration 017 + dispatch + determinism).
- Committed in the turion-satellite repo with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar"` (NOT pushed — 35-07 pushes/redeploys).
</success_criteria>

<output>
After completion, create `.planning/phases/35-editable-cad-drawings-part-management/35-01-SUMMARY.md`
</output>
