---
phase: 03-android-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/StudioProjects/eatfair-android/app/src/main/java/ai/dollor/customer/data/CustomerRideshareApiService.kt
  - /Users/jeet/StudioProjects/eatfair-android/shared/src/main/java/ai/dollor/shared/data/remote/DollorApiService.kt
  - /Users/jeet/StudioProjects/eatfair-android/app/src/test/java/ai/dollor/customer/staging/OrderCreationFieldMappingTest.kt
  - /Users/jeet/StudioProjects/eatfair-android/app/src/test/java/ai/dollor/customer/staging/CustomerAppStagingApiTest.kt
  - /Users/jeet/StudioProjects/eatfair-android/app/src/main/java/ai/dollor/customer/ui/rideshare/RideRequestScreen.kt
  - /Users/jeet/StudioProjects/eatfair-android/driver/src/main/java/ai/dollor/driver/ui/profile/ProfileScreen.kt
autonomous: true
requirements: [ANDROID-01, ANDROID-02, ANDROID-03]

must_haves:
  truths:
    - "Android customer app calls correct backend paths for order creation, ride history, ride cancel, and ride rating"
    - "Android customer app calls correct path for deleting recurring rides"
    - "Android staging tests reference the real staging URL (d34u5ixl0bulv4.cloudfront.net), not the old production CF domain"
    - "Android photo URL resolution uses AppConfig.apiBaseUrl instead of hardcoded CloudFront domain"
    - "All 3 Android modules compile without errors after changes"
  artifacts:
    - path: "/Users/jeet/StudioProjects/eatfair-android/shared/src/main/java/ai/dollor/shared/data/remote/DollorApiService.kt"
      provides: "Corrected Retrofit API paths"
      contains: "erp/orders/create"
    - path: "/Users/jeet/StudioProjects/eatfair-android/app/src/main/java/ai/dollor/customer/data/CustomerRideshareApiService.kt"
      provides: "Corrected OkHttp recurring-rides delete path"
      contains: "recurring-rides"
    - path: "/Users/jeet/StudioProjects/eatfair-android/app/src/test/java/ai/dollor/customer/staging/OrderCreationFieldMappingTest.kt"
      provides: "Staging test with correct URL"
      contains: "d34u5ixl0bulv4"
    - path: "/Users/jeet/StudioProjects/eatfair-android/app/src/test/java/ai/dollor/customer/staging/CustomerAppStagingApiTest.kt"
      provides: "Staging test with correct URL"
      contains: "d34u5ixl0bulv4"
  key_links:
    - from: "DollorApiService.kt"
      to: "Backend main_new.py + bid_routes.py"
      via: "Retrofit annotation paths match FastAPI route decorators"
      pattern: "erp/orders/create|customer/rides/history|rides/request/.*/cancel|rides/.*/rate"
    - from: "CustomerRideshareApiService.kt"
      to: "Backend bid_routes.py"
      via: "OkHttp URL string matches FastAPI route decorator"
      pattern: "recurring-rides"
---

<objective>
Commit the 5 uncommitted Android API path fixes, fix wrong staging URL in test files, and replace hardcoded photo URL domain with AppConfig.apiBaseUrl in UI files.

Purpose: Android apps currently have 5 API paths that don't match backend routes, causing silent 404s. Two staging tests use a wrong URL (production CloudFront domain disguised as staging). Five UI files hardcode a CloudFront domain for photo resolution instead of using the centralized config.

Output: Committed, building Android codebase with all API paths matching backend, correct staging URL in tests, and centralized photo URL resolution.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-android-fixes/03-RESEARCH.md

IMPORTANT: All work is in the ANDROID repo at /Users/jeet/StudioProjects/eatfair-android (NOT the main doordash-p2p repo). Use absolute paths.

The 5 API path fixes are ALREADY in the working tree (modified but uncommitted). The staging URL and photo URL fixes are NEW changes to make.

Backend route verification (from research, all confirmed):
- `DELETE /api/rides/recurring-rides/{ride_id}` -- bid_routes.py:2993
- `POST /api/erp/orders/create` -- main_new.py:14941
- `GET /api/customer/rides/history` -- main_new.py:6825
- `POST /api/rides/request/{rideId}/cancel` -- bid_routes.py:896
- `POST /api/rides/{rideId}/rate` -- main_new.py:16012
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit 5 API path fixes already in working tree</name>
  <files>
    /Users/jeet/StudioProjects/eatfair-android/app/src/main/java/ai/dollor/customer/data/CustomerRideshareApiService.kt
    /Users/jeet/StudioProjects/eatfair-android/shared/src/main/java/ai/dollor/shared/data/remote/DollorApiService.kt
  </files>
  <action>
These 2 files already have the correct changes in the working tree. Verify the diff matches exactly these 5 fixes before committing:

1. **CustomerRideshareApiService.kt** (1 fix):
   - `DELETE /api/rides/recurring/{id}` changed to `DELETE /api/rides/recurring-rides/{id}`
   - Verify: Line ~951 should have `.url("$BASE_URL/api/rides/recurring-rides/$id")`

2. **DollorApiService.kt** (4 fixes):
   - `@POST("orders/create")` changed to `@POST("erp/orders/create")`
   - `@GET("customer/rides")` changed to `@GET("customer/rides/history")`
   - `@POST("rides/{rideId}/cancel")` changed to `@POST("rides/request/{rideId}/cancel")`
   - `@POST("erp/rides/{rideId}/rate")` changed to `@POST("rides/{rideId}/rate")`

Verification steps before commit:
- Run `cd /Users/jeet/StudioProjects/eatfair-android && git diff` and confirm exactly 5 path changes, no other modifications
- Run `./gradlew :app:compileDebugKotlin :shared:compileDebugKotlin` to verify compilation

Commit in the Android repo:
```
cd /Users/jeet/StudioProjects/eatfair-android
git add app/src/main/java/ai/dollor/customer/data/CustomerRideshareApiService.kt shared/src/main/java/ai/dollor/shared/data/remote/DollorApiService.kt
git commit -m "fix: correct 5 Android API paths to match backend routes

- CustomerRideshareApiService: recurring/{id} -> recurring-rides/{id}
- DollorApiService: orders/create -> erp/orders/create
- DollorApiService: customer/rides -> customer/rides/history
- DollorApiService: rides/{id}/cancel -> rides/request/{id}/cancel
- DollorApiService: erp/rides/{id}/rate -> rides/{id}/rate

All paths verified against API_REGISTRY.md and backend source.

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```
  </action>
  <verify>
- `cd /Users/jeet/StudioProjects/eatfair-android && git log --oneline -1` shows the path fix commit
- `git diff HEAD~1 --stat` shows exactly 2 files changed
- `./gradlew :app:compileDebugKotlin :shared:compileDebugKotlin` succeeds (BUILD SUCCESSFUL)
  </verify>
  <done>5 API path fixes committed in Android repo. Both files compile. git status shows clean for those 2 files.</done>
</task>

<task type="auto">
  <name>Task 2: Fix staging URL in tests + replace hardcoded photo URLs with AppConfig</name>
  <files>
    /Users/jeet/StudioProjects/eatfair-android/app/src/test/java/ai/dollor/customer/staging/OrderCreationFieldMappingTest.kt
    /Users/jeet/StudioProjects/eatfair-android/app/src/test/java/ai/dollor/customer/staging/CustomerAppStagingApiTest.kt
    /Users/jeet/StudioProjects/eatfair-android/app/src/main/java/ai/dollor/customer/ui/rideshare/RideRequestScreen.kt
    /Users/jeet/StudioProjects/eatfair-android/driver/src/main/java/ai/dollor/driver/ui/profile/ProfileScreen.kt
  </files>
  <action>
**Part A: Fix staging URL in 2 test files**

Replace all occurrences of `https://d3kuu45w6kl8hr.cloudfront.net` with `https://d34u5ixl0bulv4.cloudfront.net` in:

1. `app/src/test/java/ai/dollor/customer/staging/OrderCreationFieldMappingTest.kt` (line 40):
   - Change `STAGING_BASE_URL = "https://d3kuu45w6kl8hr.cloudfront.net"` to `STAGING_BASE_URL = "https://d34u5ixl0bulv4.cloudfront.net"`

2. `app/src/test/java/ai/dollor/customer/staging/CustomerAppStagingApiTest.kt` (lines 20, 40):
   - Line 20 (comment): Update `https://d3kuu45w6kl8hr.cloudfront.net` to `https://d34u5ixl0bulv4.cloudfront.net`
   - Line 40 (constant): Change `STAGING_BASE_URL = "https://d3kuu45w6kl8hr.cloudfront.net"` to `STAGING_BASE_URL = "https://d34u5ixl0bulv4.cloudfront.net"`

**Part B: Replace hardcoded photo URL domain with AppConfig in 2 UI files**

First, check what `AppConfig` provides. Read `shared/.../AppConfig.kt` to find the correct property name for the API base URL (expected: `AppConfig.apiBaseUrl` or `AppConfig.API_BASE_URL`). Also check the import path.

Then replace the hardcoded photo URL pattern in these files:

3. `app/.../RideRequestScreen.kt` (4 instances at lines ~1297, ~1511, ~1514, ~2155):
   - Replace: `"https://d3kuu45w6kl8hr.cloudfront.net$url"`
   - With: `"${AppConfig.apiBaseUrl}$url"` (or the correct property -- verify from AppConfig.kt)
   - Ensure `import ai.dollor.shared.data.AppConfig` (or correct import path) is present

4. `driver/.../ProfileScreen.kt` (1 instance at line ~275):
   - Same replacement pattern
   - Ensure AppConfig import is present

IMPORTANT: The pattern is `if (url.startsWith("http")) url else "https://d3kuu45w6kl8hr.cloudfront.net$url"`. The `if` check stays -- only the fallback domain changes.

After making all changes, compile:
```
cd /Users/jeet/StudioProjects/eatfair-android
./gradlew :app:compileDebugKotlin :driver:compileDebugKotlin
```

Then commit:
```
git add app/src/test/java/ai/dollor/customer/staging/OrderCreationFieldMappingTest.kt \
       app/src/test/java/ai/dollor/customer/staging/CustomerAppStagingApiTest.kt \
       app/src/main/java/ai/dollor/customer/ui/rideshare/RideRequestScreen.kt \
       driver/src/main/java/ai/dollor/driver/ui/profile/ProfileScreen.kt
git commit -m "fix: correct staging URL in tests + use AppConfig for photo URLs

- Fix 2 staging tests: d3kuu45w6kl8hr (production CF) -> d34u5ixl0bulv4 (real staging)
- Fix 5 hardcoded photo URL fallbacks in RideRequestScreen + ProfileScreen
  to use AppConfig.apiBaseUrl instead of hardcoded CloudFront domain

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```
  </action>
  <verify>
- `cd /Users/jeet/StudioProjects/eatfair-android && grep -rn "d3kuu45w6kl8hr" --include="*.kt"` returns ZERO results
- `./gradlew :app:compileDebugKotlin :driver:compileDebugKotlin` succeeds
- `grep -n "d34u5ixl0bulv4" app/src/test/java/ai/dollor/customer/staging/OrderCreationFieldMappingTest.kt` shows the corrected URL
- `grep -n "AppConfig" app/src/main/java/ai/dollor/customer/ui/rideshare/RideRequestScreen.kt` shows AppConfig usage for photo URLs
  </verify>
  <done>Zero instances of wrong staging URL remain. All photo URL resolution uses AppConfig instead of hardcoded domain. All modules compile.</done>
</task>

<task type="auto">
  <name>Task 3: Full build verification + unit tests</name>
  <files></files>
  <action>
Run full Android build and test suite to confirm nothing is broken:

```
cd /Users/jeet/StudioProjects/eatfair-android
./gradlew :app:compileDebugKotlin :driver:compileDebugKotlin :partner:compileDebugKotlin :shared:compileDebugKotlin
./gradlew :app:testDebugUnitTest
```

Expected results:
- All 4 modules compile successfully
- Unit tests: 72/73 or 73/73 pass (the 1 potential failure is the OrderCreationFieldMappingTest integration test which makes real HTTP calls -- it may still fail because the endpoint now requires auth, which is a separate issue)

If all tests pass (73/73): Done.
If 72/73 pass (OrderCreationFieldMappingTest still fails): This is expected -- the test makes real HTTP calls to staging and the endpoint now requires auth (Phase 01/02 security work). Document this as a known issue for Phase 04 (Fix CI). Do NOT try to fix the auth issue in the test -- that's a separate concern.

Verify git status is clean:
```
cd /Users/jeet/StudioProjects/eatfair-android && git status
```

Verify final commit history:
```
git log --oneline -3
```
  </action>
  <verify>
- `./gradlew :app:compileDebugKotlin :driver:compileDebugKotlin :partner:compileDebugKotlin :shared:compileDebugKotlin` -- BUILD SUCCESSFUL
- `./gradlew :app:testDebugUnitTest` -- 72+ tests pass, no NEW failures
- `git status` shows clean working tree (no uncommitted changes)
- `git log --oneline -3` shows both commits from Tasks 1 and 2
  </verify>
  <done>All 4 Android modules compile. Unit tests pass (no regressions from our changes). Working tree is clean with 2 commits covering all fixes.</done>
</task>

</tasks>

<verification>
Phase-level checks after all tasks complete:

1. **Path correctness**: Run `grep -n "orders/create\|customer/rides\"\|rides/{rideId}/cancel\|erp/rides/{rideId}/rate\|rides/recurring/" /Users/jeet/StudioProjects/eatfair-android/shared/src/main/java/ai/dollor/shared/data/remote/DollorApiService.kt` -- should return ZERO results (all old paths replaced)

2. **No hardcoded wrong URLs**: `cd /Users/jeet/StudioProjects/eatfair-android && grep -rn "d3kuu45w6kl8hr" --include="*.kt"` returns nothing

3. **Build green**: All 4 modules compile, unit tests have no regressions

4. **Git history**: 2 clean commits in Android repo documenting all changes
</verification>

<success_criteria>
- 5 Android API paths match their backend routes exactly
- Zero instances of wrong staging URL (d3kuu45w6kl8hr) in Kotlin files
- Photo URLs resolved via AppConfig, not hardcoded domain
- All 4 modules compile, no test regressions
- 2 atomic commits in Android repo with descriptive messages
</success_criteria>

<output>
After completion, create `.planning/phases/03-android-fixes/03-01-SUMMARY.md`
</output>
