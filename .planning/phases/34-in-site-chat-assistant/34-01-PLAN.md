---
phase: 34-in-site-chat-assistant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/turion-satellite/backend/package.json
  - /Users/jeet/turion-satellite/backend/package-lock.json
  - /Users/jeet/turion-satellite/backend/src/assistant-knowledge.ts
  - /Users/jeet/turion-satellite/backend/src/routes/assistant.ts
  - /Users/jeet/turion-satellite/backend/src/app.ts
  - /Users/jeet/turion-satellite/backend/src/routes/__tests__/assistant.test.ts
autonomous: true
requirements: [ChatEndpoint, SiteKnowledgePrompt, GracefulNoKey]

must_haves:
  truths:
    - "POST /api/assistant/chat exists, is mounted in app.ts, and rejects unauthenticated requests with 401"
    - "When no Anthropic key is configured (no ANTHROPIC_API_KEY env var AND no ANTHROPIC_API_KEY_ARN), the endpoint returns 200 {configured:false, reply:<friendly message>} — never 4xx/5xx"
    - "When a key is present, the endpoint calls Anthropic messages.create with the SITE_KNOWLEDGE system prompt and returns 200 {configured:true, reply}"
    - "On a real Anthropic API failure the endpoint returns 502 {error:...} (generic, no err.message leak)"
    - "SITE_KNOWLEDGE describes every satellite page, navigation, the Phase-33 sales-order→delivery workflow, the make/buy distinction, where data lives, and common tasks"
    - "@anthropic-ai/sdk is in package.json dependencies (not devDependencies); package-lock.json regenerated"
  artifacts:
    - path: "/Users/jeet/turion-satellite/backend/src/assistant-knowledge.ts"
      provides: "exported const SITE_KNOWLEDGE string (~3-6KB)"
      contains: "SITE_KNOWLEDGE"
      min_lines: 40
    - path: "/Users/jeet/turion-satellite/backend/src/routes/assistant.ts"
      provides: "POST /chat router with requireAuth, lazy memoized secret fetch, graceful-no-key, hardened catch"
      exports: ["default"]
      contains: "requireAuth"
      min_lines: 50
    - path: "/Users/jeet/turion-satellite/backend/src/app.ts"
      provides: "mounts /api/assistant"
      contains: "assistant"
    - path: "/Users/jeet/turion-satellite/backend/src/routes/__tests__/assistant.test.ts"
      provides: "vitest+supertest coverage: 401 no-auth, 200 configured:false no-key, 200 configured:true mocked, 400 bad messages"
      min_lines: 30
    - path: "/Users/jeet/turion-satellite/backend/package.json"
      provides: "@anthropic-ai/sdk dependency"
      contains: "@anthropic-ai/sdk"
  key_links:
    - from: "/Users/jeet/turion-satellite/backend/src/app.ts"
      to: "/Users/jeet/turion-satellite/backend/src/routes/assistant.ts"
      via: "import + app.use('/api/assistant', assistantRouter)"
      pattern: "app\\.use\\(['\"]\\/api\\/assistant['\"]"
    - from: "/Users/jeet/turion-satellite/backend/src/routes/assistant.ts"
      to: "/Users/jeet/turion-satellite/backend/src/assistant-knowledge.ts"
      via: "import { SITE_KNOWLEDGE }"
      pattern: "SITE_KNOWLEDGE"
    - from: "/Users/jeet/turion-satellite/backend/src/routes/assistant.ts"
      to: "@anthropic-ai/sdk"
      via: "client.messages.create({ model, max_tokens, system, messages })"
      pattern: "messages\\.create"
---

<objective>
Add the backend half of the in-site chat assistant: a new `POST /api/assistant/chat` route on the turion-satellite Lambda that proxies one chat turn to Anthropic's Messages API with a curated `SITE_KNOWLEDGE` system prompt. Stateless (client sends recent history each call), auth-gated, and graceful when the API key isn't configured yet.

Purpose: This is the foundation — the frontend widget (Plan 02) calls this route, and the button audit derives its allowlist from `app.ts`, so the route MUST be mounted before the widget ships. Shipping with `{configured:false}` lets the phase deploy before the user creates the secret.
Output: `@anthropic-ai/sdk` dependency, `backend/src/assistant-knowledge.ts` (`SITE_KNOWLEDGE`), `backend/src/routes/assistant.ts` (`POST /chat`), the `app.ts` mount, and vitest tests.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-in-site-chat-assistant/34-RESEARCH.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-06-SUMMARY.md

# Backend repo (NOT in this GSD repo):
# /Users/jeet/turion-satellite/backend/src/app.ts        — Express app; copy the app.use(...) tree pattern
# /Users/jeet/turion-satellite/backend/src/lambda.ts     — serverless-http wrap + loadSecrets() at module scope — DO NOT TOUCH (cold-start crash risk)
# /Users/jeet/turion-satellite/backend/src/secrets.ts    — fetchSecret(arn) pattern (reference only — do NOT modify)
# /Users/jeet/turion-satellite/backend/src/routes/sales-orders.ts — THE route pattern to mirror (Router, requireAuth, hardened catch)
# /Users/jeet/turion-satellite/backend/src/middleware/auth.ts — requireAuth middleware
# /Users/jeet/turion-satellite/backend/lambda-build      — Docker build: `npm ci --omit=dev` then COPY dist/ → SDK MUST be a prod dependency
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @anthropic-ai/sdk dependency + write SITE_KNOWLEDGE</name>
  <files>/Users/jeet/turion-satellite/backend/package.json, /Users/jeet/turion-satellite/backend/package-lock.json, /Users/jeet/turion-satellite/backend/src/assistant-knowledge.ts</files>
  <action>
    In `/Users/jeet/turion-satellite/backend/`: run `npm install @anthropic-ai/sdk` (NOT `--save-dev` — the Docker build does `npm ci --omit=dev`, so it MUST land in `dependencies`). Confirm it's under `"dependencies"` in `package.json` and that `package-lock.json` was updated. Pin whatever version `npm` resolved.

    Create `backend/src/assistant-knowledge.ts` exporting `export const SITE_KNOWLEDGE: string` — a plain template literal, ~3-6 KB, factual and terse (the model returns short answers). Cover these 6 section groups:
    (a) **Pages** — one line each: `index.html` (homepage / satellite list — reach `program-new.html` from here), `program-new.html` ("New satellite program" wizard → creates a sales_order → spawns the satellite), `sat.html` (satellite detail — lifecycle status dropdown PATCHes `/api/satellites/:id`; programProgress strip), `bom.html` (recursive BOM tree; `?sat=` filter; "🧊 3D" deep-links), `kanban.html` (parts by lifecycle stage), `parts.html` (part definitions list; `?subsystem=` filter), `part.html` (part definition detail — 2D/3D CAD `.cad-frame` toggle; for MAKE parts: Build-process / Materials / labor-cost panels; for BUY parts: a procurement panel — PR → VO → PO → invoiced), `instance.html` (part instance — Manufacturing/Procurement panel; 3D viewer; "Purchase order & invoice" card), `work-orders.html` (work order list), `work-order.html` (work order detail — build-step sign-offs), `cost.html` (cost rollup), `cost-detail.html` (per-part cost detail — `bom.html?sat=` link).
    (b) **Navigation & search/filter** — topbar shows the signed-in email; `?sat=` / `?subsystem=` query params filter; the forward chain is sat → bom → kanban → instance → work-order and back.
    (c) **Sales-order → delivery workflow (Phase 33)** — the "New satellite program" wizard on `program-new.html` creates a `sales_order` row, then `POST /api/satellites` (spawn) creates the satellite + ~261 part_instances + ~241 bom_lines + 261 stage-0 `drawing`/`forward`/`entered` `part_stage_events`; status advances design → build → … via the `sat.html` dropdown.
    (d) **Make vs buy** — every part has a recorded make/buy DECISION (decision + rationale + decided_by_name + decided_at), fetched via `GET /api/make-buy-decisions/:satId/:partDefId`; MAKE → build/materials/labor panels; BUY → procurement panel (purchase request → vendor order → PO issued → invoiced); the BUY "quote" lives on `buy_costs.quoted_unit_cost_usd` / `nre_cost_usd` (the `rfqs` table is empty).
    (e) **Where data lives** — Supabase Postgres, schema `turion_satellite`: tables include `satellites`, `sales_orders`, `part_definitions`, `part_instances`, `bom_lines`, `work_orders`, `build_steps`, `procurement_requests`, `vendor_orders`, `buy_costs`, `part_stage_events`, `make_buy_decisions`, `audit_log`.
    (f) **Common tasks** — "advance a lifecycle stage?" → `sat.html`, status dropdown; "see a part's 3D model?" → `part.html` / `instance.html`, 2D/3D `.cad-frame` toggle; "place a vendor order?" → a BUY part's `part.html` procurement panel; "create a new satellite program?" → `program-new.html`, reachable from the homepage.

    Harvest the wording from `33-RESEARCH.md`, `33-06-SUMMARY.md`, and the make/buy paragraph in the project MEMORY.md (Phase 32). Keep it self-contained — no per-customer/per-vendor literals.
  </action>
  <verify>`cd /Users/jeet/turion-satellite/backend && node -e "require('@anthropic-ai/sdk')"` succeeds (or `npm ls @anthropic-ai/sdk` shows it under dependencies). `grep -n 'SITE_KNOWLEDGE' src/assistant-knowledge.ts` shows the export. `wc -c src/assistant-knowledge.ts` is roughly 3000-7000 bytes.</verify>
  <done>`@anthropic-ai/sdk` is in `package.json` dependencies, `package-lock.json` updated; `backend/src/assistant-knowledge.ts` exports a `SITE_KNOWLEDGE` string covering all 6 section groups.</done>
</task>

<task type="auto">
  <name>Task 2: Create POST /api/assistant/chat route + mount in app.ts</name>
  <files>/Users/jeet/turion-satellite/backend/src/routes/assistant.ts, /Users/jeet/turion-satellite/backend/src/app.ts</files>
  <action>
    Create `backend/src/routes/assistant.ts` mirroring `routes/sales-orders.ts` (Router, `requireAuth` on every handler, hardened catch — `console.error(...)` server-side + generic `{ error }`, NEVER echo `err.message`). It does NO DB work — skip the schema-qualified-SQL bit.

    Module constants at the top: `const MODEL = 'claude-haiku-4-5';` `const MAX_TOKENS = 1024;` `const MAX_HISTORY = 12;` `const NOT_CONFIGURED = "The assistant isn't configured yet. An administrator needs to add the Anthropic API key. Once that's done, I'll be able to help you navigate the satellite app.";`

    Lazy memoized key fetch — `let cachedKey: string | null | undefined;` (`undefined` = not tried, `null` = tried & absent, `string` = the key). `async function getApiKey()`: if `process.env.ANTHROPIC_API_KEY` set → use it; else if `process.env.ANTHROPIC_API_KEY_ARN` set → `new SecretsManagerClient({region: process.env.AWS_REGION ?? 'us-east-1'})` + `GetSecretValueCommand({SecretId: arn})`, then `JSON.parse(raw)` and read `.ANTHROPIC_API_KEY` — if parse fails, treat the whole string as the key (be liberal); else (no ARN) → `null`. Wrap the whole thing in try/catch so a missing/broken secret → `cachedKey = null` (NEVER throws). DO NOT touch `loadSecrets()` / `lambda.ts`.

    `router.post('/chat', requireAuth, async (req, res) => {...})`:
    - `const key = await getApiKey(); if (!key) { res.json({ configured: false, reply: NOT_CONFIGURED }); return; }`
    - Read `body.page` (string, slice to 200) and `body.messages` (array). Filter to `{role:'user'|'assistant', content: non-empty string}`, slice last `MAX_HISTORY`, map content to `String(...).slice(0, 4000)`. If `messages.length === 0` or the last role isn't `'user'` → `res.status(400).json({ error: 'messages must end with a user turn' }); return;`
    - `const client = new Anthropic({ apiKey: key });` `const system = SITE_KNOWLEDGE + (page ? '\n\nThe user is currently on the page: ' + page : '');` `const out = await client.messages.create({ model: MODEL, max_tokens: MAX_TOKENS, system, messages });`
    - `const reply = out.content.filter((b:any)=>b.type==='text').map((b:any)=>b.text).join('\n').trim() || "Sorry — I couldn't generate a reply.";` `res.json({ configured: true, reply });`
    - catch: `console.error('[assistant] chat failed:', err); res.status(502).json({ error: 'The assistant is temporarily unavailable.' });`
    - `export default router;`

    Verify the `client.messages.create` signature against the installed `@anthropic-ai/sdk` version.

    In `backend/src/app.ts`: add `import assistantRouter from './routes/assistant';` and `app.use('/api/assistant', assistantRouter);` (place it alongside the other `app.use('/api/...', ...)` mounts).
  </action>
  <verify>`cd /Users/jeet/turion-satellite/backend && npx tsc --noEmit` passes. `grep -n "api/assistant" src/app.ts` shows the mount. `grep -n "requireAuth" src/routes/assistant.ts` present. `grep -n "err.message" src/routes/assistant.ts` returns nothing.</verify>
  <done>`POST /chat` route exists with `requireAuth`, lazy memoized secret fetch, graceful-no-key (`200 {configured:false}`), 400 on bad messages, 502 on Anthropic failure, hardened catch; mounted at `/api/assistant` in `app.ts`; `tsc --noEmit` clean.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest + supertest coverage for the route</name>
  <files>/Users/jeet/turion-satellite/backend/src/routes/__tests__/assistant.test.ts</files>
  <action>
    Add `backend/src/routes/__tests__/assistant.test.ts` (match the existing test layout — check where the repo's other route tests live, e.g. `routes/__tests__/` or a top-level `tests/`; mirror that). Cover:
    1. **401 no auth** — POST `/api/assistant/chat` with no/invalid JWT → expect 401.
    2. **200 configured:false (no key)** — with `ANTHROPIC_API_KEY` and `ANTHROPIC_API_KEY_ARN` both unset (and `cachedKey` reset if the test imports the module fresh — `vi.resetModules()`), a valid-auth POST → expect `200`, body `{ configured: false, reply: <non-empty string> }`.
    3. **200 configured:true (mocked Anthropic)** — `vi.mock('@anthropic-ai/sdk', ...)` so `messages.create` resolves `{ content: [{ type: 'text', text: 'On sat.html, use the status dropdown.' }] }`; set `process.env.ANTHROPIC_API_KEY = 'sk-ant-test'` (reset module cache); valid-auth POST with `{ messages: [{role:'user', content:'how do I advance a stage?'}], page: '/satellite/sat.html' }` → expect `200`, body `{ configured: true, reply: 'On sat.html, use the status dropdown.' }`.
    4. **400 bad messages** — with a key set + Anthropic mocked, POST `{ messages: [] }` or `{ messages: [{role:'assistant', content:'hi'}] }` → expect `400`.
    Use whatever test-JWT minting helper the repo's other route tests use for the valid-auth cases (check `routes/sales-orders.ts` tests).
  </action>
  <verify>`cd /Users/jeet/turion-satellite/backend && npx vitest run src/routes/__tests__/assistant.test.ts` — all 4 cases pass. Also run the full suite `npx vitest run` — no regressions.</verify>
  <done>4 vitest cases (401, 200 configured:false, 200 configured:true mocked, 400) pass; full backend suite green.</done>
</task>

</tasks>

<verification>
- `cd /Users/jeet/turion-satellite/backend && npx tsc --noEmit` clean
- `npx vitest run` — all tests pass including the new `assistant.test.ts`
- `grep -n "@anthropic-ai/sdk" package.json` shows it under `"dependencies"`
- `grep -n "app.use('/api/assistant'" src/app.ts` (or equivalent) present
- `node scripts/audit-satellite-buttons.mjs` (run from `backend/`) still reports `violations: 0` (the new route adds an allowlist entry — harmless; no `onclick` added)
- Commits in `/Users/jeet/turion-satellite` use `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit ...` — DO NOT push (Plan 03's deploy task pushes + redeploys). Commit `package.json`, `package-lock.json`, `src/assistant-knowledge.ts`, `src/routes/assistant.ts`, `src/app.ts`, and the test file.
</verification>

<success_criteria>
- `POST /api/assistant/chat` mounted in `app.ts`, gated by `requireAuth`
- No-key → `200 {configured:false, reply}`; mocked-key → `200 {configured:true, reply}`; bad messages → `400`; Anthropic failure → `502 {error}`; no `err.message` leak
- `SITE_KNOWLEDGE` covers pages / navigation / Phase-33 workflow / make-buy / data-location / common-tasks
- `@anthropic-ai/sdk` in `dependencies`, lockfile updated
- vitest green; `tsc --noEmit` clean; button audit still 0 violations
- Changes committed (not pushed) in `/Users/jeet/turion-satellite` with the correct identity
</success_criteria>

<output>
After completion, create `.planning/phases/34-in-site-chat-assistant/34-01-SUMMARY.md` (commit it to /Users/jeet/doordash-p2p with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar"`).
</output>
