---
phase: 34-in-site-chat-assistant
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - /Users/jeet/turion-space-demo/satellite/satellite-chat.js
  - /Users/jeet/turion-space-demo/satellite/index.html
  - /Users/jeet/turion-space-demo/satellite/sat.html
  - /Users/jeet/turion-space-demo/satellite/bom.html
  - /Users/jeet/turion-space-demo/satellite/kanban.html
  - /Users/jeet/turion-space-demo/satellite/instance.html
  - /Users/jeet/turion-space-demo/satellite/part.html
  - /Users/jeet/turion-space-demo/satellite/parts.html
  - /Users/jeet/turion-space-demo/satellite/work-order.html
  - /Users/jeet/turion-space-demo/satellite/work-orders.html
  - /Users/jeet/turion-space-demo/satellite/cost.html
  - /Users/jeet/turion-space-demo/satellite/cost-detail.html
  - /Users/jeet/turion-space-demo/satellite/program-new.html
autonomous: true
requirements: [ChatWidget]

must_haves:
  truths:
    - "Every satellite content page (12 pages) shows a floating chat button bottom-right that opens a chat panel"
    - "Sending a message POSTs to /api/assistant/chat via window.satelliteApi.post with the recent history and the current page path"
    - "The widget renders the assistant reply; on {configured:false} it shows the not-configured message and disables the textarea"
    - "The widget uses ZERO inline onclick — all events via addEventListener — so the Phase-29 button audit stays 0 violations"
    - "satellite-chat.js is the LAST script on each page (after satellite-api.js / satellite-render.js) so window.satelliteApi exists when it loads"
    - "login.html and 3d-test.html do NOT get the script (pre-auth / dev harness)"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/satellite/satellite-chat.js"
      provides: "self-injecting chat widget — button + panel DOM created in JS, addEventListener wiring, calls satelliteApi.post('/api/assistant/chat', {messages, page})"
      contains: "/api/assistant/chat"
      min_lines: 80
  key_links:
    - from: "/Users/jeet/turion-space-demo/satellite/satellite-chat.js"
      to: "/api/assistant/chat"
      via: "window.satelliteApi.post"
      pattern: "satelliteApi\\.post\\(['\"]\\/api\\/assistant\\/chat"
    - from: "/Users/jeet/turion-space-demo/satellite/index.html"
      to: "/Users/jeet/turion-space-demo/satellite/satellite-chat.js"
      via: "<script src> tag (last script)"
      pattern: "satellite-chat\\.js"
    - from: "/Users/jeet/turion-space-demo/satellite/sat.html"
      to: "/Users/jeet/turion-space-demo/satellite/satellite-chat.js"
      via: "<script src> tag"
      pattern: "satellite-chat\\.js"
---

<objective>
Add the frontend half of the chat assistant: a new shared vanilla-JS module `satellite/satellite-chat.js` that self-injects a floating chat button + a chat panel on page load (all DOM created in JS, all events via `addEventListener` — zero inline `onclick`), calls `window.satelliteApi.post('/api/assistant/chat', {messages, page})` (already auth-bearing, on-401-refresh wrapper, and on the button-audit allowlist now that Plan 01 mounted the route), passes `location.pathname` so answers are page-aware, and renders `{configured:false}` gracefully. Then add one `<script src="/satellite/satellite-chat.js">` line to each of the 12 satellite content pages.

Purpose: Gives users help on every page without a 12-file markup copy-paste — the per-page change is just one `<script>` line. Depends on Plan 01 because the audit derives its allowlist from `app.ts`, and the route must resolve.
Output: `satellite/satellite-chat.js` + one `<script>` line in `index/sat/bom/kanban/instance/part/parts/work-order/work-orders/cost/cost-detail/program-new.html`.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-in-site-chat-assistant/34-RESEARCH.md
@.planning/phases/34-in-site-chat-assistant/34-01-PLAN.md

# Frontend repo (NOT in this GSD repo):
# /Users/jeet/turion-space-demo/satellite/satellite-api.js     — window.satelliteApi.{get,post,patch}(path, body) — Bearer + on-401-refresh-retry + redirect-to-login
# /Users/jeet/turion-space-demo/satellite/satellite-config.js  — config
# /Users/jeet/turion-space-demo/satellite/satellite-render.js  — window.satelliteRender.topbarHTML(email); each page's inline IIFE renders #topbar from it
# /Users/jeet/turion-space-demo/satellite/*.html               — hand-written static pages; script load order: config → supabase UMD → auth → api → page-specific → render → inline IIFE
# /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs — parses app.ts for route allowlist + scans frontend for `onclick=` and `satelliteApi.{get,post,patch}(...)`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write satellite/satellite-chat.js (self-injecting widget)</name>
  <files>/Users/jeet/turion-space-demo/satellite/satellite-chat.js</files>
  <action>
    Create `satellite/satellite-chat.js` as an IIFE `(function () { ... })();` that, on load:
    1. Guard: `if (!window.satelliteApi) { console.warn('[satellite-chat] satelliteApi not ready'); return; }`
    2. Inject a `<style>` block (append to `document.head`) with `.chat-*` rules — keep it fully self-contained (scoped class names; fixed positioning) so it can't break any page. Bottom-right: `#chat-fab` (a round button), `#chat-panel` (hidden by default — `display:none` or `.open` toggle) containing a header (title + a close `×` button), `#chat-log` (scrollable message list), and a footer with a `<textarea>` + a Send button.
    3. Create those elements in JS (`document.createElement`), append `#chat-fab` and `#chat-panel` to `document.body`. NO `innerHTML` with event handlers; NO inline `onclick` anywhere.
    4. State: `var history = [];` — `[{role:'user'|'assistant', content:string}]`. A `render()` function that rebuilds `#chat-log` from `history` (plus any system/status message), `textContent` for user/assistant content (no HTML injection).
    5. Wiring (all `addEventListener`):
       - `chatFab.addEventListener('click', () => panel.classList.toggle('open'))` (or set `display`).
       - close button → close the panel.
       - Send button `addEventListener('click', send)`; textarea `addEventListener('keydown', e => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); send(); } })`.
    6. `async function send()`:
       - `var text = ta.value.trim(); if (!text) return;`
       - `history.push({role:'user', content:text}); render(); ta.value = '';`
       - show a transient "…" thinking indicator.
       - `try { var data = await window.satelliteApi.post('/api/assistant/chat', { messages: history.slice(-8), page: location.pathname }); ` — handle: if `data && data.configured === false` → append `data.reply` as a system message, set `ta.disabled = true`, hide the thinking indicator, `return;` else `history.push({role:'assistant', content: data.reply}); render();` `} catch (e) { append a system message 'Sorry — the assistant is unavailable right now.'; } finally { remove the thinking indicator; }`
    7. On first open, show a one-line greeting system message ("Hi! Ask me how to navigate the satellite app.").
    Keep it ~100-200 lines, dependency-free except `window.satelliteApi`.
  </action>
  <verify>`grep -n "onclick" /Users/jeet/turion-space-demo/satellite/satellite-chat.js` returns nothing. `grep -n "satelliteApi.post('/api/assistant/chat'" /Users/jeet/turion-space-demo/satellite/satellite-chat.js` present. `node --check /Users/jeet/turion-space-demo/satellite/satellite-chat.js` passes.</verify>
  <done>`satellite-chat.js` exists: self-injects button + panel, addEventListener-only, calls `satelliteApi.post('/api/assistant/chat', {messages, page})`, handles `{configured:false}` (shows message + disables textarea), renders replies; syntactically valid; zero `onclick`.</done>
</task>

<task type="auto">
  <name>Task 2: Add the <script src> line to all 12 satellite content pages</name>
  <files>/Users/jeet/turion-space-demo/satellite/index.html, /Users/jeet/turion-space-demo/satellite/sat.html, /Users/jeet/turion-space-demo/satellite/bom.html, /Users/jeet/turion-space-demo/satellite/kanban.html, /Users/jeet/turion-space-demo/satellite/instance.html, /Users/jeet/turion-space-demo/satellite/part.html, /Users/jeet/turion-space-demo/satellite/parts.html, /Users/jeet/turion-space-demo/satellite/work-order.html, /Users/jeet/turion-space-demo/satellite/work-orders.html, /Users/jeet/turion-space-demo/satellite/cost.html, /Users/jeet/turion-space-demo/satellite/cost-detail.html, /Users/jeet/turion-space-demo/satellite/program-new.html</files>
  <action>
    First confirm the page list: `ls /Users/jeet/turion-space-demo/satellite/*.html`. The 12 content pages to touch: `index.html`, `sat.html`, `bom.html`, `kanban.html`, `instance.html`, `part.html`, `parts.html`, `work-order.html`, `work-orders.html`, `cost.html`, `cost-detail.html`, `program-new.html`. **DO NOT** add it to `login.html` (pre-auth — `satelliteApi`/session won't exist) or `3d-test.html` (dev harness — skip).

    On each of the 12 pages, add exactly one line:
    `<script src="/satellite/satellite-chat.js"></script>`
    placed AFTER `satellite-api.js` (and after `satellite-render.js` if present) — i.e. as the LAST `<script src>` on the page, right before `</body>` (or after the page's inline `#topbar` IIFE if that's the last block). The widget guards on `window.satelliteApi`, so order matters: it must come after `satellite-api.js`.

    If a page already has a final inline `<script>` IIFE (the `#topbar` render), put the `satellite-chat.js` `<script src>` line immediately AFTER that inline block (or before it — both work since the IIFE is synchronous and `satelliteApi` is set earlier; placing it last is cleanest).
  </action>
  <verify>`for f in index sat bom kanban instance part parts work-order work-orders cost cost-detail program-new; do echo -n "$f: "; grep -c "satellite-chat.js" /Users/jeet/turion-space-demo/satellite/$f.html; done` — every page reports `1`. `grep -c "satellite-chat.js" /Users/jeet/turion-space-demo/satellite/login.html /Users/jeet/turion-space-demo/satellite/3d-test.html` — both `0`. Spot-check one page that the line is after `satellite-api.js`: `grep -n "satellite-api.js\|satellite-chat.js" /Users/jeet/turion-space-demo/satellite/sat.html`.</verify>
  <done>All 12 content pages have exactly one `<script src="/satellite/satellite-chat.js">` line, positioned after `satellite-api.js`; `login.html` and `3d-test.html` do not.</done>
</task>

<task type="auto">
  <name>Task 3: Run the button audit in both repos — expect 0 violations</name>
  <files>(verification only — no files modified)</files>
  <action>
    Run `node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` — the script scans `turion-satellite/backend` AND `turion-space-demo/satellite`. It parses `app.ts` for the route allowlist (Plan 01 added `POST /api/assistant/chat`) and scans the frontend for `onclick=` attributes + `satelliteApi.{get,post,patch}(...)` calls — each must resolve to an allowlisted route. The widget uses `satelliteApi.post('/api/assistant/chat', ...)` (resolves) and no inline `onclick` (nothing new to flag). Expect `violations: 0, exit 0` for both repos.

    If the audit reports a missing endpoint for `/api/assistant/chat`: that means Plan 01's `app.ts` mount didn't land — STOP and fix Plan 01's `app.ts` before continuing (the audit is the gate). If it reports an `onclick` violation in `satellite-chat.js`: replace that `onclick` with `addEventListener`.
  </action>
  <verify>`cd /Users/jeet/turion-satellite/backend && node scripts/audit-satellite-buttons.mjs; echo "exit=$?"` → output shows `violations: 0` for both `turion-satellite/backend` and `turion-space-demo`, `exit=0`.</verify>
  <done>Button audit reports 0 violations in both repos, exit 0.</done>
</task>

</tasks>

<verification>
- `node --check /Users/jeet/turion-space-demo/satellite/satellite-chat.js` passes
- `grep -rn "onclick" /Users/jeet/turion-space-demo/satellite/satellite-chat.js` → nothing
- All 12 content pages reference `satellite-chat.js` exactly once; `login.html` / `3d-test.html` do not
- `node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` → 0 violations both repos, exit 0
- Commits in `/Users/jeet/turion-space-demo` use `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit ...` — DO NOT push (Plan 03's deploy task pushes + deploys). Commit `satellite/satellite-chat.js` and the 12 modified HTML pages.
</verification>

<success_criteria>
- `satellite-chat.js` exists: self-injecting widget, addEventListener-only, calls `satelliteApi.post('/api/assistant/chat', {messages, page})`, page-aware via `location.pathname`, renders `{configured:false}` gracefully
- All 12 content pages load it (last script, after `satellite-api.js`); `login.html` / `3d-test.html` excluded
- Button audit 0 violations both repos
- Changes committed (not pushed) in `/Users/jeet/turion-space-demo` with the correct identity
</success_criteria>

<output>
After completion, create `.planning/phases/34-in-site-chat-assistant/34-02-SUMMARY.md` (commit it to /Users/jeet/doordash-p2p with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar"`).
</output>
