---
phase: 34-in-site-chat-assistant
plan: 03
type: execute
wave: 3
depends_on: ["34-01", "34-02"]
files_modified:
  - /Users/jeet/doordash-p2p/.planning/STATE.md
  - /Users/jeet/doordash-p2p/.planning/ROADMAP.md
  - /Users/jeet/doordash-p2p/.planning/phases/34-in-site-chat-assistant/34-03-SUMMARY.md
autonomous: false
requirements: [ChatEndpoint, SiteKnowledgePrompt, ChatWidget, GracefulNoKey]
user_setup:
  - service: anthropic
    why: "The chat assistant calls the Anthropic Messages API; the phase ships in the {configured:false} state until the secret + Lambda env var + resource policy are added."
    env_vars:
      - name: ANTHROPIC_API_KEY_ARN
        source: "Lambda env var on function `turion-satellite-api` — set to the full ARN of the new secret (keep existing DATABASE_URL_ARN, SUPABASE_JWT_SECRET_ARN, S3_FILES_BUCKET)"
    dashboard_config:
      - task: "Create AWS Secrets Manager secret `turion-satellite/production/anthropic-key` (us-east-1, acct 134607809447) with payload {\"ANTHROPIC_API_KEY\":\"sk-ant-...\"}"
        location: "AWS Secrets Manager (us-east-1)"
      - task: "Attach a resource policy to that secret granting role arn:aws:iam::134607809447:role/zietra-api-lambda-role secretsmanager:GetSecretValue (the Lambda role has NO secretsmanager:* IAM permission — per-secret resource policy is how database-url works)"
        location: "aws secretsmanager put-resource-policy --secret-id turion-satellite/production/anthropic-key --resource-policy '{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"AWS\":\"arn:aws:iam::134607809447:role/zietra-api-lambda-role\"},\"Action\":\"secretsmanager:GetSecretValue\",\"Resource\":\"*\"}]}'"
      - task: "Set the Lambda env var: aws lambda update-function-configuration --function-name turion-satellite-api --environment 'Variables={...existing...,ANTHROPIC_API_KEY_ARN=<the-secret-arn>}'"
        location: "AWS Lambda (us-east-1)"

must_haves:
  truths:
    - "turion-satellite Lambda redeployed via build-and-push.sh — CodeSha256 changes from before→after"
    - "POST /api/assistant/chat on the live API returns 200 {configured:false} (the key is NOT set at deploy time) — and 401 unauthenticated, 404 on a bogus sibling path"
    - "turion-space-demo frontend deployed via deploy-frontend.sh (with the F6 pre-flight stash/restore) and CloudFront invalidation reaches Completed"
    - "satellite-chat.js is reachable at https://turionspace.zietra.com/satellite/satellite-chat.js (HTTP 200) and at least 2 content pages grep for satellite-chat.js"
    - "Button audit reports 0 violations in BOTH repos"
    - "STATE.md + ROADMAP.md updated to mark Phase 34 complete and document the user's secret/env-var/resource-policy steps"
  artifacts:
    - path: "/Users/jeet/doordash-p2p/.planning/STATE.md"
      provides: "Phase 34 complete entry + the 'to light it up' user steps"
      contains: "Phase 34"
    - path: "/Users/jeet/doordash-p2p/.planning/ROADMAP.md"
      provides: "Phase 34 plan checkboxes + plan count updated"
      contains: "Phase 34"
    - path: "/Users/jeet/doordash-p2p/.planning/phases/34-in-site-chat-assistant/34-03-SUMMARY.md"
      provides: "deploy + verification records"
  key_links:
    - from: "build-and-push.sh"
      to: "turion-satellite-api Lambda"
      via: "Docker build + ECR push + Lambda update; CodeSha256 before→after recorded"
      pattern: "CodeSha256"
    - from: "deploy-frontend.sh"
      to: "S3 turion-demo-static + CloudFront E37R9PT8IL44L2"
      via: "aws s3 sync . --delete + CF invalidation"
      pattern: "satellite-chat\\.js"
---

<objective>
Ship Phase 34: push both repos, redeploy the turion-satellite Lambda (`build-and-push.sh`), deploy the turion-space-demo frontend (`deploy-frontend.sh` with the F6 deploy-hygiene pre-flight), invalidate CloudFront, curl-smoke the live `POST /api/assistant/chat` (expect `200 {configured:false}` — the key isn't set yet — plus `401` unauthenticated and `404` on a bogus path), curl-smoke a couple of `https://turionspace.zietra.com/satellite/*.html` pages + `satellite-chat.js`, run the button audit in both repos (0 violations), then update STATE.md + ROADMAP.md and document the user's secret-setup steps.

Purpose: Make the assistant live (in its `{configured:false}` form) and hand the user the exact AWS steps to "light it up." The smoke test asserts the graceful-no-key path because that IS the expected state at deploy time.
Output: redeployed Lambda + frontend, verification records, updated STATE/ROADMAP, `34-03-SUMMARY.md`.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-in-site-chat-assistant/34-RESEARCH.md
@.planning/phases/34-in-site-chat-assistant/34-01-PLAN.md
@.planning/phases/34-in-site-chat-assistant/34-02-PLAN.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-06-SUMMARY.md

# Deploy scripts (NOT in this GSD repo):
# /Users/jeet/turion-satellite/build-and-push.sh   — Docker build + ECR push + Lambda update for turion-satellite-api
# /Users/jeet/turion-space-demo/deploy-frontend.sh — aws s3 sync . --delete (excludes backend/*, *.sh, *.md, deploy-*, .git/*) + CF invalidate /*
# Live API: https://rjydekliee.execute-api.us-east-1.amazonaws.com   ·   Live site: https://turionspace.zietra.com
# CloudFront distribution: E37R9PT8IL44L2 · S3 bucket: turion-demo-static
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push + redeploy turion-satellite Lambda; smoke the new route</name>
  <files>(deploy + smoke — no GSD-repo files modified in this task)</files>
  <action>
    In `/Users/jeet/turion-satellite`:
    1. Confirm Plan 01's commits are present (`git log --oneline -5` — `assistant.ts`, `assistant-knowledge.ts`, `app.ts` mount, `package.json` + `package-lock.json` with `@anthropic-ai/sdk`, the test). If anything is uncommitted, commit it with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit ...`.
    2. `aws lambda get-function-configuration --function-name turion-satellite-api --query CodeSha256 --output text` → record **before**.
    3. `git push origin main` (record the push range).
    4. `./build-and-push.sh` (NOT manual docker/ecr/ecs — this script is the deploy path for this app).
    5. `aws lambda get-function-configuration --function-name turion-satellite-api --query CodeSha256 --output text` → record **after**; assert it changed.
    6. Smoke against `https://rjydekliee.execute-api.us-east-1.amazonaws.com`:
       - `curl -s -o /dev/null -w '%{http_code}' -X POST https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/assistant/chat -H 'Content-Type: application/json' -d '{}'` → expect **401** (no auth).
       - If you can mint a valid test JWT the way the repo's tests do, POST with `{messages:[{role:'user',content:'hi'}]}` + the `Authorization: Bearer` header → expect **200**, body `{"configured":false,...}` (the secret isn't set at deploy time — this is the expected pass). If no JWT is available, the 401 above + the unit test (Plan 01 case 2) cover the `{configured:false}` path; record that you accepted 401-only.
       - `curl -s -o /dev/null -w '%{http_code}' https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/assistant/this-does-not-exist` → expect **404** (sanity: not everything 401s).
       - `curl -s https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/health` → `{"db":"ok",...}`.
  </action>
  <verify>CodeSha256 before ≠ after recorded; `POST /api/assistant/chat` → 401 (no auth); bogus path → 404; `/api/health` ok; (if JWT available) authed POST → 200 {configured:false}.</verify>
  <done>Lambda redeployed (CodeSha256 changed); new route live and behaves: 401 unauth / 200 {configured:false} authed / 404 bogus / health ok.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy turion-space-demo frontend with the F6 pre-flight; post-deploy smoke + audit</name>
  <files>(deploy + smoke — no GSD-repo files modified in this task)</files>
  <action>
    In `/Users/jeet/turion-space-demo`:
    1. Confirm Plan 02's commits are present (`git log --oneline -5` — `satellite/satellite-chat.js` + the 12 modified HTML pages). If uncommitted, commit with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit ...`. `git push origin main` (record range).
    2. **F6 deploy-hygiene pre-flight** (per Phase 33's `33-06-SUMMARY.md`): `git stash push -- about-this-demo.html agent-sales-cash.html dashboard-cio.html` (and any other dirty ROOT HTML — check `git status`); `mv .superpowers /tmp/superpowers-stash-34` (if it exists). Confirm `git status` shows no WIP root HTML and `.superpowers` is gone. (`deploy-frontend.sh` already excludes `backend/*`, `.git/*`, `*.sh`, `*.md`, `deploy-*` — dirty `backend/` files are safe; it's the root HTML + `.superpowers` that must be stashed.)
    3. `./deploy-frontend.sh` (NOT manual `aws s3 sync` — use the script). Record the CloudFront invalidation ID it prints.
    4. **Restore (even if the deploy fails):** `git stash pop`; `mv /tmp/superpowers-stash-34 .superpowers`. Confirm `git stash list` empty and the working tree == pre-deploy baseline.
    5. Poll the CF invalidation to `Completed`: `aws cloudfront get-invalidation --distribution-id E37R9PT8IL44L2 --id <id> --query Invalidation.Status`.
    6. Smoke `https://turionspace.zietra.com/satellite/...` (all HTTP 200):
       - `satellite-chat.js` → 200, `content-type: text/javascript` (or `application/javascript`), and the body contains `/api/assistant/chat`.
       - `sat.html` → 200, `grep("satellite-chat.js")` ≥ 1.
       - `index.html` → 200, `grep("satellite-chat.js")` ≥ 1.
       - `bom.html` / `part.html` → 200 (Phase-27..33 regression check — also `part.html grep("mount3DViewer")` still present, `sat.html grep("programProgress")` still present).
       - `login.html` → 200, `grep("satellite-chat.js")` == 0 (correctly excluded).
    7. Run `node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` → expect `violations: 0` for BOTH `turion-satellite/backend` and `turion-space-demo`, exit 0.
  </action>
  <verify>CF invalidation reaches `Completed`; `satellite-chat.js` 200 + contains `/api/assistant/chat`; `sat.html` & `index.html` grep `satellite-chat.js` ≥1; `login.html` grep == 0; Phase-27..33 regression substrings still present; button audit 0 violations both repos; working tree restored (stash list empty, `.superpowers` back).</verify>
  <done>Frontend deployed with F6 pre-flight/restore; CF invalidation Completed; widget JS live + linked on content pages; audit clean; tree restored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify the live chat assistant (headless-substitute OK)</name>
  <files>(verification only — no files modified)</files>
  <what-built>The in-site chat assistant: backend route `POST /api/assistant/chat` (live, in `{configured:false}` state since no key yet) + the floating chat widget on every satellite content page (`satellite-chat.js`). Headless-substitute verification done: Lambda CodeSha256 changed, route smoked (401 unauth / 404 bogus / health ok — and the unit test covers the 200 {configured:false} path), frontend deployed + CF invalidation Completed, `satellite-chat.js` 200 and linked on 12 content pages, button audit 0 violations in both repos — consistent with the Phases 27-33 headless-substitute precedent.</what-built>
  <action>
    PAUSE for the user. Present the headless-substitute verification results (CodeSha256 before→after, the 401/404/health smoke, the {configured:false} unit-test coverage, the deployed `satellite-chat.js` 200 + page links, the 0-violation button audit in both repos) and offer the optional live browser walk below. The headless smoke + audit stand in for the browser visual sign-off, per the Phases 27-33 precedent — so "approved" without a browser walk is acceptable.

    Optional live browser walk:
    1. Visit https://turionspace.zietra.com/satellite/index.html — magic-link sign in.
    2. Confirm a floating chat button appears bottom-right; click it → a chat panel opens with a greeting.
    3. Type "how do I create a new satellite program?" + Send → expect the assistant reply to be the "not configured yet" message (the key isn't set) and the textarea to disable.
    4. Navigate to another page (e.g. `sat.html`) → the chat button is there too.
    5. Browser devtools console — no errors from `satellite-chat.js`.

    The "light it up" steps (these are the USER's to run, not the executor's):
    - Create secret `turion-satellite/production/anthropic-key` (us-east-1, acct 134607809447), payload `{"ANTHROPIC_API_KEY":"sk-ant-..."}`.
    - `aws secretsmanager put-resource-policy --secret-id turion-satellite/production/anthropic-key --resource-policy '{"Version":"2012-10-17","Statement":[{"Effect":"Allow","Principal":{"AWS":"arn:aws:iam::134607809447:role/zietra-api-lambda-role"},"Action":"secretsmanager:GetSecretValue","Resource":"*"}]}'`
    - `aws lambda update-function-configuration --function-name turion-satellite-api --environment 'Variables={DATABASE_URL_ARN=...,SUPABASE_JWT_SECRET_ARN=...,S3_FILES_BUCKET=...,ANTHROPIC_API_KEY_ARN=<the-new-secret-arn>}'` (keep ALL existing vars; add the new one).
  </action>
  <how-to-verify>
    Live browser walk (optional — headless smoke + audit already stand in, per phases 27-33):
    1. https://turionspace.zietra.com/satellite/index.html → sign in → floating chat button bottom-right → opens a panel with a greeting.
    2. Send "how do I create a new satellite program?" → reply is the "not configured yet" message (no key yet), textarea disables.
    3. Another page (e.g. `sat.html`) → chat button present; no console errors from `satellite-chat.js`.
  </how-to-verify>
  <verify>Headless-substitute evidence presented (CodeSha256 changed, route smoke 401/404/health, {configured:false} unit-test, `satellite-chat.js` 200 + page links, button audit 0 violations both repos). User responds "approved" or describes issues.</verify>
  <done>User approves (headless-substitute accepted) — or, if a browser walk is done: the chat button appears + opens on at least 2 pages, sending shows the "not configured yet" message + disables the textarea, no `satellite-chat.js` console errors.</done>
  <resume-signal>Type "approved" (headless-substitute is fine) or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md + ROADMAP.md; commit docs</name>
  <files>/Users/jeet/doordash-p2p/.planning/STATE.md, /Users/jeet/doordash-p2p/.planning/ROADMAP.md, /Users/jeet/doordash-p2p/.planning/phases/34-in-site-chat-assistant/34-03-SUMMARY.md</files>
  <action>
    STATE.md is narrative-format (gsd-tools can't parse it — edit it manually, consistent with phases 27-33):
    1. **STATE.md** — rewrite "Current Position" to "**Phase 34 COMPLETE (3/3 plans)** — in-site AI chat assistant". Recap: `@anthropic-ai/sdk` dep + `assistant-knowledge.ts` (`SITE_KNOWLEDGE`) + `routes/assistant.ts` (`POST /api/assistant/chat`, `requireAuth`, lazy memoized secret fetch, graceful-no-key 200 {configured:false}, 502 on Anthropic failure) + `app.ts` mount + vitest; `satellite/satellite-chat.js` self-injecting widget on 12 content pages; Lambda CodeSha256 before→after; CF invalidation id; F6 pre-flight done; smoke results (401 unauth / 200 {configured:false} / 404 bogus / health ok / `satellite-chat.js` 200); button audit 0 violations both repos. **Plus the "to light it up" user steps**: create secret `turion-satellite/production/anthropic-key` (us-east-1, payload `{"ANTHROPIC_API_KEY":"sk-ant-..."}`), attach the resource policy granting `zietra-api-lambda-role` `secretsmanager:GetSecretValue`, set the `ANTHROPIC_API_KEY_ARN` Lambda env var — until then the widget shows "not configured". Move the prior Phase-33 position into a `<details>` block.
    2. **ROADMAP.md** — under `### Phase 34`: change `**Plans:** 0 plans` → `**Plans:** 3 plans (3 waves) — complete`; replace the `Plans:` list with:
       - `- [x] 34-01-PLAN.md — backend: @anthropic-ai/sdk dep + assistant-knowledge.ts (SITE_KNOWLEDGE) + routes/assistant.ts (POST /api/assistant/chat, requireAuth, graceful-no-key) + app.ts mount + vitest`
       - `- [x] 34-02-PLAN.md — frontend: satellite/satellite-chat.js self-injecting widget + <script> line on 12 content pages; button audit 0 violations`
       - `- [x] 34-03-PLAN.md — deploy: build-and-push.sh Lambda redeploy + deploy-frontend.sh (F6 pre-flight) + CF invalidation + curl smoke (200 {configured:false}) + button audit both repos + STATE/ROADMAP; user adds the anthropic-key secret + resource policy + ANTHROPIC_API_KEY_ARN env var to light it up`
       (match how Phases 27-33 plan rows are recorded.)
    3. Write `34-03-SUMMARY.md` with the deploy + verification records (CodeSha256 before/after, push ranges, CF invalidation id, all smoke results, audit results, the F6 stash/restore, the deviations if any, the "to light it up" steps).
    4. Commit all GSD-repo doc changes (this repo, `/Users/jeet/doordash-p2p`) with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit -m "docs(34): in-site chat assistant — complete (3/3 plans)"`.
  </action>
  <verify>`grep -n "Phase 34 COMPLETE" /Users/jeet/doordash-p2p/.planning/STATE.md`; `grep -n "34-03-PLAN.md" /Users/jeet/doordash-p2p/.planning/ROADMAP.md`; `ls /Users/jeet/doordash-p2p/.planning/phases/34-in-site-chat-assistant/34-03-SUMMARY.md`; `cd /Users/jeet/doordash-p2p && git log --oneline -1` shows the docs commit by `jeet-avatar <jm@techcloudpro.com>`.</verify>
  <done>STATE.md says Phase 34 COMPLETE (3/3) with the user "light it up" steps; ROADMAP.md Phase 34 shows 3 plans complete; `34-03-SUMMARY.md` exists; docs committed with the correct identity.</done>
</task>

</tasks>

<verification>
- Lambda `turion-satellite-api` CodeSha256 changed; `POST /api/assistant/chat` → 401 unauth, 404 on a bogus sibling, `/api/health` ok; (if JWT) authed → 200 {configured:false}
- `https://turionspace.zietra.com/satellite/satellite-chat.js` → 200, contains `/api/assistant/chat`; `sat.html` & `index.html` link it; `login.html` doesn't; Phase-27..33 regression substrings intact
- CF invalidation Completed; F6 stash/restore done (working tree restored)
- `node scripts/audit-satellite-buttons.mjs` → 0 violations both repos, exit 0
- STATE.md + ROADMAP.md updated; `34-03-SUMMARY.md` written; docs committed with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar"`
- All deploys via the apps' own scripts (`build-and-push.sh`, `deploy-frontend.sh`) — NOT the dollor.ai CI/CD workflows, NOT manual docker/ecr/ecs/s3
</verification>

<success_criteria>
- Backend redeployed; new route live and behaves correctly (incl. graceful-no-key path asserted)
- Frontend deployed with F6 pre-flight; widget JS live + linked on all 12 content pages; tree restored
- Button audit 0 violations both repos
- STATE/ROADMAP mark Phase 34 complete; user's secret/resource-policy/env-var steps documented; `34-03-SUMMARY.md` written; docs committed
- Headless-substitute checkpoint approved (or live browser walk done)
</success_criteria>

<output>
After completion, ensure `.planning/phases/34-in-site-chat-assistant/34-03-SUMMARY.md` exists and is committed to /Users/jeet/doordash-p2p with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar"`.
</output>
