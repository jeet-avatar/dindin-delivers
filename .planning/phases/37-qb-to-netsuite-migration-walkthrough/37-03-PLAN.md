---
phase: 37-qb-to-netsuite-migration-walkthrough
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - /Users/jeet/turion-space-demo/quickbooks.html
  - /Users/jeet/turion-space-demo/quickbooks-coa.html
  - /Users/jeet/turion-space-demo/quickbooks-customers.html
  - /Users/jeet/turion-space-demo/quickbooks-vendors.html
  - /Users/jeet/turion-space-demo/quickbooks-items.html
  - /Users/jeet/turion-space-demo/quickbooks-invoices.html
  - /Users/jeet/turion-space-demo/quickbooks-bills.html
  - /Users/jeet/turion-space-demo/ramp.html
  - /Users/jeet/turion-space-demo/index.html
  - /Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js
autonomous: true
requirements: [QbMigrationWizard, RampMiniModule, NetSuiteGoLiveScreens]

must_haves:
  truths:
    - "User can navigate to /quickbooks from the home page top nav (green tile) — sees 6 record-type tiles with live status pills (new/migrated/total counts) fetched from GET /api/quickbooks/status"
    - "User can click any tile and reach the per-type wizard page (e.g. /quickbooks/customers → quickbooks-customers.html) which shows the 3-pane layout (QB rows left, field-map middle, NS preview right)"
    - "User can select QB rows via checkboxes, sees the count update, the field-map preview updates from GET /api/quickbooks/:type/mapping, and the right-pane NS preview updates client-side from the selected row"
    - "User clicks 'Migrate batch ▸', sees a confirm dialog with the count + target NS table, POSTs to /api/quickbooks/:type/migrate with the selected qbIds, and gets an nsToast success/error feedback"
    - "Post-migration, the wizard's left pane refreshes — the migrated rows show 'migrated' pills and become disabled (no double-migrate); the success toast includes a 'View in NetSuite →' link to the relevant existing page (e.g. customers → /sales/orders)"
    - "User can navigate to /ramp from the home page (purple-yellow tile) and use the SAME 3-pane wizard for corporate-card txns; the migrated rows land in turion.bills"
    - "Top nav of index.html has TWO new entries (QuickBooks green + Ramp purple/yellow) at the END of the existing module grid, in a 'Migration tools' section"
    - "All 8 new HTML pages use addEventListener (NOT inline onclick=) for user-defined handlers; all fetches use the template-literal pattern fetch(`${API_BASE}/api/...`) so audit-erp-buttons.mjs picks them up"
    - "All 8 clean URLs are registered in cf-function-source/turion-clean-urls.js (/quickbooks, /quickbooks/coa, /quickbooks/customers, /quickbooks/vendors, /quickbooks/items, /quickbooks/invoices, /quickbooks/bills, /ramp)"
    - "All 8 pages load TURION_CONFIG.API_BASE from turion-config.js — NEVER hardcode the APIGW host"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/quickbooks.html"
      provides: "Landing page: 6 type tiles + recent migration runs panel"
      min_lines: 120
    - path: "/Users/jeet/turion-space-demo/quickbooks-customers.html"
      provides: "Customers wizard (template for the other 5)"
      min_lines: 200
    - path: "/Users/jeet/turion-space-demo/ramp.html"
      provides: "Ramp mini-module wizard"
      min_lines: 200
    - path: "/Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js"
      provides: "8 new clean-URL rewrites added to the R map"
      contains: "/quickbooks"
    - path: "/Users/jeet/turion-space-demo/index.html"
      provides: "Migration tools section with 2 tiles (QB green + Ramp purple/yellow)"
      contains: "Migration tools"
  key_links:
    - from: "Each wizard's 'Migrate batch ▸' button"
      to: "POST /api/quickbooks/:type/migrate or POST /api/ramp/card-txns/migrate"
      via: "fetch(`${API_BASE}/api/quickbooks/${TYPE}/migrate`, { method:'POST', ... })"
      pattern: "fetch.*api/(quickbooks|ramp)"
    - from: "Wizard middle pane"
      to: "GET /api/quickbooks/:type/mapping"
      via: "fetch on page load"
      pattern: "fetch.*api/quickbooks.*mapping"
    - from: "Wizard left pane status pills"
      to: "GET /api/quickbooks/:type rows[].status"
      via: "fetch on page load + after every migrate POST"
      pattern: "renderLeft"
---

<objective>
Build the 8 new vanilla-HTML wizard pages + the navigation glue: `quickbooks.html` (landing with 6 type tiles), 6 per-type wizard pages (`quickbooks-{coa,customers,vendors,items,invoices,bills}.html`) using the canonical 3-pane CSS-grid layout, `ramp.html` (the single-record-type Ramp wizard with the same 3-pane treatment), top-nav additions on `index.html` (2 new tiles in a "Migration tools" section), and 8 clean-URL rewrites in `cf-function-source/turion-clean-urls.js`. All pages use the existing `turion-config.js` + `data-loader.js` + `ns-toast.js` stack — NO new shared JS.

Purpose: This IS the deliverable. The team's experience on Thursday: open turionspace.zietra.com, click "QuickBooks", pick a record type, see QB rows on the left + the field-map in the middle + the NS preview on the right, click "Migrate batch", see the rows move, click through to the existing NS pages and see them appear.

Output: 8 HTML files + nav additions + CF rewrites. All wired against the GET routes from 37-01 (already live in code) + the POST routes from 37-02 (parallel plan — wizard works against the 501 stub until 37-04 deploys 37-02; we accept this).

PARALLELISM NOTE: This plan and 37-02 run in parallel (Wave 2). They share NO files (37-02 is backend TS; this is frontend HTML + nav + CF). The wizard's POST URL signature is set by 37-01's route registration `r.post('/:type/migrate', ...)` — the body shape `{qbIds: string[]}` and the response shape `{ok, migrated, skipped, errors, summary}` are documented in 37-01's route stub + 37-02's plan. We code the wizard against this contract; if 37-02 ships first, the wizard works end-to-end after deploy; if this plan ships first, the wizard's button gets a 501 from the stub until 37-02 deploys (37-04 handles deploy).
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/37-qb-to-netsuite-migration-walkthrough/37-RESEARCH.md
@.planning/phases/37-qb-to-netsuite-migration-walkthrough/37-01-PLAN.md

# Read-only references (DO NOT modify — copy patterns):
@/Users/jeet/turion-space-demo/netsuite-items.html
@/Users/jeet/turion-space-demo/netsuite-new-item.html
@/Users/jeet/turion-space-demo/index.html
@/Users/jeet/turion-space-demo/data-loader.js
@/Users/jeet/turion-space-demo/ns-toast.js
@/Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create quickbooks.html (landing) + the 6 per-type QB wizard pages + ramp.html using the canonical 3-pane skeleton</name>
  <files>
    /Users/jeet/turion-space-demo/quickbooks.html
    /Users/jeet/turion-space-demo/quickbooks-coa.html
    /Users/jeet/turion-space-demo/quickbooks-customers.html
    /Users/jeet/turion-space-demo/quickbooks-vendors.html
    /Users/jeet/turion-space-demo/quickbooks-items.html
    /Users/jeet/turion-space-demo/quickbooks-invoices.html
    /Users/jeet/turion-space-demo/quickbooks-bills.html
    /Users/jeet/turion-space-demo/ramp.html
  </files>
  <action>
    PART A — `quickbooks.html` LANDING (~130 lines):
    - `<head>` script chain: `ns-toast.js` → `turion-config.js` → `data-loader.js` (research §"Page bootstrap").
    - `<body data-system="quickbooks">` with the standard header (green banner `background:#2ca01c;` per QB brand).
    - Main grid: 6 tiles, one per QB type (COA, Customers, Vendors, Items, Invoices, Bills), each with:
      - Title + icon emoji + a `.status-pills` row showing "X new · Y migrated · Z total" from `GET /api/quickbooks/status`.
      - A "Start migration ▸" link → `/quickbooks/{type}` (the per-type page).
      - A small dependency hint pill: "Recommended order: 1. COA → 2. Customers → 3. Vendors → 4. Items → 5. Invoices → 6. Bills" (per research §Pitfall 1).
    - Below the tiles, a `<section id="recentRuns">` showing the last 5 rows from `GET /api/quickbooks/runs?limit=5` — table of `run_type · started_at · migrated_count · error_count · summary`. Empty state: "No migration runs yet — start with COA above."
    - Inline-IIFE `<script>` at the bottom:
      - On DOMContentLoaded (NOT `turion-data-ready` — these are migration-specific endpoints, not in `/api/data/all`), Promise.all the two GETs, render the tiles + recent runs.
      - Re-poll `GET /api/quickbooks/status` every 8s (research §Plan Breakdown — optional but recommended).
      - All event handlers via addEventListener; no inline `onclick=` (except `window.location` navigation, which the audit allows).

    PART B — `quickbooks-customers.html` (~250 lines) — THE CANONICAL WIZARD TEMPLATE:
    Use the skeleton from research §"Frontend: wizard page skeleton" verbatim (it's the full file, ~110 lines as written; flesh out the stubbed helpers `extractField`, `applyMappingClientSide`, `renderRight` to ~250 lines).

    Key elements:
    - 3-pane CSS-grid: `display:grid; grid-template-columns:1fr 1fr 1fr; gap:14px; height:calc(100vh - 100px)`.
    - Left pane: paginated checkbox list of QB rows from `GET /api/quickbooks/customer`. Each row: `<input type="checkbox" data-qb-id="..." [disabled if status=migrated]>` + `qb_id` + a one-line summary from `source_data.DisplayName` + a status pill (`new`/`migrated`/`error`).
    - Middle pane: `<table id="mappingTable">` rendered from `GET /api/quickbooks/customer/mapping` — columns: QB field · Sample value (extracted from the top selected QB row) · NS field · Transform/notes.
    - Right pane: `<pre id="nsPreview">` showing the JSON of what the top selected row WOULD become after migration — apply the mapping client-side as a preview (the server re-applies authoritatively).
    - Sticky footer: `<button id="migrateBtn">` (disabled when 0 selected); clicking it opens `confirm()` ("Migrate N QB customers to NetSuite. This INSERTs N rows into turion.customers."); on confirm, POSTs to `/api/quickbooks/customer/migrate` with `{qbIds: [...]}`, awaits the response, shows `nsToast('✓ Migrated X (Y skipped, Z failed). View in NetSuite →', 'success')` with a clickable link to `/sales/orders` (the existing NS customer page), then refetches the left pane.

    The applyMappingClientSide function MUST mirror the server's logic for COA/Customers/Vendors/Items at minimum (the simpler 4 types are ~10 lines each; Invoices + Bills can be a best-effort preview that omits cross-ref resolution — note in a comment: "Server resolves CustomerRef/VendorRef; preview shows the QB ref string"). Don't duplicate the full server logic — it's a preview.

    PART C — The OTHER 5 wizard pages (COA, Vendors, Items, Invoices, Bills): copy `quickbooks-customers.html` and change ONLY:
    - `const TYPE = 'customer';` → `'coa'` / `'vendor'` / `'item'` / `'invoice'` / `'bill'`
    - `<title>` text + header text
    - The applyMappingClientSide branch invoked (or one shared function with a switch on TYPE)
    - The "View in NetSuite →" toast link target (research §"NetSuiteGoLiveScreens"):
      - coa → `/finance/chart-of-accounts`
      - customer → `/sales/orders` (existing NS customer-and-SO page)
      - vendor → `/procurement/orders`
      - item → `/inventory/items`
      - invoice → `/finance/revenue-management`
      - bill → `/procurement/orders` (bills live alongside POs)
    - Display field for the left-pane row label (Customers uses DisplayName; COA uses Name; Items uses Name; Invoices uses DocNumber; Bills uses DocNumber).

    PART D — `ramp.html` (~220 lines): same skeleton as `quickbooks-customers.html` but:
    - `TYPE = 'card_txn'`
    - All fetches against `/api/ramp/card-txns` (not `/api/quickbooks/...`)
    - Left pane row label: `ramp_id` + `source_data.merchant_name` + `$amount`
    - Right pane preview: apply the Ramp mapping client-side (mirror `applyRampMapping` from 37-02 — simple, 15 lines)
    - "Migrate batch ▸" POSTs to `/api/ramp/card-txns/migrate` with `{rampIds: [...]}`
    - Success toast: "Migrated N corporate-card txns to NetSuite Bills. View in NetSuite →" linking to `/procurement/orders`
    - Header banner color: `background: #7c3aed;` (Ramp purple) with a `#fbbf24` (yellow) accent strip.

    AVOID:
    - Inline `onclick=` for any user-defined handler (research §Pitfall 5 + Phase 36-08 audit). Use `addEventListener`. Inline `onclick="window.location='/...'"` for plain navigation IS allowed.
    - Hardcoding the APIGW URL (`lo254mvukl.execute-api...`). Use `window.TURION_CONFIG.API_BASE` (research §"Don't Hand-Roll" + Phase 36 rule).
    - Hardcoding the QB type as a string literal scattered through the file — declare `const TYPE = '...';` once at the top of the IIFE.
    - `<script>fetch('/api/quickbooks/...')</script>` without `${API_BASE}` — the audit script parses `fetch(\`${API_BASE}/api/...\`)` and a missing `${API_BASE}` will fail at deploy (the page is served from S3, fetch resolves against turionspace.zietra.com, NOT the APIGW host).
    - `alert()` / `prompt()` — use `nsToast()` for success/error, `confirm()` for the migrate-confirm modal (research §Don't Hand-Roll + the existing pattern in `netsuite-new-item.html:103`).
    - `extractField` returning the entire object when the path is `(derived)` — the middle-pane table renders `(derived)` as the QB field with `—` as the sample.

    NOTE on file conventions verified by research:
    - `<base href="/">` at the top of `<head>` is the existing pattern (research §Frontend bootstrap)
    - Existing pages use `class="sl"` for plain tables (`netsuite-items.html`); use the same to match the look
    - The `.pill.new`/`.pill.migrated`/`.pill.error` classes need defining — either inline `<style>` at the page top OR add to `ns-shared.css`. RECOMMEND: inline `<style>` per page (each is self-contained, no shared CSS coupling — matches the "single file per page" pattern).
  </action>
  <verify>
    `ls -1 /Users/jeet/turion-space-demo/quickbooks*.html /Users/jeet/turion-space-demo/ramp.html | wc -l` → 8.
    `grep -L "TURION_CONFIG.API_BASE" /Users/jeet/turion-space-demo/quickbooks*.html /Users/jeet/turion-space-demo/ramp.html` → empty (all pages reference it).
    `grep -l 'lo254mvukl' /Users/jeet/turion-space-demo/quickbooks*.html /Users/jeet/turion-space-demo/ramp.html` → empty (no hardcoded APIGW host).
    `grep -c "addEventListener" /Users/jeet/turion-space-demo/quickbooks-customers.html` → ≥3.
    `grep -E 'onclick=\"(?!window\\.location)' /Users/jeet/turion-space-demo/quickbooks*.html /Users/jeet/turion-space-demo/ramp.html 2>/dev/null` → empty (no inline user-handlers).
    `grep -c "fetch.*api/quickbooks\\|fetch.*api/ramp" /Users/jeet/turion-space-demo/quickbooks*.html /Users/jeet/turion-space-demo/ramp.html | grep -v ':0$' | wc -l` → ≥8 (every page calls the API).
  </verify>
  <done>
    8 new HTML pages exist, all use the 3-pane wizard pattern, all wire correctly through TURION_CONFIG.API_BASE + addEventListener, all have a "View in NetSuite →" deep-link in their success path.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add "Migration tools" section to index.html + register 8 clean URLs in cf-function-source/turion-clean-urls.js + commit</name>
  <files>
    /Users/jeet/turion-space-demo/index.html
    /Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js
  </files>
  <action>
    PART A — `index.html` (read the file first; find the END of the NS module grid, around line 410 per research):

    Insert AFTER the closing `</div>` of the last `<a class="ns-mod">` card the EXACT block from research §"Top-nav additions to index.html" — a `<div>` with `style="margin-top: 32px; padding: 18px; background: #f0fdf4; border-left: 3px solid #2ca01c; border-radius: 0 8px 8px 0;">` containing:
    - `<h3>⇄ Migration tools — bring your old books into NetSuite</h3>`
    - One descriptive `<p>` (research provides the wording)
    - A 2-column `.ns-module-grid` with two `<a class="ns-mod">` cards:
      - QuickBooks (green `#2ca01c` border): href=`/quickbooks`, h4="QuickBooks → NetSuite", role="Interactive migration walkthrough", bullets covering the 6 types + 3-pane wizard + audit trail
      - Ramp (yellow `#fbbf24` border, purple `#7c3aed` accent on the num): href=`/ramp`, h4="Ramp → NetSuite", role="Corporate card → Expense / Bill", bullets covering auto-categorization + 3-pane treatment

    The full HTML is in research §"Top-nav additions to index.html" — copy verbatim.

    AVOID: modifying anything else in `index.html`. The Phase 36 button audit covers it; gratuitous edits are noise.

    PART B — `/Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js`:

    Read the file. Find the `R` map (a const map of clean-URL → .html-key entries). Add 8 new entries (research §"URL routing"):

      '/quickbooks': '/quickbooks.html',
      '/quickbooks/coa': '/quickbooks-coa.html',
      '/quickbooks/customers': '/quickbooks-customers.html',
      '/quickbooks/vendors': '/quickbooks-vendors.html',
      '/quickbooks/items': '/quickbooks-items.html',
      '/quickbooks/invoices': '/quickbooks-invoices.html',
      '/quickbooks/bills': '/quickbooks-bills.html',
      '/ramp': '/ramp.html',

    Maintain the file's existing formatting (likely a JS const object literal). If the file uses a trailing-comma convention, follow it. If it uses key-without-quotes, follow that. Match the existing style — these entries get deployed by `deploy-frontend.sh` which updates the CF function automatically (research §URL routing).

    NOTE: The CF function update propagates in 5-15 min after deploy. Direct .html URLs (`/quickbooks.html`) work as a fallback during propagation. This is documented in research §"What's risky for the deadline".

    PART C — Local audit check (do NOT deploy yet — 37-04 handles deploy):

      cd /Users/jeet/turion-space-demo
      npm run audit-buttons 2>&1 | tail -20

    Expected output: "0 violations" or similar. If violations appear:
    - "fetch(...) not in allowlist" → check that the route's path matches one of the routers mounted in `backend/src/app.ts` (37-01 mounted `/api/quickbooks` + `/api/ramp`); template-literal segments `${TYPE}` should normalize to `:type` per `audit-erp-buttons.mjs:35`.
    - "onclick=... not defined" → swap to `addEventListener`. If it's a plain `window.location` navigation, it's allowlisted; if not, fix.

    Fix any violations before committing. Re-run `audit-buttons` until 0.

    PART D — Commit (do NOT push):

      cd /Users/jeet/turion-space-demo
      git status --short
      git -c user.email='jm@techcloudpro.com' -c user.name='jeet-avatar' \
        add quickbooks.html quickbooks-coa.html quickbooks-customers.html quickbooks-vendors.html quickbooks-items.html quickbooks-invoices.html quickbooks-bills.html ramp.html index.html cf-function-source/turion-clean-urls.js
      git -c user.email='jm@techcloudpro.com' -c user.name='jeet-avatar' commit -m \
        "feat(37-03): wizard frontend — 8 HTML pages + index.html migration tools section + CF clean URLs"
  </action>
  <verify>
    `grep -c '/quickbooks\\|/ramp' /Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js` → ≥8.
    `grep -n "Migration tools\\|QuickBooks → NetSuite\\|Ramp → NetSuite" /Users/jeet/turion-space-demo/index.html` → all 3 lines present.
    `cd /Users/jeet/turion-space-demo && npm run audit-buttons 2>&1 | tail -3` → "violations: 0" (or equivalent zero-state output).
    `cd /Users/jeet/turion-space-demo && git log -1 --pretty='%h %an %ae %s'` → "feat(37-03): …" by jeet-avatar / jm@techcloudpro.com.
  </verify>
  <done>
    index.html has the migration-tools section; 8 clean URLs registered; `npm run audit-buttons` returns 0 violations; commit landed with the correct author identity; no push yet.
  </done>
</task>

</tasks>

<verification>
End-of-plan audit + page-count smoke:

  ls -1 /Users/jeet/turion-space-demo/quickbooks*.html /Users/jeet/turion-space-demo/ramp.html | wc -l
  # expect 8

  grep -L "TURION_CONFIG.API_BASE" /Users/jeet/turion-space-demo/quickbooks*.html /Users/jeet/turion-space-demo/ramp.html
  # expect empty (all 8 pages reference it)

  cd /Users/jeet/turion-space-demo && npm run audit-buttons 2>&1 | grep -i "violation\\|error\\|0"
  # expect "0 violations" or equivalent

  cd /Users/jeet/turion-space-demo && git log --oneline -3
  # expect feat(37-03) on top
</verification>

<success_criteria>
- 8 new HTML pages exist, all wired to the existing helper stack (turion-config + data-loader + ns-toast), all using addEventListener + template-literal fetches
- index.html has the "Migration tools" section with QB + Ramp tiles
- cf-function-source/turion-clean-urls.js has 8 new entries (one per clean URL)
- npm run audit-buttons returns 0 violations
- All 3 requirement IDs covered: QbMigrationWizard (the 6 wizard pages), RampMiniModule (ramp.html), NetSuiteGoLiveScreens (the "View in NetSuite →" deep-links + the fact that post-migration the existing NS pages auto-pick-up the rows via /api/data/all)
- One additional commit on turion-space-demo: `feat(37-03): ...`
</success_criteria>

<output>
After completion, create `.planning/phases/37-qb-to-netsuite-migration-walkthrough/37-03-SUMMARY.md` per the standard template. Record: total LOC across the 8 HTML files, the exact line count of the index.html addition, audit-buttons output (must show 0), the commit hash, any visual deviations from the research's color/layout suggestions.
</output>
