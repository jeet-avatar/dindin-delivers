---
phase: 37-qb-to-netsuite-migration-walkthrough
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/turion-space-demo/backend/migrations/023-qb-ramp.sql
  - /Users/jeet/turion-space-demo/backend/src/routes/quickbooks.ts
  - /Users/jeet/turion-space-demo/backend/src/routes/ramp.ts
  - /Users/jeet/turion-space-demo/backend/src/routes/netsuite.ts
  - /Users/jeet/turion-space-demo/backend/src/app.ts
autonomous: true
requirements: [QbSourceData, MigrationAuditTrail, RampMiniModule]

must_haves:
  truths:
    - "Migration 023 is applied against prod Supabase — turion.qb_records, turion.ramp_card_txns, turion.migration_runs exist"
    - "turion.qb_records has ≥20 rows for each of the 6 qb_types (coa, customer, vendor, item, invoice, bill) — total ≥120 rows"
    - "turion.ramp_card_txns has ≥20 rows of corporate-card transactions"
    - "GET /api/quickbooks/status returns per-type counts (new/migrated/error/total) for all 6 types"
    - "GET /api/quickbooks/:type returns the QB rows for that type with status + migration metadata"
    - "GET /api/quickbooks/:type/mapping returns the canonical field-map for the wizard's middle pane"
    - "GET /api/ramp/card-txns returns the seeded Ramp txns; GET /api/ramp/card-txns/mapping returns the field-map; GET /api/ramp/status returns counts"
    - "turion.bills and turion.gl_accounts have keyedEntity CRUD routes registered (parity with the other NS tables)"
    - "Backend builds clean (tsc → 0 errors) and the new routers are mounted in app.ts"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/backend/migrations/023-qb-ramp.sql"
      provides: "DDL for qb_records/ramp_card_txns/migration_runs + seed data (~170 rows total)"
      contains: "create table if not exists turion.qb_records"
    - path: "/Users/jeet/turion-space-demo/backend/src/routes/quickbooks.ts"
      provides: "QB GET routes + FIELD_MAPS const (POST /migrate stubbed for 37-02)"
      exports: ["default"]
    - path: "/Users/jeet/turion-space-demo/backend/src/routes/ramp.ts"
      provides: "Ramp GET routes (POST /migrate stubbed for 37-02)"
      exports: ["default"]
  key_links:
    - from: "/Users/jeet/turion-space-demo/backend/src/app.ts"
      to: "/api/quickbooks + /api/ramp"
      via: "app.use mount"
      pattern: "app\\.use\\('/api/(quickbooks|ramp)'"
    - from: "migration 023"
      to: "prod Supabase turion schema"
      via: "psql against the rotated DATABASE_URL"
      pattern: "create table if not exists turion\\.(qb_records|ramp_card_txns|migration_runs)"
---

<objective>
Lay the entire backend foundation for the Phase 37 migration walkthrough in ONE plan: create + apply migration 023 (DDL for `turion.qb_records`, `turion.ramp_card_txns`, `turion.migration_runs`, plus ~170 seed rows of realistic Turion-style synthetic QB + Ramp data), create the `quickbooks.ts` + `ramp.ts` routers with all GET routes (status, list, mapping) + the `FIELD_MAPS` const that drives the wizard's middle pane, add `keyedEntity('/bills','bills')` + `keyedEntity('/gl-accounts','gl_accounts')` to `netsuite.ts` for symmetry, and mount the new routers in `app.ts`. POST `/migrate` is registered as a 501 stub (37-02 implements `applyMapping` + the INSERT loop).

Purpose: Unblock 37-02 (migrate route) and 37-03 (frontend wizard) to run in parallel in Wave 2. Both need: (a) the tables exist with data, (b) the GET routes return the right shapes, (c) the FIELD_MAPS module exports the canonical mapping for both server-side `applyMapping` (37-02) and the wizard's `GET /mapping` (37-03 fetches it).

Output: Migration 023 file checked in + applied to prod; quickbooks.ts (~250 lines) + ramp.ts (~120 lines); 6-line addition to netsuite.ts; 2-line mount addition to app.ts; tsc clean; curl smoke against the new GET routes returns expected shapes.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-qb-to-netsuite-migration-walkthrough/37-RESEARCH.md
@.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-09-SUMMARY.md

# Read-only references in the turion-space-demo repo (DO NOT modify):
@/Users/jeet/turion-space-demo/backend/src/routes/netsuite.ts
@/Users/jeet/turion-space-demo/backend/src/db.ts
@/Users/jeet/turion-space-demo/backend/src/app.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write + apply migration 023 (DDL + seed data) against prod Supabase</name>
  <files>/Users/jeet/turion-space-demo/backend/migrations/023-qb-ramp.sql</files>
  <action>
    BEFORE STARTING — confirm DB connectivity (the Phase 36 password rotation is the only known blocker):

      curl -fsS https://lo254mvukl.execute-api.us-east-1.amazonaws.com/api/health
      # expect: {"ok":true,"db":"ok",...}; if "db":"error", STOP and surface as a blocker (out-of-band rotation needed).

    Create `/Users/jeet/turion-space-demo/backend/migrations/023-qb-ramp.sql` with THREE sections (all idempotent — `create table if not exists`, `on conflict do nothing` on seeds):

    SECTION 1 — DDL (copy verbatim from the research §"Source Table Design — RECOMMENDED"; ~50 lines):
    - `turion.qb_records (id uuid pk default gen_random_uuid(), qb_type text not null, qb_id text not null, source_data jsonb not null, status text not null default 'new', migrated_at timestamptz, migration_run_id uuid, created_at timestamptz default now(), updated_at timestamptz default now(), unique (qb_type, qb_id), check (qb_type in ('coa','customer','vendor','item','invoice','bill')), check (status in ('new','migrated','error')))`
    - `turion.ramp_card_txns (id uuid pk default gen_random_uuid(), ramp_id text unique not null, source_data jsonb not null, status text not null default 'new', migrated_at timestamptz, migration_run_id uuid, created_at timestamptz default now(), check (status in ('new','migrated','error')))`
    - `turion.migration_runs (id uuid pk default gen_random_uuid(), run_type text not null, source_data jsonb not null, created_at timestamptz default now())` — mirrors `turion.sync_runs` jsonb-blob shape (research §confirmed).
    - Indexes: `create index if not exists qb_records_type_status_idx on turion.qb_records (qb_type, status)`.
    - Widen `turion.audit_log` action CHECK constraint to include `'qb_migrate_batch'` + `'ramp_migrate_batch'` IF the constraint exists and rejects them — run `select pg_get_constraintdef(oid) from pg_constraint where conname like 'audit_log%action%check';` first; if no CHECK constraint, skip this. (The existing `audit()` helper uses 'CREATE'/'UPDATE'/'DELETE' which we'll keep; the per-row audit writes use those.)

    SECTION 2 — SEED DATA (~170 rows total; use the research §"Synthetic Data Sketch" as the canonical template; flesh out 20-30 rows per QB type + 20-30 Ramp txns). EVERY insert uses `on conflict (qb_type, qb_id) do nothing` (or `on conflict (ramp_id) do nothing`). Coverage by type:
    - **COA (~30 rows)**: 1000-series Assets (Checking-WF, Savings, AR, Inventory), 2000-series Liabilities (AP, Credit Cards · Ramp), 3000 Equity, 4000-series Revenue (Product, Service), 5000 COGS, 6000-series Expenses (Office Rent, SaaS, Travel, Payroll, Insurance, Legal, R&D). Reference research §Chart-of-Accounts sample for shape.
    - **Customers (~25 rows)**: govt-adjacent + commercial aerospace mix (Space Logistics Co, Lunar Outpost, Sierra Space subcontract, etc. — make up reasonable names; CO/CA/TX addresses; Net 30/Net 45 mix; some 0 balance, some with AR balance).
    - **Vendors (~25 rows)**: aerospace component suppliers (Blue Canyon Technologies, Moog Inc, Honeybee Robotics, Maxar parts, AAC Clyde) + everyday (AWS, Google Workspace, Office Depot, Cushman Wakefield). All 1099 boolean varied.
    - **Items (~25 rows)**: aerospace parts (Reaction Wheel · Medium, Star Tracker, Sun Sensor, Solar Panel Wing, Thruster · Cold Gas) + services (Engineering · hourly, Integration · day-rate, Project Mgmt · hourly). 4-digit IncomeAccountRef/ExpenseAccountRef pointing to the seeded COA `qb_id`s.
    - **Invoices (~20 rows)**: tied to customer qb_ids; mix of `Balance:0` (Paid) + `Balance>0` (Open); 1-3 line items each referencing item qb_ids; TotalAmt $5K-$80K.
    - **Bills (~20 rows)**: tied to vendor qb_ids; mix of Paid/Open; AccountRef referencing COA qb_ids (mostly 5000 COGS or 6000-series expense accounts); TotalAmt $200-$50K.
    - **Ramp card txns (~25 rows)**: spread across categories (SaaS, Travel, Office Supplies, Marketing, Hardware), $50-$3000, with cardholder names from a small pool (Avery Lin, Marcus Reyes, Priya Shah, Daniel Cho), card_last4 4-digit suffixes, project_tag values like 'SAT-001'/'SAT-002'/'SAT-003' or null, gl_category_guess hinting at the target COA.

    SECTION 3 — small comment block at the bottom showing the recommended psql run command (for ops doc).

    APPLY THE MIGRATION (mandatory part of this task — don't defer):

      DB_URL='postgresql://postgres.lbpkbpfwdpnwlccmlfxn:Thirumala977%21@aws-1-us-east-2.pooler.supabase.com:6543/postgres'
      psql "$DB_URL" -v ON_ERROR_STOP=1 -f /Users/jeet/turion-space-demo/backend/migrations/023-qb-ramp.sql

    (Note: the `!` in the rotated Phase-36 password is URL-encoded as `%21` for the conn string. If a single-quoted shell context fails, fall back to `psql -h aws-1-us-east-2.pooler.supabase.com -p 6543 -U postgres.lbpkbpfwdpnwlccmlfxn -d postgres` and let psql prompt for the password.)

    AVOID: hardcoding the password in any committed file; running without `on conflict do nothing` (re-runs would 23505); using `DROP TABLE` (these tables MUST be idempotent — Phase 38+ may add rows, we don't drop them).

    NOTE: Make sure `gen_random_uuid()` works — it's from the `pgcrypto` or `uuid-ossp` extension. Check `select gen_random_uuid()` runs cleanly. If it fails, prepend `create extension if not exists pgcrypto;` to migration 023 (idempotent).
  </action>
  <verify>
    1. File exists: `ls -la /Users/jeet/turion-space-demo/backend/migrations/023-qb-ramp.sql` — non-empty.
    2. Migration applied — counts match:
       `psql "$DB_URL" -t -c "select qb_type, count(*) from turion.qb_records group by 1 order by 1"` → 6 rows, each ≥20.
       `psql "$DB_URL" -t -c "select count(*) from turion.ramp_card_txns"` → ≥20.
       `psql "$DB_URL" -t -c "select count(*) from turion.migration_runs"` → 0 (empty until 37-02 runs migrations).
    3. Re-run the migration; counts unchanged (idempotency test).
    4. `psql "$DB_URL" -t -c "select source_data->>'Name' from turion.qb_records where qb_type='coa' limit 1"` → returns a non-null COA name.
  </verify>
  <done>
    Migration 023 is in the repo and applied; the three new tables exist in prod with ≥120 QB records + ≥20 Ramp txns; idempotency confirmed; pgcrypto present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create quickbooks.ts + ramp.ts routers with GET routes + FIELD_MAPS const + stubbed POST /migrate</name>
  <files>
    /Users/jeet/turion-space-demo/backend/src/routes/quickbooks.ts
    /Users/jeet/turion-space-demo/backend/src/routes/ramp.ts
  </files>
  <action>
    Create `/Users/jeet/turion-space-demo/backend/src/routes/quickbooks.ts` (~280 lines) using the exact skeleton from research §"Code Examples → Backend":

    Top of file:
      import { Router } from 'express';
      import { query, audit, pool } from '../db';
      const r = Router();
      const QB_TYPES = ['coa','customer','vendor','item','invoice','bill'] as const;
      type QbType = typeof QB_TYPES[number];
      const NS_TABLE: Record<QbType, string> = { coa:'gl_accounts', customer:'customers', vendor:'vendors', item:'items', invoice:'invoices', bill:'bills' };
      function assertType(t: string): t is QbType { return (QB_TYPES as readonly string[]).includes(t); }

    Routes:
    - **`r.get('/status', ...)`** — single query `SELECT qb_type, status, count(*)::int AS n FROM turion.qb_records GROUP BY 1,2` → assemble into `{ coa:{new,migrated,error,total}, customer:{...}, ... }` shape exactly as research §"Code Examples → r.get('/status', ...)". MUST be registered BEFORE the parametric `:type` route (Express matches in registration order).
    - **`r.get('/runs', ...)`** — `SELECT id, run_type, source_data, created_at FROM turion.migration_runs ORDER BY created_at DESC LIMIT ${parseInt(req.query.limit) || 5}` (clamp 1..50). Returns `{ runs: [...] }`. (Research §Open Q #4 — recommended addition for the landing page; 30 lines.)
    - **`r.get('/:type', ...)`** — `assertType(t)` guard → `SELECT qb_id, source_data, status, migrated_at, migration_run_id FROM turion.qb_records WHERE qb_type=$1 ORDER BY qb_id` → `{ qbType, nsTable, rows }`.
    - **`r.get('/:type/mapping', ...)`** — `assertType(t)` guard → `{ qbType, nsTable, fields: FIELD_MAPS[t] }`.
    - **`r.post('/:type/migrate', ...)`** — STUB ONLY for this plan (37-02 implements the body): `res.status(501).json({ error: 'POST /migrate not implemented until 37-02' });` — but DO register the route so audit-erp-buttons picks it up immediately.

    **FIELD_MAPS const** (the canonical mapping — drives both the wizard's middle pane via `/mapping` AND `applyMapping` server-side in 37-02). Define as `const FIELD_MAPS: Record<QbType, FieldMap[]> = {...}` where:
      type FieldMap = { qbField: string; nsField: string; transform?: string; notes?: string };

    Populate from research §"Field Mappings" tables (sections 1-6, ~70 entries total across 6 types). DO NOT skip any field — the wizard renders ALL of them. Include the `(derived)` fields with `qbField: '(derived)'`.

    Example for `coa`:
      coa: [
        { qbField: 'Name', nsField: 'name', transform: 'direct' },
        { qbField: 'AccountType', nsField: 'type', transform: 'direct' },
        { qbField: 'AccountSubType', nsField: 'subType', transform: 'direct' },
        { qbField: 'CurrentBalance', nsField: 'openingBalance', transform: 'direct (snapshot at migration)' },
        { qbField: 'Currency', nsField: 'currency', transform: 'direct' },
        { qbField: 'Active', nsField: 'active', transform: 'direct' },
        { qbField: 'Description', nsField: 'description', transform: 'direct' },
        { qbField: 'qb_id', nsField: 'id', transform: 'preserve QB account number as NS id' },
        { qbField: '(derived)', nsField: 'accountNumber', transform: '= qb_id' },
        { qbField: '(derived)', nsField: 'migratedFrom', transform: "= 'quickbooks'" },
      ],

    Repeat for customer/vendor/item/invoice/bill — pull each row verbatim from the research field-map tables.

    `export default r;`

    Create `/Users/jeet/turion-space-demo/backend/src/routes/ramp.ts` (~140 lines) — same shape but ONE record type:
      const r = Router();
      // r.get('/status', ...) — { card_txn: { new, migrated, error, total } } from `SELECT status, count(*) FROM turion.ramp_card_txns GROUP BY 1`
      // r.get('/card-txns', ...) — `SELECT ramp_id, source_data, status, migrated_at, migration_run_id FROM turion.ramp_card_txns ORDER BY ramp_id` → { rows }
      // r.get('/card-txns/mapping', ...) — { fields: RAMP_FIELD_MAP } from research §Ramp field-map (15 entries)
      // r.post('/card-txns/migrate', ...) — 501 stub (37-02 implements)
      // export default r;

    AVOID:
    - Hardcoding the API_BASE — these are backend, no client URLs.
    - Using `err.message` in the response — wrap catches with `res.status(500).json({ error: 'internal' });` per Phase 36-08 audit. (See `netsuite.ts:227-230` for the hardened-catch shape.)
    - Adding `requireAuth` middleware — verify by reading `netsuite.ts`: the existing ERP routers use `cors '*'` + NO auth middleware (the demo has no JWT — research §Open Q #6 confirms `actor: 'demo-user'` hardcoded). Match the existing pattern; do NOT introduce auth here. (The CLAUDE-md note "All routes `requireAuth`" in the ROADMAP narrative is aspirational — the actual code pattern is open.)
    - Building dynamic SQL with `nsTable` from user input — `NS_TABLE` is server-defined; the `qb_type` URL param is validated via `assertType()` before any SQL runs (research §Pitfall 4).

    NOTE: `keyedEntity` from `netsuite.ts` is file-local. We do NOT import it — 37-02 will inline a stripped variant in the POST /migrate handler. For these GET routes, raw `query()` from `db.ts` is sufficient.
  </action>
  <verify>
    `cd /Users/jeet/turion-space-demo/backend && npx tsc --noEmit` → 0 errors.
    `grep -n "FIELD_MAPS" /Users/jeet/turion-space-demo/backend/src/routes/quickbooks.ts` → const defined + referenced from /mapping route.
    `grep -c "qbField:" /Users/jeet/turion-space-demo/backend/src/routes/quickbooks.ts` → ≥60 (all 6 type maps with all fields).
    `grep -n "RAMP_FIELD_MAP\|ramp_id\|card-txns" /Users/jeet/turion-space-demo/backend/src/routes/ramp.ts` → all present.
  </verify>
  <done>
    Both routers compile clean, expose the documented GET routes, the FIELD_MAPS const is the single source of truth for the field mappings, and POST /migrate is registered (501) so the audit script and 37-03's wizard fetch don't 404.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mount routers in app.ts + add keyedEntity('/bills','bills') + keyedEntity('/gl-accounts','gl_accounts') to netsuite.ts + smoke locally</name>
  <files>
    /Users/jeet/turion-space-demo/backend/src/app.ts
    /Users/jeet/turion-space-demo/backend/src/routes/netsuite.ts
  </files>
  <action>
    PART A — `/Users/jeet/turion-space-demo/backend/src/app.ts`:

    Read the file. Find the existing module mount block (around line 127 per research) — it looks like:
      app.use('/api/salesforce', salesforce);
      app.use('/api/netsuite', netsuite);
      app.use('/api/arena', arena);
      app.use('/api/mes', mes);
      app.use('/api/vendor', vendor);
      app.use('/api/integration', integration);
      app.use('/api/extras', extras);
      app.use('/api/notify', notify);
      app.use('/api/agents', agents);

    Add at the TOP of the file (with the other imports):
      import quickbooks from './routes/quickbooks';
      import ramp from './routes/ramp';

    Add AFTER the existing `/api/agents` mount:
      app.use('/api/quickbooks', quickbooks);
      app.use('/api/ramp', ramp);

    Do NOT reorder existing mounts. Do NOT touch the `/api/health`, `/api/data/all`, or `/api/{module}/lookups` blocks.

    PART B — `/Users/jeet/turion-space-demo/backend/src/routes/netsuite.ts`:

    Find the existing `keyedEntity('/items', 'items')` (research §existing routes — items, sales-orders, clins, work-orders, projects, journal-entries, invoices, contracts, mrp-runs, rfqs are present). Add TWO new registrations right after the last existing one:

      keyedEntity('/bills', 'bills');
      keyedEntity('/gl-accounts', 'gl_accounts');

    These give the existing `/inventory/items`-style "View in NetSuite" pages the ability to PATCH `turion.bills` and `turion.gl_accounts` rows post-migration (research §"Existing NS-side Target Tables" — `turion.bills` is read by `/api/data/all` but has NO existing CRUD route; same for `gl_accounts`). 6 lines of code total. DO NOT add `skipPost:true` — we want the full GET/POST/PATCH bundle.

    NOTE: If `keyedEntity` is already called for these tables (sanity-check first via `grep keyedEntity netsuite.ts`), skip the duplicate registration — Express will throw on duplicate route handlers.

    PART C — Build + local-only smoke (no deploy yet — 37-04 deploys):

      cd /Users/jeet/turion-space-demo/backend
      npm run build  # if no build script, fall back to `npx tsc --noEmit`

    Confirm 0 errors. If the build fails on the new files, fix BEFORE proceeding (likely culprits: missing import, missing type narrowing, `req.params.type` not narrowed).

    PART D — Commit (DO NOT push — 37-04 pushes + deploys):

      cd /Users/jeet/turion-space-demo
      git status --short
      # Add ONLY the named files (no `git add .`):
      git -c user.email='jm@techcloudpro.com' -c user.name='jeet-avatar' \
        add backend/migrations/023-qb-ramp.sql backend/src/routes/quickbooks.ts backend/src/routes/ramp.ts backend/src/routes/netsuite.ts backend/src/app.ts
      git -c user.email='jm@techcloudpro.com' -c user.name='jeet-avatar' commit -m \
        "feat(37-01): qb_records + ramp_card_txns + migration_runs DDL/seed + GET routes (POST /migrate is stub for 37-02)"

    AVOID: pushing to remote in this plan (37-04 does the push). Mass-adding (`git add .`) — there may be unrelated files in the working tree.
  </action>
  <verify>
    `cd /Users/jeet/turion-space-demo/backend && npm run build 2>&1 | tail -5` → no errors.
    `grep -n "app.use('/api/quickbooks'\|app.use('/api/ramp'" /Users/jeet/turion-space-demo/backend/src/app.ts` → 2 lines.
    `grep -n "keyedEntity('/bills'\|keyedEntity('/gl-accounts'" /Users/jeet/turion-space-demo/backend/src/routes/netsuite.ts` → 2 lines.
    `cd /Users/jeet/turion-space-demo && git log -1 --pretty='%h %an %ae %s'` → "feat(37-01): …" by jeet-avatar / jm@techcloudpro.com.
  </verify>
  <done>
    Backend builds clean; quickbooks + ramp mounted; bills + gl_accounts have keyedEntity routes; commit landed in the turion-space-demo repo with the correct author identity; no push yet.
  </done>
</task>

</tasks>

<verification>
End-of-plan smoke (single command block):

  # 1. tsc clean
  cd /Users/jeet/turion-space-demo/backend && npx tsc --noEmit

  # 2. Migration landed in prod
  DB_URL='postgresql://postgres.lbpkbpfwdpnwlccmlfxn:Thirumala977%21@aws-1-us-east-2.pooler.supabase.com:6543/postgres'
  psql "$DB_URL" -t -c "select qb_type, count(*) from turion.qb_records group by 1 order by 1"
  # expect 6 rows, each ≥20

  psql "$DB_URL" -t -c "select count(*) from turion.ramp_card_txns"
  # expect ≥20

  psql "$DB_URL" -t -c "select count(*) from turion.migration_runs"
  # expect 0

  # 3. Routes registered (file-level grep — actual HTTP smoke happens in 37-04 after deploy)
  grep -c "r.get\|r.post" /Users/jeet/turion-space-demo/backend/src/routes/quickbooks.ts
  # expect ≥5  (status, runs, :type, :type/mapping, :type/migrate)

  grep -c "r.get\|r.post" /Users/jeet/turion-space-demo/backend/src/routes/ramp.ts
  # expect ≥4  (status, card-txns, card-txns/mapping, card-txns/migrate)

  # 4. Commit on local repo
  cd /Users/jeet/turion-space-demo && git log -1 --oneline
</verification>

<success_criteria>
- Migration 023 applied to prod Supabase; the three new tables exist with the expected row counts
- quickbooks.ts + ramp.ts compile clean and are mounted in app.ts
- FIELD_MAPS const is the single source of truth for the per-type field mappings (~70 entries total)
- keyedEntity registrations for bills + gl_accounts present
- One commit on local turion-space-demo repo with `feat(37-01):` prefix
- POST /migrate is a registered 501 stub (so 37-03's frontend doesn't 404 + the button audit finds the route)
- All 3 requirement IDs covered: QbSourceData (qb_records + seeds), MigrationAuditTrail (migration_runs table exists), RampMiniModule (ramp_card_txns + seeds + GET routes)
</success_criteria>

<output>
After completion, create `.planning/phases/37-qb-to-netsuite-migration-walkthrough/37-01-SUMMARY.md` per the standard SUMMARY template — record: row counts per qb_type, total line count of quickbooks.ts + ramp.ts, exact commit hash in turion-space-demo, tsc clean confirmation, and any deviation from the research's recommended seed-data distribution (the executor will likely flesh out the rows past the example one-per-type — note the actual counts).
</output>
