---
phase: 37-qb-to-netsuite-migration-walkthrough
plan: 04
type: execute
wave: 3
depends_on: ["37-01", "37-02", "37-03"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [QbSourceData, QbMigrationRoutes, QbMigrationWizard, RampMiniModule, MigrationAuditTrail, NetSuiteGoLiveScreens]

must_haves:
  truths:
    - "Both repos pushed: turion-space-demo HEAD has the 37-01/02/03 commits; .planning files in dindin (doordash-p2p) have the 4 PLAN.md + SUMMARY.md files committed and pushed"
    - "Lambda turion-demo-api is redeployed via turion-space-demo/backend/build-and-push.sh — the new /api/quickbooks/* and /api/ramp/* routes return 200/expected from the deployed APIGW (lo254mvukl.execute-api.us-east-1.amazonaws.com), the POST /migrate is no longer a 501 stub (CodeSha256 changed from prior deploy)"
    - "Frontend deployed via turion-space-demo/deploy-frontend.sh with a recomputed F6 pre-flight: stash any genuinely-uncommitted leftovers + mv .superpowers/ aside, restore after deploy even on failure; CF E37R9PT8IL44L2 invalidation polled to Status=Completed"
    - "Curl smoke (live APIGW): /api/health → {db:ok}; /api/quickbooks/status → 200 with 6 type keys; /api/quickbooks/customer/mapping → 200 with FIELD_MAPS payload; /api/ramp/status → 200; bogus path → 404"
    - "DB-direct E2E walk (the headless-substitute checkpoint): start from a clean baseline (all qb_records.status='new'); POST /api/quickbooks/customer/migrate {qbIds:[<one CUST id>]} → migrated[1], skipped[0], errors[0]; psql confirms the turion.customers row exists; psql confirms turion.migration_runs has 1 new row with run_type='qb-customer'; psql confirms turion.audit_log has 1 new CREATE row for entity='customers'; re-POST the same qbId → skipped[1], migrated[0]; cleanup back to baseline (DELETE the customer row + UPDATE qb_records back to status='new' + DELETE the migration_run + DELETE the audit_log row); same walk repeated for one Ramp txn → turion.bills"
    - "npm run audit-buttons on BOTH satellite and ERP frontends returns 0 violations"
    - "Phase 27-36 regression intact: /api/health on satellite + ERP APIGWs both 200; turionspace.zietra.com/satellite/, /finance/general-ledger, /sales/orders, /inventory/items, /procurement/orders, /quickbooks, /ramp all HEAD 200; the existing NS pages auto-load the migrated rows after a refresh (no static *-data.js regression)"
    - ".planning/STATE.md updated (narrative-format, manual) with the Phase 37 completion entry"
    - ".planning/ROADMAP.md Phase 37 marked complete with the 4-plan checklist"
    - "Headless-substitute human-verify checkpoint approved by the user (curl/HEAD smoke + DB-direct walk + audit are the substitute for a browser walk; browser walk noted as pending if not done)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Phase 37 completion narrative entry"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 37 marked complete with plan checklist"
  key_links:
    - from: "./backend/build-and-push.sh"
      to: "ECR + Lambda turion-demo-api"
      via: "docker buildx --platform linux/arm64 + aws lambda update-function-code"
      pattern: "turion-demo-api"
    - from: "./deploy-frontend.sh"
      to: "S3 turion-demo-static + CloudFront E37R9PT8IL44L2"
      via: "aws s3 sync . --delete + aws cloudfront create-invalidation"
      pattern: "E37R9PT8IL44L2"
---

<objective>
Ship Phase 37. Push both repos; redeploy `turion-demo-api` Lambda; deploy the static frontend with a recomputed F6 pre-flight; poll the CloudFront invalidation to Completed; curl smoke the new routes against the LIVE APIGW; run the DB-direct E2E walk (the headless-substitute checkpoint — the same pattern Phases 27-36 used: seed → POST migrate → confirm rows in turion.* tables → cleanup back to baseline); re-run `npm run audit-buttons` on both frontends; regression-check the Phase 27-36 deliverables; update STATE.md + ROADMAP.md; and pause at a single human-verify checkpoint summarizing what was verified.

Purpose: Close all 6 requirements. This is the only plan with a checkpoint (`autonomous: false`) per the Phase 27-36 precedent for deploy plans. The checkpoint summarizes the smoke + walk + audit results as the headless substitute for a manual browser walk on Thursday.

Output: turion-demo-api Lambda CodeSha256 changed; frontend deployed; CF invalidation Completed; smoke + DB walk + audit all pass; STATE.md + ROADMAP.md updated and pushed in dindin; the team has a live demo URL to walk on Thursday.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-qb-to-netsuite-migration-walkthrough/37-RESEARCH.md
@.planning/phases/37-qb-to-netsuite-migration-walkthrough/37-01-SUMMARY.md
@.planning/phases/37-qb-to-netsuite-migration-walkthrough/37-02-SUMMARY.md
@.planning/phases/37-qb-to-netsuite-migration-walkthrough/37-03-SUMMARY.md
@.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit (both frontends) + push turion-space-demo + redeploy Lambda + redeploy frontend + CF invalidation</name>
  <files>
    /Users/jeet/turion-space-demo/(no source edits — deploy actions only)
  </files>
  <action>
    PART A — Audit BOTH frontends (the deploy gate per Phase 36's precedent):

      cd /Users/jeet/turion-space-demo
      npm run audit-buttons
      # expected: "violations: 0" (or equivalent zero output) on the ERP frontend

      # If the satellite-button audit is in this repo too, run it:
      npm run audit-satellite-buttons 2>/dev/null || true

    If ANY violations, STOP, fix in the relevant plan's files, recommit, re-run audit. The deploy gate is non-negotiable (research §Pitfall 7).

    PART B — Push the turion-space-demo commits to remote (the 3 commits from 37-01/02/03):

      cd /Users/jeet/turion-space-demo
      git status --short
      # expect a small list — ideally clean except any genuinely uncommitted .superpowers/ etc.
      git log --oneline -5
      # expect feat(37-03), feat(37-02), feat(37-01) at the top
      git push origin main
      # if the remote requires force or has diverged, DO NOT force-push; investigate (likely a Phase 36 commit not yet on main locally — `git pull --rebase origin main` first)

    PART C — Lambda redeploy:

      cd /Users/jeet/turion-space-demo
      ./backend/build-and-push.sh
      # this script does: docker buildx --platform linux/arm64 → ECR push → aws lambda update-function-code --function-name turion-demo-api
      # watch for: "Update function code completed" + a new CodeSha256

      # Capture the new CodeSha256:
      aws lambda get-function-configuration --function-name turion-demo-api --query 'CodeSha256' --output text
      # save this value; compare to the prior deploy (from STATE.md Phase 36 line — "c716f0d2…")
      # the new SHA MUST differ — if identical, the build didn't pick up the new routes (cache issue; run with --no-cache)

    PART D — Wait for Lambda warm + immediate curl smoke (research §"What's risky → Lambda cold-start"):

      sleep 5  # let the update propagate
      API=https://lo254mvukl.execute-api.us-east-1.amazonaws.com
      curl -fsS $API/api/health | jq '{ok, db}'
      # expect {ok:true, db:"ok"}

      curl -fsS $API/api/quickbooks/status | jq 'keys'
      # expect ["bill","coa","customer","invoice","item","vendor"]

      curl -fsS $API/api/quickbooks/customer/mapping | jq '.fields | length'
      # expect >0 (likely ≥10)

      curl -fsS $API/api/quickbooks/customer | jq '.rows | length'
      # expect ≥20 (the seeded customers)

      curl -fsS $API/api/ramp/status | jq
      curl -fsS $API/api/ramp/card-txns | jq '.rows | length'
      # expect ≥20

      # confirm POST /migrate is no longer 501:
      curl -fsS -X POST $API/api/quickbooks/customer/migrate -H 'content-type: application/json' -d '{"qbIds":[]}' | jq
      # expect 400 "qbIds required (non-empty array)" — NOT 501

    PART E — F6 pre-flight + frontend deploy:

      cd /Users/jeet/turion-space-demo
      git status --short
      # If there are genuinely uncommitted leftovers (NOT the .planning files — those are in dindin) — note them.
      # Phase 36 left the tree mostly clean; this stash should be tiny.

      # F6 stash mechanics (per Phase 36-09 SUMMARY):
      STASH_LIST=$(git status --short | awk '{print $2}' | grep -v '^\\.planning' | head -50)
      if [ -d .superpowers ]; then mv .superpowers .superpowers.aside.$$; fi
      if [ -n "$STASH_LIST" ]; then
        git -c user.email='jm@techcloudpro.com' -c user.name='jeet-avatar' stash push -u -m "phase-37-04-frontend-deploy" -- $STASH_LIST 2>/dev/null || true
      fi

      ./deploy-frontend.sh
      # this regenerates turion-config.js (with API_BASE injected), aws s3 sync ., aws cloudfront create-invalidation --paths '/*'
      # capture the invalidation ID from stdout

      # Restore (even on failure):
      if [ -d .superpowers.aside.$$ ]; then mv .superpowers.aside.$$ .superpowers; fi
      if [ -n "$STASH_LIST" ]; then git stash pop 2>/dev/null || true; fi

    PART F — Poll CF invalidation to Completed:

      INV_ID=<from PART E stdout>
      while true; do
        STATUS=$(aws cloudfront get-invalidation --distribution-id E37R9PT8IL44L2 --id "$INV_ID" --query 'Invalidation.Status' --output text)
        echo "CF invalidation $INV_ID: $STATUS"
        [ "$STATUS" = "Completed" ] && break
        sleep 15
      done

    PART G — Quick HEAD smoke on the new clean URLs:

      for u in /quickbooks /quickbooks/coa /quickbooks/customers /quickbooks/vendors /quickbooks/items /quickbooks/invoices /quickbooks/bills /ramp; do
        curl -fsS -o /dev/null -w "%{http_code} $u\\n" "https://turionspace.zietra.com$u"
      done
      # expect 200 on all 8 (CF function update propagates after `deploy-frontend.sh` pushes new fn source)
      # if any 404, the CF function update hasn't propagated yet — wait 5-10 min, retry; .html URLs work as fallback

    AVOID:
    - `git push --force` on main (Phase 36 had no force pushes; preserve history)
    - Manual `docker build` + `aws lambda update-function-code` — ALWAYS use `./backend/build-and-push.sh` (the script is the canonical deploy path per Phase 36 + CLAUDE.md)
    - Skipping the F6 stash mechanics — if .superpowers/ or uncommitted files exist, deploy-frontend.sh will `aws s3 sync . --delete` them into prod (and then it can't restore them locally)
    - Polling CF invalidation faster than every 15s (the API rate-limits)
  </action>
  <verify>
    1. `aws lambda get-function-configuration --function-name turion-demo-api --query 'CodeSha256' --output text` → different from prior recorded SHA.
    2. `curl -fsS https://lo254mvukl.execute-api.us-east-1.amazonaws.com/api/quickbooks/status | jq 'keys | length'` → 6.
    3. `curl -fsS https://lo254mvukl.execute-api.us-east-1.amazonaws.com/api/ramp/status` → 200 with card_txn key.
    4. CloudFront invalidation Status=Completed.
    5. 8 clean URLs HEAD 200 from turionspace.zietra.com.
    6. `npm run audit-buttons` → 0 violations.
    7. `cd /Users/jeet/turion-space-demo && git log -1 origin/main --oneline` shows the 37-03 commit (so push landed).
  </verify>
  <done>
    Both new routers are LIVE on APIGW with new CodeSha256; frontend is LIVE with 8 new clean URLs; CF invalidation Completed; audit-buttons 0 violations; both repos pushed.
  </done>
</task>

<task type="auto">
  <name>Task 2: DB-direct E2E walk (the headless-substitute checkpoint) — QB customer + Ramp card txn round-trips with cleanup</name>
  <files>
    /Users/jeet/turion-space-demo/(no edits — DB + curl-driven walk)
  </files>
  <action>
    DB connection (Phase 36-09 conn string, same Phase-36-rotated password; `!` URL-encoded as `%21`):

      DB_URL='postgresql://postgres.lbpkbpfwdpnwlccmlfxn:Thirumala977%21@aws-1-us-east-2.pooler.supabase.com:6543/postgres'
      API=https://lo254mvukl.execute-api.us-east-1.amazonaws.com

    STEP 1 — Confirm baseline + pick a test row that exists + isn't already migrated:

      psql "$DB_URL" -t -c "select qb_id from turion.qb_records where qb_type='customer' and status='new' limit 1"
      # expect a CUST-XXX id — capture it as TEST_CUST

      psql "$DB_URL" -t -c "select ramp_id from turion.ramp_card_txns where status='new' limit 1"
      # expect a RMP-TXN-XXXXX id — capture it as TEST_RAMP

      psql "$DB_URL" -t -c "select count(*) from turion.migration_runs"
      # save this as BASELINE_RUNS

      psql "$DB_URL" -t -c "select count(*) from turion.customers"
      # save as BASELINE_CUSTOMERS

      psql "$DB_URL" -t -c "select count(*) from turion.bills"
      # save as BASELINE_BILLS

    STEP 2 — Migrate ONE QB customer via the LIVE API:

      curl -fsS -X POST $API/api/quickbooks/customer/migrate \
        -H 'content-type: application/json' \
        -d "{\\"qbIds\\":[\\"$TEST_CUST\\"]}" | jq
      # expect: { ok:true, migrated:["CUST-XXX"], skipped:[], errors:[], summary:{ migrated_count:1, ... } }

    STEP 3 — DB confirms the round-trip:

      psql "$DB_URL" -c "select id, name, source_data->>'migratedFrom' from turion.customers where id='$TEST_CUST'"
      # expect 1 row, migratedFrom='quickbooks'

      psql "$DB_URL" -c "select status, migrated_at::date, migration_run_id::text from turion.qb_records where qb_type='customer' and qb_id='$TEST_CUST'"
      # expect status=migrated, migrated_at=today, migration_run_id=<uuid>

      psql "$DB_URL" -c "select run_type, source_data->>'migrated_count' as m, source_data->>'summary' as summary from turion.migration_runs order by created_at desc limit 1"
      # expect run_type=qb-customer, m=1

      psql "$DB_URL" -c "select entity, entity_id, action from turion.audit_log where entity='customers' and entity_id='$TEST_CUST' order by id desc limit 1"
      # expect entity=customers, action=CREATE

    STEP 4 — Idempotency check (re-POST the same id):

      curl -fsS -X POST $API/api/quickbooks/customer/migrate \
        -H 'content-type: application/json' \
        -d "{\\"qbIds\\":[\\"$TEST_CUST\\"]}" | jq '{ migrated:.migrated, skipped:.skipped, errors:.errors }'
      # expect: migrated:[], skipped:["CUST-XXX"], errors:[]

    STEP 5 — Migrate ONE Ramp txn:

      curl -fsS -X POST $API/api/ramp/card-txns/migrate \
        -H 'content-type: application/json' \
        -d "{\\"rampIds\\":[\\"$TEST_RAMP\\"]}" | jq
      # expect: { ok:true, migrated:["RMP-TXN-XXXXX"], ... }

      psql "$DB_URL" -c "select id, source_data->>'vendor', source_data->>'migratedFrom' from turion.bills where id='RMP-$TEST_RAMP'"
      # expect 1 row, vendor='Ramp · Corporate Card', migratedFrom='ramp'

      psql "$DB_URL" -c "select run_type from turion.migration_runs order by created_at desc limit 1"
      # expect ramp-card-txns

    STEP 6 — CLEANUP back to baseline:

      psql "$DB_URL" -c "delete from turion.customers where id='$TEST_CUST'"
      psql "$DB_URL" -c "delete from turion.bills where id='RMP-$TEST_RAMP'"
      psql "$DB_URL" -c "update turion.qb_records set status='new', migrated_at=null, migration_run_id=null where qb_type='customer' and qb_id='$TEST_CUST'"
      psql "$DB_URL" -c "update turion.ramp_card_txns set status='new', migrated_at=null, migration_run_id=null where ramp_id='$TEST_RAMP'"
      psql "$DB_URL" -c "delete from turion.migration_runs where run_type in ('qb-customer','ramp-card-txns') and (source_data->>'started_at')::timestamptz > now() - interval '1 hour'"
      psql "$DB_URL" -c "delete from turion.audit_log where entity in ('customers','bills') and entity_id in ('$TEST_CUST','RMP-$TEST_RAMP')"

      # Re-confirm baselines:
      psql "$DB_URL" -t -c "select count(*) from turion.customers"
      # MUST equal BASELINE_CUSTOMERS
      psql "$DB_URL" -t -c "select count(*) from turion.bills"
      # MUST equal BASELINE_BILLS
      psql "$DB_URL" -t -c "select count(*) from turion.migration_runs"
      # MUST equal BASELINE_RUNS

    STEP 7 — Phase 27-36 regression spot-check (no new actions; just verify pre-existing pages didn't break):

      for u in /satellite /index.html /finance/general-ledger /sales/orders /inventory/items /procurement/orders; do
        curl -fsS -o /dev/null -w "%{http_code} $u\\n" "https://turionspace.zietra.com$u"
      done
      # expect 200 on all

      curl -fsS https://lo254mvukl.execute-api.us-east-1.amazonaws.com/api/data/all | jq 'keys | length'
      # expect ≥10 (the existing /api/data/all bundle is intact)

      curl -fsS https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/health | jq '{ok, db}'
      # expect ok:true, db:ok — Phase 27-31 satellite backend regression intact

    AVOID:
    - Skipping cleanup — if you leave migrated rows in prod, the next demo run will see "1 customer already migrated" and the team gets a confusing wizard view
    - Cleaning up via `truncate` — only delete the SPECIFIC rows this walk created
    - Running the walk against a stale Lambda — confirm `aws lambda get-function-configuration` CodeSha256 changed first (Task 1 PART C)
  </action>
  <verify>
    Capture and report in the final checkpoint:
    - The new turion-demo-api CodeSha256 (vs prior `c716f0d2…`)
    - All curl-smoke status codes (8 routes → 200)
    - DB round-trip results (1 customer round-trip confirmed; 1 Ramp txn round-trip confirmed; idempotency works)
    - Cleanup baselines matched (counts equal to recorded baselines)
    - Phase 27-36 regression spot-check (6+ URLs HTTP 200; both APIGW /api/health OK)
    - audit-buttons 0 violations
  </verify>
  <done>
    Live API can ingest a real migration POST end-to-end; the audit trail writes correctly; idempotency is provable; the cleanup leaves prod at exactly the pre-walk baseline; Phase 27-36 deliverables are intact.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md + ROADMAP.md + push dindin + Phase 37 SUMMARY scaffold</name>
  <files>
    /Users/jeet/doordash-p2p/.planning/STATE.md
    /Users/jeet/doordash-p2p/.planning/ROADMAP.md
  </files>
  <action>
    PART A — `.planning/ROADMAP.md`:

    Read the file. Find the Phase 37 entry (`### Phase 37: QuickBooks → NetSuite migration walkthrough …`). Update:
    - `**Plans:** 0 plans` → `**Plans:** 4 plans (all complete)`
    - Replace the placeholder plan list:
        Plans:
        - [ ] TBD (run /gsd:plan-phase 37 to break down)
      with the actual 4-plan checklist (all `[x]`):
        Plans:
        - [x] 37-01-PLAN.md — Backend foundation: migration 023 + qb_records/ramp_card_txns/migration_runs + GET routes + FIELD_MAPS
        - [x] 37-02-PLAN.md — POST /migrate routes: applyMapping per QB type + Ramp + atomic txns + audit trail
        - [x] 37-03-PLAN.md — Frontend wizard: 8 HTML pages + 3-pane layout + index.html migration tools + CF clean URLs
        - [x] 37-04-PLAN.md — Audit + deploy + DB-direct E2E walk + STATE/ROADMAP

    PART B — `.planning/STATE.md`:

    Read the file. Add at the TOP of `## Current Position` (preserving the existing Phase 36 narrative below — it's now history, but keep it):

    A narrative paragraph in the same prose style as the Phase 36 entry (research §STATE.md style — single dense paragraph, ~400-500 words; the narrative MUST cover):
    - Phase 37 4/4 plans complete; Wave structure (W1: 37-01, W2: 37-02 ‖ 37-03, W3: 37-04 deploy)
    - What shipped: turion.qb_records (~150+ rows across 6 QB types) + turion.ramp_card_txns (~25 rows) + turion.migration_runs (audit) tables in prod Supabase via migration 023; quickbooks.ts router (~280 lines: status, runs, list, mapping, POST /migrate with FIELD_MAPS for all 6 types) + ramp.ts router (~150 lines) mounted in app.ts; keyedEntity /bills + /gl-accounts added to netsuite.ts; 8 new HTML pages (quickbooks.html landing + 6 per-type wizards + ramp.html) with 3-pane CSS-grid (QB-left / mapping-middle / NS-right) layout; index.html "Migration tools" section with 2 tiles (QB green + Ramp purple/yellow); 8 clean URLs added to cf-function-source/turion-clean-urls.js
    - Deploy: turion-space-demo pushed (hashes), Lambda turion-demo-api redeployed (new CodeSha256 vs prior), frontend deployed via deploy-frontend.sh, CF E37R9PT8IL44L2 invalidation Completed, 8 new clean URLs HEAD 200 on turionspace.zietra.com
    - E2E proven: 1 QB customer round-trip (migrate → DB confirm → idempotency check → cleanup); 1 Ramp txn round-trip (migrate → DB confirm → cleanup); baselines restored exactly
    - Audit: 0 violations on ERP frontend
    - Headless-substitute checkpoint approved (curl/HEAD smoke + DB walk + audit = substitute for browser walk; browser walk by the team noted as the actual Thursday demo)
    - Deferrals (intentional): JE table-side payments (embedded paymentRecorded on Invoices/Bills covers the Paid story); the per-row __warnings UI bling beyond the JSON pre.
    - 6 requirement IDs all closed: QbSourceData, QbMigrationRoutes, QbMigrationWizard, RampMiniModule, MigrationAuditTrail, NetSuiteGoLiveScreens
    - Risk notes: Lambda cold-start on the first migrate POST after deploy (~5-10s — first click in the demo may feel slow, document for the team).

    Also append a small line to the `*Last updated: ...*` block at the very bottom of ROADMAP.md (the marker line that records the latest phase state — see Phase 36's `*Last updated: 2026-05-12 ...*` line for the format).

    PART C — Phase SUMMARY (`.planning/phases/37-qb-to-netsuite-migration-walkthrough/37-04-SUMMARY.md`):

    Create the Phase-level summary per the standard template. Cover: all 4 plans' outcomes (linking 37-01/02/03 SUMMARYs), the deploy state (Lambda SHAs, CF invalidation ID), the DB walk results, the audit result, the requirements check.

    PART D — Commit + push the dindin planning files:

      cd /Users/jeet/doordash-p2p
      git status --short
      # add the 4 PLAN.md + 4 SUMMARY.md + ROADMAP.md + STATE.md
      git -c user.email='jm@techcloudpro.com' -c user.name='jeet-avatar' \
        add .planning/phases/37-qb-to-netsuite-migration-walkthrough/37-01-PLAN.md \
            .planning/phases/37-qb-to-netsuite-migration-walkthrough/37-02-PLAN.md \
            .planning/phases/37-qb-to-netsuite-migration-walkthrough/37-03-PLAN.md \
            .planning/phases/37-qb-to-netsuite-migration-walkthrough/37-04-PLAN.md \
            .planning/phases/37-qb-to-netsuite-migration-walkthrough/37-01-SUMMARY.md \
            .planning/phases/37-qb-to-netsuite-migration-walkthrough/37-02-SUMMARY.md \
            .planning/phases/37-qb-to-netsuite-migration-walkthrough/37-03-SUMMARY.md \
            .planning/phases/37-qb-to-netsuite-migration-walkthrough/37-04-SUMMARY.md \
            .planning/ROADMAP.md .planning/STATE.md

      git -c user.email='jm@techcloudpro.com' -c user.name='jeet-avatar' commit -m \
        "docs(37): apply phase — QB→NS + Ramp migration walkthrough live"

      git push origin "$(git branch --show-current)"
      # if branch is gsd/phase-37-* and remote tracking is set, no -u needed; otherwise: git push -u origin <branch>

    NOTE: dindin is on a phase branch (`gsd/phase-26-data-densification` per the git status at session start; the executor may be on a 37 branch by now — push whichever branch is current).

    AVOID:
    - `git push --force`
    - Pushing to `main` directly if on a phase branch — let the PR/merge flow handle it
    - Skipping the SUMMARY files — they're cited by future-phase research digest
  </action>
  <verify>
    `cd /Users/jeet/doordash-p2p && git log -1 --pretty='%h %an %ae %s'` → "docs(37): apply phase — …" by jeet-avatar / jm@techcloudpro.com.
    `grep -A 4 "Phase 37" /Users/jeet/doordash-p2p/.planning/ROADMAP.md | head -8` → shows `**Plans:** 4 plans (all complete)` + the 4 `[x]` checkboxes.
    `head -100 /Users/jeet/doordash-p2p/.planning/STATE.md | grep -i "phase 37"` → finds the new Phase 37 entry above the Phase 36 one.
    `ls /Users/jeet/doordash-p2p/.planning/phases/37-qb-to-netsuite-migration-walkthrough/*-SUMMARY.md | wc -l` → 4 (one per plan).
    `git push` succeeded (returncode 0).
  </verify>
  <done>
    STATE.md + ROADMAP.md updated, all 4 SUMMARYs created, dindin commit pushed to remote. Phase 37 is now reflected in project history.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Headless-substitute checkpoint — review what was verified before declaring Phase 37 complete</name>
  <files>
    (no files modified — this is a checkpoint review of Task 1-3 outputs)
  </files>
  <action>
    Pause and present the Phase 37 completion summary to the user for approval. Surface the captured evidence from Tasks 1-3 (Lambda CodeSha256 delta, curl smoke results, DB round-trip results, audit-buttons output, CF invalidation status, regression spot-check). The user types "approved" to finalize, or describes any gap → file a Phase 38 gap-closure plan.
  </action>
  <verify>
    User responds with "approved" OR a list of issues. If issues, do NOT mark Phase 37 complete — capture the issues and stop.
  </verify>
  <done>
    User has approved (or has flagged specific gaps that will be addressed in a follow-up phase).
  </done>
  <what-built>
    Phase 37 is DEPLOYED to prod (turion-demo-api Lambda + turionspace.zietra.com frontend). The new functionality:
    1. **8 new pages** live at https://turionspace.zietra.com/quickbooks, /quickbooks/coa, /quickbooks/customers, /quickbooks/vendors, /quickbooks/items, /quickbooks/invoices, /quickbooks/bills, /ramp
    2. **3-pane wizard** on each per-type page: QB rows (left) · canonical field-map (middle) · live NS preview (right) · "Migrate batch ▸" button moves rows from turion.qb_records → turion.<ns_table> with an atomic transaction, audit_log entry per row, and migration_runs entry per batch
    3. **Ramp mini-module** at /ramp: same 3-pane treatment for corporate-card txns → turion.bills with vendor="Ramp · Corporate Card" + embedded paymentRecorded leg
    4. **index.html** has a new "Migration tools" section with 2 tiles (QB green + Ramp purple/yellow) at the bottom of the existing module grid
    5. **CodeSha256** of turion-demo-api Lambda changed from prior deploy (record the new SHA in the SUMMARY)
    6. **CF invalidation** Completed
    7. **audit-buttons** returns 0 violations on the ERP frontend
    8. **DB-direct round-trip walk** PASSED (1 customer + 1 Ramp txn migrated → DB confirmed → idempotency confirmed → cleanup verified baselines restored)
    9. **Phase 27-36 regression** intact (satellite + ERP /api/health both 200; 6+ existing pages HEAD 200)
  </what-built>
  <how-to-verify>
    The work is verified HEADLESS via curl + psql + audit-buttons (per the Phase 27-36 precedent — visual browser walk happens on Thursday with the team).

    Review the captured outputs in the 37-04-SUMMARY.md:
    1. The new turion-demo-api CodeSha256 (must differ from c716f0d2…)
    2. /api/quickbooks/status JSON (6 keys, real counts)
    3. /api/quickbooks/customer/mapping (field-map payload)
    4. The DB round-trip: 1 customer migrated, 1 customer skipped (idempotency), 1 customer cleaned up; 1 Ramp txn migrated + cleaned up
    5. audit-buttons: 0 violations
    6. CF invalidation Status=Completed
    7. 8 clean URLs HEAD 200

    Optional manual sanity (Thursday morning, before the team session):
    - Open https://turionspace.zietra.com/quickbooks in a browser — see the 6 tiles with status pills
    - Click into /quickbooks/customers — see the 3-pane wizard; select 1-2 rows; verify the right pane shows the NS preview
    - Click "Migrate batch ▸"; confirm; verify the success toast + the "View in NetSuite →" link works
    - Click /sales/orders → confirm the migrated customer shows up
    - Open /ramp; verify the same 3-pane treatment for card txns
    - Cleanup: optional — the demo data is intentionally re-runnable; the team can re-migrate during the session

    Known limitations to surface:
    - First migrate POST after a 15+ min idle period will be slow (~5-10s Lambda cold start) — document for the team
    - JE table-side payment leg is INTENTIONALLY scoped out — paymentRecorded is embedded in the Invoice/Bill row (research §Pitfall + the deferred-from-roadmap call)
    - The wizard's left pane pagination caps at 50 rows visible; for a real demo with 150+ migrated COA rows, scrolling is required (acceptable for v1)
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 37 complete, or describe any issues (e.g. "the customer wizard right-pane preview is empty" → file a Phase 38 gap-closure).</resume-signal>
</task>


</tasks>

<verification>
End-of-phase checks (all must be green to declare Phase 37 complete):
- Both repos pushed (turion-space-demo HEAD includes feat(37-01/02/03); dindin includes docs(37): apply phase)
- turion-demo-api Lambda CodeSha256 differs from prior recorded value
- 8 clean URLs HEAD 200 on turionspace.zietra.com
- /api/quickbooks/status returns 200 with 6 type keys; /api/quickbooks/{type}/mapping returns 200; /api/ramp/* returns 200
- DB round-trip walk passed for QB customer + Ramp txn; baselines restored
- audit-buttons: 0 violations on ERP frontend
- Phase 27-36 regression intact (satellite + ERP /api/health 200; 6+ existing pages HEAD 200)
- STATE.md + ROADMAP.md updated; 4 SUMMARYs exist
- Human-verify checkpoint approved
</verification>

<success_criteria>
- All 6 requirement IDs closed: QbSourceData (qb_records + seeds live), QbMigrationRoutes (POST /migrate live for 6 types), QbMigrationWizard (8 wizard pages live), RampMiniModule (ramp.html + /api/ramp live), MigrationAuditTrail (migration_runs + audit_log writes confirmed), NetSuiteGoLiveScreens ("View in NetSuite →" deep-links live + existing NS pages auto-pick-up the migrated rows)
- 4/4 plans in the phase are complete
- The team can walk the QB→NS + Ramp→NS migration on the live site for Thursday's session
</success_criteria>

<output>
After completion, create `.planning/phases/37-qb-to-netsuite-migration-walkthrough/37-04-SUMMARY.md` per the standard template + a top-level Phase 37 narrative entry in STATE.md (described in Task 3 PART B). Record: Lambda CodeSha256 delta, CF invalidation ID, all smoke command outputs, the DB round-trip results, the audit results.
</output>
