---
phase: 33-end-to-end-satellite-build-flow
plan: 06
type: execute
wave: 5
depends_on: [02, 04, 05]
files_modified:
  - /Users/jeet/turion-satellite/backend/src
  - /Users/jeet/turion-space-demo/satellite
autonomous: false
requirements: [E2EFlowVerified, NoDeadEnds]
must_haves:
  truths:
    - "The turion-satellite Lambda is redeployed via ./build-and-push.sh — CodeSha256 changes; POST /api/sales-orders + POST /api/satellites + PATCH /api/satellites/:id respond 401 without a JWT (route alive, auth-gated) and 404 on a bogus path"
    - "The turion-space-demo frontend is deployed via deploy-frontend.sh with the F6 pre-flight (stash ERP-demo WIP HTML + mv .superpowers/ aside before `aws s3 sync . --delete`, restore after); CloudFront E37R9PT8IL44L2 invalidation polled to Completed"
    - "program-new.html is live (HTTP 200, HTML content-type, contains the wizard markup); satellite-render.js live and contains programProgress; the '+ New satellite program' CTA is in the deployed index.html; the Next-step links are in the deployed sat/bom/kanban/instance/work-order/work-orders/part/cost/cost-detail pages"
    - "audit-satellite-buttons.mjs run from both repos reports 0 violations, exit 0 (post-deploy)"
    - "DB-direct E2E walk: a test program is spawned (INSERT a sales_orders row + SELECT spawn_satellite_program(...)) → the chain is confirmed: 1 new satellites row with sales_order_id set, N part_instances on it (= SAT-003's instance count), N bom_lines rewired (root parent NULL preserved, no parent/child pointing at SAT-003 instances), N part_stage_events at stage 'drawing' direction 'forward' status 'entered' — then the test program is cleaned up (DELETE the satellite cascades / DELETE its instances+bom_lines+events + the sales_orders row), leaving prod as it was"
    - "STATE.md / ROADMAP.md updated to reflect Phase 33 complete; nothing in Phases 27-32 regressed (curl-smoke the 3D pages, BOM tree, integrations panel as before)"
  artifacts:
    - path: /Users/jeet/doordash-p2p/.planning/phases/33-end-to-end-satellite-build-flow/33-06-SUMMARY.md
      provides: "deploy + E2E verification record"
  key_links:
    - from: deploy-frontend.sh
      to: S3 + CloudFront E37R9PT8IL44L2
      via: "F6 pre-flight stash → aws s3 sync . --delete → restore → CF invalidation poll"
      pattern: "E37R9PT8IL44L2"
    - from: spawn_satellite_program (live)
      to: a verified satellites + part_instances + bom_lines + part_stage_events chain
      via: "DB-direct E2E walk against prod, then cleanup"
      pattern: "spawn_satellite_program"
---

<objective>
Ship Phase 33: redeploy the turion-satellite Lambda (new routes), deploy the turion-space-demo frontend (wizard + wiring) with the F6 pre-flight, run the button audit in both repos, and verify the whole end-to-end flow with a DB-direct walk (spawn a throwaway program → confirm the satellite/BOM/instances/stage-0-events chain → clean it up), since the live magic-link browser walk isn't possible headlessly (Pitfall 4). Then update STATE/ROADMAP.

Purpose: requirement E2EFlowVerified — and the deploy gate that makes everything in 33-01..05 actually live.
Output: live backend + frontend, audits clean, E2E chain verified, docs updated.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-RESEARCH.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-01-SUMMARY.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-02-SUMMARY.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-03-SUMMARY.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-04-SUMMARY.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit + redeploy the turion-satellite Lambda; smoke the new routes</name>
  <files>/Users/jeet/turion-satellite/backend/src</files>
  <action>
1. Confirm migration 020 is already applied to prod (33-01 did this). If somehow not, apply it now (`psql "$DB_URL" -f migrations/020_...` — DB URL from `aws secretsmanager get-secret-value --secret-id arn:aws:secretsmanager:us-east-1:134607809447:secret:turion-satellite/production/database-url-NCbgX6 ...`, strip `?schema=`). The migration MUST be live before the backend that calls `spawn_satellite_program` is deployed.
2. In `/Users/jeet/turion-satellite`: ensure all Phase-33 backend changes (sales-orders.ts, satellites.ts, app.ts, tests) are committed — `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" add backend/src/routes/sales-orders.ts backend/src/routes/satellites.ts backend/src/app.ts backend/src/routes/*.test.ts migrations/020_*.sql && git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit -m "feat(33): sales-orders + spawn-satellite routes + migration 020"` — then `git push`.
3. Capture the current Lambda CodeSha256: `aws lambda get-function-configuration --function-name turion-satellite-api --query CodeSha256 --output text` (note it).
4. Redeploy: `cd /Users/jeet/turion-satellite && ./build-and-push.sh` (builds the arm64 image, pushes to ECR, updates Lambda `turion-satellite-api`). Wait for it to finish; then re-capture CodeSha256 — it MUST have changed.
5. Smoke the new routes against the live API base `https://rjydekliee.execute-api.us-east-1.amazonaws.com`:
   - `curl -s -o /dev/null -w '%{http_code}' -X POST https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/sales-orders` → expect `401` (auth-gated, route alive).
   - `curl -s -o /dev/null -w '%{http_code}' -X POST https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/satellites` → expect `401`.
   - `curl -s -o /dev/null -w '%{http_code}' -X PATCH https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/satellites/00000000-0000-0000-0000-000000000000` → expect `401`.
   - `curl -s -o /dev/null -w '%{http_code}' https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/this-route-does-not-exist` → expect `404` (sanity: not everything is 401).
   - `curl -s https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/health` → expect a healthy response.
  </action>
  <verify>
CodeSha256 changed after `./build-and-push.sh`. The four curl smoke checks return 401/401/401/404 as expected. `/api/health` healthy.
  </verify>
  <done>
turion-satellite Lambda redeployed with the new routes; routes are alive + auth-gated; bogus path 404s.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy the turion-space-demo frontend with the F6 pre-flight; commit; post-deploy audit</name>
  <files>/Users/jeet/turion-space-demo/satellite</files>
  <action>
1. In `/Users/jeet/turion-space-demo`: commit all Phase-33 frontend changes — `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" add satellite/program-new.html satellite/index.html satellite/satellite-render.js satellite/sat.html satellite/bom.html satellite/kanban.html satellite/instance.html satellite/work-order.html satellite/work-orders.html satellite/part.html satellite/cost.html satellite/cost-detail.html satellite/satellite-shell.css && git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit -m "feat(33): new-satellite-program wizard + next-step wiring across all satellite pages" && git push` (only `git add` files that actually changed).
2. F6 PRE-FLIGHT (MANDATORY — `deploy-frontend.sh` does `aws s3 sync . --delete` over the whole repo):
   - `git stash push -- about-this-demo.html agent-sales-cash.html dashboard-cio.html` (or whichever ERP-demo WIP HTML files are dirty in `git status` at repo root — stash any dirty `*.html` outside `satellite/`).
   - If `.superpowers/` exists at repo root: `mv .superpowers /tmp/superpowers-stash-33`.
   - Confirm `git status` is clean of WIP root HTML and `.superpowers/` is gone.
3. Deploy: `cd /Users/jeet/turion-space-demo && ./deploy-frontend.sh` (s3 sync + CF invalidate). Capture the CloudFront invalidation ID it prints.
4. RESTORE (always, even if the deploy failed): `git stash pop` (if you stashed); `mv /tmp/superpowers-stash-33 .superpowers` (if you moved it). Confirm `git stash list` is empty and the working tree matches the pre-deploy baseline.
5. Poll the CF invalidation to `Status=Completed`: `aws cloudfront get-invalidation --distribution-id E37R9PT8IL44L2 --id <invId> --query 'Invalidation.Status' --output text` in a loop until `Completed`.
6. Curl-smoke the deployed pages (`https://turionspace.zietra.com/satellite/...`):
   - `program-new.html` → 200, `content-type: text/html`, body contains the wizard markup (e.g. a string like "New satellite program" or the step-1 form id).
   - `satellite-render.js` → 200, `content-type: ...javascript`, body contains `programProgress`.
   - `index.html` (`/satellite/`) → 200, body contains `program-new.html`.
   - `sat.html` → 200, body contains `programProgress` and `satelliteApi.patch('/api/satellites/`.
   - `bom.html`, `kanban.html` → 200, contain `programProgress`.
   - `work-order.html` → 200, contains `satelliteApi.patch('/api/work-orders/`.
   - `cost-detail.html` → 200, contains `bom.html?sat=`.
   - `instance.html`, `work-orders.html`, `part.html`, `cost.html` → 200.
   - `login.html` → 200 (auth still works).
   - Phase-27-32 regression smoke: `3d-test.html` → 200; the jsDelivr `three@0.184.0` import-map URL still 200; `part.html` still contains `mount3DViewer` / the import map; `bom.html` still contains the `🧊 3D` deep-link badge.
7. Post-deploy button audit from BOTH repos: `cd /Users/jeet/turion-satellite/backend && node scripts/audit-satellite-buttons.mjs` → 0 violations, exit 0. (The audit scans the frontend HTML on disk; run it after the frontend changes are committed so it sees the final state.)
  </action>
  <verify>
`git stash list` empty after restore; working tree == pre-deploy baseline. CF invalidation `Completed`. All curl-smoke checks pass (200s, expected substrings present). Phase-27-32 regression smoke passes. `node scripts/audit-satellite-buttons.mjs` → 0 violations, exit 0.
  </verify>
  <done>
Frontend deployed with the F6 pre-flight; wizard + all wiring live; CF invalidated; Phases 27-32 pages unregressed; button audit 0 violations.
  </done>
</task>

<task type="auto">
  <name>Task 3: DB-direct end-to-end walk — spawn a test program, confirm the chain, clean up</name>
  <files>/Users/jeet/turion-satellite/backend/src</files>
  <action>
Headless E2E substitute (Pitfall 4 — no browser, no magic-link, no synthetic-JWT path; verify the data path directly against prod). DB URL from the secrets-manager arn (strip `?schema=`). Use a transaction or a clearly-tagged designation so cleanup is safe.

1. Record baseline: `psql "$DB_URL" -c "SELECT count(*) FROM turion_satellite.satellites;"` and `SELECT count(*) FROM turion_satellite.part_instances WHERE satellite_id='24587565-b15b-42ce-b590-87ecf9b6bb99';` (= SAT-003's instance count, call it N) and `... bom_lines WHERE satellite_id='24587565-...';` (call it M) — note N, M.
2. Spawn a test program:
   - `INSERT INTO turion_satellite.sales_orders (order_number, customer_name, program_name, status, source_data) VALUES ('SO-E2E-TEST-33', 'E2E Test Customer', 'E2E Test Program', 'open', '{}'::jsonb) RETURNING id;` → capture `$SO`.
   - `SELECT turion_satellite.spawn_satellite_program('E2E Test Sat', 'SAT-999', '$SO'::uuid, NULL, 'standard-bus');` → capture `$NEWSAT`.
3. Confirm the chain (all against prod):
   - `SELECT id, name, designation, status, sales_order_id FROM turion_satellite.satellites WHERE id='$NEWSAT';` → 1 row, `status='design'`, `sales_order_id='$SO'`.
   - `SELECT satellite_id FROM turion_satellite.sales_orders WHERE id='$SO';` → `'$NEWSAT'` (back-link set).
   - `SELECT count(*) FROM turion_satellite.part_instances WHERE satellite_id='$NEWSAT';` → equals N.
   - `SELECT count(*) FROM turion_satellite.bom_lines WHERE satellite_id='$NEWSAT';` → equals M.
   - Rewiring sanity — no dangling pointers at SAT-003: `SELECT count(*) FROM turion_satellite.bom_lines WHERE satellite_id='$NEWSAT' AND (parent_part_instance_id IN (SELECT id FROM turion_satellite.part_instances WHERE satellite_id='24587565-...') OR child_part_instance_id IN (SELECT id FROM turion_satellite.part_instances WHERE satellite_id='24587565-...'));` → **0**.
   - Root preserved: `SELECT count(*) FROM turion_satellite.bom_lines WHERE satellite_id='$NEWSAT' AND parent_part_instance_id IS NULL;` → equals the SAT-003 root-line count (>0).
   - Stage-0 events: `SELECT count(*) FROM turion_satellite.part_stage_events e JOIN turion_satellite.lifecycle_stages s ON s.id=e.stage_id WHERE e.satellite_id='$NEWSAT' AND s.code='drawing' AND e.direction='forward' AND e.status='entered';` → equals N (one per new instance).
   - No stage events at any other stage on the new satellite: `SELECT count(*) FROM turion_satellite.part_stage_events e JOIN turion_satellite.lifecycle_stages s ON s.id=e.stage_id WHERE e.satellite_id='$NEWSAT' AND s.code <> 'drawing';` → 0.
   - Status advance works: `UPDATE turion_satellite.satellites SET status='build' WHERE id='$NEWSAT' RETURNING status;` → `'build'` (proves the PATCH route's underlying update is valid; the HTTP path was already 401-smoked in Task 1).
4. Clean up — leave prod EXACTLY as it was:
   - `DELETE FROM turion_satellite.part_stage_events WHERE satellite_id='$NEWSAT';`
   - `DELETE FROM turion_satellite.bom_lines WHERE satellite_id='$NEWSAT';`
   - `DELETE FROM turion_satellite.part_instances WHERE satellite_id='$NEWSAT';`
   - `DELETE FROM turion_satellite.satellites WHERE id='$NEWSAT';`
   - `DELETE FROM turion_satellite.sales_orders WHERE id='$SO';`
   - (If there are FK ON DELETE CASCADE rules, a single `DELETE FROM satellites WHERE id='$NEWSAT'` + the `sales_orders` delete may suffice — but do the explicit ordered deletes to be safe.)
5. Confirm cleanup: `SELECT count(*) FROM turion_satellite.satellites;` == the baseline from step 1; `SELECT count(*) FROM turion_satellite.sales_orders WHERE order_number='SO-E2E-TEST-33';` → 0; `SELECT count(*) FROM turion_satellite.satellites WHERE designation='SAT-999';` → 0.
Record every query + result in the SUMMARY.
  </action>
  <verify>
All chain-confirmation queries return the expected values (N instances, M bom_lines, 0 dangling, N stage-0 events, 0 other-stage events, status advance OK). Cleanup confirmed: satellites count back to baseline, no SAT-999 / SO-E2E-TEST-33 rows remain.
  </verify>
  <done>
The spawn → satellite → part_instances → bom_lines → stage-0-events chain is verified end-to-end against prod, and the test program is fully removed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task: Human verification of the live E2E flow (optional follow-up)</name>
  <what-built>
Phase 33 is deployed: the turion-satellite Lambda has the new routes (`POST /api/sales-orders`, `POST /api/satellites`, `PATCH /api/satellites/:id`); migration 020 (sales_orders table + spawn_satellite_program function) is live on prod; the turion-space-demo frontend has the new "+ New satellite program" wizard (`/satellite/program-new.html`) wired from the constellation index, plus "Next step ▸" links + a programProgress lifecycle strip on sat / bom / kanban / instance / work-order / work-orders / part / cost / cost-detail — no dead ends. Button audit: 0 violations in both repos. A DB-direct E2E walk confirmed the spawn chain (satellite + N part_instances + M bom_lines + N stage-0 events, correctly rewired) and cleaned itself up. The live magic-link browser walk was NOT performed (headless orchestrator, no browser, ES256-JWKS auth, no synthetic-JWT path) — verified via curl/HEAD text-proxy smoke + the DB-direct walk, per the Phase 27-32 headless-substitute precedent.
  </what-built>
  <how-to-verify>
Optional human follow-up (not required to mark the phase complete):
1. Visit https://turionspace.zietra.com/satellite/ — sign in with the demo magic-link email.
2. Confirm a "+ New satellite program" button is visible. Click it → lands on `/satellite/program-new.html`.
3. Fill the wizard: program name, customer name, accept the suggested designation (e.g. SAT-005) → submit. Confirm it shows "Program created ✓" with a parts/BOM-lines summary and redirects to the new satellite's page.
4. On the new satellite page: confirm the lifecycle progress strip is visible, the bus SVG renders, the BOM tree has nodes, and there's a "Next step ▸" CTA.
5. Walk forward: satellite → "Open the BOM tree ▸" → "Start the lifecycle — open Kanban ▸" → click a card → instance page → advance a stage → (if make) "Open / create a work order ▸" → work order → "Mark this work order complete ▸" → "Back to the instance ▸". Confirm no page leaves you stuck. (Then delete the test satellite via psql if you want prod left pristine — or leave it as a second demo satellite.)
6. Confirm Phases 27-32 still work: 3D viewer on a part page, BOM 3D badges, integrations panels.
  </how-to-verify>
  <action>Pause for the user. Present the what-built summary and the how-to-verify steps above. This checkpoint is informational/optional — the phase's E2E requirement is already satisfied by Task 3's DB-direct walk + Task 1/2's curl-smoke; the live browser walk is a nice-to-have follow-up.</action>
  <verify>User responds "approved" (or reports a specific page issue → file a /gsd:debug).</verify>
  <done>User has been shown the live URLs and the walkthrough; phase may proceed to Task 4 regardless of whether the optional browser walk was performed.</done>
  <resume-signal>Type "approved" (or describe issues — if found, file a /gsd:debug for the specific page).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update STATE.md + ROADMAP.md; commit docs</name>
  <files>/Users/jeet/doordash-p2p/.planning/STATE.md, /Users/jeet/doordash-p2p/.planning/ROADMAP.md</files>
  <action>
1. ROADMAP.md: mark Phase 33 complete (the established marker for completed phases in this roadmap — match how Phases 27-32 are marked), update the Phase-33 `**Plans:**` line to "6 plans" and replace the "TBD" plan list with the 6 plan filenames + brief objectives.
2. STATE.md: add a Phase-33 entry under the per-phase notes section (mirroring the Phase-27..31 entries) capturing the key decisions/outcomes: migration 020 (sales_orders + spawn_satellite_program, full SAT-003 clone template, SAT-003 UUID 24587565-...), the 3 new backend routes, the program-new.html wizard, the programProgress strip + next-step wiring, the Lambda CodeSha256 before→after, the CF invalidation ID, the F6 pre-flight, the DB-direct E2E walk result, audit 0 violations both repos.
3. Commit: `node /Users/jeet/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(33): mark phase complete — E2E satellite-build flow shipped" --files .planning/STATE.md .planning/ROADMAP.md .planning/phases/33-end-to-end-satellite-build-flow/33-*-SUMMARY.md` (or a plain `git -c user.email=... commit` if gsd-tools isn't appropriate here).
  </action>
  <verify>
ROADMAP.md shows Phase 33 complete with 6 plans listed. STATE.md has a Phase-33 entry. The docs commit exists.
  </verify>
  <done>
STATE.md + ROADMAP.md reflect Phase 33 complete; docs committed.
  </done>
</task>

</tasks>

<verification>
- Lambda redeployed (CodeSha256 changed); new routes 401-gated, bogus path 404s.
- Frontend deployed with F6 pre-flight; CF invalidation Completed; working tree restored.
- Curl-smoke: wizard + wiring live; Phases 27-32 pages unregressed.
- Button audit: 0 violations in both repos.
- DB-direct E2E walk: spawn chain verified (N instances, M bom_lines, 0 dangling, N stage-0 events), test program cleaned up.
- STATE.md / ROADMAP.md updated; docs committed.
</verification>

<success_criteria>
`curl -s -o /dev/null -w '%{http_code}' -X POST https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/satellites` → 401. `curl -s https://turionspace.zietra.com/satellite/program-new.html | grep -c "New satellite program"` → >0. `node scripts/audit-satellite-buttons.mjs` → 0 violations. The E2E walk's chain queries all return expected values; cleanup confirmed. ROADMAP Phase 33 = complete.
</success_criteria>

<output>
After completion, create `.planning/phases/33-end-to-end-satellite-build-flow/33-06-SUMMARY.md`
</output>
