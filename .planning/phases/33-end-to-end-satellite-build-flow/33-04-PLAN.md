---
phase: 33-end-to-end-satellite-build-flow
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - /Users/jeet/turion-space-demo/satellite/satellite-render.js
  - /Users/jeet/turion-space-demo/satellite/sat.html
  - /Users/jeet/turion-space-demo/satellite/bom.html
  - /Users/jeet/turion-space-demo/satellite/kanban.html
autonomous: true
requirements: [LifecycleWiring, NoDeadEnds]
must_haves:
  truths:
    - "satellite-render.js exports a new programProgress(currentStage, satStatus) helper that renders a horizontal strip of the 6 lifecycle stages (drawing → component → bom → assembly → plm_review → production) with the current/majority stage highlighted, plus the satellites.status chip — a self-contained DOM/HTML-string helper, no new library"
    - "sat.html shows the programProgress strip and a 'Next step ▸' CTA: 'Open the BOM tree ▸' when the BOM hasn't been worked yet, 'Continue building — open Kanban ▸' otherwise, and 'Advance program to <next status> ▸' (PATCH /api/satellites/:id) when all root instances are at the final lifecycle stage"
    - "bom.html shows the programProgress strip and a 'Next step ▸' CTA: 'Start the lifecycle — open Kanban ▸' → kanban.html?sat=<satId>"
    - "kanban.html shows the programProgress strip and a 'Back to satellite ▸' CTA; when every card is in the final column it shows 'All parts built — back to the satellite to advance the program ▸'"
    - "All new interactivity uses addEventListener (no onclick attributes); the PATCH call uses window.satelliteApi.patch (no bare fetch); the PATCH path /api/satellites/:id is mounted (33-02)"
    - "audit-satellite-buttons.mjs reports 0 violations after these changes"
    - "Phases 27-32 features on these pages (sat.html bus SVG + add-instance modal, bom.html BOM tree + 3D badges + add-BOM-line modal, kanban columns) still work — only forward links + the progress strip added"
  artifacts:
    - path: /Users/jeet/turion-space-demo/satellite/satellite-render.js
      provides: "programProgress() helper on window.satelliteRender"
      contains: "programProgress"
    - path: /Users/jeet/turion-space-demo/satellite/sat.html
      provides: "progress strip + Next step CTA (incl. advance-program-status)"
      contains: "satelliteApi.patch"
  key_links:
    - from: satellite/sat.html
      to: /api/satellites/:id (PATCH)
      via: "await window.satelliteApi.patch('/api/satellites/'+satId, {status:nextStatus})"
      pattern: "satelliteApi\\.patch\\('/api/satellites/"
    - from: satellite/bom.html
      to: satellite/kanban.html
      via: "a 'Start the lifecycle — open Kanban ▸' link"
      pattern: "kanban\\.html\\?sat="
---

<objective>
Add the shared `programProgress()` lifecycle strip and wire "Next step ▸" links on the three hub pages — `sat.html`, `bom.html`, `kanban.html` — so a user can walk constellation → satellite → BOM → Kanban → instances without dead-ending, and can advance the satellite's status when all parts are built.

Purpose: requirements LifecycleWiring + NoDeadEnds for the hub pages. The progress strip is the cheapest way to make the whole flow feel walkable.
Output: programProgress() in satellite-render.js + Next-step wiring on sat/bom/kanban.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-RESEARCH.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-02-SUMMARY.md

# Patterns to mirror (read before editing):
@/Users/jeet/turion-space-demo/satellite/satellite-render.js
@/Users/jeet/turion-space-demo/satellite/sat.html
@/Users/jeet/turion-space-demo/satellite/bom.html
@/Users/jeet/turion-space-demo/satellite/kanban.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add programProgress() to satellite-render.js</name>
  <files>/Users/jeet/turion-space-demo/satellite/satellite-render.js</files>
  <action>
Read `satellite-render.js` — it already exports `escapeHtml, fmtDate, fmtTimeAgo, statusTag, breadcrumb, skeleton, toast, topbarHTML, getQueryParam, renderIntegrationsPanel` on `window.satelliteRender`. Add a sibling `programProgress(currentStage, satStatus)`:
- The 6 lifecycle stages, in order: `drawing`, `component`, `bom`, `assembly`, `plm_review`, `production` (these are the `lifecycle_stages.code` values per RESEARCH §B — confirm by `grep` if any other page hardcodes them; reuse, don't reinvent). Render a horizontal strip (a `<div>` of `<span>`s or a flex row) — each stage as a labelled pill; the pill matching `currentStage` gets a highlight class; stages before it get a "done" class, stages after a "pending" class. Use a human label per code (`drawing`→"Drawing", `component`→"Component", `bom`→"BOM", `assembly`→"Assembly", `plm_review`→"PLM Review", `production`→"Production").
- After the stage strip, render the `satStatus` chip — reuse `statusTag(satStatus)` if it produces something suitable, else a small `<span class="chip">`. (`satStatus` is `satellites.status`: design/build/test/ship/launch/orbit per RESEARCH §B.)
- Return an HTML string (matching how `breadcrumb()` works — pages do `el.innerHTML = r.breadcrumb(...)`), OR a DOM node — match whatever `breadcrumb()` returns so consuming pages use it the same way.
- Add minimal styling: prefer adding to `satellite-shell.css` if that's where shared component CSS lives; if the established pattern is per-page `<style>` blocks (STATE note: Phase-30/31 HUD CSS lives per-page), then put a tiny `<style>` for `.progress-strip` in each consuming page instead — match the codebase's actual convention. Either way the strip must look right against the dark theme.
- Update the `window.satelliteRender = { ... }` export object to include `programProgress`.

Do NOT change any existing export's behaviour.
  </action>
  <verify>
`grep -n "programProgress" satellite/satellite-render.js` shows the function + the export. Mentally render: with `currentStage='bom'`, stages drawing/component show "done", bom shows "current", assembly/plm_review/production show "pending"; the satStatus chip renders.
  </verify>
  <done>
programProgress(currentStage, satStatus) exists on window.satelliteRender, returns a renderable strip of the 6 stages + the status chip, styled for the dark theme; no existing helper changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire sat.html — progress strip + Next-step CTA + advance-program-status</name>
  <files>/Users/jeet/turion-space-demo/satellite/sat.html</files>
  <action>
Edit `sat.html` (the single-satellite page — bus SVG + subsystem drawer + add-instance modal). It already fetches `GET /api/satellites/:id` which returns `stage_summary` (per RESEARCH §B/§E). Additions:
1. Render the progress strip near the top (under the header / breadcrumb): compute the "majority stage" from `stage_summary` (the stage with the most instances, or the lowest stage that still has instances — pick a sensible definition) and call `window.satelliteRender.programProgress(majorityStage, sat.status)`; inject into a new `<div id="progressStrip">`.
2. Add a "Next step ▸" CTA block. Logic:
   - If the satellite's instances are essentially all still at `drawing` (BOM not worked yet — define from `stage_summary`): primary CTA "Open the BOM tree ▸" → `bom.html?sat=${satId}` (use the real param name `sat.html`/`bom.html` use — grep to confirm).
   - Else if all root-level instances are at the final stage (`production`) — i.e. the build is done: primary CTA "Advance program to ${NEXT_STATUS} ▸" where `NEXT_STATUS` is the next value after `sat.status` in `[design, build, test, ship, launch, orbit]` (RESEARCH §B). Clicking it: `await window.satelliteApi.patch('/api/satellites/'+satId, { status: NEXT_STATUS });` then `window.satelliteRender.toast('Program advanced to '+NEXT_STATUS)` + `location.reload()`. Wrap in try/catch → toast on ApiError. If `sat.status` is already `orbit` (terminal), show instead "Program complete — view cost rollup ▸" → `cost.html?sat=${satId}`.
   - Else (mid-build): primary CTA "Continue building — open Kanban ▸" → `kanban.html?sat=${satId}`. Also keep a secondary "Open the BOM tree ▸" link.
   All buttons/links wired with `addEventListener` (the PATCH button) or plain `<a href>` (the navigation links). No `onclick=`.
3. Leave the bus SVG, the subsystem drawer, the "+ Add part instance" modal, and the existing kanban/BOM/WO jump buttons untouched.
  </action>
  <verify>
`grep -n "programProgress" satellite/sat.html` present. `grep -n "satelliteApi.patch('/api/satellites/" satellite/sat.html` present. `grep -n "onclick=" satellite/sat.html` — no NEW ones. `grep -n "bom.html?sat=" satellite/sat.html` and `grep -n "kanban.html?sat=" satellite/sat.html` present.
  </verify>
  <done>
sat.html shows the progress strip + a context-aware "Next step ▸" CTA including PATCH-driven program-status advancement; existing Phase-25-32 features intact.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire bom.html + kanban.html — progress strip + Next-step CTAs + frontend audit</name>
  <files>/Users/jeet/turion-space-demo/satellite/bom.html, /Users/jeet/turion-space-demo/satellite/kanban.html</files>
  <action>
1. `bom.html` (recursive BOM tree + 3D badges + "+ Add BOM line" modal): add a `<div id="progressStrip">` near the top — to render it you need the satellite's `stage_summary` + `status`; either it's already fetched, or add a `GET /api/satellites/${satId}` call alongside the existing `GET .../bom` fetch (don't refetch per node — RESEARCH Pitfall 8). Call `window.satelliteRender.programProgress(majorityStage, sat.status)`. Add a "Next step ▸" CTA: "Start the lifecycle — open Kanban ▸" → `kanban.html?sat=${satId}`. Plain `<a href>`. Leave the tree, the 3D badges (Phase-30), and the add-BOM-line modal (Phase-29) untouched.
2. `kanban.html` (lifecycle kanban — columns by stage, cards = instances): add the `<div id="progressStrip">` (needs `stage_summary` + `status` — fetch `GET /api/satellites/${satId}` if not already; the kanban already groups instances by stage so the "majority stage" is easy to derive from what it has). Add CTAs: always a "Back to satellite ▸" → `sat.html?id=${satId}` (or the real param). When EVERY card is in the final column (`production`): also show "All parts built — back to the satellite to advance the program ▸" → `sat.html?id=${satId}`. Plain `<a href>` links. Leave the columns + card drill-downs untouched.
3. Run the frontend button audit: `cd /Users/jeet/turion-satellite/backend && node scripts/audit-satellite-buttons.mjs` → 0 violations, exit 0. (`GET /api/satellites/:id` is already a mounted route; the new `addEventListener` PATCH on sat.html hits the now-mounted `PATCH /api/satellites/:id`.)
  </action>
  <verify>
`grep -n "programProgress" satellite/bom.html satellite/kanban.html` — present in both. `grep -n "kanban.html?sat=" satellite/bom.html` present. `grep -n "sat.html?id=" satellite/kanban.html` present. `grep -n "onclick=" satellite/bom.html satellite/kanban.html` — no NEW ones. `node scripts/audit-satellite-buttons.mjs` → 0 violations.
  </verify>
  <done>
bom.html and kanban.html show the progress strip + Next-step CTAs; no dead end on either; existing features intact; button audit 0 violations.
  </done>
</task>

</tasks>

<verification>
- programProgress() exported on window.satelliteRender; renders the 6-stage strip + status chip; dark-theme styled.
- sat.html / bom.html / kanban.html each show the strip and a forward "Next step ▸" CTA — no dead end.
- sat.html can PATCH the satellite status.
- All new interactivity via addEventListener or `<a href>`; no bare fetch.
- Button audit: 0 violations.
- Phases 27-32 features on these three pages unchanged.
</verification>

<success_criteria>
`grep "programProgress" satellite/satellite-render.js satellite/sat.html satellite/bom.html satellite/kanban.html` — present in all four. `grep "satelliteApi.patch('/api/satellites/" satellite/sat.html` present. `node scripts/audit-satellite-buttons.mjs` → 0 violations.
</success_criteria>

<output>
After completion, create `.planning/phases/33-end-to-end-satellite-build-flow/33-04-SUMMARY.md`
</output>
