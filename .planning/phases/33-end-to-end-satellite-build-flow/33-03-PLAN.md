---
phase: 33-end-to-end-satellite-build-flow
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - /Users/jeet/turion-space-demo/satellite/program-new.html
  - /Users/jeet/turion-space-demo/satellite/index.html
autonomous: true
requirements: [SalesOrderWizard, SatelliteSpawn]
must_haves:
  truths:
    - "/satellite/program-new.html exists — a full-page multi-step wizard following the established satellite-app page skeleton (satellite-config.js → supabase-js UMD → satellite-auth.js → satellite-api.js → satellite-render.js, requireSession(), topbarHTML)"
    - "Step 1 collects program name, customer name, designation (pre-filled with a suggested next SAT-00N), target launch (optional), contract value (optional), notes; client-side validation before submit"
    - "On submit: POST /api/sales-orders creates the sales order; then POST /api/satellites {name, designation, sales_order_id, template:'standard-bus'} spawns the satellite + BOM clone + stage-0 events — both via window.satelliteApi.post, never bare fetch"
    - "Step 3 shows 'Program created ✓' with a summary (N parts, N bom_lines, plus a 'View the new satellite ▸' link to satellite page ?sat=<newSatId> — using whatever query-param name the live satellite page reads) and auto-redirects there"
    - "Errors are handled gracefully — if POST /api/satellites fails (the big transaction rolls back server-side), the wizard shows the error inline (#someErr or toast), does not redirect, lets the user retry; a 409 on duplicate designation shows a clear 'pick a different designation' message"
    - "/satellite/ index has a prominent '+ New satellite program' CTA (hero CTA when 0 satellites, a button in the header/nav otherwise) → program-new.html — wired via addEventListener (no onclick attribute)"
    - "audit-satellite-buttons.mjs (frontend scan) reports 0 violations on the new wizard page + index change"
  artifacts:
    - path: /Users/jeet/turion-space-demo/satellite/program-new.html
      provides: "The New satellite program wizard page"
      min_lines: 120
      contains: "spawn"
    - path: /Users/jeet/turion-space-demo/satellite/index.html
      provides: "+ New satellite program CTA"
      contains: "program-new.html"
  key_links:
    - from: satellite/program-new.html
      to: /api/sales-orders and /api/satellites
      via: "await window.satelliteApi.post('/api/sales-orders', ...) then .post('/api/satellites', ...)"
      pattern: "satelliteApi\\.post\\('/api/(sales-orders|satellites)'"
    - from: satellite/index.html
      to: satellite/program-new.html
      via: "a '+ New satellite program' link/button wired with addEventListener"
      pattern: "program-new\\.html"
---

<objective>
Build the centerpiece: a full-page "New satellite program" wizard at `/satellite/program-new.html` that captures program/customer/sales-order details, creates the sales order, spawns the satellite (with the full SAT-003 BOM clone + stage-0 events) via the new backend routes, and lands the user on the new satellite's page. Plus the entry point: a "+ New satellite program" CTA on the constellation index.

Purpose: Today there is no UI to start a new satellite — `satellites` rows are seed-only. This is requirement SalesOrderWizard + SatelliteSpawn, and the front door for the whole walkable flow.
Output: program-new.html + the index CTA.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-RESEARCH.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-02-SUMMARY.md

# Patterns to mirror (read before writing):
@/Users/jeet/turion-space-demo/satellite/index.html
@/Users/jeet/turion-space-demo/satellite/sat.html
@/Users/jeet/turion-space-demo/satellite/satellite-render.js
@/Users/jeet/turion-space-demo/satellite/satellite-api.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create satellite/program-new.html — the 3-step wizard</name>
  <files>/Users/jeet/turion-space-demo/satellite/program-new.html</files>
  <action>
Create `satellite/program-new.html`. Follow the EXACT page skeleton used by `sat.html` / `index.html`:
- `<head>`: same `<meta>`/title pattern; load `satellite-shell.css` (or whatever the live pages load); the script load order is: `<script src="satellite-config.js">` → supabase-js UMD (jsDelivr, same URL the other pages use) → `<script src="satellite-auth.js">` → `<script src="satellite-api.js">` → `<script src="satellite-render.js">` → then the page's own inline `<script>` IIFE.
- `<body>`: `<div id="topbar"></div>` then `<main class="main">` (match the live class names — STATE note: live pages use `<main class=main>`, `#crumb`, dark theme) containing the wizard.
- Inline `<script>` IIFE: `(async()=>{ const session = await window.satelliteAuth.requireSession(); document.getElementById('topbar').innerHTML = window.satelliteRender.topbarHTML(session.user.email); ... })()`. Use `window.satelliteRender` helpers (`escapeHtml`, `toast`, `breadcrumb` if useful).

Wizard structure — a single page with 3 stacked sections (or 3 panels toggled by Next/Back; pick the simpler one — 3 sections shown sequentially via `.hidden` toggles is fine):
- **Step 1 — Program details:** a form with fields: `program_name` (text, required), `customer_name` (text, required), `designation` (text, required — on page load, fetch `GET /api/satellites`, find the highest `SAT-00N`, pre-fill `SAT-00{N+1}`; user can edit), `target_launch` (date, optional → goes into `source_data`), `contract_value_usd` (number, optional), `notes` (textarea, optional → `source_data`). A "Create program ▸" submit button. Client-side validation FIRST (non-empty program_name + customer_name + designation matching `/^SAT-\d{3,}$/`) — show errors inline near each field; only call the API if valid.
- **Step 2 — Spawning (in-flight):** on submit, hide step 1, show a "Creating program…" spinner. Then: `const so = await window.satelliteApi.post('/api/sales-orders', { customer_name, program_name, contract_value_usd: <num or null>, source_data: { target_launch, notes } });` → `const sat = await window.satelliteApi.post('/api/satellites', { name: program_name, designation, sales_order_id: so.id, template: 'standard-bus' });`. (Note: satellite `name` — use `program_name` or add a separate `name` field; the wizard can just reuse `program_name` as the satellite name, or add a small "satellite name" field defaulting to program_name. Pick one and be consistent with what `POST /api/satellites` expects per 33-02.)
- **Step 3 — Done:** on success, hide the spinner, show "Program created ✓" + a summary line: `${sat.parts} part instances, ${sat.bom_lines} BOM lines seeded` + a button "View the new satellite ▸". Determine the satellite-page filename + query param the live app uses (RESEARCH §A says `sat.html?id=<satId>` but STATE note Phase-28 says some links migrated to `?inst=`/`?sat=` — `grep getQueryParam satellite/*.html` to confirm what `sat.html` actually reads; build the link to match: e.g. `sat.html?id=${sat.id}` or `satellite.html?sat=${sat.id}` — use the real one). Auto-redirect there after ~2s (`setTimeout(()=>location.href=..., 2000)`) AND give the explicit button (in case the redirect is missed).
- **Error handling:** wrap the step-2 API calls in `try/catch`. On `ApiError`: if `err.status === 409` show "That designation is already taken — go back and pick another" + a "Back" button that returns to step 1 with the form preserved. On any other error: `window.satelliteRender.toast('Could not create the program — ' + err.message)` (ApiError's `.message` is the server's generic message, safe to show) + show step 1 again with the form preserved. Do NOT redirect on error. The server rolls back the spawn transaction on failure (33-02), so a failed attempt leaves no half-created satellite — the user can fix the input and retry.

ALL interactivity via `addEventListener` (form submit, Next/Back, the View button) — NO `onclick=` attributes (Phase-29 audit). NO bare `fetch` — only `window.satelliteApi.*`.
  </action>
  <verify>
`grep -n "satelliteApi.post('/api/sales-orders'" satellite/program-new.html` and `grep -n "satelliteApi.post('/api/satellites'" satellite/program-new.html` both present. `grep -n "onclick=" satellite/program-new.html` → empty. `grep -n "addEventListener" satellite/program-new.html` → present. Open the file mentally / lint the HTML: valid structure, scripts in the right order, no `<a>` inside `<a>`.
  </verify>
  <done>
program-new.html is a complete 3-step wizard following the live page skeleton, calling POST /api/sales-orders then POST /api/satellites, handling 409 + generic errors without redirecting, redirecting to the new satellite page on success, all interactivity via addEventListener.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the "+ New satellite program" CTA to satellite/index.html + run the frontend audit</name>
  <files>/Users/jeet/turion-space-demo/satellite/index.html</files>
  <action>
Edit `satellite/index.html` (the constellation page):
- When the `GET /api/satellites` list is empty: replace (or augment) the current "Contact your administrator to seed the constellation" empty-state with a prominent hero CTA — a button/link "+ New satellite program" → `program-new.html` (`location.href='program-new.html'` via an `addEventListener('click', ...)`, or just `<a href="program-new.html" class="...">+ New satellite program</a>` styled as a button — an `<a href>` is fine and audit-safe).
- Always (whether or not there are satellites): add a smaller "+ New program" button in the page header / nav strip area (next to the existing nav links) → `program-new.html`. Match the existing nav-link styling.
- Keep everything else on index.html untouched (the KPI strip, the satellite cards, the existing nav links to parts/WO/BOM/kanban/cost).

Then run the button audit from the FRONTEND repo: `cd /Users/jeet/turion-satellite/backend && node scripts/audit-satellite-buttons.mjs` (the scanner reads `/Users/jeet/turion-space-demo/satellite/*.html` — confirm by reading the script header; it's the same audit, run from the backend repo, that scans the frontend). It must report 0 violations, exit 0 — every `/api/...` path the wizard calls (`/api/sales-orders`, `/api/satellites`) is now mounted (33-02), and there are no `onclick=` attributes.
  </action>
  <verify>
`grep -n "program-new.html" satellite/index.html` → at least 2 references (hero CTA + nav button). `grep -n "onclick=" satellite/index.html` — if the existing file has none, still none. `node scripts/audit-satellite-buttons.mjs` → 0 violations, exit 0.
  </verify>
  <done>
index.html has a "+ New satellite program" entry point (hero when empty + nav button always) → program-new.html; frontend button audit 0 violations.
  </done>
</task>

</tasks>

<verification>
- program-new.html follows the live satellite-app page skeleton (config → supabase → auth → api → render → inline IIFE; requireSession; topbarHTML).
- The wizard creates the SO then spawns the satellite via the new routes; errors handled without half-creating; success redirects to the real satellite page.
- index.html has the "+ New satellite program" CTA.
- Button audit: 0 violations.
- No other satellite-app page touched; Phases 25-32 frontend unchanged.
</verification>

<success_criteria>
`ls satellite/program-new.html` exists. `grep "satelliteApi.post('/api/satellites'" satellite/program-new.html` present. `grep "program-new.html" satellite/index.html` present. `node scripts/audit-satellite-buttons.mjs` → 0 violations.
</success_criteria>

<output>
After completion, create `.planning/phases/33-end-to-end-satellite-build-flow/33-03-SUMMARY.md`
</output>
