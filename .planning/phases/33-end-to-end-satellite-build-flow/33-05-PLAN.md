---
phase: 33-end-to-end-satellite-build-flow
plan: 05
type: execute
wave: 4
depends_on: [03]
files_modified:
  - /Users/jeet/turion-space-demo/satellite/instance.html
  - /Users/jeet/turion-space-demo/satellite/work-order.html
  - /Users/jeet/turion-space-demo/satellite/work-orders.html
  - /Users/jeet/turion-space-demo/satellite/part.html
  - /Users/jeet/turion-space-demo/satellite/cost.html
  - /Users/jeet/turion-space-demo/satellite/cost-detail.html
autonomous: true
requirements: [LifecycleWiring, NoDeadEnds]
must_haves:
  truths:
    - "instance.html: when the instance is at the final lifecycle stage it shows 'This part is done — back to the satellite ▸' → satellite page; when its make-buy decision is 'make' and it has no work order, it shows 'Open / create a work order ▸' (the make-path equivalent of the existing 'place a vendor order' buy-path hint)"
    - "work-order.html: shows a 'Mark this work order complete ▸' control (only when all build steps are signed pass, or always-enabled with a server-side guard) wired to PATCH /api/work-orders/:woId {status:'complete'} via window.satelliteApi.patch; on success it shows 'Back to the instance to advance its lifecycle stage ▸' → instance.html; also a 'Next open work order ▸' link when the satellite has more open WOs"
    - "work-orders.html: has a 'Back to satellite ▸' link; the list rows already link to each WO"
    - "part.html: when ?sat= is present, shows a 'View instances on this satellite ▸' / 'Back to the BOM tree ▸' link"
    - "cost.html: has a 'Back to satellite ▸' link (per-row 'View →' already exists)"
    - "cost-detail.html: has 'Back to the BOM tree ▸' → bom.html?sat= (and the existing 'Back to the part ▸' link) — no longer a dead end"
    - "All new interactivity via addEventListener (the PATCH-WO button) or plain <a href>; no onclick attributes; PATCH /api/work-orders/:woId is an existing mounted route"
    - "audit-satellite-buttons.mjs reports 0 violations after these changes"
    - "Phases 27-32 features on these pages (instance.html 3D viewer + lifecycle timeline + integrations panel + advance/revert; work-order.html steps; part.html 3D + make/buy panel + build-process panel; cost-detail integrations panel) still work"
  artifacts:
    - path: /Users/jeet/turion-space-demo/satellite/work-order.html
      provides: "complete-WO control + back-to-instance + next-WO links"
      contains: "satelliteApi.patch"
    - path: /Users/jeet/turion-space-demo/satellite/instance.html
      provides: "done→back-to-sat + make-path WO CTA"
      contains: "work-order"
  key_links:
    - from: satellite/work-order.html
      to: /api/work-orders/:woId (PATCH)
      via: "await window.satelliteApi.patch('/api/work-orders/'+woId, {status:'complete'})"
      pattern: "satelliteApi\\.patch\\('/api/work-orders/"
    - from: satellite/cost-detail.html
      to: satellite/bom.html
      via: "a 'Back to the BOM tree ▸' link"
      pattern: "bom\\.html\\?sat="
---

<objective>
Kill the remaining dead ends: `work-order.html` (terminal once steps are signed — add a "complete WO" control + forward links), `instance.html` (no "part done → back to satellite" link, no make-path WO CTA), `work-orders.html`, `part.html`, `cost.html`, `cost-detail.html` (terminal). Every page gets a "Next step ▸" / "Back to ▸" affordance.

Purpose: requirements LifecycleWiring + NoDeadEnds for the leaf/detail pages. Together with 33-04, no satellite-app page leaves the user stuck.
Output: forward/back links + the complete-WO control across 6 pages.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-RESEARCH.md

# Patterns to mirror (read before editing):
@/Users/jeet/turion-space-demo/satellite/instance.html
@/Users/jeet/turion-space-demo/satellite/work-order.html
@/Users/jeet/turion-space-demo/satellite/work-orders.html
@/Users/jeet/turion-space-demo/satellite/part.html
@/Users/jeet/turion-space-demo/satellite/cost.html
@/Users/jeet/turion-space-demo/satellite/cost-detail.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire work-order.html — complete-WO control + back-to-instance + next-WO</name>
  <files>/Users/jeet/turion-space-demo/satellite/work-order.html</files>
  <action>
Read `work-order.html` (one work order: status, bay, technician, its build steps). First `grep` it for any EXISTING "complete WO" / `PATCH .../work-orders/` control — RESEARCH Open Q3 says the route is live but the UI control may be missing. If it's already there, just ensure the forward links below exist; if absent, ADD it:
- A "Mark this work order complete ▸" button. Enablement: prefer enabling it only when every build step on this WO is `signed_by` non-null with `result='pass'` (the page already fetches `GET /api/work-orders/:woId/steps` — compute from that); if that's awkward, always-enable it and let the server's `PATCH /api/work-orders/:woId` enforce — but client-side gating is nicer. When the WO is already `complete`, hide the button and show a "✓ Completed" badge instead.
- Click handler (`addEventListener`, not `onclick`): `await window.satelliteApi.patch('/api/work-orders/'+woId, { status:'complete' });` → `window.satelliteRender.toast('Work order completed')` then either `location.reload()` or directly show the post-complete CTA. Wrap in try/catch → toast on ApiError.
- After completion (or always, below the steps): a "Back to the instance to advance its lifecycle stage ▸" link → `instance.html?id=${instId}&sat=${satId}` (use BOTH `?id=` and the param the live instance.html reads — STATE Phase-28 note says links carry both `?inst=` and `?id=`; match that). The page already knows the instance + satellite ids (it links back to `instance.html` per RESEARCH §A).
- A "Next open work order ▸" link when the satellite has more open WOs: fetch `GET /api/satellites/${satId}/work-orders`, find the next one with `status` in `{open,in_progress,rework}` that isn't this one → `work-order.html?id=${nextWoId}` (+ matching params). If none, omit. (Reuses an existing route — audit-safe.)
- Keep the existing status/bay/technician display + the build-steps list + the back-to-WO-list link untouched.
  </action>
  <verify>
`grep -n "satelliteApi.patch('/api/work-orders/" satellite/work-order.html` present. `grep -n "instance.html?" satellite/work-order.html` present. `grep -n "onclick=" satellite/work-order.html` — no NEW ones. `grep -n "addEventListener" satellite/work-order.html` present.
  </verify>
  <done>
work-order.html has a complete-WO control (PATCH-driven, gated/badged), a "back to the instance" CTA, and a "next open WO" link; existing display intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire instance.html + work-orders.html — done→back-to-sat, make-path WO CTA, back-to-sat</name>
  <files>/Users/jeet/turion-space-demo/satellite/instance.html, /Users/jeet/turion-space-demo/satellite/work-orders.html</files>
  <action>
1. `instance.html` (one part_instance: 3D viewer, lifecycle timeline, parent-path breadcrumb, sub-parts, manufacturing/procurement panel, integrations panel, work-orders-for-this-instance, advance/revert buttons). Additions:
   - After the lifecycle timeline / advance-revert area: if the instance's current stage is the final stage (`production`), show a "This part is done — back to the satellite ▸" link → `sat.html?id=${satId}` (the real param). (The advance button itself is the "next step" while there are stages left — leave it; just add the done-state link.)
   - Make-path WO CTA: the page knows the part's make-buy decision (it has a manufacturing/procurement panel) and its work orders. If the decision is `make` AND there are no work orders for this instance: show "Open / create a work order ▸" → if the satellite has an open WO that covers this part, link to `work-order.html?id=${woId}`; else link to `work-orders.html?sat=${satId}` (where the user can create one). This is the make-path equivalent of the existing buy-path "place a vendor order on the part page" hint (RESEARCH §E) — keep the buy-path hint too.
   - Plain `<a href>` links; no `onclick=`.
   - Leave the 3D viewer (Phase-30/31), the integrations panel (Phase-28), the advance/revert buttons, the parent breadcrumb, the sub-parts gallery untouched.
2. `work-orders.html` (WO list for a satellite): add a "Back to satellite ▸" link → `sat.html?id=${satId}` near the top/header. The list rows already link to each WO. If there's no "+ New work order" affordance and the create route exists (`POST /api/satellites/:satId/work-orders` — RESEARCH §B says yes), optionally add a "+ New work order" button → but only if it's a quick add; otherwise just the back link is enough for NoDeadEnds. Plain `<a href>`. Leave the list untouched.
  </action>
  <verify>
`grep -n "sat.html?id=" satellite/instance.html satellite/work-orders.html` present in both. `grep -n "work-order" satellite/instance.html` shows the make-path CTA. `grep -n "onclick=" satellite/instance.html satellite/work-orders.html` — no NEW ones.
  </verify>
  <done>
instance.html has a done→back-to-sat link + a make-path "open/create a work order" CTA; work-orders.html has a back-to-satellite link; existing features intact.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire part.html + cost.html + cost-detail.html — back/forward links + frontend audit</name>
  <files>/Users/jeet/turion-space-demo/satellite/part.html, /Users/jeet/turion-space-demo/satellite/cost.html, /Users/jeet/turion-space-demo/satellite/cost-detail.html</files>
  <action>
1. `part.html` (one part_definition — 3D viewer, make/buy panel, build-process panel, list of instances across satellites): when `?sat=` is present in the URL (it often is — links carry it per RESEARCH §A), add near the top: "View instances on this satellite ▸" (anchor to the existing instances list section, or → `instance.html` for the first instance on that sat) and "Back to the BOM tree ▸" → `bom.html?sat=${satId}`. If `?sat=` is absent, omit (the page still works as a catalog detail). Plain `<a href>`. Leave the 3D viewer / make-buy panel / build-process panel untouched.
2. `cost.html` (per-satellite cost rollup table): add a "Back to satellite ▸" link → `sat.html?id=${satId}` near the header (the page already has `?sat=`). The per-row "View →" already exists. Plain `<a href>`. Leave the table untouched.
3. `cost-detail.html` (one instance's cost sheet — template vs actual + variance, integrations panel): below the variance table (where it currently dead-ends), add: "Back to the BOM tree ▸" → `bom.html?sat=${satId}` and keep/ensure the existing "Back to the part ▸" link → `part.html?id=${partDefId}&sat=${satId}`. (Optional: "Next part in the BOM ▸" — skip if it needs BOM ordering you don't have.) Plain `<a href>`. Leave the variance table + the Phase-28 integrations side panel untouched.
4. Run the frontend button audit one final time for this plan: `cd /Users/jeet/turion-satellite/backend && node scripts/audit-satellite-buttons.mjs` → 0 violations, exit 0. (All `/api/...` paths touched here — `PATCH /api/work-orders/:woId`, `GET /api/satellites/:satId/work-orders` — are existing mounted routes.)
  </action>
  <verify>
`grep -n "bom.html?sat=" satellite/cost-detail.html satellite/part.html` present. `grep -n "sat.html?id=" satellite/cost.html` present. `grep -n "onclick=" satellite/part.html satellite/cost.html satellite/cost-detail.html` — no NEW ones. `node scripts/audit-satellite-buttons.mjs` → 0 violations, exit 0.
  </verify>
  <done>
part.html (when ?sat=), cost.html, cost-detail.html all have back/forward links — no dead end; frontend button audit 0 violations.
  </done>
</task>

</tasks>

<verification>
- work-order.html: complete-WO control (PATCH) + back-to-instance + next-WO — no dead end.
- instance.html: done→back-to-sat + make-path WO CTA.
- work-orders.html / part.html / cost.html / cost-detail.html: back/forward links — no dead end.
- All new interactivity via addEventListener (the one PATCH button) or `<a href>`; no bare fetch.
- Button audit: 0 violations.
- Phases 27-32 features on all six pages unchanged.
</verification>

<success_criteria>
`grep "satelliteApi.patch('/api/work-orders/" satellite/work-order.html` present. `grep "bom.html?sat=" satellite/cost-detail.html` present. No `onclick=` added anywhere. `node scripts/audit-satellite-buttons.mjs` → 0 violations.
</success_criteria>

<output>
After completion, create `.planning/phases/33-end-to-end-satellite-build-flow/33-05-SUMMARY.md`
</output>
