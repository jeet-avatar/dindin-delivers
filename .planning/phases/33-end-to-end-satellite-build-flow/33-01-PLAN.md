---
phase: 33-end-to-end-satellite-build-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/turion-satellite/migrations/020_add_sales_orders_and_program_seed.sql
autonomous: true
requirements: [SatelliteSpawn, SalesOrderWizard]
must_haves:
  truths:
    - "turion_satellite.sales_orders table exists with (id uuid pk, order_number text, customer_name text, program_name text, contract_value_usd numeric, status text, source_data jsonb, satellite_id uuid, created_at)"
    - "satellites table has a nullable sales_order_id uuid column referencing turion_satellite.sales_orders(id)"
    - "spawn_satellite_program(p_name, p_designation, p_sales_order_id, p_actor, p_template) plpgsql function exists, schema-qualified, in turion_satellite"
    - "Calling spawn_satellite_program(...) in one transaction creates: 1 satellites row + one part_instances row per SAT-003 instance (cloned by part_definition_id+instance_index) + one bom_lines row per SAT-003 bom_line (parent/child rewired to the new instances, root NULL preserved) + one part_stage_events row per new instance at stage 'drawing' direction 'forward' status 'entered'"
    - "Migration is idempotent: re-applying produces no errors and no duplicate rows (CREATE TABLE IF NOT EXISTS, ALTER ... ADD COLUMN IF NOT EXISTS, CREATE OR REPLACE FUNCTION; the demo spawn call is run-once)"
    - "Migration applied to production Postgres; double-apply proof shown"
  artifacts:
    - path: /Users/jeet/turion-satellite/migrations/020_add_sales_orders_and_program_seed.sql
      provides: "sales_orders table + satellites.sales_order_id column + spawn_satellite_program() function"
      contains: "spawn_satellite_program"
  key_links:
    - from: spawn_satellite_program
      to: part_instances/bom_lines/part_stage_events on the new satellite
      via: "INSERT ... SELECT from SAT-003 with an old->new instance id map joined on (part_definition_id, instance_index)"
      pattern: "part_definition_id.*instance_index"
---

<objective>
Add the database foundation for the "New satellite program" wizard: a `turion_satellite.sales_orders` table, a nullable `satellites.sales_order_id` link column, and a `spawn_satellite_program()` plpgsql function that transactionally clones SAT-003/Cygnus's full BOM structure onto a brand-new satellite and seeds stage-0 lifecycle events. Then apply the migration to production.

Purpose: Everything else in Phase 33 (the wizard page, the POST /api/satellites route) calls this function. It must exist and be live before the backend that wraps it is deployed.
Output: migration 020 file + a live, idempotent prod migration.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-RESEARCH.md

# Pattern templates to mirror (read these before writing the migration):
@/Users/jeet/turion-satellite/migrations/008_add_cross_system_fks.sql
@/Users/jeet/turion-satellite/migrations/012_densify_instances_and_bom.sql
@/Users/jeet/turion-satellite/migrations/001_create_turion_satellite_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 020 — sales_orders table + satellites.sales_order_id + spawn_satellite_program()</name>
  <files>/Users/jeet/turion-satellite/migrations/020_add_sales_orders_and_program_seed.sql</files>
  <action>
Create `migrations/020_add_sales_orders_and_program_seed.sql`. Follow the established migration idiom (study 008 + 012 + 001 first):
- Header comment block describing what the migration does.
- `SET search_path TO turion_satellite, public;` at the top.
- Sanity guard: `DO $$ BEGIN IF current_database() NOT IN ('postgres') THEN RAISE EXCEPTION 'wrong database'; END IF; END $$;` (match how prior migrations guard — if prior migrations use a different db name, match that exact pattern; if they have no guard, skip it).

1. `CREATE TABLE IF NOT EXISTS turion_satellite.sales_orders (` with columns (comment each): `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `order_number text NOT NULL UNIQUE`, `customer_name text NOT NULL`, `program_name text NOT NULL`, `contract_value_usd numeric(14,2)`, `status text NOT NULL DEFAULT 'open'` (with a CHECK on a small enum: `'open','in_build','delivered','cancelled'`), `source_data jsonb NOT NULL DEFAULT '{}'::jsonb`, `satellite_id uuid REFERENCES turion_satellite.satellites(id) ON DELETE SET NULL`, `created_at timestamptz NOT NULL DEFAULT now()`. Per locked decision 2: the SO links to the satellite directly via `satellite_id`; we do NOT touch `part_instances.sales_order_id` (that TEXT column → `turion.sales_orders` stays the legacy-sync path).

2. `ALTER TABLE turion_satellite.satellites ADD COLUMN IF NOT EXISTS sales_order_id uuid REFERENCES turion_satellite.sales_orders(id) ON DELETE SET NULL;` (the back-link, so a satellite knows its SO). Add a partial index `CREATE INDEX IF NOT EXISTS idx_satellites_sales_order ON turion_satellite.satellites(sales_order_id) WHERE sales_order_id IS NOT NULL;`.

3. `CREATE OR REPLACE FUNCTION turion_satellite.spawn_satellite_program(p_name text, p_designation text, p_sales_order_id uuid, p_actor uuid, p_template text DEFAULT 'standard-bus') RETURNS uuid LANGUAGE plpgsql AS $$` — body:
   - Resolve the template source satellite id: SAT-003/Cygnus UUID `24587565-b15b-42ce-b590-87ecf9b6bb99`. (Hardcode this as `v_src_sat constant uuid := '24587565-...'::uuid;` — it is the canonical template per the locked decision; also accept a `p_template` arg for future flexibility but only 'standard-bus' is implemented → if `p_template <> 'standard-bus'` then `RAISE EXCEPTION`.)
   - INSERT the new `satellites` row: `INSERT INTO turion_satellite.satellites (name, designation, status, sales_order_id, program_start) VALUES (p_name, p_designation, 'design', p_sales_order_id, now()::date) RETURNING id INTO v_new_sat;` (match the actual NOT NULL columns of `satellites` from migration 001 — if `program_start` or `status` have different names/types, adapt; `status` default is `'design'` per RESEARCH §B).
   - Set the SO's back-link: `UPDATE turion_satellite.sales_orders SET satellite_id = v_new_sat WHERE id = p_sales_order_id;`
   - Clone instances into a mapping table (CTE or temp table): `INSERT INTO turion_satellite.part_instances (satellite_id, part_definition_id, instance_index, serial_number, notes) SELECT v_new_sat, part_definition_id, instance_index, NULL, notes FROM turion_satellite.part_instances WHERE satellite_id = v_src_sat;` (match the real column set of `part_instances` from migration 001 — copy everything EXCEPT id/satellite_id/serial_number and the Phase-25 cross-system FK columns which stay NULL on a fresh satellite).
   - Build the old→new instance id map: `WITH m AS (SELECT old.id AS old_id, new.id AS new_id FROM turion_satellite.part_instances old JOIN turion_satellite.part_instances new ON new.satellite_id = v_new_sat AND new.part_definition_id = old.part_definition_id AND new.instance_index = old.instance_index WHERE old.satellite_id = v_src_sat)` — (`part_definition_id, instance_index` is UNIQUE within a satellite per RESEARCH §B, Pitfall 3).
   - Clone bom_lines rewired via the map: `INSERT INTO turion_satellite.bom_lines (satellite_id, parent_part_instance_id, child_part_instance_id, qty, uom, ref_designator, status) SELECT v_new_sat, pm.new_id, cm.new_id, b.qty, b.uom, b.ref_designator, b.status FROM turion_satellite.bom_lines b LEFT JOIN m pm ON pm.old_id = b.parent_part_instance_id JOIN m cm ON cm.old_id = b.child_part_instance_id WHERE b.satellite_id = v_src_sat;` — LEFT JOIN on parent so root lines (`parent_part_instance_id IS NULL`) keep NULL (Pitfall 3); INNER JOIN on child (every child must map). (Match the real `bom_lines` column set from migration 001.)
   - Seed stage-0 events: resolve the `drawing` stage id (`SELECT id INTO v_stage0 FROM turion_satellite.lifecycle_stages WHERE code = 'drawing';`), then `INSERT INTO turion_satellite.part_stage_events (satellite_id, part_instance_id, stage_id, direction, status, actor) SELECT v_new_sat, pi.id, v_stage0, 'forward', 'entered', p_actor FROM turion_satellite.part_instances pi WHERE pi.satellite_id = v_new_sat;` — `direction='forward'`, `status='entered'` per Pitfall 5 (NOT 'advance'/'revert'/'completed'). (Match the real `part_stage_events` column set + CHECK constraints from migration 001 — if `actor` is NOT NULL and p_actor could be NULL, handle it.)
   - `RETURN v_new_sat;`
   - Function is NOT idempotent by design (each call = a new satellite). The migration's table/column/function statements ARE idempotent.

4. (Optional, only if you want a demo seed row) — do NOT auto-spawn a demo program in the migration; leave the table empty. The deploy plan (33-06) does a run-once spawn for E2E verification then cleans it up.

Schema-qualify EVERYTHING (`turion_satellite.*`) — pgbouncer transaction mode strips `search_path` at runtime (the `SET search_path` only helps at migration-apply time).

IMPORTANT: before writing, `cat /Users/jeet/turion-satellite/migrations/001_create_turion_satellite_schema.sql` and verify the exact column names / NOT NULL / CHECK constraints of `satellites`, `part_instances`, `bom_lines`, `part_stage_events`, `lifecycle_stages` — adapt the INSERTs to the real schema. The column lists above are from RESEARCH §B and may be slightly off.
  </action>
  <verify>
`psql "$DB_URL" -f /Users/jeet/turion-satellite/migrations/020_add_sales_orders_and_program_seed.sql` runs clean against a scratch/local DB (or, if no local DB, `psql ... --set ON_ERROR_STOP=1` against prod in Task 2). `\d turion_satellite.sales_orders` shows the columns. `\df turion_satellite.spawn_satellite_program` shows the function.
  </verify>
  <done>
Migration file exists, parses, creates the table + column + function; column lists match migration 001's real schema.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply migration 020 to production + idempotency double-apply proof</name>
  <files>/Users/jeet/turion-satellite/migrations/020_add_sales_orders_and_program_seed.sql</files>
  <action>
Get the prod DB URL: `aws secretsmanager get-secret-value --secret-id arn:aws:secretsmanager:us-east-1:134607809447:secret:turion-satellite/production/database-url-NCbgX6 --query SecretString --output text` — strip any `?schema=...` query suffix before passing to `psql` (per prior phases; pgbouncer/psql doesn't accept it).

1. Apply: `psql "$DB_URL" --set ON_ERROR_STOP=1 -f migrations/020_add_sales_orders_and_program_seed.sql` — capture output.
2. Verify objects exist:
   - `psql "$DB_URL" -c "\d turion_satellite.sales_orders"`
   - `psql "$DB_URL" -c "\df turion_satellite.spawn_satellite_program"`
   - `psql "$DB_URL" -c "SELECT column_name FROM information_schema.columns WHERE table_schema='turion_satellite' AND table_name='satellites' AND column_name='sales_order_id';"` → 1 row.
3. Double-apply proof: run the same `psql -f migrations/020_...` AGAIN — it must succeed with no errors (CREATE TABLE IF NOT EXISTS = no-op, ADD COLUMN IF NOT EXISTS = no-op, CREATE OR REPLACE FUNCTION = no-op). Capture the second-run output (should be all NOTICEs / "already exists, skipping" — zero ERRORs, zero rows inserted).
4. Do NOT call `spawn_satellite_program` here — the smoke spawn happens in the deploy plan (33-06) so it can be cleaned up alongside the E2E verification.

Record the apply output + double-apply output in the plan SUMMARY.
  </action>
  <verify>
First apply: no ERROR lines. `\d turion_satellite.sales_orders` lists the 9 columns. `\df spawn_satellite_program` shows the function with 5 args returning uuid. Second apply: no ERROR lines, only "already exists, skipping" NOTICEs.
  </verify>
  <done>
Production Postgres has `turion_satellite.sales_orders`, `satellites.sales_order_id`, and `spawn_satellite_program()`; re-applying the migration is a clean no-op.
  </done>
</task>

</tasks>

<verification>
- Migration file follows the established idiom (search_path, idempotent statements, commented columns), schema-qualified everywhere.
- Prod DB has the table, the column, the function.
- Double-apply is a clean no-op.
- Nothing in Phases 25-32 regressed (we only ADDED a table + a nullable column + a function — no ALTER of existing columns, no data changes).
</verification>

<success_criteria>
`SELECT 1 FROM information_schema.tables WHERE table_schema='turion_satellite' AND table_name='sales_orders'` returns a row on prod. `\df turion_satellite.spawn_satellite_program` shows the function. Migration re-apply: 0 errors.
</success_criteria>

<output>
After completion, create `.planning/phases/33-end-to-end-satellite-build-flow/33-01-SUMMARY.md`
</output>
