---
phase: 33-end-to-end-satellite-build-flow
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - /Users/jeet/turion-satellite/backend/src/routes/sales-orders.ts
  - /Users/jeet/turion-satellite/backend/src/routes/satellites.ts
  - /Users/jeet/turion-satellite/backend/src/app.ts
  - /Users/jeet/turion-satellite/backend/src/routes/satellites.test.ts
  - /Users/jeet/turion-satellite/backend/src/routes/sales-orders.test.ts
autonomous: true
requirements: [SalesOrderWizard, SatelliteSpawn]
must_haves:
  truths:
    - "POST /api/sales-orders creates a turion_satellite.sales_orders row and returns it (201) with its id; requires auth (401 without a JWT)"
    - "GET /api/sales-orders lists sales orders; GET /api/sales-orders/:id returns one (404 on unknown id)"
    - "POST /api/satellites accepts {name, designation, sales_order_id, template?} → calls spawn_satellite_program(...) inside a BEGIN/COMMIT, writes an audit_log entry, returns 201 with the new satellite id + counts (parts, bom_lines, stage_events); requires auth; 409 on duplicate designation"
    - "PATCH /api/satellites/:id accepts {status} (one of the satellites.status enum) and advances the satellite's status; requires auth; 404 on unknown id; rejects an invalid status value"
    - "app.ts mounts the new sales-orders router at /api/sales-orders; the new routes on satellites.ts are reachable"
    - "audit-satellite-buttons.mjs run from the backend repo reports 0 violations (the new /api/sales-orders + /api/satellites POST + /api/satellites/:id PATCH paths are now in the allowlist derived from app.ts)"
    - "Existing satellite backend tests still pass; new tests cover the new routes"
  artifacts:
    - path: /Users/jeet/turion-satellite/backend/src/routes/sales-orders.ts
      provides: "POST/GET /api/sales-orders router"
      exports: ["default"]
    - path: /Users/jeet/turion-satellite/backend/src/routes/satellites.ts
      provides: "POST / and PATCH /:id added to the satellites router"
      contains: "spawn_satellite_program"
    - path: /Users/jeet/turion-satellite/backend/src/app.ts
      provides: "mount for the sales-orders router"
      contains: "sales-orders"
  key_links:
    - from: POST /api/satellites
      to: turion_satellite.spawn_satellite_program
      via: "client.query('SELECT turion_satellite.spawn_satellite_program($1,$2,$3,$4,$5)', ...)"
      pattern: "spawn_satellite_program"
    - from: app.ts
      to: routes/sales-orders.ts
      via: "app.use('/api/sales-orders', salesOrdersRouter)"
      pattern: "/api/sales-orders"
---

<objective>
Add the backend routes the wizard calls: `POST /api/sales-orders` (+ GET list/detail), `POST /api/satellites` (wraps `spawn_satellite_program`), `PATCH /api/satellites/:id` (advance satellite status), mount the new router in `app.ts`, and keep the Phase-29 button audit at 0 violations. (Lambda redeploy is deferred to the deploy plan 33-06.)

Purpose: The wizard page (33-03) and the "advance program status" affordance (33-04/05) call these. The audit allowlist is derived from `app.ts` — so these routes MUST be mounted before the frontend that calls them ships.
Output: new router + 2 new routes on satellites.ts + app.ts mount + tests + clean audit.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-RESEARCH.md
@.planning/phases/33-end-to-end-satellite-build-flow/33-01-SUMMARY.md

# Patterns to mirror (read before writing):
@/Users/jeet/turion-satellite/backend/src/app.ts
@/Users/jeet/turion-satellite/backend/src/routes/satellites.ts
@/Users/jeet/turion-satellite/backend/src/routes/integration.ts
@/Users/jeet/turion-satellite/backend/src/routes/work-orders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create routes/sales-orders.ts + mount in app.ts</name>
  <files>/Users/jeet/turion-satellite/backend/src/routes/sales-orders.ts, /Users/jeet/turion-satellite/backend/src/app.ts</files>
  <action>
Create `backend/src/routes/sales-orders.ts` — an Express router, matching the hardened pattern used by `work-orders.ts` / `integration.ts` (`requireAuth` middleware on every route; `try/catch` with `console.error(...)` + a generic `res.status(500).json({error:'...'})` — NEVER leak `err.message`; schema-qualify all SQL with `turion_satellite.`):
- `POST /` — body `{order_number?, customer_name, program_name, contract_value_usd?, source_data?}`. Validate `customer_name` and `program_name` non-empty (return 400 with `{error:'...'}` otherwise). If `order_number` not supplied, compute the next free one server-side: `SELECT 'SO-' || LPAD((COALESCE(MAX(NULLIF(regexp_replace(order_number,'\D','','g'),''))::int,0)+1)::text, 4, '0') FROM turion_satellite.sales_orders` (or simpler: `'SO-' || to_char(now(),'YYYYMMDDHH24MISS')` — pick the cleaner one; the point is no UNIQUE collision). `INSERT INTO turion_satellite.sales_orders (order_number, customer_name, program_name, contract_value_usd, source_data, status) VALUES ($1,$2,$3,$4,$5,'open') RETURNING *` → `res.status(201).json(row)`. On a UNIQUE violation (Postgres error code `23505`) return `res.status(409).json({error:'order_number already exists'})`.
- `GET /` — `SELECT * FROM turion_satellite.sales_orders ORDER BY created_at DESC` → `res.json(rows)`.
- `GET /:id` — `SELECT * FROM turion_satellite.sales_orders WHERE id = $1` → 200 with the row, or 404 `{error:'not found'}`. Validate `:id` is a uuid first (regex or a try/catch on the cast) → 400 on a malformed id.
- `export default router;`

In `app.ts`: import the new router and `app.use('/api/sales-orders', salesOrdersRouter);` — place the mount alongside the other top-level `app.use('/api/...', ...)` calls (e.g. near `/api/work-orders`). Match the exact import style (relative path, `.ts`/no-ext) used by the other route imports in `app.ts`.

Verify route registration: `cd /Users/jeet/turion-satellite/backend && npm run build` (or `npx tsc --noEmit`) compiles; if there's a route-list/health introspection, check `/api/sales-orders` appears.
  </action>
  <verify>
`cd /Users/jeet/turion-satellite/backend && npx tsc --noEmit` → 0 errors. `grep -n "sales-orders" src/app.ts` shows the import + mount. `grep -n "router\.\(post\|get\)" src/routes/sales-orders.ts` shows POST / + GET / + GET /:id, all with `requireAuth`.
  </verify>
  <done>
sales-orders router exists with POST/GET/GET-by-id (auth-gated, hardened catch, schema-qualified), mounted at /api/sales-orders in app.ts, compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add POST / and PATCH /:id to routes/satellites.ts</name>
  <files>/Users/jeet/turion-satellite/backend/src/routes/satellites.ts</files>
  <action>
Add two routes to `satellites.ts` (it currently has only `GET /` and `GET /:id`):

1. `POST /` — `requireAuth`. Body `{name, designation, sales_order_id, template?}`. Validate `name` non-empty, `designation` matches `/^SAT-\d{3,}$/` (or accept any non-empty string — match whatever the existing seed designations look like), `sales_order_id` is a uuid (and exists — a `SELECT 1 FROM turion_satellite.sales_orders WHERE id=$1` check, 400 if missing). Then, mirroring `integration.ts`'s transaction pattern: `const client = await pool.connect(); try { await client.query('BEGIN'); const r = await client.query('SELECT turion_satellite.spawn_satellite_program($1,$2,$3,$4,$5) AS sat_id', [name, designation, sales_order_id, req.user?.id ?? null, template ?? 'standard-bus']); const satId = r.rows[0].sat_id; ` then count what was created: `const c = await client.query("SELECT (SELECT count(*) FROM turion_satellite.part_instances WHERE satellite_id=$1) AS parts, (SELECT count(*) FROM turion_satellite.bom_lines WHERE satellite_id=$1) AS bom_lines, (SELECT count(*) FROM turion_satellite.part_stage_events WHERE satellite_id=$1) AS stage_events", [satId]);` then an audit_log insert (match how `integration.ts` writes its `audit_log` row — same columns; action e.g. `'spawn_satellite_program'`, entity the new satellite id, a small JSON detail with the counts + sales_order_id), then `await client.query('COMMIT'); res.status(201).json({ id: satId, ...c.rows[0] });` — `catch`: `await client.query('ROLLBACK')`; if the error is a UNIQUE violation on `designation` (code `23505`) → `res.status(409).json({error:'designation already exists'})`; else `console.error('spawn failed', err); res.status(500).json({error:'Failed to create satellite program'})`. `finally`: `client.release()`. (Per locked decision: the whole spawn is one transaction; if it fails, roll back — never half-create.)

2. `PATCH /:id` — `requireAuth`. Validate `:id` uuid → 400. Body `{status}`. Validate `status` is one of the `satellites.status` enum values (from migration 001 — RESEARCH §B lists `design,build,test,ship,launch,orbit`; read migration 001 to confirm the exact set) → 400 on an invalid value. `UPDATE turion_satellite.satellites SET status=$1 WHERE id=$2 RETURNING *` → 200 with the row, or 404 `{error:'not found'}` if no row updated. Optionally also write an `audit_log` entry (`'advance_satellite_status'`). Hardened catch.

Keep the existing `GET /` and `GET /:id` untouched.
  </action>
  <verify>
`npx tsc --noEmit` → 0 errors. `grep -n "router\.\(post\|patch\)" src/routes/satellites.ts` shows `POST /` + `PATCH /:id` with `requireAuth`. `grep -n "spawn_satellite_program" src/routes/satellites.ts` → present.
  </verify>
  <done>
satellites.ts has POST / (transactional spawn wrapper, audit_log, 409 on dup designation) + PATCH /:id (status advance, enum-validated), both auth-gated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests for the new routes + run the button audit (0 violations)</name>
  <files>/Users/jeet/turion-satellite/backend/src/routes/sales-orders.test.ts, /Users/jeet/turion-satellite/backend/src/routes/satellites.test.ts</files>
  <action>
1. Create `sales-orders.test.ts` mirroring an existing route test (e.g. `work-orders.test.ts` or `satellites.test.ts` — copy its setup: the mocked `pool`/`query`, the supertest app, the auth-header helper). Cases: (a) `POST /api/sales-orders` without auth → 401; (b) with auth + valid body → 201, response has an `id`; (c) with auth + missing `customer_name` → 400; (d) `GET /api/sales-orders` with auth → 200 array; (e) `GET /api/sales-orders/:badId` → 400; (f) `GET /api/sales-orders/<uuid>` when query returns no rows → 404.
2. Extend `satellites.test.ts` (or add cases): (a) `POST /api/satellites` without auth → 401; (b) with auth + valid body → 201, response has `id` + `parts`/`bom_lines`/`stage_events` counts (mock the `SELECT spawn_satellite_program(...)` to return a fake uuid + the count query to return numbers); (c) `POST /api/satellites` when the mocked query throws a `{code:'23505'}` → 409; (d) `PATCH /api/satellites/:id` with an invalid `status` → 400; (e) `PATCH /api/satellites/<uuid>` with valid `{status:'build'}` + a returned row → 200; (f) `PATCH` when no row updated → 404.
3. Run: `cd /Users/jeet/turion-satellite/backend && npm test` — all green (new + existing).
4. Run the button audit from the backend repo: `cd /Users/jeet/turion-satellite/backend && node scripts/audit-satellite-buttons.mjs` (or `npm run audit-buttons` — check package.json for the exact script name). It must report 0 violations and exit 0 — the allowlist is derived from `app.ts`'s mount tree, so the new `/api/sales-orders`, `POST /api/satellites`, `PATCH /api/satellites/:id` paths are now allowlisted. (If it scans the frontend repo's HTML and that hasn't shipped the wizard yet, the audit just won't see new calls — still 0 violations. The frontend audit re-runs in 33-06 after the wizard lands.)
  </action>
  <verify>
`npm test` → all suites pass, new cases included. `node scripts/audit-satellite-buttons.mjs` → "0 violations", exit 0.
  </verify>
  <done>
New routes have test coverage; full backend test suite green; button audit 0 violations.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean; `npm test` green.
- New routes auth-gated, hardened catch (no err.message leak), schema-qualified SQL.
- `app.ts` mounts `/api/sales-orders`.
- `POST /api/satellites` is a single BEGIN/COMMIT transaction calling `spawn_satellite_program` + an audit_log row.
- Button audit: 0 violations.
- Existing satellite backend behaviour unchanged (GET routes untouched; Phases 25-32 routes untouched).
</verification>

<success_criteria>
`grep -rn "sales-orders" backend/src/app.ts` shows the mount. `grep -n "spawn_satellite_program" backend/src/routes/satellites.ts` present. `npm test` green. `node scripts/audit-satellite-buttons.mjs` → 0 violations.
</success_criteria>

<output>
After completion, create `.planning/phases/33-end-to-end-satellite-build-flow/33-02-SUMMARY.md`
</output>
