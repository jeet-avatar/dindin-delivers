---
phase: 00-api-standardization
task: 2
total_tasks: 4
status: in_progress
last_updated: 2026-02-19T08:48:43.421Z
---

<current_state>
9 mobile endpoint fixes made manually by user across 3 files (5 Android, 4 iOS). All verified correct against backend source. None committed yet. Tier 2 test file updates identified but not started. 17 of 26 red marks remain unaddressed.

GSD project NOT yet initialized — no PROJECT.md, ROADMAP.md, or REQUIREMENTS.md exist. The `.planning/` directory only has codebase maps and quick tasks from earlier sessions. Next session should run `/gsd:new-milestone` to create a proper roadmap for the remaining cross-platform standardization work.
</current_state>

<completed_work>

- Task 1: Full 6-way API audit — DONE (6 parallel agents audited all endpoints from iOS Customer, iOS Driver, iOS Restaurant, Android Customer, Android Driver, Android Partner)
- Task 2: 9 endpoint fixes — DONE (user made manually, all backend-verified)
  - Android DollorApiService.kt: createOrder, getCustomerRides, cancelRide, rateRide
  - Android CustomerRideshareApiService.kt: deleteRecurringRide
  - iOS P2PAPIService.swift: addFavorite, setDefaultCard, cancelRide, rideChatMessages
- Task 2b: Delivery flow fixes — DONE + COMMITTED + PUSHED + FIREBASE DISTRIBUTED (commit 9c42c21d)
  - 5 files: ApiModels.kt, DollorApiService.kt, DollorRepository.kt, ActiveDeliveryViewModel.kt, AvailableOrdersScreen.kt
</completed_work>

<remaining_work>

- Task 2 (finish): Commit the 9 endpoint fixes (both repos), build, test
- Task 3: Update Tier 2 files (6 files still reference /api/chat/messages/{id} instead of /p2p/ride-requests/{id}/chat)
- Task 4: Address remaining 17 red marks across 3 app pairs
  - Customer: 6 remaining (Apple auth path, FCM token, delete account method, surge pricing, dispute by ID)
  - Driver: 8 remaining (global location, online status, active deliveries, driver bids, ride interval, update bid, negotiate body, complete ride simple)
  - Restaurant: 6 remaining (order status, accept order body, online toggle, availability toggle, self-delivery, delivery proof, FCM)
- Future: Backend alias cleanup (Step 4 — only AFTER mobile apps shipped and users updated)
</remaining_work>

<decisions_made>

- Decided to fix BOTH platforms toward canonical backend endpoints (not just align Android to iOS or vice versa)
- Backend aliases kept for backward compat — will delete only after new mobile apps are shipped and old traffic drops
- `/api/erp/rides/{id}/rate` MUST be kept — web frontend (RideBooking.tsx, OrderTracking.tsx) still calls it
- `/api/rides/recurring/{id}` was never a backend alias — it was a pure client bug (404), no backend cleanup needed
- DO NOT touch `/api/chat/messages/{order_id}` — that's ORDER chat at main_new.py:16574, separate from ride chat
</decisions_made>

<blockers>
- None blocking. All 9 fixes are ready to commit.
</blockers>

<context>
User prefers strict anti-hallucination approach: always read files before editing, verify against backend code, state assumptions explicitly, never invent APIs. User made all 9 fixes manually this session — they know the codebase well.

The 4-step ship plan was user-approved:
1. Commit + build + test locally
2. Ship new app versions (Play Store + TestFlight)
3. Wait for user adoption (monitor alias traffic)
4. Backend alias cleanup (separate deploy)

GSD should be initialized next session to create a proper roadmap for the remaining 17 red marks + backend cleanup + other pending items (platform_fees_paid bug, serialize_bid vehicle fields, order timeouts).
</context>

<next_action>
Start with: `/gsd:new-milestone "Cross-Platform API Standardization"` to create a proper roadmap. Then commit the 9 endpoint fixes, build both platforms, and test against production. The next-session.md at `/Users/jeet/.claude/projects/-Users-jeet-doordash-p2p/memory/next-session.md` has the full detailed handoff.
</next_action>
