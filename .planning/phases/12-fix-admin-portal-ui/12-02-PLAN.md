---
phase: 12-fix-admin-portal-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/p2p-platform/frontend/src/app/screens/dashboard/Main.tsx
  - apps/web/p2p-platform/frontend/src/app/screens/dashboard/Dashboard.tsx
  - apps/web/p2p-platform/frontend/src/app/screens/dashboard/tabs/Jira.tsx
  - apps/web/p2p-platform/frontend/src/app/screens/dashboard/tabs/Netsuite.tsx
  - apps/web/p2p-platform/frontend/src/app/screens/dashboard/tabs/Zip.tsx
  - apps/web/p2p-platform/frontend/src/app/screens/dashboard/tabs/ProcessUtility.tsx
  - apps/web/p2p-platform/frontend/src/app/screens/dashboard/tabs/NetSuiteWolt.tsx
  - apps/web/p2p-platform/frontend/src/app/components/layout/MainLayout.tsx
  - apps/web/p2p-platform/frontend/src/App.tsx
  - apps/web/p2p-platform/frontend/src/app/constants/Apis.tsx
  - apps/web/p2p-platform/frontend/src/app/constants/mockData.ts
  - apps/web/p2p-platform/frontend/src/app/constants/mockNetSuiteTransactions.ts
  - apps/web/p2p-platform/frontend/src/app/screens/jiraDashboard/Main.tsx
  - apps/web/p2p-platform/frontend/src/app/screens/netsuiteDashboard/Main.tsx
  - apps/web/p2p-platform/frontend/src/app/screens/systemDashboard/Main.tsx
  - apps/web/p2p-platform/frontend/src/app/screens/transactions/Main.tsx
  - apps/web/p2p-platform/frontend/src/app/constants/Bridge.tsx
autonomous: true
requirements: [ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-05]

must_haves:
  truths:
    - "Main dashboard shows real Dollor.ai stats (orders, revenue, customers, drivers) from /api/dashboard/stats"
    - "No ERP tabs (Jira, NetSuite, ZIP, ProcessUtility, NetSuiteWolt) appear in the dashboard"
    - "Sidebar has no links to mock screens (Jira Dashboard, NetSuite Dashboard, Transactions)"
    - "No mock data files exist in constants directory"
    - "All admin routes in App.tsx point to real, working screens"
  artifacts:
    - path: "apps/web/p2p-platform/frontend/src/app/screens/dashboard/Main.tsx"
      provides: "Real Dollor.ai dashboard with live stats"
      contains: "/dashboard/stats"
    - path: "apps/web/p2p-platform/frontend/src/app/components/layout/MainLayout.tsx"
      provides: "Clean sidebar without mock ERP links"
    - path: "apps/web/p2p-platform/frontend/src/App.tsx"
      provides: "Routes only to real screens"
  key_links:
    - from: "apps/web/p2p-platform/frontend/src/app/screens/dashboard/Main.tsx"
      to: "/api/dashboard/stats"
      via: "api axios instance"
      pattern: "api\\.get.*dashboard"
    - from: "apps/web/p2p-platform/frontend/src/app/components/layout/MainLayout.tsx"
      to: "apps/web/p2p-platform/frontend/src/App.tsx"
      via: "sidebar nav items match route definitions"
      pattern: "to=.*admin"
---

<objective>
Remove all mock ERP dashboards, rewire main dashboard to real Dollor.ai stats, and clean sidebar navigation.

Purpose: The admin portal currently shows fake ERP integration screens (Jira, NetSuite, ZIP) that call nonexistent backend endpoints and display mock data. This makes the portal look broken and unprofessional. Replacing with real operational data makes the portal production-ready.
Output: Clean admin portal with real dashboard stats, no mock screens, and a focused sidebar.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fix-admin-portal-ui/12-RESEARCH.md
@apps/web/p2p-platform/frontend/src/App.tsx
@apps/web/p2p-platform/frontend/src/app/components/layout/MainLayout.tsx
@apps/web/p2p-platform/frontend/src/app/screens/dashboard/Main.tsx
@apps/web/p2p-platform/frontend/src/app/screens/dashboard/Dashboard.tsx
@apps/web/p2p-platform/frontend/src/app/constants/Apis.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire main dashboard to real stats and remove mock ERP tabs</name>
  <files>
    apps/web/p2p-platform/frontend/src/app/screens/dashboard/Main.tsx,
    apps/web/p2p-platform/frontend/src/app/screens/dashboard/Dashboard.tsx,
    apps/web/p2p-platform/frontend/src/app/screens/dashboard/tabs/ConsolidatedView.tsx
  </files>
  <action>
**Dashboard Main.tsx / Dashboard.tsx:**
1. Remove all tab references to Jira, NetSuite, ZIP, ProcessUtility, NetSuiteWolt. Keep only the ConsolidatedView tab if it works, or replace the entire tabbed layout with a single dashboard view.
2. The main dashboard should fetch from `GET /api/dashboard/stats` (confirmed to exist in backend) using the `api` axios instance. This returns: `total_orders`, `total_revenue`, `total_customers`, `total_drivers`, etc.
3. Also fetch from `GET /api/dashboard/recent-activity` for an activity feed.
4. Display key metrics as stat cards (using Ant Design Card or existing Tailwind components): Total Orders, Total Revenue, Active Drivers, Active Customers, Active Vendors.
5. Display recent activity as a simple list/timeline below the stat cards.
6. Use chart.js/react-chartjs-2 (already installed) for any charts if the data supports it (e.g., orders over time).
7. Remove imports of deleted tab components.

**ConsolidatedView.tsx:**
If this tab already calls `/api/dashboard/consolidated` and works, keep it and make it the default/only view. If it also references mock endpoints, fix it to use real ones.

**Delete mock tab files (or gut them):**
- `tabs/Jira.tsx` -- delete file
- `tabs/Netsuite.tsx` -- delete file
- `tabs/Zip.tsx` -- delete file
- `tabs/ProcessUtility.tsx` -- delete file
- `tabs/NetSuiteWolt.tsx` -- delete file

If deleting files causes import errors elsewhere, replace file contents with a simple "This feature has been removed" placeholder component instead.
  </action>
  <verify>
`cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/frontend && npx tsc --noEmit` compiles clean. `grep -rn 'jira-tab\|netsuite-tab\|zip-tab\|ProcessUtility\|NetSuiteWolt' src/app/screens/dashboard/` returns 0 results for nonexistent endpoint calls. Dashboard Main.tsx contains `api.get` call to `/dashboard/stats`.
  </verify>
  <done>Main dashboard fetches real stats from /api/dashboard/stats, mock ERP tabs removed, TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Clean sidebar, routes, and remove mock screen files and data</name>
  <files>
    apps/web/p2p-platform/frontend/src/app/components/layout/MainLayout.tsx,
    apps/web/p2p-platform/frontend/src/App.tsx,
    apps/web/p2p-platform/frontend/src/app/constants/Apis.tsx,
    apps/web/p2p-platform/frontend/src/app/constants/mockData.ts,
    apps/web/p2p-platform/frontend/src/app/constants/mockNetSuiteTransactions.ts,
    apps/web/p2p-platform/frontend/src/app/constants/Bridge.tsx,
    apps/web/p2p-platform/frontend/src/app/screens/jiraDashboard/Main.tsx,
    apps/web/p2p-platform/frontend/src/app/screens/netsuiteDashboard/Main.tsx,
    apps/web/p2p-platform/frontend/src/app/screens/systemDashboard/Main.tsx,
    apps/web/p2p-platform/frontend/src/app/screens/transactions/Main.tsx
  </files>
  <action>
**MainLayout.tsx (sidebar navigation):**
1. Remove the entire "ERP" section from sidebar nav (Coupa, NetSuite, JIRA, Transactions links).
2. Keep all working sections: Dashboard, Food Delivery (Orders, Restaurants, Menu Review), Rideshare (Ride Requests, Active Rides), Partners (Restaurants, Drivers, Document Review), Finance (Platform Revenue, Financial Reports, Settlement), Customers, Project Tracker, Change Management, Invoices.
3. If "Onboarding (ZIP)" in Partners section links to the ZipDashboard which is confirmed WORKING per research, keep it.

**App.tsx (routes):**
1. Remove routes for: `/admin/jira-dashboard`, `/admin/netsuite-dashboard`, `/admin/transactions`, `/admin/system-dashboard`.
2. Keep route for `/admin/coupa-dashboard` if Coupa endpoints exist in backend (they do per research -- lines 8104-8474 in main_new.py). But remove it from sidebar since it's not production-ready.
3. Keep all other working routes.
4. Remove imports for deleted screen components.

**Delete mock data files:**
1. Delete `src/app/constants/mockData.ts` entirely.
2. Delete `src/app/constants/mockNetSuiteTransactions.ts` entirely.
3. If other files import from these, remove those imports and any code that uses the mock data.

**Clean up Apis.tsx:**
1. Remove URL constants for nonexistent endpoints (`/api/dashboard/jira-tab`, `/api/dashboard/netsuite-tab`, `/api/dashboard/zip-tab`, `/api/transactions/coupa`, `/api/transactions/netsuite`, etc.).
2. Keep URL constants for real endpoints.

**Clean up Bridge.tsx:**
1. Remove function calls to nonexistent dashboard endpoints.
2. Keep any functions that call real endpoints.

**Delete or gut mock screen files:**
1. `jiraDashboard/Main.tsx` -- delete the file or replace with redirect to dashboard.
2. `netsuiteDashboard/Main.tsx` -- delete or replace.
3. `systemDashboard/Main.tsx` -- delete or replace.
4. `transactions/Main.tsx` -- delete or replace.

If deleting directories/files causes import issues, replace with minimal placeholder that redirects to `/admin`.
  </action>
  <verify>
`cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/frontend && npx tsc --noEmit` compiles clean. Verify sidebar cleanup: `grep -n 'jira\|netsuite\|NetSuite\|JIRA' src/app/components/layout/MainLayout.tsx` returns 0 for removed items (case-insensitive check for ERP refs). Verify mock files gone: `ls src/app/constants/mockData.ts src/app/constants/mockNetSuiteTransactions.ts 2>&1` shows "No such file". Verify routes clean: `grep -n 'jira-dashboard\|netsuite-dashboard\|system-dashboard' src/App.tsx` returns 0 results.
  </verify>
  <done>Sidebar has no ERP links, mock screen routes removed from App.tsx, mock data files deleted, Apis.tsx cleaned, TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/frontend && npx tsc --noEmit` -- zero TypeScript errors
2. `grep -rn 'mockData\|mockNetSuiteTransactions' src/` -- zero results (mock imports gone)
3. `grep -rn 'jira-dashboard\|netsuite-dashboard\|system-dashboard' src/App.tsx` -- zero results
4. `grep -in 'jira\|netsuite' src/app/components/layout/MainLayout.tsx` -- zero results for ERP sidebar items
5. Dashboard Main.tsx contains `api.get('/dashboard/stats')` or equivalent real endpoint call
6. `npm run build` completes without errors (full production build check)
</verification>

<success_criteria>
Main dashboard shows real Dollor.ai operational data from backend endpoints. No mock ERP screens accessible via sidebar or routes. No mock data files in codebase. TypeScript and production build both pass clean.
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-admin-portal-ui/12-02-SUMMARY.md`
</output>
