---
phase: 12-fix-admin-portal-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/Main.tsx
  - apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/DocumentReview.tsx
  - apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/MenuReview.tsx
autonomous: true
requirements: [ADMIN-01, ADMIN-05, ADMIN-06]

must_haves:
  truths:
    - "Vendor management screen loads real vendor data from /api/vendors without 401 errors"
    - "Document review screen loads vendor documents without 401 errors"
    - "Menu review screen loads vendor menus without 401 errors"
    - "Admin can publish/unpublish vendors without auth failures"
    - "No mock vendor data (Tech Solutions Inc, Global Supplies Co) appears anywhere"
  artifacts:
    - path: "apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/Main.tsx"
      provides: "Vendor management with real API data and auth"
      contains: "import api from"
    - path: "apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/DocumentReview.tsx"
      provides: "Document review with auth headers"
      contains: "import api from"
    - path: "apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/MenuReview.tsx"
      provides: "Menu review with auth headers"
      contains: "import api from"
  key_links:
    - from: "apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/Main.tsx"
      to: "/api/vendors"
      via: "api axios instance with auth interceptor"
      pattern: "api\\.get\\("
    - from: "apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/DocumentReview.tsx"
      to: "/api/admin/vendors"
      via: "api axios instance with auth interceptor"
      pattern: "api\\.(get|post|put)\\("
    - from: "apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/MenuReview.tsx"
      to: "/api/vendors"
      via: "api axios instance with auth interceptor"
      pattern: "api\\.(get|post|put)\\("
---

<objective>
Fix all 3 vendor management screens to use the `api` axios instance instead of raw `fetch()`, removing auth failures and mock data.

Purpose: Vendor management is the highest-priority admin fix -- these screens return 401 in production because raw `fetch()` bypasses the auth interceptor. Admin cannot manage restaurants until this is fixed.
Output: Three working vendor management screens (Main, DocumentReview, MenuReview) with real API data and proper auth headers.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fix-admin-portal-ui/12-RESEARCH.md
@apps/web/p2p-platform/frontend/src/app/api/api.ts
@apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/Main.tsx
@apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/DocumentReview.tsx
@apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/MenuReview.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix VendorManagement/Main.tsx -- replace raw fetch with api instance, remove mock data</name>
  <files>apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/Main.tsx</files>
  <action>
Replace all raw `fetch()` calls with the `api` axios instance from `../../api/api`:

1. **Replace fetchVendors (line ~123):** Change `fetch(\`${getApiUrl()}/api/vendors\`)` to use `getVendors()` from api.ts or `api.get('/vendors')`. The response data comes from `response.data` (axios), not `response.json()` (fetch).

2. **Replace publish action (line ~477-478):** Change `localStorage.getItem('auth_token')` + raw `fetch()` POST to `api.post(\`/admin/vendors/${vendor.id}/publish\`, { platforms: ['ios', 'android', 'web'] })`. The `auth_token` key is WRONG -- admin login stores as `token`. Using `api` instance avoids this entirely.

3. **Replace checklist/unpublish actions (line ~511-512):** Same pattern -- replace any raw `fetch()` with `api.post()` or `api.put()` calls.

4. **Remove `_mockVendors` state (lines ~172-318):** Delete the entire mock data block containing fake companies like "Tech Solutions Inc.", "Global Supplies Co.", "Innovation Labs", etc. These are never used since `vendors` state from `fetchVendors()` provides real data.

5. **Fix Vendor interface mapping (lines ~127-163):** The current mapping creates fields like `risk_rating`, `performance_score`, `contract_status` that do not exist in the backend Vendor model. Align with actual backend fields: `onboarding_status`, `onboarding_phase`, `is_published`, `restaurant_name`, `email`, `phone`. Remove fields that have no backend source or map them to sensible defaults.

6. **Remove unused imports:** After removing fetch/mock data, clean up any imports that are no longer needed (e.g., `getApiUrl` if only used for fetch).

Do NOT change the visual layout or Ant Design components -- only fix data fetching and remove mock data.
  </action>
  <verify>
Run `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/frontend && npx tsc --noEmit` to verify TypeScript compiles without errors. Then grep to confirm no raw `fetch(` calls remain: `grep -n 'fetch(' src/app/screens/vendorManagement/Main.tsx` should return 0 results. Grep to confirm no mock data: `grep -n '_mockVendors\|Tech Solutions Inc\|Global Supplies Co' src/app/screens/vendorManagement/Main.tsx` should return 0 results.
  </verify>
  <done>VendorManagement/Main.tsx uses api axios instance for all API calls, no raw fetch(), no mock data, TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix DocumentReview.tsx and MenuReview.tsx -- replace raw fetch with api instance</name>
  <files>apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/DocumentReview.tsx, apps/web/p2p-platform/frontend/src/app/screens/vendorManagement/MenuReview.tsx</files>
  <action>
**DocumentReview.tsx (lines ~100, 158, 196):**
1. Replace all raw `fetch()` calls with `api` axios instance. Likely calls: fetching vendor documents list, approving/rejecting a document, fetching document details.
2. Import `api` from `../../api/api`.
3. Change `response.json()` patterns to `response.data` (axios convention).
4. Remove any `getApiUrl()` usage if only used for fetch calls.
5. Check if it uses `/api/admin/vendors/all-documents` endpoint (confirmed to exist in backend per research). If it calls a different endpoint, verify against backend first with grep.

**MenuReview.tsx (lines ~97, 106, 204, 231, 253):**
1. Replace all 5 raw `fetch()` calls with `api` axios instance. Likely calls: fetching vendor menus, approving menu items, updating menu status, fetching menu details, rejecting items.
2. Import `api` from `../../api/api`.
3. Change `response.json()` patterns to `response.data`.
4. Remove any `getApiUrl()` or manual `Authorization` header construction.
5. For any POST/PUT calls that manually set `Content-Type: application/json`, remove the header -- axios sets it automatically.

For BOTH files: Do NOT change visual layout, Ant Design table columns, or UI behavior. Only fix the data fetching layer.
  </action>
  <verify>
Run `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/frontend && npx tsc --noEmit` to verify TypeScript compiles. Then grep: `grep -rn 'fetch(' src/app/screens/vendorManagement/DocumentReview.tsx src/app/screens/vendorManagement/MenuReview.tsx` should return 0 results. Confirm api import: `grep -n "import api" src/app/screens/vendorManagement/DocumentReview.tsx src/app/screens/vendorManagement/MenuReview.tsx` should show imports in both files.
  </verify>
  <done>DocumentReview.tsx and MenuReview.tsx use api axios instance for all API calls, no raw fetch(), TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/frontend && npx tsc --noEmit` -- zero TypeScript errors
2. `grep -rn 'fetch(' src/app/screens/vendorManagement/` -- zero results (no raw fetch remaining)
3. `grep -rn '_mockVendors\|Tech Solutions Inc\|Global Supplies Co' src/app/screens/vendorManagement/` -- zero results (no mock data)
4. `grep -rn "import api\|from '../../api/api'" src/app/screens/vendorManagement/` -- all 3 files import api
5. `grep -rn "auth_token" src/app/screens/vendorManagement/` -- zero results (wrong token key removed)
</verification>

<success_criteria>
All 3 vendor management screens compile without TypeScript errors, use the api axios instance for every API call, contain no raw fetch() calls, no mock vendor data, and no references to the wrong localStorage key 'auth_token'.
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-admin-portal-ui/12-01-SUMMARY.md`
</output>
