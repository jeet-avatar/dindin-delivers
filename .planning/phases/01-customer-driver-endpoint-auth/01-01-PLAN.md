---
phase: 01-customer-driver-endpoint-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/p2p-platform/backend/main_new.py
autonomous: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "Every customer endpoint in main_new.py rejects requests without a valid customer JWT (401)"
    - "Customer endpoints with customer_id in path verify authenticated customer owns the resource (403 on mismatch)"
    - "Cart endpoints require authentication instead of optional Header-based token parsing"
    - "No customer endpoint uses get_current_user, get_current_customer, or manual oauth2_scheme patterns"
  artifacts:
    - path: "apps/web/p2p-platform/backend/main_new.py"
      provides: "All customer endpoints converted to Depends(require_customer)"
      contains: "Depends(require_customer)"
  key_links:
    - from: "apps/web/p2p-platform/backend/main_new.py"
      to: "apps/web/p2p-platform/backend/auth_utils.py"
      via: "Depends(require_customer) imported and used on all customer endpoints"
      pattern: "customer.*Depends\\(require_customer\\)"
---

<objective>
Convert all customer endpoints in main_new.py from ad-hoc authentication patterns to standardized `Depends(require_customer)` from auth_utils.py. This covers 45 endpoints across 5 auth pattern groups: simple get_current_customer swap, get_current_user role enforcement, Header body pattern removal, promo endpoint, and manual oauth2_scheme JWT decode removal. Also includes address, FCM token, and order create endpoints.

Purpose: Enforce role-specific auth on every customer endpoint so a driver or vendor JWT cannot access customer data (closes the primary security gap where get_current_user accepts any role).
Output: main_new.py with all customer endpoints using Depends(require_customer) + ownership checks where needed.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-customer-driver-endpoint-auth/01-RESEARCH.md
@apps/web/p2p-platform/backend/auth_utils.py
@apps/web/p2p-platform/backend/main_new.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Group A + Group C + Group D customer endpoints (simple + cart conversions)</name>
  <files>apps/web/p2p-platform/backend/main_new.py</files>
  <action>
Convert these customer endpoints from their current auth pattern to `Depends(require_customer)`. The import already exists at line 33.

**Group A: get_current_customer -> require_customer (10 endpoints, LOW risk)**
These are simple parameter swaps -- replace `Depends(get_current_customer)` with `Depends(require_customer)`. The Customer object is already used correctly in the function body. No variable renaming needed since both return Customer. Rename parameter from `current_customer` (if used) to `customer` for consistency.

Endpoints (line numbers from research, re-verify before editing):
- Line ~6743: GET `/api/customer/profile`
- Line ~6767: GET `/api/customer/dashboard`
- Line ~6829: GET `/api/customer/rides/history`
- Line ~17514: GET `/api/customers/{customer_id}/cards` (already has ownership check -- keep it)
- Line ~17575: POST `/api/customers/{customer_id}/cards` (already has ownership check -- keep it)
- Line ~17632: DELETE `/api/customers/{customer_id}/cards/{card_id}` (already has ownership check -- keep it)
- Line ~17661: POST `/api/customers/{customer_id}/cards/{card_id}/default` (already has ownership check -- keep it)
- Line ~18559: GET `/api/customer/notifications`
- Line ~18588: PUT `/api/customer/notifications/{notification_id}/read`
- Line ~18606: DELETE `/api/customer/notifications`

For each: find `Depends(get_current_customer)` in the function signature, replace with `Depends(require_customer)`. If the variable is named `current_customer`, rename to `customer` and update all references in the function body.

**Group C: Authorization Header body pattern -> require_customer (6 endpoints, MEDIUM risk)**
Replace `authorization: Optional[str] = Header(None)` + `get_current_customer_from_token(authorization, db)` with `customer: Customer = Depends(require_customer)`. Remove the manual null check `if not customer: raise HTTPException(401)` since require_customer handles that.

Endpoints:
- Line ~6962: GET `/api/cart`
- Line ~7000: POST `/api/cart/items`
- Line ~7072: PUT `/api/cart/items/{item_id}`
- Line ~7118: DELETE `/api/cart/items/{item_id}`
- Line ~7153: DELETE `/api/cart`
- Line ~7184: POST `/api/cart/apply-promo`

For each:
1. Remove `authorization: Optional[str] = Header(None)` from signature
2. Add `customer: Customer = Depends(require_customer)` to signature
3. Remove the `customer = get_current_customer_from_token(authorization, db)` call
4. Remove the `if not customer: raise HTTPException(status_code=401, ...)` block
5. Update references to use the `customer` parameter directly (it's already a Customer object)

**Group D: get_current_user -> require_customer on promo endpoint (1 endpoint)**
- Line ~7247: DELETE `/api/cart/promo`
Replace `Depends(get_current_user)` with `Depends(require_customer)`. The function body needs to change from `current_user.email` to `customer.email` to look up the customer. Since require_customer returns Customer directly, remove the secondary customer lookup by email.

DO NOT modify any driver, vendor, admin, or shared ride endpoints -- those are in Plans 02 and 03.
DO NOT remove or modify the `get_current_customer` or `get_current_user` function definitions -- other endpoints (driver, vendor, admin) still use them.
  </action>
  <verify>
1. `grep -n "get_current_customer" apps/web/p2p-platform/backend/main_new.py | grep -v "def get_current_customer" | grep -v "get_current_customer_from_token"` should return zero lines (no more usages except the function definition itself)
2. `grep -n "get_current_customer_from_token" apps/web/p2p-platform/backend/main_new.py` should return only the function definition, not any call sites
3. `cd apps/web/p2p-platform/backend && python -c "from main_new import app; print('Import OK')"` should succeed (no syntax errors)
  </verify>
  <done>All 17 Group A + C + D customer endpoints use Depends(require_customer). No get_current_customer or Header-based auth patterns remain in customer endpoint call sites. Cart endpoints require mandatory auth instead of optional.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Group B + Group E + address + FCM + order create customer endpoints (role enforcement + ownership)</name>
  <files>apps/web/p2p-platform/backend/main_new.py</files>
  <action>
Convert remaining customer endpoints that use `Depends(get_current_user)` or manual `Depends(oauth2_scheme)` to `Depends(require_customer)` with ownership checks.

**Group B: get_current_user -> require_customer (15 endpoints, MEDIUM risk)**
Replace `current_user: User = Depends(get_current_user)` with `customer: Customer = Depends(require_customer)`. Update ALL references from `current_user` to `customer` in the function body. Key property mappings: `current_user.email` -> `customer.email`, `current_user.id` -> look up carefully (User.id != Customer.id). For endpoints that filter by `customer_email`, use `customer.email`. For endpoints with `customer_id` in path, add ownership check: `if customer.id != customer_id: raise HTTPException(403, "Access denied")`.

Endpoints with NO ownership check needed (use customer.email for filtering):
- Line ~15362: GET `/api/customer/orders` -- change `current_user.email` to `customer.email`
- Line ~15471: GET `/api/customer/rides` -- looks up customer by email, use `customer` directly
- Line ~7247 (Group D already done in Task 1)

Endpoints WITH ownership check needed (customer_id in path or order_id requiring verification):
- Line ~15549: POST `/api/orders/{order_id}/tip-driver` -- verify customer placed this order
- Line ~15580: POST `/api/orders/{order_id}/cancel` -- verify customer placed this order
- Line ~15630: GET `/api/orders/{order_id}/refund-status` -- verify customer placed this order
- Line ~15852: GET `/api/customer/{customer_id}/active-orders` -- add `if customer.id != customer_id: raise HTTPException(403, "Access denied")`
- Line ~15945: GET `/api/customer/orders/{order_id}/track` -- verify customer owns order
- Line ~16872: GET `/api/customer/favorites/{customer_id}` -- add ownership check
- Line ~16905: POST `/api/customer/favorites/{customer_id}/{vendor_id}` -- add ownership check
- Line ~16934: DELETE `/api/customer/favorites/{customer_id}/{vendor_id}` -- add ownership check
- Line ~16959: GET `/api/customer/favorites/{customer_id}/check/{vendor_id}` -- add ownership check
- Line ~16981: GET `/api/customer/orders/{order_id}/chat` -- verify customer owns order
- Line ~17020: POST `/api/customer/orders/{order_id}/chat` -- verify customer owns order
- Line ~17695: POST `/api/customer/orders/{order_id}/rate-driver` -- verify customer owns order
- Line ~17714: POST `/api/customer/orders/{order_id}/rate-restaurant` -- verify customer owns order

For order ownership checks: after getting customer from Depends, query the order and verify `order.customer_email == customer.email` or `order.customer_id == customer.id` (check which field the Order model uses). If no match, raise HTTPException(403, "Access denied").

For favorites/active-orders ownership: simply check `customer.id != customer_id` path param.

**Group E: Manual oauth2_scheme -> require_customer (4 endpoints, HIGH risk)**
Replace `token: str = Depends(oauth2_scheme)` + manual `jwt.decode()` + error handling with `customer: Customer = Depends(require_customer)`. Remove the entire try/except JWT block. Use `customer.id` instead of `payload.get("customer_id")`.

- Line ~3509: DELETE `/api/customers/{customer_id}/delete` -- add `if customer.id != customer_id: raise HTTPException(403, "Access denied")`
- Line ~3622: POST `/api/customer/email/send-verification` -- use `customer.id` and `customer.email` directly
- Line ~3685: POST `/api/customer/email/verify` -- use `customer.id` directly
- Line ~3761: GET `/api/customer/email/status` -- use `customer.id` directly

**Address endpoints: get_current_user -> require_customer (6 endpoints)**
- Line ~16640: GET `/api/addresses/{customer_id}` -- add ownership check
- Line ~16678: GET `/api/addresses/{customer_id}/default` -- add ownership check
- Line ~16712: POST `/api/addresses/{customer_id}` -- add ownership check
- Line ~16761: PUT `/api/addresses/{customer_id}/{address_id}` -- add ownership check
- Line ~16811: DELETE `/api/addresses/{customer_id}/{address_id}` -- add ownership check
- Line ~16845: POST `/api/addresses/{customer_id}/{address_id}/set-default` -- add ownership check

For each: replace `Depends(get_current_user)` with `Depends(require_customer)`, add `if customer.id != customer_id: raise HTTPException(status_code=403, detail="Access denied")`.

**FCM token endpoints: get_current_user -> require_customer (2 endpoints)**
- Line ~18217: POST `/api/erp/customers/{customer_id}/fcm-token` -- add ownership check
- Line ~18298: DELETE `/api/erp/customers/{customer_id}/fcm-token` -- add ownership check

**Order create: get_current_user -> require_customer (1 endpoint)**
- Line ~15351: POST `/api/orders/create` -- replace auth, use `customer.email` for order creation

For ALL endpoints: rename `current_user` variable to `customer` throughout the function body. Ensure every reference to the old variable name is updated to prevent NameError.

DO NOT modify driver, vendor, admin, shared ride, or bid_routes.py endpoints.
DO NOT remove `get_current_user` function definition -- driver and other endpoints still use it.
  </action>
  <verify>
1. `grep -cn "Depends(get_current_user)" apps/web/p2p-platform/backend/main_new.py` -- count should show ONLY driver/vendor/admin/shared endpoint usages remain (non-customer endpoints). Verify by spot-checking that remaining `get_current_user` calls are on paths like `/api/driver/`, `/api/rides/`, `/api/erp/rides/`, or non-customer paths.
2. `grep -cn "Depends(oauth2_scheme)" apps/web/p2p-platform/backend/main_new.py` -- count should show only driver endpoints with manual JWT remain (the ~9 driver endpoints from research Group C).
3. `cd apps/web/p2p-platform/backend && python -c "from main_new import app; print('Import OK')"` -- no syntax/import errors.
4. `cd apps/web/p2p-platform/backend && pytest tests/test_api_contracts.py -x -q 2>&1 | tail -5` -- contract tests should pass (or show only pre-existing failures, no new 500s from NameError).
  </verify>
  <done>All 28 Group B + E + address + FCM + order create customer endpoints use Depends(require_customer) with appropriate ownership checks. Every customer endpoint with customer_id in path verifies the authenticated customer owns the resource. Every customer endpoint with order_id verifies the customer placed that order. No customer endpoint accepts a driver or vendor JWT.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -n "Depends(require_customer)" apps/web/p2p-platform/backend/main_new.py | wc -l` should show ~45+ lines (all customer endpoints)
2. No customer endpoint path (`/api/customer/`, `/api/cart/`, `/api/addresses/`, `/api/orders/create`, `/api/orders/{id}/tip-driver`, `/api/orders/{id}/cancel`) uses `get_current_user`, `get_current_customer`, `get_current_customer_from_token`, or `oauth2_scheme`
3. `cd apps/web/p2p-platform/backend && python -c "from main_new import app; print('Import OK')"` succeeds
4. `cd apps/web/p2p-platform/backend && pytest tests/ -x -q --timeout=30 2>&1 | tail -10` -- no new failures from these changes
</verification>

<success_criteria>
- All 45 customer endpoints in main_new.py use Depends(require_customer) from auth_utils.py
- All endpoints with customer_id path parameters have ownership checks (403 on mismatch)
- All endpoints with order_id path parameters verify the customer placed the order (403 on mismatch)
- Cart endpoints require mandatory JWT auth (no more optional Header pattern)
- Manual JWT decode blocks removed from customer email verification endpoints
- Python import succeeds without errors
- No NameError from unrenamed variable references
</success_criteria>

<output>
After completion, create `.planning/phases/01-customer-driver-endpoint-auth/01-01-SUMMARY.md`
</output>
