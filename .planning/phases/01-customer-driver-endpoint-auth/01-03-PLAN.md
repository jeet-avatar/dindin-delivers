---
phase: 01-customer-driver-endpoint-auth
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/p2p-platform/backend/bid_routes.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "Every customer-specific endpoint in bid_routes.py rejects requests without a valid customer JWT (401)"
    - "Every driver-specific endpoint in bid_routes.py rejects requests without a valid driver JWT (401)"
    - "Customer endpoints with customer_id in path verify the authenticated customer owns the resource (403 on mismatch)"
    - "Driver endpoints with driver_id in path verify the authenticated driver owns the resource (403 on mismatch)"
    - "Shared ride endpoints in bid_routes.py have explicit per-endpoint auth via require_any_auth"
    - "Contract tests pass without new regressions after auth additions"
  artifacts:
    - path: "apps/web/p2p-platform/backend/bid_routes.py"
      provides: "All bid_routes.py endpoints with per-endpoint auth"
      contains: "from auth_utils import require_customer, require_driver, require_any_auth"
  key_links:
    - from: "apps/web/p2p-platform/backend/bid_routes.py"
      to: "apps/web/p2p-platform/backend/auth_utils.py"
      via: "Import and Depends() on all endpoints"
      pattern: "from auth_utils import"
---

<objective>
Add per-endpoint authentication to all ~30 endpoints in bid_routes.py. This file currently has NO auth imports and relies entirely on the global middleware safety net. Customer-specific endpoints get `Depends(require_customer)`, driver-specific endpoints get `Depends(require_driver)`, and shared endpoints (called by both roles during ride lifecycle) get `Depends(require_any_auth)`.

Purpose: bid_routes.py is the HIGHEST PRIORITY conversion because its endpoints have zero per-endpoint auth -- anyone who passes the global middleware can access any endpoint. Adding role-specific auth prevents customer data leaks to drivers and vice versa.
Output: bid_routes.py with auth imports and Depends() on every endpoint + ownership checks.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-customer-driver-endpoint-auth/01-RESEARCH.md
@apps/web/p2p-platform/backend/auth_utils.py
@apps/web/p2p-platform/backend/bid_routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth imports and convert customer + driver endpoints in bid_routes.py</name>
  <files>apps/web/p2p-platform/backend/bid_routes.py</files>
  <action>
**Step 1: Add auth imports to bid_routes.py**

At the top of bid_routes.py (after existing imports), add:
```python
from auth_utils import require_customer, require_driver, require_any_auth
```

Also add the HTTPException import if not already present:
```python
from fastapi import HTTPException
```

And the Customer/Driver model imports if not already present:
```python
from models import Customer, Driver
```

**Step 2: Convert customer-specific endpoints**

Add `customer: Customer = Depends(require_customer)` to each function signature and add ownership check `if customer.id != customer_id: raise HTTPException(status_code=403, detail="Access denied")`.

Customer endpoints in bid_routes.py (verify exact line numbers by grepping the file):
- `/api/rides/customer/{customer_id}/requests` (GET, ~line 481) -- add require_customer + ownership check
- `/api/rides/customer/{customer_id}/disputes` (GET, ~line 2692) -- add require_customer + ownership check
- `/api/rides/customer/{customer_id}/recurring-rides` (POST, ~line 2854) -- add require_customer + ownership check
- `/api/rides/customer/{customer_id}/recurring-rides` (GET, ~line 2925) -- add require_customer + ownership check
- `/api/rides/request` (POST, ~line 299) -- add require_customer (no ownership check -- creates new ride request, use `customer.id` as the customer_id instead of trusting client-provided value)
- `/api/rides/request/{request_id}/cancel` (POST, ~line 896) -- add require_customer, verify ride belongs to customer

For the ride request creation endpoint (`POST /api/rides/request`): after adding `customer: Customer = Depends(require_customer)`, override the `data.customer_id` with `customer.id` to prevent customer_id spoofing:
```python
# Prevent customer_id spoofing -- always use authenticated customer's ID
data.customer_id = customer.id
```

**Step 3: Convert driver-specific endpoints**

Add `driver: Driver = Depends(require_driver)` to each function signature and add ownership check where driver_id is in path.

Driver endpoints in bid_routes.py:
- `/api/rides/driver/{driver_id}/bids` (GET, ~line 1590) -- add require_driver + ownership check
- `/api/rides/driver/{driver_id}/payout-history` (GET, ~line 2485) -- add require_driver + ownership check
- `/api/rides/request/{request_id}/bid` (POST, ~line 1051) -- add require_driver (use `driver.id` as bidding driver instead of trusting client-provided value)
- `/api/rides/request/{request_id}/arrived` (POST, ~line 1634) -- add require_driver, verify driver is assigned to this ride
- `/api/rides/request/{request_id}/start` (POST, ~line 1878) -- add require_driver, verify driver is assigned
- `/api/rides/request/{request_id}/complete` (POST, ~line 1968) -- add require_driver, verify driver is assigned
- `/api/rides/request/{request_id}/driver-cancel` (POST, ~line 1709) -- add require_driver, verify driver is assigned

For bid submission (`POST /api/rides/request/{request_id}/bid`): override `data.driver_id` with `driver.id` to prevent driver_id spoofing.

For ride lifecycle endpoints (arrived/start/complete/driver-cancel): after getting the ride request from DB, verify `ride_request.driver_id == driver.id` or `ride_request.matched_driver_id == driver.id` (check which field the model uses for the assigned driver). If no match, raise HTTPException(403, "Access denied").

**Step 4: Convert shared endpoints**

Add `_auth: dict = Depends(require_any_auth)` to each shared endpoint function signature. These are called by both customer and driver apps during the ride lifecycle.

Shared endpoints in bid_routes.py (grep for all remaining endpoints not covered above):
- All bid response/counter-offer endpoints (both roles participate in negotiation)
- All bid listing endpoints that don't have a role-specific path
- Chat within rides
- Any endpoint on `/api/rides/request/{id}/...` that isn't already customer or driver specific

For each shared endpoint: add `_auth: dict = Depends(require_any_auth)` as a parameter. This ensures explicit per-endpoint auth (satisfying AUTH-05/AUTH-06) without role restriction.

IMPORTANT: Carefully review EVERY endpoint handler function in bid_routes.py. Every single `async def` or `def` that is a route handler MUST have one of: `Depends(require_customer)`, `Depends(require_driver)`, or `Depends(require_any_auth)`. Leave ZERO endpoints without per-endpoint auth.

To find all endpoints: `grep -n "@router\.\(get\|post\|put\|delete\|patch\)" bid_routes.py`
  </action>
  <verify>
1. `grep -n "def.*Depends" apps/web/p2p-platform/backend/bid_routes.py | grep -c "require_"` -- should match total endpoint count
2. `grep -n "@router\." apps/web/p2p-platform/backend/bid_routes.py | wc -l` (endpoint count) should equal `grep -n "require_customer\|require_driver\|require_any_auth" apps/web/p2p-platform/backend/bid_routes.py | wc -l` (auth count)
3. `cd apps/web/p2p-platform/backend && python -c "from bid_routes import router; print('Import OK')"` succeeds
4. `cd apps/web/p2p-platform/backend && python -c "from main_new import app; print('Import OK')"` succeeds (bid_routes is mounted via include_router)
  </verify>
  <done>Every endpoint in bid_routes.py has explicit per-endpoint auth via Depends(require_customer), Depends(require_driver), or Depends(require_any_auth). Customer_id and driver_id spoofing prevented on ride request creation and bid submission. Ownership checks on all path-parameterized endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and fix any auth-related test regressions</name>
  <files>apps/web/p2p-platform/backend/bid_routes.py</files>
  <action>
Run the full backend test suite to identify any regressions caused by all three plans' auth changes (Plan 01, Plan 02 if already complete, and this plan).

**Step 1: Run test suite**
```bash
cd apps/web/p2p-platform/backend && pytest tests/ -v --timeout=60 2>&1
```

**Step 2: Analyze failures**

Expected failure patterns and fixes:

1. **Tests using `auth_headers` fixture on customer endpoints now getting 401**: These tests use a User-based JWT that doesn't contain `customer_id`. Fix by switching to `customer_auth_headers` fixture (already exists in conftest.py -- creates JWT with `customer_id`).

2. **Tests using `auth_headers` fixture on driver endpoints now getting 401**: Fix by switching to `driver_auth_headers` fixture (already exists in conftest.py -- creates JWT with `driver_id`).

3. **Tests calling bid_routes.py endpoints without any auth now getting 401**: Add auth headers to the test's client calls. Use `customer_auth_headers` for customer endpoints, `driver_auth_headers` for driver endpoints.

4. **Tests where NameError occurs**: A variable was renamed from `current_user` to `customer` or `driver` but a reference was missed. Fix in the source file.

**Step 3: Fix regressions**

For each failing test:
1. Determine if it's an auth fixture issue (switch to role-specific fixture)
2. Determine if it's a source code bug (fix the endpoint handler)
3. Determine if it's a pre-existing failure (document and skip)

Do NOT modify test assertions that verify correct auth behavior (e.g., "this endpoint should return 401 without auth" -- that's now correct behavior).

**Step 4: Final verification**
```bash
cd apps/web/p2p-platform/backend && pytest tests/ -v --timeout=60 2>&1
```

All tests that passed before these auth changes should still pass. New 401 responses on previously-unprotected endpoints are CORRECT behavior, not regressions.
  </action>
  <verify>
1. `cd apps/web/p2p-platform/backend && pytest tests/ -q --timeout=60 2>&1 | tail -5` -- test count should be same or higher, pass rate should be same or higher
2. `cd apps/web/p2p-platform/backend && pytest tests/test_api_contracts.py -v --timeout=60 2>&1 | tail -20` -- contract tests pass
3. No new NameError or ImportError in test output
  </verify>
  <done>Full test suite passes with no new regressions from auth changes. Any tests that were updated use the correct role-specific auth fixtures (customer_auth_headers, driver_auth_headers). All pre-existing test failures documented.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Every endpoint in bid_routes.py has per-endpoint Depends() auth
2. `cd apps/web/p2p-platform/backend && python -c "from main_new import app; print('Import OK')"` succeeds
3. `cd apps/web/p2p-platform/backend && pytest tests/ -q --timeout=60 2>&1 | tail -5` -- pass rate maintained
4. `grep -rn "customer_id" apps/web/p2p-platform/backend/bid_routes.py` -- every customer_id usage either comes from `customer.id` (authenticated) or has an ownership check
5. `grep -rn "driver_id" apps/web/p2p-platform/backend/bid_routes.py` -- every driver_id usage either comes from `driver.id` (authenticated) or has an ownership check
</verification>

<success_criteria>
- auth_utils imported at top of bid_routes.py
- Every endpoint has Depends(require_customer), Depends(require_driver), or Depends(require_any_auth)
- Customer_id spoofing prevented on ride request creation
- Driver_id spoofing prevented on bid submission
- Ownership checks on all endpoints with customer_id or driver_id in path
- Test suite passes without new regressions
- No endpoint in bid_routes.py relies solely on global middleware for auth
</success_criteria>

<output>
After completion, create `.planning/phases/01-customer-driver-endpoint-auth/01-03-SUMMARY.md`
</output>
