---
phase: 54.1-m6-multi-user-per-tenant-team-invites-role-middleware
plan: 03
type: execute
wave: 2
depends_on:
  - 54.1-01
files_modified:
  - /Users/jeet/turion-space-demo/team.html
  - /Users/jeet/turion-space-demo/accept-invite.html
  - /Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js
  - /Users/jeet/turion-space-demo/deploy-frontend.sh
autonomous: true
requirements:
  - TeamPage

must_haves:
  truths:
    - "GET /team renders the real team page (NOT the Phase 54 stub) — header shows 'Team — N members' with N from /api/team, table shows email/role/status/joined per row"
    - "Invite-member button is HIDDEN when current user role is 'member' or 'viewer', VISIBLE when role is 'admin' or 'manager'"
    - "Invite modal role dropdown shows ['admin','manager','member','viewer'] when caller is admin, but only ['member','viewer'] when caller is manager"
    - "Submitting the invite form POSTs to /api/team/invite via window.erpApi.post and refetches the member list on success (no full page reload required, but reload acceptable for demo)"
    - "GET /accept-invite serves accept-invite.html (CloudFront Function rewrite published + invalidation completed)"
    - "accept-invite.html POSTs {token} to /api/invites/accept-invite, calls cognitoAuth.signInWithMagicLink(email) on success, redirects to /erp-login.html after 4s"
    - "accept-invite.html shows 'invalid or expired' message + /signup CTA on 410 response (no infinite spinner, no console error spam)"
    - "accept-invite.html includes <meta name='referrer' content='no-referrer'> AND clears the URL via history.replaceState (Pitfall 7 — token leak prevention)"
    - "Phase 54 inject-shell idempotency PRESERVED: running `node scripts/inject-shell.mjs` after the team.html rewrite reports `injected: 0` (i.e., the <!-- ZIETRA-SHELL-INJECTED --> marker is intact)"
    - "Frontend deployed via deploy-frontend.sh (S3 sync + CF invalidation) AND CF Function published via aws cloudfront update-function + publish-function + create-invalidation"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/team.html"
      provides: "REAL team page replacing the Phase 54 stub: header + member table + invite modal + role-aware action menu"
      contains: "/api/team"
      min_lines: 80
    - path: "/Users/jeet/turion-space-demo/accept-invite.html"
      provides: "PUBLIC invite-acceptance landing — token parser + accept-invite POST + magic-link trigger + redirect"
      contains: "/api/invites/accept-invite"
      min_lines: 60
    - path: "/Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js"
      provides: "CloudFront Function `R` map with `/accept-invite` → `/accept-invite.html` rewrite added"
      contains: "/accept-invite"
  key_links:
    - from: "team.html inline <script>"
      to: "/api/team"
      via: "window.erpApi.get('/api/team') on load + window.erpApi.post('/api/team/invite', {email,role}) on submit"
      pattern: "erpApi\\.(get|post)\\(['\"]\\/api\\/team"
    - from: "accept-invite.html inline <script>"
      to: "/api/invites/accept-invite"
      via: "fetch(API_BASE + '/api/invites/accept-invite', {method:'POST', body:{token}})"
      pattern: "/api/invites/accept-invite"
    - from: "accept-invite.html success branch"
      to: "cognitoAuth.signInWithMagicLink"
      via: "window.cognitoAuth.signInWithMagicLink(data.email)"
      pattern: "signInWithMagicLink"
    - from: "cf-function-source/turion-clean-urls.js R map"
      to: "/accept-invite.html"
      via: "URI rewrite at viewer-request"
      pattern: "'\\/accept-invite':\\s*'\\/accept-invite\\.html'"
---

<objective>
Replace the Phase 54 `/team` stub with the real team-management UI (member list + invite modal + role-aware actions) AND add the public `/accept-invite` landing page that consumes one-time invite tokens, then publish both via the existing frontend deploy + CloudFront Function update flow.

Purpose: This is what 54.1's user-facing promise resolves to. Without this plan, 54.1-02's backend endpoints exist but no human can see/touch them. After this plan, an admin can invite a colleague + the colleague can accept via URL.

Output:
- `team.html` rewritten — Phase 54 stub body replaced with real `<main class="z-team">` content; `<head>` preserved byte-for-byte so inject-shell.mjs idempotency holds
- `accept-invite.html` created — vanilla HTML mirroring `cognito-auth-callback.html` style; references-no-leak meta + URL clear; POST to `/api/invites/accept-invite`; success → `cognitoAuth.signInWithMagicLink` → redirect
- `cf-function-source/turion-clean-urls.js` updated with one new R-map entry, function published, distribution invalidated
- S3 sync via `deploy-frontend.sh` runs; both pages live at `https://turion.zietra.com/team` and `https://turion.zietra.com/accept-invite`
- inject-shell.mjs run AFTER the rewrite reports `injected: 0` (no double-inject)
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54.1-m6-multi-user-per-tenant-team-invites-role-middleware/CONTEXT.md
@.planning/phases/54.1-m6-multi-user-per-tenant-team-invites-role-middleware/54.1-RESEARCH.md
@.planning/phases/54.1-m6-multi-user-per-tenant-team-invites-role-middleware/54.1-01-SUMMARY.md
@/Users/jeet/turion-space-demo/team.html
@/Users/jeet/turion-space-demo/cognito-auth-callback.html
@/Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js
@/Users/jeet/turion-space-demo/scripts/inject-shell.mjs
@/Users/jeet/turion-space-demo/deploy-frontend.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite team.html — replace stub body, preserve <head>, render real member table + invite modal</name>
  <files>/Users/jeet/turion-space-demo/team.html</files>
  <action>
Read the current `team.html` (64 LOC stub from Phase 54). The `<head>` section contains: turion-config.js → cognito-auth.js → erp-api.js → requireSession() IIFE → `<!-- ZIETRA-SHELL-INJECTED -->` marker → app-shell.css + app-shell.js defer.

**Rewrite RULE:** preserve `<head>` byte-for-byte (Pitfall 9 — don't break the inject-shell marker). Replace ONLY the `<body>` section and the inline `.z-stub` `<style>` block (which becomes the `.z-team` style block).

Pattern source: 54.1-RESEARCH.md §"team.html replacement body" (lines 1019-1083).

New `<body>` structure:

```html
<body>
  <main class="z-team" style="max-width: 920px; margin: 32px auto; padding: 24px;">
    <header class="z-team-hdr">
      <h1 id="hdr">Team — <span id="memberCount">…</span> members</h1>
      <button id="inviteBtn" class="z-cta-btn" hidden>Invite member</button>
    </header>
    <table id="memberTable" class="z-team-table">
      <thead>
        <tr><th>Name / Email</th><th>Role</th><th>Status</th><th>Joined</th><th></th></tr>
      </thead>
      <tbody id="memberRows">
        <tr><td colspan="5" class="z-empty">Loading…</td></tr>
      </tbody>
    </table>
  </main>

  <dialog id="inviteModal">
    <form id="inviteForm">
      <h3>Invite member</h3>
      <label>Email <input type="email" name="email" required autocomplete="off"></label>
      <label>Role <select name="role" id="roleSelect" required></select></label>
      <p id="inviteError" class="z-err" role="alert" aria-live="polite"></p>
      <menu>
        <button type="button" id="cancelInvite">Cancel</button>
        <button type="submit" id="sendInvite">Send invite</button>
      </menu>
    </form>
  </dialog>

  <script>
  (async () => {
    await window.cognitoAuth.requireSession();

    const me = window.cognitoAuth.getCurrentUser?.() || {};
    const data = await window.erpApi.get('/api/team');
    const members = data.members || [];

    const ownRow = members.find(m => m.cognito_sub && m.cognito_sub === me.sub);
    const myRole = ownRow?.role || 'viewer';

    document.getElementById('memberCount').textContent = members.length;

    // Render member rows — NO hardcoded role/status, all from server response
    const tbody = document.getElementById('memberRows');
    tbody.innerHTML = '';
    for (const m of members) {
      const tr = document.createElement('tr');
      const joined = m.joined_at ? new Date(m.joined_at).toLocaleDateString() : '—';
      const isMe = m.cognito_sub === me.sub;
      const canActOnRow = myRole === 'admin' && !isMe;
      tr.innerHTML = `
        <td>${escapeHtml(m.email)}${isMe ? ' <span class="z-you">(you)</span>' : ''}</td>
        <td><span class="z-badge z-badge-${escapeHtml(m.role)}">${escapeHtml(m.role)}</span></td>
        <td><span class="z-status z-status-${escapeHtml(m.status)}">${escapeHtml(m.status)}</span></td>
        <td>${joined}</td>
        <td>${canActOnRow ? `<button class="z-link z-row-remove" data-id="${m.id}">Remove</button>` : ''}</td>`;
      tbody.appendChild(tr);
    }

    // Invite button visibility — DB role, not JWT
    if (myRole === 'admin' || myRole === 'manager') {
      const btn = document.getElementById('inviteBtn');
      btn.hidden = false;
      const sel = document.getElementById('roleSelect');
      // Manager can only invite member/viewer; admin can invite any role
      const opts = myRole === 'admin' ? ['admin','manager','member','viewer'] : ['member','viewer'];
      sel.innerHTML = opts.map(o => `<option value="${o}">${o}</option>`).join('');
      btn.addEventListener('click', () => document.getElementById('inviteModal').showModal());
    }

    // Modal cancel
    document.getElementById('cancelInvite').addEventListener('click', () => {
      document.getElementById('inviteModal').close();
      document.getElementById('inviteError').textContent = '';
    });

    // Submit invite
    document.getElementById('inviteForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      const fd = new FormData(e.target);
      const errEl = document.getElementById('inviteError');
      errEl.textContent = '';
      try {
        await window.erpApi.post('/api/team/invite', { email: fd.get('email'), role: fd.get('role') });
        document.getElementById('inviteModal').close();
        window.location.reload(); // simplest refetch — see RESEARCH §"Pattern 2 best-effort SES" for in-place option
      } catch (err) {
        errEl.textContent = err?.message || err?.body?.error || 'Invite failed.';
      }
    });

    // Remove member (admin only)
    tbody.addEventListener('click', async (e) => {
      const target = e.target.closest('.z-row-remove');
      if (!target) return;
      if (!confirm('Remove this member?')) return;
      try {
        await window.erpApi.delete(`/api/team/${target.dataset.id}`);
        window.location.reload();
      } catch (err) {
        alert(err?.message || err?.body?.error || 'Remove failed.');
      }
    });

    function escapeHtml(s) {
      return String(s ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
    }
  })();
  </script>
</body>
```

Inline `<style>` block (replaces `.z-stub`):

```css
.z-team-hdr { display:flex; align-items:center; justify-content:space-between; margin-bottom:18px; }
.z-team-hdr h1 { margin:0; font-size:22px; }
.z-team-table { width:100%; border-collapse:collapse; }
.z-team-table th, .z-team-table td { padding:10px 12px; border-bottom:1px solid var(--z-border, #243150); text-align:left; }
.z-team-table th { font-size:12px; font-weight:600; opacity:.7; text-transform:uppercase; letter-spacing:.04em; }
.z-empty { padding:24px; text-align:center; opacity:.7; }
.z-cta-btn { padding:8px 16px; border-radius:6px; background:var(--z-accent,#7c3aed); color:#fff; border:0; cursor:pointer; font-weight:600; }
.z-cta-btn:hover { filter:brightness(1.08); }
.z-badge { display:inline-block; padding:2px 8px; border-radius:4px; font-size:12px; font-weight:600; }
.z-badge-admin { background:#7c3aed; color:#fff; }
.z-badge-manager { background:#0ea5e9; color:#fff; }
.z-badge-member { background:#6b7280; color:#fff; }
.z-badge-viewer { background:#374151; color:#d1d5db; }
.z-status-active { color:#22c55e; }
.z-status-pending { color:#f59e0b; }
.z-status-suspended, .z-status-removed { color:#9ca3af; }
.z-you { font-size:11px; opacity:.6; }
.z-link { background:none; border:0; color:#6cb6ff; cursor:pointer; padding:0; font-size:13px; text-decoration:underline; }
.z-err { color:#ff6b6b; min-height:1.2em; margin:6px 0 0; font-size:13px; }
dialog#inviteModal { border:0; border-radius:10px; padding:24px; max-width:380px; box-shadow:0 8px 32px rgba(0,0,0,.35); }
dialog#inviteModal h3 { margin:0 0 12px; }
dialog#inviteModal label { display:block; font-size:13px; margin:8px 0; }
dialog#inviteModal input, dialog#inviteModal select { display:block; width:100%; padding:6px 8px; border-radius:4px; border:1px solid #2a3553; background:#0b1020; color:#e7ecf3; margin-top:4px; }
dialog#inviteModal menu { display:flex; gap:8px; justify-content:flex-end; padding:0; margin:16px 0 0; }
dialog#inviteModal menu button[type=submit] { background:var(--z-accent,#7c3aed); color:#fff; border:0; padding:6px 14px; border-radius:4px; cursor:pointer; }
dialog#inviteModal menu button[type=button] { background:transparent; color:#e7ecf3; border:1px solid #2a3553; padding:6px 14px; border-radius:4px; cursor:pointer; }
```

Implementation rules (NO hardcoded data — Rule 1):
- Member count → `data.members.length` from API (NOT `(N)` literal)
- Role badges → derive class from `m.role` value, NOT hardcoded list
- Status colors → derive class from `m.status` value
- Role dropdown options → derived from `myRole` (admin sees all 4; manager sees 2)
- Invite-button visibility → derived from DB role via /api/team self-row lookup, NOT JWT cognito:groups
- Joined date → `m.joined_at` formatted client-side; '—' when null (pending)
- Self-row marker → comparison `m.cognito_sub === me.sub` (no hardcoded email)
- Remove button → only renders for admins, never for self (canActOnRow guard)

Rule 2 (every link works): the Remove button POSTs to `/api/team/:id`; the Cancel button closes the modal; Send Invite POSTs to `/api/team/invite`. NO toast-only no-op buttons. NO dead `<a>` tags.

Rule 5 (remove dead code): the entire Phase 54 `.z-stub` style block + the "Status: Ships in Phase 54.1" placeholder copy is GONE. Verify no `z-stub` references remain.

Rule 6 (no unnecessary code): no error handling for impossible cases — `data.members` is guaranteed by the backend contract; `me.sub` exists or `requireSession()` would have redirected.

Pre-edit: capture the exact `<head>` byte content via `head -c` count; post-edit: confirm same count.

Post-edit: run `node /Users/jeet/turion-space-demo/scripts/inject-shell.mjs` and confirm the report ends with `injected: 0` (everything `already:`).

Git: commit in turion-space-demo with `feat(54.1-03): rewrite team.html — real member table + invite modal + role-aware actions`.
  </action>
  <verify>
1. team.html size grew (was 1864 B stub):
   ```bash
   wc -c /Users/jeet/turion-space-demo/team.html
   ```
   Expected: at least 4500 B (real page).
2. NO `z-stub` references remain:
   ```bash
   grep -c "z-stub\|Status: Ships in Phase" /Users/jeet/turion-space-demo/team.html
   ```
   Expected: `0`.
3. Real API call present:
   ```bash
   grep -c "/api/team" /Users/jeet/turion-space-demo/team.html
   ```
   Expected: at least `2` (one GET, one POST, possibly DELETE inline).
4. inject-shell marker still present:
   ```bash
   grep "ZIETRA-SHELL-INJECTED" /Users/jeet/turion-space-demo/team.html | wc -l
   ```
   Expected: `1`.
5. inject-shell.mjs idempotency holds:
   ```bash
   cd /Users/jeet/turion-space-demo && node scripts/inject-shell.mjs 2>&1 | grep -E "injected:\s*[0-9]+"
   ```
   Expected output contains `injected: 0`.
6. Role dropdown is dynamic (no hardcoded options in HTML):
   ```bash
   grep -E "<option value=\"(admin|manager|member|viewer)\">" /Users/jeet/turion-space-demo/team.html | wc -l
   ```
   Expected: `0` (options are injected by JS, not in static HTML).
7. Invite button has `hidden` attribute by default (NOT visible until JS unhides):
   ```bash
   grep "id=\"inviteBtn\".*hidden" /Users/jeet/turion-space-demo/team.html
   ```
   Output MUST contain the line.
  </verify>
  <done>
team.html stub body replaced with real UI; `<head>` preserved + inject-shell idempotent; no hardcoded role/status data; invite button is role-gated client-side; commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create accept-invite.html (PUBLIC token-acceptance landing)</name>
  <files>/Users/jeet/turion-space-demo/accept-invite.html</files>
  <action>
Create `/Users/jeet/turion-space-demo/accept-invite.html` (NEW file). Pattern source: 54.1-RESEARCH.md §"accept-invite.html (full reference)" (lines 934-1017) + style mirror of `/Users/jeet/turion-space-demo/cognito-auth-callback.html`.

Critical content rules:
- `<head>` MUST include `<meta name="referrer" content="no-referrer">` (Pitfall 7 — token leak prevention)
- Inline `<script>` MUST call `history.replaceState(null, '', '/accept-invite')` AFTER reading the token but BEFORE the network call (clears the URL bar)
- The page MUST NOT call `cognitoAuth.requireSession()` — invitee is unauthenticated by definition
- Network call: `fetch(window.TURION_CONFIG.API_BASE + '/api/invites/accept-invite', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({token})})`
- Success branch: call `window.cognitoAuth.signInWithMagicLink(data.email)` to send the invitee a magic link, show "Check `<email>` for your sign-in link", redirect to `/erp-login.html` after 4 seconds via `setTimeout(() => window.location.replace('/erp-login.html'), 4000)`
- Error branches:
  - No token in URL → "Missing invite token." + show /signup CTA
  - Network error → "Network error. Try again." (no retry button — let user reload)
  - 410 / 4xx response → display backend error message + show /signup CTA "Create your own workspace"
  - signInWithMagicLink throws → "Account ready, but magic-link send failed. Try /erp-login." + 3s redirect to `/erp-login.html`

Style: mirror `cognito-auth-callback.html` — dark theme `#0b1020` body bg, `#131a2e` card bg, `#e7ecf3` text, spinner with `#6cb6ff` accent.

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="referrer" content="no-referrer">
  <title>Accepting invite · Zietra</title>
  <style>
    body { font-family: system-ui, -apple-system, sans-serif; background: #0b1020; color: #e7ecf3;
           display: flex; align-items: center; justify-content: center; min-height: 100vh; margin: 0; }
    .cb-card { background: #131a2e; padding: 32px 40px; border-radius: 12px;
               box-shadow: 0 8px 32px rgba(0,0,0,.35); text-align: center; max-width: 380px; }
    .spinner { width: 36px; height: 36px; border: 3px solid #2a3553; border-top-color: #6cb6ff;
               border-radius: 50%; margin: 0 auto 16px; animation: spin 1s linear infinite; }
    @keyframes spin { to { transform: rotate(360deg); } }
    .msg { margin: 8px 0 0; font-size: 15px; opacity: .85; }
    .err { color: #ff6b6b; margin: 12px 0 0; font-size: 14px; min-height: 1.2em; }
    .btn { display:inline-block; margin-top:14px; padding:8px 16px; border-radius:6px;
           background:#7c3aed; color:#fff; text-decoration:none; font-weight:600; font-size:13px; }
  </style>
</head>
<body>
  <div class="cb-card">
    <div class="spinner" id="spinner"></div>
    <p class="msg" id="msg">Accepting your invite…</p>
    <p id="error" class="err" role="alert" aria-live="polite"></p>
    <div id="signupCta" hidden>
      <a class="btn" href="/signup">Create your own workspace</a>
    </div>
  </div>

  <script src="/turion-config.js"></script>
  <script src="/cognito-auth.js"></script>
  <script>
    (async () => {
      const params = new URLSearchParams(window.location.search);
      const token = params.get('token');
      // Pitfall 7 — clear token from URL before any network call
      history.replaceState(null, '', '/accept-invite');

      const spinner = document.getElementById('spinner');
      const msg = document.getElementById('msg');
      const errEl = document.getElementById('error');
      const cta = document.getElementById('signupCta');

      if (!token) {
        spinner.style.display = 'none';
        msg.textContent = 'Missing invite token.';
        cta.hidden = false;
        return;
      }

      let resp;
      try {
        resp = await fetch(window.TURION_CONFIG.API_BASE + '/api/invites/accept-invite', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ token }),
        });
      } catch (e) {
        spinner.style.display = 'none';
        errEl.textContent = 'Network error. Try again.';
        return;
      }

      const data = await resp.json().catch(() => ({}));
      if (!resp.ok) {
        spinner.style.display = 'none';
        msg.textContent = data.error || 'This invite link is invalid or expired.';
        cta.hidden = false;
        return;
      }

      // Success — trigger magic-link sign-in
      try {
        await window.cognitoAuth.signInWithMagicLink(data.email);
      } catch (e) {
        errEl.textContent = 'Account ready, but magic-link send failed. Try /erp-login.';
        setTimeout(() => window.location.replace('/erp-login.html'), 3000);
        return;
      }
      spinner.style.display = 'none';
      msg.innerHTML = 'Account ready. Check <b>' + escapeHtml(data.email) + '</b> for your sign-in link.';
      setTimeout(() => window.location.replace('/erp-login.html'), 4000);

      function escapeHtml(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
    })();
  </script>
</body>
</html>
```

Implementation rules:
- TURION_CONFIG.API_BASE → from `/turion-config.js` (set per-environment); NO hardcoded APIGW URL
- The page is PUBLIC — no shell injection, no requireSession. Verify by absence of `app-shell.css` reference and `requireSession()` call.

Git: commit in turion-space-demo with `feat(54.1-03): accept-invite.html — public token-acceptance landing with magic-link trigger`.
  </action>
  <verify>
1. File exists:
   ```bash
   ls -la /Users/jeet/turion-space-demo/accept-invite.html
   ```
2. NO requireSession call (page is public):
   ```bash
   grep -c "requireSession" /Users/jeet/turion-space-demo/accept-invite.html
   ```
   Expected: `0`.
3. referrer meta present (Pitfall 7):
   ```bash
   grep "name=\"referrer\" content=\"no-referrer\"" /Users/jeet/turion-space-demo/accept-invite.html
   ```
   Output MUST contain the line.
4. URL clear via history.replaceState present:
   ```bash
   grep "history.replaceState" /Users/jeet/turion-space-demo/accept-invite.html
   ```
   Output MUST contain the call.
5. Calls /api/invites/accept-invite (NOT /api/team/accept-invite):
   ```bash
   grep "/api/invites/accept-invite" /Users/jeet/turion-space-demo/accept-invite.html
   ```
   Output MUST contain the path.
6. Success branch calls signInWithMagicLink:
   ```bash
   grep "signInWithMagicLink" /Users/jeet/turion-space-demo/accept-invite.html
   ```
   Output MUST contain the call.
7. NO hardcoded API URL (uses TURION_CONFIG.API_BASE):
   ```bash
   grep -E "https://[a-z0-9]+\.execute-api\." /Users/jeet/turion-space-demo/accept-invite.html
   ```
   Expected: empty.
  </verify>
  <done>
accept-invite.html committed; referrer meta + URL clear in place; calls correct backend path; uses TURION_CONFIG for API base; magic-link trigger on success.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add /accept-invite rewrite to CloudFront Function + publish + invalidate</name>
  <files>/Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js</files>
  <action>
Add ONE line to the `R` map in `/Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js` so that `/accept-invite` clean URL serves `/accept-invite.html`.

Pattern source: 54.1-RESEARCH.md §"10. CloudFront Function rewrite" (lines 263-278). Current file size 9714 B; one-line add ≈ 50 B → ≈ 9764 B → still under 10240 B cap.

Read the file, find the line `'/signup': '/signup.html',` near line 152, add IMMEDIATELY AFTER it:

```js
  '/accept-invite': '/accept-invite.html',  // Phase 54.1 — PUBLIC invite acceptance landing
```

Verify the file is still under 10 KB after edit:

```bash
wc -c /Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js
```

Expected: less than 10240 bytes.

Then publish the CF Function (deploy-frontend.sh does NOT do this — manual aws CLI per Phase 52-03 pattern):

```bash
# Get current ETag of the LIVE (production-stage) function
FN_NAME=turion-clean-urls
ETAG=$(aws cloudfront describe-function --name "$FN_NAME" --stage LIVE --query 'ETag' --output text 2>/dev/null \
  || aws cloudfront describe-function --name "$FN_NAME" --query 'ETag' --output text)
echo "ETag: $ETAG"

# Update function code
aws cloudfront update-function \
  --name "$FN_NAME" \
  --function-config "Comment=turion-clean-urls,Runtime=cloudfront-js-2.0" \
  --function-code fileb:///Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js \
  --if-match "$ETAG" \
  --query 'ETag' --output text > /tmp/new-etag.txt
NEW_ETAG=$(cat /tmp/new-etag.txt)
echo "New ETag (DEVELOPMENT stage): $NEW_ETAG"

# Publish to LIVE
aws cloudfront publish-function \
  --name "$FN_NAME" \
  --if-match "$NEW_ETAG"
echo "[cf] published $FN_NAME"

# Invalidate the distribution so any cached HTML 404s get re-fetched
aws cloudfront create-invalidation \
  --distribution-id E37R9PT8IL44L2 \
  --paths "/accept-invite" "/accept-invite.html" "/team" "/team.html" \
  --query 'Invalidation.Id' --output text
```

Capture the invalidation ID. Wait for it to complete (~3-5 min) before the smoke test in Task 4.

Git: commit in turion-space-demo with `feat(54.1-03): CF Function /accept-invite → /accept-invite.html rewrite`.
  </action>
  <verify>
1. File contains the new entry:
   ```bash
   grep "/accept-invite.*accept-invite.html" /Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js
   ```
   Output MUST contain the rewrite.
2. File still under 10 KB cap:
   ```bash
   awk 'END{print NR " lines, " ARGV[1]}' /Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js
   wc -c /Users/jeet/turion-space-demo/cf-function-source/turion-clean-urls.js
   ```
   Byte count MUST be less than 10240.
3. CF Function describes the new code:
   ```bash
   aws cloudfront describe-function --name turion-clean-urls --stage LIVE --query 'FunctionMetadata.Stage' --output text
   ```
   Expected: `LIVE`.
   ```bash
   aws cloudfront get-function --name turion-clean-urls --stage LIVE --output text 2>/dev/null | grep -c "/accept-invite"
   ```
   Expected: at least `1`.
4. Invalidation completed:
   ```bash
   aws cloudfront list-invalidations --distribution-id E37R9PT8IL44L2 --query 'InvalidationList.Items[0].[Id,Status]' --output text
   ```
   Status MUST be `Completed` (wait if `InProgress`).
  </verify>
  <done>
CF Function `R` map updated with `/accept-invite` rewrite; size still under 10 KB; published to LIVE; invalidation completed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Deploy frontend to S3 + smoke test both pages live</name>
  <files>/Users/jeet/turion-space-demo/deploy-frontend.sh</files>
  <action>
Run the existing frontend deploy script (do NOT modify unless a real bug is found — file appears in `files_modified` only because the deploy run touches its mtime):

```bash
cd /Users/jeet/turion-space-demo
./deploy-frontend.sh
```

This script: `aws s3 sync` to `turion-demo-static` bucket + `aws cloudfront create-invalidation /*` on `E37R9PT8IL44L2`.

Then smoke test BOTH pages live:

```bash
# 1. /team — should respond 200 (clean URL, served via CF Function rewrite to /team.html OR direct from S3 if no rewrite needed for /team)
curl -sI -o /tmp/team-headers.txt -w "GET /team: %{http_code}\n" https://turion.zietra.com/team
# 2. /team.html — fallback path, should also 200
curl -sI -o /dev/null -w "GET /team.html: %{http_code}\n" https://turion.zietra.com/team.html
# 3. /accept-invite — clean URL via CF Function rewrite → /accept-invite.html
curl -sI -o /dev/null -w "GET /accept-invite: %{http_code}\n" https://turion.zietra.com/accept-invite
# 4. /accept-invite.html — direct S3 path
curl -sI -o /dev/null -w "GET /accept-invite.html: %{http_code}\n" https://turion.zietra.com/accept-invite.html
# 5. Confirm /accept-invite content has the referrer meta + no requireSession
curl -s https://turion.zietra.com/accept-invite | grep -E "name=\"referrer\".*no-referrer|requireSession" | sort -u
```

Expected output:
```
GET /team: 200
GET /team.html: 200
GET /accept-invite: 200
GET /accept-invite.html: 200
<meta name="referrer" content="no-referrer">
```
(The 5th line should ONLY contain the referrer meta — `requireSession` should NOT appear in the served HTML.)

Confirm the served `/team` content has the new structure (NOT the old stub):

```bash
curl -s https://turion.zietra.com/team | grep -c "z-stub\|Status: Ships in Phase 54.1"
```

Expected: `0`.

```bash
curl -s https://turion.zietra.com/team | grep -c "/api/team\|inviteModal"
```

Expected: at least `2`.

If any 4xx/5xx, debug:
- 403 on /accept-invite → S3 file not synced (re-run deploy-frontend.sh)
- 200 on /team but old stub content → CF cached; re-invalidate `/team`, `/team.html`
- /accept-invite returns 404 → CF Function not yet published or rewrite not in R map; re-check Task 3

Document the deploy-frontend.sh output (S3 sync byte count + invalidation ID) in the SUMMARY.

Git: typically nothing to commit (deploy-frontend.sh untouched). Skip if no diff.
  </action>
  <verify>
1. Both pages serve 200:
   ```bash
   for path in /team /team.html /accept-invite /accept-invite.html; do
     code=$(curl -s -o /dev/null -w "%{http_code}" "https://turion.zietra.com$path")
     echo "$path: $code"
   done
   ```
   Expected: all 4 lines end with `200`.
2. /team serves the NEW content (NOT the stub):
   ```bash
   curl -s https://turion.zietra.com/team | grep -c "Status: Ships in Phase\|z-stub"
   ```
   Expected: `0`.
3. /team has the new API call:
   ```bash
   curl -s https://turion.zietra.com/team | grep -c "/api/team"
   ```
   Expected: at least `2`.
4. /accept-invite has the referrer meta:
   ```bash
   curl -s https://turion.zietra.com/accept-invite | grep -c 'name="referrer" content="no-referrer"'
   ```
   Expected: `1`.
5. /accept-invite is PUBLIC (no requireSession):
   ```bash
   curl -s https://turion.zietra.com/accept-invite | grep -c "requireSession"
   ```
   Expected: `0`.
6. Both pages reference the correct backend paths:
   ```bash
   curl -s https://turion.zietra.com/accept-invite | grep -c "/api/invites/accept-invite"
   ```
   Expected: `1`.
  </verify>
  <done>
Both pages live at https://turion.zietra.com/team and https://turion.zietra.com/accept-invite, returning 200 with the correct content (no Phase 54 stub remnants on /team; referrer meta + correct API path on /accept-invite).
  </done>
</task>

</tasks>

<verification>
After all tasks:

1. **team.html rewritten:**
   - File ≥ 4500 bytes (was 1864 B stub)
   - Contains real `<main class="z-team">` + member table + invite modal
   - Calls `/api/team` and `/api/team/invite` via window.erpApi
   - inject-shell.mjs reports `injected: 0` (marker preserved)
   - NO `z-stub` references; NO Phase 54 placeholder copy
2. **accept-invite.html created:**
   - PUBLIC page (no requireSession)
   - `<meta name="referrer" content="no-referrer">` present
   - `history.replaceState` clears token from URL
   - POSTs to `/api/invites/accept-invite`
   - Success → `cognitoAuth.signInWithMagicLink` → 4s redirect to `/erp-login.html`
   - 4xx/410 → shows `/signup` CTA
3. **CloudFront Function updated:**
   - R map has new `/accept-invite` → `/accept-invite.html` entry
   - File still under 10 KB cap
   - Published to LIVE stage
   - Distribution invalidation completed
4. **Both pages live + verified via curl:**
   - `https://turion.zietra.com/team` → 200, new content
   - `https://turion.zietra.com/accept-invite` → 200, referrer meta present
   - `/team.html` and `/accept-invite.html` direct paths also 200
5. **No regression to inherited contracts:**
   - inject-shell.mjs idempotency PRESERVED (Phase 54 invariant)
   - Phase 54 nav + shell still visible on /team (head section unchanged)
   - 4 zietra-cognito-* Lambdas NOT touched
   - apex zietra.com distribution NOT touched
   - Other turion-space-demo HTML pages NOT touched
</verification>

<success_criteria>
- [ ] team.html replaces Phase 54 stub; new size ≥ 4500 B; no z-stub references
- [ ] team.html role/status/dropdown options ALL derived from API/role state, NOT hardcoded
- [ ] team.html invite + remove buttons POST to live backend (no toast-only no-ops)
- [ ] inject-shell.mjs reports `injected: 0` after team.html rewrite
- [ ] accept-invite.html created with referrer meta + URL clear + magic-link success path
- [ ] CF Function R map has /accept-invite entry; file still under 10 KB; published to LIVE
- [ ] CloudFront invalidation completed
- [ ] Live curl: /team and /accept-invite both 200
- [ ] Live /team content contains "/api/team" and "inviteModal"
- [ ] Live /accept-invite content contains referrer meta + /api/invites/accept-invite path
- [ ] Phase 54 stub artifacts gone from production
</success_criteria>

<output>
After completion, create `.planning/phases/54.1-m6-multi-user-per-tenant-team-invites-role-middleware/54.1-03-SUMMARY.md` documenting:
- 3 file paths created/modified + commit hashes
- inject-shell.mjs run output (proving `injected: 0`)
- CF Function ETag before/after + publish output + invalidation ID + Status:Completed
- 6-line curl smoke output (team + team.html + accept-invite + accept-invite.html status codes + referrer meta presence + signInWithMagicLink presence)
- Deploy log: deploy-frontend.sh S3 sync byte count + final CF invalidation ID
- Confirmation that Phase 54 nav/shell unchanged + inject-shell idempotency holds
- Open items deferred:
  - In-place re-render after invite (currently `window.location.reload()` — defer optimization to 54.3)
  - Resend-invite UX from the UI (just close + reload triggers same flow; explicit "Resend" button is 54.5 if needed)
  - Bulk invite via CSV — explicitly out of scope per CONTEXT.md
</output>
