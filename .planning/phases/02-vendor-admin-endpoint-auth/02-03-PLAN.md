---
phase: 02-vendor-admin-endpoint-auth
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - apps/web/p2p-platform/backend/main_new.py
autonomous: true
requirements: [AUTH-05, AUTH-06]
must_haves:
  truths:
    - "All admin portal endpoints (invoice, client, dashboard, ticket, accounting, procurement) use Depends(require_admin) instead of get_current_user"
    - "Chat endpoints use Depends(require_any_auth) since they serve customers, drivers, and admin"
    - "Delivery decision endpoints use Depends(require_vendor) since they are restaurant-only operations"
    - "GET /api/orders uses Depends(require_any_auth) with existing admin-vs-non-admin filter logic preserved"
    - "Zero endpoints in the codebase rely solely on global middleware for auth -- every endpoint has explicit Depends()"
    - "A grep audit confirms no endpoint handler function lacks an auth dependency parameter"
  artifacts:
    - path: "apps/web/p2p-platform/backend/main_new.py"
      provides: "~70 admin portal/ERP endpoints with per-endpoint Depends() auth"
      contains: "Depends(require_admin)"
  key_links:
    - from: "main_new.py admin portal endpoints"
      to: "auth_utils.require_admin"
      via: "Depends(require_admin)"
      pattern: "Depends\\(require_admin\\)"
    - from: "main_new.py chat endpoints"
      to: "auth_utils.require_any_auth"
      via: "Depends(require_any_auth)"
      pattern: "Depends\\(require_any_auth\\)"
---

<objective>
Convert all remaining ~70 admin portal/ERP endpoints from get_current_user to role-appropriate Depends(), then verify AUTH-06 (zero endpoints rely solely on global middleware -- all have explicit Depends()).

Purpose: Satisfy AUTH-05 (all remaining middleware-only endpoints converted) and AUTH-06 (zero endpoints without explicit Depends()), completing the entire API surface auth migration.
Output: main_new.py with zero get_current_user callers in endpoint signatures (except GET /api/auth/me), and a verification grep confirming every endpoint has explicit auth.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vendor-admin-endpoint-auth/02-RESEARCH.md
@.planning/phases/02-vendor-admin-endpoint-auth/02-01-SUMMARY.md
@.planning/phases/02-vendor-admin-endpoint-auth/02-02-SUMMARY.md
@apps/web/p2p-platform/backend/auth_utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert admin portal endpoints (invoice, client, dashboard, ticket, accounting, procurement) to Depends(require_admin)</name>
  <files>apps/web/p2p-platform/backend/main_new.py</files>
  <action>
Convert all Group C admin portal endpoints from `current_user: User = Depends(get_current_user)` to `admin: User = Depends(require_admin)`.

**These are all admin-only portal features.** The pattern is the same for each: replace `current_user: User = Depends(get_current_user)` with `admin: User = Depends(require_admin)`, then replace any `current_user` references in the body with `admin`.

**C1. Client endpoints (5 endpoints):**
Lines ~7299, ~7308, ~7324, ~7332, ~7346

**C2. Invoice endpoints (15-16 endpoints):**
Lines ~7363, ~7418, ~7466, ~7566, ~7580, ~7594, ~7609, ~7634, ~7648, ~7752, ~7801, ~7853, ~7903, ~7956, ~8002, ~8062

**C3. Dashboard endpoints (2 endpoints):**
Lines ~8096, ~8176

**C4. Coupa dashboard/procurement endpoints (10-11 endpoints):**
Lines ~8331, ~8376, ~8425, ~8464, ~8512, ~8561, ~8606, ~8641, ~8660, ~8679, ~8698

**C6. Accounting endpoints (~4-5 endpoints):**
Lines ~9005, ~9143, ~9250, ~9314, ~9366

**C7. Ticket/helpdesk endpoints (8 endpoints):**
Lines ~9250, ~9314, ~9366, ~9419, ~9451, ~9486, ~9521, ~9582

**C10. Debug endpoint (1 endpoint):**
Line ~19127: GET /api/debug/order/{id} -- use `admin: User = Depends(require_admin)`. This is already blocked in production via IS_PRODUCTION check, but explicit auth adds defense-in-depth.

**C11. Admin vendor docs summary (1 endpoint):**
Line ~12985 -- verify current state; convert if still using get_current_user.

**SPECIAL CASES -- NOT require_admin:**

**C5. GET /api/orders (line ~8724):**
Use `_auth: dict = Depends(require_any_auth)` (NOT require_admin). This endpoint has conditional logic where admins see all orders and non-admins see their own. Preserve the existing admin-vs-non-admin filter logic. Replace `current_user` usage with JWT payload data from `_auth` dict (use `_auth.get("sub")` for email, `_auth.get("role")` for role check).

**C8. Chat endpoints (4 endpoints -- lines ~16086, ~16769, ~16837, ~16880):**
Use `_auth: dict = Depends(require_any_auth)` (NOT require_admin). Chat is used by customers, drivers, AND admin. If the endpoint currently uses `current_user` for anything beyond auth check (e.g., logging who sent a message), extract the needed info from `_auth` dict.

**C9. Delivery decision endpoints (3-4 endpoints -- lines ~16109, ~16151, ~16247):**
Use `_auth_vendor: Vendor = Depends(require_vendor)` (NOT require_admin). These are restaurant-only operations (start-delivery-decision, confirm-delivery-decision, delivery-decision-status). If the endpoint has a vendor_id path param, add ownership check.

**IMPORTANT:** Line numbers are approximate. Grep for each endpoint path to find exact current line. Some endpoints may have been partially converted in v1.2 Phase 01 -- check current state before editing.
  </action>
  <verify>
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && grep -n "Depends(get_current_user)" main_new.py | grep -v "^.*#" | grep -v "def get_current_user"` -- should show only GET /api/auth/me (the legitimate any-role user info endpoint).
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && python -c "import main_new; print('Import OK')"` -- should succeed.
  </verify>
  <done>All ~70 admin portal/ERP endpoints converted. Admin-only endpoints use require_admin. Chat uses require_any_auth. Delivery decisions use require_vendor. GET /api/orders uses require_any_auth with preserved filter logic. get_current_user only remains on GET /api/auth/me.</done>
</task>

<task type="auto">
  <name>Task 2: Final AUTH-06 verification audit and test suite validation</name>
  <files>apps/web/p2p-platform/backend/main_new.py</files>
  <action>
Run a comprehensive audit to verify AUTH-06: zero endpoints rely solely on global middleware for auth.

**Step 1: Verify get_current_user usage**
Run: `grep -n "Depends(get_current_user)" main_new.py` -- should ONLY show GET /api/auth/me.
If any other endpoint still uses get_current_user, convert it to the appropriate Depends() based on its purpose.

**Step 2: Verify get_current_vendor usage**
Run: `grep -n "Depends(get_current_vendor)" main_new.py` -- should return ZERO matches in endpoint signatures.
If any remain, convert to Depends(require_vendor).

**Step 3: Verify no manual oauth2_scheme remains on convertible endpoints**
Run: `grep -n "token.*Depends(oauth2_scheme)" main_new.py` -- should only show ADMIN_SECRET_KEY endpoints (backfill-payouts, migrate) and any legitimate token-based flows.

**Step 4: Check for endpoints with NO auth parameter at all**
This is the key AUTH-06 check. For every `@app.get`, `@app.post`, `@app.put`, `@app.patch`, `@app.delete` decorated function, verify it has at least one `Depends()` auth parameter OR is in the public path allowlist.

Strategy: grep for endpoint decorators and check the function signature that follows. Specifically look for functions whose only Depends is `get_db` with no auth parameter. Cross-reference against the public path allowlist in `require_auth_middleware`.

**Step 5: Run full test suite**
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && pytest tests/ -v 2>&1 | tail -20` -- all tests should pass.

**Step 6: Fix any gaps found in Steps 1-4**
If any endpoint is found without explicit auth and is NOT in the public allowlist, add the appropriate Depends() based on its purpose:
- Admin endpoint (/api/admin/*) -> Depends(require_admin)
- Vendor endpoint (/api/vendor/*) -> Depends(require_vendor)
- Driver endpoint (/api/driver/*) -> Depends(require_driver)
- Customer endpoint (/api/customer/*) -> Depends(require_customer)
- Multi-role endpoint -> Depends(require_any_auth)
  </action>
  <verify>
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && grep -c "Depends(get_current_user)" main_new.py` -- should be exactly 1 (GET /api/auth/me) or 0 if converted to require_any_auth.
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && grep -c "Depends(get_current_vendor)" main_new.py` -- should be 0.
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && pytest tests/ -x -q 2>&1 | tail -5` -- all tests pass.
  </verify>
  <done>AUTH-06 verified: zero endpoints rely solely on global middleware. Every endpoint has explicit Depends() auth or is in the public path allowlist. Full test suite passes. get_current_user has at most 1 caller (GET /api/auth/me). get_current_vendor has 0 callers in endpoint signatures.</done>
</task>

</tasks>

<verification>
1. `grep -c "Depends(get_current_user)" main_new.py` returns 0 or 1 (only /api/auth/me)
2. `grep -c "Depends(get_current_vendor)" main_new.py` returns 0
3. Every `@app.` decorated endpoint function has at least one auth Depends() parameter or is in public allowlist
4. Chat endpoints use require_any_auth (multi-role)
5. Delivery decision endpoints use require_vendor (restaurant-only)
6. GET /api/orders preserves admin-vs-non-admin conditional filter logic
7. `python -c "import main_new"` succeeds
8. `pytest tests/ -v` passes -- all tests pass, no regressions
</verification>

<success_criteria>
- All ~70 admin portal endpoints converted to role-appropriate Depends()
- Zero get_current_user callers remain in endpoint signatures (except possibly GET /api/auth/me)
- Zero get_current_vendor callers remain in endpoint signatures
- AUTH-06 audit passes: every endpoint has explicit Depends() or is in public allowlist
- Full test suite passes with no regressions
- Phase 02 requirements AUTH-03, AUTH-04, AUTH-05, AUTH-06 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-vendor-admin-endpoint-auth/02-03-SUMMARY.md`
</output>
