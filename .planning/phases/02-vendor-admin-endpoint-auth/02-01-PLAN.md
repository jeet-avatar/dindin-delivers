---
phase: 02-vendor-admin-endpoint-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/p2p-platform/backend/main_new.py
autonomous: true
requirements: [AUTH-03]
must_haves:
  truths:
    - "Every vendor endpoint rejects requests without a valid vendor JWT (returns 401)"
    - "Vendor endpoints with {vendor_id} path param verify the authenticated vendor owns that vendor_id (returns 403 on mismatch)"
    - "Vendor self-access endpoints (/api/vendor/profile, /api/vendor/earnings, etc.) use require_vendor without needing path param ownership check"
    - "Vendor Stripe endpoints replace manual JWT decode blocks with Depends(require_vendor)"
    - "get_current_vendor has zero callers in endpoint signatures after conversion"
  artifacts:
    - path: "apps/web/p2p-platform/backend/main_new.py"
      provides: "31 vendor endpoints with Depends(require_vendor) + ownership checks"
      contains: "Depends(require_vendor)"
  key_links:
    - from: "main_new.py vendor endpoints"
      to: "auth_utils.require_vendor"
      via: "Depends(require_vendor)"
      pattern: "_auth_vendor.*Depends\\(require_vendor\\)"
---

<objective>
Convert all 31 vendor endpoints in main_new.py from ad-hoc auth (get_current_user, get_current_vendor, manual JWT decode) to standardized Depends(require_vendor) with ownership checks on all {vendor_id} path parameter endpoints.

Purpose: Satisfy AUTH-03 -- every vendor endpoint has per-endpoint Depends(require_vendor) with ownership verification, preventing cross-vendor data access.
Output: main_new.py with all vendor endpoints using require_vendor from auth_utils.py.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-vendor-admin-endpoint-auth/02-RESEARCH.md
@apps/web/p2p-platform/backend/auth_utils.py
@.planning/phases/01-customer-driver-endpoint-auth/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert vendor endpoints using get_current_user and get_current_vendor to Depends(require_vendor)</name>
  <files>apps/web/p2p-platform/backend/main_new.py</files>
  <action>
Convert all Group A1 + A2 + A4 vendor endpoints (25 endpoints total) from get_current_user/get_current_vendor to Depends(require_vendor) with ownership checks.

**Ensure `require_vendor` is imported from auth_utils** at the top of main_new.py (it should already be imported from Phase 01 work -- verify with grep).

**Pattern for {vendor_id} path param endpoints (A1 + A4 subset):**
Replace `current_user: User = Depends(get_current_user)` with `_auth_vendor: Vendor = Depends(require_vendor)`.
Remove any manual role checks like `if current_user.role != UserRole.ADMIN and current_user.vendor_id != vendor_id`.
Add ownership check: `if _auth_vendor.id != vendor_id: raise HTTPException(status_code=403, detail="Access denied - not your vendor account")`.
Remove any admin-bypass checks per Phase 01 decision (admin uses admin endpoints).

**Pattern for self-access endpoints (A2 + A4 subset -- /api/vendor/profile, /api/vendor/earnings, etc.):**
Replace `current_user: User = Depends(get_current_user)` or `current_vendor: Vendor = Depends(get_current_vendor)` with `vendor: Vendor = Depends(require_vendor)`.
Remove manual role checks and `current_user.vendor_id` lookups -- `require_vendor` already returns the Vendor ORM object.
Use `vendor.id` directly where the old code used `current_user.vendor_id`.

**Specific endpoints by research line numbers:**

A1 (14 endpoints using get_current_user):
- Line ~10519: GET /api/vendors/{vendor_id} -- ownership check
- Line ~10533: GET /api/vendor/profile -- self-access (no path param)
- Line ~10847: PUT /api/vendors/{vendor_id} -- ownership check
- Line ~10898: PATCH /api/vendors/{vendor_id} -- ownership check
- Line ~11186: PUT /api/vendors/{vendor_id}/online-status -- ownership check
- Line ~11296: GET /api/vendors/{vendor_id}/documents -- ownership check
- Line ~11345: POST /api/vendors/{vendor_id}/documents -- ownership check
- Line ~11408: DELETE /api/vendors/{vendor_id}/documents/{id} -- ownership check
- Line ~11435: PATCH /api/vendors/{vendor_id}/documents -- ownership check
- Line ~11728: GET /api/vendor/my-documents -- self-access
- Line ~11807: POST /api/vendor/my-documents/upload -- self-access
- Line ~13598: POST /api/vendors/{vendor_id}/menu -- ownership check

A2 (4 endpoints using get_current_vendor):
- Line ~10547: GET /api/vendor/earnings -- self-access
- Line ~10629: GET /api/vendor/kot-config -- self-access
- Line ~10656: PUT /api/vendor/kot-config -- self-access
- Line ~10726: POST /api/vendor/kot-test -- self-access

A4 (7 endpoints needing different auth -- vendor subset):
- Line ~13744: PUT /api/vendors/{id}/menu/{item_id} -- ADD MISSING ownership check + require_vendor
- Line ~13800: DELETE /api/vendors/{id}/menu/{item_id} -- ADD MISSING ownership check + require_vendor
- Line ~17483: GET /api/vendors/{id}/reviews -- ownership check + require_vendor
- Line ~17948: POST /api/erp/vendors/{id}/fcm-token -- ADD MISSING ownership check + require_vendor
- Line ~18001: DELETE /api/erp/vendors/{id}/fcm-token -- ADD MISSING ownership check + require_vendor
- Line ~20918: GET /api/vendors/{id}/ai-insights -- ownership check + require_vendor

**Admin-only endpoints in A1/A4 (convert to require_admin, NOT require_vendor):**
- Line ~10225: POST /api/vendors (creates vendor) -- use `admin: User = Depends(require_admin)`
- Line ~11457: DELETE /api/vendors/{vendor_id} -- use `admin: User = Depends(require_admin)`
- Line ~10941: PATCH /api/vendors/{id}/status -- use `admin: User = Depends(require_admin)`
- Line ~11241: POST /api/vendors/{id}/create-account -- use `admin: User = Depends(require_admin)`

**CRITICAL: Do NOT add admin bypass to vendor ownership checks.** Phase 01 decision: admin uses admin-specific endpoints, not vendor endpoints.
  </action>
  <verify>
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && grep -n "get_current_vendor\b" main_new.py | grep -v "^.*#" | grep -v "def get_current_vendor"` -- should return ZERO matches in endpoint signatures.
Run: `grep -n "Depends(require_vendor)" main_new.py | wc -l` -- should be >= 21 (existing + new conversions).
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && python -c "import main_new; print('Import OK')"` -- should succeed without errors.
  </verify>
  <done>All 25 vendor endpoints (A1+A2+A4 vendor subset) use Depends(require_vendor) with ownership checks on {vendor_id} path param endpoints. 4 admin-only vendor management endpoints use Depends(require_admin). get_current_vendor has zero callers in endpoint signatures.</done>
</task>

<task type="auto">
  <name>Task 2: Convert vendor Stripe endpoints from manual JWT decode to Depends(require_vendor)</name>
  <files>apps/web/p2p-platform/backend/main_new.py</files>
  <action>
Convert all Group A3 vendor endpoints (6 endpoints) that use manual `jwt.decode()` + `token: str = Depends(oauth2_scheme)` to `Depends(require_vendor)` with ownership checks.

**Pattern for each endpoint:**
1. Replace `token: str = Depends(oauth2_scheme)` with `_auth_vendor: Vendor = Depends(require_vendor)` in the function signature
2. Remove the entire `try: payload = jwt.decode(...)` block (typically 6-10 lines)
3. Add ownership check: `if _auth_vendor.id != vendor_id: raise HTTPException(status_code=403, detail="Access denied - not your vendor account")`
4. Keep the rest of the function body unchanged

**Specific endpoints (from research):**
- Line ~5003: POST /api/vendors/{id}/stripe/connect
- Line ~5081: GET /api/vendors/{id}/stripe/onboarding-link
- Line ~5159: GET /api/vendors/{id}/stripe/status
- Line ~5245: POST /api/vendors/{id}/stripe/dashboard-link
- Line ~12709: PATCH /api/vendors/{id}/location
- Line ~13827: POST /api/vendors/{id}/register-app

**Also convert:**
- Line ~12747: POST /api/vendors/{id}/quick-publish -- this is admin-only (manual JWT admin check), convert to `admin: User = Depends(require_admin)` and remove the JWT decode block.

**IMPORTANT:** These endpoints currently have `token_role != "admin"` checks as part of the JWT decode. Per Phase 01 decision, remove the admin bypass entirely -- vendor endpoints accept vendor JWTs only. Quick-publish is an exception because it IS admin-only.

**WARNING:** Some of these endpoints may have already been partially converted in v1.2 Phase 01 (finish-endpoint-auth). Grep for the current state of each endpoint BEFORE editing. If already using `Depends(require_vendor)`, skip it.
  </action>
  <verify>
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && grep -n "token.*Depends(oauth2_scheme)" main_new.py` -- count should be significantly reduced (only ADMIN_SECRET_KEY endpoints should remain).
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && python -c "import main_new; print('Import OK')"` -- should succeed.
Run: `cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && pytest tests/ -x -q 2>&1 | tail -5` -- no new failures.
  </verify>
  <done>All 6 vendor Stripe/location/register endpoints use Depends(require_vendor) with ownership checks. Manual JWT decode blocks removed. Quick-publish uses Depends(require_admin). No token: str = Depends(oauth2_scheme) remains on any vendor endpoint.</done>
</task>

</tasks>

<verification>
1. `grep -cn "Depends(get_current_vendor)" main_new.py` returns 0 (excluding function definitions)
2. `grep -cn "Depends(get_current_user)" main_new.py` -- count should be reduced by 20+ from pre-Phase-02 count (remaining are admin/portal endpoints for Plan 02/03)
3. Every vendor endpoint with `{vendor_id}` path param has a `_auth_vendor.id != vendor_id` ownership check
4. `python -c "import main_new"` succeeds
5. `pytest tests/ -x -q` passes with no new failures
</verification>

<success_criteria>
- All 31 vendor endpoints converted: 25 to require_vendor, 6 to require_admin (admin-only vendor management)
- Zero get_current_vendor callers in endpoint signatures
- Ownership checks on all {vendor_id} path param endpoints
- Missing ownership checks ADDED on menu edit, menu delete, FCM token, reviews, ai-insights endpoints
- No admin bypass in any vendor endpoint
- Tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-vendor-admin-endpoint-auth/02-01-SUMMARY.md`
</output>
