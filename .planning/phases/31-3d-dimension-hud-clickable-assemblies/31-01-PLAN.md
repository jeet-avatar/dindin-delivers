---
phase: 31-3d-dimension-hud-clickable-assemblies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/turion-satellite/backend/src/routes/parts.ts
  - /Users/jeet/turion-satellite/backend/tests/parts.test.ts
autonomous: true
requirements: [ChildrenSpecsAPI]

must_haves:
  truths:
    - "GET /api/parts/:partDefId/children?sat= returns a `specifications` field on every child row"
    - "The existing /children behaviour (?sat= 400 guard, auth, bl.status='released', JOINs, ORDER BY, the 500-no-leak path) is unchanged"
    - "`npx vitest run` in turion-satellite/backend stays green (all suites), with parts.test.ts asserting the new `specifications` field"
  artifacts:
    - path: "/Users/jeet/turion-satellite/backend/src/routes/parts.ts"
      provides: "GET /api/parts/:partDefId/children SELECT with c_pd.specifications AS specifications"
      contains: "c_pd.specifications AS specifications"
    - path: "/Users/jeet/turion-satellite/backend/tests/parts.test.ts"
      provides: "Updated mock child rows + an assertion on res.body[].specifications"
      contains: "specifications"
  key_links:
    - from: "/Users/jeet/turion-satellite/backend/src/routes/parts.ts"
      to: "part_definitions.specifications"
      via: "SELECT column in the /children query"
      pattern: "c_pd\\.specifications"
---

<objective>
Add the part `specifications` JSONB to the `GET /api/parts/:partDefId/children` response so the Phase-30 3D viewer (extended in Plan 31-02) has each BOM child's real `dimensions_mm` / `weight_grams` / `material` without N+1 fetches.

Purpose: The multi-mesh assembly viewer + dimension HUD (Plans 31-02 / 31-03) need each child's `specifications`; today `/children` only returns thumbnail/identity columns. This is the smallest possible backend change — one SELECT column, no DB migration (the `specifications` column has existed on `part_definitions` since migration 009, `DEFAULT '{}'::jsonb`).
Output: `parts.ts` `/children` SELECT gains `c_pd.specifications AS specifications`; `parts.test.ts` mock child rows gain a `specifications` object + one assertion. (Deploy of the rebuilt Lambda happens in Plan 31-04.)
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-3d-dimension-hud-clickable-assemblies/31-RESEARCH.md

# Backend route + test live in a sibling repo, not in doordash-p2p:
@/Users/jeet/turion-satellite/backend/src/routes/parts.ts
@/Users/jeet/turion-satellite/backend/tests/parts.test.ts
@/Users/jeet/turion-satellite/backend/src/app.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `specifications` to the /children SELECT + cover it in parts.test.ts</name>
  <files>/Users/jeet/turion-satellite/backend/src/routes/parts.ts, /Users/jeet/turion-satellite/backend/tests/parts.test.ts</files>
  <action>
In `/Users/jeet/turion-satellite/backend/src/routes/parts.ts`, find the `GET /api/parts/:partDefId/children` handler (route registered via `router.get('/:partDefId/children', ...)`; mounted at `/api/parts` in `app.ts:29`; the handler is around line 285-325 and has a `WITH parent AS (SELECT id FROM part_instances WHERE part_definition_id = $1 AND satellite_id = $2 LIMIT 1)` CTE, an `if (!satId) { res.status(400)... }` guard, `WHERE bl.status = 'released'`, and a SELECT column list ending `...c_pd.drawing_svg, s.code AS subsystem_code, s.label AS subsystem_label`).

Add ONE column to the SELECT column list, immediately after `s.label AS subsystem_label` (i.e. as the last column before `FROM`):
```
        c_pd.specifications AS specifications
```
(`c_pd` is the alias for the joined child `part_definitions` row — verify the actual alias used in this query and match it; the research used `c_pd`, but use whatever the file uses for the child part_definitions table.) Remember the new column needs a trailing comma added to the line above it. Change NOTHING else in this query — not the `WITH parent` CTE, not the JOINs, not the WHERE, not the ORDER BY, not the `?sat=` 400 guard, not the auth, not the 500-no-leak catch. Do NOT touch the `/bom/tree` route or any other route in the file.

In `/Users/jeet/turion-satellite/backend/tests/parts.test.ts`, find `describe('GET /api/parts/:partDefId/children'...)` (around line 317-396). It mocks `query()` via `vi.mocked(query).mockImplementation(...)` returning two hardcoded mock child rows, then asserts things like `res.body[0].part_number`, `res.body[1].drawing_svg`, and `capturedParams` equal to `['pd-parent-1', 'sat-uuid-1']`. Because `query()` is mocked, the SELECT-column change is invisible to the mock — so:
1. Add a `specifications` property to EACH of the two mock child rows (keep all existing properties), e.g. `specifications: { dimensions_mm: { length: 18, width: 6, height: 6 }, weight_grams: 4, material: 'Stainless 302' }` on the first and a different plausible object on the second.
2. Add ONE new assertion in the success-path test, e.g. `expect(res.body[0].specifications).toMatchObject({ dimensions_mm: { length: 18 }, weight_grams: 4 });` — pick values that match whatever you put in the first mock row.
Do NOT change any existing assertion, the `?sat=` 400 test, the auth test, or the 500-no-leak test. Do NOT edit `bom-tree.test.ts` (different route, untouched by this change).
  </action>
  <verify>
`cd /Users/jeet/turion-satellite/backend && npx tsc --noEmit` exits 0 (no type errors from the SELECT change — it's a string).
`cd /Users/jeet/turion-satellite/backend && npx vitest run` — ALL suites green, including the updated `parts.test.ts` (the new `specifications` assertion passes; nothing else regresses; `bom-tree.test.ts` still green).
`grep -n "c_pd.specifications" /Users/jeet/turion-satellite/backend/src/routes/parts.ts` shows exactly one match, inside the `/children` SELECT.
  </verify>
  <done>
`parts.ts` `/children` SELECT includes `c_pd.specifications AS specifications` (and nothing else changed in that query or file); `parts.test.ts`'s two mock child rows carry a `specifications` object and the success-path test asserts on `res.body[0].specifications`; `npx tsc --noEmit` clean and `npx vitest run` all green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit the backend change (do NOT push or redeploy — Plan 31-04 owns deploy)</name>
  <files>/Users/jeet/turion-satellite/backend/src/routes/parts.ts, /Users/jeet/turion-satellite/backend/tests/parts.test.ts</files>
  <action>
In the `/Users/jeet/turion-satellite` repo, commit the two edited files with the correct author identity (per MEMORY: `user.email="jm@techcloudpro.com"` and `user.name="jeet-avatar"` for `github.com/jeet-avatar/*`):
```
cd /Users/jeet/turion-satellite && git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit -m "feat(31-01): add specifications to GET /api/parts/:partDefId/children SELECT" -- backend/src/routes/parts.ts backend/tests/parts.test.ts
```
Do NOT `git push` — Plan 31-04 (the deploy wave) owns the push + `./build-and-push.sh` Lambda redeploy. Do NOT run `./build-and-push.sh` here. If `dist/` or other unrelated files are dirty, do not stage them — commit only the two files named above (use the explicit `-- <files>` form).
  </action>
  <verify>
`cd /Users/jeet/turion-satellite && git log -1 --format='%an <%ae> %s'` shows `jeet-avatar <jm@techcloudpro.com> feat(31-01): add specifications to GET /api/parts/:partDefId/children SELECT`.
`cd /Users/jeet/turion-satellite && git show --stat HEAD` lists exactly `backend/src/routes/parts.ts` and `backend/tests/parts.test.ts`.
`cd /Users/jeet/turion-satellite && git log origin/main..HEAD --oneline` shows the new commit is local-only (not pushed).
  </verify>
  <done>
One local commit on `turion-satellite` main authored `jeet-avatar <jm@techcloudpro.com>` touching only `backend/src/routes/parts.ts` + `backend/tests/parts.test.ts`; not pushed; Lambda not redeployed (that's Plan 31-04).
  </done>
</task>

</tasks>

<verification>
- `grep -n "c_pd.specifications AS specifications" /Users/jeet/turion-satellite/backend/src/routes/parts.ts` → 1 match (inside `/children` SELECT)
- `cd /Users/jeet/turion-satellite/backend && npx vitest run` → all suites pass; `parts.test.ts` asserts the new `specifications` field; `bom-tree.test.ts` unchanged + green
- `cd /Users/jeet/turion-satellite/backend && npx tsc --noEmit` → exit 0
- `git -C /Users/jeet/turion-satellite log -1 --format='%an <%ae> %s'` → `jeet-avatar <jm@techcloudpro.com> feat(31-01): ...`
- NO DB migration created; NO `./build-and-push.sh` run; commit NOT pushed
</verification>

<success_criteria>
- `GET /api/parts/:partDefId/children?sat=` SELECT returns each child's `specifications` JSONB (one added column; everything else byte-identical)
- `parts.test.ts` mock child rows include `specifications` + one assertion; full backend test suite green; `tsc --noEmit` clean
- Single local commit on `turion-satellite` authored `jeet-avatar <jm@techcloudpro.com>`, scoped to the two files, not pushed
- No DB migration; Lambda redeploy deferred to Plan 31-04
</success_criteria>

<output>
After completion, create `.planning/phases/31-3d-dimension-hud-clickable-assemblies/31-01-SUMMARY.md`
</output>
