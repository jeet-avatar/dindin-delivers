---
phase: 31-3d-dimension-hud-clickable-assemblies
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/turion-space-demo/satellite/satellite-3d.js
autonomous: true
requirements: [AssemblyMultiMesh, RaycastPicker]

must_haves:
  truths:
    - "mount3DViewer with no `opts.assemblyChildren` behaves EXACTLY as Phase 30 (single normalized mesh, same camera framing, same dispose)"
    - "mount3DViewer with a non-empty `opts.assemblyChildren` array renders N child meshes (one per child via buildPartMesh) laid out on a self-scaling radial ring instead of the single mesh"
    - "Hovering a child mesh highlights it (emissive boost using the child's subsystem palette accent) and restores the original emissive on pointer-out / pointer-leave (no stuck-lit children)"
    - "Clicking a child selects it: camera tweens to frame it (controls disabled during the tween), the child stays lit, and opts.onSelect(childData) fires"
    - "viewerHandle.deselect() (also called by clicking empty canvas space) un-highlights, tweens the camera back to the whole-ring framing, and fires opts.onSelect(null)"
    - "dispose() removes the pointermove/click/pointerleave listeners, resets the canvas cursor, and (via the existing scene.traverse) disposes the N child geometries/materials"
    - "Picking raycasts only the N child groups (recursive — children are THREE.Groups of meshes), using canvas-getBoundingClientRect()-relative NDC (NOT window.innerWidth/Height)"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/satellite/satellite-3d.js"
      provides: "mount3DViewer extended with opts.assemblyChildren + opts.onSelect; a layoutAssemblyChildren helper; THREE.Raycaster picker; camera-tween state machine; viewerHandle.deselect()"
      contains: "assemblyChildren"
  key_links:
    - from: "mount3DViewer assembly branch"
      to: "buildPartMesh(childData)"
      via: "one buildPartMesh call per child row, each independently normalized to ~0.9 unit"
      pattern: "buildPartMesh\\("
    - from: "renderer.domElement pointer events"
      to: "THREE.Raycaster.intersectObjects(childGroups, true)"
      via: "canvas-rect-relative NDC → setFromCamera → intersectObjects recursive"
      pattern: "getBoundingClientRect"
    - from: "child select"
      to: "opts.onSelect(childData)"
      via: "selectChild() callback after camera tween starts"
      pattern: "onSelect"
---

<objective>
Extend the Phase-30 `mount3DViewer(containerEl, partData, opts)` in `satellite/satellite-3d.js` so it can render assembly parts as multiple clickable meshes — one per BOM child — laid out on a radial ring, picked with `THREE.Raycaster` + pointer events, with a camera fly-to on select. Leaf parts (no `assemblyChildren`) keep the identical Phase-30 single-mesh path.

Purpose: This is the engine for the Phase-31 "clickable assemblies" feature. Pages (Plan 31-03) just pass `opts.assemblyChildren` (the `/children?sat=` rows, now carrying `specifications` from Plan 31-01) + `opts.onSelect`; this module does the multi-mesh layout + raycasting + camera tween. The HUD stays page-owned — this module only fires `opts.onSelect(childData)` / `opts.onSelect(null)`.
Output: `satellite-3d.js` `mount3DViewer` gains `opts.assemblyChildren` + `opts.onSelect`, a `layoutAssemblyChildren` helper, a raycaster picker, a tween state machine in the RAF loop, and a `viewerHandle.deselect()`. NO change to `buildPartMesh`, `chooseTemplate3D`, `normalizeDims`, `perturbForPartNumber`, `paletteFor3D`, `materialFor`, `isWebGLAvailable`, the single-mesh path, or `3d-test.html` (one optional extra demo cell in 3d-test.html is allowed but not required).
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-3d-dimension-hud-clickable-assemblies/31-RESEARCH.md
@.planning/phases/30-interactive-webgl-3d-part-viewer/30-01-SUMMARY.md

# Module lives in a sibling repo, not in doordash-p2p:
@/Users/jeet/turion-space-demo/satellite/satellite-3d.js
@/Users/jeet/turion-space-demo/satellite/3d-test.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the assembly branch to mount3DViewer — radial-ring layout + raycaster picker + camera tween</name>
  <files>/Users/jeet/turion-space-demo/satellite/satellite-3d.js</files>
  <action>
Edit `mount3DViewer(containerEl, partData, opts = {})` in `/Users/jeet/turion-space-demo/satellite/satellite-3d.js`. Keep the renderer / scene / camera / lights / GridHelper / OrbitControls / ResizeObserver / RAF-tick scaffolding. Add a top-level branch right where the single mesh is built today (`const mesh = buildPartMesh(partData); ...Box3 normalize → scale 2/maxDim → recenter → scene.add → frame camera`):

```
const isAssembly = Array.isArray(opts.assemblyChildren) && opts.assemblyChildren.length > 0;
let childGroups = [], raycaster = null, pointer = null, hovered = null, tween = null;
let onPointerMove = null, onClick = null, onPointerLeave = null, onPointerDown = null;
let initialCamPos = null, initialTarget = null;
```

WHEN `!isAssembly`: run the EXISTING Phase-30 single-mesh block UNCHANGED. Do not refactor it.

WHEN `isAssembly`:
1. **Layout (a small `layoutAssemblyChildren(children) → { ringRoot, childGroups }` helper — define it near `buildPartMesh`, keep it as a named function so the ring↔grid layout is swappable later):**
   - `const ringRoot = new THREE.Group();`
   - `const N = children.length;`
   - `const childUnit = N > 10 ? Math.min(0.9, (6 * Math.PI) / N) : 0.9;`
   - `const ringRadius = N === 1 ? 0 : Math.max(2.2, Math.min(6, (childUnit * N) / Math.PI));`
   - for each `cd, i` in `children`: `const g = buildPartMesh(cd);` → `Box3.setFromObject(g)` → `g.scale.multiplyScalar(childUnit / Math.max(size.x, size.y, size.z, 1e-4))` → recenter on own origin via a second `Box3.getCenter` + `g.position.sub(center)` → `const theta = (i / N) * Math.PI * 2; g.position.x += Math.cos(theta) * ringRadius; g.position.z += Math.sin(theta) * ringRadius;` → optionally `g.lookAt(0, 0, 0)` (cosmetic) → `g.userData.childData = cd; g.traverse(o => { o.userData.childGroup = g; });` → `ringRoot.add(g);` and collect into `childGroups`.
   - return `{ ringRoot, childGroups }`. In `mount3DViewer`: `const layout = layoutAssemblyChildren(opts.assemblyChildren); childGroups = layout.childGroups; scene.add(layout.ringRoot);`
   - **Do NOT render a parent envelope box** (per locked decision #2 — the ring of children IS the assembly; the HUD names the parent). Keep the existing `GridHelper` as a scale floor (optionally widen to `GridHelper(12, 24, 0x2a3a55, 0x1a2438)` at `y = -1.05` if the ring is large — discretion).
   - Frame the camera off `ringRoot`'s bounding sphere: `Box3.setFromObject(ringRoot).getBoundingSphere(sphere)` → `const safeR = Math.max(sphere.radius, 1e-3); const dist = safeR / Math.sin(THREE.MathUtils.degToRad(camera.fov) / 2); camera.position.set(0.8 * dist, 0.55 * dist, dist); camera.lookAt(0, 0, 0);` → `controls.target.set(0, 0, 0); controls.minDistance = safeR * 0.4; controls.maxDistance = safeR * 8; controls.update();` → `initialCamPos = camera.position.clone(); initialTarget = controls.target.clone();`

2. **Raycaster picker (Pattern 3 from RESEARCH — canvas-rect-relative NDC, NOT window):**
   - `raycaster = new THREE.Raycaster(); pointer = new THREE.Vector2();`
   - `setEmissive(group, on)`: traverse the group; for each mesh material that has an `emissive` property (skip `MeshBasicMaterial` which has none): on first hover save `m.userData._emSaved = { c: m.emissive.clone(), i: m.emissiveIntensity }`, then `m.emissive.set(paletteFor3D(group.userData.childData?.subsystem_code).accent); m.emissiveIntensity = 0.5;`; on un-hover, if `_emSaved`: `m.emissive.copy(_emSaved.c); m.emissiveIntensity = _emSaved.i; m.userData._emSaved = null;`
   - `function ndc(ev) { const rect = renderer.domElement.getBoundingClientRect(); pointer.x = ((ev.clientX - rect.left) / rect.width) * 2 - 1; pointer.y = -((ev.clientY - rect.top) / rect.height) * 2 + 1; }` — recompute the rect every call, never cache it.
   - `onPointerMove(ev)`: `ndc(ev); raycaster.setFromCamera(pointer, camera); const hits = raycaster.intersectObjects(childGroups, true); const grp = hits.length ? hits[0].object.userData.childGroup : null;` if `grp !== hovered`: un-light old `hovered` (unless it equals the currently-selected group — keep the selected one lit), set `hovered = grp`, light the new one if non-null, set `renderer.domElement.style.cursor = hovered ? 'pointer' : 'grab'`.
   - `onPointerLeave()`: if `hovered` and `hovered` is not the selected group: `setEmissive(hovered, false)`; `hovered = (selected || null)`; reset cursor.
   - `onPointerDown(ev)`: record `{ x: ev.clientX, y: ev.clientY }` into a `down` variable (for the small drag-vs-click guard).
   - `onClick(ev)`: if `down` and `Math.hypot(ev.clientX - down.x, ev.clientY - down.y) > 4` → ignore (it was an orbit drag). Else: `ndc(ev); raycaster.setFromCamera(pointer, camera); const hits = raycaster.intersectObjects(childGroups, true);` if `hits.length`: `selectChild(hits[0].object.userData.childGroup)` else `deselect()`.
   - `renderer.domElement.addEventListener('pointermove', onPointerMove); renderer.domElement.addEventListener('pointerleave', onPointerLeave); renderer.domElement.addEventListener('pointerdown', onPointerDown); renderer.domElement.addEventListener('click', onClick);`

3. **Select / deselect + camera tween (Pattern 4 from RESEARCH — `Vector3.lerp` in the RAF loop, controls disabled during the tween, NO tween library):**
   - keep a `let selected = null;`
   - `selectChild(grp)`: if `selected && selected !== grp` un-light it; `selected = grp; hovered = grp; setEmissive(grp, true);` compute the child's world position `const wp = grp.getWorldPosition(new THREE.Vector3());` + its bounding sphere `const s = new THREE.Box3().setFromObject(grp).getBoundingSphere(new THREE.Sphere());` + a frame distance `const d = Math.max(s.radius, 1e-3) / Math.sin(THREE.MathUtils.degToRad(camera.fov) / 2) * 1.6;` → approach from the camera's CURRENT direction: `const dir = camera.position.clone().sub(controls.target).normalize();` → `tween = { camTo: wp.clone().add(dir.multiplyScalar(d)), tgtTo: wp.clone() }; controls.enabled = false;` → if `typeof opts.onSelect === 'function'` call `opts.onSelect(grp.userData.childData)`.
   - `deselect()`: if `selected` un-light it; `selected = null;` if `hovered` un-light + `hovered = null;` → `tween = { camTo: initialCamPos.clone(), tgtTo: initialTarget.clone() }; controls.enabled = false;` → if `typeof opts.onSelect === 'function'` call `opts.onSelect(null)`.
   - in the RAF `tick()`: if `tween`: `camera.position.lerp(tween.camTo, 0.12); controls.target.lerp(tween.tgtTo, 0.12);` if `camera.position.distanceTo(tween.camTo) < 0.01 && controls.target.distanceTo(tween.tgtTo) < 0.01`: `camera.position.copy(tween.camTo); controls.target.copy(tween.tgtTo); tween = null; controls.enabled = true;` — then `controls.update(); renderer.render(scene, camera);` as today. (When `tween` is active, you may skip `controls.update()` entirely that frame to avoid damping fighting the tween — either approach is fine; the `controls.enabled = false` + the exact `.copy()` at tween-end already prevents the snap-back.)

4. **Return value:** keep `return { controls, dispose() {...} }` for the leaf path; for the assembly path return `{ controls, deselect: deselect, dispose() {...} }` — i.e. always include a `deselect` key (a no-op `() => {}` in the leaf branch) so callers can wire a "back to assembly" button unconditionally. In `dispose()`: keep `cancelAnimationFrame(raf); ro.disconnect(); controls.dispose();` then — if `isAssembly` — `renderer.domElement.removeEventListener('pointermove', onPointerMove); removeEventListener('pointerleave', onPointerLeave); removeEventListener('pointerdown', onPointerDown); removeEventListener('click', onClick); renderer.domElement.style.cursor = '';` then the EXISTING `scene.traverse(o => { ...dispose geometries / array-aware materials / m.map })` (it already covers the N child groups since they're in the scene graph), then `renderer.dispose(); forceContextLoss(); canvas removal` UNCHANGED.

5. **Optional (not required):** add a 10th cell to `3d-test.html` that calls `mount3DViewer(el, somePart, { assemblyChildren: [ ...3-4 hardcoded child objects with part_number + subsystem_code + specifications... ], onSelect: cd => console.log('selected', cd) })` to visually demo the assembly path. If you do this, do not break the existing 9 cells; if it's getting heavy, skip it.

Constraints to honor:
- `buildPartMesh` returns a `THREE.Group` (not a bare `Mesh`) — `intersectObjects(childGroups, true)` (recursive) is mandatory; map a deep `Mesh` hit back to its top-level child group via `hit.object.userData.childGroup` (set on every descendant via `g.traverse`).
- `buildPartMesh` reads only `part_number` + `subsystem_code` + `specifications` — child rows lacking `default_make_buy` are fine; do NOT add any `default_make_buy` requirement.
- NDC must be canvas-`getBoundingClientRect()`-relative, recomputed per event — NOT `window.innerWidth/innerHeight`, NOT a cached rect.
- Hover highlight = emissive save/restore — NO `OutlinePass`/`EffectComposer`/`EdgesGeometry` overlay.
- Camera fly-to = `Vector3.lerp` in the existing RAF loop — NO `@tweenjs/tween.js`/GSAP/new importmap entry/new CDN dep.
- Three.js stays pinned at `0.184.0` via the existing importmap — no new bare specifiers.
- Do NOT touch `chooseTemplate3D`, `normalizeDims`, `perturbForPartNumber`, `paletteFor3D`, `materialFor`, `isWebGLAvailable`, or the leaf single-mesh code path.
  </action>
  <verify>
`node --check` on `satellite-3d.js` is EXPECTED to fail on the bare `import * as THREE from 'three'` line (the importmap resolves it in the browser, not Node) — same as Phase 30. Instead: strip the two top-of-file `import` lines to a temp copy and `node --check /tmp/s3d-check.js` → exit 0 (no syntax errors in the new code).
`grep -n "assemblyChildren" /Users/jeet/turion-space-demo/satellite/satellite-3d.js` → ≥2 matches (the `isAssembly` guard + the layout helper).
`grep -n "getBoundingClientRect" /Users/jeet/turion-space-demo/satellite/satellite-3d.js` → ≥1 match; `grep -n "window.innerWidth\|window.innerHeight" /Users/jeet/turion-space-demo/satellite/satellite-3d.js` → 0 matches (no window-relative NDC).
`grep -n "intersectObjects(childGroups, true)\|intersectObjects(childGroups,true)" /Users/jeet/turion-space-demo/satellite/satellite-3d.js` → ≥1 match (recursive picking on the child groups, not `scene.children`).
`grep -n "removeEventListener" /Users/jeet/turion-space-demo/satellite/satellite-3d.js` → ≥3 matches (pointermove/pointerleave/click cleanup in dispose; pointerdown too if added).
`grep -n "deselect" /Users/jeet/turion-space-demo/satellite/satellite-3d.js` → ≥2 matches (the function + the returned key).
`grep -n "@tweenjs\|tween.js\|gsap\|OutlinePass\|EffectComposer\|EdgesGeometry" /Users/jeet/turion-space-demo/satellite/satellite-3d.js` → 0 matches.
The leaf path is untouched: `git -C /Users/jeet/turion-space-demo diff satellite/satellite-3d.js` shows the single-mesh `const mesh = buildPartMesh(partData)` block + `chooseTemplate3D`/`normalizeDims`/`paletteFor3D`/`materialFor`/`buildPartMesh` bodies UNCHANGED (only additions: the `isAssembly` branch, the `layoutAssemblyChildren` helper, the picker, the tween, the `deselect` return key, the dispose listener-removal).
  </verify>
  <done>
`mount3DViewer` has an `isAssembly` branch (active when `opts.assemblyChildren` is a non-empty array) that lays N child meshes on a self-scaling radial ring (via a named `layoutAssemblyChildren` helper), wires a `THREE.Raycaster` + `pointermove`/`pointerleave`/`pointerdown`/`click` on `renderer.domElement` using canvas-rect-relative NDC, highlights the hovered/selected child via emissive save/restore, camera-tweens to a selected child (`controls.enabled = false` during the tween, lerp in the RAF loop), fires `opts.onSelect(childData)` / `opts.onSelect(null)`, exposes `viewerHandle.deselect()`, and removes the pointer listeners + resets the cursor in `dispose()`. The leaf single-mesh path, `buildPartMesh`, `chooseTemplate3D`, `normalizeDims`, `perturbForPartNumber`, `paletteFor3D`, `materialFor`, `isWebGLAvailable` are byte-identical. No new deps / importmap entries. (3d-test.html optionally gains one assembly demo cell.) Temp-stripped `node --check` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit the satellite-3d.js change (do NOT push — Plan 31-04 owns deploy)</name>
  <files>/Users/jeet/turion-space-demo/satellite/satellite-3d.js</files>
  <action>
In `/Users/jeet/turion-space-demo`, commit ONLY the edited file(s) — `satellite/satellite-3d.js` (and `satellite/3d-test.html` if you added the optional demo cell) — with the correct author identity (`user.email="jm@techcloudpro.com"`, `user.name="jeet-avatar"`):
```
cd /Users/jeet/turion-space-demo && git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit -m "feat(31-02): multi-mesh assembly viewer (radial ring + raycaster picker + camera tween) in satellite-3d.js" -- satellite/satellite-3d.js
```
(If 3d-test.html was edited, add it to the `-- <files>` list.) Do NOT `git push` — Plan 31-04 owns the push + `deploy-frontend.sh`. Do NOT touch the unrelated dirty ERP-demo HTML (`about-this-demo.html`, `agent-sales-cash.html`, `dashboard-cio.html`), `backend/*`, or `.superpowers/` — use the explicit `-- <files>` form so they're not staged.
  </action>
  <verify>
`cd /Users/jeet/turion-space-demo && git log -1 --format='%an <%ae> %s'` → `jeet-avatar <jm@techcloudpro.com> feat(31-02): ...`.
`cd /Users/jeet/turion-space-demo && git show --stat HEAD` lists only `satellite/satellite-3d.js` (and `satellite/3d-test.html` if you edited it) — no ERP HTML, no `backend/*`, no `.superpowers/`.
`cd /Users/jeet/turion-space-demo && git log origin/main..HEAD --oneline` shows the commit is local-only.
  </verify>
  <done>
One local commit on `turion-space-demo` main authored `jeet-avatar <jm@techcloudpro.com>` scoped to `satellite/satellite-3d.js` (+ optional `3d-test.html`); not pushed; the unrelated dirty ERP-demo WIP / `backend/*` / `.superpowers/` left untouched.
  </done>
</task>

</tasks>

<verification>
- `grep -c "assemblyChildren" satellite-3d.js` ≥ 2; `grep -c "getBoundingClientRect" satellite-3d.js` ≥ 1; `grep -c "window.innerWidth" satellite-3d.js` == 0
- `grep -c "intersectObjects(childGroups" satellite-3d.js` ≥ 1; `grep -c "deselect" satellite-3d.js` ≥ 2; `grep -c "removeEventListener" satellite-3d.js` ≥ 3
- `grep -c "@tweenjs\|gsap\|OutlinePass\|EffectComposer\|EdgesGeometry" satellite-3d.js` == 0
- Temp-stripped `node --check` on satellite-3d.js → exit 0
- `git -C /Users/jeet/turion-space-demo show --stat HEAD` → only `satellite/satellite-3d.js` (+ optional `3d-test.html`)
- `git -C /Users/jeet/turion-space-demo log -1 --format='%an <%ae>'` → `jeet-avatar <jm@techcloudpro.com>`
- Leaf single-mesh path + buildPartMesh/chooseTemplate3D/normalizeDims/paletteFor3D/materialFor/isWebGLAvailable unchanged in the diff
</verification>

<success_criteria>
- `mount3DViewer` renders N child meshes on a radial ring when `opts.assemblyChildren` is non-empty; hover-highlights + click-selects each child via raycasting; camera-tweens to a selected child; fires `opts.onSelect(childData)`/`opts.onSelect(null)`; exposes `deselect()`; cleans up pointer listeners in `dispose()`
- Leaf parts (no `assemblyChildren`) get the identical Phase-30 single-mesh viewer; `buildPartMesh` & the other 6 exports untouched
- No new dependencies / importmap entries; Three.js stays `0.184.0`; no `OutlinePass`/tween-lib
- One local commit on `turion-space-demo` authored `jeet-avatar <jm@techcloudpro.com>`, scoped to `satellite-3d.js` (+ optional `3d-test.html`), not pushed
</success_criteria>

<output>
After completion, create `.planning/phases/31-3d-dimension-hud-clickable-assemblies/31-02-SUMMARY.md`
</output>
