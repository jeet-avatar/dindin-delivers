---
phase: 31-3d-dimension-hud-clickable-assemblies
plan: 04
type: execute
wave: 3
depends_on: ["31-01", "31-02", "31-03"]
files_modified: []
autonomous: false
requirements: [DimensionHUD, AssemblyMultiMesh, ChildrenSpecsAPI]

must_haves:
  truths:
    - "turion-satellite Lambda `turion-satellite-api` is redeployed (new CodeSha256) so GET /api/parts/:partDefId/children?sat= returns `specifications` in production"
    - "turion-space-demo frontend is redeployed to S3 `turion-demo-static` + CloudFront `E37R9PT8IL44L2` invalidated, so the live part.html / instance.html have the `cad-hud` HUD + the new `mount3DViewer` opts"
    - "The F6 deploy-hygiene pre-flight ran before deploy-frontend.sh (unrelated dirty ERP-demo HTML git-stashed, .superpowers/ moved aside) and was restored after — no unrelated WIP rode the `aws s3 sync . --delete`"
    - "Phase-29 button audit = 0 violations from BOTH repos, before and after deploy"
    - "Smoke checks pass: deployed part.html + instance.html contain `cad-hud` and the new mount3DViewer opts; jsDelivr Three.js 0.184.0 still 200+CORS; /api/parts/:id/children?sat= behaviour confirmed (specifications present — DB-direct or via a 401-probe proving the route is alive)"
    - "The live 3D dimension HUD + clickable-children behaviour is verified (browser walk if available; otherwise the documented headless-substitute curl/HEAD smoke + the 3d-test.html harness, per the Phase 27-30 precedent)"
  artifacts: []
  key_links:
    - from: "git push (both repos)"
      to: "deploy-frontend.sh + build-and-push.sh"
      via: "code on origin/main → S3 sync + CloudFront invalidation; ECR + Lambda update-function-code"
      pattern: "deploy-frontend\\.sh"
---

<objective>
Ship Phase 31: push the Plan 31-01 backend commit + the Plan 31-02/31-03 frontend commits, redeploy the `turion-satellite` Lambda via `./build-and-push.sh` (so `/api/parts/:id/children` returns `specifications` in prod), redeploy the `turion-space-demo` frontend via `deploy-frontend.sh` WITH the F6 deploy-hygiene pre-flight (the script does `aws s3 sync . --delete` over the whole repo — unrelated dirty ERP-demo WIP + `.superpowers/` must be stashed/moved aside first, restored after) + CloudFront invalidation, run the Phase-29 button audit (0 violations) + curl/HEAD smoke checks, then the human-verify checkpoint for the live 3D HUD + clickable assemblies (with a headless-substitute path per the Phase 27-30 precedent).

Purpose: A deploy is a task inside a GSD phase plan, never ad-hoc. This wave is the only one that pushes / runs `build-and-push.sh` / runs `deploy-frontend.sh`. It also guards against `deploy-frontend.sh`'s `--delete` clobbering unrelated WIP and against a `dead-onclick`/`missing-endpoint` regression.
Output: Lambda `turion-satellite-api` redeployed (new CodeSha256); frontend live at `turionspace.zietra.com/satellite/part.html` + `instance.html` with the dimension HUD + clickable-assembly ring; both repos' new commits pushed under `jeet-avatar <jm@techcloudpro.com>`; audit 0 violations; smoke checks pass; SUMMARY written.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-3d-dimension-hud-clickable-assemblies/31-RESEARCH.md
@.planning/phases/30-interactive-webgl-3d-part-viewer/30-03-SUMMARY.md
@.planning/phases/31-3d-dimension-hud-clickable-assemblies/31-01-PLAN.md
@.planning/phases/31-3d-dimension-hud-clickable-assemblies/31-02-PLAN.md
@.planning/phases/31-3d-dimension-hud-clickable-assemblies/31-03-PLAN.md

# Deploy scripts live in the sibling repos:
@/Users/jeet/turion-satellite/build-and-push.sh
@/Users/jeet/turion-space-demo/deploy-frontend.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deploy audit + push both repos + redeploy the turion-satellite Lambda</name>
  <files>none (deploy actions — no files in this repo)</files>
  <action>
1. **Pre-deploy button audit (from BOTH repos):**
   - `cd /Users/jeet/turion-satellite/backend && node scripts/audit-satellite-buttons.mjs` → MUST print `0 violations`, exit 0.
   - `cd /Users/jeet/turion-space-demo && node scripts/audit-satellite-buttons.mjs` (the thin re-export wrapper) → MUST print `0 violations`, exit 0.
   - If either reports a violation, STOP — do not deploy; report it (the fix belongs in Plan 31-03, not here).
2. **Confirm the expected local commits exist (from Plans 31-01/31-02/31-03):**
   - `cd /Users/jeet/turion-satellite && git log origin/main..HEAD --oneline` → expect the `feat(31-01): add specifications to GET /api/parts/:partDefId/children SELECT` commit.
   - `cd /Users/jeet/turion-space-demo && git log origin/main..HEAD --oneline` → expect `feat(31-02): multi-mesh assembly viewer ...` + `feat(31-03): dimension HUD + clickable-assembly wiring ...` (and optionally a 3d-test.html demo cell if it landed inside 31-02's commit).
   - Verify each is authored `jeet-avatar <jm@techcloudpro.com>` (`git log --format='%an <%ae>' origin/main..HEAD`).
3. **Push both repos** (push BEFORE deploying — per CLAUDE.md):
   - `cd /Users/jeet/turion-satellite && git push origin main`
   - `cd /Users/jeet/turion-space-demo && git push origin main`
4. **Redeploy the turion-satellite Lambda** (the same path Phase 28's `/bom/tree` route used — `build-and-push.sh` does: tsc build → docker `linux/arm64` → ECR push → `aws lambda update-function-code` → `wait function-updated`):
   - Capture the pre-deploy CodeSha256: `aws lambda get-function-configuration --function-name turion-satellite-api --query 'CodeSha256' --output text`.
   - `cd /Users/jeet/turion-satellite && ./build-and-push.sh`
   - Capture the post-deploy CodeSha256 + State + LastUpdateStatus: `aws lambda get-function-configuration --function-name turion-satellite-api --query '[CodeSha256,State,LastUpdateStatus]' --output text` — CodeSha256 MUST differ from the pre-deploy value; State MUST be `Active`; LastUpdateStatus MUST be `Successful`.
   - If `./build-and-push.sh` fails (build, docker, ECR, or the Lambda update), STOP and report — do NOT retry manually with ad-hoc `aws`/`docker` commands; this is a deploy failure to investigate (`/gsd:debug` territory).
  </action>
  <verify>
Both `audit-satellite-buttons.mjs` runs printed `0 violations` (exit 0).
`cd /Users/jeet/turion-satellite && git log origin/main..HEAD --oneline` → empty AFTER the push (all commits pushed); `git log -1 --format='%an <%ae>'` → `jeet-avatar <jm@techcloudpro.com>`.
`cd /Users/jeet/turion-space-demo && git log origin/main..HEAD --oneline` → empty AFTER the push.
`aws lambda get-function-configuration --function-name turion-satellite-api --query '[CodeSha256,State,LastUpdateStatus]' --output text` → CodeSha256 differs from the pre-deploy value; State=`Active`; LastUpdateStatus=`Successful`.
`curl -s -o /dev/null -w "%{http_code}" -X GET "https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/parts/00000000-0000-0000-0000-000000000000/children?sat=24587565-b15b-42ce-b590-87ecf9b6bb99"` → `401` (route alive, auth-gated — a `401` here vs a `404` for a bogus path proves the route exists; matches the Phase-28/29 pattern). If feasible, also confirm `SELECT specifications FROM turion_satellite.part_definitions LIMIT 1;` returns a JSONB object via the AWS Secrets Manager DB URL (`arn:aws:secretsmanager:us-east-1:134607809447:secret:turion-satellite/production/database-url-NCbgX6`, strip `?schema=`) and — if a Bearer token / the demo whitelisted email is available — curl `/api/parts/<a real partDef with children>/children?sat=24587565-...` and confirm `specifications` is present in each returned row. Otherwise the 401-probe + the Plan 31-01 `parts.test.ts` assertion are the authoritative gate — document that in the SUMMARY.
  </verify>
  <done>
Audit 0 violations from both repos; both repos' new commits pushed under `jeet-avatar <jm@techcloudpro.com>`; Lambda `turion-satellite-api` redeployed (new CodeSha256, Active/Successful); `/api/parts/:id/children?sat=` route confirmed alive (401-probe) and (DB-direct or token-curl, if feasible — else documented as deferred to the parts.test.ts assertion) returning `specifications`.
  </done>
</task>

<task type="auto">
  <name>Task 2: F6 deploy-hygiene pre-flight → deploy-frontend.sh → CloudFront invalidation → smoke checks → restore</name>
  <files>none (deploy actions — no files in this repo)</files>
  <action>
1. **F6 pre-flight (the `deploy-frontend.sh` does `aws s3 sync . --delete` over the WHOLE `turion-space-demo` repo — keep unrelated dirty WIP out of it):**
   - `cd /Users/jeet/turion-space-demo && git status --short` — identify the dirty/untracked files NOT part of Phase 31. Per the Phase-30 SUMMARY these are: `about-this-demo.html`, `agent-sales-cash.html`, `dashboard-cio.html` (uncommitted ERP-demo WIP), `backend/*` (already `--exclude`d by the script), and untracked `.superpowers/` (contains `.html` files — would ride along).
   - Stash the unrelated tracked WIP: `git stash push -- about-this-demo.html agent-sales-cash.html dashboard-cio.html` (adjust the file list to whatever `git status --short` actually shows as unrelated tracked-dirty files — do NOT stash `satellite/*`, which is Phase-31 work already committed in HEAD).
   - Move `.superpowers/` aside: `mv .superpowers /tmp/phase31-superpowers-stash` (only if it exists).
   - Confirm `git status --short` now shows ONLY `backend/*` (excluded by the script) and a clean `satellite/` (the 31-02/31-03 commits are in HEAD, not WIP).
2. **Deploy the frontend:**
   - `cd /Users/jeet/turion-space-demo && bash deploy-frontend.sh` — this regenerates the gitignored `satellite-config.js` from AWS Secrets Manager, then `aws s3 sync . s3://turion-demo-static --delete` (uploading `satellite/satellite-3d.js`, `satellite/part.html`, `satellite/instance.html`, the regenerated config, and the COMMITTED bytes of any other tracked files — `.superpowers/*` and `backend/*` correctly absent), then a CloudFront invalidation on dist `E37R9PT8IL44L2`.
   - Capture the CloudFront invalidation ID from the script output; poll `aws cloudfront get-invalidation --distribution-id E37R9PT8IL44L2 --id <INVALIDATION_ID> --query 'Invalidation.Status' --output text` until it's `Completed`.
3. **Smoke checks (curl/HEAD against the live site):**
   - `curl -sI "https://turionspace.zietra.com/satellite/satellite-3d.js"` → `HTTP/2 200` + a JS content-type; `curl -s "https://turionspace.zietra.com/satellite/satellite-3d.js" | grep -c "assemblyChildren"` → ≥1; `... | grep -c "intersectObjects(childGroups"` → ≥1.
   - `curl -s "https://turionspace.zietra.com/satellite/part.html" | grep -c "cad-hud"` → ≥3; `... | grep -c "assemblyChildren"` → ≥1; `... | grep -c 'type="importmap"'` → ≥1; `... | grep -c "cdn.jsdelivr.net/npm/three@0.184.0"` → ≥1; `... | grep -c "mode-3d"` → ≥1; `... | grep -c "frame-svg"` → ≥1 (SVG-fallback path intact).
   - `curl -s "https://turionspace.zietra.com/satellite/instance.html" | grep -c "cad-hud"` → ≥3; `... | grep -c "/children?sat="` → ≥1; `... | grep -c "assemblyChildren"` → ≥1; `... | grep -c "mode-3d"` → ≥1; `... | grep -c "frame-svg"` → ≥1.
   - `curl -sI "https://turionspace.zietra.com/satellite/3d-test.html"` → `HTTP/2 200`.
   - `curl -sI "https://turionspace.zietra.com/satellite/bom.html" | head -1` → `HTTP/2 200`; `curl -s "https://turionspace.zietra.com/satellite/bom.html" | grep -c "view=3d"` → ≥1 (Phase-30 `🧊 3D` deep-link badge UNCHANGED).
   - `curl -sI "https://turionspace.zietra.com/satellite/login.html"` → `HTTP/2 200` (the `--delete` didn't break the app).
   - jsDelivr still good: `curl -sI "https://cdn.jsdelivr.net/npm/three@0.184.0/build/three.module.min.js"` and `curl -sI "https://cdn.jsdelivr.net/npm/three@0.184.0/examples/jsm/controls/OrbitControls.js"` → both `HTTP/2 200` + `access-control-allow-origin: *`.
4. **Post-deploy audit:** `cd /Users/jeet/turion-satellite/backend && node scripts/audit-satellite-buttons.mjs` → `0 violations`, exit 0 (re-confirm after deploy).
5. **Restore the working tree:** `mv /tmp/phase31-superpowers-stash .superpowers` (if it was moved) + `cd /Users/jeet/turion-space-demo && git stash pop` (restores the ERP-demo WIP). Confirm `git status --short` matches the pre-deploy baseline and `git stash list` is empty.

Do NOT run any ad-hoc `aws ecs` / `docker build` / `docker push` / direct ECR commands — only `./build-and-push.sh` (Task 1) and `deploy-frontend.sh` (here).
  </action>
  <verify>
The F6 pre-flight ran: `git stash list` was non-empty mid-deploy and is empty after `stash pop`; `.superpowers/` was absent from the `aws s3 sync` output and is back in the working tree after restore.
CloudFront invalidation polled to `Completed`.
All smoke checks above pass (the greps return ≥ their expected counts; the HEAD checks return 200; jsDelivr 200+CORS).
Post-deploy `node scripts/audit-satellite-buttons.mjs` → 0 violations, exit 0.
`cd /Users/jeet/turion-space-demo && git status --short` after restore == the pre-deploy baseline (the 3 ERP HTML modifications + `backend/*` + `?? .superpowers/`); `git stash list` empty.
  </verify>
  <done>
F6 pre-flight ran (unrelated ERP WIP stashed, `.superpowers/` moved aside) before `deploy-frontend.sh`; the frontend is live on `turionspace.zietra.com/satellite/` (S3 `turion-demo-static`, CloudFront `E37R9PT8IL44L2` invalidation `Completed`); curl/HEAD smoke checks all pass (`part.html`/`instance.html` carry `cad-hud` + the new mount3DViewer opts; `satellite-3d.js` carries `assemblyChildren` + `intersectObjects(childGroups`; `bom.html` `view=3d` badge unchanged; `login.html` 200; jsDelivr Three.js 0.184.0 200+CORS); post-deploy audit 0 violations; the working tree restored to the pre-deploy baseline with an empty stash.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the live 3D dimension HUD + clickable-assembly ring</name>
  <files>none (verification only)</files>
  <action>
This is a blocking human-verify checkpoint AFTER the deploy automation in Tasks 1-2.

**What's built (live at `turionspace.zietra.com/satellite/`):** (1) a DOM dimension HUD on `.cad-frame` (bottom-left, `Fira Code` mono on a dark panel) showing `part_number` / (description) / `L × W × H mm` / `Mass: g` / `Material:` — always visible in 3D mode; (2) assembly parts (those with ≥1 BOM child on the satellite) render N child meshes on a radial ring instead of a single mesh — hovering a child glows it, clicking it flies the camera in + lights it + updates the HUD to that child's dims + `ref:` designator; a `↩ back to assembly` chip (bottom-right) and clicking empty space reset to the whole-assembly framing + the parent's dims; (3) leaf parts keep the Phase-30 single-mesh viewer with a static parent-dims HUD; (4) the backend `/api/parts/:id/children?sat=` now returns each child's `specifications`. The Phase-30 2D-SVG fallback, `?view=2d/3d`, the `.mode-3d` toggle, `#autoRotateChk`, and `bom.html`'s `🧊 3D` deep-link are all unchanged.

**Browser walk (if a browser/WebGL is available):**
1. Open `https://turionspace.zietra.com/satellite/part.html?id=<a leaf part id>&sat=24587565-b15b-42ce-b590-87ecf9b6bb99` — the bottom-left HUD shows `L × W × H` mm, `Mass`, `Material`; rotate/zoom (OrbitControls) works; the `view: 3D` chip toggles to the 2D isometric SVG and back.
2. Open `https://turionspace.zietra.com/satellite/part.html?id=<an assembly part id with BOM children, e.g. an *-ASSY part on SAT-003>&sat=24587565-b15b-42ce-b590-87ecf9b6bb99` — you see MULTIPLE small meshes in a ring (one per BOM child), not a single mesh; hover a child → it glows; click a child → the camera flies in to frame it, it stays lit, the HUD updates to that child's `part_number` / `L × W × H` mm / `Mass` / `Material` / `ref: <designator>`, and a `↩ back to assembly` chip appears bottom-right; click the chip (or click empty space) → the camera flies back to the whole ring and the HUD shows the parent again.
3. Open `https://turionspace.zietra.com/satellite/instance.html?sat=24587565-b15b-42ce-b590-87ecf9b6bb99&id=<an instance of an assembly partDef>` — same HUD + clickable-ring behaviour; the existing BOM-children gallery / integrations panel / lifecycle timeline below still render.
4. Open `https://turionspace.zietra.com/satellite/3d-test.html` — the 8-family harness still renders (and the optional 10th assembly cell, if added, shows a ring of meshes + a console log on click).
5. Open `https://turionspace.zietra.com/satellite/bom.html?sat=24587565-b15b-42ce-b590-87ecf9b6bb99` — the `🧊 3D` per-row deep-link still works and lands on `part.html?...&view=3d`.
6. With WebGL disabled, `part.html` falls back to the 2D isometric SVG with the Phase-27 callouts — no HUD, no crash.

**Headless substitute (if no browser/WebGL — the established case for Phases 27-30):** the curl/HEAD smoke checks in Task 2 (deployed `part.html`/`instance.html` contain `cad-hud` + `assemblyChildren`; `satellite-3d.js` contains `assemblyChildren` + `intersectObjects(childGroups`; `bom.html` `view=3d` unchanged; `login.html` 200; jsDelivr 200+CORS) + the post-deploy audit (0 violations) + the `3d-test.html` HEAD-200 (the visual harness is deployed) + the `/api/parts/:id/children?sat=` 401-probe (route alive) + the Plan 31-01 `parts.test.ts` `specifications` assertion ARE the orchestrator's approval to complete the plan. Record in the SUMMARY: "visual sign-off DEFERRED — headless environment; curl/HEAD smoke + audit + 3d-test.html-deployed + 401-probe passed; a literal browser walk of the HUD + clickable-assembly ring + camera fly-to + 2D/3D toggle + WebGL-off fallback is a follow-up if a sign-off is required." Do NOT block the phase on the browser walk in a headless run — same precedent as Phases 27/28/29/30.

**Resume signal:** Type "approved" (or, in a headless run, the orchestrator's headless-substitute approval), or describe issues.
  </action>
  <verify>
Either: a human confirms the browser walk (steps 1-6 above) and types "approved"; OR (headless) the Task-2 smoke checks + post-deploy audit + `3d-test.html` HEAD-200 + the `/api/parts/:id/children?sat=` 401-probe all passed and the headless-substitute approval is recorded in the SUMMARY per the Phase 27-30 precedent.
  </verify>
  <done>
The live 3D dimension HUD + clickable-assembly ring + camera fly-to + `↩ back to assembly` chip is verified — either by a browser walk ("approved") or by the documented headless-substitute (Task-2 smoke + audit + `3d-test.html` deployed + 401-probe) per the Phase 27-30 precedent; the outcome is recorded in the SUMMARY.
  </done>
</task>

</tasks>

<verification>
- Pre- AND post-deploy `audit-satellite-buttons.mjs` (both repos) → 0 violations, exit 0
- Both repos pushed (`git log origin/main..HEAD` empty after push); commits authored `jeet-avatar <jm@techcloudpro.com>`
- `aws lambda get-function-configuration --function-name turion-satellite-api` → new CodeSha256, State=Active, LastUpdateStatus=Successful
- `/api/parts/:id/children?sat=` 401-probe (route alive) + (DB-direct or token-curl `specifications` present, if feasible — else deferred to parts.test.ts)
- F6 pre-flight ran (ERP WIP stashed, `.superpowers/` moved aside, restored after — `git stash list` empty post-restore, working tree == pre-deploy baseline)
- `deploy-frontend.sh` ran; CloudFront `E37R9PT8IL44L2` invalidation `Completed`
- Live smoke: `part.html`/`instance.html` contain `cad-hud` + `assemblyChildren`; `satellite-3d.js` contains `assemblyChildren` + `intersectObjects(childGroups`; `bom.html` `view=3d` unchanged; `login.html` 200; jsDelivr Three.js 0.184.0 200+CORS; `3d-test.html` 200
- Human-verify checkpoint reached (browser walk OR documented headless-substitute approval)
</verification>

<success_criteria>
- `turion-satellite` Lambda redeployed → `/api/parts/:id/children?sat=` returns `specifications` in prod
- `turion-space-demo` frontend live on `turionspace.zietra.com/satellite/` with the dimension HUD + clickable-assembly ring + camera fly-to + page-owned `↩ back to assembly` chip; SVG fallback / `?view=` / `.mode-3d` / `#autoRotateChk` / `bom.html` unchanged
- F6 pre-flight kept unrelated ERP WIP + `.superpowers/` out of the `aws s3 sync . --delete`; working tree restored
- Phase-29 button audit 0 violations (before + after); both repos' commits pushed under `jeet-avatar <jm@techcloudpro.com>`
- Human-verify checkpoint satisfied (browser walk or headless-substitute per the Phase 27-30 precedent)
- No ad-hoc `aws ecs`/`docker build`/`docker push`/manual ECR commands — only `./build-and-push.sh` + `deploy-frontend.sh`
</success_criteria>

<output>
After completion, create `.planning/phases/31-3d-dimension-hud-clickable-assemblies/31-04-SUMMARY.md`
</output>
