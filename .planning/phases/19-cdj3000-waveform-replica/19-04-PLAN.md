---
phase: 19-cdj3000-waveform-replica
plan: 04
type: execute
wave: 4
depends_on: ["19-03"]
files_modified:
  - apps/mixmind/frontend/src/components/DJWaveformView.tsx
autonomous: true
requirements:
  - SPEC-04

must_haves:
  truths:
    - "RB button shows green badge (#00E676) when Rekordbox data available"
    - "MM button shows purple badge (#AA00FF) when MixMind data available"
    - "Auto button shows blue badge (#7C4DFF) and prefers MM when available"
    - "Active source label shows which source is rendering (e.g., 'Source: RB' or 'Source: MM')"
    - "Switching source immediately re-renders waveform with different analysis data"
    - "Unavailable sources are grayed out and disabled"
  artifacts:
    - path: "apps/mixmind/frontend/src/components/DJWaveformView.tsx"
      provides: "RB/MM/Auto source toggle with visual indicators"
      contains: "anlzSource"
  key_links:
    - from: "anlzSource state"
      to: "anlzData derivation"
      via: "conditional selection from dualData.rekordbox vs dualData.mixmind"
      pattern: "anlzSource === 'mm'"
---

<objective>
Polish the RB/MM/Auto source toggle to match the CDJ-3000 design spec. Add clear visual badges, status labels, and ensure the waveform updates correctly when switching data sources.

Purpose: DJs need to see at a glance which analysis source is active and switch between Rekordbox and MixMind analysis. The current toggle works but needs visual polish to match the spec.
Output: Clear, CDJ-style source toggle with colored badges and status indicator.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-cdj3000-waveform-replica/19-01-SUMMARY.md
@apps/mixmind/frontend/src/components/DJWaveformView.tsx
@docs/superpowers/specs/2026-03-29-cdj3000-waveform-replica-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish RB/MM/Auto source toggle with CDJ-3000 style badges</name>
  <files>apps/mixmind/frontend/src/components/DJWaveformView.tsx</files>
  <action>
Update the source toggle section in the header bar to match the design spec:

**1. Badge Colors (per spec):**
- RB (Rekordbox): `#00E676` green — active background `rgba(0,230,118,0.25)`, active text `#00E676`
- MM (MixMind): `#AA00FF` purple — active background `rgba(170,0,255,0.25)`, active text `#AA00FF`
- Auto: `#7C4DFF` blue — active background `rgba(124,77,255,0.25)`, active text `#7C4DFF`

These colors are already close to the existing implementation but verify they match exactly.

**2. Availability Indicators:**
- When a source has NO data (e.g., `dualData.rekordbox === null`), the button should be:
  - Text color: `rgba(255,255,255,0.15)` (very dim)
  - Cursor: `default` (not pointer)
  - `disabled={true}` on the button
- When a source HAS data but is not active:
  - Text color: `rgba(255,255,255,0.4)` (dim but readable)
  - Cursor: `pointer`

**3. Active Source Status Label:**
Replace the existing "Analyzed: RB + MM" / "Analyzed: MixMind" / "Analyzed: Rekordbox" label with a cleaner format:

Determine the EFFECTIVE source (what's actually rendering):
```
let effectiveSource: string;
if (anlzSource === 'rb') effectiveSource = 'RB';
else if (anlzSource === 'mm') effectiveSource = 'MM';
else { // auto
  effectiveSource = dualData?.mixmind ? 'MM' : 'RB';
}
```

Show a small colored dot + label:
```
<span style={{ ... }}>
  <span style={{
    width: 6, height: 6, borderRadius: '50%', display: 'inline-block', marginRight: 4,
    background: effectiveSource === 'MM' ? '#AA00FF' : '#00E676'
  }} />
  Source: {effectiveSource}
</span>
```

Also show availability: `[RB + MM]` or `[RB only]` or `[MM only]` in small dim text.

**4. Waveform Update on Switch:**
The existing logic already derives `anlzData` from `dualData` based on `anlzSource`. Verify this works correctly:
- Switching from RB to MM should show MixMind's beat grid, sections, and waveform (4-stem if available)
- Switching from MM to RB should show Rekordbox's 3-band waveform
- Auto should prefer MM when available, fall back to RB
- The `mmToAnlz` helper function converts MixMindAnalysis to TrackAnlzData format — verify it preserves waveform_3band=null (MixMind has 4-stem, not 3-band) so the fallback to waveform_preview or 4-stem rendering works correctly.

**5. If MixMind has no waveform_3band** (it doesn't — it has waveform_4stem), ensure the 3Band color mode falls through to either 4-stem rendering or mono preview gracefully. Do NOT show a blank waveform when MM source is selected.

Do NOT change: waveform rendering logic, beat grid, color modes, data fetching.
  </action>
  <verify>
Run `cd /Users/jeet/doordash-p2p/apps/mixmind/frontend && npx tsc --noEmit` — no TypeScript errors.
Visually verify in browser:
1. Source toggle shows RB (green), MM (purple), Auto (blue) buttons
2. Unavailable sources are grayed out
3. Active source label shows colored dot + "Source: RB" or "Source: MM"
4. Switching from RB to MM changes the waveform (different data)
5. Auto defaults to MM when available
  </verify>
  <done>
Source toggle has clear CDJ-style badges with correct colors. Status label shows active source. Switching sources updates waveform. Unavailable sources disabled.
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Badge colors match spec: green (#00E676), purple (#AA00FF), blue (#7C4DFF)
- Disabled sources visually distinct from enabled
- Source label accurately reflects rendering source
- Waveform re-renders when source changes
</verification>

<success_criteria>
- RB green, MM purple, Auto blue badges with correct hex values
- Status label with colored dot shows effective rendering source
- Switching sources changes displayed waveform data
- Graceful fallback when selected source lacks 3-band data
</success_criteria>

<output>
After completion, create `.planning/phases/19-cdj3000-waveform-replica/19-04-SUMMARY.md`
</output>
