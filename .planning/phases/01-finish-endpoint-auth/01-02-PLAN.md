---
phase: 01-finish-endpoint-auth
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/web/p2p-platform/backend/main_new.py
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "All /api/ai/* endpoints require admin JWT to access"
    - "Admin-only analytics dashboard rejects non-admin users"
    - "GET /api/vendors listing requires authentication"
    - "AI menu review, auto-publish, and vendor processing are admin-gated"
  artifacts:
    - path: "apps/web/p2p-platform/backend/main_new.py"
      provides: "Per-endpoint Depends(require_admin) on all admin/AI endpoints"
      contains: "require_admin"
  key_links:
    - from: "main_new.py AI endpoints"
      to: "auth_utils.py require_admin"
      via: "Depends(require_admin) in function signature"
      pattern: "Depends\\(require_admin\\)"
---

<objective>
Add per-endpoint Depends() auth to all admin-only and AI employee endpoints in main_new.py.

Purpose: The global middleware blocks unauthenticated requests, but these internal/admin endpoints need explicit admin role checks to prevent privilege escalation (e.g., a customer JWT accessing AI employee functions).

Output: main_new.py with ~14 admin/AI endpoints secured with Depends(require_admin) or Depends(require_any_auth).
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-finish-endpoint-auth/01-RESEARCH.md
@.planning/phases/01-finish-endpoint-auth/01-01-SUMMARY.md
@apps/web/p2p-platform/backend/auth_utils.py
@apps/web/p2p-platform/backend/main_new.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Depends(require_admin) to admin and AI endpoints</name>
  <files>apps/web/p2p-platform/backend/main_new.py</files>
  <action>
Add per-endpoint Depends() auth to the remaining admin-only and AI employee endpoints. The import from auth_utils should already exist from Plan 01.

ADMIN endpoints (use `admin: User = Depends(require_admin)`):
- Line ~8148: `GET /api/dashboard/consolidated` -- admin-only analytics dashboard. Add `admin: User = Depends(require_admin)`.
- Line ~10161: `GET /api/vendors` -- admin vendor listing (different from public `/api/vendors/published`). Add `admin: User = Depends(require_admin)`. NOTE: Verify this is NOT the public listing. The public one is `/api/vendors/published` which is in the allowlist.

AI EMPLOYEE endpoints (use `admin: User = Depends(require_admin)`):
These are called by the admin portal or external admin API calls, NOT by background tasks. They are safe to admin-lock.
- Line ~12068: `POST /api/ai/menu/review-all/{vendor_id}` -- add `admin: User = Depends(require_admin)`
- Line ~12125: `POST /api/ai/menu/review-item/{item_id}` -- add `admin: User = Depends(require_admin)`
- Line ~12258: `POST /api/ai/vendor/check-publish-ready/{vendor_id}` -- add `admin: User = Depends(require_admin)`
- Line ~12276: `POST /api/ai/vendor/auto-publish/{vendor_id}` -- add `admin: User = Depends(require_admin)`
- Line ~12289: `POST /api/ai/process-new-vendor/{vendor_id}` -- add `admin: User = Depends(require_admin)`
- Line ~12353: `GET /api/ai/dashboard` -- add `admin: User = Depends(require_admin)`
- Line ~12415: `GET /api/ai/pending-reviews` -- add `admin: User = Depends(require_admin)`

IMPORTANT NOTES:
- Use `admin: User = Depends(require_admin)` which returns a User object with confirmed admin role.
- Line numbers are APPROXIMATE. Use grep to find actual lines: `grep -n "def.*ai.*menu\|def.*ai.*vendor\|def.*ai.*dashboard\|def.*ai.*pending\|def.*consolidated\|def get_vendors" main_new.py`
- The `require_admin` function in auth_utils.py checks `user.role != UserRole.ADMIN` and raises 403 if not admin. This is the correct behavior.
- Do NOT add auth to `/api/ai/*` paths that are defined in separate router files -- only modify endpoints defined with `@app.get`/`@app.post` in main_new.py.
- Verify each endpoint is actually defined with `@app.` decorator (not in a router) before modifying.
  </action>
  <verify>
Run tests:
```bash
cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && python -m pytest tests/ -x -q 2>&1 | tail -20
```

Verify admin auth on AI endpoints:
```bash
grep -A3 "def.*review_all\|def.*review_item\|def.*check_publish_ready\|def.*auto_publish\|def.*process_new_vendor\|def.*ai_dashboard\|def.*pending_reviews\|def.*consolidated" /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend/main_new.py | grep "require_admin"
```
Should show require_admin in 9 endpoint signatures.

Count total Depends(require_admin) in main_new.py:
```bash
grep -c "require_admin" /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend/main_new.py
```
  </verify>
  <done>
All 9 admin/AI endpoints have Depends(require_admin). Non-admin JWTs are rejected with 403 at these endpoints. Tests pass.
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/ -x -q` passes
2. All 9 admin/AI endpoints have `Depends(require_admin)` in their signatures
3. `grep -c "require_admin" main_new.py` shows ~9 new occurrences (plus any from v1.1)
</verification>

<success_criteria>
- 9 admin/AI endpoints in main_new.py have Depends(require_admin)
- Non-admin users get 403 on AI employee endpoints
- Non-admin users get 403 on consolidated dashboard
- Test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-finish-endpoint-auth/01-02-SUMMARY.md`
</output>
