---
phase: 01-finish-endpoint-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/p2p-platform/backend/main_new.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Verification webhooks (Persona, Onfido, Veriff) are accessible without JWT"
    - "Tax calculation endpoints are accessible without JWT"
    - "All customer-facing endpoints reject requests without a valid customer JWT"
    - "All driver-facing endpoints reject requests without a valid driver JWT"
    - "All vendor-facing endpoints reject requests without a valid vendor JWT"
    - "Driver dashboard ownership is enforced (driver can only see own dashboard)"
  artifacts:
    - path: "apps/web/p2p-platform/backend/main_new.py"
      provides: "Updated public allowlist + per-endpoint Depends() on ~20 role-specific endpoints"
      contains: "require_customer|require_driver|require_vendor"
  key_links:
    - from: "main_new.py allowlist"
      to: "require_auth_middleware"
      via: "_PUBLIC_PREFIXES includes /api/verification/webhook/"
      pattern: "/api/verification/webhook/"
    - from: "main_new.py endpoint signatures"
      to: "auth_utils.py"
      via: "Depends(require_driver) / Depends(require_customer) / Depends(require_vendor)"
      pattern: "Depends\\(require_(customer|driver|vendor)\\)"
---

<objective>
Fix the public path allowlist to unblock verification webhooks and tax endpoints, then add per-endpoint Depends() auth to all customer, driver, and vendor role-specific endpoints in main_new.py.

Purpose: The global middleware already blocks unauthenticated requests, but per-endpoint Depends() adds role-based access control, ownership verification, and OpenAPI documentation. The allowlist fix prevents verification webhook 401s.

Output: main_new.py with corrected allowlist + ~20 endpoints secured with role-specific Depends() auth.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-finish-endpoint-auth/01-RESEARCH.md
@apps/web/p2p-platform/backend/auth_utils.py
@apps/web/p2p-platform/backend/main_new.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix public path allowlist and add role-specific Depends() to customer/driver/vendor endpoints</name>
  <files>apps/web/p2p-platform/backend/main_new.py</files>
  <action>
This task has two parts. IMPORTANT: auth_utils.py already exists with require_any_auth, require_customer, require_driver, require_vendor, require_admin. The import `from auth_utils import ...` may already exist at the top of main_new.py from v1.1 work -- verify before adding a duplicate import.

**Part A: Fix public allowlist (lines ~256-365)**

Add `/api/verification/webhook/` to `_PUBLIC_PREFIXES` (around line 332). This ensures Persona, Onfido, and Veriff webhooks are not blocked by the global middleware. These webhooks use their own signature verification, NOT JWT.

Also verify that the following are already covered by existing patterns (they should be -- just confirm):
- `/api/vendors/{vendor_id}/menu` -- covered by regex `^/api/vendors/\d+/menu(/.*)?$`
- `/api/vendors/{vendor_id}/menu/categories` -- covered by same regex
- `/api/tax/calculate` and `/api/tax/estimate/{state}` -- covered by regex `^/api/tax/(calculate|estimate/).*$`
- `/api/verification/required-documents/{entity_type}` -- covered by regex `^/api/verification/required-documents/.*$`
- `/api/verification/{entity_type}/{entity_id}/status` -- NOT covered. Add a new regex pattern to `_PUBLIC_PATTERN_PATHS`: `(_re.compile(r"^/api/verification/\w+/\d+/status$"), {"GET"})` for verification status checks (pre-auth flow).

**Part B: Add Depends() auth to role-specific endpoints**

For each endpoint below, add the specified Depends() parameter to the function signature. Use the EXACT pattern from auth_utils.py. The parameter goes BEFORE `db: Session = Depends(get_db)` in the signature.

CUSTOMER endpoints (use `customer: Customer = Depends(require_customer)`):
- Line ~3795: `POST /api/erp/rides/request` -- this has manual Header(None) auth, REPLACE the manual auth block with `customer: Customer = Depends(require_customer)`. Remove the manual `authorization: Optional[str] = Header(None)` param and all the jwt.decode/driver lookup boilerplate. Use `customer.id` where `customer_id` was derived from the JWT.
- Line ~16956: `GET /api/chat/customer/{customer_id}/conversations` -- add `customer: Customer = Depends(require_customer)` and add ownership check: `if customer.id != customer_id: raise HTTPException(403, "Access denied")`

DRIVER endpoints (use `driver: Driver = Depends(require_driver)`):
- Line ~4609: `PATCH /drivers/{driver_id}/status` -- add `driver: Driver = Depends(require_driver)` + ownership: `if driver.id != driver_id: raise HTTPException(403, "Access denied")`
- Line ~4686: `GET /api/drivers/{driver_id}/status` -- use `_auth: dict = Depends(require_any_auth)` (any authenticated user can check driver status)
- Line ~5405: `POST /api/rides/{ride_id}/complete-and-pay` -- has manual Header(None) auth. REPLACE with `driver: Driver = Depends(require_driver)`. Remove manual jwt.decode boilerplate.
- Line ~6894: `GET /api/driver/dashboard` -- has manual Header(None) auth. REPLACE with `driver: Driver = Depends(require_driver)`. Remove manual jwt.decode boilerplate.
- Line ~7047: `GET /api/v5/driver/{driver_id}/dashboard` -- add `driver: Driver = Depends(require_driver)` + ownership: `if driver.id != driver_id: raise HTTPException(403, "Access denied")`
- Line ~15859: `GET /api/rides/available` -- add `driver: Driver = Depends(require_driver)`
- Line ~16923: `GET /api/chat/driver/{driver_id}/conversations` -- add `driver: Driver = Depends(require_driver)` + ownership: `if driver.id != driver_id: raise HTTPException(403, "Access denied")`
- Line ~20656: `GET /api/driver/active-delivery` -- has manual Header(None) auth. REPLACE with `driver: Driver = Depends(require_driver)`. Remove the entire manual jwt.decode block (~15 lines).
- Line ~20745: `GET /api/driver/messages` -- has manual Header(None) auth. REPLACE with `driver: Driver = Depends(require_driver)`. Remove manual jwt.decode boilerplate.
- Line ~21011: `POST /api/v2/driver/deliveries/{delivery_id}/accept` -- has manual Header(None) auth. REPLACE with `driver: Driver = Depends(require_driver)`.
- Line ~21133: `POST /api/v2/driver/deliveries/{delivery_id}/pickup` -- has manual Header(None) auth. REPLACE with `driver: Driver = Depends(require_driver)`.
- Line ~21165: `POST /api/v2/driver/deliveries/{delivery_id}/complete` -- has manual Header(None) auth. REPLACE with `driver: Driver = Depends(require_driver)`.

VENDOR endpoints (use `vendor: Vendor = Depends(require_vendor)`):
- Line ~10726: `POST /api/erp/orders/{order_id}/print-kot` -- add `vendor: Vendor = Depends(require_vendor)`. If there's an ownership check needed (vendor of the order), add it.
- Line ~10999: `GET /api/vendors/{vendor_id}/publish-checklist` -- add `vendor: Vendor = Depends(require_vendor)` + ownership: `if vendor.id != vendor_id: raise HTTPException(403, "Access denied")`
- Line ~13674: `PATCH /api/vendors/{vendor_id}/menu/{item_id}/customizations` -- add `vendor: Vendor = Depends(require_vendor)` + ownership check
- Line ~14256: `POST /api/vendors/{vendor_id}/upload-image` -- add `vendor: Vendor = Depends(require_vendor)` + ownership check
- Line ~14314: `POST /api/vendors/{vendor_id}/assign-stock-image` -- add `vendor: Vendor = Depends(require_vendor)` + ownership check
- Line ~14346: `POST /api/vendors/{vendor_id}/menu/assign-stock-images` -- add `vendor: Vendor = Depends(require_vendor)` + ownership check

ANY-AUTH endpoints (use `_auth: dict = Depends(require_any_auth)`):
- Line ~8729: `GET /api/orders/{order_id}` -- add `_auth: dict = Depends(require_any_auth)`
- Line ~16856: `POST /api/chat/typing/{order_id}` -- add `_auth: dict = Depends(require_any_auth)`
- Line ~16871: `GET /api/chat/conversation/{order_id}` -- add `_auth: dict = Depends(require_any_auth)`

IMPORTANT NOTES:
- When replacing manual Header(None) auth: remove `authorization: Optional[str] = Header(None)` parameter, remove the entire if/try/except jwt.decode block, use the Depends() return value (e.g., `driver` object) where the manually-looked-up object was used.
- Line numbers are APPROXIMATE from the research. Use grep to find the actual current line: `grep -n "def function_name" main_new.py`.
- Ensure `from auth_utils import require_any_auth, require_customer, require_driver, require_vendor` is at the top of main_new.py (may already exist).
- Do NOT touch endpoints in router files (order_flow.py, bid_routes.py, etc.) -- those already have auth.
  </action>
  <verify>
Run the test suite to confirm no regressions:
```bash
cd /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend && python -m pytest tests/ -x -q 2>&1 | tail -20
```

Verify the allowlist fix with grep:
```bash
grep -n "verification/webhook" /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend/main_new.py
```

Verify Depends() was added to key endpoints:
```bash
grep -n "require_driver\|require_customer\|require_vendor\|require_any_auth\|require_admin" /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend/main_new.py | wc -l
```
The count should increase by approximately 25 over the baseline.

Verify no manual Header(None) auth remains in the targeted endpoints:
```bash
grep -B2 "Header(None)" /Users/jeet/doordash-p2p/apps/web/p2p-platform/backend/main_new.py
```
Lines ~3795, ~5405, ~6894, ~20656, ~20745, ~21011, ~21133, ~21165 should NO LONGER have Header(None).
  </verify>
  <done>
Public allowlist includes `/api/verification/webhook/` prefix and verification status regex pattern. All 25 customer/driver/vendor/any-auth endpoints have per-endpoint Depends() auth. All 8 manual Header(None) auth blocks are replaced with Depends(). Tests pass.
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/ -x -q` passes (no regressions from auth changes)
2. `grep -c "Depends(require_" main_new.py` shows increase of ~25 over baseline
3. `grep "Header(None)" main_new.py` shows no remaining manual auth at the 8 targeted endpoints
4. `grep "verification/webhook" main_new.py` shows the prefix in _PUBLIC_PREFIXES
</verification>

<success_criteria>
- Public allowlist has verification webhook prefix and verification status pattern
- 25 endpoints in main_new.py have new Depends() auth parameters
- 8 manual Header(None) auth blocks replaced with Depends()
- All ownership checks added where path params expose IDs
- Test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-finish-endpoint-auth/01-01-SUMMARY.md`
</output>
