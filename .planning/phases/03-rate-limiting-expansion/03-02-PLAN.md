---
phase: 03-rate-limiting-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/web/p2p-platform/backend/main_new.py
  - apps/web/p2p-platform/backend/stripe_integration.py
  - apps/web/p2p-platform/backend/rideshare_payments.py
  - apps/web/p2p-platform/backend/matchmaking_routes.py
  - apps/web/p2p-platform/backend/order_flow.py
autonomous: true
requirements:
  - RATE-02
  - RATE-03
  - RATE-05

must_haves:
  truths:
    - "Payment/checkout endpoints return 429 after 10 requests per minute per authenticated user"
    - "Admin mutation endpoints return 429 after 30 requests per minute per admin IP"
    - "Stripe webhook endpoints are NOT rate limited"
    - "All 429 responses include a Retry-After header"
    - "No test regressions"
  artifacts:
    - path: "apps/web/p2p-platform/backend/main_new.py"
      provides: "Rate limit calls on payment and admin mutation endpoints"
      contains: "payment_limiter"
    - path: "apps/web/p2p-platform/backend/stripe_integration.py"
      provides: "Rate limit on /payments/create-intent"
      contains: "check_rate_limit"
    - path: "apps/web/p2p-platform/backend/rideshare_payments.py"
      provides: "Rate limit on /payments/ride/create-intent"
      contains: "check_rate_limit"
    - path: "apps/web/p2p-platform/backend/matchmaking_routes.py"
      provides: "Rate limit on /matchmaking/accept-bid"
      contains: "check_rate_limit"
    - path: "apps/web/p2p-platform/backend/order_flow.py"
      provides: "Rate limit on /orders/{id}/confirm-payment"
      contains: "check_rate_limit"
  key_links:
    - from: "apps/web/p2p-platform/backend/stripe_integration.py"
      to: "apps/web/p2p-platform/backend/cache.py"
      via: "from cache import RateLimiter, check_rate_limit"
      pattern: "from cache import.*check_rate_limit"
    - from: "apps/web/p2p-platform/backend/rideshare_payments.py"
      to: "apps/web/p2p-platform/backend/cache.py"
      via: "from cache import RateLimiter, check_rate_limit"
      pattern: "from cache import.*check_rate_limit"
    - from: "apps/web/p2p-platform/backend/matchmaking_routes.py"
      to: "apps/web/p2p-platform/backend/cache.py"
      via: "from cache import RateLimiter, check_rate_limit"
      pattern: "from cache import.*check_rate_limit"
    - from: "apps/web/p2p-platform/backend/order_flow.py"
      to: "apps/web/p2p-platform/backend/cache.py"
      via: "from cache import RateLimiter, check_rate_limit"
      pattern: "from cache import.*check_rate_limit"
---

<objective>
Add rate limiting to all payment/checkout endpoints (10 req/min per user) and all admin mutation endpoints (30 req/min per admin IP).

Purpose: Prevents duplicate payment charges from hammering and accidental mass admin operations. Completes the rate limiting expansion across all sensitive endpoint categories.
Output: Rate limit calls added to 10 payment endpoints across 5 files and 18 admin mutation endpoints in main_new.py.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rate-limiting-expansion/03-RESEARCH.md
@.planning/phases/03-rate-limiting-expansion/03-01-SUMMARY.md
@apps/web/p2p-platform/backend/cache.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rate limiting to payment/checkout endpoints across all files</name>
  <files>
    apps/web/p2p-platform/backend/main_new.py
    apps/web/p2p-platform/backend/stripe_integration.py
    apps/web/p2p-platform/backend/rideshare_payments.py
    apps/web/p2p-platform/backend/matchmaking_routes.py
    apps/web/p2p-platform/backend/order_flow.py
  </files>
  <action>
    **For each router file** (stripe_integration.py, rideshare_payments.py, matchmaking_routes.py, order_flow.py):
    1. Add import at the top: `from cache import check_rate_limit, RateLimiter`
    2. Add a module-level limiter instance: `payment_limiter = RateLimiter(max_requests=10, window_seconds=60)`
       (Or import `payment_limiter` from main_new -- but defining locally is safer to avoid circular imports)

    **Payment endpoints to rate-limit (RATE-02) -- 10 endpoints total:**

    For each endpoint, add `check_rate_limit(request, payment_limiter, "payment", identifier=str(user_id))` as the FIRST line of the function body. The `user_id` should be the authenticated user's ID -- obtained from the Depends() auth parameter that was added in Phase 01/02.

    **In main_new.py (6 endpoints):**

    1. `POST /api/rides/{ride_id}/complete-and-pay` (~line 5529):
       - User: driver (from `_auth_driver: Driver = Depends(require_driver)`)
       - Call: `check_rate_limit(request, payment_limiter, "payment", identifier=str(_auth_driver.id))`
       - Add `request: Request` param if not present

    2. `POST /api/invoices/{invoice_id}/payments` (~line 7570):
       - User: admin (from `admin: User = Depends(require_admin)`)
       - Call: `check_rate_limit(request, payment_limiter, "payment", identifier=str(admin.id))`
       - Add `request: Request` param if not present

    3. `POST /api/orders/{order_id}/tip-driver` (~line 15036):
       - User: customer (from `_auth_customer: Customer = Depends(require_customer)`)
       - Call: `check_rate_limit(request, payment_limiter, "payment", identifier=str(_auth_customer.id))`
       - Add `request: Request` param if not present

    4. `POST /api/rides/{ride_id}/tip` (~line 15570):
       - User: customer (from auth param)
       - Call: `check_rate_limit(request, payment_limiter, "payment", identifier=str(customer_id))`
       - Use whichever customer identifier is available from the auth dependency

    5. `POST /api/erp/payments/intent` (~line 17534):
       - User: any auth (from `_auth: dict = Depends(require_any_auth)`)
       - Call: `check_rate_limit(request, payment_limiter, "payment", identifier=str(_auth.get("user_id", _auth.get("customer_id", _auth.get("driver_id", "unknown")))))`
       - Add `request: Request` param if not present

    6. `POST /api/erp/payments/refund` (~line 17658):
       - Same auth pattern as #5
       - Call: `check_rate_limit(request, payment_limiter, "payment_refund", identifier=str(_auth.get("user_id", _auth.get("customer_id", _auth.get("driver_id", "unknown")))))`

    **In stripe_integration.py (1 endpoint):**

    7. `POST /api/payments/create-intent` (~line 112):
       - Import Request from starlette if not present
       - Add `request: Request` param
       - Use auth param to get user identifier
       - Call: `check_rate_limit(request, payment_limiter, "payment", identifier=str(user_id))`

    **In rideshare_payments.py (1 endpoint):**

    8. `POST /api/payments/ride/create-intent` (~line 62):
       - Same pattern as stripe_integration.py
       - Call: `check_rate_limit(request, payment_limiter, "payment", identifier=str(user_id))`

    **In matchmaking_routes.py (1 endpoint):**

    9. `POST /api/matchmaking/accept-bid` (~line 377):
       - Call: `check_rate_limit(request, payment_limiter, "payment", identifier=str(user_id))`

    **In order_flow.py (1 endpoint):**

    10. `POST /api/erp/orders/{order_id}/confirm-payment` (~line 1404):
        - Call: `check_rate_limit(request, payment_limiter, "payment", identifier=str(user_id))`

    **CRITICAL EXCLUSIONS (DO NOT RATE LIMIT):**
    - `POST /api/webhooks/stripe-connect` (main_new.py) -- Stripe server-to-server webhook
    - `POST /api/webhooks/stripe` (stripe_integration.py) -- Stripe server-to-server webhook
    - `POST /api/erp/payouts/{payout_id}/process` (order_flow.py) -- admin payout processing
    - `POST /api/drivers/{driver_id}/payouts` (main_new.py) -- driver payout, not a charge
    - `POST /api/matchmaking/driver/payment-info` (matchmaking_routes.py) -- info retrieval only

    **NOTE:** Line numbers are approximate from research phase. Use `grep -n "def endpoint_name\|POST.*endpoint_path"` to find actual locations. Some function signatures changed during Phase 01/02 auth work.
  </action>
  <verify>
    Run `grep -n "check_rate_limit.*payment" apps/web/p2p-platform/backend/main_new.py apps/web/p2p-platform/backend/stripe_integration.py apps/web/p2p-platform/backend/rideshare_payments.py apps/web/p2p-platform/backend/matchmaking_routes.py apps/web/p2p-platform/backend/order_flow.py` -- should show 10 payment rate limit calls.
    Run `grep -n "from cache import" apps/web/p2p-platform/backend/stripe_integration.py apps/web/p2p-platform/backend/rideshare_payments.py apps/web/p2p-platform/backend/matchmaking_routes.py apps/web/p2p-platform/backend/order_flow.py` -- all 4 router files import check_rate_limit.
    Verify webhooks are NOT rate limited: `grep -A2 "webhook" apps/web/p2p-platform/backend/stripe_integration.py | grep -v "check_rate_limit"`.
  </verify>
  <done>
    All 10 payment/checkout endpoints rate-limited at 10 req/min per user ID. All 4 router files import check_rate_limit from cache.py. Stripe webhook endpoints confirmed NOT rate limited.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add rate limiting to admin mutation endpoints</name>
  <files>
    apps/web/p2p-platform/backend/main_new.py
  </files>
  <action>
    Add `check_rate_limit(request, admin_mutation_limiter, "admin_mutation")` as the FIRST line of each admin mutation endpoint body. These are IP-based (no identifier needed -- the function falls back to IP extraction when identifier is None).

    If the endpoint doesn't have `request: Request` in its signature, add it.

    **Admin Mutation Endpoints (RATE-03) -- 18 endpoints:**

    1. `POST /api/admin/backfill-payouts` (~line 526)
    2. `POST /api/admin/migrate` (~line 572)
    3. `DELETE /api/admin/customers/by-email/{email}` (~line 3480)
    4. `POST /api/admin/vendors/{vendor_id}/documents/{document_type}/approve` (~line 11423)
    5. `POST /api/admin/vendors/{vendor_id}/documents/{document_type}/reject` (~line 11469)
    6. `POST /api/admin/vendors/{vendor_id}/documents/upload` (~line 11512)
    7. `POST /api/admin/set-document-status` (~line 11608)
    8. `POST /api/admin/menu/{item_id}/approve` (~line 11863)
    9. `POST /api/admin/menu/{item_id}/reject` (~line 11894)
    10. `POST /api/admin/menu/{item_id}/flag` (~line 11928)
    11. `POST /api/admin/vendors/{vendor_id}/verify-menu` (~line 12476)
    12. `POST /api/admin/vendors/{vendor_id}/publish` (~line 12527)
    13. `POST /api/admin/vendors/{vendor_id}/unpublish` (~line 12688)
    14. `POST /api/admin/cleanup-expired-bids` (~line 19038)
    15. `POST /api/admin/drivers/{driver_id}/set-documents` (~line 20290)
    16. `POST /api/admin/drivers/{driver_id}/verify` (~line 20346)
    17. `POST /api/admin/cleanup/pending-orders` (~line 20391)
    18. `POST /api/admin/cleanup/all-incomplete` (~line 20452)

    **EXCLUDED from admin rate limiting (already rate limited or one-time):**
    - `POST /api/admin/login` -- already has auth_rate_limiter
    - `POST /api/auth/admin/setup-production` -- one-time setup
    - `DELETE /api/auth/admin/legacy-cleanup` -- one-time cleanup
    - `POST /api/auth/admin/demo-login` -- demo endpoint
    - All admin GET endpoints -- reads don't need rate limiting

    **NOTE:** Line numbers are approximate. Use `grep -n` to find actual function signatures. Each endpoint should already have `admin: User = Depends(require_admin)` or similar from Phase 02 auth work.

    **Run full test suite** after all changes to confirm no regressions.
  </action>
  <verify>
    Run `grep -c "admin_mutation_limiter" apps/web/p2p-platform/backend/main_new.py` -- should show 18 (plus 1 for the instance definition = 19 total).
    Run `grep -n "admin_mutation_limiter" apps/web/p2p-platform/backend/main_new.py` and verify all 18 endpoints are covered.
    Run `pytest tests/ -v --no-header -q` to confirm no test regressions.
  </verify>
  <done>
    All 18 admin mutation endpoints rate-limited at 30 req/min per IP. Admin login rate limit unchanged (10 req/min via auth_rate_limiter). Admin GET endpoints confirmed NOT rate limited. All 429 responses include Retry-After header. Full test suite passes.
  </done>
</task>

</tasks>

<verification>
1. **Payment coverage:** `grep -rn "payment_limiter" apps/web/p2p-platform/backend/*.py` -- 10 endpoint calls + 5 instance definitions (1 per file) = ~15 matches
2. **Admin coverage:** `grep -c "admin_mutation_limiter" apps/web/p2p-platform/backend/main_new.py` -- 19 matches (18 calls + 1 definition)
3. **Webhook safety:** `grep -B5 "webhook" apps/web/p2p-platform/backend/stripe_integration.py apps/web/p2p-platform/backend/main_new.py | grep -c "check_rate_limit"` -- should be 0
4. **Router imports:** All 4 router files have `from cache import.*check_rate_limit`
5. **Full test suite:** `pytest tests/ -v --no-header -q` -- all pass
6. **Total rate limit calls:** `grep -rn "check_rate_limit" apps/web/p2p-platform/backend/main_new.py apps/web/p2p-platform/backend/stripe_integration.py apps/web/p2p-platform/backend/rideshare_payments.py apps/web/p2p-platform/backend/matchmaking_routes.py apps/web/p2p-platform/backend/order_flow.py | wc -l` -- should be ~50 (22 from Plan 01 + 28 from Plan 02)
</verification>

<success_criteria>
- 10 payment/checkout endpoints rate-limited at 10 req/min per user ID
- 18 admin mutation endpoints rate-limited at 30 req/min per admin IP
- Stripe webhook endpoints confirmed NOT rate limited
- All 4 router files import check_rate_limit from cache.py
- All 429 responses include Retry-After header
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-rate-limiting-expansion/03-02-SUMMARY.md`
</output>
