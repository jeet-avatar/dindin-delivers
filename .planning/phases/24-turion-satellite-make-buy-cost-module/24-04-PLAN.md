---
phase: 24-turion-satellite-make-buy-cost-module
plan: 04
type: execute
wave: 4
depends_on: ["24-03"]
files_modified:
  - /Users/jeet/turion-space-demo/satellite/cost.html
  - /Users/jeet/turion-space-demo/satellite/cost-detail.html
  - /Users/jeet/turion-space-demo/satellite/cost-render.js
  - /Users/jeet/turion-space-demo/satellite/part.html
  - /Users/jeet/turion-space-demo/satellite/index.html
  - /Users/jeet/turion-space-demo/satellite/sat.html
  - /Users/jeet/turion-space-demo/satellite/parts.html
  - /Users/jeet/turion-space-demo/satellite/work-orders.html
  - /Users/jeet/turion-space-demo/satellite/instance.html
  - /Users/jeet/turion-space-demo/satellite/bom.html
  - /Users/jeet/turion-space-demo/satellite/kanban.html
autonomous: true
repo: turion-space-demo
requirements:
  - "UI-Surface"
  - "Variance"
  - "Make-vs-Buy"
  - "Cost-Rollup"
  - "Currency-FX"

must_haves:
  truths:
    - "User clicks 'Cost' in the top nav and lands on /satellite/cost.html"
    - "cost.html shows constellation-level rollup or a satellite-picker → drills into per-subsystem rollup → drills into per-part cost detail"
    - "Per-part cost detail shows make-cost sheet (planned + actuals + per-line variance with traffic-light) AND buy-cost sheet (RFQ → quoted → PO → invoiced + BOTH variances side-by-side)"
    - "Make-vs-buy decision panel shows side-by-side make total vs lowest buy quote, decision dropdown ('make' / 'buy'), free-text rationale (min 20 chars enforced client-side), Save button POSTs to /api/make-buy-decisions"
    - "Re-evaluate button calls POST /api/make-buy-decisions/:satId/:partDefId/re-evaluate"
    - "Variance traffic-light uses thresholds: |Δ| ≤ 5% green, 5% < |Δ| ≤ 15% yellow, >15% red — implemented in shared cost-render.js"
    - "Delta-vs-previous-satellite section is collapsed by default with a clickable disclosure toggle"
    - "part.html cost_breakdown panel is REPLACED with first-class data: GET /api/make-costs/:satId/:partInstId for make parts, GET /api/buy-costs/:satId/:partInstId for buy parts; falls back to a 'Cost sheet not yet entered' empty state with a link to cost-detail.html for editing"
    - "All money values rendered via formatUSD-equivalent client helper (Number(stringFromAPI).toLocaleString) — never raw '15000' or 'NaN'"
    - "Top nav entry 'Cost' added to all 9 navigated pages (index, sat, parts, work-orders, instance, bom, kanban, part, cost itself)"
  artifacts:
    - path: /Users/jeet/turion-space-demo/satellite/cost.html
      provides: "Cost analytics summary page — picker, rollup table, drill-down links"
      min_lines: 200
    - path: /Users/jeet/turion-space-demo/satellite/cost-detail.html
      provides: "Per-part cost detail — make sheet + buy sheet + decision panel + delta-vs-prev"
      min_lines: 350
    - path: /Users/jeet/turion-space-demo/satellite/cost-render.js
      provides: "Shared helpers: trafficLight(deltaPct), renderMakeSheet(row), renderBuySheet(row), renderDecisionPanel(row), formatMoney(str)"
      exports: ["trafficLight", "renderMakeSheet", "renderBuySheet", "renderDecisionPanel", "formatMoney"]
    - path: /Users/jeet/turion-space-demo/satellite/part.html
      provides: "Replaced cost_breakdown panel with /api/make-costs OR /api/buy-costs data + link to cost-detail.html for editing"
      contains: "/api/make-costs"
  key_links:
    - from: /Users/jeet/turion-space-demo/satellite/cost.html
      to: /api/analytics/cost-rollup/:satId
      via: "fetch on satellite selection"
      pattern: "/api/analytics/cost-rollup"
    - from: /Users/jeet/turion-space-demo/satellite/cost-detail.html
      to: /api/make-costs/:satId/:partInstId AND /api/buy-costs/:satId/:partInstId AND /api/make-buy-decisions/:satId/:partDefId
      via: "Three parallel fetches on page load + Save handlers PUT/POST"
      pattern: "/api/(make-costs|buy-costs|make-buy-decisions)"
    - from: /Users/jeet/turion-space-demo/satellite/part.html
      to: /api/make-costs/:satId/:partInstId
      via: "Replaces old cost_breakdown panel"
      pattern: "fetch.*api/make-costs"
---

<objective>
Build the cost.html primary surface (CONTEXT decision #1: dedicated page, top-level nav, drill-down), the cost-detail page for per-part editing (#2: template + actuals), the variance traffic-light component (#6: ±5%/±15% thresholds), the delta-vs-previous-satellite collapsible (#5), the user-driven make-vs-buy decision panel with rationale ≥20 chars (#7), and replace the old approximate cost_breakdown panel on part.html with first-class data (#3).

Purpose: The cost module is invisible until the frontend exposes it. CONTEXT.md is explicit: maximum vertical space, drill-down from constellation → satellite → part → cost; replace the part.html approximate panel; user makes decisions with rationale; both variances side-by-side; traffic-light thresholds; delta-vs-prev collapsed by default. This plan ships all of it.
Output: 2 new HTML pages + 1 new JS module + part.html patch + nav additions across 9 pages.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jeet/doordash-p2p/.planning/phases/24-turion-satellite-make-buy-cost-module/24-CONTEXT.md
@/Users/jeet/doordash-p2p/.planning/phases/24-turion-satellite-make-buy-cost-module/24-RESEARCH.md
@/Users/jeet/.claude/handoffs/2026-05-10-turion-satellite-frontend-v2.md
@/Users/jeet/turion-space-demo/satellite/part.html
@/Users/jeet/turion-space-demo/satellite/sat.html
@/Users/jeet/turion-space-demo/satellite/instance.html
@/Users/jeet/turion-space-demo/satellite/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: cost-render.js shared helpers + cost.html (analytics summary + drill-down)</name>
  <files>
    /Users/jeet/turion-space-demo/satellite/cost-render.js
    /Users/jeet/turion-space-demo/satellite/cost.html
  </files>
  <action>
    Repo: turion-space-demo. Author MUST be `jm@techcloudpro.com` / `jeet-avatar`.

    **cost-render.js** — vanilla ES module style matching existing `satellite-render.js`. Exports as window globals (no bundler in use):
    - `formatMoney(strOrNum, currencyCode='USD')` — accepts API-returned string or number; renders `$1,234.56` (USD) using `Intl.NumberFormat`. For non-USD shows native symbol via `Intl.NumberFormat(navigator.language, { style: 'currency', currency: currencyCode })`. Returns `'—'` for null/undefined.
    - `formatPct(pct)` — `+5.2%` / `-3.1%` with sign. Null → `'—'`.
    - `trafficLight(deltaPct)` — returns `{ class: 'green'|'yellow'|'red', label: '✓'|'!'|'⚠' }` based on |deltaPct| ≤ 5 (green), ≤ 15 (yellow), >15 (red). Null → `{class:'gray', label:'—'}`. CSS classes already declared in cost.html `<style>` block (see below).
    - `renderRollupRow(rowObj)` — returns innerHTML for a `<tr>` cell: subsystem code, label, make_total, buy_invoiced, buy_committed, instance_count, plus a clickable drill-down link to `cost-detail.html?sat=...&part_inst=...` (drill into the FIRST instance in that subsystem; or to `parts.html?sat=...&subsystem=...` if that filter exists).
    - `renderTotalsCard({ make_total_usd, buy_invoiced_usd, buy_committed_usd })` — large numeric tiles with labels.
    - `renderPrevSatDelta(deltaObj, currentName)` — renders the collapsed-by-default disclosure block. `<details>` element with `<summary>Δ vs previous satellite (collapsed)</summary>` and a table inside.

    Use the SAME visual conventions as existing render JS files: muted text colour, monospace font for IDs, accent colour `#0080ff` for links. Do not introduce new CSS frameworks.

    **cost.html** — vanilla HTML/CSS/JS, mirrors structure of `sat.html`. Layout:
    1. Top nav (same structure as `sat.html`'s nav — copy the nav bar HTML; add a `Cost` link with `class="active"` on this page).
    2. Header: "Cost Analytics" + satellite picker `<select id="satSel">` populated from GET /api/satellites (existing endpoint).
    3. Default view = constellation rollup: when satellite picker = "All satellites", show a table with one row per satellite (name, program_start, total_make, total_buy, instance_count). Implementation: fetch `/api/satellites` then loop and call `/api/analytics/cost-rollup/:satId` per satellite (max 5 satellites in demo so N+1 is acceptable). Sum totals per sat.
    4. Per-satellite view: when picker = specific sat, fetch `/api/analytics/cost-rollup/:satId` and render: totals card at top, by_subsystem table below, prev_satellite_delta as collapsed details element.
    5. Each row in by_subsystem table has a "View parts in this subsystem" link to `parts.html?sat=...&subsystem=<code>` so the user can drill down to a part list.
    6. From parts list, clicking any part navigates to `cost-detail.html?sat=...&part_inst=<id>` (built in Task 2).

    Use `satellite-auth.js` (existing) for magic-link bearer-token fetch. Replicate the `apiFetch(path)` wrapper pattern from `sat.html`.

    Add `<script src="cost-render.js"></script>` after `satellite-render.js` in the page script block.

    Smoke locally if possible: `python3 -m http.server 8080 -d /Users/jeet/turion-space-demo` then load `http://localhost:8080/satellite/cost.html` (auth will redirect to login — that's expected pre-deploy). Ensure no JS errors in console for the static load.

    Commit (turion-space-demo repo): `cd /Users/jeet/turion-space-demo && git add satellite/cost-render.js satellite/cost.html && git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit -m "feat(24-04): cost.html analytics summary + cost-render.js shared helpers"`.
  </action>
  <verify>
    `test -f /Users/jeet/turion-space-demo/satellite/cost.html` returns 0 (file exists).
    `test -f /Users/jeet/turion-space-demo/satellite/cost-render.js` returns 0.
    `grep -c "trafficLight\|formatMoney\|renderPrevSatDelta" /Users/jeet/turion-space-demo/satellite/cost-render.js` returns >= 3.
    `grep -c "/api/analytics/cost-rollup" /Users/jeet/turion-space-demo/satellite/cost.html` returns >= 1.
    `grep -c "satellite-auth.js" /Users/jeet/turion-space-demo/satellite/cost.html` returns >= 1.
    HTML lints valid (open in browser, check console — no JS errors on initial paint).
  </verify>
  <done>
    cost.html ships with the analytics rollup view, satellite picker, totals card, by_subsystem table, collapsible prev-sat delta. cost-render.js exports shared helpers. Both committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: cost-detail.html (make sheet + buy sheet + decision panel + variance traffic-light)</name>
  <files>
    /Users/jeet/turion-space-demo/satellite/cost-detail.html
  </files>
  <action>
    Repo: turion-space-demo. Same git author rule.

    Create `satellite/cost-detail.html`. URL params: `?sat=<satId>&part_inst=<partInstId>`. Mirror layout of `instance.html` (existing) for consistency.

    **Page sections** (top-to-bottom):
    1. Top nav with `Cost` active.
    2. Breadcrumb: Constellation → Satellite → Part. Resolve part_definition + satellite name via existing `/api/parts/:id` and `/api/satellites/:id`. Provide back-link to `part.html?id=<partDefId>&sat=<satId>`.
    3. Title: "Cost Sheet — <part_number> on <satellite_name>".
    4. **Make-vs-Buy Decision panel** (CONTEXT decision #7, hard gate-driving):
       - Two columns side-by-side: "Make total (current planned)" and "Lowest buy quote (current)" — sourced from /api/make-costs and /api/buy-costs (lowest of `quoted_unit_cost_usd × ordered_qty + nre_cost_usd`).
       - Decision form:
         - Radio: ⦿ Make ⦿ Buy
         - Textarea (rows=4): "Rationale (min 20 chars)" with live char-count + green/red border based on `value.trim().length >= 20`.
         - Save button (disabled until form valid): POST `/api/make-buy-decisions/:satId/:partDefId` with `{ decision, rationale, part_instance_id: <partInstId> }`. On 200, refresh decision row + variance.
       - Current-state pill: shows existing `decision` + `decision_status` + `decided_at` + `decided_by_name` if a row exists, OR "Not yet decided" pill.
       - Re-evaluate button (visible only if decision exists and status='approved'): POST `/api/make-buy-decisions/:satId/:partDefId/re-evaluate`. On success, the pill flips to status='re_evaluate'.
       - If GET decision returned `re_evaluate: true` (derived flag — newer cost data after decision), display an amber banner "Costs have changed since this decision — consider re-evaluating".
    5. **Make Cost Sheet** (always shown, even for buy parts — useful for the alternative-path analysis):
       - Two-column grid: column 1 "Planned (template)", column 2 "Actual (this satellite)". For v1 we don't yet have a separate template row in DB, so column 1 may be a sibling read of the FIRST instance's cost row from another satellite (or simply hidden if not available). Column 2 from GET /api/make-costs/:satId/:partInstId.
       - Per-line rows: Labor ($/hr × hrs), Material, Tooling, Cleanroom ($/hr × hrs), Test ($/hr × hrs), TOTAL (the GENERATED total_cost_usd).
       - Variance column showing Δ$ + Δ% + traffic-light per line (vs planned column when available; vs prev satellite's row when not).
       - "Edit" button reveals an inline form (or modal — Claude's discretion per CONTEXT.md). Form fields = all NUMERIC columns. Save = PUT /api/make-costs/:satId/:partInstId; on success re-fetch and re-render. Show optimistic spinner.
    6. **Buy Cost Sheet** (always shown):
       - Single-column-per-vendor table (today only one vendor; multi-vendor future): RFQ quoted unit cost, NRE, ordered qty, computed `quote_total = quoted × qty + NRE`, PO value, invoiced value, both variance columns side-by-side per CONTEXT decision #4: `variance_po_vs_quote ($, %)` and `variance_actual_vs_po ($, %)` each with traffic-light. Source: GET /api/buy-costs/:satId/:partInstId (which JOINs the buy_costs_variance view).
       - "Edit" button → form fields → PUT /api/buy-costs/:satId/:partInstId.
    7. **Δ vs previous satellite** — collapsed `<details>` (CONTEXT decision #5). Inside: per-line make-cost diff vs the same part on the previous satellite (program_start ordering). Source: query the cost-rollup endpoint OR derive client-side from a second `/api/make-costs/<prevSat>/<prevInst>` fetch. Recommend: add a tiny derivation: fetch `/api/satellites` → find prev sat by program_start order → fetch /api/make-costs for prev sat's instance of the same part_definition → diff.
    8. **History** (collapsed by default): inline `<details>` with rows from `/api/make-costs/:satId/:partInstId/history` and `/api/buy-costs/:satId/:partInstId/history`. Each row: version, total, created_at, created_by, notes.

    Wire to `cost-render.js` helpers (`trafficLight`, `formatMoney`, `formatPct`, `renderMakeSheet`, `renderBuySheet`, `renderDecisionPanel`). Implement those helpers in cost-render.js if not already in Task 1 (extend the file).

    All money values rendered via `formatMoney(...)`. The API returns money as JSON strings (Decimal.toJSON) — pass through formatMoney without `parseFloat`. For arithmetic on the client (e.g. delta), use `Number(stringValue)` only at the last moment for display; the API provides pre-computed variances so client-side math is minimal.

    Form validation MUST happen client-side (rationale length, positive numerics) BEFORE hitting the API. The API enforces too (defense in depth) but UX wants instant feedback.

    Smoke locally as in Task 1.

    Commit: `cd /Users/jeet/turion-space-demo && git add satellite/cost-detail.html satellite/cost-render.js && git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit -m "feat(24-04): cost-detail.html — make/buy sheets + decision panel + variance + delta-vs-prev"`.
  </action>
  <verify>
    `test -f /Users/jeet/turion-space-demo/satellite/cost-detail.html` returns 0.
    `grep -c "/api/make-costs" /Users/jeet/turion-space-demo/satellite/cost-detail.html` returns >= 2.
    `grep -c "/api/buy-costs" /Users/jeet/turion-space-demo/satellite/cost-detail.html` returns >= 2.
    `grep -c "/api/make-buy-decisions" /Users/jeet/turion-space-demo/satellite/cost-detail.html` returns >= 2.
    `grep -c "variance_po_vs_quote\|variance_actual_vs_po" /Users/jeet/turion-space-demo/satellite/cost-detail.html` returns >= 2.
    `grep -c "rationale\|min 20" /Users/jeet/turion-space-demo/satellite/cost-detail.html` returns >= 2.
    `grep -c "<details>" /Users/jeet/turion-space-demo/satellite/cost-detail.html` returns >= 2 (delta-vs-prev + history).
    HTML loads in browser without JS errors (auth-redirect on the live deploy is expected; static load should not error).
  </verify>
  <done>
    cost-detail.html ships with all 5 panels (decision, make sheet, buy sheet, delta-vs-prev, history). All API endpoints wired; rationale enforced ≥20 chars client-side; both variances rendered with traffic-light; delta-vs-prev is `<details>` collapsed-by-default. File committed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace part.html cost_breakdown panel + add Cost nav entry to all pages</name>
  <files>
    /Users/jeet/turion-space-demo/satellite/part.html
    /Users/jeet/turion-space-demo/satellite/index.html
    /Users/jeet/turion-space-demo/satellite/sat.html
    /Users/jeet/turion-space-demo/satellite/parts.html
    /Users/jeet/turion-space-demo/satellite/work-orders.html
    /Users/jeet/turion-space-demo/satellite/instance.html
    /Users/jeet/turion-space-demo/satellite/bom.html
    /Users/jeet/turion-space-demo/satellite/kanban.html
  </files>
  <action>
    Repo: turion-space-demo. Same git author rule.

    **Step 1 — Replace cost_breakdown panel on part.html** (CONTEXT decision #3 — first-class data, not approximation).

    Find the existing `cost_breakdown` panel in `part.html` (rendered from `/api/parts/:id/process` `cost_breakdown` and `materials_required` fields). Replace its data source:
    - For MAKE parts (`part.method === 'make'`): fetch `/api/make-costs/:sat/:partInstId` for the FIRST instance in the current sat. The handler doesn't have a "first instance" concept — derive client-side from the existing `/api/parts/:id/process` `instances[]` array (which the page already has). Pick the first.
    - For BUY parts: fetch `/api/buy-costs/:sat/:partInstId` similarly.
    - On 404 (no cost sheet entered yet): render a friendly empty state "Cost sheet not yet entered" + a CTA button "Enter cost sheet" linking to `cost-detail.html?sat=<satId>&part_inst=<firstInstId>`.
    - On success: render a compact panel showing TOTAL + per-line breakdown for make (or quoted/PO/invoiced for buy) + a "View full sheet" link to `cost-detail.html?...`.

    REMOVE the old approximation logic: the `materials_required` aggregation + `labor_cost = hrs × $150` computation must NOT be part of the new cost panel. Keep `materials_required` as a separate "Materials required" panel below if the page already had one — that's a distinct panel showing aggregated procurement_request data and stays.

    **Step 2 — Add `Cost` to top nav on 8 pages**:
    Each page (`index.html`, `sat.html`, `parts.html`, `work-orders.html`, `instance.html`, `bom.html`, `kanban.html`, `part.html`) has a `<nav>` block with links to other pages. Add a new link `<a href="cost.html" class="nav-link">Cost</a>` (or `class="nav-link active"` if the current page IS cost.html) in the SAME position across all pages — between "Work Orders" and "BOM" or wherever fits the existing alphabetical/logical order. Use grep to find each nav block first; reuse the existing nav classes/styling.

    **Step 3 — Smoke locally**: `python3 -m http.server 8080 -d /Users/jeet/turion-space-demo` then visit `http://localhost:8080/satellite/sat.html` and verify the nav now includes a `Cost` link. Click each nav-link to confirm navigation. (Pages still gated by auth; that's fine.)

    Commit: `cd /Users/jeet/turion-space-demo && git add satellite/part.html satellite/index.html satellite/sat.html satellite/parts.html satellite/work-orders.html satellite/instance.html satellite/bom.html satellite/kanban.html && git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit -m "feat(24-04): replace part.html approx cost panel with first-class data + add Cost nav"`.
  </action>
  <verify>
    `grep -l "cost.html" /Users/jeet/turion-space-demo/satellite/*.html | wc -l` returns >= 9 (cost.html itself + cost-detail.html + 7-8 others linking to it via nav).
    `grep -c "/api/make-costs\|/api/buy-costs" /Users/jeet/turion-space-demo/satellite/part.html` returns >= 2.
    `grep -c "LABOR_RATE\|150" /Users/jeet/turion-space-demo/satellite/part.html` returns 0 (the hardcoded $150/hr arithmetic is gone from the cost panel).
    Each of the 8 nav-modified pages contains `<a href="cost.html"`: `for f in index sat parts work-orders instance bom kanban part; do grep -q 'href="cost.html"' /Users/jeet/turion-space-demo/satellite/$f.html && echo OK $f || echo MISSING $f; done` — all 8 must print "OK".
  </verify>
  <done>
    part.html cost panel replaced with first-class data + empty-state + link to cost-detail. All 8 navigated pages have a `Cost` nav link. No more $150 hardcode in part.html. All committed.
  </done>
</task>

</tasks>

<verification>
- 2 new pages (cost.html, cost-detail.html) + 1 new JS module (cost-render.js) + 9 modified pages.
- All cost-related endpoints reachable from UI: rollup (cost.html), per-part make/buy/decision (cost-detail.html), per-part replacement panel (part.html).
- Local browser smoke: navigate to all 9 pages, no JS console errors (auth-gated content is expected).
- 3 commits on `github.com/jeet-avatar/turion-space-demo` with author `jm@techcloudpro.com` / `jeet-avatar`.
</verification>

<success_criteria>
- cost.html exists with satellite picker + per-subsystem rollup + totals card + collapsed prev-sat-delta.
- cost-detail.html exists with decision panel (rationale ≥20 client-validated) + make sheet + buy sheet (BOTH variances + traffic-light) + delta-vs-prev (collapsed) + history.
- cost-render.js exports `trafficLight`, `formatMoney`, `formatPct`, plus the per-section render helpers.
- part.html cost panel reads from /api/make-costs OR /api/buy-costs and shows empty-state with CTA when missing.
- 8 navigated pages have `Cost` nav link.
- Variance traffic-light thresholds = ±5% green / ±15% yellow / >15% red (CONTEXT decision #6).
- No browser console errors on static load.
</success_criteria>

<output>
After completion, create `.planning/phases/24-turion-satellite-make-buy-cost-module/24-04-SUMMARY.md`.
</output>
