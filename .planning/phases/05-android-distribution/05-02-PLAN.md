---
phase: 05-android-distribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/StudioProjects/eatfair-android/driver/build.gradle.kts
autonomous: true
requirements:
  - DIST-05

must_haves:
  truths:
    - "Driver app versionCode is 24 and versionName is 1.0.23"
    - "Driver APK is signed with the release keystore"
    - "Driver APK is uploaded to Firebase App Distribution for tester jeetnair.in@gmail.com"
    - "Driver app points to production API (api.dollor.ai)"
  artifacts:
    - path: "/Users/jeet/StudioProjects/eatfair-android/driver/build.gradle.kts"
      provides: "Bumped version numbers"
      contains: "versionCode = 24"
    - path: "/Users/jeet/StudioProjects/eatfair-android/driver/build/outputs/apk/release/driver-release.apk"
      provides: "Signed release APK"
  key_links:
    - from: "driver/build.gradle.kts"
      to: "Firebase App Distribution"
      via: "firebase appdistribution:distribute CLI"
      pattern: "driver-release.apk"
---

<objective>
Bump Android Driver app version, build release APK, and upload to Firebase App Distribution.

Purpose: Distribute the latest Driver app (with all API verification fixes from Phase 03) to testers via Firebase.
Output: Driver app vC=24/1.0.23 on Firebase App Distribution.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@/Users/jeet/StudioProjects/eatfair-android/driver/build.gradle.kts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump Driver app version and build release APK</name>
  <files>/Users/jeet/StudioProjects/eatfair-android/driver/build.gradle.kts</files>
  <action>
In `/Users/jeet/StudioProjects/eatfair-android/driver/build.gradle.kts`, update the defaultConfig block:
- Change `versionCode = 23` to `versionCode = 24`
- Change `versionName = "1.0.22"` to `versionName = "1.0.23"`

Verify the file already has `API_BASE_URL` set to `"https://api.dollor.ai/api"` and `IS_PRODUCTION` set to `true`. Do NOT change these -- they are already correct.

Then build the release APK:
```bash
cd /Users/jeet/StudioProjects/eatfair-android && ./gradlew :driver:assembleRelease
```

The APK will be at: `driver/build/outputs/apk/release/driver-release.apk`

If the build fails due to signing, check that `local.properties` has `RELEASE_KEYSTORE_PATH`, `RELEASE_KEYSTORE_PASSWORD`, `RELEASE_KEY_ALIAS`, `RELEASE_KEY_PASSWORD` set. These are loaded dynamically by build.gradle.kts.
  </action>
  <verify>
Run: `ls -la /Users/jeet/StudioProjects/eatfair-android/driver/build/outputs/apk/release/driver-release.apk` -- file must exist.
Run: `grep "versionCode = 24" /Users/jeet/StudioProjects/eatfair-android/driver/build.gradle.kts` -- must match.
Run: `grep 'versionName = "1.0.23"' /Users/jeet/StudioProjects/eatfair-android/driver/build.gradle.kts` -- must match.
  </verify>
  <done>Driver app build.gradle.kts has versionCode=24/versionName=1.0.23, and driver-release.apk exists in build outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Upload Driver APK to Firebase App Distribution</name>
  <files>/Users/jeet/StudioProjects/eatfair-android/driver/build/outputs/apk/release/driver-release.apk</files>
  <action>
Upload the built APK to Firebase App Distribution:
```bash
cd /Users/jeet/StudioProjects/eatfair-android && firebase appdistribution:distribute driver/build/outputs/apk/release/driver-release.apk \
  --app "1:65740760476:android:7d9bed1ee685434c42d459" \
  --testers "jeetnair.in@gmail.com" \
  --release-notes "Driver v1.0.23 (vC=24) - API verification fixes from Phase 03" \
  --project dollorai-production
```

Firebase App ID for Driver: `1:65740760476:android:7d9bed1ee685434c42d459`
Tester: `jeetnair.in@gmail.com`
Project: `dollorai-production`

If firebase CLI is not authenticated, run `firebase login` first.
  </action>
  <verify>
The firebase command output must show "Distribution complete" or equivalent success message. No error output.
  </verify>
  <done>Driver APK v1.0.23 (vC=24) is uploaded to Firebase App Distribution and tester jeetnair.in@gmail.com has been notified.</done>
</task>

</tasks>

<verification>
1. `grep "versionCode = 24" /Users/jeet/StudioProjects/eatfair-android/driver/build.gradle.kts` returns a match
2. `grep 'api.dollor.ai' /Users/jeet/StudioProjects/eatfair-android/driver/build.gradle.kts` confirms production API URL
3. Firebase App Distribution shows Driver v1.0.23 available for jeetnair.in@gmail.com
</verification>

<success_criteria>
- Driver app versionCode is 24 and versionName is 1.0.23 in build.gradle.kts
- Release APK was built successfully (signed with release keystore)
- APK uploaded to Firebase App Distribution for project dollorai-production
- Tester jeetnair.in@gmail.com notified of new build
</success_criteria>

<output>
After completion, create `.planning/phases/05-android-distribution/05-02-SUMMARY.md`
</output>
