---
phase: 32-build-procurement-process-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: ["/Users/jeet/turion-space-demo/satellite/satellite-3d.js", "/Users/jeet/turion-space-demo/satellite/work-order.html"]
autonomous: true
requirements: [ProcessConsistency]

must_haves:
  truths:
    - "satellite-3d.js no longer carries the dead diagnostics: `let frameCount = 0;` (decl), `frameCount++;` in tick(), and the entire `debugInfo: () => { ... }` method on the returned viewer handle are removed"
    - "the returned viewer handle still exposes `resize`, `deselect`, `selectChild`, `dispose` (and `controls` if it did before) — the leaf single-mesh path AND the Phase-31 assembly multi-mesh path (radial ring, raycaster picker, camera fly-to) are unchanged; the `resize: () => resize()` member STAYS (still used by part.html/instance.html's 2D→3D toggle re-measure)"
    - "work-order.html shows `signed_by_name` for each build step's sign-off instead of `(s.signed_by || '').slice(0,8)` (the UUID slice) — `GET /api/work-orders/:woId/steps` already returns `signed_by_name` (instance.html uses it), null-safe to '—'"
    - "nothing else regresses — the 3D viewer (leaf + assembly), the 2D/3D toggle, `?view=`, `#autoRotateChk`, the SVG fallback, the bom.html `🧊 3D` deep-link, and work-order.html's build-step list + sign-off action are all intact"
    - "Phase-29 audit stays 0 violations (no new inline onclick; no new API path)"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/satellite/satellite-3d.js"
      provides: "debugInfo()/frameCount removal (keeps resize/deselect/selectChild/dispose)"
    - path: "/Users/jeet/turion-space-demo/satellite/work-order.html"
      provides: "signed_by_name instead of UUID slice on the sign-off line"
  key_links:
    - from: "work-order.html (build-step rows)"
      to: "GET /api/work-orders/:woId/steps response field signed_by_name"
      via: "render r.escapeHtml(s.signed_by_name || '—') instead of (s.signed_by||'').slice(0,8)"
      pattern: "signed_by_name"
---

<objective>
Two consistency/cleanup edits, both pure-frontend and independent of the part.html / instance.html plans:
1. **satellite-3d.js** — remove the now-dead diagnostics added in commit 8179277 to chase the size-blowup (made redundant by commit 178aff1's real fix): `let frameCount = 0;` (~644), `frameCount++;` in `tick()` (~666), and the entire `debugInfo: () => { ... }` method on the returned viewer handle (~679-696). KEEP `resize: () => resize()`, `deselect`, `selectChild`, `dispose` (and `controls` if present) — `resize` is still used by part.html/instance.html's 2D→3D toggle re-measure. The leaf single-mesh path and the Phase-31 assembly multi-mesh path (radial ring + raycaster picker + camera fly-to) are untouched.
2. **work-order.html** — swap the build-step sign-off display from `(s.signed_by || '').slice(0,8)` (the first 8 chars of the technician's UUID) to `r.escapeHtml(s.signed_by_name || '—')` — `GET /api/work-orders/:woId/steps` already returns `signed_by_name` (instance.html consumes it); this just brings work-order.html into consistency with instance.html.

Purpose: The `[3d-wd]` watchdog (removed from part.html in 32-01, instance.html in 32-02) calls `viewerHandle.debugInfo` — once those page-side blocks are gone, the `debugInfo`/`frameCount` plumbing in satellite-3d.js is pure dead weight. And work-order.html showing a UUID slice instead of a name is the one remaining `ProcessConsistency` gap on that page. Both are small, low-risk, and have no dependency on the other plans (different files).
Output: `satellite-3d.js` with the diagnostics removed (handle API otherwise unchanged); `work-order.html` showing `signed_by_name` on sign-offs. FRONTEND-ONLY.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-build-procurement-process-documentation/32-RESEARCH.md
@.planning/phases/31-3d-dimension-hud-clickable-assemblies/31-02-SUMMARY.md

@/Users/jeet/turion-space-demo/satellite/satellite-3d.js
@/Users/jeet/turion-space-demo/satellite/work-order.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: satellite-3d.js — remove debugInfo() + frameCount (keep resize/deselect/selectChild/dispose)</name>
  <files>/Users/jeet/turion-space-demo/satellite/satellite-3d.js</files>
  <action>
1. Read `satellite-3d.js`. Find:
   - `let frameCount = 0;` (~644, near the returned handle / tick loop) — **delete the declaration.**
   - `frameCount++;` inside `tick()` (~666, the render loop) — **delete that line.**
   - The `debugInfo: () => { ... }` method in the returned handle object (~679-696, between the other handle members) — **delete the whole method** including its trailing comma. The handle is the object returned by `mount3DViewer` (search for `return {` near the bottom of `mount3DViewer`).
2. **Confirm what stays** on the returned handle: `resize: () => resize()` (or however it's spelled — it must still be a function), `deselect`, `selectChild`, `dispose` (and `controls` if it was exposed). Do NOT remove or rename any of these. The Phase-31 additions (`assemblyChildren` handling, `onSelect`, the raycaster picker, the camera fly-to tween, `intersectObjects(childGroups...)`) and the leaf single-mesh path are untouched.
3. Sanity: after the edit, `tick()` still calls `requestAnimationFrame(tick)` (or the equivalent), `renderer.render(...)`, and `controls.update()` — only the `frameCount++` line is gone. The `resize()` function (the one `resize: () => resize()` wraps) still does the `renderer.setSize(...)` / camera-aspect update — untouched.
  </action>
  <verify>
`grep -c "frameCount" /Users/jeet/turion-space-demo/satellite/satellite-3d.js` → 0; `grep -c "debugInfo" satellite-3d.js` → 0; `grep -c "resize:" satellite-3d.js` → ≥1 (or `grep -n "resize" satellite-3d.js` shows the handle member intact); `grep -c "dispose:" satellite-3d.js` → ≥1; `grep -c "selectChild\|deselect" satellite-3d.js` → ≥1; `grep -c "intersectObjects" satellite-3d.js` → ≥1 (Phase-31 picker intact). `node -c /Users/jeet/turion-space-demo/satellite/satellite-3d.js` if it's a CommonJS/script file, or open `https://...` later in 32-04 — at minimum the file must still parse (no dangling comma / brace). Open `3d-test.html` (locally or via the deploy smoke in 32-04): the 8-family harness still renders.
  </verify>
  <done>
satellite-3d.js no longer contains `frameCount` or `debugInfo`; the returned viewer handle still exposes `resize`, `deselect`, `selectChild`, `dispose` (and `controls` if before); the leaf single-mesh path and the Phase-31 assembly multi-mesh path (radial ring, raycaster picker, camera fly-to) are unchanged; the file parses cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: work-order.html — show signed_by_name instead of the UUID slice on build-step sign-offs</name>
  <files>/Users/jeet/turion-space-demo/satellite/work-order.html</files>
  <action>
1. Read `work-order.html` (168 lines). Find where it renders each build step's sign-off — RESEARCH says it's `(s.signed_by || '').slice(0,8)` (the first 8 chars of the technician UUID). Confirm the exact expression and the loop variable name (`s` for the step).
2. Confirm `GET /api/work-orders/:woId/steps` returns `signed_by_name` — RESEARCH says yes (the route resolves it via a `team_members` join; instance.html consumes `signed by ${signed_by_name}`). If you want extra certainty, grep `/Users/jeet/turion-satellite/backend/src/routes/work-orders.ts` (or wherever `/:woId/steps` lives) for `signed_by_name` / `full_name`. Do NOT add a backend change — if `signed_by_name` somehow isn't there, fall back to keeping the slice and note it; but RESEARCH is HIGH-confidence it is there.
3. **Replace** `(s.signed_by || '').slice(0,8)` with `r.escapeHtml(s.signed_by_name || '—')` (use the page's actual shared-render alias — it's `r` / `window.satelliteRender` per the page idiom; confirm by reading the file's `<script>` header). If the sign-off line currently shows e.g. `Signed: a1b2c3d4` change it to `Signed: {name}` (or `—` when unsigned). Keep the `signed_at` date rendering (via `r.fmtDate`) as-is. Keep the build-step list structure, the result pass/fail/rework badges, the step_type tags, the torque/duration/inspection fields, and the sign-off ACTION (the POST that signs a step) — only the *display* of who signed changes.
  </action>
  <verify>
`grep -c "signed_by.*slice(0" /Users/jeet/turion-space-demo/satellite/work-order.html` → 0; `grep -n "signed_by_name" work-order.html` → ≥1; `grep -n "escapeHtml" work-order.html` → the new sign-off render goes through it. Open `work-order.html?id=<a work-order id with signed steps on SAT-003>` (or curl the markup + reason about it): signed build steps show the technician's name (not an 8-char hex string); unsigned steps show `—`; the result badges / step_type tags / torque / duration / the sign-off POST action all still work.
  </verify>
  <done>
work-order.html renders `r.escapeHtml(s.signed_by_name || '—')` for each build step's sign-off instead of the UUID slice; the rest of the build-step list + the sign-off action are unchanged; consistent with how instance.html shows the signer.
  </done>
</task>

</tasks>

<verification>
- `grep` proof: `frameCount` + `debugInfo` absent (0) in satellite-3d.js; `resize:` / `dispose:` / `selectChild`/`deselect` / `intersectObjects` still present; `signed_by.*slice(0` absent (0) in work-order.html, `signed_by_name` present
- satellite-3d.js parses cleanly (no dangling comma/brace) — `node -c` or the 3d-test.html render in 32-04
- 3D viewer (leaf + Phase-31 assembly), 2D/3D toggle, `?view=`, `#autoRotateChk`, SVG fallback, bom.html `🧊 3D` deep-link all intact (verified in 32-04 deploy smoke)
- work-order.html: signed steps show the technician name, unsigned show `—`; result badges / step_type / torque / duration / sign-off POST action unchanged
- Phase-29 audit 0 violations (no new inline onclick / API path) — verified in 32-04
</verification>

<success_criteria>
- `debugInfo()` + `frameCount` removed from satellite-3d.js; the viewer handle API (`resize`/`deselect`/`selectChild`/`dispose`/`controls`) and both render paths (leaf + Phase-31 assembly) unchanged
- work-order.html shows `signed_by_name` (null-safe `—`) on build-step sign-offs instead of an 8-char UUID slice — `ProcessConsistency` gap on that page closed
- FRONTEND-ONLY; nothing in Phases 27-31 regresses; Phase-29 audit stays 0 violations
</success_criteria>

<output>
After completion, create `.planning/phases/32-build-procurement-process-documentation/32-03-SUMMARY.md`
</output>
