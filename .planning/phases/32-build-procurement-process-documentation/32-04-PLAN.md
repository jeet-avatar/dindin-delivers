---
phase: 32-build-procurement-process-documentation
plan: 04
type: execute
wave: 2
depends_on: ["32-01", "32-02", "32-03"]
files_modified: []
autonomous: false
requirements: [MakeBuyDecisionUI, MakeProcessUI, BuyProcessUI, ProcessConsistency]

must_haves:
  truths:
    - "the turion-space-demo frontend is redeployed to S3 `turion-demo-static` + CloudFront `E37R9PT8IL44L2` invalidated, so live part.html / instance.html / work-order.html / satellite-3d.js carry the Phase-32 changes — FRONTEND-ONLY, no turion-satellite Lambda redeploy (no build-and-push.sh)"
    - "the F6 deploy-hygiene pre-flight ran before deploy-frontend.sh: the unrelated dirty `*.html` outside `satellite/` (about-this-demo.html, agent-sales-cash.html, dashboard-cio.html — adjust to whatever `git status --short` actually shows) were git-stashed and the untracked `.superpowers/` was moved aside before `aws s3 sync . --delete`, both restored after — no unrelated WIP rode the deploy"
    - "the Phase-29 button audit = 0 violations (BOTH the turion-satellite/backend/scripts/audit-satellite-buttons.mjs source and the turion-space-demo re-export wrapper), before AND after deploy — any new satelliteApi.get('/api/...') path resolves against an already-mounted route, no new inline onclick"
    - "live curl/HEAD smoke passes: deployed part.html contains `make-buy-decisions` + `Procurement chain` + `PO issued` and does NOT contain `[3d-wd]` / `RFQ · PO · Vendor build` / `Receiving`; deployed instance.html contains `make-buy-decisions` + `buy-costs` and does NOT contain `[3d-wd]`; deployed satellite-3d.js does NOT contain `frameCount` / `debugInfo` and DOES contain `intersectObjects` / `resize`; work-order.html contains `signed_by_name` and not `signed_by`...`slice(0`; bom.html `view=3d` deep-link unchanged; login.html 200; jsDelivr Three.js 0.184.0 200+CORS"
    - "the live behaviour is verified — a browser walk if available, otherwise the documented headless-substitute (curl/HEAD smoke + audit 0 violations + the route-alive probes for /api/make-buy-decisions and /api/buy-costs) per the Phase 27-31 precedent — and the outcome is recorded in the SUMMARY"
    - "both the new turion-space-demo commits (Plans 32-01/32-02/32-03) are pushed to origin/main authored `jeet-avatar <jm@techcloudpro.com>`; no turion-satellite commit/push/deploy this phase"
  artifacts: []
  key_links:
    - from: "git push (turion-space-demo only)"
      to: "deploy-frontend.sh → S3 turion-demo-static + CloudFront E37R9PT8IL44L2 invalidation"
      via: "code on origin/main → aws s3 sync . --delete → CloudFront invalidation"
      pattern: "deploy-frontend\\.sh"
---

<objective>
Ship Phase 32 — FRONTEND-ONLY. Run the Phase-29 button audit (0 violations, both repos), push the Plan 32-01/32-02/32-03 commits in `turion-space-demo` (no turion-satellite changes this phase — no `build-and-push.sh`, no Lambda redeploy), redeploy the frontend via `deploy-frontend.sh` WITH the F6 deploy-hygiene pre-flight (the script does `aws s3 sync . --delete` over the whole repo — unrelated dirty ERP-demo `*.html` + `.superpowers/` must be stashed/moved aside first, restored after) + the CloudFront `E37R9PT8IL44L2` invalidation, run curl/HEAD smoke checks against the live site, then the human-verify checkpoint for the live Realization / Build & Procurement section (with the documented headless-substitute path per the Phase 27-31 precedent).

Purpose: A deploy is a task inside a GSD phase plan, never ad-hoc. This is the only wave that pushes / runs `deploy-frontend.sh`. It guards against `--delete` clobbering unrelated WIP and against a `dead-onclick` / `missing-endpoint` audit regression.
Output: frontend live at `turionspace.zietra.com/satellite/` with the Decision card + symmetric BUY "Procurement chain" panel + fixed BUY workflow visualizer (part.html), the decision card + buy_costs numbers on the Manufacturing/Procurement panel (instance.html), `signed_by_name` on sign-offs (work-order.html), and the `[3d-wd]`/`debugInfo`/`frameCount` diagnostics gone; the turion-space-demo commits pushed under `jeet-avatar <jm@techcloudpro.com>`; audit 0 violations; smoke checks pass; SUMMARY written. NO turion-satellite Lambda redeploy.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-build-procurement-process-documentation/32-RESEARCH.md
@.planning/phases/31-3d-dimension-hud-clickable-assemblies/31-04-PLAN.md
@.planning/phases/31-3d-dimension-hud-clickable-assemblies/31-04-SUMMARY.md
@.planning/phases/32-build-procurement-process-documentation/32-01-PLAN.md
@.planning/phases/32-build-procurement-process-documentation/32-02-PLAN.md
@.planning/phases/32-build-procurement-process-documentation/32-03-PLAN.md

# The deploy script lives in the sibling repo:
@/Users/jeet/turion-space-demo/deploy-frontend.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deploy audit + confirm + push the turion-space-demo commits (frontend-only — NO turion-satellite push/deploy)</name>
  <files>none (deploy actions — no files in this repo)</files>
  <action>
1. **Pre-deploy button audit (both repos):**
   - `cd /Users/jeet/turion-satellite/backend && node scripts/audit-satellite-buttons.mjs` → MUST print `0 violations`, exit 0. (This audit scans the turion-space-demo `satellite/*.html` — it lives in the backend repo.)
   - `cd /Users/jeet/turion-space-demo && node scripts/audit-satellite-buttons.mjs` (the thin re-export wrapper, if present) → MUST print `0 violations`, exit 0.
   - If either reports a violation, STOP — do NOT deploy; report it (the fix belongs in Plan 32-01/32-02/32-03, not here).
2. **Confirm the expected local commits exist (from Plans 32-01/32-02/32-03 — all in turion-space-demo):**
   - `cd /Users/jeet/turion-space-demo && git log origin/main..HEAD --oneline` → expect commit(s) for: part.html (decision card + Procurement chain + BUY workflow fix + `[3d-wd]` removal), instance.html (decision card + buy_costs numbers + `[3d-wd]` removal), satellite-3d.js (`debugInfo`/`frameCount` removal) + work-order.html (`signed_by_name`). Could be 1–3 commits depending on how the executors batched — what matters is the diff covers all four files.
   - Verify each is authored `jeet-avatar <jm@techcloudpro.com>`: `git log --format='%an <%ae>' origin/main..HEAD` → all `jeet-avatar <jm@techcloudpro.com>`.
   - `cd /Users/jeet/turion-satellite && git log origin/main..HEAD --oneline` → MUST be EMPTY (Phase 32 makes NO backend change — if there's an unexpected backend commit, STOP and investigate; the phase is frontend-only).
3. **Push (turion-space-demo only):** `cd /Users/jeet/turion-space-demo && git push origin main`. Do NOT push or build/deploy turion-satellite — there's nothing to push there, and `build-and-push.sh` is NOT run this phase.
  </action>
  <verify>
Both `audit-satellite-buttons.mjs` runs printed `0 violations` (exit 0).
`cd /Users/jeet/turion-space-demo && git log origin/main..HEAD --oneline` → empty AFTER the push; `git log -1 --format='%an <%ae>'` → `jeet-avatar <jm@techcloudpro.com>`; the cumulative diff of the pushed commits touches `satellite/part.html`, `satellite/instance.html`, `satellite/satellite-3d.js`, `satellite/work-order.html` (and nothing else).
`cd /Users/jeet/turion-satellite && git log origin/main..HEAD --oneline` → empty (no backend commit this phase).
  </verify>
  <done>
Audit 0 violations from both repos; the turion-space-demo Phase-32 commit(s) (covering part.html / instance.html / satellite-3d.js / work-order.html) pushed under `jeet-avatar <jm@techcloudpro.com>`; turion-satellite has NO Phase-32 commit and is NOT deployed this phase.
  </done>
</task>

<task type="auto">
  <name>Task 2: F6 deploy-hygiene pre-flight → deploy-frontend.sh → CloudFront invalidation → smoke checks → restore</name>
  <files>none (deploy actions — no files in this repo)</files>
  <action>
1. **F6 pre-flight** (the `deploy-frontend.sh` does `aws s3 sync . --delete` over the WHOLE `turion-space-demo` repo — keep unrelated dirty WIP out of it):
   - `cd /Users/jeet/turion-space-demo && git status --short` — identify the dirty/untracked files NOT part of Phase 32. Per the Phase 30/31 precedent these are: `about-this-demo.html`, `agent-sales-cash.html`, `dashboard-cio.html` (uncommitted ERP-demo WIP — adjust to whatever `git status --short` actually shows as unrelated tracked-dirty `*.html`), `backend/*` (already `--exclude`d by the script), and untracked `.superpowers/` (contains `.html` files — would ride along).
   - Stash the unrelated tracked WIP: `git stash push -- about-this-demo.html agent-sales-cash.html dashboard-cio.html` (adjust the file list to the actual `git status --short` output — do NOT stash `satellite/*`, which is Phase-32 work already committed in HEAD).
   - Move `.superpowers/` aside: `mv .superpowers /tmp/phase32-superpowers-stash` (only if it exists).
   - Confirm `git status --short` now shows ONLY `backend/*` (excluded by the script) and a clean `satellite/`.
2. **Deploy the frontend:**
   - `cd /Users/jeet/turion-space-demo && bash deploy-frontend.sh` — this regenerates the gitignored `satellite-config.js` from AWS Secrets Manager, then `aws s3 sync . s3://turion-demo-static --delete` (uploading the committed `satellite/part.html`, `satellite/instance.html`, `satellite/satellite-3d.js`, `satellite/work-order.html`, the regenerated config, and the COMMITTED bytes of any other tracked files — `.superpowers/*` and `backend/*` correctly absent), then a CloudFront invalidation on dist `E37R9PT8IL44L2`.
   - Capture the CloudFront invalidation ID from the script output; poll `aws cloudfront get-invalidation --distribution-id E37R9PT8IL44L2 --id <INVALIDATION_ID> --query 'Invalidation.Status' --output text` until it's `Completed`.
3. **Smoke checks (curl/HEAD against the live site):**
   - **part.html:** `curl -s "https://turionspace.zietra.com/satellite/part.html"` → `grep -c "make-buy-decisions"` ≥1; `grep -c "Procurement chain"` ≥1; `grep -c "PO issued"` ≥1; `grep -c "\[3d-wd\]"` == 0; `grep -c "RFQ · PO · Vendor build"` == 0; `grep -c "Receiving"` == 0; `grep -c "debugInfo"` == 0; `grep -c 'type="importmap"'` ≥1; `grep -c "cdn.jsdelivr.net/npm/three@0.184.0"` ≥1; `grep -c "mode-3d"` ≥1; `grep -c "frame-svg"` ≥1; `grep -c "cad-hud"` ≥3 (Phase-31 HUD intact).
   - **instance.html:** `curl -s "https://turionspace.zietra.com/satellite/instance.html"` → `grep -c "make-buy-decisions"` ≥1; `grep -c "buy-costs"` ≥1; `grep -c "\[3d-wd\]"` == 0; `grep -c "debugInfo"` == 0; `grep -c "cad-hud"` ≥3; `grep -c "/children?sat="` ≥1 (Phase-31 fetch intact); `grep -c "mode-3d"` ≥1; `grep -c "frame-svg"` ≥1.
   - **satellite-3d.js:** `curl -s "https://turionspace.zietra.com/satellite/satellite-3d.js"` → `grep -c "frameCount"` == 0; `grep -c "debugInfo"` == 0; `grep -c "intersectObjects"` ≥1 (Phase-31 picker intact); `grep -c "resize"` ≥1; `curl -sI "https://turionspace.zietra.com/satellite/satellite-3d.js"` → `HTTP/2 200` + a JS content-type.
   - **work-order.html:** `curl -s "https://turionspace.zietra.com/satellite/work-order.html"` → `grep -c "signed_by_name"` ≥1; `grep -Ec "signed_by[^_].{0,12}slice\(0"` == 0 (the UUID slice is gone).
   - **regression guards:** `curl -sI "https://turionspace.zietra.com/satellite/3d-test.html"` → `HTTP/2 200`; `curl -s "https://turionspace.zietra.com/satellite/bom.html" | grep -c "view=3d"` ≥1 (Phase-30 `🧊 3D` deep-link unchanged); `curl -sI "https://turionspace.zietra.com/satellite/cost-detail.html"` → `HTTP/2 200`; `curl -sI "https://turionspace.zietra.com/satellite/login.html"` → `HTTP/2 200` (the `--delete` didn't break the app).
   - **jsDelivr:** `curl -sI "https://cdn.jsdelivr.net/npm/three@0.184.0/build/three.module.min.js"` and `curl -sI "https://cdn.jsdelivr.net/npm/three@0.184.0/examples/jsm/controls/OrbitControls.js"` → both `HTTP/2 200` + `access-control-allow-origin: *`.
   - **route-alive probes:** `curl -s -o /dev/null -w "%{http_code}" "https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/make-buy-decisions/24587565-b15b-42ce-b590-87ecf9b6bb99/00000000-0000-0000-0000-000000000000"` → `401` or `404` (route mounted — a `401`/`404` here vs a generic 404 for a bogus prefix proves the route exists; do NOT expect 200, no auth token); same for `curl -s -o /dev/null -w "%{http_code}" "https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/buy-costs/24587565-b15b-42ce-b590-87ecf9b6bb99/00000000-0000-0000-0000-000000000000"` → `401`/`404`. (Both routes already existed before Phase 32 — this just confirms the frontend isn't pointing at a phantom path.)
4. **Post-deploy audit:** `cd /Users/jeet/turion-satellite/backend && node scripts/audit-satellite-buttons.mjs` → `0 violations`, exit 0.
5. **Restore the working tree:** `mv /tmp/phase32-superpowers-stash .superpowers` (if it was moved) + `cd /Users/jeet/turion-space-demo && git stash pop` (restores the ERP-demo WIP). Confirm `git status --short` matches the pre-deploy baseline and `git stash list` is empty.

Do NOT run any ad-hoc `aws ecs` / `docker build` / `docker push` / direct ECR commands, and do NOT run `build-and-push.sh` — Phase 32 is FRONTEND-ONLY; only `deploy-frontend.sh` runs.
  </action>
  <verify>
The F6 pre-flight ran: `git stash list` was non-empty mid-deploy and is empty after `stash pop`; `.superpowers/` was absent from the `aws s3 sync` output and is back in the working tree after restore.
CloudFront invalidation polled to `Completed`.
All smoke checks above pass (the part.html / instance.html / satellite-3d.js / work-order.html greps return their expected counts including the `== 0` negatives; the HEAD checks return 200; bom.html `view=3d` unchanged; jsDelivr 200+CORS; the `/api/make-buy-decisions` and `/api/buy-costs` probes return 401/404 = route alive).
Post-deploy `node scripts/audit-satellite-buttons.mjs` → 0 violations, exit 0.
`cd /Users/jeet/turion-space-demo && git status --short` after restore == the pre-deploy baseline (the ERP HTML modifications + `backend/*` + `?? .superpowers/`); `git stash list` empty.
  </verify>
  <done>
F6 pre-flight ran (unrelated ERP WIP stashed, `.superpowers/` moved aside) before `deploy-frontend.sh`; the frontend is live on `turionspace.zietra.com/satellite/` (S3 `turion-demo-static`, CloudFront `E37R9PT8IL44L2` invalidation `Completed`); curl/HEAD smoke checks all pass (part.html carries `make-buy-decisions` + `Procurement chain` + `PO issued`, no `[3d-wd]` / `RFQ · PO · Vendor build` / `Receiving` / `debugInfo`; instance.html carries `make-buy-decisions` + `buy-costs`, no `[3d-wd]`; satellite-3d.js has no `frameCount`/`debugInfo` but keeps `intersectObjects`/`resize`; work-order.html has `signed_by_name`, no UUID slice; bom.html `view=3d` unchanged; login.html / cost-detail.html / 3d-test.html 200; jsDelivr Three.js 0.184.0 200+CORS; the `/api/make-buy-decisions` + `/api/buy-costs` route-alive probes pass); post-deploy audit 0 violations; the working tree restored to the pre-deploy baseline with an empty stash. NO `build-and-push.sh` was run (frontend-only).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the live Realization / Build & Procurement section (make AND buy)</name>
  <files>none (verification only)</files>
  <action>
This is a blocking human-verify checkpoint AFTER the deploy automation in Tasks 1-2.

**What's built (live at `turionspace.zietra.com/satellite/`):** (1) `part.html` now has a "Realization" section: a Decision card at the top (MAKE/BUY tag · rationale · "decided by {name} on {date}" · re-evaluate note — from `make_buy_decisions`, latest non-superseded; "open from a satellite" copy when there's no `?sat=`); for MAKE parts the existing Build process panel + Materials required + labor cost breakdown sit under it; for BUY parts a top-level "Procurement chain" panel (purchase-request cards → vendor-order cards → PO + invoiced from `/api/buy-costs`) with the SAME prominence as "Build process", and the workflow visualizer's BUY steps are now `Decision → Quote → Purchase request → Vendor order → PO issued → Invoiced` (no phantom RFQ/Receiving/Acceptance). (2) `instance.html`'s "Manufacturing / Procurement" panel now leads with a Decision card; BUY instances additionally show the authoritative buy_costs numbers (quoted/NRE/PO value/invoiced) after the existing PR + VO cards; the per-unit `cost_breakdown` panel is unchanged. (3) `work-order.html` shows the technician name on sign-offs instead of an 8-char UUID slice. (4) The temporary `[3d-wd]` watchdog (part.html/instance.html) and `debugInfo()`/`frameCount` (satellite-3d.js) are removed — the 3D viewer (leaf + Phase-31 assembly ring), the 2D/3D toggle, `?view=`, `#autoRotateChk`, the SVG fallback, the Phase-31 dimension HUD, and `bom.html`'s `🧊 3D` deep-link are all unchanged.

**Browser walk (if a browser/WebGL is available):**
1. `https://turionspace.zietra.com/satellite/part.html?id=<a MAKE partDef on SAT-003>&sat=24587565-b15b-42ce-b590-87ecf9b6bb99` — the "Realization" section shows a Decision card with a `MAKE — internal build` tag + the rationale text + "decided by {Name} on {date}"; below it the Build process panel (step rows with pass/fail/rework circles + step_type + torque + est duration), Materials required, and the labor cost breakdown; the workflow visualizer shows the MAKE steps with the right one highlighted.
2. `https://turionspace.zietra.com/satellite/part.html?id=<a BUY partDef on SAT-003>&sat=24587565-b15b-42ce-b590-87ecf9b6bb99` — the Decision card shows a `BUY — vendor-supplied` tag + rationale + signer/date; below it a "Procurement chain" panel (📋 purchase-request card(s) → 📦 vendor-order card(s) with vendor/country/ITAR + qty + PO + lead → a "Purchase order & invoice" block with quoted unit cost / NRE / PO value / invoiced) — visually as prominent as "Build process" is for make parts; the workflow visualizer shows `Decision → Quote → Purchase request → Vendor order → PO issued → Invoiced` with the right step lit; no "No build process recorded yet" placeholder.
3. `https://turionspace.zietra.com/satellite/part.html?id=<the same BUY partDef>` (NO `&sat=`) — the Decision card shows "Open this part from a satellite to see the recorded make/buy decision" + the default chip; no crash, no error toast.
4. `https://turionspace.zietra.com/satellite/instance.html?sat=24587565-b15b-42ce-b590-87ecf9b6bb99&id=<a BUY instance#1 on SAT-003>` — the "Manufacturing / Procurement" panel leads with a Decision card (BUY), then the existing PR + VO cards, then a "Purchase order & invoice" card with the buy_costs figures; the per-unit cost panel above is unchanged; the integrations panel / subtree cost rollup / lifecycle timeline / BOM-children gallery / sibling instances below all still render; the Phase-31 dimension HUD on the CAD strip still shows.
5. `https://turionspace.zietra.com/satellite/instance.html?sat=...&id=<a MAKE instance#1 on SAT-003>` — Decision card (MAKE) → the existing WO list + inline build steps (with "signed by {Name}").
6. `https://turionspace.zietra.com/satellite/work-order.html?id=<a WO with signed steps on SAT-003>` — signed build steps show the technician's name (not hex); unsigned show `—`; the result badges / step_type tags / torque / duration / the sign-off action all still work.
7. `https://turionspace.zietra.com/satellite/cost-detail.html?sat=...&part=<a partDef>` — unchanged (still has its own decision panel + make/buy cost sheets + integrations panel).
8. `https://turionspace.zietra.com/satellite/bom.html?sat=...` — the `🧊 3D` per-row deep-link still works; the 3D viewer on part.html/instance.html still rotates/zooms and the 2D/3D toggle works; with WebGL disabled, part.html falls back to the 2D isometric SVG with Phase-27 callouts — no crash.

**Headless substitute (if no browser/WebGL — the established case for Phases 27-31):** the curl/HEAD smoke checks in Task 2 (deployed part.html contains `make-buy-decisions` + `Procurement chain` + `PO issued` and not `[3d-wd]`/`RFQ · PO · Vendor build`/`Receiving`/`debugInfo`; instance.html contains `make-buy-decisions` + `buy-costs` and not `[3d-wd]`; satellite-3d.js has no `frameCount`/`debugInfo` but keeps `intersectObjects`/`resize`; work-order.html has `signed_by_name` and not the UUID slice; bom.html `view=3d` unchanged; login.html/cost-detail.html/3d-test.html 200; jsDelivr 200+CORS) + the post-deploy audit (0 violations) + the `/api/make-buy-decisions` and `/api/buy-costs` route-alive probes (401/404) ARE the orchestrator's approval to complete the plan. Record in the SUMMARY: "visual sign-off DEFERRED — headless environment; curl/HEAD smoke + audit 0 violations + the route-alive probes passed; a literal browser walk of the Realization section (MAKE: Decision card + Build process + Materials + labor cost; BUY: Decision card + Procurement chain panel + 6-step workflow viz), instance.html's Decision card + buy_costs card, work-order.html's signed_by_name, and the 3D viewer / 2D-3D toggle / WebGL-off fallback is a follow-up if a sign-off is required." Do NOT block the phase on the browser walk in a headless run — same precedent as Phases 27/28/29/30/31.

**Resume signal:** Type "approved" (or, in a headless run, the orchestrator's headless-substitute approval), or describe issues.
  </action>
  <verify>
Either: a human confirms the browser walk (steps 1-8 above) and types "approved"; OR (headless) the Task-2 curl/HEAD smoke checks + post-deploy audit (0 violations) + the `/api/make-buy-decisions` & `/api/buy-costs` 401/404 route-alive probes all passed and the headless-substitute approval is recorded in the SUMMARY per the Phase 27-31 precedent.
  </verify>
  <done>
The live Realization / Build & Procurement section (part.html: Decision card + MAKE Build process / BUY Procurement chain + fixed BUY workflow viz; instance.html: Decision card + buy_costs card; work-order.html: signed_by_name; `[3d-wd]`/`debugInfo`/`frameCount` gone; Phases 27-31 intact) is verified — either by a browser walk ("approved") or by the documented headless-substitute (Task-2 smoke + audit + route-alive probes) per the Phase 27-31 precedent; the outcome is recorded in the SUMMARY.
  </done>
</task>

</tasks>

<verification>
- Pre- AND post-deploy `audit-satellite-buttons.mjs` (both repos) → 0 violations, exit 0
- turion-space-demo pushed (`git log origin/main..HEAD` empty after push); commits authored `jeet-avatar <jm@techcloudpro.com>`; the diff touches only `satellite/part.html`, `satellite/instance.html`, `satellite/satellite-3d.js`, `satellite/work-order.html`
- turion-satellite has NO Phase-32 commit; `build-and-push.sh` NOT run; no Lambda redeploy
- F6 pre-flight ran (ERP WIP stashed, `.superpowers/` moved aside, restored after — `git stash list` empty post-restore, working tree == pre-deploy baseline)
- `deploy-frontend.sh` ran; CloudFront `E37R9PT8IL44L2` invalidation `Completed`
- Live smoke: part.html has `make-buy-decisions`+`Procurement chain`+`PO issued`, NOT `[3d-wd]`/`RFQ · PO · Vendor build`/`Receiving`/`debugInfo`; instance.html has `make-buy-decisions`+`buy-costs`, NOT `[3d-wd]`; satellite-3d.js has NO `frameCount`/`debugInfo`, HAS `intersectObjects`/`resize`; work-order.html has `signed_by_name`, NOT the UUID slice; bom.html `view=3d` unchanged; login.html/cost-detail.html/3d-test.html 200; jsDelivr Three.js 0.184.0 200+CORS; `/api/make-buy-decisions` & `/api/buy-costs` 401/404 route-alive
- Human-verify checkpoint reached (browser walk OR documented headless-substitute approval per Phase 27-31)
</verification>

<success_criteria>
- frontend live on `turionspace.zietra.com/satellite/` with the Phase-32 Realization section (make + buy), the decision card + buy_costs on instance.html, `signed_by_name` on work-order.html, and the `[3d-wd]`/`debugInfo`/`frameCount` diagnostics removed; Phases 27-31 (3D viewer + Phase-31 HUD + assembly ring, BOM gallery, integrations panel, sub-parts gallery, parent trail, sibling instances, cost rollup, `?view=`, SVG fallback, bom.html `🧊 3D`) all unchanged
- FRONTEND-ONLY — no turion-satellite commit, no `build-and-push.sh`, no Lambda redeploy
- F6 pre-flight kept unrelated ERP WIP + `.superpowers/` out of the `aws s3 sync . --delete`; working tree restored
- Phase-29 button audit 0 violations (before + after); turion-space-demo commits pushed under `jeet-avatar <jm@techcloudpro.com>`
- Human-verify checkpoint satisfied (browser walk or headless-substitute per the Phase 27-31 precedent)
- No ad-hoc `aws ecs`/`docker build`/`docker push`/manual ECR commands; only `deploy-frontend.sh`
</success_criteria>

<output>
After completion, create `.planning/phases/32-build-procurement-process-documentation/32-04-SUMMARY.md`
</output>
