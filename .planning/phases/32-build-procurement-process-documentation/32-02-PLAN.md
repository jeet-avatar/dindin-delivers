---
phase: 32-build-procurement-process-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["/Users/jeet/turion-space-demo/satellite/instance.html"]
autonomous: true
requirements: [MakeBuyDecisionUI, BuyProcessUI, ProcessConsistency]

must_haves:
  truths:
    - "instance.html's 'Manufacturing / Procurement' panel (the commit-29260a0 panel, #procPanelAnchor / #procPanelTitle / #woMeta / #woList) shows the recorded make/buy DECISION card at the top — decision (MAKE/BUY tag), rationale, decided_by_name, decided_at — fetched from GET /api/make-buy-decisions/:satId/:partDefId (?sat= is always present on instance.html; partDefId = inst.part_definition_id), 404 treated as 'no decision yet' (no error toast)"
    - "for BUY instances, the panel additionally shows the authoritative buy_costs numbers — quoted_unit_cost_usd, nre_cost_usd, po_value_usd, invoiced_value_usd — fetched from GET /api/buy-costs/:satId/:partDefId?part_inst=<partInstId> (verify the exact query-param shape against buy-costs.ts), rendered alongside the existing PR cards (📋 Purchase request) and VO cards (📦 Vendor order — vendor); the existing per-unit cost panel (sourced from /process's heuristic cost_breakdown) is left as-is per the locked decision"
    - "the existing MAKE side of the panel (title 'Manufacturing — build process', work-order list with WO-xxxxxxxx / bay / started_at / status, inline build steps for the active WO with step circle pass/fail/pending + step_type · est duration · torque · 'signed by Name') is unchanged except now sits under the new decision card"
    - "the temporary [3d-wd] watchdog block in instance.html (~493-508, the viewerHandle.debugInfo setInterval) is removed; the requestAnimationFrame(() => viewerHandle.resize()) call in set3D stays"
    - "nothing else on instance.html regresses — the hero CAD strip + 3D viewer + dimension HUD (Phase 31), the spec sheet, the integrations panel (r.renderIntegrationsPanel), the subtree cost rollup + parent trail, the lifecycle timeline + advance/revert, the BOM-children make/buy-aware empty state, the sibling instances; the instance_index>1 'tracked on instance #1' hints are NOT touched"
    - "no NEW inline onclick added (new clicks via addEventListener); the new satelliteApi.get('/api/make-buy-decisions/...') and '/api/buy-costs/...' paths both resolve against already-mounted routes — Phase-29 audit stays 0 violations"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/satellite/instance.html"
      provides: "decision card on the Manufacturing/Procurement panel + buy_costs PO/invoiced numbers + [3d-wd] removal"
  key_links:
    - from: "instance.html"
      to: "GET /api/make-buy-decisions/:satId/:partDefId"
      via: "safeGet (404 → null); satId from ?sat=, partDefId from inst.part_definition_id"
      pattern: "make-buy-decisions/\\$\\{"
    - from: "instance.html (BUY branch of the Manufacturing/Procurement panel)"
      to: "GET /api/buy-costs/:satId/:partDefId?part_inst=..."
      via: "new fetch in the buy branch (verify the param shape against buy-costs.ts)"
      pattern: "buy-costs/\\$\\{"
---

<objective>
On `instance.html`: (1) fetch the recorded make/buy DECISION (`GET /api/make-buy-decisions/:satId/:partDefId`, satId from `?sat=`, partDefId from `inst.part_definition_id`) and render a Decision card (decision MAKE/BUY tag · rationale · decided_by_name · decided_at; `re_evaluate` amber note) at the TOP of the existing commit-29260a0 "Manufacturing / Procurement" panel — replacing the bare `badge-make`/`badge-buy` chip. (2) For BUY instances, fetch `GET /api/buy-costs/:satId/:partDefId?part_inst=<partInstId>` (verify the exact route/param shape against `backend/src/routes/buy-costs.ts`) and render the authoritative buy_costs numbers — `quoted_unit_cost_usd`, `nre_cost_usd`, `po_value_usd`, `invoiced_value_usd` — alongside the existing PR cards + VO cards (the VO card today shows the PO *number* and lead weeks but NOT the PO *value* or invoiced value — those live in buy_costs, not vendor_orders). Leave the existing per-unit cost panel (sourced from `/process`'s heuristic `cost_breakdown`) AS-IS per the locked decision — the new procurement numbers carry the authoritative figures. (3) Cleanup: remove the temporary `[3d-wd]` watchdog block (keep the `resize()` call).

Purpose: instance.html's "Manufacturing / Procurement" panel is already the best symmetric thing in the app, but it's missing the make/buy decision row and the buy_costs quoted/PO/invoiced numbers. This brings it up to cost-detail.html's level and makes instance.html consistent with the upgraded part.html.
Output: `instance.html` with a Decision card on the Manufacturing/Procurement panel, the buy_costs PO/invoiced numbers on the BUY branch, and the `[3d-wd]` watchdog removed. FRONTEND-ONLY.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-build-procurement-process-documentation/32-RESEARCH.md

# The file to edit + the shared helpers + the backend route to verify the param shape:
@/Users/jeet/turion-space-demo/satellite/instance.html
@/Users/jeet/turion-space-demo/satellite/cost-render.js
@/Users/jeet/turion-satellite/backend/src/routes/buy-costs.ts
@/Users/jeet/turion-satellite/backend/src/routes/make-buy-decisions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: instance.html — fetch the make/buy decision + render the Decision card at the top of the Manufacturing/Procurement panel</name>
  <files>/Users/jeet/turion-space-demo/satellite/instance.html</files>
  <action>
1. **Read** the existing "Manufacturing / Procurement" panel (markup ~210-214: `#procPanelAnchor` / `#procPanelTitle` / `#woMeta` / `#woList`; JS ~705-813 — the make/buy-aware branch). Confirm the variable names: the instance object (`inst` / `instance`), `inst.part_definition_id`, the `?sat=` query param value (`satId`), the `partInstId` (the instance id), and `processData` (the `/api/parts/:id/process` response already fetched).
2. **Add a decision fetch** alongside the existing `Promise.all([...])` / per-resource fetches, using a `safeGet` 404-as-null wrapper (the cost-detail.html pattern):
   ```js
   async function safeGet(path){ try { return { data: await window.satelliteApi.get(path), error:null }; } catch(e){ return { data:null, error:e }; } }
   const decRes = await safeGet(`/api/make-buy-decisions/${encodeURIComponent(satId)}/${encodeURIComponent(inst.part_definition_id)}`);
   const decision = (decRes.error && decRes.error.status === 404) ? null : (decRes.data || null);
   ```
   `decision` = `{ decision, rationale, decided_by_name, decided_at, decision_status, re_evaluate, ... } | null`. (instance.html always has `?sat=` — but still guard `if (satId && inst.part_definition_id)`.)
3. **Compute** `const effMakeBuy = decision?.decision || processData?.make_buy || part.default_make_buy || null;` and use it wherever the panel currently decides make-vs-buy (the `#procPanelTitle` text, the woList-vs-PR/VO branch). The existing branch already keys on something like `processData.make_buy` / `part.default_make_buy` — swap in `effMakeBuy`.
4. **Render a Decision card** as the first element inside the panel body (above `#woMeta`/`#woList`), using the panel's existing card chrome (the `padding:10px;background:var(--bg-3);border-radius:4px` cards the 29260a0 panel already uses — match it):
   - If `decision`: a `MAKE — internal build` / `BUY — vendor-supplied` tag (reuse the existing `badge-make`/`badge-buy` chip), then `r.escapeHtml(decision.rationale)`, then a muted line `decided by ${r.escapeHtml(decision.decided_by_name || '—')} on ${r.fmtDate(decision.decided_at)}`. If `decision.re_evaluate`: an amber note `Underlying costs have changed since this decision` linking to `cost-detail.html?sat=${satId}&part=${inst.part_definition_id}` (match the cost-detail link shape used elsewhere on the page if any; otherwise this form).
   - If `decision` is null: `Make/buy decision not yet recorded for this satellite` + the `effMakeBuy` default chip (if available).
   - Use `r.escapeHtml` / `r.fmtDate`. Do NOT introduce a second decision/money formatting path — reuse `window.costRender.renderDecisionPanel(...)` if it fits, else a small inline card.
   - Keep the existing `#procPanelTitle` ("Manufacturing — build process" for make / "Procurement — vendor-supplied" for buy) and the existing "Procurement chain for this instance: purchase request → vendor order → PO → invoice" header line — the decision card sits ABOVE them.
5. Remove the bare `badge-make`/`badge-buy` chip that was previously the only make/buy indicator IF it's now redundant with the decision-card tag — or keep it in the page header and just add the card to the panel; either is fine, just don't show two conflicting make/buy indications.
  </action>
  <verify>
`grep -n "make-buy-decisions" /Users/jeet/turion-space-demo/satellite/instance.html` → ≥1; `grep -n "safeGet" instance.html` → ≥1; `grep -n "re_evaluate" instance.html` → ≥1; `grep -n "effMakeBuy" instance.html` → used in the panel branch. Open `instance.html?sat=24587565-b15b-42ce-b590-87ecf9b6bb99&id=<a BUY instance#1 on SAT-003>`: the Manufacturing/Procurement panel shows a Decision card (BUY tag + rationale + "decided by {name} on {date}") above the PR/VO cards. Open `instance.html?sat=...&id=<a MAKE instance#1 on SAT-003>`: the panel shows a Decision card (MAKE tag) above the WO list + build steps. The page does not throw if the decision endpoint 404s (e.g. an instance whose partDef has no decision row — rare on SAT-003 but the guard must hold).
  </verify>
  <done>
instance.html fetches `GET /api/make-buy-decisions/:satId/:partDefId` (404→null) and renders a Decision card (decision tag · rationale · decided_by_name · decided_at · re_evaluate note) at the top of the existing Manufacturing/Procurement panel, using `effMakeBuy` for the make/buy branch; the existing `#procPanelTitle` + "Procurement chain for this instance" header + WO/PR/VO content are unchanged below it.
  </done>
</task>

<task type="auto">
  <name>Task 2: instance.html — add the buy_costs quoted/NRE/PO-value/invoiced numbers to the BUY branch</name>
  <files>/Users/jeet/turion-space-demo/satellite/instance.html</files>
  <action>
1. **Verify the buy-costs route shape FIRST** — read `/Users/jeet/turion-satellite/backend/src/routes/buy-costs.ts` and `app.ts` to confirm: the path is `GET /api/buy-costs/:satId/:partDefId`, and how an instance-level (actual) row is selected — RESEARCH says it's `?part_inst=<partInstId>` (matching what `part.html`'s `renderCostFirstClass` does: `/api/buy-costs/${satId}/${partId}?part_inst=${partInstId}`) and the response shape is `{template, actual}`. If the actual route/param differs, use what the code says — do NOT invent. (App.ts mounts `/api/buy-costs` — confirm.)
2. **In the BUY branch** of the Manufacturing/Procurement panel (only when `effMakeBuy === 'buy'`), add the fetch:
   ```js
   let buyCostData = null;
   if (satId && inst.part_definition_id) {
     const bc = await safeGet(`/api/buy-costs/${encodeURIComponent(satId)}/${encodeURIComponent(inst.part_definition_id)}?part_inst=${encodeURIComponent(partInstId)}`);
     buyCostData = (bc.error && bc.error.status === 404) ? null : (bc.data || null);
   }
   ```
   (Use whatever param name buy-costs.ts actually expects, confirmed in step 1.)
3. **Render the buy_costs numbers** as a card/sub-section inside the BUY branch, AFTER the existing PR cards + VO cards (so the chain reads PR → VO → PO/invoiced — matching the panel's own "purchase request → vendor order → PO → invoice" header). Two options, pick one:
   - **Preferred:** `procPanelBody.insertAdjacentHTML('beforeend', window.costRender.renderBuySheet(buyCostData))` — `renderBuySheet` already emits "RFQ quoted unit cost", "NRE", "PO value", "Invoiced" rows + the template/actual variance, with money formatting handled. Wrap it in the panel's card chrome with a small header `<strong>Purchase order &amp; invoice</strong>`.
   - **Or:** a small `data` table: rows for `Quoted unit cost` → `costRender.formatMoney(buyCostData.actual?.quoted_unit_cost_usd, buyCostData.actual?.currency_code)`, `NRE` → `formatMoney(...nre_cost_usd...)`, `PO number` → `r.escapeHtml(...po_number || '—')`, `PO value` → `formatMoney(...po_value_usd...)`, `Invoiced` → `formatMoney(...invoiced_value_usd...)`.
   - If `buyCostData` is null / `actual` absent: a friendly empty state inside the card ("no purchase-order / invoice records yet for this instance") — do NOT crash, do NOT show "$undefined".
   - Add a link to the full cost sheet `cost-detail.html?sat=${satId}&part=${inst.part_definition_id}` (match the link form used by `part.html`'s cost panel; if cost-detail expects `part_inst` too, include it).
4. **Do NOT touch** the existing per-unit cost panel (the one sourced from `processData.cost_breakdown` — "Sourcing / Material-or-unit cost / PO number / Lead time / Total" for buy). Per the locked decision it stays as the "per-unit estimate" view; the new card carries the authoritative buy_costs figures. Do NOT touch the `instance_index > 1` "tracked on instance #1" hints.
5. Any new click-through wired via `addEventListener` after `innerHTML`/`insertAdjacentHTML` — NO new inline `onclick`.
  </action>
  <verify>
`grep -n "buy-costs" /Users/jeet/turion-space-demo/satellite/instance.html` → ≥1; `grep -n "renderBuySheet\|po_value_usd\|invoiced_value_usd" instance.html` → ≥1; the param name in the new `/api/buy-costs/...` URL matches what `buy-costs.ts` expects (cross-check). Open `instance.html?sat=24587565-b15b-42ce-b590-87ecf9b6bb99&id=<a BUY instance#1 on SAT-003>`: the Manufacturing/Procurement panel shows, after the PR + VO cards, a "Purchase order & invoice" card with quoted unit cost / NRE / PO number / PO value / invoiced (real numbers from buy_costs, not `cost_breakdown`). Open the same for a MAKE instance: no buy-costs card; the WO list + build steps render as before; the per-unit cost panel is unchanged for both. The page does not throw if `/api/buy-costs/...` 404s.
  </verify>
  <done>
instance.html's BUY branch fetches `GET /api/buy-costs/:satId/:partDefId` (with the param shape confirmed against buy-costs.ts) and renders the authoritative buy_costs numbers (quoted_unit_cost_usd / nre_cost_usd / po_number / po_value_usd / invoiced_value_usd) — via `costRender.renderBuySheet` or an equivalent data table — after the existing PR + VO cards, with a link to the full cost sheet; the existing per-unit `cost_breakdown` panel and the `instance_index>1` hints are untouched; no new inline onclick.
  </done>
</task>

<task type="auto">
  <name>Task 3: instance.html — remove the temporary [3d-wd] watchdog block</name>
  <files>/Users/jeet/turion-space-demo/satellite/instance.html</files>
  <action>
1. Find the `[3d-wd]` block (~493-508) — the `if (typeof viewerHandle.debugInfo === 'function') { ... setInterval( ... '[3d-wd]' ... ) }` block in the hero CAD/3D viewer section. **Delete the entire block.** Dead since commit 178aff1.
2. **Do NOT touch** the `requestAnimationFrame(() => viewerHandle.resize())` call in `set3D()` — that's the 2D→3D toggle re-measure, not a diagnostic; `resize` stays on the satellite-3d.js handle (Plan 32-03). Also do not touch the Phase-31 dimension HUD (`cad-hud`), `#autoRotateChk`, the importmap, the `?view=` toggle, or the SVG fallback.
  </action>
  <verify>
`grep -c "\[3d-wd\]" /Users/jeet/turion-space-demo/satellite/instance.html` → 0; `grep -c "debugInfo" /Users/jeet/turion-space-demo/satellite/instance.html` → 0; `grep -c "viewerHandle.resize" /Users/jeet/turion-space-demo/satellite/instance.html` → ≥1; `grep -c "cad-hud" instance.html` → ≥3 (Phase-31 HUD intact). Open `instance.html?sat=...&id=<any instance>` (or curl the markup): the 3D viewer + dimension HUD + `mode-3d` toggle + `frame-svg` fallback are all present; no `[3d-wd]` console spam.
  </verify>
  <done>
The `[3d-wd]` watchdog block is removed from instance.html; the `viewerHandle.resize()` call in set3D, the Phase-31 `cad-hud` HUD, `#autoRotateChk`, the importmap, the `?view=` toggle, and the SVG fallback are untouched.
  </done>
</task>

</tasks>

<verification>
- `grep` proof: `make-buy-decisions` + `safeGet` + `re_evaluate` + `effMakeBuy` + `buy-costs` + (`renderBuySheet` or `po_value_usd`) present in instance.html; `[3d-wd]` + `debugInfo` absent (0); `cad-hud` count ≥3 (Phase-31 HUD intact)
- The new `/api/buy-costs/...` URL param shape matches `buy-costs.ts` (cross-checked, not invented)
- No new inline `onclick=` in instance.html
- Browser/curl walk: BUY instance#1 on SAT-003 → Manufacturing/Procurement panel shows Decision card (BUY) → PR cards → VO cards → "Purchase order & invoice" card (quoted/NRE/PO value/invoiced from buy_costs); MAKE instance → Decision card (MAKE) → WO list → build steps; both → the per-unit `cost_breakdown` panel unchanged; integrations panel / cost rollup / lifecycle timeline / BOM-children gallery / sibling instances all still render
- 3D viewer + Phase-31 HUD still work — no `[3d-wd]` output
</verification>

<success_criteria>
- instance.html's Manufacturing/Procurement panel shows the recorded make/buy decision (decision · rationale · decided_by_name · decided_at) from `/api/make-buy-decisions/:satId/:partDefId`, 404-as-undecided
- BUY instances additionally show the authoritative buy_costs numbers (quoted_unit_cost_usd / nre_cost_usd / po_value_usd / invoiced_value_usd) after the existing PR + VO cards, from `/api/buy-costs/:satId/:partDefId` (param shape confirmed against the backend)
- The existing per-unit `cost_breakdown` panel and the `instance_index>1` hints are untouched (per the locked decision)
- `[3d-wd]` watchdog removed from instance.html; `resize`/toggle/SVG-fallback/Phase-31 HUD intact
- FRONTEND-ONLY; Phase-29 audit stays 0 violations (verified in 32-04); nothing in Phases 27-31 regresses
</success_criteria>

<output>
After completion, create `.planning/phases/32-build-procurement-process-documentation/32-02-SUMMARY.md`
</output>
