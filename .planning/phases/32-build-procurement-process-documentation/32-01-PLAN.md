---
phase: 32-build-procurement-process-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["/Users/jeet/turion-space-demo/satellite/part.html"]
autonomous: true
requirements: [MakeBuyDecisionUI, MakeProcessUI, BuyProcessUI, ProcessConsistency]

must_haves:
  truths:
    - "part.html opened from a satellite (?sat=) shows the recorded make/buy DECISION — decision (MAKE/BUY tag), rationale, decided_by_name, decided_at — fetched from GET /api/make-buy-decisions/:satId/:partDefId, treating 404 as 'no decision yet' (no error toast); when ?sat= is absent it degrades to part.default_make_buy + 'open from a satellite for the recorded decision'"
    - "BUY parts on part.html show a top-level 'Procurement chain' .panel with the SAME prominence as the MAKE 'Build process' panel — purchase-request card(s) (material_description, estimated_cost_usd, status, requested date) → vendor-order card(s) (vendor name + country + ITAR-compliant badge, qty, po_number, quoted_lead_weeks, status, created date) → PO + invoiced (po_number, po_value_usd, invoiced_value_usd, quoted_unit_cost_usd, nre_cost_usd from the existing /api/buy-costs/:satId/:partDefId fetch) — not the thin 'No build process recorded yet' placeholder"
    - "the BUY workflow visualizer step list is Decision → Quote → Purchase request → Vendor order → PO issued → Invoiced (the phantom RFQ / Receiving / Acceptance steps are removed), each step active/done based on whether that data exists (decision row → buy_costs.quoted_unit_cost_usd present → any procurement_request → any vendor_order → vendor_order.po_number or buy_costs.po_value_usd present → buy_costs.invoiced_value_usd present)"
    - "MAKE parts on part.html keep the existing Build process panel + Materials required + labor cost breakdown, now grouped under the decision card (the symmetric 'Realization' section heading style: <div class=panel><div class=panel-header><strong>…</strong> <span class=subtitle>…</span></div>…</div>)"
    - "the temporary [3d-wd] watchdog block in part.html (the 'TEMP diagnostic + self-heal watchdog' setInterval that calls viewerHandle.debugInfo) is removed; the requestAnimationFrame(() => viewerHandle.resize()) call in set3D stays"
    - "no NEW inline onclick added (any new click wired via addEventListener after innerHTML, matching the renderWorkflow data-attr pattern); any new satelliteApi.get('/api/...') path resolves against an already-mounted route — Phase-29 audit stays 0 violations"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/satellite/part.html"
      provides: "decision card + symmetric BUY 'Procurement chain' panel + fixed BUY workflow visualizer + [3d-wd] removal"
  key_links:
    - from: "part.html"
      to: "GET /api/make-buy-decisions/:satId/:partDefId"
      via: "safeGet pattern (404 → null), wired only when ?sat= present"
      pattern: "make-buy-decisions/\\$\\{"
    - from: "part.html (new procurement panel)"
      to: "GET /api/buy-costs/:satId/:partDefId"
      via: "reuse the response already fetched in renderCostFirstClass"
      pattern: "buy-costs/\\$\\{"
---

<objective>
On `part.html`: (1) fetch the recorded make/buy DECISION (`GET /api/make-buy-decisions/:satId/:partDefId`) and render a small Decision card (decision MAKE/BUY tag · rationale · decided_by_name · decided_at; if `re_evaluate`, an amber "underlying costs changed since this decision" note linking to cost-detail.html) at the top of a new "Realization" section — replacing the hard-coded "MAKE — internal build" / "BUY — vendor-supplied" labels in the current "Make/Buy detail" panel. (2) For BUY parts, add a top-level "Procurement chain" `.panel` with the SAME visual prominence as the MAKE "Build process" panel: purchase-request card(s) → vendor-order card(s) → PO + invoiced (from the existing `/api/buy-costs` fetch), instead of the thin "No build process recorded yet" placeholder. (3) Fix the BUY workflow visualizer's step list to `Decision → Quote → Purchase request → Vendor order → PO issued → Invoiced` (drop the phantom RFQ/Receiving/Acceptance steps), lighting each step from real data. (4) Keep the MAKE side (workflow visualizer, Build process panel, Materials required, labor cost breakdown) — grouped under the same section heading style for consistency. (5) Cleanup: remove the temporary `[3d-wd]` watchdog block (keep the `resize()` call).

Purpose: This is the core of Phase 32 for `part.html` — the BUY path currently has nothing with the prominence of "Build process"; the make/buy decision (rationale, signer, date) is shown only on cost-detail.html. After this, every part page documents how the part is realized with make and buy given equal weight.
Output: `part.html` with a Decision card (real `make_buy_decisions` row), a symmetric BUY "Procurement chain" panel, a fixed BUY workflow visualizer, the MAKE panels grouped under the section heading, and the `[3d-wd]` watchdog removed. FRONTEND-ONLY — no backend route change.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-build-procurement-process-documentation/32-RESEARCH.md

# The file to edit + the shared helpers it uses:
@/Users/jeet/turion-space-demo/satellite/part.html
@/Users/jeet/turion-space-demo/satellite/cost-render.js
@/Users/jeet/turion-space-demo/satellite/cost-detail.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: part.html — fetch the make/buy decision + render the Decision card under a "Realization" section heading</name>
  <files>/Users/jeet/turion-space-demo/satellite/part.html</files>
  <action>
1. **Locate** the current "Make/Buy detail" panel (markup ~176-181, JS ~480-504) and the section that holds it. The page already reads `?sat=` (used by `renderCostFirstClass` for the cost panel) and has `partId` (the partDef id) — confirm the variable names by reading the file.
2. **Add a decision fetch** alongside the existing `Promise.all([...])` data fetches, using the `safeGet` 404-as-null pattern from cost-detail.html — but ONLY when `satId` is present:
   ```js
   async function safeGet(path){ try { return { data: await window.satelliteApi.get(path), error: null }; } catch(e){ return { data:null, error:e }; } }
   let decision = null;
   if (satId) {
     const decRes = await safeGet(`/api/make-buy-decisions/${encodeURIComponent(satId)}/${encodeURIComponent(partId)}`);
     decision = (decRes.error && decRes.error.status === 404) ? null : (decRes.data || null);
   }
   ```
   `decision` = `{ decision:'make'|'buy', rationale, decided_by_name, decided_at, decision_status, re_evaluate, ... } | null`.
3. **Compute the effective make/buy:** `const effMakeBuy = decision?.decision || (process && process.make_buy) || part.default_make_buy || null;` Use `effMakeBuy` everywhere the page currently uses `mb = process.make_buy ?? part.default_make_buy` (the workflow visualizer, the Make/Buy detail panel branch, the cost panel branch, the materials-required `display` toggle).
4. **Render a Decision card** at the top of a NEW "Realization" / "Build & Procurement" section (a `.panel` with the established header style — `<div class="panel"><div class="panel-header"><strong>Realization</strong> <span class="subtitle">how this part is built or procured</span></div>…</div>`; check `satellite-shell.css` / the page `<style>` for the exact `panel` / `panel-header` / `subtitle` class names actually in use and match them). The card content:
   - If `decision`: a `MAKE — internal build` or `BUY — vendor-supplied` tag (reuse the existing `badge-make`/`badge-buy` or `statusTag`-style chip), then `r.escapeHtml(decision.rationale)`, then a muted line `decided by ${r.escapeHtml(decision.decided_by_name || '—')} on ${r.fmtDate(decision.decided_at)}`. If `decision.re_evaluate` is truthy, an amber note `Underlying costs have changed since this decision` linking to `cost-detail.html?sat=${satId}&part=${partId}` (check the cost-detail link shape the page already uses in renderCostFirstClass).
   - If `decision` is null but `satId` IS present: `Make/buy decision not yet recorded for this satellite` (+ still show the `effMakeBuy` default chip if available).
   - If `satId` is absent: `Open this part from a satellite to see the recorded make/buy decision` (+ the `part.default_make_buy` chip).
   - Consider extracting/reusing `window.costRender.renderDecisionPanel(...)` if it produces a fitting read-only card — but a small inline card is fine; do NOT introduce a second money/decision formatting path. Use `r.escapeHtml`, `r.fmtDate` (null-safe).
5. **Remove the hard-coded labels** in the current "Make/Buy detail" panel ("Decision: MAKE — internal build" / "Decision: BUY — vendor-supplied" rows) — the Decision card is now the source of truth. Keep the panel's other rows (Avg build time / Completed builds for make; preferred vendor name/country/ITAR for buy) but fold them under the new section heading (the MAKE side keeps "Build process" + "Materials required" + the labor cost panel as-is; just ensure they sit under the "Realization" heading so make and buy use the same section chrome).
  </action>
  <verify>
`grep -n "make-buy-decisions" /Users/jeet/turion-space-demo/satellite/part.html` → ≥1 match; `grep -n "safeGet" part.html` → ≥1; `grep -n "re_evaluate" part.html` → ≥1; `grep -c "MAKE — internal build" part.html` → 0 (the hard-coded label is gone, replaced by the decision-row render — OR it appears only as a fallback chip label, not as "Decision: ..."). `grep -n "effMakeBuy" part.html` → used in the workflow/cost/materials branches. Open `part.html?id=<a buy partDef on SAT-003>&sat=24587565-b15b-42ce-b590-87ecf9b6bb99` (or curl + inspect markup): a "Realization" panel renders with a `BUY — vendor-supplied` tag + the rationale text + "decided by {name} on {date}". Open `part.html?id=<same>` with NO `&sat=`: the page does not throw; the Realization panel shows "Open this part from a satellite …" + the default chip.
  </verify>
  <done>
part.html fetches `GET /api/make-buy-decisions/:satId/:partDefId` (404→null, only when ?sat= present), computes `effMakeBuy` from the decision (falling back to process.make_buy → part.default_make_buy), and renders a Decision card (decision tag · rationale · decided_by_name · decided_at · re_evaluate note) at the top of a "Realization" section using the established panel header style; the hard-coded "MAKE/BUY — …" decision labels are removed; degrades gracefully without ?sat=.
  </done>
</task>

<task type="auto">
  <name>Task 2: part.html — symmetric BUY "Procurement chain" panel + fix the BUY workflow visualizer step list</name>
  <files>/Users/jeet/turion-space-demo/satellite/part.html</files>
  <action>
1. **The BUY "Procurement chain" panel** — add a top-level `.panel` (sibling of the MAKE "Build process" panel, same `panel`/`panel-header` chrome, only rendered when `effMakeBuy === 'buy'`) inside the "Realization" section. Title `<strong>Procurement chain</strong> <span class="subtitle">RFQ quote → purchase request → vendor order → PO → invoiced</span>`. Data sources (all already fetched — do NOT add a new endpoint):
   - **Purchase requests:** the `procurement_request`-kind rows from `process.recent_orders` (filter `kind === 'procurement_request'`). Card per row: `📋 Purchase request` label + `r.statusTag(row.status)` + `r.escapeHtml(row.material_description)` + estimated cost via `window.costRender.formatMoney(row.estimated_cost_usd)` + `r.fmtDate(row.created_at)` (the satellite_designation / serial_number context line if present). Card chrome = the existing `padding:10px;background:var(--bg-3);border-radius:4px` cards (match what instance.html's 29260a0 panel uses).
   - **Vendor orders:** the `vendor_order`-kind rows from `process.recent_orders` (filter `kind === 'vendor_order'`). Card per row: `📦 Vendor order — ${r.escapeHtml(row.vendor_name)}` + `r.statusTag(row.status)` + `qty ${row.qty}` + `PO ${r.escapeHtml(row.po_number || '—')}` + `lead ${row.lead_weeks ?? '—'} wk` + `r.fmtDate(row.created_at)`. If `process.preferred_vendor` has `country` / `itar_compliant`, surface the country + an `ITAR-OK` chip on the vendor line (match the spec-sheet chip style).
   - **PO + invoiced:** from the `/api/buy-costs/:satId/:partDefId` response the page ALREADY fetches in `renderCostFirstClass` (reuse that response — hoist it / store it in a variable so this panel can read it; if the page fetches it lazily inside renderCostFirstClass, refactor so the fetch result is available here too). Render either via `window.costRender.renderBuySheet(buyCostData)` (it already emits "RFQ quoted unit cost", "NRE", PO value, invoiced rows + variances — preferred for consistency with cost-detail.html) OR a small `data` table with `quoted_unit_cost_usd`, `nre_cost_usd`, `po_number`, `po_value_usd`, `invoiced_value_usd` using `costRender.formatMoney`. If `buyCostData` is empty / `satId` absent: a friendly empty state ("no purchase-order / invoice records yet — open from a satellite to see them").
   - Empty-state for the whole panel: if there are zero PR rows AND zero VO rows AND no buy_costs → "No procurement records yet for this part" (not a crash).
   - If any card needs a click-through (e.g. to instance.html or cost-detail.html), wire it via `addEventListener` after `innerHTML` (the `renderWorkflow` `el.querySelectorAll('[data-go]').forEach(n => n.addEventListener('click', …))` pattern) — NO new inline `onclick`. If you rework the existing "Recent orders" table that uses inline `onclick` (lines ~845/~896), convert those to `addEventListener` too (but reworking it is optional — leaving it is fine since the audit currently passes).
2. **MAKE side stays:** when `effMakeBuy === 'make'`, the existing "Build process" panel (representative_build_steps step rows), "Materials required" panel, and the labor cost breakdown render as today — just confirm they're inside the "Realization" section so the heading chrome matches the BUY side. (Optional: if the `/process` `representative_build_steps` someday carries `signed_by_name` you could show the signer; today it doesn't — do NOT show "signed by undefined". Leave the signer to instance.html. No backend change in this phase.)
3. **Fix the BUY workflow visualizer** (`renderWorkflow`, ~515-598): replace the BUY step list `Drawing · RFQ · PO · Vendor build · Receiving · Acceptance` (which has phantom RFQ/Receiving/Acceptance) with `Decision · Quote · Purchase request · Vendor order · PO issued · Invoiced`. Compute `currentIdx` from real data:
   - step 0 "Decision" active/done if `decision` is non-null;
   - step 1 "Quote" done if `buyCostData?.actual?.quoted_unit_cost_usd` (or `template.quoted_unit_cost_usd`) is present;
   - step 2 "Purchase request" done if any `recent_orders` row has `kind === 'procurement_request'`;
   - step 3 "Vendor order" done if any row has `kind === 'vendor_order'`;
   - step 4 "PO issued" done if any vendor_order row has a non-empty `po_number` OR `buyCostData?.actual?.po_value_usd` is present;
   - step 5 "Invoiced" done if `buyCostData?.actual?.invoiced_value_usd` is present.
   `currentIdx` = the highest done index (or 0). Keep the existing click-to-scroll-to-target-panel behaviour (wired via `addEventListener`), but point the BUY steps at the new "Procurement chain" panel anchor. The MAKE step list (`Drawing · Materials · Build steps · QA · Done`) is unchanged. Make sure NO `onclick=` is introduced.
  </action>
  <verify>
`grep -c "RFQ · PO · Vendor build" /Users/jeet/turion-space-demo/satellite/part.html` → 0 (old phantom BUY steps gone); `grep -n "Purchase request" part.html` and `grep -n "PO issued" part.html` and `grep -n "Invoiced" part.html` → ≥1 each (new BUY step list / panel); `grep -n "Procurement chain" part.html` → ≥1; `grep -c "Receiving" part.html` → 0 (phantom step removed); `grep -n "renderBuySheet\|formatMoney" part.html` → ≥1 (reusing costRender). `grep -ci "onclick=" part.html` → must NOT increase vs the pre-edit count (run the audit in Task verification of plan 32-04 anyway). Open `part.html?id=<buy partDef on SAT-003>&sat=24587565-b15b-42ce-b590-87ecf9b6bb99`: a "Procurement chain" panel renders with PR card(s) → VO card(s) → PO/invoiced rows; the workflow visualizer shows the 6 new BUY steps with the right one highlighted. Open `part.html?id=<a MAKE partDef on SAT-003>&sat=...`: the "Build process" / "Materials required" / labor-cost panels still render under the "Realization" heading; no "Procurement chain" panel; MAKE workflow steps unchanged.
  </verify>
  <done>
BUY parts on part.html show a top-level "Procurement chain" .panel (PR cards → VO cards → PO/invoiced from the reused /api/buy-costs response, via costRender.renderBuySheet or an equivalent data table) with the same prominence as the MAKE "Build process" panel; the BUY workflow visualizer step list is Decision→Quote→Purchase request→Vendor order→PO issued→Invoiced with currentIdx driven by real data (no phantom RFQ/Receiving/Acceptance); MAKE panels unchanged under the same section heading; no new inline onclick.
  </done>
</task>

<task type="auto">
  <name>Task 3: part.html — remove the temporary [3d-wd] watchdog block</name>
  <files>/Users/jeet/turion-space-demo/satellite/part.html</files>
  <action>
1. Find the `[3d-wd]` block (~444-462) — the comment says "TEMP diagnostic + self-heal watchdog"; it does `if (typeof viewerHandle.debugInfo === 'function') { ... setInterval( ... '[3d-wd]' ... ) }`. **Delete the entire block** (the `if`, the `setInterval`, the console logging). This is dead weight since commit 178aff1 fixed the size-blowup with a definite `#viewer3d{height}` + `overflow:hidden` + `renderer.setSize` without `,false`.
2. **Do NOT touch** the `requestAnimationFrame(() => viewerHandle.resize())` call in `set3D()` — that's not a diagnostic; it's the 2D→3D toggle re-measure and `resize` stays on the handle (Plan 32-03 keeps `resize`/`deselect`/`selectChild`/`dispose` on the satellite-3d.js handle). Also do not touch `#autoRotateChk`, the importmap, the `?view=` toggle, or the SVG fallback.
  </action>
  <verify>
`grep -c "\[3d-wd\]" /Users/jeet/turion-space-demo/satellite/part.html` → 0; `grep -c "debugInfo" /Users/jeet/turion-space-demo/satellite/part.html` → 0; `grep -c "viewerHandle.resize" /Users/jeet/turion-space-demo/satellite/part.html` → ≥1 (the toggle re-measure call still present). Open `part.html?id=<any part>&sat=...` (or curl the markup): the 3D viewer markup + importmap + `mode-3d` toggle + `frame-svg` fallback are all still present; no `[3d-wd]` console spam.
  </verify>
  <done>
The `[3d-wd]` watchdog block (the `viewerHandle.debugInfo` setInterval) is removed from part.html; the `viewerHandle.resize()` call in set3D, the importmap, `#autoRotateChk`, the `?view=` toggle, and the SVG fallback are untouched.
  </done>
</task>

</tasks>

<verification>
- `grep` proof: `make-buy-decisions` + `safeGet` + `re_evaluate` + `effMakeBuy` + `Procurement chain` + `PO issued` present in part.html; `RFQ · PO · Vendor build` + `Receiving` + `[3d-wd]` + `debugInfo` absent (0)
- No new inline `onclick=` in part.html (count not increased vs pre-edit)
- Browser/curl walk: `part.html?id=<buy partDef>&sat=24587565-b15b-42ce-b590-87ecf9b6bb99` → Realization section with Decision card (BUY tag + rationale + signer + date) + Procurement chain panel (PR → VO → PO/invoiced) + 6-step BUY workflow viz; `part.html?id=<make partDef>&sat=...` → Decision card (MAKE) + Build process + Materials required + labor cost; `part.html?id=<any>` with NO `&sat=` → no crash, "open from a satellite" copy
- 3D viewer still works (importmap, mode-3d toggle, SVG fallback) — no `[3d-wd]` console output
</verification>

<success_criteria>
- part.html shows the recorded make/buy decision (decision · rationale · decided_by_name · decided_at) from `/api/make-buy-decisions/:satId/:partDefId`, 404-as-undecided, graceful without ?sat=
- BUY parts get a top-level "Procurement chain" panel with the same prominence as MAKE's "Build process" — PR cards → VO cards → PO/invoiced — sourced from `recent_orders` + the reused `/api/buy-costs` response (no new endpoint)
- The BUY workflow visualizer has no phantom steps — `Decision → Quote → Purchase request → Vendor order → PO issued → Invoiced`, currentIdx from real data
- MAKE panels (Build process, Materials required, labor cost) unchanged, grouped under the same "Realization" section heading
- `[3d-wd]` watchdog removed from part.html; `resize`/toggle/SVG-fallback intact
- FRONTEND-ONLY; Phase-29 audit stays 0 violations (verified in 32-04)
</success_criteria>

<output>
After completion, create `.planning/phases/32-build-procurement-process-documentation/32-01-SUMMARY.md`
</output>
