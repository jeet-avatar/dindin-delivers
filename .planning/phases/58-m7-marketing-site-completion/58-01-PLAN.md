---
phase: 58-m7-marketing-site-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/zietra/marketing/package.json
  - /Users/jeet/zietra/marketing/package-lock.json
  - /Users/jeet/zietra/marketing/src/pages/LoginPage.tsx
  - /Users/jeet/zietra/marketing/src/pages/SignupPage.tsx
  - /Users/jeet/zietra/marketing/src/pages/DashboardPage.tsx
  - /Users/jeet/zietra/marketing/src/pages/HomePage.tsx
  - /Users/jeet/zietra/marketing/src/pages/PrivacyPage.tsx
  - /Users/jeet/zietra/marketing/src/pages/TermsPage.tsx
  - /Users/jeet/zietra/marketing/src/pages/NotFoundPage.tsx
  - /Users/jeet/zietra/marketing/src/data/pricing.ts
  - /Users/jeet/zietra/marketing/src/components/PricingSection.tsx
  - /Users/jeet/zietra/marketing/src/components/SiteFooter.tsx
  - /Users/jeet/zietra/marketing/src/components/NavBar.tsx
  - /Users/jeet/zietra/marketing/src/lib/config.ts
  - /Users/jeet/zietra/marketing/src/lib/auth.ts
  - /Users/jeet/zietra/marketing/src/lib/supabase.ts
  - /Users/jeet/zietra/marketing/scripts/gen-sitemap.mjs
  - /Users/jeet/zietra/marketing/public/sitemap.xml
  - /Users/jeet/zietra/marketing/public/robots.txt
  - /Users/jeet/zietra/marketing/public/llms.txt
autonomous: true
requirements:
  - CognitoMigratedAuth
  - PricingPageStripePlaceholder
  - MarketingHomeRefresh
  - SeoBaseline404SitemapRobots

must_haves:
  truths:
    - "Marketing site has ZERO Supabase code. `@supabase/supabase-js` removed from `package.json` dependencies (was `2.103.3`). `src/lib/auth.ts` (108 LOC) and `src/lib/supabase.ts` (25 LOC) DELETED from git. `npm run build` succeeds with no missing-import errors."
    - "`src/lib/config.ts` (NEW) exports `APP_URL='https://app.zietra.com'`, `APP_SIGNUP`, `APP_LOGIN`, `APP_DASHBOARD`, `APP_ONBOARDING_RECOMMEND`, `API_URL` (env-driven via `import.meta.env.VITE_API_URL`, default `https://lo254mvukl.execute-api.us-east-1.amazonaws.com`), and an `appUrl(path, preserveQuery=true)` helper that appends `window.location.search` when called in the browser."
    - "`LoginPage.tsx` (was 113 LOC Supabase form) SHRUNK to ~25 LOC: `useEffect` runs `window.location.replace(\\`${APP_LOGIN}${window.location.search}\\`)`. Body shows 'Redirecting to login…'. `<Helmet>` includes `<meta name='robots' content='noindex'>`."
    - "`SignupPage.tsx` (was 125 LOC Supabase form) SHRUNK to ~25 LOC with same redirect pattern to `APP_SIGNUP` — query string preserved so `?intent=<module>` lands at Cognito."
    - "`DashboardPage.tsx` (was 80 LOC) SHRUNK to ~25 LOC redirect to `APP_DASHBOARD` with same query preservation + noindex."
    - "`src/data/pricing.ts` REFACTORED: `PricingTier` interface gains `ctaHref: string` (replaces the implicit `/signup` Link) and optional `comingSoon?: boolean`. Three tiers updated to match M1 kickoff pricing: `Starter (trial)` ($0/mo, 30 days, all 13 modules, cta='Start free trial', ctaHref=APP_SIGNUP), `Base` ($99/mo, featured, cta='Upgrade to paid', ctaHref='#', comingSoon=true), `Add-ons` (Custom, cta='See modules', ctaHref='/modules'). The stale Starter/Growth/Scale $0/$79/$149 shape is REPLACED."
    - "`PricingSection.tsx` UPDATED at lines ~84-98 (the hardcoded `<Link to='/signup'>` block): renders `<a href={tier.ctaHref}>` for non-comingSoon tiers; for `comingSoon=true` renders a disabled-looking button with `title='Stripe checkout coming soon'` tooltip + cursor:not-allowed style. No click handler routes to a real checkout URL."
    - "`HomePage.tsx` content refresh: the existing `FEATURES` array (currently HubSpot/Buffer/Calendly framing at lines ~251-336) REPLACED with a `WHATS_IN_THE_BOX` section that renders 13 module cards from a TEMPORARY inline minimal data array (full data ships in 58-02). Each card: icon (lucide), name, 1-line tagline, link `/modules/<slug>`. Hero headline updated from HubSpot-replacement framing to '13 modules of a Fortune-500 ERP, at SMB pricing.' Sub-headline mentions 'CRM, Sales, NetSuite, PLM, MES, Quality, ASC 606, Royalty, Drop-ship, AI Agents — one tenant, one bill.' NO mention of HubSpot/Buffer/Calendly/DocuSign/Loom/Mailchimp."
    - "`SiteFooter.tsx` (lines ~14-29 `FOOTER_COLS` array): every `href='#'` REPLACED with real route. Product col: /modules, /pricing, /case-studies, /docs. Company col: /about, /contact, /privacy, /terms. Resources col: /docs + mailto:security@zietra.com. Blog/Careers/Press DELETED (no pages yet — open question 1)."
    - "`NavBar.tsx` mid-nav additions: `/modules` link (between Pricing and the existing #features anchor), `/case-studies` link. CTA button on right stays as 'Start free trial' → APP_SIGNUP. Existing `#features` and `#stories` anchors retained for home-page scroll."
    - "`PrivacyPage.tsx` line ~76 'Database: Supabase Postgres' REPLACED with 'Database: AWS Aurora PostgreSQL (multi-tenant with RLS)'. Sub-processors list at lines ~105-109: REMOVE 'Supabase', ADD 'Amazon Web Services (Cognito, Aurora, SES, CloudFront, Lambda)'. Keep existing Stripe / Vercel entries (drop Vercel if listed — marketing moved to S3+CF per MEMORY)."
    - "`TermsPage.tsx` lines ~54-58 'you connect your own SMTP' REMOVED — we send via SES on our infra now. Replace with a sentence about our SES outbound email + the user receiving messages only from `noreply@zietra.com` and `support@zietra.com`."
    - "`NotFoundPage.tsx`: keep existing structure; ADD a 'Popular pages' section with 6 links: Home, /modules, /pricing, /case-studies, /contact, /docs. (/case-studies, /contact, /docs are 404 themselves until 58-02/03/04 ship; that's acceptable for 58-01 — they 404 to the same NotFoundPage which displays the same links — a self-recovering loop.) `<Helmet>` already has `<meta name='robots' content='noindex'>`."
    - "`scripts/gen-sitemap.mjs` NEW (~50 LOC): pure Node, no deps. Imports MODULES (empty array in 58-01 because `src/data/modules.ts` doesn't exist yet — script handles the missing-file case gracefully and writes only static routes), CASE_STUDIES (same), iterates static routes (/, /pricing, /modules, /case-studies, /about, /contact, /docs, /privacy, /terms), writes `public/sitemap.xml` with `<urlset xmlns='http://www.sitemaps.org/schemas/sitemap/0.9'>` + per-URL `<loc>`, `<changefreq>`, `<priority>`. Login/Signup/Dashboard intentionally OMITTED (noindex). Script writes a console line `✅ Wrote N URLs to public/sitemap.xml`."
    - "`package.json` `scripts.build` UPDATED: `'node scripts/gen-sitemap.mjs && tsc -b && vite build'` (sync-modules is added in 58-02). `scripts.dev` UPDATED to also run `gen-sitemap.mjs` first."
    - "`public/sitemap.xml` REGENERATED via `npm run build`: contains 9 static routes (no modules/case-studies yet). The 6-URL stale file is overwritten."
    - "`public/robots.txt` reviewed: already allows all + references sitemap.xml — kept as-is (no change required per RESEARCH §sources)."
    - "`public/llms.txt` REVIEWED: contains stale 'Database: Supabase Postgres' + non-13-module framing. UPDATE inline to mirror the new HomePage framing (13-module ERP, Aurora + Cognito stack). One-paragraph rewrite."
    - "CloudFront `E1X82T89JWL8CA` verified: `CustomErrorResponses` for 404+403 → `/index.html` with `ResponseCode=200, ErrorCachingMinTTL=0`. If missing, added via `aws cloudfront update-distribution` (idempotent — only modify if needed). Pre-existing behavior must be preserved if already configured."
    - "Deploy via `./deploy.sh` (existing — no script changes in 58-01). CloudFront invalidation Completed. Live smoke: `https://zietra.com/` returns 200 with new HomePage content (no HubSpot framing); `https://zietra.com/login` returns 200 + 'Redirecting…' text (then JS redirects); `https://zietra.com/sitemap.xml` shows 9 URLs; `https://zietra.com/random-fake-path` returns 200 + SPA HTML (SPA fallback works)."
  artifacts:
    - path: "/Users/jeet/zietra/marketing/src/lib/config.ts"
      provides: "NEW — centralized cross-origin URL constants + appUrl() query-preserving helper."
      contains: "APP_URL"
      min_lines: 20
    - path: "/Users/jeet/zietra/marketing/src/pages/LoginPage.tsx"
      provides: "SHRUNK — 113 LOC Supabase form → ~25 LOC Cognito redirect."
      contains: "window.location.replace"
      min_lines: 15
    - path: "/Users/jeet/zietra/marketing/src/pages/SignupPage.tsx"
      provides: "SHRUNK — 125 LOC → ~25 LOC redirect, preserves ?intent= query."
      contains: "window.location.replace"
      min_lines: 15
    - path: "/Users/jeet/zietra/marketing/src/pages/DashboardPage.tsx"
      provides: "SHRUNK — 80 LOC → ~25 LOC redirect to APP_DASHBOARD."
      contains: "window.location.replace"
      min_lines: 15
    - path: "/Users/jeet/zietra/marketing/src/data/pricing.ts"
      provides: "REFACTORED — 3 tiers ($0 trial / $99 Base / Custom Add-ons), ctaHref + comingSoon fields."
      contains: "comingSoon"
      min_lines: 40
    - path: "/Users/jeet/zietra/marketing/src/components/PricingSection.tsx"
      provides: "MODIFIED — CTA renders <a href={ctaHref}> or tooltip when comingSoon=true."
      contains: "comingSoon"
      min_lines: 70
    - path: "/Users/jeet/zietra/marketing/src/pages/HomePage.tsx"
      provides: "MODIFIED — replaces HubSpot/Buffer/Calendly framing with 13-module catalog framing."
      contains: "13 modules"
      min_lines: 400
    - path: "/Users/jeet/zietra/marketing/src/components/SiteFooter.tsx"
      provides: "MODIFIED — dead href='#' replaced with real routes; Blog/Careers/Press removed."
      contains: "/modules"
      min_lines: 30
    - path: "/Users/jeet/zietra/marketing/src/components/NavBar.tsx"
      provides: "MODIFIED — added /modules + /case-studies links to mid-nav."
      contains: "/modules"
      min_lines: 30
    - path: "/Users/jeet/zietra/marketing/src/pages/PrivacyPage.tsx"
      provides: "MODIFIED — Supabase removed, Aurora + Cognito + SES added to Sub-processors."
      contains: "AWS Aurora"
      min_lines: 80
    - path: "/Users/jeet/zietra/marketing/src/pages/TermsPage.tsx"
      provides: "MODIFIED — 'you connect SMTP' language removed; SES-on-our-infra clarified."
      contains: "noreply@zietra.com"
      min_lines: 50
    - path: "/Users/jeet/zietra/marketing/src/pages/NotFoundPage.tsx"
      provides: "MODIFIED — Popular pages recovery list added (6 links)."
      contains: "Popular pages"
      min_lines: 30
    - path: "/Users/jeet/zietra/marketing/scripts/gen-sitemap.mjs"
      provides: "NEW — build-time sitemap generator (handles missing modules.ts gracefully)."
      contains: "urlset"
      min_lines: 40
    - path: "/Users/jeet/zietra/marketing/package.json"
      provides: "MODIFIED — @supabase/supabase-js removed from deps; build script runs gen-sitemap.mjs first."
      contains: "gen-sitemap"
      min_lines: 30
  key_links:
    - from: "LoginPage / SignupPage / DashboardPage redirect components"
      to: "src/lib/config.ts APP_LOGIN / APP_SIGNUP / APP_DASHBOARD"
      via: "window.location.replace with window.location.search preserved (Pitfall 3)"
      pattern: "window\\.location\\.replace.*APP_(LOGIN|SIGNUP|DASHBOARD)"
    - from: "PricingSection.tsx render path"
      to: "src/data/pricing.ts new ctaHref + comingSoon fields"
      via: "JSX renders <a href={tier.ctaHref}> OR tooltip-disabled button when comingSoon"
      pattern: "ctaHref|comingSoon"
    - from: "HomePage WHATS_IN_THE_BOX section"
      to: "/modules/<slug> routes (to be created in 58-02)"
      via: "13 module card links — slug values match upstream module-catalog.js codes"
      pattern: "href=\"/modules/"
    - from: "SiteFooter.tsx links"
      to: "/about, /contact, /modules, /case-studies, /docs, /privacy, /terms routes"
      via: "react-router <Link> components — all non-# hrefs"
      pattern: "to=\"/(about|contact|modules|case-studies|docs|privacy|terms)\""
    - from: "package.json build script"
      to: "public/sitemap.xml output"
      via: "node scripts/gen-sitemap.mjs runs before tsc + vite build"
      pattern: "gen-sitemap.mjs"
    - from: "scripts/gen-sitemap.mjs"
      to: "src/data/modules.ts AND src/data/case-studies.ts (will exist after 58-02 + 58-03)"
      via: "dynamic import with try/catch — missing file = empty array"
      pattern: "import.*data/(modules|case-studies)"
---

<objective>
Ship Wave 1 of Phase 58 — the foundation wave. Strip Supabase from marketing entirely, wire Login/Signup/Dashboard pages as thin Cognito redirects, refresh HomePage to mirror the actual 13-module catalog (no more HubSpot/Buffer/Calendly framing), update PricingSection to render the Stripe placeholder with a 'Coming soon' tooltip, fix dead footer/nav links, update Privacy + Terms for the post-Phase-55 Aurora stack, ship the build-time sitemap generator, and deploy to zietra.com.

Purpose: Closes ROADMAP requirements `CognitoMigratedAuth`, `PricingPageStripePlaceholder`, `MarketingHomeRefresh`, and the first half of `SeoBaseline404SitemapRobots`. Removes ~133 LOC of dead Supabase code + ~70 KB gzip from bundle. Establishes the centralized `APP_URL` / `appUrl()` pattern that 58-02/03/04 all consume.

Output:
- 1 NEW file: `src/lib/config.ts`
- 1 NEW file: `scripts/gen-sitemap.mjs`
- 3 SHRUNK files: LoginPage.tsx, SignupPage.tsx, DashboardPage.tsx (113+125+80 → ~25 each = 250 LOC saved)
- 2 DELETED files: `src/lib/auth.ts` (108 LOC), `src/lib/supabase.ts` (25 LOC)
- 1 REMOVED dep: `@supabase/supabase-js` (~70 KB gzip)
- MODIFIED: HomePage, PricingSection, pricing.ts, SiteFooter, NavBar, PrivacyPage, TermsPage, NotFoundPage, package.json, public/sitemap.xml, public/llms.txt
- Deploy to S3 `zietra-marketing` + CF `E1X82T89JWL8CA` invalidation
- Live smoke 9 routes
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
@/Users/jeet/.claude/get-shit-done/references/ui-brand.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/58-m7-marketing-site-completion/58-RESEARCH.md
@.planning/phases/57-m6-module-page-completion-replace-stubs-tenant-aware-pages/CHECKPOINT.md
@/Users/jeet/zietra/marketing/package.json
@/Users/jeet/zietra/marketing/src/App.tsx
@/Users/jeet/zietra/marketing/src/pages/HomePage.tsx
@/Users/jeet/zietra/marketing/src/pages/LoginPage.tsx
@/Users/jeet/zietra/marketing/src/pages/SignupPage.tsx
@/Users/jeet/zietra/marketing/src/pages/DashboardPage.tsx
@/Users/jeet/zietra/marketing/src/pages/PrivacyPage.tsx
@/Users/jeet/zietra/marketing/src/pages/TermsPage.tsx
@/Users/jeet/zietra/marketing/src/pages/NotFoundPage.tsx
@/Users/jeet/zietra/marketing/src/components/SiteFooter.tsx
@/Users/jeet/zietra/marketing/src/components/NavBar.tsx
@/Users/jeet/zietra/marketing/src/components/PricingSection.tsx
@/Users/jeet/zietra/marketing/src/data/pricing.ts
@/Users/jeet/zietra/marketing/deploy.sh
@/Users/jeet/zietra/marketing/vite.config.ts
@/Users/jeet/turion-space-demo/lib/module-catalog.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize config + shrink auth pages to Cognito redirects + remove Supabase dep (build-clean order)</name>
  <files>/Users/jeet/zietra/marketing/src/lib/config.ts</files>
  <action>
**Step A — Verify CloudFront SPA fallback (Pitfall 1, prerequisite check):**

```bash
aws cloudfront get-distribution-config --id E1X82T89JWL8CA \
  --query 'DistributionConfig.CustomErrorResponses' --output json
```

Expected: at least two entries with `ErrorCode=404` and `ErrorCode=403`, both with `ResponseCode=200, ResponsePagePath=/index.html, ErrorCachingMinTTL=0`. If MISSING, queue an update at the end (Step F) — DO NOT modify CF mid-deploy. If present, note ETag and skip.

**Step B — Create `src/lib/config.ts` (NEW):**

Use the exact pattern from RESEARCH §"Example 4: `src/lib/config.ts`" (lines 810-833). Required exports: `APP_URL`, `APP_SIGNUP`, `APP_LOGIN`, `APP_DASHBOARD`, `APP_ONBOARDING_RECOMMEND`, `API_URL` (env-driven via `import.meta.env.VITE_API_URL` with default `https://lo254mvukl.execute-api.us-east-1.amazonaws.com`), and `appUrl(path, preserveQuery=true)` helper. Use `typeof window !== 'undefined'` guard for SSR safety.

**Step C — SHRINK Login/Signup/Dashboard pages BEFORE removing Supabase deps (Pitfall 5 — strict order matters):**

Replace `src/pages/LoginPage.tsx` content entirely:

```tsx
import { useEffect } from 'react'
import { Helmet } from 'react-helmet-async'
import { APP_LOGIN } from '../lib/config'

export default function LoginPage() {
  useEffect(() => {
    window.location.replace(`${APP_LOGIN}${window.location.search}`)
  }, [])
  return (
    <>
      <Helmet>
        <title>Log in · Zietra</title>
        <meta name="robots" content="noindex" />
      </Helmet>
      <main style={{ minHeight: '60vh', display: 'grid', placeItems: 'center', textAlign: 'center' }}>
        <p>Redirecting to log in…</p>
      </main>
    </>
  )
}
```

Same shape for `SignupPage.tsx` (use `APP_SIGNUP`, title 'Sign up · Zietra') and `DashboardPage.tsx` (use `APP_DASHBOARD`, title 'Dashboard · Zietra'). All three SHRUNK to ~25 LOC. NO Supabase imports.

**Step D — Verify build passes WITH Supabase still installed (intermediate check):**

```bash
cd /Users/jeet/zietra/marketing
npm run build
```

Expected: 0 TS errors. If any file still imports from `./lib/auth` or `./lib/supabase`, fix it now (those imports should ONLY exist in the 3 shrunk pages, which we just replaced).

**Step E — DELETE Supabase lib files:**

```bash
cd /Users/jeet/zietra/marketing
git rm src/lib/auth.ts src/lib/supabase.ts
npm run build
```

Expected: still 0 TS errors (no remaining imports).

**Step F — Uninstall Supabase + final build:**

```bash
cd /Users/jeet/zietra/marketing
npm uninstall @supabase/supabase-js
npm run build
```

Expected: 0 errors. `package.json` no longer lists `@supabase/supabase-js`. Bundle size drops ~70 KB gzip (note in commit message — actual delta).

**Step G — If CloudFront SPA fallback was missing (from Step A), add it now:**

```bash
ETAG=$(aws cloudfront get-distribution-config --id E1X82T89JWL8CA --query 'ETag' --output text)
# Add 404+403 → /index.html via update-distribution. Use existing dist-config JSON and append CustomErrorResponses.
# Defer to operator-style careful patch — only if Step A confirmed missing.
```

Document in commit: "CF SPA fallback was [already configured | added in this commit]."

**Step H — Commits:**

1. `feat(58-01): src/lib/config.ts — centralized APP_URL + appUrl() helper`
2. `refactor(58-01): LoginPage + SignupPage + DashboardPage — shrunk to Cognito redirects (preserves ?intent= query)`
3. `chore(58-01): delete src/lib/{auth,supabase}.ts and remove @supabase/supabase-js dep (-133 LOC, -70KB gzip)`
  </action>
  <verify>
```bash
grep -q "APP_URL" /Users/jeet/zietra/marketing/src/lib/config.ts
test ! -f /Users/jeet/zietra/marketing/src/lib/auth.ts
test ! -f /Users/jeet/zietra/marketing/src/lib/supabase.ts
grep -L "@supabase" /Users/jeet/zietra/marketing/package.json
wc -l /Users/jeet/zietra/marketing/src/pages/LoginPage.tsx  # ≤ 30
wc -l /Users/jeet/zietra/marketing/src/pages/SignupPage.tsx  # ≤ 30
wc -l /Users/jeet/zietra/marketing/src/pages/DashboardPage.tsx  # ≤ 30
grep -q "window.location.replace" /Users/jeet/zietra/marketing/src/pages/SignupPage.tsx
cd /Users/jeet/zietra/marketing && npm run build  # 0 errors
```
  </verify>
  <done>
config.ts created with all 6 exports + helper. 3 auth pages shrunk to ~25 LOC each with Cognito redirects preserving query string. 2 Supabase lib files deleted. `@supabase/supabase-js` removed from package.json. `npm run build` succeeds with 0 errors and bundle ~70KB lighter. CloudFront SPA fallback verified (or added if missing).
  </done>
</task>

<task type="auto">
  <name>Task 2: HomePage + Pricing + Footer + NavBar + Privacy/Terms content refresh + sitemap generator</name>
  <files>/Users/jeet/zietra/marketing/src/pages/HomePage.tsx</files>
  <action>
**Step A — Refactor `src/data/pricing.ts`** (use RESEARCH §"Example 6" lines 942-1008 as exact template):

Add to `PricingTier` interface: `ctaHref: string`, `comingSoon?: boolean`. Replace the 3-tier array with: Starter (trial) $0, Base $99 featured comingSoon=true, Add-ons Custom. Import `APP_SIGNUP` from `../lib/config`. Drop the existing Starter/Growth/Scale $0/$79/$149 shape entirely.

**Step B — Update `src/components/PricingSection.tsx` CTA render block** (lines ~84-98):

Replace the hardcoded `<Link to="/signup">` with conditional render:
```tsx
{tier.comingSoon ? (
  <button
    type="button"
    disabled
    title="Stripe checkout coming soon"
    style={{ cursor: 'not-allowed', opacity: 0.6 }}
    aria-disabled="true"
  >
    {tier.cta}
  </button>
) : tier.ctaHref.startsWith('http') ? (
  <a href={tier.ctaHref} className="cta-primary">{tier.cta}</a>
) : (
  <Link to={tier.ctaHref} className="cta-primary">{tier.cta}</Link>
)}
```

Keep `Link` import from react-router for relative paths; use `<a>` for cross-origin app.zietra.com links.

**Step C — Refresh `src/pages/HomePage.tsx`** (current file has HubSpot/Buffer/Calendly framing at lines ~251-336):

Replace the `FEATURES` array with `WHATS_IN_THE_BOX` array — 13 entries, one per module:

```tsx
const WHATS_IN_THE_BOX = [
  { slug: 'crm',           icon: 'Users',          name: 'Salesforce CRM',           tagline: 'Accounts, opportunities, pipeline.' },
  { slug: 'sales',         icon: 'ShoppingCart',   name: 'NetSuite Sales',           tagline: 'Quote-to-cash with multi-tier pricing.' },
  { slug: 'purchase',      icon: 'Truck',          name: 'NetSuite Purchase',        tagline: 'POs, vendor management, 3-way match.' },
  { slug: 'items',         icon: 'Package',        name: 'Items + Inventory',        tagline: 'SKU master across all your stores.' },
  { slug: 'plm',           icon: 'GitBranch',      name: 'Arena PLM',                tagline: 'BOM control, change orders, drawings.' },
  { slug: 'mes',           icon: 'Factory',        name: 'MES Shop Floor',           tagline: 'Work orders, build steps, real-time WIP.' },
  { slug: 'quality',       icon: 'ShieldCheck',    name: 'Quality (NCR/CAPA/Audits)', tagline: 'Closed-loop quality with AI triage.' },
  { slug: 'asc606',        icon: 'Landmark',       name: 'ASC 606 (Aperture)',       tagline: 'Multi-element rev rec, audit-ready.' },
  { slug: 'royalty',       icon: 'Coins',          name: 'Royalty Management',       tagline: 'Agreements, accruals, payouts.' },
  { slug: 'dropship',      icon: 'PackageOpen',    name: 'Drop-ship',                tagline: 'PO routing to fulfillment vendors.' },
  { slug: 'lean-erp-pro',  icon: 'Settings2',      name: 'Lean ERP Pro',             tagline: 'Best-of-breed workflows, your way.' },
  { slug: 'ai-agents',     icon: 'Bot',            name: 'AI Agents',                tagline: 'NCR-CAPA, EVMS, Integration Sentinel.' },
  { slug: 'qb-migration',  icon: 'GitPullRequestArrow', name: 'QuickBooks → NetSuite', tagline: 'One-time data + COA migration.' },
]
```

(Icons referenced from `lucide-react` — verify each name exists before committing.) Map this array into a grid section titled 'What's in the box — 13 modules' with each card rendering as `<Link to={\`/modules/\${item.slug}\`}>` (those routes don't exist YET, they ship in 58-02; 404 fallback handles it gracefully in the interim).

Hero headline change (find existing hero text): 'Replaces HubSpot, Buffer, Calendly...' → '13 modules of a Fortune-500 ERP, at SMB pricing.' Sub-headline: 'CRM, Sales, NetSuite, PLM, MES, Quality, ASC 606, Royalty, Drop-ship, AI Agents — one tenant, one bill. $99/mo base. Free 30-day trial.'

Hero CTA already uses `<Link to="/signup">` — UPDATE to `<a href={APP_SIGNUP}>` (import from config). Same for any other home-page signup CTAs.

**Step D — Fix `src/components/SiteFooter.tsx`** (lines ~14-29 `FOOTER_COLS`):

Update the dead `href='#'` entries to real routes. Suggested structure:
```tsx
const FOOTER_COLS = [
  { title: 'Product',  links: [
    { label: 'Modules', to: '/modules' },
    { label: 'Pricing', to: '/pricing' },
    { label: 'Case studies', to: '/case-studies' },
    { label: 'Docs', to: '/docs' },
  ]},
  { title: 'Company',  links: [
    { label: 'About', to: '/about' },
    { label: 'Contact', to: '/contact' },
    { label: 'Privacy', to: '/privacy' },
    { label: 'Terms', to: '/terms' },
  ]},
  { title: 'Resources', links: [
    { label: 'Documentation', to: '/docs' },
    { label: 'Security', href: 'mailto:security@zietra.com' },
    { label: 'Support', href: 'mailto:support@zietra.com' },
  ]},
]
```

Render: if `to` use `<Link to={to}>`, if `href` use `<a href={href}>`. NO `href='#'`. Drop Blog/Careers/Press (open question 1 → omit).

**Step E — Update `src/components/NavBar.tsx`** (around lines ~38-42):

Add `/modules` and `/case-studies` to the mid-nav links. Existing `Pricing` link stays. Existing `#features` and `#stories` anchors retain (home-page only). CTA button on right stays 'Start free trial' but UPDATE its `<Link to="/signup">` to `<a href={APP_SIGNUP}>`.

**Step F — Fix `src/pages/PrivacyPage.tsx`** line ~76: 'Database: Supabase Postgres' → 'Database: AWS Aurora PostgreSQL (multi-tenant with Row Level Security)'. Sub-processors list at ~105-109: remove 'Supabase', add 'Amazon Web Services (Cognito identity, Aurora database, SES email, CloudFront CDN, Lambda compute)'. Drop Vercel if listed.

**Step G — Fix `src/pages/TermsPage.tsx`** lines ~54-58: remove 'you connect your own SMTP credentials' language. Replace with: 'Email notifications (signup, password reset, system alerts) are sent from `noreply@zietra.com` via AWS SES on our infrastructure. Support replies originate from `support@zietra.com`.'

**Step H — Update `src/pages/NotFoundPage.tsx`** — add a 'Popular pages' section below the existing 404 header:

```tsx
<section style={{ marginTop: 24 }}>
  <h3>Popular pages</h3>
  <ul>
    <li><Link to="/">Home</Link></li>
    <li><Link to="/modules">Modules</Link></li>
    <li><Link to="/pricing">Pricing</Link></li>
    <li><Link to="/case-studies">Case studies</Link></li>
    <li><Link to="/contact">Contact</Link></li>
    <li><Link to="/docs">Docs</Link></li>
  </ul>
</section>
```

Keep the existing `<meta name="robots" content="noindex">` Helmet.

**Step I — Update `public/llms.txt`** — STALE Supabase + non-13-module framing. Rewrite as a single concise paragraph mirroring HomePage:

```
# Zietra

Zietra is a multi-tenant SaaS ERP bundling 13 modules (CRM, Sales, Purchase, Items, PLM, MES, Quality, ASC 606, Royalty, Drop-ship, Lean ERP Pro, AI Agents, QuickBooks migration) on one tenant for $99/mo base + modular add-ons. Built on AWS Aurora PostgreSQL with Row Level Security tenant isolation, AWS Cognito identity (RS256 JWT), AWS SES email, and CloudFront CDN. Free 30-day trial at https://app.zietra.com/signup. Module catalog: https://zietra.com/modules. Case studies: https://zietra.com/case-studies. Contact: https://zietra.com/contact.
```

**Step J — Write `scripts/gen-sitemap.mjs`** (NEW):

Use RESEARCH §"Example 2: scripts/gen-sitemap.mjs" (lines 663-711) as template, but ADD graceful handling for missing data files:

```javascript
let MODULES = []
let CASE_STUDIES = []
try { ({ MODULES } = await import('../src/data/modules.ts')) } catch { /* not yet — 58-02 */ }
try { ({ CASE_STUDIES } = await import('../src/data/case-studies.ts')) } catch { /* not yet — 58-03 */ }
```

(NOTE: dynamic import of TS files from a Node script requires either compiled .js OR tsx/ts-node. For Phase 58-01, since both data files DON'T EXIST yet, the try/catch returns empty arrays and only static routes are written. In 58-02 we'll either compile via `node --import tsx/esm` flag or convert sync-modules.mjs to write a `.mjs` data file alongside. For now: try-catch + empty fallback. Static routes always present.)

STATIC_ROUTES same as RESEARCH example (9 routes, excludes /login /signup /dashboard /404).

After script writes file, log `✅ Wrote N URLs to public/sitemap.xml`.

**Step K — Update `package.json`:**

```json
"scripts": {
  "dev": "node scripts/gen-sitemap.mjs && vite",
  "build": "node scripts/gen-sitemap.mjs && tsc -b && vite build",
  ...
}
```

**Step L — Run build to regenerate sitemap:**

```bash
cd /Users/jeet/zietra/marketing
npm run build
grep -c "<loc>" public/sitemap.xml  # expect 9 (no modules/case-studies yet)
```

**Step M — Deploy:**

```bash
cd /Users/jeet/zietra/marketing
./deploy.sh
INV_ID=$(aws cloudfront list-invalidations --distribution-id E1X82T89JWL8CA --max-items 1 --query 'InvalidationList.Items[0].Id' --output text)
aws cloudfront wait invalidation-completed --distribution-id E1X82T89JWL8CA --id "$INV_ID"
```

**Step N — Live smoke (use whichever domain works — zietra.com if cert validated, else dlzyv23o98bvo.cloudfront.net):**

```bash
HOST="${ZIETRA_HOST:-https://zietra.com}"  # fallback https://dlzyv23o98bvo.cloudfront.net
PASS=0; FAIL=0
for p in / /pricing /privacy /terms /login /signup /dashboard /not-a-real-page /sitemap.xml; do
  CODE=$(curl -s -o /dev/null -w "%{http_code}" "$HOST$p")
  if [ "$CODE" = "200" ]; then PASS=$((PASS+1)); echo "OK   $p"; else FAIL=$((FAIL+1)); echo "FAIL $p → $CODE"; fi
done
echo "=== $PASS/9 ==="
test "$FAIL" = "0" || exit 1

# Verify HomePage no longer has HubSpot framing
curl -s "$HOST/" | grep -i "hubspot" && { echo "FAIL: HubSpot still in HomePage"; exit 1; } || echo "OK: HubSpot removed"

# Verify sitemap has expected URLs
curl -s "$HOST/sitemap.xml" | grep -c "<loc>"  # expect ≥ 9
```

**Step O — Commits:**

4. `feat(58-01): pricing.ts + PricingSection.tsx — 3-tier ($0/$99/Custom) with Stripe placeholder tooltip`
5. `feat(58-01): HomePage content refresh — 13-module 'What's in the box' framing replaces HubSpot/Buffer/Calendly`
6. `fix(58-01): SiteFooter + NavBar — real routes for /modules /case-studies /about /contact /docs (no more href='#')`
7. `fix(58-01): Privacy + Terms — Aurora replaces Supabase; SES-on-our-infra replaces user-SMTP language`
8. `feat(58-01): NotFoundPage — Popular pages recovery list (6 links)`
9. `feat(58-01): scripts/gen-sitemap.mjs + llms.txt refresh — build-time sitemap (9 routes; modules/case-studies graceful-missing)`
10. `chore(58-01): deploy Wave 1 — HomePage + 8 other routes live, sitemap 9 URLs, bundle -70KB`
  </action>
  <verify>
```bash
# Content checks
grep -q "comingSoon" /Users/jeet/zietra/marketing/src/data/pricing.ts
grep -q "WHATS_IN_THE_BOX\|13 modules" /Users/jeet/zietra/marketing/src/pages/HomePage.tsx
grep -q "/modules" /Users/jeet/zietra/marketing/src/components/SiteFooter.tsx
grep -q "/case-studies" /Users/jeet/zietra/marketing/src/components/NavBar.tsx
grep -q "AWS Aurora" /Users/jeet/zietra/marketing/src/pages/PrivacyPage.tsx
grep -q "noreply@zietra.com" /Users/jeet/zietra/marketing/src/pages/TermsPage.tsx
grep -q "Popular pages" /Users/jeet/zietra/marketing/src/pages/NotFoundPage.tsx
grep -q "AWS Aurora\|Cognito" /Users/jeet/zietra/marketing/public/llms.txt
# Build artifacts
test -f /Users/jeet/zietra/marketing/scripts/gen-sitemap.mjs
grep -c "<loc>" /Users/jeet/zietra/marketing/public/sitemap.xml  # ≥ 9
# Live (substitute dlzyv23o98bvo if zietra.com cert not ready)
curl -s -o /dev/null -w "%{http_code}\n" https://zietra.com/  # 200
curl -s https://zietra.com/ | grep -ci hubspot  # 0
```
  </verify>
  <done>
pricing.ts/PricingSection use new ctaHref + comingSoon; HomePage has 13-module framing (no HubSpot/Buffer/Calendly); SiteFooter + NavBar have real routes; Privacy says AWS Aurora not Supabase; Terms says SES-on-our-infra; NotFoundPage has Popular pages; gen-sitemap.mjs writes 9 routes; deployed to zietra.com (or fallback CF URL); live smoke 9/9 pass; HubSpot mentions absent from HomePage HTML.
  </done>
</task>

</tasks>

<verification>
- `test ! -f src/lib/auth.ts && test ! -f src/lib/supabase.ts` — Supabase files deleted
- `grep -L "@supabase" package.json` — Supabase dep removed
- `wc -l src/pages/{Login,Signup,Dashboard}Page.tsx` — each ≤ 30 LOC
- `grep -q "AWS Aurora" src/pages/PrivacyPage.tsx` — Privacy updated
- `grep -q "13 modules" src/pages/HomePage.tsx` — HomePage refreshed
- `grep -q "comingSoon" src/data/pricing.ts` — Stripe placeholder wired
- `grep -c "<loc>" public/sitemap.xml` — ≥ 9 routes
- `curl -s -o /dev/null -w "%{http_code}" https://zietra.com/` — 200
- `curl -s https://zietra.com/ | grep -ci hubspot` — 0 matches
- `curl -s -o /dev/null -w "%{http_code}" https://zietra.com/random-fake-path` — 200 (SPA fallback)
- 10 atomic commits pushed
</verification>

<success_criteria>
- [x] `@supabase/supabase-js` uninstalled; auth.ts + supabase.ts deleted
- [x] Login/Signup/Dashboard pages = ~25 LOC Cognito redirects preserving query string
- [x] `src/lib/config.ts` exports APP_URL + appUrl() helper
- [x] HomePage shows 13-module catalog (no HubSpot/Buffer/Calendly)
- [x] Pricing renders Stripe placeholder tooltip on Base tier
- [x] SiteFooter + NavBar link to /modules /case-studies /about /contact /docs
- [x] Privacy/Terms accurate post-Phase-55 (Aurora + Cognito + SES)
- [x] NotFoundPage has Popular pages recovery list
- [x] scripts/gen-sitemap.mjs writes 9 URLs (modules/case-studies absent until 58-02/03)
- [x] CloudFront SPA fallback (404+403 → /index.html, 200) verified/added
- [x] Deployed to zietra.com (or fallback CF URL); 9-path live smoke 100% pass; bundle -70KB
</success_criteria>

<output>
After completion, create `.planning/phases/58-m7-marketing-site-completion/58-01-SUMMARY.md`.
</output>
