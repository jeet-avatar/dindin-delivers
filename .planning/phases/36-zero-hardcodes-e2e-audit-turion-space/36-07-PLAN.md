---
phase: 36-zero-hardcodes-e2e-audit-turion-space
plan: 07
type: execute
wave: 3
depends_on: ["36-02", "36-05"]
files_modified:
  - /Users/jeet/turion-space-demo/backend/src/routes/notify.ts
  - /Users/jeet/turion-space-demo/backend/dist/routes/notify.js
  - /Users/jeet/turion-space-demo/backend/dist/routes/agents.js
  - /Users/jeet/turion-space-demo/backend/dist/app.js
  - /Users/jeet/turion-space-demo/backend/src/routes/agents.ts
  - /Users/jeet/turion-space-demo/backend/lambda-build
  - /Users/jeet/turion-space-demo/agent-sales-cash.html
  - /Users/jeet/turion-space-demo/dashboard-cio.html
  - /Users/jeet/turion-space-demo/about-this-demo.html
  - /Users/jeet/turion-space-demo/.gitignore
autonomous: true
requirements: [WipResolved]
user_setup:
  - service: resend
    why: "ERP-demo transactional email (notify.ts) — the WIP swapped SES→Resend; the plaintext key in source must move to a secret + Lambda env var"
    env_vars:
      - name: RESEND_API_KEY
        source: "AWS Secrets Manager — create secret 'turion-demo/production/resend-key' with payload {\"RESEND_API_KEY\":\"re_...\"} (rotate the exposed key first), then set RESEND_API_KEY (or RESEND_API_KEY_ARN) as a Lambda env var on turion-demo-api"
    dashboard_config:
      - task: "Rotate the exposed Resend key (re_JRdox6wH_...) — it was committed to the WIP diff incl. dist/"
        location: "Resend dashboard → API Keys"
must_haves:
  truths:
    - "backend/src/routes/notify.ts no longer contains the plaintext Resend API key; it reads the key lazily from process.env.RESEND_API_KEY (read at call time, not at module load — does not touch the cold-start path), mirroring how Phase-34's assistant.ts reads the Anthropic key"
    - "The plaintext key is also scrubbed from backend/dist/routes/notify.js (rebuild dist/ from cleaned src/)"
    - "backend/node_modules is no longer tracked by git (git rm --cached -r backend/node_modules) and node_modules/ is confirmed gitignored"
    - "The agents WIP is committed (NOT reverted): backend/src/routes/agents.ts + the rebuilt dist/, agent-sales-cash.html, dashboard-cio.html, about-this-demo.html — it's a real, working feature (3 AI agents doing real DB writes + audit_log)"
    - "backend/lambda-build's diff is confirmed benign (e.g. a CACHEBUST arg bump) before committing"
    - "npm run build + npx tsc --noEmit green in backend/; the SUMMARY documents the required user-action (create the secret + set the Lambda env var + rotate the key) for plan 36-09's deploy"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/backend/src/routes/notify.ts"
      provides: "Resend send via process.env.RESEND_API_KEY (no literal)"
      contains: "process.env.RESEND_API_KEY"
    - path: "/Users/jeet/turion-space-demo/backend/src/routes/agents.ts"
      provides: "3 AI agents (NCR→CAPA closure, EVMS Watchdog, Integration Sentinel) — real DB writes + audit"
      exports: ["default"]
  key_links:
    - from: "agent-sales-cash.html agent buttons"
      to: "POST /api/agents/{run,ncr-capa,evms,integration-sentinel}"
      via: "fetch"
      pattern: "/api/agents/"
    - from: "notify.ts"
      to: "process.env.RESEND_API_KEY"
      via: "lazy read at send time"
      pattern: "process\\.env\\.RESEND_API_KEY"
---

<objective>
Resolve the long-standing in-tree WIP in `turion-space-demo` (it's been getting stashed during every deploy): the agents feature is real and working — KEEP it; the blocker is `backend/src/routes/notify.ts` has a **hardcoded plaintext Resend API key** (`re_JRdox6wH_...`) which was also committed to the WIP's `dist/`. This plan: (1) move the Resend key to `process.env.RESEND_API_KEY` (lazy-read at send time, not module load — don't touch cold start; mirror Phase-34's `assistant.ts` Anthropic-key pattern), scrub it from `src/` AND `dist/`; (2) `git rm --cached -r backend/node_modules` (it's tracked despite `.gitignore` — npm churn that shouldn't be in git); (3) confirm `backend/lambda-build`'s diff is benign; (4) `npm run build` to regenerate a clean `dist/`; (5) commit the whole feature — `agents.ts`, the rebuilt `dist/`, `notify.ts`, `agent-sales-cash.html`, `dashboard-cio.html`, `about-this-demo.html`, `lambda-build` — under `jeet-avatar <jm@techcloudpro.com>`; (6) document the user-action (create the secret, set the Lambda env var, rotate the exposed key) in the SUMMARY for plan 36-09.

Purpose: WipResolved — the uncommitted changes are a coherent near-finished feature; finish + secure + commit (do NOT revert), and remove the only thing blocking it (the plaintext secret).
Output: A clean, committed agents feature; `node_modules` untracked; the Resend key secured; commit-not-pushed (plan 36-09 owns the push + Lambda redeploy + setting the env var). This plan OWNS the `backend/src/routes/agents.ts` / `backend/src/routes/notify.ts` / `backend/dist/routes/agents.js` / `backend/dist/routes/notify.js` / `backend/dist/app.js` / `backend/lambda-build` / `agent-sales-cash.html` / `dashboard-cio.html` / `about-this-demo.html` commits — other plans (36-02, 36-05) own different `backend/*` files (`lookups.ts`, `mes.ts`, `app.ts`); since 36-02 (W1) and 36-05 (W2) both edit `backend/src/app.ts`, this plan (W3) runs AFTER them: rebase onto their committed `src/` changes, then rebuild `dist/` LAST — after rebasing onto 36-02's + 36-05's committed `src/` changes — so `dist/` reflects the merged source.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-RESEARCH.md
@/Users/jeet/turion-space-demo/backend/src/routes/notify.ts
@/Users/jeet/turion-space-demo/backend/src/routes/agents.ts
@/Users/jeet/turion-space-demo/backend/lambda-build
@/Users/jeet/turion-space-demo/.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Secure notify.ts — move the Resend key to process.env, scrub from src/ + dist/</name>
  <files>/Users/jeet/turion-space-demo/backend/src/routes/notify.ts, /Users/jeet/turion-space-demo/backend/dist/routes/notify.js</files>
  <action>
Read `backend/src/routes/notify.ts` — it has `const RESEND_KEY = 're_JRdox6wH_...'` (plaintext) + `FROM = 'hello@artha.build'` + a Resend HTTPS POST. Find Phase-34's `assistant.ts` (or wherever the Anthropic key is read) in `/Users/jeet/turion-space-demo/backend/src/` to see the established lazy-secret pattern (read `process.env.X` at call time inside the handler, NOT at module top-level — so a missing env var doesn't crash cold start; just fails the email send with a clear error). Apply that pattern: replace the literal with `const resendKey = process.env.RESEND_API_KEY;` read INSIDE the send function; if it's falsy, return/throw a clear "RESEND_API_KEY not configured" error (do NOT fall back to a literal — and do NOT crash the Lambda). Keep `FROM = 'hello@artha.build'` (that's not a secret). Then `cd /Users/jeet/turion-space-demo/backend && npm run build` to regenerate `dist/routes/notify.js` (which will no longer contain the literal). VERIFY the scrub: `grep -rn "re_JRdox6wH" backend/` → ZERO matches in BOTH `src/` and `dist/`. If `git log` history of `dist/routes/notify.js` already committed the key in a PRIOR commit — that's out of scope here (history rewrite is risky) but NOTE it in the SUMMARY so the user knows to rotate; for THIS phase, the working-tree + the new commit must be clean.
  </action>
  <verify>`grep -rn "re_JRdox6wH" /Users/jeet/turion-space-demo/backend` → ZERO matches. `grep -n "process.env.RESEND_API_KEY" backend/src/routes/notify.ts` → matches, and it's inside a function (not a module-top `const`). `cd backend && npm run build && npx tsc --noEmit` → green. `node --check backend/dist/routes/notify.js` → clean.</verify>
  <done>No plaintext Resend key anywhere in `backend/`; `notify.ts` reads it from env lazily; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Untrack node_modules + confirm lambda-build diff is benign</name>
  <files>/Users/jeet/turion-space-demo/.gitignore, /Users/jeet/turion-space-demo/backend/lambda-build</files>
  <action>
`cd /Users/jeet/turion-space-demo && git ls-files backend/node_modules | head` — if it returns files, `git rm --cached -r backend/node_modules` (this only un-stages/un-tracks; it does NOT delete the files from disk). Confirm `node_modules/` (or `backend/node_modules/`) is in `.gitignore` — if not, add it. Confirm `backend/lambda-build` does `RUN npm ci --omit=dev` (so the runtime gets deps from `package-lock.json` at build time, not from a tracked `node_modules`) — read the file.
`git diff backend/lambda-build` — confirm the diff is benign (RESEARCH expects a `ARG CACHEBUST=1` line bump or a `COPY` tweak). If it's something risky (e.g. removing a needed `RUN`), STOP and flag it in the SUMMARY — but per RESEARCH it's harmless; keep it as-is.
  </action>
  <verify>`git ls-files backend/node_modules | wc -l` → 0 (after `git rm --cached`). `grep -n "node_modules" .gitignore` → matches. `git diff backend/lambda-build` reviewed; the SUMMARY states what the diff is and that it's benign (or flags it).</verify>
  <done>`node_modules` no longer tracked; `.gitignore` covers it; `lambda-build` diff confirmed benign.</done>
</task>

<task type="auto">
  <name>Task 3: Review the HTML/agents diffs + commit the feature</name>
  <files>/Users/jeet/turion-space-demo/backend/src/routes/agents.ts, /Users/jeet/turion-space-demo/backend/dist/routes/agents.js, /Users/jeet/turion-space-demo/backend/dist/app.js, /Users/jeet/turion-space-demo/agent-sales-cash.html, /Users/jeet/turion-space-demo/dashboard-cio.html, /Users/jeet/turion-space-demo/about-this-demo.html</files>
  <action>
Review the diffs: `git diff backend/src/routes/agents.ts` (the 3 agents — NCR→CAPA closure, EVMS Watchdog, Integration Sentinel; `runAgentLoop()` helper; real DB writes + `audit_log`), `git diff agent-sales-cash.html` (wires the agent "▶ Run Agent" buttons → `POST /api/agents/{run,ncr-capa,evms,integration-sentinel}`), `git diff dashboard-cio.html` (+~252 lines), `git diff about-this-demo.html` (visual redesign, pure presentation). Confirm: `agents.ts` references tables that exist in the `turion` schema (`ncrs`/`capas`/`wbs_elements`/`variances`/`sync_runs` etc. — the `app.ts` health-check query confirms them), and falls back to canned IDs if a table is empty. Confirm `agent-sales-cash.html`'s fetch calls target routes `agents.ts` actually defines (`/api/agents/run`, `/ncr-capa`, `/evms`, `/integration-sentinel` — match against the router). Confirm `about-this-demo.html`'s "data origin" table is editorial content (not DB data) — leave it as the WIP authored it. Confirm `agents.ts`'s Anthropic key is ALSO read from env (not a literal — grep `backend/src/routes/agents.ts` for `re_`/`sk-ant`; it should use the same `process.env`-lazy pattern as `assistant.ts`; if it has a literal, fix it the same way as notify.ts in Task 1).
Then `npm run build` ONE LAST TIME (so `dist/` reflects everything). Commit in `turion-space-demo` with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit` — `git add` ONLY: `backend/src/routes/notify.ts`, `backend/src/routes/agents.ts`, `backend/dist/routes/notify.js`, `backend/dist/routes/agents.js`, `backend/dist/app.js` (if changed by the rebuild — but ONLY the agents/notify-related changes; if 36-02/36-05 also changed `dist/app.js` and already committed, this rebuild's diff to `dist/app.js` should be empty or just reflect their committed src — `git diff --cached backend/dist/app.js` to sanity-check), `backend/lambda-build`, `.gitignore` (the node_modules entry), `agent-sales-cash.html`, `dashboard-cio.html`, `about-this-demo.html`, plus the `git rm --cached -r backend/node_modules` removal (stage it in the same commit). Do NOT `git add -A`. Do NOT push, do NOT run `build-and-push.sh` (plan 36-09 owns the deploy AND setting the `RESEND_API_KEY` Lambda env var). In the SUMMARY, prominently document the REQUIRED user-action for 36-09: create AWS Secrets Manager secret `turion-demo/production/resend-key` (payload `{"RESEND_API_KEY":"re_..."}` — with a freshly ROTATED key, since the old one was committed to the WIP), and set `RESEND_API_KEY` (or `RESEND_API_KEY_ARN` + a lazy SM read, matching the `DATABASE_URL` mechanism — check `aws lambda get-function-configuration --function-name turion-demo-api` for which pattern the Lambda uses) as a Lambda env var on `turion-demo-api`.
  </action>
  <verify>`git diff backend/src/routes/agents.ts | grep -c "re_\|sk-ant"` → 0 (no literal keys). `git log -1 --format=%an` (turion-space-demo) shows `jeet-avatar`. `git status --short` after the commit: the WIP files (`agents.ts`, `notify.ts`, `agent-sales-cash.html`, `dashboard-cio.html`, `about-this-demo.html`, `dist/*`, `lambda-build`) are no longer dirty; `backend/node_modules` is no longer tracked. `cd backend && npx tsc --noEmit` → green. The SUMMARY contains the user-action block.</verify>
  <done>The agents feature is committed (not reverted), secured (no plaintext keys), `node_modules` untracked; the deploy user-action is documented.</done>
</task>

</tasks>

<verification>
- No plaintext Resend/Anthropic key anywhere in `backend/` (src or dist).
- `notify.ts` (and `agents.ts` if it had one) reads keys lazily from `process.env`.
- `backend/node_modules` untracked; `.gitignore` covers it.
- `backend/lambda-build` diff confirmed benign.
- `npm run build` + `npx tsc --noEmit` green in `backend/`.
- The agents feature + 3 HTML changes committed under `jeet-avatar <jm@techcloudpro.com>`; not pushed.
- SUMMARY documents the required user-action (secret + Lambda env var + key rotation) for plan 36-09.
</verification>

<success_criteria>
- The in-tree WIP is resolved: finished + secured + committed (not reverted).
- The only blocker (plaintext Resend key) is removed from source and dist.
- Plan 36-09 has a clear checklist for the secret/env-var setup before deploying `turion-demo-api`.
</success_criteria>

<output>
After completion, create `.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-07-SUMMARY.md` — MUST include: the `notify.ts` before/after, confirmation no literal keys remain, the `lambda-build` diff description, and a prominent USER-ACTION block (create `turion-demo/production/resend-key`, rotate the exposed key, set the Lambda env var) for plan 36-09.
</output>
