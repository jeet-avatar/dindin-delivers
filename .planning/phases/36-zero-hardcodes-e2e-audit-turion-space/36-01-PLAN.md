---
phase: 36-zero-hardcodes-e2e-audit-turion-space
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/turion-satellite/backend/src/app.ts
  - /Users/jeet/turion-satellite/backend/src/routes/lookups.ts
  - /Users/jeet/turion-satellite/backend/tests/lookups.test.ts
autonomous: true
requirements: [LookupEndpoints]
must_haves:
  truths:
    - "GET /api/lifecycle-stages returns the program stages as {code,label,...} rows (already exists — verified, not assumed)"
    - "If any satellite-frontend dropdown/enum is NOT covered by an existing satellite lookup route, a new lookup route returns it"
    - "Satellite backend vitest suite stays green (no regressions)"
  artifacts:
    - path: "/Users/jeet/turion-satellite/backend/src/routes/lookups.ts"
      provides: "Any new satellite lookup endpoint(s) the satellite frontend needs (may be empty/no-op if already complete)"
    - path: "/Users/jeet/turion-satellite/backend/tests/lookups.test.ts"
      provides: "Tests for any new lookup route"
  key_links:
    - from: "satellite/satellite-render.js PROGRAM_STAGES (consumed in plan 36-03)"
      to: "GET /api/lifecycle-stages"
      via: "satelliteApi.get"
      pattern: "lifecycle-stages"
---

<objective>
Make the satellite (turion-satellite) backend lookup-complete so plan 36-03 can de-hardcode the satellite frontend (`PROGRAM_STAGES`) by fetching from an API. Per the RESEARCH, the satellite backend already exposes `/api/lifecycle-stages`, `/api/subsystems`, `/api/vendors`, labor-rates, fx-rates — so this plan is mostly a VERIFY pass plus filling any genuine gap. Do NOT invent endpoints the frontend doesn't need.

Purpose: Honor the PERMANENT "Turion frontend — zero hardcoding" rule — every dropdown/enum/stage list the satellite pages render must come from an API. RESEARCH found exactly one satellite-frontend hardcode (`PROGRAM_STAGES` in `satellite-render.js:135`), and it maps to the already-existing `/api/lifecycle-stages`.
Output: A confirmed-complete satellite lookup surface; a new `lookups.ts` router only if a real gap is found; tests; one local commit (NOT pushed — plan 36-09 owns the push + Lambda redeploy).
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-RESEARCH.md
@/Users/jeet/turion-satellite/backend/src/app.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the satellite frontend's dropdowns/enums and cross-check against existing satellite backend routes</name>
  <files>/Users/jeet/turion-satellite/backend/src/app.ts (read-only here)</files>
  <action>
In `/Users/jeet/turion-space-demo/satellite/`, grep every `*.html` + `*.js` for `<option`, `<select`, status-array literals, stage-array literals (e.g. `PROGRAM_STAGES`), and any hardcoded list that is DB-derivable (satellite/subsystem/vendor/labor-rate/fx-rate/lifecycle-stage lists). For each one, decide: (a) it's a domain constant (e.g. the `make`/`buy` 2-value enum, the 3D part-family→geometry map in `satellite-3d.js`) → LEAVE IT, note why; or (b) it's DB-derivable and needs an API → record which existing satellite route serves it. Read `/Users/jeet/turion-satellite/backend/src/app.ts` + `ls /Users/jeet/turion-satellite/backend/src/routes/` to enumerate the existing routes (`lifecycle-stages`, `subsystems`, `vendors`, labor-rates, fx-rates, etc.). RESEARCH's expectation: the ONLY real gap is `PROGRAM_STAGES` → `/api/lifecycle-stages` which already exists, so this task likely produces a "no new endpoint needed" finding. Write the inventory into the plan's SUMMARY.
  </action>
  <verify>The SUMMARY contains a table: each satellite-frontend list → {domain-constant: leave | DB-derivable: served-by-route X | DB-derivable: NEEDS new route}. Confirm `GET /api/lifecycle-stages` exists in `app.ts` and returns `{code,label,...}`-shaped rows (read the route handler).</verify>
  <done>Every satellite-frontend dropdown/enum is classified; the list of NEEDED new endpoints (likely empty) is explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Add any genuinely-needed satellite lookup endpoint(s) (skip if none) + tests + commit</name>
  <files>/Users/jeet/turion-satellite/backend/src/routes/lookups.ts, /Users/jeet/turion-satellite/backend/src/app.ts, /Users/jeet/turion-satellite/backend/tests/lookups.test.ts</files>
  <action>
IF Task 1 found a real gap: create `backend/src/routes/lookups.ts` (Express Router, same idioms as the existing routers — `requireAuth` per the established pattern, hardened catch that does NOT leak `err.message`, returns JSON), add `app.use('/api/lookups', lookupsRouter)` (or fold into an existing router if more natural), and add a route returning the needed list (derive from `SELECT DISTINCT ...` or hold the canonical list server-side — server-side canonical is fine). Add `backend/tests/lookups.test.ts` mirroring the existing test style (mocked `query()`, asserts shape + 401-unauth + 500-no-leak). IF Task 1 found NO gap: skip the file creation; just leave a note in the SUMMARY that the satellite backend is already lookup-complete (and 36-03 will point `PROGRAM_STAGES` at the existing `/api/lifecycle-stages`).
Then run `cd /Users/jeet/turion-satellite/backend && npx vitest run && npx tsc --noEmit` — must be green. Commit any changes in the `turion-satellite` repo with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit` (named files only — do NOT touch other dirty WIP). Do NOT push and do NOT run `build-and-push.sh` (plan 36-09 owns deploy). If nothing changed, no commit needed.
  </action>
  <verify>`cd /Users/jeet/turion-satellite/backend && npx vitest run` exit 0 (no regressions vs the 403-pass baseline) AND `npx tsc --noEmit` clean. If a new route was added: `node backend/scripts/audit-satellite-buttons.mjs` still exits 0 (the new route is allowlisted automatically since the allowlist is derived from `app.ts`). `git log -1 --format=%an` (in turion-satellite) shows `jeet-avatar` IF a commit was made.</verify>
  <done>Satellite backend is provably lookup-complete (existing routes cover everything OR a new route + tests added); vitest + tsc green; commit made if needed (not pushed).</done>
</task>

</tasks>

<verification>
- `GET /api/lifecycle-stages` confirmed to exist and return `{code,label}`-shaped rows.
- Every satellite-frontend dropdown/enum is classified (domain-constant vs DB-derivable-and-served).
- `npx vitest run` + `npx tsc --noEmit` green in `/Users/jeet/turion-satellite/backend`.
- Button audit (`audit-satellite-buttons.mjs`) still 0 violations.
</verification>

<success_criteria>
- The satellite backend serves every DB-derivable list the satellite frontend renders (proven by inventory cross-check, not assumed).
- Any new endpoint has tests and passes the audit.
- No push, no Lambda redeploy (plan 36-09 owns deploy).
</success_criteria>

<output>
After completion, create `.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-01-SUMMARY.md` — MUST include the inventory table and the explicit list of new endpoints added (or "none — already complete").
</output>
