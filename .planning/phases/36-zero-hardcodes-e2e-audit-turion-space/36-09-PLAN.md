---
phase: 36-zero-hardcodes-e2e-audit-turion-space
plan: 09
type: execute
wave: 5
depends_on: ["36-01", "36-02", "36-03", "36-04", "36-05", "36-06", "36-07", "36-08"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [SatellitePlmE2E, ArenaE2E, SalesforceE2E, NetSuiteE2E, MesE2E, NoFrontendHardcodes, LookupEndpoints, WipResolved, ButtonAuditCleanBothFrontends]
user_setup:
  - service: resend
    why: "Plan 36-07 secured notify.ts to read RESEND_API_KEY from env; before redeploying turion-demo-api the secret must exist + the env var must be set + the exposed key rotated"
    env_vars:
      - name: RESEND_API_KEY
        source: "AWS Secrets Manager 'turion-demo/production/resend-key' {\"RESEND_API_KEY\":\"re_...\"} (rotated), then set on Lambda turion-demo-api as RESEND_API_KEY (or RESEND_API_KEY_ARN + lazy SM read, matching the DATABASE_URL mechanism — check `aws lambda get-function-configuration --function-name turion-demo-api`)"
    dashboard_config:
      - task: "Rotate the exposed Resend key (re_JRdox6wH_...)"
        location: "Resend dashboard → API Keys"
must_haves:
  truths:
    - "Both backends are redeployed: turion-satellite via ./build-and-push.sh (only if its backend changed in 36-01); turion-demo-api via turion-space-demo/backend/build-and-push.sh (lookup endpoints + agents WIP + notify env-var change + any MES route)"
    - "The RESEND_API_KEY secret + Lambda env var are in place BEFORE the turion-demo-api redeploy (or the redeploy is held at a checkpoint until the user confirms)"
    - "The frontend is deployed via turion-space-demo/deploy-frontend.sh with a RECOMPUTED F6 pre-flight (git status --short → stash only the genuinely-uncommitted leftovers + mv .superpowers/ aside; restore after, even on failure) — note the WIP HTMLs are NOW COMMITTED (36-07) so the stash list is much smaller than Phases 30-35"
    - "CloudFront E37R9PT8IL44L2 invalidation polled to Status=Completed"
    - "Curl smoke: /api/health on BOTH APIGW 200; /api/data/all non-empty; turionspace.zietra.com/satellite/ + /index.html + a handful of ERP pages HEAD 200 with NO *-data.js 404 in source; /turion-config.js + /satellite/satellite-config.js both 200 and define their globals"
    - "DB-direct E2E walk per module against prod: Salesforce (POST customer→GET it→PATCH a field→GET), NetSuite (POST sales-order→GET; if a JE/invoice post button exists, PATCH→GET), Arena (POST NCR→PATCH close→GET), MES (PATCH stage→GET), Satellite PLM (sales-order→spawn→BOM rows→PATCH instance stage→create WO→sign off a build step→cost rollup updates) — using the ERP DB conn string from the turion-demo-api Lambda's secret (get its name via `aws lambda get-function-configuration --function-name turion-demo-api`) and the satellite DB secret `turion-satellite/production/database-url` (plain conn string, strip `?schema=`)"
    - "npm run audit-buttons → both audits violations:0"
    - "Phases 27-35 regression intact: 3D viewer (satellite-3d.js + import map), satellite-chat.js widget, svg-editor.js / CAD editor, BOM tree, part-management UI, the 165-part Cygnus dataset — all still load/work"
    - "STATE.md (narrative-format, manual) + ROADMAP.md updated to mark Phase 36 complete"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Phase 36 completion entry (narrative format, manual)"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 36 marked complete with plan checklist"
  key_links:
    - from: "deploy-frontend.sh"
      to: "S3 turion-demo-static + CloudFront E37R9PT8IL44L2"
      via: "aws s3 sync . --delete + create-invalidation"
      pattern: "E37R9PT8IL44L2"
---

<objective>
Deploy everything and prove it works end to end. Push BOTH repos (`turion-satellite` + `turion-space-demo`); redeploy BOTH Lambdas (`turion-satellite-api` only if 36-01 changed it; `turion-demo-api` for the lookup endpoints + the agents WIP + the `notify.ts` env-var change + any new MES route); deploy the frontend via `turion-space-demo/deploy-frontend.sh` with a recomputed F6 pre-flight; poll the CloudFront invalidation to Completed; curl smoke both apps; DB-direct E2E walk per module against prod; `npm run audit-buttons` (both = 0); regression-check Phases 27-35; update STATE.md + ROADMAP.md. Ends with a headless-substitute human-verify checkpoint (per Phases 27-35 — curl/HEAD smoke + DB walk + audit ARE the substitute; visual browser walk noted as pending). `autonomous: false`.

Purpose: Ship the phase; close all 9 requirements.
Output: Both Lambdas redeployed, frontend live, CF invalidated, all smoke + E2E + audit + regression checks pass, STATE/ROADMAP updated.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-RESEARCH.md
@.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-07-SUMMARY.md
@/Users/jeet/turion-space-demo/deploy-frontend.sh
@/Users/jeet/turion-space-demo/backend/build-and-push.sh
@/Users/jeet/turion-satellite/backend/build-and-push.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set the RESEND_API_KEY secret + Lambda env var, then push + redeploy both backends</name>
  <files>(deploy actions; no repo files modified here)</files>
  <action>
PRE-FLIGHT (the user-action from 36-07): check `aws lambda get-function-configuration --function-name turion-demo-api` for how it pulls `DATABASE_URL` (direct env var vs `*_ARN` + lazy SM read). Mirror that for Resend: create AWS Secrets Manager secret `turion-demo/production/resend-key` with payload `{"RESEND_API_KEY":"re_..."}` using a FRESHLY-ROTATED key (the old `re_JRdox6wH_...` was committed to the WIP — rotate it in the Resend dashboard first). If the executor cannot rotate the key (no Resend access) → STOP and surface this in the checkpoint (Task 6) as a blocker; do NOT redeploy `turion-demo-api` with a stale/exposed key. Set the env var on `turion-demo-api` (`aws lambda update-function-configuration --function-name turion-demo-api --environment ...` merging with existing vars — do NOT clobber `DATABASE_URL` etc.). Verify `aws lambda get-function-configuration` shows the new var.
THEN:
- `cd /Users/jeet/turion-satellite && git push origin main` (the 36-01 commit, if any) → `cd backend && ./build-and-push.sh` (Docker arm64 → ECR `turion-satellite-api` → `aws lambda update-function-code` → `aws lambda wait function-updated`). SKIP this whole step if 36-01 made no satellite-backend change (the SUMMARY says so) — but if 36-01 added a route, this is required. Confirm `curl -s https://rjydekliee.execute-api.us-east-1.amazonaws.com/api/health` (or whatever the health path is) → 200, and the new route (if any) is reachable (401-gated unauth is fine).
- `cd /Users/jeet/turion-space-demo && git push origin main` (the 36-02/36-03/36-04/36-05/36-06/36-07/36-08 commits) → `cd backend && ./build-and-push.sh` (Docker arm64 from `backend/lambda-build` → ECR `turion-demo-api` → `aws lambda update-function-code` → wait). Confirm `curl -s https://lo254mvukl.execute-api.us-east-1.amazonaws.com/api/health` → 200; `curl -s .../api/data/all` → non-empty JSON; `curl -s .../api/arena/lookups` (+ netsuite/salesforce/mes) → the enum lists; `curl -s -X POST .../api/agents/run` (or HEAD) → reachable (auth/validation error is fine, 404 is not).
  </action>
  <verify>`aws lambda get-function-configuration --function-name turion-demo-api` shows `RESEND_API_KEY` (or `RESEND_API_KEY_ARN`). Both `/api/health` endpoints 200. `/api/data/all` non-empty. `/api/{arena,netsuite,salesforce,mes}/lookups` return lists. `/api/agents/*` routes resolve (not 404). `git log -1 --format=%an` on both repos shows `jeet-avatar` and `git status` shows nothing un-pushed.</verify>
  <done>Both backends redeployed (satellite only if changed); the Resend secret + env var in place; health + key endpoints confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy the frontend with a recomputed F6 pre-flight + poll CloudFront to Completed</name>
  <files>(deploy actions)</files>
  <action>
RECOMPUTE the F6 pre-flight (the WIP HTMLs are NOW committed by 36-07, so the stash list is much smaller than Phases 30-35): `cd /Users/jeet/turion-space-demo && git status --short` → for any genuinely-uncommitted dirty file (should be near-empty after plans 02-08 committed their named files; possibly `turion-config.js`/`satellite-config.js` which are gitignored-and-regenerated — those are FINE, deploy-frontend.sh regenerates them; possibly a stray leftover) → `git stash push -- <those files>` ONLY if they'd pollute the `aws s3 sync . --delete`; AND `mv .superpowers/ /tmp/.superpowers-stash-36 2>/dev/null` (it contains `*.html` which `--include "*.html"` would carry) and `mv .DS_Store /tmp/ 2>/dev/null`. Record what was stashed/moved.
Run `./deploy-frontend.sh` (it calls `scripts/generate-satellite-config.sh` AND — per 36-02 — `scripts/generate-turion-config.sh`, then `aws s3 sync . s3://turion-demo-static --delete --include "*.html" "*.js" "*.css" "*.jpg" "*.png" "*.svg" --exclude "backend/*" "*.md" "*.sh"`, then `aws cloudfront create-invalidation --distribution-id E37R9PT8IL44L2 --paths "/*"`). Capture the invalidation ID. Poll `aws cloudfront get-invalidation --distribution-id E37R9PT8IL44L2 --id <ID>` until `Status=Completed`.
RESTORE (always, even if the deploy failed): `git stash pop` (if anything was stashed) + `mv /tmp/.superpowers-stash-36 .superpowers 2>/dev/null` + `mv /tmp/.DS_Store . 2>/dev/null`. Confirm `git stash list` is empty and `git status --short` matches the pre-deploy baseline.
  </action>
  <verify>`deploy-frontend.sh` exits 0. CloudFront invalidation `Status=Completed`. `git stash list` empty after restore. `git status --short` == pre-deploy baseline. The new `turion-config.js` was generated (deploy log shows `generate-turion-config.sh` ran) and synced.</verify>
  <done>Frontend deployed (both apps, one bucket); CF invalidated to Completed; F6 stash restored cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Curl smoke — both apps, no broken includes, configs resolve</name>
  <files>(read-only verification)</files>
  <action>
- `curl -sI https://turionspace.zietra.com/satellite/index.html` → 200; same for `/satellite/{login,sat,parts,part,bom,kanban,instance,cost,cost-detail,work-orders,work-order,program-new}.html`.
- `curl -sI https://turionspace.zietra.com/index.html` → 200; same for a handful of ERP pages across modules: `salesforce-account.html`, `netsuite-setup.html`, a couple of `netsuite-*.html`, `arena-qms.html`, `arena-bom.html`, `mes-shop-floor.html`, `integration-hub.html`, `agent-sales-cash.html`, `dashboard-cio.html`, `about-this-demo.html`.
- `curl -s https://turionspace.zietra.com/turion-config.js` → 200 + contains `window.TURION_CONFIG` with the right `API_BASE`. `curl -s https://turionspace.zietra.com/satellite/satellite-config.js` → 200 + contains `SATELLITE_CONFIG`/`API_BASE`.
- For each ERP page fetched: `curl -s <page> | grep -E 'enterprise-data.js|coa-data.js|fpa-data.js|evms-data.js|arm-data.js|setup-data.js|bom-data.js|qms-data.js|arena-doc-data.js|mes-data.js|integration-.*-data.js'` → NO matches (the static fallback files are gone; pages don't reference them). And `curl -sI https://turionspace.zietra.com/enterprise-data.js` → 403/404 (deleted).
- `curl -s https://lo254mvukl.execute-api.us-east-1.amazonaws.com/api/health` → 200; `.../api/data/all` → non-empty.
  </action>
  <verify>All listed pages 200. `turion-config.js` + `satellite-config.js` 200 with their globals. No ERP page references a deleted `*-data.js`; the deleted files 403/404. Backend `/api/health` + `/api/data/all` OK.</verify>
  <done>Both apps live; no broken includes; configs resolve; backend healthy.</done>
</task>

<task type="auto">
  <name>Task 4: DB-direct E2E walk per module against prod</name>
  <files>(read-only verification against prod DBs)</files>
  <action>
⚠️ ENDPOINT-VERIFICATION GATE (do this FIRST, before using ANY endpoint path below): every `POST`/`PATCH`/`GET` path written in this task's per-module list is a CANDIDATE PATH — a generic-CRUD guess — NOT asserted fact. Before using one, verify it actually exists: for the ERP backend `grep -rn 'the/path' /Users/jeet/turion-space-demo/backend/src/routes/*.ts` (and `app.ts` for inline routes); for the satellite backend `grep -rn 'the/path' /Users/jeet/turion-satellite/backend/src/app.ts` and `/Users/jeet/turion-satellite/backend/src/routes/*` — and adapt to the REAL route name + request-body shape (the new-form HTML and the 36-04/36-05/36-06 SUMMARYs tell you which routes the buttons actually POST to). If a module has NO write endpoint at all, do that module's E2E walk via direct `psql` INSERT/UPDATE/DELETE against the schema instead, and note in the SUMMARY that the module's persistence is read-mostly. Treat the list below as 'candidate paths — verify first', not a contract.

Get the ERP DB conn string: `aws lambda get-function-configuration --function-name turion-demo-api` → find the `DATABASE_URL` value (or the secret ARN; if a secret, `aws secretsmanager get-secret-value --secret-id <arn>`). Get the satellite DB conn string: `aws secretsmanager get-secret-value --secret-id turion-satellite/production/database-url` (plain conn string — strip any `?schema=` suffix; the schema is `turion_satellite`). Use `psql` (or a quick node `pg` script) for each module. The endpoint paths below are CANDIDATE PATHS — verify each one per the ENDPOINT-VERIFICATION GATE above before using it:
- **Salesforce** (schema `turion`): `INSERT` a test customer via `POST /api/salesforce/customers` (or `/api/netsuite/customers` — whichever the new-form uses) → confirm the row in `turion.customers` → `PATCH /api/salesforce/customers/:id` a field → confirm the `source_data` change in the DB → confirm an `audit_log` entry. Clean up the test row.
- **NetSuite** (schema `turion`): `POST /api/netsuite/sales-orders` a test SO → confirm in `turion.sales_orders`. If a JE/invoice "post" button exists (per 36-06), `PATCH /api/netsuite/journal_entries/:id` (or `invoices/:id`) → confirm the status change. Clean up.
- **Arena** (schema `turion`): `POST /api/arena/ncrs-create` a test NCR → confirm in `turion.ncrs` → `PATCH /api/arena/ncrs/:id` to close it → confirm status. Optionally `POST /api/agents/ncr-capa` → confirm it created a `turion.capas` row + closed an NCR + wrote `audit_log`. Clean up test rows.
- **MES** (schema `turion`): `PATCH /api/mes/stages/:num` → confirm the stage's state changed in the DB. Revert.
- **Satellite PLM** (schema `turion_satellite`, via `https://rjydekliee.execute-api...`): the chain — `POST /api/sales-orders` → confirm a satellite spawned (`turion_satellite.satellites`) → confirm `bom_lines` rows → `PATCH` an instance's lifecycle stage → confirm in `part_instances` → `POST /api/work-orders` → sign off a `build_steps` row → confirm `analytics/cost-rollup` reflects it. (These routes are auth-gated — use the same auth approach Phase 35-07's deploy walk used, or do the writes directly via SQL if the route auth is impractical headless, mirroring prior phases' DB-direct substitute.) Clean up test rows; confirm row counts return to baseline (165 Cygnus part_definitions, ~261 part_instances per Phase 31's note).
Record every step + result in the SUMMARY.
  </action>
  <verify>Each module's write+read+update round-trip succeeds against the prod DB; `audit_log` entries present where expected; all test rows cleaned up; baseline row counts restored.</verify>
  <done>Every module is proven E2E-persistent against production.</done>
</task>

<task type="auto">
  <name>Task 5: Button audit (both) + Phases 27-35 regression check</name>
  <files>(read-only verification)</files>
  <action>
- `cd /Users/jeet/turion-space-demo && npm run audit-buttons` → BOTH the satellite audit AND the new ERP audit print `violations:0`, command exits 0. Also `node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` directly → `violations:0`, exit 0.
- Regression-check Phases 27-35 (the satellite PLM app must still fully work):
  - `curl -s https://turionspace.zietra.com/satellite/part.html | grep -E 'satellite-3d.js|importmap|three@0.184.0|svg-editor.js|satellite-chat.js'` → all present.
  - `curl -s https://turionspace.zietra.com/satellite/instance.html | grep -E 'satellite-3d.js|cad-hud|satellite-chat.js'` → present.
  - `curl -sI https://turionspace.zietra.com/satellite/3d-test.html` → 200.
  - `curl -s https://turionspace.zietra.com/satellite/bom.html | grep -E 'view=3d|treeContainer|drawing_svg'` → present (the BOM tree + 3D deep-links + inline SVGs).
  - `curl -sI https://turionspace.zietra.com/satellite/satellite-3d.js` → 200 + JS content-type + `grep mount3DViewer`.
  - DB (schema `turion_satellite`): `SELECT count(*) FROM part_definitions WHERE retired_at IS NULL` → 165 (the Cygnus dataset); `SELECT count(*) FROM part_instances` → ~261; spot-check a part still has its `drawing_svg`.
  - `curl -s https://turionspace.zietra.com/satellite/kanban.html | grep -E 'lifecycle-stages|Pick a satellite'` → present.
  - `curl -s https://turionspace.zietra.com/satellite/sat.html | grep programProgress` → present (and it now uses fetched stages, not the removed `PROGRAM_STAGES` literal).
Record results in the SUMMARY. If any regression is found → STOP, surface in the checkpoint.
  </action>
  <verify>Both audits `violations:0`. All Phase 27-35 regression spot-checks pass (pages/scripts present, dataset counts intact). No regression surfaced.</verify>
  <done>Audits clean on both frontends; Phases 27-35 confirmed intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6 (checkpoint): Headless-substitute human-verify of the deployed demo</name>
  <action>Present the curl/HEAD smoke results, the per-module DB-direct E2E walk results, and the dual button-audit (0/0) as the headless substitute for a visual browser walk (per Phases 27-35). The user reviews and approves or flags issues. Visual browser walk is the optional follow-up.</action>
  <what-built>
  Phase 36 deployed: BOTH backends redeployed (turion-satellite-api if changed; turion-demo-api with lookup endpoints + the agents WIP + the secured notify.ts), the frontend deployed (both the satellite PLM app and the legacy ERP demo) with the new generated `turion-config.js` + the static `*-data.js` fallback files DELETED so every ERP page is API-driven, the `ns-editable.js` edits now PATCH the backend, the primary state-change buttons per module persist, the button audit is 0 violations on BOTH frontends, the WIP is resolved (agents committed, Resend key moved to AWS Secrets Manager + a Lambda env var). DB-direct E2E walks against prod confirmed persistence per module (Salesforce/NetSuite/Arena/MES + the full satellite PLM chain). Phases 27-35 regression confirmed intact.
  </what-built>
  <how-to-verify>
  1. Visit https://turionspace.zietra.com/index.html — the ERP demo lands, all module tiles reachable.
  2. Visit a few ERP pages: salesforce-account.html, netsuite-setup.html, arena-qms.html, mes-shop-floor.html, agent-sales-cash.html — they load with real data, no console 404s for `*-data.js`, dropdowns populated.
  3. On agent-sales-cash.html, click a "▶ Run Agent" button — it hits `/api/agents/...`, returns an outcome card (real DB write).
  4. On salesforce-account.html, enter Edit mode, change a field, Save, then reload — the new value persists (it came from the DB, not localStorage).
  5. Visit https://turionspace.zietra.com/satellite/ — the satellite PLM app still works: open a part, the 2D/3D toggle + 3D viewer render, the lifecycle progress strip shows the right stages, the BOM tree drills down, the chat widget loads.
  6. Confirm the curl/DB-walk/audit evidence in 36-09-SUMMARY.md looks complete (this is the headless substitute for a full visual walk — visual browser walk is the optional follow-up, per Phases 27-35).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 36 complete, or describe issues to address.</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Update STATE.md + ROADMAP.md (manual, narrative format)</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
Update `.planning/STATE.md` (narrative format — manual, consistent with Phases 27-35): add a Phase 36 completion entry summarizing what shipped — zero hardcodes on both frontends (satellite `PROGRAM_STAGES` → `/api/lifecycle-stages`; ERP static `*-data.js` fallback deleted, pages render from `/api/data/all`, `turion-config.js` single source of truth), lookup endpoints added (`/api/{arena,netsuite,salesforce,mes}/lookups` + any satellite one), ERP persistence (`ns-editable.js` → PATCH, primary state-change buttons wired per module), the WIP resolved (3 AI agents committed, Resend key → Secrets Manager `turion-demo/production/resend-key` + Lambda env var on `turion-demo-api` — NOTE the required key rotation), button audit 0 violations both frontends (`audit-erp-buttons.mjs` added), both backends redeployed (CodeSha256 before→after for each), frontend deployed (CF invalidation ID), Phases 27-35 regression intact. Note any deferred items (e.g. MES domain depth stays in the satellite app; `ns_invoice_id` NULL / instance>1 dups remain pre-existing out-of-scope). Note the headless-substitute checkpoint approval (per Phases 27-35 precedent). Mark all 9 requirements satisfied.
Update `.planning/ROADMAP.md`: mark Phase 36 `[x]` complete, update `**Plans:** 9 plans`, fill the plan checklist (`- [x] 36-01-PLAN.md — ...` etc.), update the `*Last updated:*` footer line.
Then commit BOTH files to `/Users/jeet/doordash-p2p` with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit -m "docs(36): Phase 36 complete — zero hardcodes + E2E audit across the Turion Space demo"`.
  </action>
  <verify>`grep -n "Phase 36" .planning/STATE.md` shows the completion entry. `grep -n "36-09-PLAN.md" .planning/ROADMAP.md` shows `[x]`. `git log -1 --format=%an` in `/Users/jeet/doordash-p2p` shows `jeet-avatar`.</verify>
  <done>STATE.md + ROADMAP.md reflect Phase 36 complete; committed.</done>
</task>

</tasks>

<verification>
- RESEND_API_KEY secret + Lambda env var in place; exposed key rotated (or blocker surfaced at checkpoint).
- Both backends redeployed (satellite only if 36-01 changed it); both `/api/health` 200.
- Frontend deployed with recomputed F6 pre-flight; CF invalidation Completed; stash restored cleanly.
- Curl smoke: both apps 200, no `*-data.js` 404, `turion-config.js` + `satellite-config.js` resolve.
- DB-direct E2E walk per module passes (writes persist; audit_log entries; test rows cleaned up).
- `npm run audit-buttons` → both audits violations:0.
- Phases 27-35 regression intact.
- Headless-substitute checkpoint approved.
- STATE.md + ROADMAP.md updated + committed.
</verification>

<success_criteria>
- The entire turionspace.zietra.com demo (both apps) is hardcode-free in the frontend, persists every primary action to the right backend, passes the button audit on both frontends, with the WIP resolved and both backends + the frontend deployed — all 9 Phase-36 requirements satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-09-SUMMARY.md` — MUST include: the secret/env-var setup (+ key-rotation status), both repos' push commits, both Lambdas' CodeSha256 before→after, the CF invalidation ID, the full curl-smoke table, the per-module DB-direct E2E walk results, the `audit-buttons` output for both, the Phase 27-35 regression results, and any deferred items.
</output>
