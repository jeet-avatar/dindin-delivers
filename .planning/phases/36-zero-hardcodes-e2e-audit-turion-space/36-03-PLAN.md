---
phase: 36-zero-hardcodes-e2e-audit-turion-space
plan: 03
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - /Users/jeet/turion-space-demo/satellite/satellite-render.js
autonomous: true
requirements: [NoFrontendHardcodes, SatellitePlmE2E]
must_haves:
  truths:
    - "satellite/satellite-render.js no longer hardcodes the program-stage list (PROGRAM_STAGES); programProgress() / majorityStage() get the stages from /api/lifecycle-stages"
    - "The lifecycle progress strip on part/instance/kanban/sat pages still renders correctly (same stages, same order, same labels)"
    - "node backend/scripts/audit-satellite-buttons.mjs (in turion-satellite) still exits 0"
    - "The Phase-33 satellite PLM flow is spot-checked end-to-end (sales order → spawn → BOM → kanban → lifecycle → WO → build steps → cost rollup → delivery FKs) — pages load, no dead buttons surfaced"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/satellite/satellite-render.js"
      provides: "programProgress/majorityStage driven by fetched lifecycle stages, not a literal array"
      contains: "lifecycle-stages"
  key_links:
    - from: "satellite/satellite-render.js"
      to: "GET /api/lifecycle-stages"
      via: "satelliteApi.get (lazy-fetch+cache) OR caller passes the already-fetched stages array as a param"
      pattern: "lifecycle-stages"
---

<objective>
De-hardcode the satellite frontend's ONE real hardcode: `const PROGRAM_STAGES = [...]` in `satellite/satellite-render.js:135` — it duplicates the `/api/lifecycle-stages` table and feeds `programProgress()` (the lifecycle progress strip used on part/instance/kanban/sat pages). Make it come from the API. Then spot-check the Phase-33 PLM flow still works.

Purpose: Honor the PERMANENT "Turion frontend — zero hardcoding" rule on the satellite side; the rest of the satellite frontend is already clean (the 3D part-family meshes and `make`/`buy` 2-value enums are domain constants — LEAVE them).
Output: `satellite-render.js` with no hardcoded stage list; button audit still 0 violations; a spot-check note on the PLM flow. Commit-not-pushed in `turion-space-demo` (plan 36-09 owns deploy + CF invalidation).
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-RESEARCH.md
@/Users/jeet/turion-space-demo/satellite/satellite-render.js
@/Users/jeet/turion-space-demo/satellite/kanban.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace PROGRAM_STAGES with lifecycle-stages fetched from the API</name>
  <files>/Users/jeet/turion-space-demo/satellite/satellite-render.js</files>
  <action>
Read `satellite-render.js` around line 135: `const PROGRAM_STAGES = [{code:'drawing',label:'Drawing'},...]` and find every consumer (`programProgress()`, `majorityStage()`, anything iterating it). Read `kanban.html` to see how it already fetches `GET /api/lifecycle-stages` (it does — copy that idiom). Choose ONE approach (RESEARCH suggests either):
(a) **Lazy-fetch+cache in satellite-render.js**: add a module-level `let _stages = null; async function getStages(){ if(!_stages){ _stages = await satelliteApi.get('/api/lifecycle-stages'); } return _stages; }` and make `programProgress()`/`majorityStage()` `async` (await `getStages()`), updating callers; OR
(b) **Caller-passes-stages**: change `programProgress(stages, ...)` / `majorityStage(stages, ...)` to take the stages array as the first param; the calling pages (part.html, instance.html, kanban.html, sat.html) already fetch `/api/lifecycle-stages` (or add that fetch to the ones that don't) and pass it in.
Pick whichever is least invasive given how the callers are structured (read part.html/instance.html/sat.html to decide). Map the API row shape (`{code,label,...}` — confirm field names from the actual `/api/lifecycle-stages` response or the route handler) to whatever `programProgress()` expects. Preserve the exact stage order (the API returns them ordered — confirm; if not, order by the same key the old literal implied). Remove the `PROGRAM_STAGES` literal entirely. `node --check satellite-render.js`.
  </action>
  <verify>`grep -n "PROGRAM_STAGES" satellite-render.js` → no matches (or only a comment noting it was removed). `grep -n "lifecycle-stages" satellite-render.js` (or the caller pages) → matches. `node --check satellite/satellite-render.js` clean. `node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` → exit 0, violations:0.</verify>
  <done>No hardcoded stage list in `satellite-render.js`; stages come from `/api/lifecycle-stages`; audit clean; JS parses.</done>
</task>

<task type="auto">
  <name>Task 2: Spot-check the Phase-33 PLM flow + commit</name>
  <files>(read-only spot-check; commit satellite-render.js)</files>
  <action>
SPOT-CHECK only (do NOT rebuild — Phase 33 built this; just confirm it still works):
- `curl -sI` (or HEAD) the deployed satellite pages: `turionspace.zietra.com/satellite/index.html`, `/satellite/program-new.html`, `/satellite/sat.html`, `/satellite/bom.html`, `/satellite/kanban.html`, `/satellite/work-orders.html`, `/satellite/work-order.html`, `/satellite/cost.html`, `/satellite/cost-detail.html`, `/satellite/part.html`, `/satellite/instance.html` → all 200.
- Confirm the satellite backend routes for the full chain still exist: `grep` in `/Users/jeet/turion-satellite/backend/src/app.ts` (or `routes/`) for `sales-orders`, `satellites` (spawn), `bom`, `lifecycle-stages`, `lifecycle`, `work-orders`, `build-steps` (or `work-orders/:woId/steps`), `make-costs`, `buy-costs`, `make-buy-decisions`, `analytics/cost-rollup`.
- Confirm `kanban.html` still fetches `/api/lifecycle-stages` for its columns.
Note any genuinely-broken page/button in the SUMMARY (do NOT fix here unless it's a one-liner — file it as a deferred item; the deep E2E DB walk is plan 36-09's job).
Then commit `satellite-render.js` in `turion-space-demo` with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit` — named file only; do NOT touch the 36-07-owned WIP. Do NOT push, do NOT deploy.
  </action>
  <verify>All listed satellite pages return HTTP 200. The full-chain routes are confirmed present in the satellite backend. `git log -1 --format=%an` (turion-space-demo) shows `jeet-avatar`. 36-07 WIP still dirty.</verify>
  <done>PLM-flow pages all reachable; full-chain routes confirmed; commit made (not pushed).</done>
</task>

</tasks>

<verification>
- `satellite-render.js` has no `PROGRAM_STAGES` literal; stages fetched from `/api/lifecycle-stages`.
- `audit-satellite-buttons.mjs` exits 0.
- All ~11 satellite PLM pages return 200.
- Commit under `jeet-avatar <jm@techcloudpro.com>`.
</verification>

<success_criteria>
- The satellite frontend is hardcode-free per the PERMANENT rule (only domain constants remain).
- The Phase-33 PLM flow is confirmed still wired (pages reachable, routes present).
</success_criteria>

<output>
After completion, create `.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-03-SUMMARY.md` — include which approach (a/b) was chosen, the field-shape mapping, and the PLM-flow spot-check results (any deferred items).
</output>
