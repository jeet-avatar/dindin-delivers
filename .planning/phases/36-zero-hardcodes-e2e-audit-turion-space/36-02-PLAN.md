---
phase: 36-zero-hardcodes-e2e-audit-turion-space
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/turion-space-demo/backend/src/routes/lookups.ts
  - /Users/jeet/turion-space-demo/backend/src/app.ts
  - /Users/jeet/turion-space-demo/scripts/generate-turion-config.sh
  - /Users/jeet/turion-space-demo/deploy-frontend.sh
  - /Users/jeet/turion-space-demo/.gitignore
  - /Users/jeet/turion-space-demo/data-loader.js
  - /Users/jeet/turion-space-demo/data-loader-sf.js
  - /Users/jeet/turion-space-demo/sales-new-account.html
  - /Users/jeet/turion-space-demo/sales-new-contact.html
  - /Users/jeet/turion-space-demo/sales-new-opportunity.html
  - /Users/jeet/turion-space-demo/sales-new-quote.html
  - /Users/jeet/turion-space-demo/sales-new-contract.html
  - /Users/jeet/turion-space-demo/sales-new-case.html
  - /Users/jeet/turion-space-demo/sales-new-cdrl.html
  - /Users/jeet/turion-space-demo/sales-new-activity.html
  - /Users/jeet/turion-space-demo/sales-new-order.html
  - /Users/jeet/turion-space-demo/netsuite-new-item.html
  - /Users/jeet/turion-space-demo/netsuite-new-po.html
  - /Users/jeet/turion-space-demo/netsuite-new-project.html
  - /Users/jeet/turion-space-demo/netsuite-new-vendor.html
autonomous: true
requirements: [LookupEndpoints]
must_haves:
  truths:
    - "Each ERP module has a GET /api/{arena,netsuite,salesforce,mes}/lookups endpoint returning the small enum lists its forms' dropdowns need"
    - "GET /api/data/all is confirmed to return every window.<NAME>_DATA global the frontend reads (gap-list produced; any missing key added to the endpoint)"
    - "A single source of truth for the ERP APIGW base URL exists: window.TURION_CONFIG.API_BASE from a generated turion-config.js (gitignored, generated at deploy)"
    - "Every previously-copy-pasted `const API_BASE = 'https://lo254mvukl...'` (≈12 files incl. data-loader.js) now reads window.TURION_CONFIG.API_BASE"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/backend/src/routes/lookups.ts"
      provides: "GET /api/arena/lookups, /api/netsuite/lookups, /api/salesforce/lookups, /api/mes/lookups"
      exports: ["default"]
    - path: "/Users/jeet/turion-space-demo/scripts/generate-turion-config.sh"
      provides: "Emits turion-config.js with window.TURION_CONFIG.API_BASE"
    - path: "/Users/jeet/turion-space-demo/deploy-frontend.sh"
      provides: "Calls generate-turion-config.sh (alongside generate-satellite-config.sh) before s3 sync"
      contains: "generate-turion-config"
  key_links:
    - from: "ERP frontend forms (sales-new-*.html, netsuite-new-*.html, data-loader.js)"
      to: "window.TURION_CONFIG.API_BASE"
      via: "replace local const API_BASE"
      pattern: "TURION_CONFIG"
    - from: "ERP module forms' dropdowns (consumed in plans 36-04/36-05)"
      to: "GET /api/{arena,netsuite,salesforce,mes}/lookups"
      via: "fetch"
      pattern: "/lookups"
---

<objective>
Two things, both prerequisites for the W2 ERP de-hardcode plans:
(1) Add small per-module lookup endpoints to the ERP backend (`turion-space-demo/backend/`) so the forms can fetch their dropdown options instead of owning hardcoded `<option>` lists; confirm `GET /api/data/all` is complete (RESEARCH asserts it is, but verify per-page before W2 deletes any `*-data.js`).
(2) Kill the copy-pasted `const API_BASE = 'https://lo254mvukl...'` (≈12 files) by introducing a generated `turion-config.js` (gitignored, generated at deploy — exactly like `satellite-config.js`), exposing `window.TURION_CONFIG.API_BASE`, wired into `deploy-frontend.sh`.

Purpose: Extend the PERMANENT "zero hardcoding" rule to the ERP-demo side — one source of truth for the API URL, and DB-derivable dropdowns served by an API.
Output: `backend/src/routes/lookups.ts` (4 routes) mounted in `app.ts`; `scripts/generate-turion-config.sh`; `deploy-frontend.sh` updated; ≈12 frontend files switched to `window.TURION_CONFIG.API_BASE`; `turion-config.js` added to `.gitignore`. Commit-not-pushed in `turion-space-demo` (plan 36-09 owns deploy + Lambda redeploy). Touch ONLY this plan's named files when committing — the repo has long-standing dirty WIP owned by plan 36-07.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-RESEARCH.md
@/Users/jeet/turion-space-demo/backend/src/app.ts
@/Users/jeet/turion-space-demo/scripts/generate-satellite-config.sh
@/Users/jeet/turion-space-demo/deploy-frontend.sh
@/Users/jeet/turion-space-demo/data-loader.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify /api/data/all completeness + add per-module lookup endpoints to the ERP backend</name>
  <files>/Users/jeet/turion-space-demo/backend/src/routes/lookups.ts, /Users/jeet/turion-space-demo/backend/src/app.ts</files>
  <action>
First, in `/Users/jeet/turion-space-demo/`, grep every `*.html` (excluding `satellite/`) for `window.<NAME>` and bare uppercase `<NAME>_DATA`/`<NAME>_CONFIG`/`<NAME>_INTEGRATIONS` usages; collect the full set of globals the pages read. Open `backend/src/app.ts` and read the `GET /api/data/all` (and `/api/data/sf`, `/api/data/ns`) handler — list every key it returns. Diff: any global a page reads that the loader does NOT set → record it; if such a gap exists, ADD that table to the `/api/data/all` SELECT list (it's a `SELECT id, source_data FROM turion.<table>` pattern). Write the diff into the SUMMARY.
Then create `backend/src/routes/lookups.ts` (Express Router, idioms matching the existing routers — hardened catch, no `err.message` leak, JSON responses) exporting routes that get mounted as `/api/arena/lookups`, `/api/netsuite/lookups`, `/api/salesforce/lookups`, `/api/mes/lookups`. Each returns the small enum lists that module's `*-new-*.html` forms hardcode today (per RESEARCH: Arena → NCR severities, doc types, ECO dispositions; NetSuite → item types, project types, vendor terms; Salesforce → opportunity stages, case priorities; MES → stage statuses). Derive via `SELECT DISTINCT source_data->>'...'` where a real column exists, or hold the canonical list server-side (server-side canonical is acceptable — the point is the frontend doesn't own it). Mount in `app.ts` (either a single `app.use('/api', lookupsRouter)` with prefixed paths, or fold each into the existing per-module router — pick whichever matches the codebase's mount style). Run `cd /Users/jeet/turion-space-demo/backend && npm run build` (rebuilds `dist/`) so `dist/` stays consistent — but do NOT commit `dist/` here if the WIP plan 36-07 also touches `dist/`; instead leave `dist/` rebuilt and let 36-07's commit (or 36-09's deploy build) capture it... ACTUALLY: to avoid a merge headache, commit the freshly-built `dist/` for the lookups change too (named `dist/` files only — `dist/routes/lookups.js` + `dist/app.js`), since 36-07 touches different `dist/` files (`dist/routes/agents.js`, `dist/routes/notify.js`). Note this in the SUMMARY.
  </action>
  <verify>`cd /Users/jeet/turion-space-demo/backend && npm run build` succeeds. Locally start the backend (`node dist/lambda.js` won't work standalone — instead) run a quick smoke if there's a dev server script, or at minimum `npx tsc --noEmit` clean. Confirm `app.ts` mounts `/api/arena/lookups` etc. (grep). The `/api/data/all` gap-list is in the SUMMARY (empty or with the added tables noted).</verify>
  <done>4 lookup routes exist + mounted; `/api/data/all` proven complete (or completed); tsc/build green.</done>
</task>

<task type="auto">
  <name>Task 2: Add generate-turion-config.sh + wire into deploy-frontend.sh + .gitignore</name>
  <files>/Users/jeet/turion-space-demo/scripts/generate-turion-config.sh, /Users/jeet/turion-space-demo/deploy-frontend.sh, /Users/jeet/turion-space-demo/.gitignore</files>
  <action>
Read `scripts/generate-satellite-config.sh` to copy its style. Create `scripts/generate-turion-config.sh` that writes `/Users/jeet/turion-space-demo/turion-config.js` containing:
```
window.TURION_CONFIG = { API_BASE: 'https://lo254mvukl.execute-api.us-east-1.amazonaws.com' };
```
(The ERP APIGW base — confirm the exact host from `data-loader.js`'s current `const API_BASE`. The URL is public/non-secret, so a literal in the generator script is fine — the win is ONE source of truth.) Make the script executable (`chmod +x`). Edit `deploy-frontend.sh` to call `scripts/generate-turion-config.sh` right next to where it calls `scripts/generate-satellite-config.sh` (before the `aws s3 sync`). Add `turion-config.js` to `.gitignore` (next to the existing `satellite-config.js` ignore entry). Add a `turion-config.js` 404-safe note: pages will `<script src="/turion-config.js">` (added in W2); locally-served pages without the generated file will get `window.TURION_CONFIG` undefined — so the W2 plans must defensively `(window.TURION_CONFIG && window.TURION_CONFIG.API_BASE) || 'https://lo254mvukl...'` as a last-resort literal fallback. Note this contract in the SUMMARY for 36-04/36-05.
  </action>
  <verify>`bash scripts/generate-turion-config.sh && cat turion-config.js` shows the `window.TURION_CONFIG` assignment with the correct host. `grep generate-turion-config deploy-frontend.sh` matches. `grep turion-config.js .gitignore` matches. `git status --short` does NOT show `turion-config.js` as untracked-after-ignore (it's ignored).</verify>
  <done>Generator script exists + executable; deploy script calls it; `turion-config.js` is gitignored.</done>
</task>

<task type="auto">
  <name>Task 3: Replace every copy-pasted `const API_BASE` with window.TURION_CONFIG.API_BASE + commit</name>
  <files>/Users/jeet/turion-space-demo/data-loader.js, /Users/jeet/turion-space-demo/data-loader-sf.js, /Users/jeet/turion-space-demo/sales-new-account.html, /Users/jeet/turion-space-demo/sales-new-contact.html, /Users/jeet/turion-space-demo/sales-new-opportunity.html, /Users/jeet/turion-space-demo/sales-new-quote.html, /Users/jeet/turion-space-demo/sales-new-contract.html, /Users/jeet/turion-space-demo/sales-new-case.html, /Users/jeet/turion-space-demo/sales-new-cdrl.html, /Users/jeet/turion-space-demo/sales-new-activity.html, /Users/jeet/turion-space-demo/sales-new-order.html, /Users/jeet/turion-space-demo/netsuite-new-item.html, /Users/jeet/turion-space-demo/netsuite-new-po.html, /Users/jeet/turion-space-demo/netsuite-new-project.html, /Users/jeet/turion-space-demo/netsuite-new-vendor.html</files>
  <action>
`grep -rln "const API_BASE" /Users/jeet/turion-space-demo --include='*.html' --include='*.js'` (exclude `satellite/`, `backend/`, `node_modules/`) to get the AUTHORITATIVE file list — the list above is from RESEARCH and may be ±2. For each file: ensure `<script src="/turion-config.js"></script>` is included BEFORE the script block that uses `API_BASE` (for `data-loader.js`/`data-loader-sf.js`, the page that includes those must also include `turion-config.js` first — add the `<script src="/turion-config.js">` tag to every page that includes `data-loader.js` if not already; grep `data-loader.js` includes to find them). Replace `const API_BASE = 'https://lo254mvukl...';` with `const API_BASE = (window.TURION_CONFIG && window.TURION_CONFIG.API_BASE) || 'https://lo254mvukl.execute-api.us-east-1.amazonaws.com';` (keep the literal as a last-resort fallback so a missing generated file doesn't blank the demo). Do NOT change any other behavior. `node --check` each `.js`; for HTML pages, eyeball the inline `<script>` blocks (or extract+`node --check` if practical).
Then commit in `turion-space-demo` with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit` — `git add` ONLY this plan's named files (the backend `lookups.ts`/`app.ts`/`dist/routes/lookups.js`/`dist/app.js` from Task 1, the `scripts/`/`deploy-frontend.sh`/`.gitignore` from Task 2, and the frontend files from this task). Do NOT `git add -A` — the repo has dirty WIP (`about-this-demo.html`, `agent-sales-cash.html`, `dashboard-cio.html`, `backend/src/routes/agents.ts`, `backend/src/routes/notify.ts`, etc.) owned by plan 36-07; do not let this commit swallow it. Do NOT push, do NOT run `build-and-push.sh` or `deploy-frontend.sh` (plan 36-09 owns deploy).
  </action>
  <verify>`grep -rln "const API_BASE = 'https://lo254mvukl" --include='*.html' --include='*.js' /Users/jeet/turion-space-demo | grep -v node_modules` returns ZERO files where the literal is the ONLY definition (every remaining occurrence is the `||` fallback). `git log -1 --format=%an` (turion-space-demo) shows `jeet-avatar`. `git status --short` still shows the 36-07-owned WIP as dirty (this commit didn't touch it).</verify>
  <done>Single source of truth for the ERP API base; all ≈12 files switched; commit made (not pushed); WIP untouched.</done>
</task>

</tasks>

<verification>
- 4 `/api/{arena,netsuite,salesforce,mes}/lookups` routes mounted + return enum lists.
- `/api/data/all` completeness verified (gap-list in SUMMARY; any gap closed).
- `turion-config.js` generated by `scripts/generate-turion-config.sh`, gitignored, wired into `deploy-frontend.sh`.
- No remaining bare `const API_BASE = 'https://lo254mvukl...'` (only `||`-fallback occurrences).
- `npm run build` + `npx tsc --noEmit` green in `backend/`.
- Commit made under `jeet-avatar <jm@techcloudpro.com>`; 36-07 WIP untouched.
</verification>

<success_criteria>
- The ERP backend serves every dropdown enum the W2 plans will need.
- One source of truth for the ERP APIGW URL.
- `/api/data/all` proven sufficient to replace the static `*-data.js` fallback (so W2 can delete those files safely).
</success_criteria>

<output>
After completion, create `.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-02-SUMMARY.md` — MUST include: the `/api/data/all` completeness diff, the exact list of files switched to `window.TURION_CONFIG.API_BASE`, and the defensive-fallback contract for 36-04/36-05.
</output>
