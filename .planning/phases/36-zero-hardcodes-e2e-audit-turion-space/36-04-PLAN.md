---
phase: 36-zero-hardcodes-e2e-audit-turion-space
plan: 04
type: execute
wave: 2
depends_on: ["36-02"]
files_modified:
  - /Users/jeet/turion-space-demo/salesforce-account.html
  - /Users/jeet/turion-space-demo/netsuite-*.html
  - /Users/jeet/turion-space-demo/sales-new-*.html
  - /Users/jeet/turion-space-demo/netsuite-new-*.html
  - /Users/jeet/turion-space-demo/enterprise-data.js
  - /Users/jeet/turion-space-demo/coa-data.js
  - /Users/jeet/turion-space-demo/fpa-data.js
  - /Users/jeet/turion-space-demo/evms-data.js
  - /Users/jeet/turion-space-demo/arm-data.js
  - /Users/jeet/turion-space-demo/setup-data.js
autonomous: true
requirements: [NoFrontendHardcodes, SalesforceE2E, NetSuiteE2E]
must_haves:
  truths:
    - "No Salesforce/NetSuite ERP page <script>-includes a static *-data.js fallback file (enterprise-data.js, coa-data.js, fpa-data.js, evms-data.js, arm-data.js, setup-data.js)"
    - "Those pages render fully from GET /api/data/all (data-loader.js), gating render on the turion-data-ready event"
    - "data-loader.js's error path shows a visible 'data unavailable — retry' state, NOT a silent fall back to stale static data"
    - "The orphaned *-data.js files are deleted from the repo"
    - "Salesforce/NetSuite 'new' form dropdowns are populated from GET /api/salesforce/lookups / GET /api/netsuite/lookups (not hardcoded <option> lists)"
    - "Every Salesforce/NetSuite page still loads with real data (HEAD 200, no *-data.js 404 in the page source)"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/data-loader.js"
      provides: "Fetch /api/data/all; on error show retry banner (no stale fallback)"
      contains: "turion-data-ready"
  key_links:
    - from: "Salesforce/NetSuite view + new pages"
      to: "GET /api/data/all and GET /api/{salesforce,netsuite}/lookups"
      via: "data-loader.js + form fetch on load"
      pattern: "data/all|/lookups"
---

<objective>
De-hardcode the Salesforce CRM + NetSuite finance pages: (1) remove the `<script src="*-data.js">` static-fallback includes (`enterprise-data.js`, `coa-data.js`, `fpa-data.js`, `evms-data.js`, `arm-data.js`, `setup-data.js`) from every SF/NS page; (2) make those pages render fully from `GET /api/data/all` (via `data-loader.js`), gating render on the `turion-data-ready` event; (3) change `data-loader.js`'s catch path to show a visible retry banner instead of silently falling back to stale static data; (4) delete the orphaned `*-data.js` files; (5) populate the `sales-new-*.html` / `netsuite-new-*.html` form dropdowns from `GET /api/salesforce/lookups` / `GET /api/netsuite/lookups` instead of hardcoded `<option>` lists.

Purpose: The static `*-data.js` files ARE the frontend hardcodes the NoFrontendHardcodes requirement targets — they're a frozen copy of the `turion` schema. Killing them (after confirming `/api/data/all` is complete — done in plan 36-02) is the bulk of the ERP de-hardcode work.
Output: SF/NS pages purely API-driven; orphaned data files deleted; commit-not-pushed in `turion-space-demo` (plan 36-09 owns deploy). Touch ONLY this plan's named files/pages when committing — Arena/MES pages are plan 36-05's; the WIP HTMLs are plan 36-07's.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-RESEARCH.md
@/Users/jeet/turion-space-demo/data-loader.js
@/Users/jeet/turion-space-demo/data-loader-sf.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden data-loader.js (visible retry on error, no stale fallback) + identify the SF/NS page set</name>
  <files>/Users/jeet/turion-space-demo/data-loader.js, /Users/jeet/turion-space-demo/data-loader-sf.js</files>
  <action>
Read `data-loader.js` + `data-loader-sf.js`. Today on fetch failure they log + set `__TURION_DATA_SOURCE__ = 'static-fallback'` and the page's already-included `*-data.js` has defined the globals. Change the catch path so it: (a) does NOT silently rely on stale static data; (b) shows a visible, dismissible-but-prominent banner ("Live data unavailable — [Retry]") that re-runs the fetch on click; (c) still fires `turion-data-ready` only AFTER a successful fetch (so pages gating on it don't render with empty/stale data). Keep the `window.<NAME> = ...` assignment-only-if-non-empty behavior for successful fetches. Read `36-02-SUMMARY.md` for the `/api/data/all` completeness diff and the `window.TURION_CONFIG.API_BASE` contract — `data-loader.js`'s `API_BASE` should already be `(window.TURION_CONFIG && window.TURION_CONFIG.API_BASE) || '<literal>'` from plan 36-02; if 36-02 didn't already touch `data-loader.js`'s `API_BASE`, do it here.
Then: `grep -rln "enterprise-data.js\|coa-data.js\|fpa-data.js\|evms-data.js\|arm-data.js\|setup-data.js" /Users/jeet/turion-space-demo --include='*.html' | grep -v node_modules` → the authoritative list of SF/NS (and any other) pages that include these files. For EACH such page, also `grep` its `<script>`s + inline JS for which `window.<NAME>` globals it actually reads, and cross-check those are in `/api/data/all`'s output (per 36-02's diff) — if any page reads a global the loader doesn't set, STOP and flag it (the loader/endpoint needs that key first; coordinate via SUMMARY — but per 36-02 this should be already-complete).
  </action>
  <verify>`data-loader.js` catch path shows a retry banner (grep for the banner-creating code). `node --check data-loader.js data-loader-sf.js` clean. The page list (pages including the 6 SF/NS data files) is in the SUMMARY; the per-page global cross-check is done.</verify>
  <done>`data-loader.js` fails loud, not stale; the SF/NS page set is enumerated + cross-checked.</done>
</task>

<task type="auto">
  <name>Task 2: Remove *-data.js includes from SF/NS pages + wire form dropdowns to /lookups + delete the orphaned data files</name>
  <files>/Users/jeet/turion-space-demo/salesforce-account.html, /Users/jeet/turion-space-demo/netsuite-*.html, /Users/jeet/turion-space-demo/sales-new-*.html, /Users/jeet/turion-space-demo/netsuite-new-*.html</files>
  <action>
For every SF/NS page identified in Task 1:
- Remove the `<script src="enterprise-data.js"></script>` (and `coa-data.js`/`fpa-data.js`/`evms-data.js`/`arm-data.js`/`setup-data.js`) tags.
- Ensure `<script src="/turion-config.js"></script>` is included before `data-loader.js` (per 36-02), and `data-loader.js` (or `data-loader-sf.js`) is included.
- Ensure the page's render code runs AFTER `turion-data-ready` (if a page renders synchronously at parse time assuming the globals are already set, refactor it to listen for `turion-data-ready` first — check each page; many already do via `data-loader.js`'s event).
- For `sales-new-*.html` (account/contact/opportunity/quote/contract/case/cdrl/activity/order) + `netsuite-new-*.html` (item/po/project/vendor): replace any hardcoded `<option>` lists for DB-derivable enums (opportunity stage, case priority, item type, project type, vendor terms, etc. — per 36-02's lookup endpoints) with a `fetch` on page load to `GET /api/salesforce/lookups` / `GET /api/netsuite/lookups` (use `API_BASE` = `window.TURION_CONFIG.API_BASE`), then populate the `<select>` from the response. Leave genuine 2-value domain enums inline (note which). Keep the existing form POST behavior (`POST /api/netsuite/...` etc.) unchanged.
- Delete the now-orphaned files: `git rm enterprise-data.js coa-data.js fpa-data.js evms-data.js arm-data.js setup-data.js` — BUT FIRST `grep -rln` each one across ALL `*.html` (incl. Arena/MES pages owned by 36-05) — if an Arena/MES page still includes one of these, do NOT delete it yet; instead flag it in the SUMMARY so 36-05 removes its include and the file gets deleted there (or coordinate: 36-04 deletes only the files exclusively used by SF/NS pages, 36-05 deletes the rest). Pragmatically: `enterprise-data.js`/`coa-data.js`/`fpa-data.js`/`evms-data.js`/`arm-data.js`/`setup-data.js` are the SF/NS set per RESEARCH; `bom-data.js`/`qms-data.js`/`arena-doc-data.js`/`mes-data.js`/`integration-*-data.js` are 36-05's — so 36-04 deletes its 6, 36-05 deletes its set, no overlap expected.
`node --check` any modified standalone `.js`; eyeball each modified HTML's inline `<script>` (or extract+`node --check`).
  </action>
  <verify>`grep -rln "enterprise-data.js\|coa-data.js\|fpa-data.js\|evms-data.js\|arm-data.js\|setup-data.js" --include='*.html' /Users/jeet/turion-space-demo | grep -v node_modules` → ZERO matches. `ls enterprise-data.js coa-data.js fpa-data.js evms-data.js arm-data.js setup-data.js` → all "No such file". `grep -rn "/api/salesforce/lookups\|/api/netsuite/lookups" sales-new-*.html netsuite-new-*.html` → matches in the relevant forms. `node --check data-loader.js`.</verify>
  <done>SF/NS pages no longer include any static-data file; form dropdowns fetch from `/lookups`; the 6 orphaned files deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-check SF/NS pages locally + commit</name>
  <files>(read-only smoke; commit the modified/deleted SF/NS files + data-loader.js)</files>
  <action>
Smoke: serve `/Users/jeet/turion-space-demo` locally (`python3 -m http.server 8765` in that dir, `run_in_background: true`) and `curl -s http://localhost:8765/salesforce-account.html` etc. — confirm the page HTML no longer references any deleted `*-data.js` (no `<script src="enterprise-data.js">` in the served bytes). Kill the server. (Full live-data E2E is plan 36-09's job against prod; this is just "the page doesn't 404 on a deleted include".)
Commit in `turion-space-demo` with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit` — `git add` ONLY this plan's named files: the modified `salesforce-account.html` / `netsuite-*.html` / `sales-new-*.html` / `netsuite-new-*.html`, the modified `data-loader.js`/`data-loader-sf.js`, and `git rm` the 6 deleted `*-data.js` files. Do NOT `git add -A` (Arena/MES pages are 36-05's; WIP HTMLs are 36-07's). Do NOT push, do NOT deploy.
  </action>
  <verify>Served page HTML contains no reference to a deleted `*-data.js`. `git status --short` shows the 6 data files as deleted (`D`) staged in the commit. `git log -1 --format=%an` shows `jeet-avatar`. 36-07 WIP + Arena/MES pages still dirty/untouched.</verify>
  <done>SF/NS pages load without broken includes; commit made (not pushed).</done>
</task>

</tasks>

<verification>
- No SF/NS page includes any of the 6 static-data files; they're deleted from the repo.
- `data-loader.js` shows a retry banner on fetch failure (no stale fallback).
- `sales-new-*.html` / `netsuite-new-*.html` dropdowns fetch from `/api/{salesforce,netsuite}/lookups`.
- Local smoke: pages don't reference deleted includes.
- Commit under `jeet-avatar <jm@techcloudpro.com>`; Arena/MES pages + WIP untouched.
</verification>

<success_criteria>
- Salesforce + NetSuite pages are 100% API-driven (no hardcoded DB-derivable data, no static fallback files).
- Live-data verification (real `/api/data/all` payload rendering) deferred to plan 36-09's deploy walk.
</success_criteria>

<output>
After completion, create `.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-04-SUMMARY.md` — include the page list, which dropdowns were wired to `/lookups`, which 2-value enums stayed inline, and the deleted-files list.
</output>
