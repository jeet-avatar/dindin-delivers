---
phase: 36-zero-hardcodes-e2e-audit-turion-space
plan: 08
type: execute
wave: 4
depends_on: ["36-03", "36-04", "36-05", "36-06", "36-07"]
files_modified:
  - /Users/jeet/turion-space-demo/scripts/audit-erp-buttons.mjs
  - /Users/jeet/turion-space-demo/package.json
  - /Users/jeet/turion-space-demo/*.html
autonomous: true
requirements: [ButtonAuditCleanBothFrontends]
must_haves:
  truths:
    - "A new script (scripts/audit-erp-buttons.mjs) scans the ERP frontend (turion-space-demo/*.html, excluding satellite/) for inline onclick handlers calling undefined functions AND for API calls (fetch via API_BASE / window.TURION_CONFIG.API_BASE) targeting routes that don't exist in the ERP backend"
    - "The route allowlist is DERIVED from the ERP backend (parsing turion-space-demo/backend/src/app.ts mount tree + backend/src/routes/*.ts route definitions) — never hand-maintained; fails closed on ambiguity"
    - "It treats ns-actions.js 'More Actions' confirm:/tab:/url: menu items as valid (NOT violations) — only flags onclick→undefined-function and fetch→nonexistent-route"
    - "npm run audit-buttons runs BOTH audits (the existing satellite one + the new ERP one); both exit 0 / 0 violations"
    - "Any inline onclick→undefined-function or dangling-endpoint the ERP audit surfaces is FIXED (inline onclick → addEventListener; dangling endpoint → corrected route or removed dead call)"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/scripts/audit-erp-buttons.mjs"
      provides: "ERP-frontend button/endpoint audit, allowlist derived from the ERP backend"
      min_lines: 60
    - path: "/Users/jeet/turion-space-demo/package.json"
      provides: "audit-buttons script runs both audits"
      contains: "audit-erp-buttons"
  key_links:
    - from: "scripts/audit-erp-buttons.mjs"
      to: "turion-space-demo/backend/src/app.ts + backend/src/routes/*.ts"
      via: "parse route definitions for the allowlist"
      pattern: "app\\.use|r\\.(get|post|patch|delete)"
---

<objective>
Get the Phase-29-style button audit to 0 violations on BOTH frontends. Today the audit (`turion-space-demo/scripts/audit-satellite-buttons.mjs`, wrapping `turion-satellite/backend/scripts/audit-satellite-buttons.mjs`) only scans `satellite/*.html` for `satelliteApi.{get,post,patch,put,del,delete}(` against the satellite backend's route allowlist. The ERP pages (`turion-space-demo/*.html` minus `satellite/`) are NOT covered. This plan: add a SIBLING script `scripts/audit-erp-buttons.mjs` that (a) builds a route allowlist by parsing the ERP backend's `app.ts` mount tree + `backend/src/routes/*.ts` route defs (same fail-closed posture as the satellite one — never hand-maintained), (b) scans the ERP `*.html` for inline `onclick="..."` handlers calling undefined functions and for API calls (`fetch(`${API_BASE}/api/...`)` / `window.TURION_CONFIG.API_BASE + '/api/...'`) hitting routes not in the allowlist, (c) treats `ns-actions.js` `confirm:`/`tab:`/`url:` menu items as VALID (intentional demo simulations — only flag `onclick`→undefined-function and `fetch`→nonexistent-route). Wire `npm run audit-buttons` to run BOTH. Fix anything either audit surfaces.

Purpose: ButtonAuditCleanBothFrontends — no dead buttons, no broken links, no dangling endpoints on either app.
Output: `scripts/audit-erp-buttons.mjs`; `package.json` updated; any fixes to ERP `*.html` (inline `onclick` → `addEventListener`, dangling endpoints corrected); both audits exit 0. Commit-not-pushed in `turion-space-demo` (plan 36-09 owns deploy). Touch ONLY this plan's named files + any `*.html` you fix; do NOT touch the 36-07-owned `backend/*` / WIP HTMLs unless one of THEM has a violation (if so, coordinate — but 36-07 should have left them clean).
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-RESEARCH.md
@/Users/jeet/turion-space-demo/scripts/audit-satellite-buttons.mjs
@/Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs
@/Users/jeet/turion-space-demo/backend/src/app.ts
@/Users/jeet/turion-space-demo/ns-actions.js
@/Users/jeet/turion-space-demo/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/audit-erp-buttons.mjs (allowlist from the ERP backend; scan the ERP frontend)</name>
  <files>/Users/jeet/turion-space-demo/scripts/audit-erp-buttons.mjs, /Users/jeet/turion-space-demo/package.json</files>
  <action>
Read the satellite audit (`turion-satellite/backend/scripts/audit-satellite-buttons.mjs` + the `turion-space-demo/scripts/audit-satellite-buttons.mjs` wrapper) to mirror its structure (allowlist-derivation, fail-closed-on-ambiguity, the `event.preventDefault()`/`event.stopPropagation()` whitelist, the exit-code/violations-count output). Write `scripts/audit-erp-buttons.mjs`:
- **Allowlist:** parse `/Users/jeet/turion-space-demo/backend/src/app.ts` for `app.use('/api/<prefix>', <router>)` mounts, then parse each `backend/src/routes/*.ts` for `r.get/post/patch/put/delete('<path>'...)` (and any inline `app.get/post/...` in `app.ts` itself — e.g. `/api/data/all`, `/api/health`). Compose `<prefix><path>` into the allowlist set. Also collect the generic-CRUD pattern routes (the ERP backend has `GET/POST/PATCH /api/<module>/<entity>[/:id]` generic handlers — those accept ANY `<entity>` segment, so the audit must treat `/api/<module>/<knownentity>/...` as valid via a parameterized match, OR enumerate the entity names the routers reference). Fail closed: if a path can't be parsed, count it as a violation (don't silently allow).
- **Scan:** glob `turion-space-demo/*.html` EXCLUDING `satellite/` and EXCLUDING `node_modules/`. For each: (1) extract `onclick="<expr>"` attributes → resolve the called function name → flag if that function isn't defined anywhere in the page's inline `<script>`s or the included `*.js` files (parse the `<script src=...>` list; for `ns-actions.js`/`ns-editable.js`/`ns-menu.js`/`ns-tabs.js`/`ns-toast.js`/`live-badge.js`/`data-loader.js`/`ns-po-modal.js` etc. read those files for `function X` / `X = function` / `const X = ` definitions). (2) extract API calls: `fetch(\`${API_BASE}/api/...\`)`, `fetch(API_BASE + '/api/...')`, `fetch(\`${window.TURION_CONFIG.API_BASE}/api/...\`)`, `fetch(window.TURION_CONFIG.API_BASE + '/api/...')` → normalize the path → flag if not in the allowlist (with parameterized-segment matching). (3) `ns-actions.js` menu items: a `confirm:`/`tab:`/`url:` menu entry is NOT an API call and NOT an `onclick`→fn — treat as valid; only the `onclick` that OPENS the menu (and that function IS defined in `ns-actions.js`) matters. Output `pages:N, onclick:N, apicalls:N, violations:N` and `process.exit(violations ? 1 : 0)`.
- Wire into `package.json`: change `"audit-buttons"` to run BOTH (e.g. `"audit-buttons": "node scripts/audit-satellite-buttons.mjs && node scripts/audit-erp-buttons.mjs"`), or add `"audit-buttons-erp"` and a combined `"audit-buttons-all"` — match whatever convention the repo uses; the requirement is that ONE command runs both.
`node --check scripts/audit-erp-buttons.mjs`.
  </action>
  <verify>`node /Users/jeet/turion-space-demo/scripts/audit-erp-buttons.mjs` runs and prints a `violations:N` line + exits with code N?0. `npm run audit-buttons` (in `turion-space-demo`) runs BOTH audits. `node --check scripts/audit-erp-buttons.mjs` clean. `grep -n audit-erp-buttons package.json` matches.</verify>
  <done>The ERP audit exists, derives its allowlist from the ERP backend, scans the ERP frontend, treats demo simulations as valid; wired into `npm run audit-buttons`.</done>
</task>

<task type="auto">
  <name>Task 2: Drive both audits to 0 violations (fix what they surface) + commit</name>
  <files>/Users/jeet/turion-space-demo/*.html (only the pages with violations), /Users/jeet/turion-space-demo/scripts/audit-erp-buttons.mjs</files>
  <action>
Run `npm run audit-buttons`. For each violation the ERP audit reports:
- **Inline `onclick="fn(...)"`** → convert to `addEventListener` (give the element an `id` if it lacks one; add the listener in the page's inline `<script>` after DOM ready). This matches the satellite-side rule.
- **`onclick`→undefined function** → either the function should exist (it's a real button — define it / fix the typo / point it at the right helper) or it's dead (remove the button or the handler).
- **`fetch`→nonexistent route** → fix the path to the real route (check the backend router), or if it's a genuinely-dead aspirational call (RESEARCH mentions "aspirational services never wired to backend" on the iOS side — there may be ERP analogs), remove it / replace with a `nsToast('simulated')` style stub IF it's clearly a demo simulation (but prefer fixing). For the SF "new" forms that POST to the *netsuite* router (`POST /api/netsuite/customers` from `sales-new-account.html`) — that's intentional duplication, NOT a violation; make sure the audit's allowlist includes those netsuite routes so it doesn't false-positive.
- If the audit ITSELF is wrong (false positive — e.g. it doesn't understand a generic-CRUD parameterized route, or it can't see a function defined in a `.js` file it didn't parse) → fix `audit-erp-buttons.mjs` (improve the allowlist/scan), not the page. Document any audit-logic refinement in the SUMMARY.
Also run the satellite audit (`node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs`) — it should already be 0 (plans 36-01/36-03 kept it clean); if not, fix.
Iterate until `npm run audit-buttons` → BOTH `violations:0`, exit 0. Then commit in `turion-space-demo` with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit` — `git add` only `scripts/audit-erp-buttons.mjs`, `package.json`, and the `*.html` files you fixed. Do NOT `git add -A`. Do NOT push, do NOT deploy.
  </action>
  <verify>`cd /Users/jeet/turion-space-demo && npm run audit-buttons` → both audits print `violations:0` and the command exits 0. `node /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` → `violations:0`, exit 0. `grep -c 'onclick=' <each-fixed-page>` did not increase (ideally decreased). `git log -1 --format=%an` shows `jeet-avatar`.</verify>
  <done>Both frontends pass the button audit with 0 violations; commit made (not pushed).</done>
</task>

</tasks>

<verification>
- `scripts/audit-erp-buttons.mjs` exists; allowlist derived from the ERP backend; fail-closed.
- ns-actions.js `confirm:`/`tab:`/`url:` items treated as valid.
- `npm run audit-buttons` runs both audits; both `violations:0`, exit 0.
- Satellite audit also `violations:0`.
- Inline `onclick`→`addEventListener` for any flagged page; dangling endpoints fixed.
- Commit under `jeet-avatar <jm@techcloudpro.com>`.
</verification>

<success_criteria>
- The Phase-29 button audit is 0 violations on BOTH the satellite frontend AND the ERP frontend.
- No dead buttons, no dangling endpoints, no inline-onclick on touched pages.
</success_criteria>

<output>
After completion, create `.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-08-SUMMARY.md` — include: the ERP audit's allowlist-derivation approach, the violations it surfaced + how each was fixed, any audit-logic refinements, and the final `violations:0` proof for both audits.
</output>
