---
phase: 36-zero-hardcodes-e2e-audit-turion-space
plan: 06
type: execute
wave: 3
depends_on: ["36-04", "36-05"]
files_modified:
  - /Users/jeet/turion-space-demo/ns-editable.js
  - /Users/jeet/turion-space-demo/salesforce-account.html
  - /Users/jeet/turion-space-demo/arena-qms.html
  - /Users/jeet/turion-space-demo/arena-bom.html
  - /Users/jeet/turion-space-demo/netsuite-setup.html
  - /Users/jeet/turion-space-demo/mes-shop-floor.html
  - /Users/jeet/turion-space-demo/netsuite-*.html
autonomous: true
requirements: [SalesforceE2E, NetSuiteE2E, ArenaE2E, MesE2E]
must_haves:
  truths:
    - "In-place 'Edit mode' edits on view pages (via ns-editable.js) PATCH the backend (PATCH /api/<module>/<entity>/:id), not (only) localStorage"
    - "Each editable cell knows its entity + record id + JSON path so the PATCH body is well-formed"
    - "The primary state-change buttons persist: Salesforce opp-stage advance + case-status change (PATCH /api/salesforce/...); Arena ECO/NCR/CAPA submit·approve·close (PATCH /api/arena/...); NetSuite SO→invoice→JE post (PATCH on the SO/invoice); MES stage advance (PATCH /api/mes/stages/:num)"
    - "After an edit/state-change + page reload, the new value is still there (it came from the backend, not localStorage)"
    - "The ns-actions.js 'More Actions ▾' simulated-confirm items are LEFT ALONE (explicitly out of scope — they're intentional demo fakes)"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/ns-editable.js"
      provides: "Edit mode that persists via fetch(PATCH ...) instead of localStorage"
      contains: "PATCH"
  key_links:
    - from: "ns-editable.js editable cells"
      to: "PATCH /api/<module>/<entity>/:id"
      via: "fetch with {patch:{<jsonpath>:<newval>}}"
      pattern: "PATCH.*/api/"
    - from: "view-page primary buttons (opp-stage, case-status, ECO/NCR/CAPA submit/approve/close, JE post, MES stage advance)"
      to: "PATCH /api/{salesforce,arena,netsuite,mes}/..."
      via: "fetch onclick handler"
      pattern: "PATCH"
---

<objective>
Make the ERP-demo view pages persist. Today: in-place "Edit mode" on `salesforce-account.html` / `netsuite-*.html` / `arena-qms.html` / `mes-shop-floor.html` writes to **localStorage** (via `ns-editable.js` keyed `editable:<page>:<label>:<idx>`), NOT the backend — even though the backend has `PATCH /api/<module>/<entity>/:id` for every entity. And the primary state-change buttons (advance opp stage, change case status, submit/approve/close ECO·NCR·CAPA, post a JE, advance a MES stage) likely just `nsToast()` without persisting. This plan: (1) rework `ns-editable.js` so edits `fetch(PATCH ...)` the backend (requires teaching each editable cell its entity+id+JSON-path); (2) wire the *primary* state-change buttons per module to the right `PATCH`. Be PRAGMATIC — "no dead ends on PRIMARY actions", NOT "every read-only cell is a DB write"; don't gold-plate. EXPLICITLY leave the `ns-actions.js` "More Actions ▾" `confirm:`-dialog items alone — those are intentional demo simulations.

Purpose: ArenaE2E / SalesforceE2E / NetSuiteE2E / MesE2E — every module's primary action persists.
Output: `ns-editable.js` PATCH-backed; primary buttons wired; commit-not-pushed in `turion-space-demo`. Touch ONLY this plan's named files when committing — `data-loader.js`/the static-data files are 36-04/36-05's (already committed by then); WIP HTMLs (`about-this-demo.html`/`agent-sales-cash.html`/`dashboard-cio.html`) + `backend/*` are 36-07's.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-RESEARCH.md
@/Users/jeet/turion-space-demo/ns-editable.js
@/Users/jeet/turion-space-demo/ns-actions.js
@/Users/jeet/turion-space-demo/backend/src/routes/salesforce.ts
@/Users/jeet/turion-space-demo/backend/src/routes/arena.ts
@/Users/jeet/turion-space-demo/backend/src/routes/netsuite.ts
@/Users/jeet/turion-space-demo/backend/src/routes/mes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework ns-editable.js to PATCH the backend (entity + id + JSON path)</name>
  <files>/Users/jeet/turion-space-demo/ns-editable.js, /Users/jeet/turion-space-demo/salesforce-account.html, /Users/jeet/turion-space-demo/netsuite-*.html, /Users/jeet/turion-space-demo/arena-qms.html, /Users/jeet/turion-space-demo/mes-shop-floor.html</files>
  <action>
Read `ns-editable.js` — see how it currently resolves a *label* and writes `localStorage[editable:<page>:<label>:<idx>]`. Read 2-3 view pages (`salesforce-account.html`, a `netsuite-*.html`, `arena-qms.html`) to see how editable cells are marked + how the page knows the record id (from `?id=` query param) and the entity (from the page / a data attribute). The PATCH endpoints take a record id and (per the codebase's PATCH idiom — read `salesforce.ts`/`arena.ts`/`netsuite.ts`'s PATCH handlers) either a full `source_data` merge or a `{patch:{<jsonpath>:<val>}}` body — match whatever the backend expects.
Design: add to `ns-editable.js` a mechanism for each editable element to declare its `data-edit-entity` (e.g. `customers`), `data-edit-module` (e.g. `salesforce` or `netsuite`), `data-edit-id` (the record id — usually `?id=`), and `data-edit-path` (the `source_data` JSON path, e.g. `annual_revenue` or `address.city`). On "Save" in edit mode: `fetch(API_BASE + '/api/' + module + '/' + entity + '/' + id, {method:'PATCH', headers:{'Content-Type':'application/json'}, body: JSON.stringify(<the body shape the backend wants>)})` → on success, keep the new value in the DOM + a `nsToast('Saved')`; on failure, revert + error toast. Keep a localStorage write ONLY as an optional optimistic cache if that's already load-bearing — but the source of truth is the backend.
Then, on the view pages that have editable cells (`salesforce-account.html`, `netsuite-*.html`, `arena-qms.html`, `mes-shop-floor.html`), add the `data-edit-*` attributes to the cells that SHOULD persist — pick the handful per page that matter (e.g. account fields, opportunity amount, NCR description). Don't try to make every cell editable. Use `API_BASE = window.TURION_CONFIG.API_BASE` (per 36-02). `node --check ns-editable.js`; eyeball the modified HTML.
  </action>
  <verify>`grep -n "PATCH\|fetch(" ns-editable.js` shows the PATCH call. `grep -rn "data-edit-entity\|data-edit-path" salesforce-account.html arena-qms.html netsuite-*.html mes-shop-floor.html` shows the attributes added. `node --check ns-editable.js` clean. Local smoke (serve + curl) confirms pages still parse.</verify>
  <done>`ns-editable.js` PATCHes the backend; editable cells on the key view pages carry entity+id+path; the source of truth is the DB.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the primary state-change buttons per module</name>
  <files>/Users/jeet/turion-space-demo/salesforce-account.html, /Users/jeet/turion-space-demo/arena-qms.html, /Users/jeet/turion-space-demo/netsuite-*.html, /Users/jeet/turion-space-demo/netsuite-setup.html, /Users/jeet/turion-space-demo/mes-shop-floor.html</files>
  <action>
For each module, find the *primary* state-change control(s) that today just `nsToast()` (no persist) and wire them to the right PATCH (read the backend router to confirm the route + body shape):
- **Salesforce** (`salesforce-account.html`): opportunity-stage advance → `PATCH /api/salesforce/opportunities/:id` (set the stage field); case-status change → `PATCH /api/salesforce/cases/:id`. (`salesforce.ts` has these.)
- **Arena** (`arena-qms.html`, maybe `arena-bom.html`): ECO submit/approve, NCR approve/close, CAPA close → `PATCH /api/arena/ecos/:id` / `PATCH /api/arena/ncrs/:id` / `PATCH /api/arena/capas/:id` (set the status field). (`arena.ts` has generic GET/POST/PATCH on these.)
- **NetSuite** (`netsuite-*.html`): the SO→invoice→JE flow — if there's a "post invoice" / "approve journal entry" button, wire it to `PATCH /api/netsuite/invoices/:id` / `PATCH /api/netsuite/journal_entries/:id` (set the posted/approved status). Also CONFIRM `netsuite-setup.html`'s edit path is actually wired (it uses `PATCH /api/extras/setup/:key` / `POST /api/extras/setup/:key/entry` / `DELETE .../entry/:id` per RESEARCH) — if it's not, wire it. LEAVE the financial-statement pages (BS/TB/COA) read-only — editing a balance-sheet rollup cell is meaningless.
- **MES** (`mes-shop-floor.html`): "advance stage" / "sign off" button → `PATCH /api/mes/stages/:num` (set the stage status). (`mes.ts` has this.)
Each handler: on success, update the DOM + toast + (where appropriate) reload to reflect downstream rollups; on failure, error toast. Use `API_BASE = window.TURION_CONFIG.API_BASE`. Convert any inline `onclick="..."` you touch to `addEventListener` (the audit in 36-08 enforces this — match the satellite-side rule). Do NOT touch `ns-actions.js`'s "More Actions ▾" `confirm:`/`tab:`/`url:` items — those stay as-is (intentional demo simulations). `node --check` any standalone `.js`; eyeball each HTML.
  </action>
  <verify>`grep -rn "PATCH.*opportunities\|PATCH.*cases\|PATCH.*ecos\|PATCH.*ncrs\|PATCH.*capas\|PATCH.*invoices\|PATCH.*journal_entries\|PATCH.*mes/stages" salesforce-account.html arena-qms.html netsuite-*.html mes-shop-floor.html` shows the wired calls. `netsuite-setup.html`'s edit path confirmed wired (grep for `/api/extras/setup`). No new bare `onclick=` introduced on touched elements (grep). `ns-actions.js` unchanged (`git diff --stat ns-actions.js` → empty).</verify>
  <done>The primary state-change action per module persists to the backend; "More Actions" stubs untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Local smoke + commit</name>
  <files>(read-only smoke; commit this plan's named files)</files>
  <action>
Smoke: serve `/Users/jeet/turion-space-demo` locally, `curl -s` the modified view pages — confirm they parse + still reference `data-loader.js` (not the deleted static files) + now reference `ns-editable.js`. Kill the server. (Real PATCH→DB round-trips are plan 36-09's deploy-walk job.)
Commit in `turion-space-demo` with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit` — `git add` ONLY this plan's named files: `ns-editable.js`, `salesforce-account.html`, `arena-qms.html`, `arena-bom.html` (if touched), `netsuite-*.html` (only the ones you actually changed), `netsuite-setup.html`, `mes-shop-floor.html`. Do NOT `git add -A` (WIP HTMLs + `backend/*` are 36-07's). Do NOT push, do NOT deploy.
  </action>
  <verify>Served pages parse + reference `ns-editable.js` + no deleted-file references. `git log -1 --format=%an` shows `jeet-avatar`. 36-07 WIP still dirty/untouched.</verify>
  <done>View-page persistence wired; commit made (not pushed).</done>
</task>

</tasks>

<verification>
- `ns-editable.js` PATCHes the backend; editable cells carry entity+id+path.
- Primary state-change buttons per module (SF opp-stage/case-status, Arena ECO/NCR/CAPA submit·approve·close, NetSuite invoice/JE post + setup edit, MES stage advance) wired to PATCH routes.
- `ns-actions.js` "More Actions" simulated items untouched.
- Local smoke: pages parse, reference `ns-editable.js`, no deleted-file refs.
- Commit under `jeet-avatar <jm@techcloudpro.com>`; WIP untouched.
</verification>

<success_criteria>
- Every ERP module's PRIMARY action persists to the backend (verified by PATCH-call presence; DB round-trip proof in plan 36-09).
- No gold-plating — read-only rollup cells stay read-only; demo "More Actions" simulations stay simulations.
</success_criteria>

<output>
After completion, create `.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-06-SUMMARY.md` — include: the `data-edit-*` attribute design, the per-module primary-button → PATCH-route mapping, and which cells/buttons were deliberately LEFT read-only/simulated.
</output>
