---
phase: 36-zero-hardcodes-e2e-audit-turion-space
plan: 05
type: execute
wave: 2
depends_on: ["36-02"]
files_modified:
  - /Users/jeet/turion-space-demo/arena-qms.html
  - /Users/jeet/turion-space-demo/arena-bom.html
  - /Users/jeet/turion-space-demo/arena-new-part.html
  - /Users/jeet/turion-space-demo/arena-new-eco.html
  - /Users/jeet/turion-space-demo/arena-new-ncr.html
  - /Users/jeet/turion-space-demo/arena-new-capa.html
  - /Users/jeet/turion-space-demo/arena-new-document.html
  - /Users/jeet/turion-space-demo/arena-new-audit.html
  - /Users/jeet/turion-space-demo/mes-shop-floor.html
  - /Users/jeet/turion-space-demo/integration-arena-ns.html
  - /Users/jeet/turion-space-demo/integration-mes-ns.html
  - /Users/jeet/turion-space-demo/integration-hub.html
  - /Users/jeet/turion-space-demo/bom-data.js
  - /Users/jeet/turion-space-demo/qms-data.js
  - /Users/jeet/turion-space-demo/arena-doc-data.js
  - /Users/jeet/turion-space-demo/mes-data.js
  - /Users/jeet/turion-space-demo/integration-data.js
  - /Users/jeet/turion-space-demo/integration-arena-data.js
  - /Users/jeet/turion-space-demo/integration-mes-data.js
  - /Users/jeet/turion-space-demo/integration-vendor-data.js
  - /Users/jeet/turion-space-demo/integration-bank-siem-data.js
  - /Users/jeet/turion-space-demo/backend/src/routes/mes.ts
  - /Users/jeet/turion-space-demo/backend/src/app.ts
autonomous: true
requirements: [NoFrontendHardcodes, ArenaE2E, MesE2E]
must_haves:
  truths:
    - "No Arena/MES/integration page <script>-includes a static *-data.js fallback (bom-data.js, qms-data.js, arena-doc-data.js, mes-data.js, integration-*-data.js)"
    - "Those pages render from GET /api/data/all (data-loader.js) gating on turion-data-ready"
    - "The orphaned Arena/MES/integration *-data.js files are deleted"
    - "Arena 'new' form dropdowns + mes-shop-floor controls populate from GET /api/arena/lookups / GET /api/mes/lookups"
    - "If mes-shop-floor.html needs more than GET/PATCH /api/mes/stages to render/operate (e.g. a list of stages, or per-unit data), the minimal extra MES endpoint(s) are added — but the MES domain model is NOT gold-plated; the satellite work-orders/build-steps app remains the real MES surface"
    - "Every Arena/MES/integration page still loads with real data (HEAD 200, no *-data.js 404 in source)"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/backend/src/routes/mes.ts"
      provides: "GET /api/mes/stages[/:num] + PATCH /api/mes/stages/:num (existing) + any minimal new MES read endpoint mes-shop-floor.html needs"
  key_links:
    - from: "Arena/MES/integration pages"
      to: "GET /api/data/all and GET /api/{arena,mes}/lookups"
      via: "data-loader.js + form fetch"
      pattern: "data/all|/lookups"
---

<objective>
De-hardcode the Arena PLM/QMS pages, the MES shop-floor page, and the integration-hub pages: same treatment as plan 36-04 — remove the static `*-data.js` fallback includes (`bom-data.js`, `qms-data.js`, `arena-doc-data.js`, `mes-data.js`, `integration-data.js`, `integration-arena-data.js`, `integration-mes-data.js`, `integration-vendor-data.js`, `integration-bank-siem-data.js`), render from `/api/data/all`, wire dropdowns to `/api/arena/lookups` / `/api/mes/lookups`, delete the orphaned files. Plus: if `mes-shop-floor.html` genuinely needs more than the existing `GET/PATCH /api/mes/stages[/:num]` to render/operate (RESEARCH says the MES backend is thin), add the MINIMAL extra read endpoint(s) — do NOT build a full MES domain model (the satellite `work-orders`/`build-steps` app is the real MES surface).

Purpose: NoFrontendHardcodes on the Arena/MES/integration side; finish the static-`*-data.js` removal started by 36-04 (these are the remaining files).
Output: Arena/MES/integration pages API-driven; orphaned data files deleted; minimal MES endpoint(s) if needed; commit-not-pushed in `turion-space-demo`. Touch ONLY this plan's named files when committing — SF/NS pages are 36-04's; WIP HTMLs + `backend/src/routes/agents.ts`/`notify.ts`/`dist/agents.js`/`dist/notify.js` are 36-07's. (This plan DOES touch `backend/src/routes/mes.ts` + `dist/routes/mes.js` + `backend/src/app.ts` if a new MES endpoint is added — that's disjoint from 36-07's `agents.ts`/`notify.ts` and from 36-02's `lookups.ts`, but be careful with `app.ts`: if both 36-02 and 36-05 edit `app.ts`, that's a conflict — coordinate: 36-02 mounts `/api/{arena,netsuite,salesforce,mes}/lookups`; 36-05 only adds an `app.use` for a new MES route if needed. If 36-02 already ran, rebase your `app.ts` edit on top of it. If conflict, the executor should manually merge.)
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-RESEARCH.md
@/Users/jeet/turion-space-demo/backend/src/routes/mes.ts
@/Users/jeet/turion-space-demo/data-loader.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Arena/MES/integration *-data.js includes + wire dropdowns to /api/{arena,mes}/lookups + delete orphaned files</name>
  <files>/Users/jeet/turion-space-demo/arena-qms.html, /Users/jeet/turion-space-demo/arena-bom.html, /Users/jeet/turion-space-demo/arena-new-part.html, /Users/jeet/turion-space-demo/arena-new-eco.html, /Users/jeet/turion-space-demo/arena-new-ncr.html, /Users/jeet/turion-space-demo/arena-new-capa.html, /Users/jeet/turion-space-demo/arena-new-document.html, /Users/jeet/turion-space-demo/arena-new-audit.html, /Users/jeet/turion-space-demo/mes-shop-floor.html, /Users/jeet/turion-space-demo/integration-arena-ns.html, /Users/jeet/turion-space-demo/integration-mes-ns.html, /Users/jeet/turion-space-demo/integration-hub.html</files>
  <action>
`grep -rln "bom-data.js\|qms-data.js\|arena-doc-data.js\|mes-data.js\|integration-data.js\|integration-arena-data.js\|integration-mes-data.js\|integration-vendor-data.js\|integration-bank-siem-data.js" /Users/jeet/turion-space-demo --include='*.html' | grep -v node_modules` → the authoritative page list (the list above is from RESEARCH; ±2). For each page: remove the `<script src="*-data.js">` tags; ensure `<script src="/turion-config.js">` then `data-loader.js` are included; ensure render gates on `turion-data-ready`; cross-check the `window.<NAME>` globals each page reads against `/api/data/all`'s output (per 36-02's diff) — flag any gap. For the `arena-new-*.html` forms (part/eco/ncr/capa/document/audit): replace hardcoded `<option>` lists for DB-derivable enums (NCR severity, doc type, ECO disposition, audit type, etc.) with a `fetch` on load to `GET /api/arena/lookups`. For `mes-shop-floor.html`: replace any hardcoded stage-status `<option>` lists with `GET /api/mes/lookups`; keep the existing data-render path on `STAGE_DATA` from `/api/data/all`. Keep all existing form POST behavior unchanged. Then `git rm` the orphaned files (only the ones now referenced by ZERO `*.html` — re-grep after removing includes). `node --check` any standalone `.js`; eyeball each HTML's inline `<script>`.
  </action>
  <verify>`grep -rln "bom-data.js\|qms-data.js\|arena-doc-data.js\|mes-data.js\|integration-data.js\|integration-arena-data.js\|integration-mes-data.js\|integration-vendor-data.js\|integration-bank-siem-data.js" --include='*.html' /Users/jeet/turion-space-demo | grep -v node_modules` → ZERO. The orphaned files are deleted (`ls` → no such file). `grep -rn "/api/arena/lookups\|/api/mes/lookups" arena-new-*.html mes-shop-floor.html` → matches.</verify>
  <done>Arena/MES/integration pages no longer include static-data files; dropdowns fetch from `/lookups`; orphaned files deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Add minimal MES read endpoint(s) only if mes-shop-floor.html needs them</name>
  <files>/Users/jeet/turion-space-demo/backend/src/routes/mes.ts, /Users/jeet/turion-space-demo/backend/src/app.ts</files>
  <action>
Read `mes-shop-floor.html`'s render code: what data does it need to draw the shop floor? Today it reads `STAGE_DATA` (from `/api/data/all`) + the static `mes-data.js`. After Task 1 it relies on `STAGE_DATA` only. If that's SUFFICIENT to render the page (the stage list with their states) → do NOTHING in `mes.ts`; the existing `GET /api/mes/stages` + `GET /api/mes/stages/:num` + `PATCH /api/mes/stages/:num` plus `STAGE_DATA` from `/api/data/all` cover it. If the page needs something `STAGE_DATA` doesn't provide (e.g. a flat list endpoint `GET /api/mes/stages` it can call directly, or per-unit data) → add the MINIMAL endpoint to `backend/src/routes/mes.ts` (same idioms as the existing handlers — hardened catch, JSON). Do NOT add CRUD for a whole MES domain model. If you add `app.use`/a route in `app.ts`, coordinate with 36-02's `app.ts` edit (if 36-02 already ran, rebase; if conflict, manually merge — 36-02 mounts the `/lookups` routes, this plan adds at most one MES route). After any backend change: `cd /Users/jeet/turion-space-demo/backend && npm run build && npx tsc --noEmit` — green. Note in the SUMMARY whether any new MES endpoint was added (RESEARCH's expectation: probably none — `STAGE_DATA` is enough).
  </action>
  <verify>If no backend change: SUMMARY says "no new MES endpoint needed — STAGE_DATA from /api/data/all + existing PATCH /api/mes/stages/:num suffice". If a change: `npm run build` + `npx tsc --noEmit` clean in `backend/`, and `grep` confirms the new route is mounted.</verify>
  <done>MES backend is exactly as deep as `mes-shop-floor.html` requires — no more, no less.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-check Arena/MES/integration pages + commit</name>
  <files>(read-only smoke; commit this plan's named files)</files>
  <action>
Smoke: serve `/Users/jeet/turion-space-demo` locally, `curl -s http://localhost:8765/arena-qms.html` / `arena-bom.html` / `mes-shop-floor.html` / `integration-hub.html` etc. — confirm no served-page bytes reference a deleted `*-data.js`. Kill the server.
Commit in `turion-space-demo` with `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit` — `git add` ONLY this plan's named files: the modified Arena/MES/integration `*.html`, `git rm` the orphaned `*-data.js` files, and (if changed) `backend/src/routes/mes.ts` + `backend/src/app.ts` + `backend/dist/routes/mes.js` + `backend/dist/app.js`. Do NOT `git add -A` (SF/NS pages are 36-04's; WIP HTMLs + `agents.ts`/`notify.ts`/`dist/agents.js`/`dist/notify.js` are 36-07's). If `app.ts`/`dist/app.js` was also touched by 36-02 and 36-02 already committed it, your `git add backend/src/app.ts` will include 36-02's lines too — that's fine as long as the net diff is correct (re-check `git diff --cached backend/src/app.ts`). Do NOT push, do NOT deploy.
  </action>
  <verify>Served page HTML contains no reference to a deleted `*-data.js`. `git status --short` shows the orphaned data files as deleted (`D`). `git log -1 --format=%an` shows `jeet-avatar`. SF/NS pages + 36-07 WIP still untouched.</verify>
  <done>Arena/MES/integration pages load without broken includes; commit made (not pushed).</done>
</task>

</tasks>

<verification>
- No Arena/MES/integration page includes any static-data file; they're deleted.
- Arena `new` form dropdowns + MES stage-status dropdowns fetch from `/api/{arena,mes}/lookups`.
- MES backend extended ONLY if `mes-shop-floor.html` needs it (likely not).
- Local smoke: pages don't reference deleted includes.
- Commit under `jeet-avatar <jm@techcloudpro.com>`; SF/NS pages + WIP untouched.
</verification>

<success_criteria>
- Arena + MES + integration pages are 100% API-driven (no hardcoded DB-derivable data, no static fallback files).
- The MES domain model is not gold-plated; STATE will note that MES depth lives in the satellite app.
- Live-data verification deferred to plan 36-09's deploy walk.
</success_criteria>

<output>
After completion, create `.planning/phases/36-zero-hardcodes-e2e-audit-turion-space/36-05-SUMMARY.md` — include the page list, which dropdowns were wired, whether any MES endpoint was added, and the deleted-files list.
</output>
