---
phase: 04-fix-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/p2p-platform/backend/tests/conftest.py
  - apps/web/p2p-platform/backend/tests/integration/test_ios_api_contracts.py
autonomous: true
requirements: [CI-01, CI-02, CI-03, CI-04]

must_haves:
  truths:
    - "Contract tests cover every endpoint iOS and Android apps actually call (~160 unique paths)"
    - "Each test documents which platform (iOS/Android/both) calls that endpoint"
    - "Public endpoints tested without auth, protected endpoints tested with correct role-based auth headers"
    - "Tests verify endpoint existence, HTTP method, auth requirement, and basic response shape -- not business logic"
    - "conftest.py has customer_auth_headers fixture with customer_id in JWT payload"
    - "Missing SQLite table errors are resolved by importing models_extended in conftest"
    - "Shipped old-path aliases that the TestFlight/Firebase builds still call are tested in a dedicated class"
  artifacts:
    - path: "apps/web/p2p-platform/backend/tests/conftest.py"
      provides: "test_customer and customer_auth_headers fixtures, models_extended import"
      contains: "customer_auth_headers"
    - path: "apps/web/p2p-platform/backend/tests/integration/test_ios_api_contracts.py"
      provides: "Comprehensive API contract tests for all ~160 app-called endpoints"
      min_lines: 800
  key_links:
    - from: "tests/integration/test_ios_api_contracts.py"
      to: "tests/conftest.py"
      via: "pytest fixtures (client, customer_auth_headers, driver_auth_headers, vendor_auth_headers)"
      pattern: "customer_auth_headers|driver_auth_headers|vendor_auth_headers"
    - from: "tests/conftest.py"
      to: "auth_utils.py"
      via: "JWT payload format must match require_customer/require_driver/require_vendor expectations"
      pattern: "customer_id|driver_id|vendor_id"
---

<objective>
Rewrite API contract tests to cover all ~160 endpoints that iOS and Android apps actually call, and fix conftest.py fixtures so contract tests can authenticate as customer/driver/vendor.

Purpose: The current contract test file has only 19 tests covering ~15 wrong paths. After auth hardening, every protected endpoint returns 401. The apps call ~160 unique endpoints that have zero test coverage. This plan creates comprehensive contract tests that validate every endpoint the shipped apps depend on, including alias paths that shipped TestFlight/Firebase builds still use.

Output: Rewritten `test_ios_api_contracts.py` with ~150 test methods organized by domain, updated `conftest.py` with customer fixtures and models_extended import.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fix-ci/04-RESEARCH.md
@apps/web/p2p-platform/backend/tests/conftest.py
@apps/web/p2p-platform/backend/tests/integration/test_ios_api_contracts.py
@apps/web/p2p-platform/backend/auth_utils.py
@apps/web/p2p-platform/backend/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add customer fixtures to conftest.py and import models_extended</name>
  <files>apps/web/p2p-platform/backend/tests/conftest.py</files>
  <action>
Two changes to conftest.py:

**1. Import models_extended to register all tables (fixes missing SQLite table errors):**
After the existing `from models import ...` line (line 55), add:
```python
# Import extended models to ensure all tables are registered before create_all
import models_extended
```
This ensures Base.metadata knows about Promotion, CustomerFavorite, etc. before `create_all` is called in the `test_db` fixture.

**2. Add test_customer and customer_auth_headers fixtures:**
After the existing `driver_auth_headers` fixture (around line 272), add:

```python
@pytest.fixture(scope="function")
def test_customer(db_session) -> Customer:
    """Create a test customer for contract tests"""
    customer = Customer(
        email=f"customer_{datetime.now().timestamp()}@test.com",
        password_hash=get_password_hash("CustomerPassword123!"),
        first_name="Test",
        last_name="Customer",
        phone="+14155551234",
        is_active=True,
    )
    db_session.add(customer)
    db_session.commit()
    db_session.refresh(customer)
    return customer


@pytest.fixture(scope="function")
def customer_auth_headers(test_customer) -> Dict[str, str]:
    """Get authentication headers for customer -- includes customer_id in JWT payload"""
    token = create_access_token(data={
        "sub": test_customer.email,
        "customer_id": test_customer.id
    })
    return {"Authorization": f"Bearer {token}"}
```

IMPORTANT: The Customer model uses `first_name` and `last_name` (NOT `name`) per models.py:587-588. The JWT must include `customer_id` because `require_customer` in auth_utils.py checks for it (line 97-101).
  </action>
  <verify>
Run: `cd apps/web/p2p-platform/backend && python -c "from tests.conftest import *; print('conftest imports OK')"`
Verify no import errors. Then run a quick smoke: `cd apps/web/p2p-platform/backend && python -m pytest tests/unit/test_models.py -x -q` to confirm existing tests still pass.
  </verify>
  <done>conftest.py has test_customer fixture creating a Customer with first_name/last_name/email/is_active, customer_auth_headers fixture producing JWT with customer_id claim, and models_extended import before table creation.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite test_ios_api_contracts.py with all ~160 app-called endpoints</name>
  <files>apps/web/p2p-platform/backend/tests/integration/test_ios_api_contracts.py</files>
  <action>
Replace the entire file with a comprehensive contract test covering ALL endpoints extracted from P2PAPIService.swift and DollorApiService.kt (from 04-RESEARCH.md Part 3).

**File structure -- organize by domain with 20 test classes:**

```python
"""
API Contract Tests -- Validates every endpoint iOS and Android apps call.

Extracted from:
- iOS: P2PAPIService.swift at commit 1297b663 (TestFlight builds 1088/196/164)
- Android: DollorApiService.kt + CustomerRideshareApiService.kt at commit 9c42c21d

Each test verifies:
1. Endpoint exists (not 404/405)
2. Correct auth requirement (401 without auth, non-401 with auth)
3. HTTP method accepted
4. Basic response structure (not business logic)

Platform annotations: [iOS] [Android] [Both] indicate which app(s) call each endpoint.
"""
```

**Test classes and their endpoints (use exact paths from research):**

**Class 1: TestPublicEndpoints (~10 tests)**
No auth needed. Test these without auth headers:
- GET `/health` [Both]
- GET `/api/vendors/published` [Both]
- GET `/api/public/restaurants/{vendorId}` [Both] -- use test_vendor.id
- GET `/api/promotions/active` [Both]
- GET `/api/promotions/featured` [Both]
- POST `/api/rides/estimate` [Both] -- send JSON body with pickup_latitude, pickup_longitude, dropoff_latitude, dropoff_longitude
- GET `/api/vendors/{vendorId}/menu` [Both] -- public menu view, use test_vendor.id
- GET `/api/legal/tos` [Android]
- GET `/api/legal/privacy-policy` [Android]
- GET `/api/tax/estimate/CA` [Android]

**Class 2: TestCustomerAuthContracts (~9 tests)**
Auth endpoints accepting POST with registration/login data:
- POST `/api/auth/customer/login` [Both] -- send JSON {email, password}
- POST `/api/auth/customer/register` [Both] -- send registration JSON
- POST `/api/auth/customer/google` [Both] -- send {token: "test"}
- POST `/api/customer/apple-auth` [iOS]
- PUT `/api/customer/{customerId}/profile` [iOS] -- requires customer_auth_headers
- POST `/api/customer/password-reset/request` [Both] -- send {email: "test@test.com"}
- POST `/api/customer/password-reset/confirm` [Both] -- send {token: "test", new_password: "Test1234!"}
- DELETE `/api/customers/{customerId}/delete` [Both] -- requires customer_auth_headers
- POST `/api/customer/demo-login` [Android] -- send {secret_key: "test"}

**Class 3: TestCustomerOrderContracts (~13 tests)**
All require customer_auth_headers:
- GET `/api/customer/orders` [Both]
- POST `/api/erp/orders/create` [Both] -- send order JSON
- POST `/api/erp/orders/{orderId}/confirm-payment` [iOS] -- send {payment_intent_id: "test"}
- GET `/api/customer/orders/{orderId}/track` [iOS] -- use orderId=99999
- GET `/api/customer/{customerId}/active-orders` [iOS]
- POST `/api/orders/{orderId}/tip-driver` [Both] -- send {tip_amount: 5}
- POST `/api/orders/{orderId}/cancel` [Both]
- GET `/api/orders/{orderId}/refund-status` [iOS]
- GET `/api/orders/{orderId}/modification` [iOS]
- POST `/api/orders/{orderId}/modification/respond` [iOS]
- POST `/api/orders/{orderId}/mark-unavailable` [iOS]
- POST `/api/customer/orders/{orderId}/rate-driver` [iOS] -- send {rating: 5}
- POST `/api/customer/orders/{orderId}/rate-restaurant` [iOS] -- send {rating: 5}

**Class 4: TestCustomerOrderChatContracts (~2 tests)**
Require customer_auth_headers:
- GET `/api/customer/orders/{orderId}/chat` [iOS]
- POST `/api/customer/orders/{orderId}/chat` [iOS] -- send {message: "test"}

**Class 5: TestCustomerAddressContracts (~6 tests)**
Require customer_auth_headers, use test_customer.id for userId:
- GET `/api/addresses/{userId}` [Both]
- GET `/api/addresses/{userId}/default` [Both]
- POST `/api/addresses/{userId}` [Both] -- send address JSON with street, city, state, zip_code, latitude, longitude
- PUT `/api/addresses/{userId}/{addressId}` [Both] -- use addressId=1
- DELETE `/api/addresses/{userId}/{addressId}` [Both]
- POST `/api/addresses/{userId}/{addressId}/set-default` [Both]

**Class 6: TestCustomerFavoriteContracts (~4 tests)**
Require customer_auth_headers:
- GET `/api/customer/favorites/{customerId}` [Both]
- POST `/api/customer/favorites/{customerId}/{vendorId}` [Both] -- HEAD path, use test_vendor.id
- DELETE `/api/customer/favorites/{customerId}/{vendorId}` [Both]
- GET `/api/customer/favorites/{customerId}/check/{vendorId}` [Both]

**Class 7: TestCustomerCardContracts (~4 tests)**
Require customer_auth_headers:
- GET `/api/customers/{customerId}/cards` [Both]
- POST `/api/customers/{customerId}/cards` [Both] -- send {token: "tok_test"}
- DELETE `/api/customers/{customerId}/cards/{cardId}` [Both] -- cardId=1
- POST `/api/customers/{customerId}/cards/{cardId}/default` [Both]

**Class 8: TestCustomerCartContracts (~5 tests)**
Require customer_auth_headers:
- GET `/api/cart` [Both]
- POST `/api/cart/items` [Both] -- send {menu_item_id: 1, quantity: 1}
- PUT `/api/cart/items/{itemId}` [Both] -- itemId=1, send {quantity: 2}
- DELETE `/api/cart/items/{itemId}` [Both]
- DELETE `/api/cart` [Both]

**Class 9: TestCustomerRideshareContracts (~20 tests)**
Customer rideshare endpoints -- require customer_auth_headers:
- POST `/api/rides/request` [Both] -- send ride request JSON with pickup/dropoff coords
- GET `/api/customer/rides/history` [Both]
- POST `/api/rides/request/{requestId}/cancel` [Both] -- requestId=99999
- GET `/api/rides/request/{requestId}/bids` [Both]
- GET `/api/rides/customer/{customerId}/requests` [Both]
- POST `/api/rides/bid/{bidId}/respond` [Both] -- bidId=99999, send {action: "accept"}
- POST `/api/rides/bid/{bidId}/withdraw` [iOS]
- POST `/api/rides/bid/{bidId}/driver-counter` [Both]
- POST `/api/rides/bid/{bidId}/accept-counter` [Both]
- POST `/api/rides/bid/{bidId}/reject-counter` [Both]
- GET `/api/rides/bid/{bidId}` [Both]
- POST `/api/rides/{rideId}/rate` [Both] -- send {rating: 5}
- POST `/api/rides/{rideId}/tip` [Both] -- send {tip_amount: 5}
- GET `/api/rides/request/{rideId}/receipt` [Both]
- POST `/api/rides/request/{rideId}/email-receipt` [Both]
- POST `/api/erp/rides/{rideId}/customer-negotiate` [iOS]
- POST `/api/erp/rides/{rideId}/customer-accept-fare` [iOS]
- GET `/api/erp/rides/{rideId}/negotiation-status` [Both]
- POST `/api/payments/ride/create-intent` [iOS]
- GET `/api/p2p/ride-requests/{rideRequestId}/chat` [Both]
- POST `/api/p2p/ride-requests/{rideRequestId}/chat` [Both] -- send {message: "test"}

**Class 10: TestCustomerDisputeContracts (~6 tests)**
Require customer_auth_headers:
- POST `/api/rides/dispute` [Both] -- send dispute JSON
- GET `/api/rides/customer/{customerId}/disputes` [Both]
- GET `/api/rides/dispute/{disputeId}` [Both] -- disputeId=99999
- GET `/api/rides/customer/{customerId}/recurring-rides` [Both]
- POST `/api/rides/customer/{customerId}/recurring-rides` [Both] -- send recurring ride JSON
- DELETE `/api/rides/recurring-rides/{recurringRideId}` [Both] -- recurringRideId=99999

**Class 11: TestDriverAuthContracts (~9 tests)**
Driver auth endpoints:
- POST `/api/auth/driver/login` [Both] -- send {email, password}
- POST `/api/auth/driver/register` [Both] -- send registration JSON with first_name, last_name, email, password, phone
- POST `/api/auth/driver/apple-auth` [iOS]
- POST `/api/auth/driver/google` [Android]
- POST `/api/auth/driver/refresh` [iOS] -- requires driver_auth_headers
- POST `/api/driver/password-reset/request` [Both]
- POST `/api/driver/password-reset/confirm` [Both]
- DELETE `/api/drivers/{driverId}/delete` [Both] -- requires driver_auth_headers
- POST `/api/auth/driver/demo-login` [Android] -- send {secret_key: "test"}

**Class 12: TestDriverProfileContracts (~7 tests)**
Require driver_auth_headers:
- GET `/api/erp/drivers/{driverId}` [iOS]
- PUT `/api/erp/drivers/{driverId}` [iOS] -- send profile update JSON
- GET `/api/drivers/{driverId}` [Both]
- GET `/api/drivers/{driverId}/documents` [Both]
- POST `/api/drivers/{driverId}/documents` [Both] -- send JSON with document_type, document_url
- PUT `/api/auth/driver/location` [Both] -- send {latitude: 37.7749, longitude: -122.4194}
- PUT `/api/auth/driver/online` [iOS] -- query param is_online=true

**Class 13: TestDriverDeliveryContracts (~12 tests)**
Require driver_auth_headers:
- GET `/api/erp/orders/available-for-delivery` [Both]
- POST `/api/erp/orders/{orderId}/assign-driver` [Both]
- POST `/api/erp/orders/{orderId}/picked-up` [Both]
- POST `/api/erp/orders/{orderId}/delivered` [Both]
- POST `/api/erp/orders/{orderId}/complete-delivery` [Both]
- POST `/api/erp/orders/{orderId}/delivery-photo` [Both]
- PUT `/api/erp/orders/{orderId}/unassign-driver` [Both]
- PUT `/api/erp/orders/{orderId}/driver-location` [Both] -- send {latitude: 37.7749, longitude: -122.4194}
- GET `/api/erp/orders/driver/{driverId}/active` [Both]
- GET `/api/v5/driver/{driverId}/dashboard` [Both]
- GET `/api/erp/orders/driver/{driverId}/pending` [Android]
- GET `/api/erp/driver/{driverId}/deliveries` [Android]

**Class 14: TestDriverEarningsContracts (~10 tests)**
Require driver_auth_headers:
- GET `/api/drivers/{driverId}/earnings` [Both] -- query param period=weekly
- GET `/api/drivers/{driverId}/payout-history` [Both] -- query param limit=10
- GET `/api/rides/driver/{driverId}/payout-history` [Both]
- POST `/api/drivers/{driverId}/stripe/connect` [Both]
- GET `/api/drivers/{driverId}/stripe/onboarding-link` [Both]
- GET `/api/drivers/{driverId}/stripe/status` [Both]
- POST `/api/drivers/{driverId}/stripe/dashboard-link` [Both]
- GET `/api/drivers/{driverId}/balance` [Android]
- POST `/api/drivers/{driverId}/bank-account` [Android]
- POST `/api/drivers/{driverId}/payouts` [Android]

**Class 15: TestDriverRideshareContracts (~9 tests)**
Driver-side rideshare endpoints -- require driver_auth_headers:
- GET `/api/erp/rides/available` [Both]
- POST `/api/erp/rides/{rideId}/accept` [Both]
- POST `/api/erp/rides/{rideId}/picked-up` [Both]
- POST `/api/erp/rides/{rideId}/negotiate` [iOS]
- POST `/api/erp/rides/{rideId}/accept-fare` [iOS]
- GET `/api/erp/rides/{rideId}/track` [iOS]
- GET `/api/erp/rides/{rideId}/status` [iOS]
- POST `/api/rides/request/{rideRequestId}/arrived` [Both]
- POST `/api/rides/request/{rideRequestId}/start` [Both]
- POST `/api/rides/request/{rideRequestId}/complete` [Both]
- POST `/api/rides/request/{rideRequestId}/no-show` [Both]
- POST `/api/rides/request/{rideRequestId}/driver-cancel` [Both]
- POST `/api/rides/request/{rideRequestId}/rate-passenger` [Both]
- GET `/api/driver/bids` [Android]

**Class 16: TestVendorAuthContracts (~8 tests)**
Vendor auth endpoints:
- POST `/api/auth/vendor/login` [Both] -- send {email, password} or form data
- POST `/api/auth/vendor/register` [Both]
- POST `/api/auth/vendor/google-auth` [Both]
- POST `/api/auth/vendor/apple-auth` [iOS]
- POST `/api/vendors/public` [Both] -- public vendor registration
- POST `/api/vendor/password-reset/request` [Both]
- POST `/api/vendor/password-reset/confirm` [Both]
- POST `/api/auth/vendor/demo-login` [Both]

**Class 17: TestVendorEndpointContracts (~22 tests)**
Require vendor_auth_headers:
- GET `/api/vendors/{vendorId}` [Both]
- DELETE `/api/vendors/{vendorId}` [Both]
- GET `/api/erp/orders/vendor/{vendorId}` [Both]
- PATCH `/api/erp/orders/{orderId}/status` [Both] -- query param status=preparing
- POST `/api/erp/orders/{orderId}/restaurant-accept` [Both]
- POST `/api/erp/orders/{orderId}/restaurant-decline` [Both]
- POST `/api/erp/orders/{orderId}/restaurant-accept-delivery` [Both]
- POST `/api/erp/orders/{orderId}/restaurant-decline-delivery` [Both]
- PUT `/api/vendors/{vendorId}/online-status` [Both] -- query param is_online=true
- POST `/api/erp/orders/{orderId}/start-delivery-decision` [Both]
- POST `/api/erp/orders/{orderId}/restaurant-delivery-decision` [Both]
- GET `/api/erp/orders/{orderId}/delivery-decision-status` [Both]
- POST `/api/vendors/{vendorId}/menu` [Both] -- add menu item JSON
- PUT `/api/vendors/{vendorId}/menu/{itemId}` [Both]
- DELETE `/api/vendors/{vendorId}/menu/{itemId}` [Both]
- GET `/api/vendors/{vendorId}/menu/categories` [Both]
- POST `/api/vendors/{vendorId}/menu/assign-stock-images` [iOS]
- GET `/api/vendors/{vendorId}/documents` [Both]
- POST `/api/vendors/{vendorId}/documents` [Both]
- DELETE `/api/vendors/{vendorId}/documents/{documentId}` [Both]
- GET `/api/vendors/{vendorId}/reviews` [Android]
- GET `/api/vendors/{vendorId}/ai-insights` [Both] -- query param period=weekly
- GET `/api/menu-verification/status/{vendorId}` [iOS]
- POST `/api/menu-verification/approve-all/{vendorId}` [iOS]

**Class 18: TestVendorPromotionContracts (~8 tests)**
Require vendor_auth_headers:
- POST `/api/promotions/create` [Both] -- query param vendor_id
- GET `/api/promotions/vendor/{vendorId}` [Both]
- PUT `/api/promotions/{promotionId}` [Both]
- DELETE `/api/promotions/{promotionId}` [Both]
- GET `/api/promotions/suggestions/{vendorId}` [Both]
- GET `/api/promotions/analytics/{vendorId}` [Both]
- POST `/api/promotions/apply` [Both]
- POST `/api/promotions/quick-create/{vendorId}/{promoType}` [Both]

**Class 19: TestVendorStripeKOTContracts (~8 tests)**
Require vendor_auth_headers:
- POST `/api/vendors/{vendorId}/stripe/connect` [Android]
- GET `/api/vendors/{vendorId}/stripe/onboarding-link` [Android]
- GET `/api/vendors/{vendorId}/stripe/status` [Android]
- POST `/api/vendors/{vendorId}/stripe/dashboard-link` [Android]
- GET `/api/vendor/kot-config` [iOS]
- PUT `/api/vendor/kot-config` [iOS]
- POST `/api/vendor/kot-test` [iOS]
- POST `/api/erp/orders/{orderId}/print-kot` [iOS]

**Class 20: TestSharedEndpointContracts (~14 tests)**
Cross-app endpoints with various auth requirements:
- POST `/api/erp/customers/{customerId}/fcm-token` [iOS] -- customer_auth_headers, send {token: "test_fcm_token"}
- POST `/api/erp/drivers/{driverId}/fcm-token` [iOS] -- driver_auth_headers
- POST `/api/erp/vendors/{vendorId}/fcm-token` [iOS] -- vendor_auth_headers
- POST `/api/notifications/register-token` [Android] -- auth_headers
- PUT `/api/erp/drivers/{driverId}/location` [Both] -- driver_auth_headers
- PUT `/api/erp/drivers/{driverId}/status` [Both] -- driver_auth_headers, query param is_online=true
- GET `/api/erp/orders/{orderId}/full-tracking` [Both] -- auth_headers
- GET `/api/erp/orders/{orderId}/driver-location` [Both] -- auth_headers
- POST `/api/payments/create-intent` [Both] -- customer_auth_headers
- POST `/api/erp/payments/refund` [Both] -- admin_auth_headers
- GET `/api/tax/calculate` [Android] -- auth_headers
- POST `/api/drivers/{driverId}/status` [Android] -- driver_auth_headers
- GET `/api/drivers/{driverId}/status` [Android] -- driver_auth_headers
- GET `/api/erp/payouts/vendor/{vendorId}` [Android] -- vendor_auth_headers

**Class 21: TestShippedPathAliases (~8 tests)**
Tests for OLD paths that shipped TestFlight/Firebase builds still call. These paths have backend route aliases so the shipped apps work. Tests verify aliases remain functional:
- GET `/api/orders/{orderId}/chat` [iOS shipped] -- alias for /api/customer/orders/{id}/chat (main_new.py:21570)
- POST `/api/orders/{orderId}/chat` [iOS shipped] -- alias (main_new.py:21571)
- GET `/api/chat/messages/{rideRequestId}` [iOS shipped] -- alias for /api/p2p/ride-requests/{id}/chat (main_new.py:21552)
- POST `/api/chat/messages/{rideRequestId}` [iOS shipped] -- alias (main_new.py:21553)
- POST `/api/erp/rides/{rideId}/cancel` [iOS shipped] -- alias for /rides/request/{id}/cancel (main_new.py:15084)
- POST `/api/erp/orders/{rideId}/complete-delivery` [iOS shipped] -- alias for ride completion (main_new.py:14852)
- POST `/api/orders/create` [Android shipped] -- alias for /erp/orders/create (main_new.py:15350)
- GET `/api/customer/rides` [Android shipped] -- alias for /customer/rides/history (main_new.py:15466)

NOTE: 2 shipped iOS paths are KNOWN BROKEN (no backend alias):
- `POST /api/customer/favorites` (body-based, shipped sends customer_id/vendor_id in body not path) -- DO NOT test, will 404
- `DELETE /api/vendors/{vendorId}/delete` (shipped uses /delete suffix) -- DO NOT test, will 404
These need backend fixes in a separate phase before the next TestFlight build.

**Test pattern for each endpoint:**

For public endpoints:
```python
def test_vendors_published(self, client):
    """GET /api/vendors/published [Both] -- customer browsing restaurants"""
    response = client.get("/api/vendors/published")
    assert response.status_code == 200
```

For auth-required endpoints:
```python
def test_customer_orders(self, client, customer_auth_headers):
    """GET /api/customer/orders [Both] -- customer order history"""
    response = client.get("/api/customer/orders", headers=customer_auth_headers)
    assert response.status_code in [200, 404]

def test_customer_orders_requires_auth(self, client):
    """GET /api/customer/orders -- must return 401 without auth"""
    response = client.get("/api/customer/orders")
    assert response.status_code == 401
```

For mutation endpoints with body:
```python
def test_create_order(self, client, customer_auth_headers):
    """POST /api/erp/orders/create [Both] -- create food order"""
    response = client.post("/api/erp/orders/create", json={
        "vendor_id": 1,
        "items": [{"menu_item_id": 1, "quantity": 1}],
        "delivery_address": "123 Test St"
    }, headers=customer_auth_headers)
    assert response.status_code in [200, 201, 400, 422]
```

For shipped alias endpoints:
```python
def test_shipped_order_chat_alias(self, client, customer_auth_headers):
    """GET /api/orders/{orderId}/chat [iOS shipped] -- alias at main_new.py:21570"""
    response = client.get("/api/orders/99999/chat", headers=customer_auth_headers)
    assert response.status_code in [200, 404]  # Not 401 or 405
```

**Key rules:**
- Accept 200, 400, 404, 422 as "endpoint exists and accepts this method" -- NOT 405 (wrong method)
- For endpoints with path parameters that don't exist (orderId=99999), accept 404
- NEVER assert specific response body values -- only assert structure/type when status_code == 200
- Use `test_customer.id`, `test_driver.id`, `test_vendor.id` for path params needing real IDs
- Every test docstring starts with HTTP method + path + platform annotation
- DO NOT test endpoints that don't exist in the backend (dead iOS/Android code)
- DO NOT write auth_requires_401 tests for EVERY endpoint -- only write them for a representative sample of 5-6 endpoints across roles to verify the auth middleware is active. Writing auth-rejection tests for all 160 endpoints doubles the test count without testing new code.
  </action>
  <verify>
Run: `cd apps/web/p2p-platform/backend && python -m pytest tests/integration/test_ios_api_contracts.py -v --tb=short 2>&1 | tail -40`
Count tests: `cd apps/web/p2p-platform/backend && python -m pytest tests/integration/test_ios_api_contracts.py --collect-only -q 2>&1 | tail -5`
Verify at least 130 tests collected. Most should pass (200/400/422). Some may fail due to missing routes -- document any failures.
Also run existing unit tests to confirm no regressions: `cd apps/web/p2p-platform/backend && python -m pytest tests/unit/ -x -q --tb=short`
  </verify>
  <done>test_ios_api_contracts.py has 130+ test methods organized by domain across 21 classes, covering all ~160 endpoints that iOS and Android apps call plus 8 shipped alias paths. Each test has a docstring with HTTP method, path, and [iOS]/[Android]/[Both] annotation. Public endpoints tested without auth. Protected endpoints tested with correct role-based auth headers from conftest.py fixtures. A representative sample of auth-rejection tests verify the middleware. No business logic assertions -- only endpoint existence, method acceptance, and auth requirements.</done>
</task>

</tasks>

<verification>
1. `cd apps/web/p2p-platform/backend && python -m pytest tests/integration/test_ios_api_contracts.py --collect-only -q` -- verify 130+ tests collected
2. `cd apps/web/p2p-platform/backend && python -m pytest tests/integration/test_ios_api_contracts.py -v --tb=short` -- run all contract tests
3. `cd apps/web/p2p-platform/backend && python -m pytest tests/unit/ -x -q --tb=short` -- verify no unit test regressions
4. Grep for platform annotations: `grep -c "\[iOS\]\|\[Android\]\|\[Both\]\|\[iOS shipped\]\|\[Android shipped\]" tests/integration/test_ios_api_contracts.py` -- should be 130+
</verification>

<success_criteria>
- 130+ contract test methods covering all app-called endpoints
- Each test documents which platform calls it
- Shipped alias paths have dedicated TestShippedPathAliases class
- conftest.py has customer_auth_headers with customer_id in JWT
- models_extended imported in conftest to prevent missing table errors
- Existing unit tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-fix-ci/04-01-SUMMARY.md`
</output>
