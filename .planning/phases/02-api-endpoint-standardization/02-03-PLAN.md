---
phase: 02-api-endpoint-standardization
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - /Users/jeet/StudioProjects/eatfair-android/shared/src/main/java/ai/dollor/shared/data/remote/DollorApiService.kt
autonomous: false
requirements: [API-02, API-05, API-06]

must_haves:
  truths:
    - "Production backend serves all new routes (deploy from staging promoted)"
    - "Android vendor delete path matches backend (no /delete suffix if applicable)"
    - "All previously-404ing Android paths now reach working backend endpoints"
    - "No regressions in Android build"
  artifacts:
    - path: "/Users/jeet/StudioProjects/eatfair-android/shared/src/main/java/ai/dollor/shared/data/remote/DollorApiService.kt"
      provides: "Verified Android API paths matching backend"
  key_links:
    - from: "Android DollorApiService.kt financial endpoints"
      to: "main_new.py driver/vendor financial routes"
      via: "Retrofit HTTP calls"
      pattern: "drivers.*balance|vendors.*bank-account"
---

<objective>
Promote backend to production, verify Android API paths, and fix any remaining Android path mismatches.

Purpose: The backend changes from Plan 01 (new financial endpoints, demo-login, order chat aliases) are on staging. This plan promotes to production, then verifies all Android paths work. Android financial endpoints (balance, payouts, bank-account) are used by real screens in the Android app.

Output: Production running with all new routes. Android paths verified against live backend. Any remaining mismatches fixed.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-api-endpoint-standardization/02-01-SUMMARY.md
@.planning/phases/02-api-endpoint-standardization/02-02-SUMMARY.md
@/Users/jeet/StudioProjects/eatfair-android/shared/src/main/java/ai/dollor/shared/data/remote/DollorApiService.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote backend to production and verify</name>
  <files>apps/web/p2p-platform/backend/main_new.py</files>
  <action>
**Step 1: Deploy to production via CI/CD.**
```bash
gh workflow run deploy-dollar-ai.yml
```
Wait for the workflow to complete:
```bash
gh run list --workflow=deploy-dollar-ai.yml --limit 3
gh run watch <run-id>
```

**Step 2: Smoke test production.**
Test the new endpoints against production (`https://api.dollor.ai`):

1. Health: `curl -s "https://api.dollor.ai/api/health"` -- should return 200
2. Order chat alias: `curl -s -o /dev/null -w "%{http_code}" "https://api.dollor.ai/api/orders/1/chat"` -- should return 401 (not 404)
3. Driver balance: `curl -s -o /dev/null -w "%{http_code}" "https://api.dollor.ai/api/drivers/1/balance"` -- should return 401 (not 404)
4. Customer demo-login: `curl -s -o /dev/null -w "%{http_code}" -X POST "https://api.dollor.ai/api/customer/demo-login"` -- should return 401 or 422 (not 404)
5. Driver demo-login: `curl -s -o /dev/null -w "%{http_code}" -X POST "https://api.dollor.ai/api/auth/driver/demo-login"` -- should return 401 or 422 (not 404)
6. Vendor bank-account: `curl -s -o /dev/null -w "%{http_code}" -X POST "https://api.dollor.ai/api/vendors/1/bank-account"` -- should return 401 (not 404)
7. ERP vendor payouts: `curl -s -o /dev/null -w "%{http_code}" "https://api.dollor.ai/api/erp/payouts/vendor/1"` -- should return 401 (not 404)

ALL endpoints should return non-404 status codes. 401 means the route exists but requires auth (correct). 404 means the route was not deployed (failure).

**Step 3: Record the new task definition number from the deploy.**
Run `gh run view <run-id>` and note the ECS task definition version.
  </action>
  <verify>
Run: `gh run view <run-id> --json conclusion -q .conclusion` -- should return "success".

Run: `curl -s "https://api.dollor.ai/api/health" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('status','FAIL'))"` -- should print "healthy".

All 7 smoke test curl commands return non-404 status codes.
  </verify>
  <done>
Production backend running with all 9 new route aliases/endpoints. All smoke tests confirm routes are live (401, not 404). ECS task definition number recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix Android API paths</name>
  <files>/Users/jeet/StudioProjects/eatfair-android/shared/src/main/java/ai/dollor/shared/data/remote/DollorApiService.kt</files>
  <action>
With the backend now serving all new endpoints, verify that the Android Retrofit annotations match the backend paths.

**Step 1: Cross-reference every Android financial endpoint against backend.**
The Android endpoints and their expected backend paths (from Plan 01):

| Android Retrofit Path | Expected Backend Path | Status |
|---|---|---|
| `GET drivers/{driverId}/balance` | `GET /api/drivers/{driver_id}/balance` | Should match (Plan 01 added) |
| `POST drivers/{driverId}/bank-account` | `POST /api/drivers/{driver_id}/bank-account` | Should match (Plan 01 added) |
| `POST drivers/{driverId}/payouts` | `POST /api/drivers/{driver_id}/payouts` | Should match (Plan 01 added) |
| `POST vendors/{vendorId}/bank-account` | `POST /api/vendors/{vendor_id}/bank-account` | Should match (Plan 01 added) |
| `GET erp/payouts/vendor/{vendorId}` | `GET /api/erp/payouts/vendor/{vendor_id}` | Should match (Plan 01 added) |
| `POST customer/demo-login` | `POST /api/customer/demo-login` | Should match (Plan 01 added) |
| `POST auth/driver/demo-login` | `POST /api/auth/driver/demo-login` | Should match (Plan 01 added) |

Since Plan 01 added backend endpoints matching what Android already calls, NO Android changes should be needed for these 7 paths.

**Step 2: Verify Android vendor delete path.**
Check Android's vendor delete annotation in DollorApiService.kt. Find the `deleteVendorAccount` function. If it calls `DELETE vendors/{vendorId}` (without `/delete`), it already matches the backend. If it has `/delete` suffix, fix it.

**Step 3: Verify Android order chat path.**
Check if Android has order chat endpoints. If they call `/customer/orders/{id}/chat`, they match. If they call `/orders/{id}/chat`, they'll work via the alias from Plan 01.

**Step 4: Run Android build to verify no regressions.**
```bash
cd /Users/jeet/StudioProjects/eatfair-android && ./gradlew :shared:compileDebugKotlin 2>&1 | tail -20
```

**Step 5: If any path changes were needed, commit to Android repo.**
```bash
cd /Users/jeet/StudioProjects/eatfair-android
git add shared/src/main/java/ai/dollor/shared/data/remote/DollorApiService.kt
git commit -m "fix: align API paths with backend after endpoint standardization"
```
Only commit if changes were actually made.
  </action>
  <verify>
Run: `cd /Users/jeet/StudioProjects/eatfair-android && ./gradlew :shared:compileDebugKotlin 2>&1 | tail -5` -- should show BUILD SUCCESSFUL.

Run: `grep -n "vendors.*delete\b" /Users/jeet/StudioProjects/eatfair-android/shared/src/main/java/ai/dollor/shared/data/remote/DollorApiService.kt` -- should NOT show `/delete` suffix (or confirm it was never there).
  </verify>
  <done>
All Android API paths verified against production backend. Financial endpoints (balance, payouts, bank-account) match the new backend routes. Android shared module compiles successfully. Any path mismatches have been fixed and committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify endpoint standardization complete</name>
  <files>apps/web/p2p-platform/backend/main_new.py</files>
  <action>
Human verification checkpoint. Present the following to the user for confirmation:

Complete API endpoint standardization across iOS, Android, and backend:
- 9 new backend route aliases/endpoints (order chat, demo-login, financial)
- iOS vendor delete path fixed (App Store compliance)
- iOS order chat path fixed
- Backend deployed to production with all new routes
- Android paths verified against production

Verification steps for the user:
1. Check production health: `curl -s "https://api.dollor.ai/api/health"`
2. Verify no 404s on previously-broken endpoints:
   - `curl -s -o /dev/null -w "%{http_code}" "https://api.dollor.ai/api/orders/1/chat"` -- should be 401, not 404
   - `curl -s -o /dev/null -w "%{http_code}" "https://api.dollor.ai/api/drivers/1/balance"` -- should be 401, not 404
3. (Optional) Test iOS vendor delete in Xcode simulator -- should no longer 404
4. (Optional) Test Android financial screens -- should no longer crash/fail silently
  </action>
  <verify>User types "approved" or describes issues.</verify>
  <done>User has confirmed endpoint standardization is complete and all previously-broken paths now work.</done>
</task>

</tasks>

<verification>
1. Production backend serves all 9 new routes (401, not 404)
2. iOS paths corrected (vendor delete, order chat)
3. Android paths verified (financial, demo-login)
4. Both platform builds compile
5. No regressions in existing tests
</verification>

<success_criteria>
- Production deploy succeeded via CI/CD
- All 7 production smoke tests return non-404 status codes
- Android shared module compiles
- User approves the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-endpoint-standardization/02-03-SUMMARY.md`
</output>
