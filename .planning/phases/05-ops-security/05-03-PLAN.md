---
phase: 05-ops-security
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - CLAUDE.md
autonomous: false
requirements: [OPS-06, OPS-07]

must_haves:
  truths:
    - "CLAUDE.md documents that production uses AWS Secrets Manager for all 11 secrets"
    - "CLAUDE.md warns that .p8 files must never be committed"
    - "CLAUDE.md lists the correct staging URL only"
    - "CLAUDE.md no longer references unused keys or wrong URLs"
    - "User has been prompted to revoke key JFVA7628SX in App Store Connect"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Current production state documentation"
      contains: "Secrets Manager"
  key_links:
    - from: "CLAUDE.md"
      to: "ECS task definitions"
      via: "documentation of secret management"
      pattern: "Secrets Manager"
---

<objective>
Update CLAUDE.md to reflect the current production state after credential cleanup, and prompt user to revoke the removed App Store Connect key in Apple's dashboard.

Purpose: Ensure CLAUDE.md is the single source of truth for production infrastructure state, and complete the security loop by revoking the exposed key.
Output: Updated CLAUDE.md with Secrets Manager documentation, .p8 warning, and correct references. User prompted for key revocation.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ops-security/05-RESEARCH.md
@.planning/phases/05-ops-security/05-01-SUMMARY.md
@.planning/phases/05-ops-security/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md with production security state</name>
  <files>CLAUDE.md</files>
  <action>
    Read the current CLAUDE.md and make these targeted updates:

    1. **Add to SECURITY ARCHITECTURE section** — after the "Required Environment Variables" table, add a new subsection:

       ```markdown
       ### Production Secret Management

       All production and staging secrets are managed via AWS Secrets Manager. No secrets exist in the codebase.

       | Secret | Source |
       |--------|--------|
       | `DATABASE_URL` | `dollor/production/database-v2-gd1oKf` |
       | `JWT_SECRET_KEY` | `dollor/production/jwt-secret-kvk9j9` |
       | `STRIPE_SECRET_KEY` | `dollor/production/stripe-vT8WRA` |
       | `STRIPE_PUBLISHABLE_KEY` | `dollor/production/stripe-vT8WRA` |
       | `ADMIN_SECRET_KEY` | `dollor/production/admin-yCDIFY` |
       | `DASHBOARD_SECRET` | `dollor/production/admin-yCDIFY` |
       | `SMTP_USER` | `dollor/production/smtp-credentials-eqAwat` |
       | `SMTP_PASSWORD` | `dollor/production/smtp-credentials-eqAwat` |
       | `PERSONA_API_KEY` | `dollor/production/persona-aqEOSX` |
       | `PERSONA_TEMPLATE_ID` | `dollor/production/persona-aqEOSX` |
       | `FIREBASE_CREDENTIALS_JSON` | `dollor/production/firebase-creds-DG9fC5` |

       Staging uses separate ARNs under `dollor/staging/*`. ECS task definitions pull secrets at container start.

       ### Credential Rules

       | Rule | Details |
       |------|---------|
       | **NEVER commit `.p8` files** | App Store Connect keys. `.gitignore` blocks them. Production key `9K626GB728` lives at `~/.appstoreconnect/private_keys/` only. |
       | **NEVER commit `.env` files** | `.gitignore` blocks them. Use `.env.example` for templates. |
       | **Pre-commit hook active** | `.git/hooks/pre-commit` blocks Stripe keys, AWS keys, private key PEM blocks, `.p8` files, and raw `.env` files. |
       | **Staging URL** | `d34u5ixl0bulv4.cloudfront.net` ONLY. The old `d3kuu45w6kl8hr` URL is PRODUCTION, not staging. |
       ```

    2. **Update the "Last Updated" line** at the bottom of CLAUDE.md to `February 21, 2026`.

    Do NOT change any other section. The rest of CLAUDE.md is correct per Phase 04 docs overhaul.
  </action>
  <verify>
    - `grep "Secrets Manager" CLAUDE.md` → at least 1 match
    - `grep "NEVER commit.*p8" CLAUDE.md` → match found
    - `grep "d34u5ixl0bulv4" CLAUDE.md` → match found in Credential Rules table
    - `grep "d3kuu45w6kl8hr" CLAUDE.md` → match found ONLY in the "old URL is PRODUCTION" warning context, nowhere else as a staging reference
    - `grep "February 21, 2026" CLAUDE.md` → match found
  </verify>
  <done>CLAUDE.md documents all 11 Secrets Manager secrets, .p8 commit prohibition, pre-commit hook, correct staging URL rule, and updated date.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Revoke App Store Connect key JFVA7628SX</name>
  <files>none</files>
  <action>
    Plans 01 and 02 removed the unused .p8 key JFVA7628SX from git, added .gitignore protection, fixed all 35 old staging URLs, installed a pre-commit hook, and updated CLAUDE.md.

    The key JFVA7628SX still exists in git HISTORY (not current files). To make it permanently useless, it must be revoked in App Store Connect. This requires human action in Apple's web dashboard.

    Steps for the user:
    1. Go to https://appstoreconnect.apple.com
    2. Navigate to: Users and Access -> Integrations -> App Store Connect API -> Keys
    3. Find key with ID JFVA7628SX
    4. Click "Revoke" on that key
    5. Confirm the production key 9K626GB728 is still active (this is the one Fastlane uses)

    CRITICAL: Do NOT revoke key 9K626GB728 — that is the active production key referenced by all Fastlane Appfiles.
  </action>
  <verify>User confirms key JFVA7628SX has been revoked in App Store Connect, OR reports "skip" to handle later, OR reports "not-found" if the key does not exist.</verify>
  <done>Key JFVA7628SX is revoked (or confirmed non-existent) in App Store Connect. The key in git history is now permanently useless. Production key 9K626GB728 remains active and unaffected.</done>
</task>

</tasks>

<verification>
- CLAUDE.md contains Secrets Manager documentation, .p8 warning, correct staging URL rule
- User has been prompted to revoke JFVA7628SX in App Store Connect
- All Phase 05 objectives achieved: credentials removed, URLs fixed, docs updated, prevention installed
</verification>

<success_criteria>
CLAUDE.md reflects current production state with Secrets Manager docs, credential rules, and correct staging URL. User has been prompted to revoke the exposed key JFVA7628SX (completing the security loop).
</success_criteria>

<output>
After completion, create `.planning/phases/05-ops-security/05-03-SUMMARY.md`
</output>
