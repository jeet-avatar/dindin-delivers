---
phase: 29-ui-workflow-e2e-uat-fixes
plan: 03
type: execute
wave: 2
depends_on: ["29-01", "29-02"]
files_modified:
  - /Users/jeet/turion-space-demo/satellite/bom.html
  - /Users/jeet/turion-space-demo/satellite/parts.html
  - /Users/jeet/turion-space-demo/satellite/instance.html
  - /Users/jeet/turion-satellite/backend/src/routes/integration.ts
  - /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs
autonomous: false
requirements: [E2E_UAT, PersistenceVerify]

must_haves:
  truths:
    - "Before deploying turion-space-demo, a pre-flight confirms ONLY the intended satellite/* + shared files (and the integration.ts/audit script in turion-satellite) are dirty — unrelated ERP-demo WIP does NOT ride along on deploy-frontend.sh"
    - "The 29-01/29-02 frontend changes are live at https://turionspace.zietra.com/satellite/ (bom.html Add-BOM-line modal, parts.html URL-param filter, instance.html instance#1 hint)"
    - "The audit script reports 0 violations against the deployed code"
    - "A fresh real-browser session as the demo user (Supabase magic-link) walks all 6 primary flows on the live site without console errors or stuck skeletons"
    - "Each of the ~11 mutating interactions (stage advance/revert, place vendor order, request material, sign build step, add build step, mark WO complete, create WO, create instance, save make/buy decision, re-evaluate, add BOM line) is proven to persist by a psql query against the production turion_satellite Postgres AND survives a page reload"
    - "The 2 Phase 28 deferred items (instance_index>1 missing WO/PR; ns_invoice_id NULL everywhere) are explicitly acknowledged out-of-scope — the UAT walks instance #1 for the happy path"
    - "The SUMMARY records every flow's PASS/FAIL with the exact psql query used and the row it returned"
  artifacts:
    - path: ".planning/phases/29-ui-workflow-e2e-uat-fixes/29-03-SUMMARY.md"
      provides: "UAT report: 6-flow checklist with PASS/FAIL, per-mutation psql persistence proof, deploy verification (CloudFront invalidation id), audit-script result, deferred-items acknowledgement"
      min_lines: 40
  key_links:
    - from: "Phase 29 deploy step"
      to: "/Users/jeet/turion-space-demo/deploy-frontend.sh"
      via: "run AFTER pre-flight `git -C /Users/jeet/turion-space-demo status --porcelain` confirms only intended files dirty; then ./deploy-frontend.sh (s3 sync + CloudFront invalidate); poll invalidation to Completed"
      pattern: "deploy-frontend\\.sh"
    - from: "UAT persistence proof"
      to: "turion_satellite Postgres (AWS Secrets Manager arn:aws:secretsmanager:us-east-1:134607809447:secret:turion-satellite/production/database-url-NCbgX6)"
      via: "psql <conn-string-with-?schema=stripped> -c 'SELECT ... FROM turion_satellite.<table> WHERE ...' after each browser action"
      pattern: "turion_satellite\\."
    - from: "UAT auth"
      to: "https://turionspace.zietra.com/satellite/login.html"
      via: "real magic-link browser session as the whitelisted demo email (no synthetic JWT — Lambda verifies ES256 via JWKS)"
      pattern: "magic-link|login\\.html"
---

<objective>
Ship Phase 29 to production and run the final user-acceptance pass: deploy the 29-01/29-02 frontend + backend changes with a deploy-hygiene pre-flight (so unrelated ERP-demo WIP in turion-space-demo can't ride along), then launch a fresh browser session as the demo user and walk every primary flow on the live site, proving each mutation persists with a psql query against the production database and survives a reload. Capture the whole thing in the SUMMARY.

Purpose: The Phase 29 ROADMAP goal ends "...launch a fresh browser session as the demo user, exercise every primary flow, prove backend persistence." This plan is that. It's also the only plan that touches production. It depends on 29-01 (audit script, parts.html, instance.html, auth/callback.html) and 29-02 (bom.html modal, integration.ts comment) being committed.

Output:
- Production deploy of turion-space-demo (bom.html / parts.html / instance.html + audit wrapper) via `deploy-frontend.sh`, with a CloudFront invalidation polled to Completed
- Production deploy of turion-satellite backend ONLY IF a code path changed (the integration.ts change is a comment — `tsc` output is identical, so the Lambda likely does NOT need a redeploy; if `build-and-push.sh` would change `CodeSha256` because the `.ts` comment lands in `dist/`, redeploy; otherwise skip and document)
- All commits pushed to both repos under jm@techcloudpro.com / jeet-avatar
- `.planning/phases/29-ui-workflow-e2e-uat-fixes/29-03-SUMMARY.md` — the UAT report
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-ui-workflow-e2e-uat-fixes/29-RESEARCH.md
@.planning/phases/28-full-bom-densification-data-coverage-drill-down-ui/deferred-items.md

# Reference (read before deploying):
# /Users/jeet/turion-space-demo/deploy-frontend.sh           — s3 sync + CloudFront invalidate (syncs ALL root-level *.html — the F6 landmine)
# /Users/jeet/turion-satellite/backend/build-and-push.sh     — npm tsc -> docker arm64 -> ECR -> aws lambda update-function-code (only run if a code path actually changed)
# /Users/jeet/doordash-p2p/.planning/phases/29-ui-workflow-e2e-uat-fixes/29-01-SUMMARY.md  — audit script details, what 29-01 changed
# /Users/jeet/doordash-p2p/.planning/phases/29-ui-workflow-e2e-uat-fixes/29-02-SUMMARY.md  — bom.ts POST body field names, modal validation
# 29-RESEARCH.md Section E "How to run the final UAT pass" — the 6-flow checklist + per-mutation psql queries (copy them)
# Backend API: https://rjydekliee.execute-api.us-east-1.amazonaws.com  | Live site: https://turionspace.zietra.com/satellite/
# SAT-003 = "Cygnus", UUID 24587565-b15b-42ce-b590-87ecf9b6bb99
# DB conn: aws secretsmanager get-secret-value --secret-id arn:aws:secretsmanager:us-east-1:134607809447:secret:turion-satellite/production/database-url-NCbgX6 --query SecretString --output text  (strip "?schema=..." before passing to psql; schema-qualify tables as turion_satellite.<table>)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy-hygiene pre-flight + deploy frontend + (conditional) backend</name>
  <files>/Users/jeet/turion-space-demo/satellite/bom.html, /Users/jeet/turion-space-demo/satellite/parts.html, /Users/jeet/turion-space-demo/satellite/instance.html, /Users/jeet/turion-satellite/backend/src/routes/integration.ts, /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs</files>
  <action>
**F6 pre-flight (BLOCKING — do this first):**
1. `git -C /Users/jeet/turion-space-demo status --porcelain` — list every dirty/untracked path. EXPECTED dirty set after 29-01+29-02: `satellite/bom.html`, `satellite/parts.html`, `satellite/instance.html`, `satellite/auth/callback.html` (only if 29-01 edited it), `scripts/audit-satellite-buttons.mjs`, `package.json`, and possibly the gitignored `satellite/satellite-config.js` (regenerated at deploy time — ignore it). If ANYTHING ELSE is dirty (e.g. `about-this-demo.html`, `agent-sales-cash.html`, `dashboard-cio.html`, `backend/dist/*`, `backend/src/routes/agents.ts`, `backend/src/routes/notify.ts` — the separate ERP-demo files per Phase 27 deferred-items): **STOP**. Either `git stash push -- <those paths>` to set them aside for the deploy, or scope `deploy-frontend.sh`'s `aws s3 sync` to only `satellite/` + the shared root JS/CSS the satellite pages need. Do NOT let unrelated WIP ship. Document exactly what you found and what you did in the SUMMARY.
2. Confirm 29-01 + 29-02 commits exist locally in both repos (`git -C /Users/jeet/turion-space-demo log --oneline -5`, `git -C /Users/jeet/turion-satellite log --oneline -5`). If 29-01/29-02 committed their changes (they should have), the working tree for those files is clean and they're in the log. If not, commit them now under jm@techcloudpro.com / jeet-avatar with appropriate `feat(29-01)` / `feat(29-02)` messages before deploying.

**Run the audit one more time (BLOCKING):** `cd /Users/jeet/turion-space-demo && node scripts/audit-satellite-buttons.mjs` → must exit 0, `violations: 0`. If it fails, stop and fix before deploying.

**Deploy frontend:** `cd /Users/jeet/turion-space-demo && ./deploy-frontend.sh`. This regenerates the gitignored `satellite-config.js` from Secrets Manager, `aws s3 sync`s to `s3://turion-demo-static`, and creates a CloudFront invalidation on dist `E37R9PT8IL44L2`. Capture the invalidation ID from the output. Poll it: `aws cloudfront get-invalidation --distribution-id E37R9PT8IL44L2 --id <id> --query 'Invalidation.Status'` until `Completed`.

**Verify the deploy landed:** `curl -s https://turionspace.zietra.com/satellite/bom.html | grep -c addBomLineBtn` → ≥1. `curl -s https://turionspace.zietra.com/satellite/parts.html | grep -c "getQueryParam('subsystem')"` (or whatever exact string 29-01 added) → ≥1. `curl -s https://turionspace.zietra.com/satellite/instance.html | grep -c instance_index` → ≥1. If any is 0, the invalidation hasn't propagated or the sync missed the file — investigate (don't proceed to UAT until the deployed HTML reflects the changes).

**Backend (conditional):** The only backend change in Phase 29 is the `integration.ts` API-only comment (29-02 Task 2) and the new `audit-satellite-buttons.mjs` script + its Vitest test (29-01 Task 1). The audit script is not part of the Lambda bundle (it's a dev/test tool). The `integration.ts` comment changes `dist/routes/integration.js` only if comments are emitted (tsc strips most comments; JSDoc `/** */` blocks are typically dropped from emit). Decision: run `cd /Users/jeet/turion-satellite/backend && npm run build` (or whatever the tsc build command is), then `git status` on `dist/` — if `dist/` is byte-identical (no diff), the Lambda does NOT need a redeploy; skip `build-and-push.sh` and document "backend Lambda unchanged — comment-only change, tsc emit identical." If `dist/` did change, run `./build-and-push.sh`, capture the `CodeSha256` before→after, and confirm `State=Active` / `LastUpdateStatus=Successful`.

**Push everything:** `git -C /Users/jeet/turion-space-demo push` and `git -C /Users/jeet/turion-satellite push` — all 29-01/29-02/29-03 commits, under jm@techcloudpro.com / jeet-avatar. (If commits aren't yet made for this plan's deploy artifacts — e.g. a stash-pop, a `dist/` rebuild — make them with `chore(29-03): deploy phase 29` and push.)
  </action>
  <verify>
`git -C /Users/jeet/turion-space-demo status --porcelain` showed only the intended satellite/* + scripts + package.json (+ gitignored satellite-config.js) dirty before deploy — unrelated ERP files stashed or sync scoped. Audit script exits 0 with 0 violations. `deploy-frontend.sh` ran; CloudFront invalidation polled to `Completed`. `curl https://turionspace.zietra.com/satellite/bom.html` contains `addBomLineBtn`; `parts.html` contains the URL-param-reading code; `instance.html` contains the instance_index hint code. Backend: either `dist/` unchanged → no redeploy (documented) OR `build-and-push.sh` ran with a new CodeSha256 and `State=Active`. All commits pushed to both repos.
  </verify>
  <done>Pre-flight confirmed only intended files dirty; audit clean; frontend deployed (invalidation Completed; deployed HTML reflects 29-01+29-02); backend redeployed only if a code path actually changed (documented either way); all commits pushed to both repos under the correct git identity.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human magic-link sign-in for the live UAT session</name>
  <action>
This is a checkpoint — the agent CANNOT click a magic link in the project owner's inbox. After Task 1 has deployed Phase 29 live, the agent presents the sign-in steps below; the human (or a browser the human controls) signs in via Supabase magic-link to obtain an authenticated session for Task 3's UAT walk. There is NO synthetic-JWT path (the Lambda verifies ES256 via JWKS, no signing key available — Phase 28 SUMMARY), so a real browser session is mandatory. The agent waits for the resume signal before starting Task 3.
  </action>
  <what-built>Phase 29 frontend changes are deployed live at https://turionspace.zietra.com/satellite/. The next task (Task 3) needs an authenticated browser session as the demo user to walk the 6 primary flows. The satellite app uses Supabase magic-link auth (no password) and the backend Lambda verifies ES256 via JWKS — there is NO synthetic-JWT path, so a real browser session is mandatory.</what-built>
  <how-to-verify>
1. Open https://turionspace.zietra.com/satellite/login.html in a browser.
2. Enter the whitelisted demo email (the Supabase project owner's email — if you're not sure which email is whitelisted in the `lbpkbpfwdpnwlccmlfxn` Supabase project's Auth allowlist, tell me and I'll pause for you to confirm; do NOT guess an email).
3. Click the magic link in the inbox → it lands on `/satellite/auth/callback.html` → you should be redirected into `/satellite/` signed in (this also doubles as the F4 auth/callback.html live check — confirm it didn't error and landed you on the constellation page).
4. Confirm the topbar shows you're signed in (a "Sign out" control is visible).
5. Tell me the session is live (and paste the demo email you used, and confirm whether you want the agent to drive the browser via a tool or whether you'll click through the flows yourself reading the checklist).
  </how-to-verify>
  <resume-signal>Reply "session live as <email>" — and say "agent drives" if you want me to use a browser tool for the flows, or "I'll click through" if you'll do the UAT manually with me providing the checklist + psql queries.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Walk the 6 primary flows on the live site + psql persistence proof</name>
  <files>.planning/phases/29-ui-workflow-e2e-uat-fixes/29-03-SUMMARY.md</files>
  <action>
With the live session from Task 2, walk all 6 primary flows from 29-RESEARCH.md Section E (plus the BOM-line modal from 29-02, and the edge auth flow). For EACH mutating action: do it in the browser, then run a psql query against production to confirm the row, then reload the page and confirm it renders. Get the DB connection string once: `aws secretsmanager get-secret-value --secret-id arn:aws:secretsmanager:us-east-1:134607809447:secret:turion-satellite/production/database-url-NCbgX6 --query SecretString --output text` — strip any `?schema=...` suffix before passing to `psql`; schema-qualify every table as `turion_satellite.<table>`. Money fields arrive as JSON strings (Decimal shim) — that's expected.

**Flow 1 — Drill-down (read-only, no mutation):** `/satellite/` → Cygnus card → `sat.html` → click a subsystem region (e.g. EPS) → drawer lists parts → click a part with instances → `part.html` → "Instances across constellation" → click an **instance #1** row → `instance.html` renders hero CAD + spec + cost + integrations panel + subtree rollup + timeline + BOM children + work orders. PASS = no stuck skeletons, no console errors, no "unavailable" panels (the documented `ns_invoice_id` "—" and any `instance_index>1` empties don't count — and you're on #1 so they shouldn't appear). Record any console error verbatim.

**Flow 2 — BOM tree:** `bom.html?sat=24587565-b15b-42ce-b590-87ecf9b6bb99` → tree renders with node/root count + max depth in the header (max depth should be ≥ 4 — Phase 28 raised it) → expand-all / collapse-all work → click any row → lands on `instance.html?inst=...&id=...&sat=...`. **Then test the NEW Add-BOM-line modal (29-02):** click "+ Add BOM line" → pick a parent instance + a child instance (different) + qty 1 + an optional ref designator → "Add line" → page reloads → the new line appears in the tree under the chosen parent. **Persistence proof:** `SELECT id, parent_part_instance_id, child_part_instance_id, quantity, reference_designator FROM turion_satellite.bom_lines WHERE parent_part_instance_id = '<parent id you picked>' ORDER BY id DESC LIMIT 1;` — shows your new row (use the EXACT column names from bom.ts — see 29-02-SUMMARY). Also test a rejected case: open the modal, pick the SAME instance for parent and child → "Add line" → inline error appears, no API call, no row created.

**Flow 3 — Lifecycle advance/revert:** open `instance.html` for an **instance #1** not at the final stage → "↪ Advance to {stage}" → enter a reason → toast "Stage advanced" → reload → new event in the timeline + updated stage tag. **Persistence proof:** `SELECT direction, reason, timestamp FROM turion_satellite.part_stage_events WHERE part_instance_id = '<id>' ORDER BY timestamp DESC LIMIT 1;` shows `direction='advance'` with your reason. Then "↩ Revert to {stage}" → same loop reversed; psql shows `direction='revert'`.

**Flow 4 — Manufacturing: create WO → add step → sign step → complete WO:** `work-orders.html?sat=24587565-...` → "+ New work order" → pick an instance → reload → new row → open it → `work-order.html` → "+ Add step" → enter a description → step appears → "✓ PASS" on the first unsigned step → confirm → step shows PASS badge + signed-by → "↪ Mark complete" → confirm → reload, status=`complete`, complete button gone. **Persistence proof:** `SELECT id, status, completed_at FROM turion_satellite.work_orders WHERE id='<woId>';` (status='complete') and `SELECT description, result, signed_at, signed_by FROM turion_satellite.build_steps WHERE work_order_id='<woId>' ORDER BY id;` (your new step has result='pass', signed_at NOT NULL).

**Flow 5 — Procurement:** `part.html` for a **buy** part with ≥1 instance → "📦 Order from vendor" → pick instance + vendor + qty → "Place order" → reload → row in "Recent orders". `SELECT id, part_instance_id, vendor_id, qty FROM turion_satellite.vendor_orders WHERE part_instance_id='<id>' ORDER BY id DESC LIMIT 1;`. Then `part.html` for a **make** part → "📋 Request material" → material description + est cost → "Submit request" → reload → row in "Recent orders" + "Materials required". `SELECT id, part_instance_id, material_description, estimated_cost_usd FROM turion_satellite.procurement_requests WHERE part_instance_id='<id>' ORDER BY id DESC LIMIT 1;`.

**Flow 6 — Cost rollup + make/buy decision:** `cost.html` → constellation rollup → click Cygnus row → per-subsystem rollup + totals card → click "View parts →" — **confirm the F3 fix: parts.html opens already filtered to that subsystem** (not the full catalog) — that's a Phase 29 fix being verified live. Then reach `cost-detail.html?sat=...&part_inst=...` (from `part.html`'s "Enter cost sheet" / "→ View full cost sheet" CTA) → make/buy sheets + decision panel → pick "Make" or "Buy" + a ≥20-char rationale → "Save decision" → reload → decision pill shows → "↻ Re-evaluate" → confirm → status flips to `re_evaluate` + banner. **Persistence proof:** `SELECT id, decision, decision_status, rationale, superseded_by FROM turion_satellite.make_buy_decisions WHERE satellite_id='24587565-...' AND part_definition_id='<id>' ORDER BY decided_at DESC;` — exactly ONE row with `superseded_by IS NULL` (the new current), older rows chained via `superseded_by`.

**Flow 7 — Edge: auth.** Sign out from the topbar → redirected to `login.html`. Try a deep-link to `instance.html?...` while signed out → redirects to `login.html` (ideally back after re-auth). (Re-auth optional for this edge check; the main session is already proven.)

**Also during the walk (note, don't fix):** the cross-system "↗" links on `instance.html`/`cost-detail.html` open `turionspace.zietra.com/sales|finance|records|manufacturing/...` — those are the separate ERP-demo shells, out of Phase 29 scope. Confirm they don't 404 (a stale deploy could break them) but they're not this plan's responsibility to fix. And: `ns_invoice_id` shows "—" on every instance's integrations panel (expected — Phase 28 deferred #2); if you happen to open an `instance_index>1` instance, confirm the new 29-01 "tracked on instance #1" hint shows (don't go hunting for one — the happy path is #1).

**Write `.planning/phases/29-ui-workflow-e2e-uat-fixes/29-03-SUMMARY.md`:** a table of all 7 flows with PASS/FAIL, for each mutation the exact psql query run and the row returned, the F3 "View parts" filter result, the Add-BOM-line happy + rejected case results, the deploy verification (CloudFront invalidation ID, deployed-HTML grep results), the audit-script result, the backend redeploy decision (and CodeSha256 if redeployed), and an explicit paragraph acknowledging the 2 Phase 28 deferred items as out-of-scope (UAT walked instance #1). Any FAIL → describe it, and if it's a real Phase 29 regression (not a pre-existing data state), STOP and flag it (it would need a gap-closure plan); if it's a pre-existing/out-of-scope state, note it and continue.
  </action>
  <verify>
All 7 flows have a PASS/FAIL recorded in 29-03-SUMMARY.md. Every mutating action has a psql query + the returned row pasted. The F3 "View parts" link landed on a filtered parts.html (Phase 29 fix verified live). The Add-BOM-line modal created a real `bom_lines` row (psql-proven) and the parent==child case was rejected client-side. The make/buy decision flow shows exactly one `superseded_by IS NULL` row after save. Deploy verification (invalidation Completed, deployed HTML greps ≥1) recorded. The 2 Phase 28 deferred items are explicitly acknowledged as out-of-scope. No unflagged Phase 29 regression.
  </verify>
  <done>The live UAT report exists at .planning/phases/29-ui-workflow-e2e-uat-fixes/29-03-SUMMARY.md with all 7 flows PASS (or any FAIL flagged with disposition), every mutation psql-proven, the F3 + Add-BOM-line Phase 29 fixes verified live, deploy verified, and the Phase 28 deferred items acknowledged out-of-scope.</done>
</task>

</tasks>

<verification>
- Pre-flight: `git -C /Users/jeet/turion-space-demo status --porcelain` showed only intended files dirty before deploy (unrelated ERP WIP stashed/scoped) — documented.
- Audit script: 0 violations against deployed code.
- Frontend deploy: `deploy-frontend.sh` ran; CloudFront invalidation on `E37R9PT8IL44L2` polled to `Completed`; `curl` of bom.html / parts.html / instance.html shows the 29-01/29-02 changes.
- Backend: redeployed only if `dist/` actually changed (CodeSha256 before→after + State=Active recorded); otherwise "unchanged — comment-only" documented.
- All commits pushed to both repos under jm@techcloudpro.com / jeet-avatar.
- Live UAT: 7 flows walked as the demo user (real magic-link session), each PASS/FAIL recorded; every one of the ~11 mutations psql-proven AND survived a reload.
- F3 verified live: cost.html "View parts →" → filtered parts.html.
- F1 verified live: Add-BOM-line modal → real `bom_lines` row; parent==child rejected client-side.
- F4 verified live: magic-link → callback.html → /satellite/ signed in, no error.
- Phase 28 deferred items (instance_index>1 missing WO/PR; ns_invoice_id NULL) acknowledged out-of-scope in the SUMMARY; UAT walked instance #1.
</verification>

<success_criteria>
- Phase 29's frontend changes are live at https://turionspace.zietra.com/satellite/ with no unrelated ERP-demo WIP shipped alongside (F6 pre-flight enforced).
- The audit script reports 0 violations against the deployed code.
- A fresh real-browser session as the demo user walked all 6 primary flows (+ BOM-line modal + edge auth) successfully — every mutating interaction proven to persist by a psql query against production AND reflected on reload.
- The Phase 29-specific fixes (F1 add-BOM-line, F3 parts.html filter, F4 callback redirect, F5 instance#1 hint) are verified on the live site.
- The 2 Phase 28 deferred items are explicitly acknowledged out-of-scope; UAT walked instance #1 for the happy path.
- E2E_UAT + PersistenceVerify requirements met; SUMMARY captures the full UAT report with psql evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/29-ui-workflow-e2e-uat-fixes/29-03-SUMMARY.md` — the UAT report described in Task 3 (7-flow PASS/FAIL table, per-mutation psql evidence, deploy verification with CloudFront invalidation ID, audit result, backend redeploy decision, Phase 28 deferred-items acknowledgement, any FAIL with disposition).
</output>
