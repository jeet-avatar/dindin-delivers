---
phase: 29-ui-workflow-e2e-uat-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs
  - /Users/jeet/turion-satellite/backend/test/audit-satellite-buttons.test.ts
  - /Users/jeet/turion-space-demo/satellite/parts.html
  - /Users/jeet/turion-space-demo/satellite/instance.html
  - /Users/jeet/turion-space-demo/scripts/audit-satellite-buttons.mjs
autonomous: true
requirements: [ButtonAudit, EndpointCoverage]

must_haves:
  truths:
    - "A static audit script parses every satellite/*.html and asserts every satelliteApi.{get,post,patch} path resolves to a real route in backend/src/app.ts's mount tree"
    - "The audit script asserts every onclick= attribute references an allowlisted built-in or a function defined in the same file (zero dead handlers)"
    - "The audit runs as a Vitest case in turion-satellite/backend and fails the suite on any mismatch"
    - "auth/callback.html is reviewed and confirmed to redirect to /satellite/ (or the originating page) after the Supabase magic-link exchange, with a sensible error on a bad/expired link"
    - "Clicking cost.html 'View parts ->' (which carries ?subsystem=&sat=) lands parts.html with #subFilter pre-set to that subsystem and the table already filtered"
    - "On instance.html, when instance_index > 1 and the instance has no work orders, a one-line hint links to instance #1 instead of showing a bare empty panel"
  artifacts:
    - path: "/Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs"
      provides: "Static button/endpoint audit (parses HTML onclick + satelliteApi calls, parses app.ts mount tree, exits non-zero on mismatch)"
      min_lines: 80
    - path: "/Users/jeet/turion-satellite/backend/test/audit-satellite-buttons.test.ts"
      provides: "Vitest case that runs the audit and asserts zero violations"
      min_lines: 10
    - path: "/Users/jeet/turion-space-demo/satellite/parts.html"
      provides: "Parts catalog page that honors ?subsystem= and ?search= URL params on load"
      contains: "getQueryParam"
    - path: "/Users/jeet/turion-space-demo/satellite/instance.html"
      provides: "Instance detail page with instance_index>1 'tracked on instance #1' hint when no WOs"
      contains: "instance_index"
  key_links:
    - from: "/Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs"
      to: "/Users/jeet/turion-satellite/backend/src/app.ts"
      via: "parses app.use('/api/...', router) + router.{get,post,patch} + nested router.use('/:x', sub)"
      pattern: "app\\.ts"
    - from: "/Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs"
      to: "/Users/jeet/turion-space-demo/satellite"
      via: "reads ../../turion-space-demo/satellite/*.html (path resolved relative to repo, or via SATELLITE_DIR env)"
      pattern: "satellite"
    - from: "/Users/jeet/turion-satellite/backend/test/audit-satellite-buttons.test.ts"
      to: "/Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs"
      via: "imports + invokes the audit, asserts result.violations.length === 0"
      pattern: "audit-satellite-buttons"
    - from: "/Users/jeet/turion-space-demo/satellite/parts.html"
      to: "window.satelliteRender.getQueryParam"
      via: "reads ?subsystem= and ?search= on load, sets #subFilter.value / #searchInput.value BEFORE first load()"
      pattern: "getQueryParam\\(['\"]subsystem"
---

<objective>
Make the satellite UI's button/endpoint correctness permanent (a static audit script + a Vitest case that fails CI on any dead button or missing endpoint), close the four mechanical polish gaps the Phase 29 research surfaced that don't need backend changes (F3 parts.html URL params, F4 auth/callback.html review, F5 instance.html instance_index>1 hint), and leave the audit wired so a future regression is caught.

Purpose: The Phase 29 ROADMAP goal is "every interactive button persists + reflects on reload; catch and fix dead buttons + missing endpoints." Research found ZERO dead buttons / missing endpoints today — so the durable deliverable is the *guard* (audit script) plus the small UX papercuts. F1 (add-BOM-line modal) and F2 (document the /api/integration/sync-* endpoints) are handled in Plan 29-02; the live UAT is Plan 29-03.

Output:
- `turion-satellite/backend/scripts/audit-satellite-buttons.mjs` — dependency-free static analyzer
- `turion-satellite/backend/test/audit-satellite-buttons.test.ts` — Vitest case running it
- `turion-space-demo/scripts/audit-satellite-buttons.mjs` — thin wrapper so `npm run audit-buttons` works in the frontend repo (re-uses / re-implements the same logic; SINGLE source of truth lives in turion-satellite/backend, frontend wrapper just shells out or duplicates the ~80 lines — pick whichever is simpler given the two repos are siblings)
- `turion-space-demo/satellite/parts.html` — honors `?subsystem=`/`?search=`
- `turion-space-demo/satellite/instance.html` — instance#1 hint
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-ui-workflow-e2e-uat-fixes/29-RESEARCH.md
@.planning/phases/28-full-bom-densification-data-coverage-drill-down-ui/deferred-items.md

# Key source files (read these before editing):
# /Users/jeet/turion-satellite/backend/src/app.ts          — route mount tree (source of truth for "endpoint exists")
# /Users/jeet/turion-satellite/backend/src/routes/*.ts     — router.{get,post,patch} + nested router.use
# /Users/jeet/turion-satellite/backend/package.json         — confirm Vitest config / test glob
# /Users/jeet/turion-space-demo/satellite/parts.html        — current load() reads #subFilter in-DOM only
# /Users/jeet/turion-space-demo/satellite/instance.html     — current WO panel; has `siblings` array + inst.instance_index
# /Users/jeet/turion-space-demo/satellite/cost.html         — emits parts.html?subsystem=&sat= links (F3 target consumer)
# /Users/jeet/turion-space-demo/satellite/cost-render.js    — renderRollupRow builds the same parts.html?subsystem= link
# /Users/jeet/turion-space-demo/satellite/auth/callback.html — F4: read it, confirm exchange+redirect
# /Users/jeet/turion-space-demo/satellite/satellite-render.js — getQueryParam, escapeHtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the static button/endpoint audit script + Vitest case</name>
  <files>/Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs, /Users/jeet/turion-satellite/backend/test/audit-satellite-buttons.test.ts</files>
  <action>
Create `/Users/jeet/turion-satellite/backend/scripts/audit-satellite-buttons.mjs` — a dependency-free Node ESM script (Node 20, no npm install). It must:

1. **Locate the satellite HTML dir.** Default to `path.resolve(import.meta.dirname, '../../../turion-space-demo/satellite')` (the two repos are siblings under `/Users/jeet/`). Allow override via `process.env.SATELLITE_DIR`. If the dir doesn't exist, print a clear message and exit 2.

2. **Build the route allowlist FROM `backend/src/app.ts`** — never hand-maintain it. Parse:
   - `app.use('/api/...', someRouter)` lines in `app.ts` → maps a mount prefix to a router variable; resolve the variable to its `import ... from './routes/X'` file.
   - In each router file: `router.(get|post|patch|put|delete)('<path>', ...)` → a route at `<mountPrefix><path>`.
   - Nested mounts: `router.use('/:x', subRouter)` inside a router file → recurse, mount prefix becomes `<mountPrefix>/:x`.
   - Normalize: collapse `//` → `/`, strip trailing `/` (except root), keep `:param` segments literal.
   Output a Set of normalized route strings like `POST /api/satellites/:satId/instances`.

3. **Scan each `satellite/*.html` (recursively, including `auth/`)** for:
   - **a) `onclick="..."` attributes** — extract the JS snippet. Assert it is EITHER (i) a call to an allowlisted built-in/global: `location.reload()`, `location.href=...`, `history.back()`, `window.satelliteAuth.signOut()` (or `.signInWithMagicLink`), `document.getElementById('...').remove()`, `dispatchEvent(new Event('change'))`, `event.preventDefault()`; OR (ii) a call to a function whose definition (`function NAME(` or `const NAME = (` / `NAME = async (` / `window.NAME =`) appears in a `<script>` block in the SAME file. Anything else → a violation `{file, kind:'dead-onclick', snippet}`.
   - **b) `satelliteApi.(get|post|patch)( <string-literal-or-template> ...)` calls** — extract the path literal (handle backticks with `${...}` interpolations: replace each `${...}` with `:X` so `/api/satellites/${satId}/instances` → `/api/satellites/:satId/instances`; handle string concatenation by treating `+ ... +` boundaries as `:X` too — best-effort). Also handle `window.satelliteApi.` prefix. Normalize the same way as routes. Determine the HTTP method from the `.get/.post/.patch` call. Assert the `METHOD path` (with `:param` matched positionally against the route's `:param` slots — i.e. compare segment-by-segment, treating any `:foo` in either side as a wildcard) is present in the allowlist Set. Miss → violation `{file, kind:'missing-endpoint', method, path}`.

4. **Return `{ routes: [...], onclickCount, apiCallCount, violations: [...] }`** as a default-exported `async function auditSatelliteButtons({satelliteDir?, backendSrcDir?} = {})`. ALSO support running as a CLI: if `import.meta.url === \`file://${process.argv[1]}\``, call it, `console.log` a summary, and `process.exit(violations.length ? 1 : 0)`.

5. **Keep it regex-based** — a full HTML/JS parser is overkill. Comment the key regexes. ~80-150 lines is the right size.

Add to `/Users/jeet/turion-satellite/backend/package.json` scripts: `"audit-buttons": "node scripts/audit-satellite-buttons.mjs"`.

Then create `/Users/jeet/turion-satellite/backend/test/audit-satellite-buttons.test.ts` (match the repo's existing Vitest test location — check where the other `*.test.ts` files live; the prompt context says `backend/test/` but VERIFY and use the actual glob). It must:
```ts
import { describe, it, expect } from 'vitest';
import auditSatelliteButtons from '../scripts/audit-satellite-buttons.mjs';
describe('satellite button/endpoint audit', () => {
  it('has zero dead onclick handlers and zero missing-endpoint calls', async () => {
    const r = await auditSatelliteButtons();
    if (r.violations.length) console.error(JSON.stringify(r.violations, null, 2));
    expect(r.violations).toEqual([]);
    expect(r.routes.length).toBeGreaterThan(20);    // sanity: app.ts parsed
    expect(r.apiCallCount).toBeGreaterThan(10);      // sanity: satellite HTML found
  });
});
```
If the test can't find the sibling `turion-space-demo/satellite` dir in CI (it can't — turion-satellite/backend doesn't vendor it), make the test `it.skipIf(!fs.existsSync(...))` so CI stays green but a local `npm test` exercises it. (Document this in the SUMMARY.)
  </action>
  <verify>
`cd /Users/jeet/turion-satellite/backend && node scripts/audit-satellite-buttons.mjs` exits 0 and prints `routes: <N>` (N > 20), `violations: 0`. `cd /Users/jeet/turion-satellite/backend && npx vitest run test/audit-satellite-buttons.test.ts` passes (or skips cleanly if the sibling dir is absent). Deliberately introduce a fake `satelliteApi.post('/api/does-not-exist')` in a scratch copy of a satellite HTML file via `SATELLITE_DIR=/tmp/scratch node scripts/audit-satellite-buttons.mjs` and confirm it exits 1 with a `missing-endpoint` violation (then delete the scratch dir).
  </verify>
  <done>The audit script exists, parses app.ts's mount tree into >20 routes, scans every satellite/*.html, reports 0 violations against the current codebase, exits non-zero on an injected fake endpoint, and a Vitest case wraps it. `npm run audit-buttons` works in turion-satellite/backend.</done>
</task>

<task type="auto">
  <name>Task 2: F4 auth/callback.html review + F3 parts.html URL-param honoring + frontend audit wrapper</name>
  <files>/Users/jeet/turion-space-demo/satellite/parts.html, /Users/jeet/turion-space-demo/scripts/audit-satellite-buttons.mjs</files>
  <action>
**F4 — auth/callback.html review.** Read `/Users/jeet/turion-space-demo/satellite/auth/callback.html`. Confirm: (a) it runs the Supabase magic-link exchange (`detectSessionInUrl` / `getSessionFromUrl` / `exchangeCodeForSession` — whatever pattern is there), (b) on success it redirects to `/satellite/` (or to a `redirect_to`/`next` query param if one is carried — preserve that if present, fall back to `/satellite/`), (c) on a bad/expired link it shows a human-readable error (not a blank page or a raw exception) with a link back to `login.html`. If ALL three already hold → no code change; record the finding in the SUMMARY. If any is missing → make the minimal fix (e.g. wrap the exchange in try/catch, render an error div with a "Back to sign in" link; redirect to `/satellite/` on success). Do NOT rewrite the file or change the Supabase call style.

**F3 — parts.html honors ?subsystem= / ?search=.** In `/Users/jeet/turion-space-demo/satellite/parts.html`, find where the page initializes (the bit that sets up `#subFilter`, `#searchInput`, and calls `load()` for the first time). BEFORE that first `load()`:
```js
const r = window.satelliteRender;
const qSub = r.getQueryParam('subsystem');   // cost.html / cost-render.js emit this
const qSearch = r.getQueryParam('search');
if (qSub) {
  const sel = document.getElementById('subFilter');
  // only set if the option exists (subFilter is populated from GET /api/subsystems — may be async)
  // if the options are populated asynchronously, set the value AFTER they're loaded but BEFORE load()
  const setSub = () => { if ([...sel.options].some(o => o.value === qSub)) sel.value = qSub; };
  setSub();
}
if (qSearch) document.getElementById('searchInput').value = qSearch;
```
IMPORTANT ordering: if `#subFilter`'s `<option>`s come from an async `GET /api/subsystems`, you must set `sel.value = qSub` AFTER those options exist and BEFORE the first `load()` — restructure so `load()` runs after both the subsystem options are populated and the URL params are applied. Don't break the no-URL-param path (still defaults to "" = all). `?sat=` is not used by parts.html — leave it alone (it's harmless on the URL).

Also: confirm `cost.html`'s "View parts ->" link and `cost-render.js`'s `renderRollupRow` both emit `parts.html?subsystem=<code>&sat=<id>` (they do per research) — no change needed there, just verify the param NAME matches what parts.html now reads (`subsystem`). If there's a casing/name mismatch, fix parts.html to read whatever cost.html actually emits.

**Frontend audit wrapper.** Create `/Users/jeet/turion-space-demo/scripts/audit-satellite-buttons.mjs` — the simplest thing that works: either (a) `import` and re-export the turion-satellite one via `../../turion-satellite/backend/scripts/audit-satellite-buttons.mjs` with `SATELLITE_DIR` pointed at `../satellite`, or (b) if cross-repo import is awkward, copy the ~80-line audit logic verbatim with a header comment "SOURCE OF TRUTH: turion-satellite/backend/scripts/audit-satellite-buttons.mjs — keep in sync". Add `"audit-buttons": "node scripts/audit-satellite-buttons.mjs"` to `turion-space-demo/package.json` scripts (create the scripts block if absent). Make the wrapper exit non-zero on violations.
  </action>
  <verify>
Open `https://turionspace.zietra.com/satellite/parts.html?subsystem=EPS` locally-equivalent (or read the diff): on load, `#subFilter` shows EPS selected and the table is filtered to EPS parts (not the full catalog). No-param `parts.html` still shows all parts. `cd /Users/jeet/turion-space-demo && node scripts/audit-satellite-buttons.mjs` exits 0, `violations: 0`. `node --check` (after extracting the inline `<script>`) passes on parts.html and auth/callback.html if either was edited.
  </verify>
  <done>auth/callback.html reviewed (fixed if needed, finding recorded); parts.html pre-filters from ?subsystem=/?search= before the first load() while keeping the no-param path intact; `npm run audit-buttons` works in turion-space-demo and reports 0 violations.</done>
</task>

<task type="auto">
  <name>Task 3: F5 instance.html instance_index>1 hint</name>
  <files>/Users/jeet/turion-space-demo/satellite/instance.html</files>
  <action>
In `/Users/jeet/turion-space-demo/satellite/instance.html`, find where the "Work orders" panel for the instance is rendered (the bit that renders `myWos` / the work-order cards, and shows "No work orders" when the array is empty). When `inst.instance_index > 1` AND the resolved work-orders array for this instance is empty:
- Render a one-line hint INSTEAD OF (or just above) the bare "No work orders" text:
  `Manufacturing & procurement for this part are tracked on instance #1.` with a link to instance #1. You already have the `siblings` array on this page — find the sibling whose `instance_index === 1` for the same `part_definition_id` and link to `instance.html?sat=<satId>&id=<sibling.id>` (or `?inst=` — match the existing link param convention on this page, which accepts both per Phase 28-05). Use `window.satelliteRender.escapeHtml` for any interpolated text.
- If no `instance_index === 1` sibling is found in `siblings` (edge case), just show the plain hint text without a link.
- This is the ONLY change to instance.html in this plan. Do NOT touch the cost panel, the integrations panel, the subtree-rollup panel, or the `cost_breakdown` source (research Pitfall: don't "fix" instance.html's lightweight cost estimate to use /api/make-costs — that's intentional per Phase 24).
- Keep it purely client-side; no backend call. This is a documented pre-existing data state (Phase 28 deferred-items #1) — the hint just explains it to the UAT walker / demo user.
  </action>
  <verify>
Read the diff: on an `instance_index > 1` instance with no WOs, the WO panel shows the "tracked on instance #1" hint with a working link to the #1 sibling; on an `instance_index === 1` instance the panel is unchanged (real WOs or the normal "No work orders"). Extract the inline `<script>` and `node --check` it — passes. The audit script (Task 1) still reports 0 violations (the hint adds no new `onclick`/`satelliteApi` call, or if a link `onclick` is used it's `location.href=` which is allowlisted).
  </verify>
  <done>instance.html shows a one-line "tracked on instance #1" hint (linked to the #1 sibling when available) only when instance_index>1 and there are no work orders; nothing else on the page changed; inline JS still parses; audit still clean.</done>
</task>

</tasks>

<verification>
- `cd /Users/jeet/turion-satellite/backend && node scripts/audit-satellite-buttons.mjs` → exit 0, `violations: 0`, `routes: <N>` with N > 20.
- `cd /Users/jeet/turion-satellite/backend && npx vitest run test/audit-satellite-buttons.test.ts` → pass (or clean skip if sibling dir absent).
- `cd /Users/jeet/turion-space-demo && node scripts/audit-satellite-buttons.mjs` → exit 0, `violations: 0`.
- Injected fake endpoint (`SATELLITE_DIR=/tmp/scratch`) → audit exits 1 with a `missing-endpoint` violation.
- `node --check` on the extracted inline `<script>` of every edited satellite HTML page → no syntax errors.
- auth/callback.html: confirmed (or fixed to) exchange-then-redirect-to-/satellite/ with a readable error on a bad link — finding in SUMMARY.
- parts.html: `?subsystem=EPS` pre-selects EPS in `#subFilter` and filters the table; no-param path still shows all.
- instance.html: instance_index>1 + no WOs → hint with link to #1; instance #1 → unchanged.
- No backend route added (audit allowlist is derived, not extended).
</verification>

<success_criteria>
- A dependency-free static audit script lives in `turion-satellite/backend/scripts/audit-satellite-buttons.mjs`, derives its route allowlist from `app.ts`, scans every `satellite/*.html`, reports 0 violations today, and exits non-zero on a fabricated dead button / missing endpoint.
- A Vitest case (`turion-satellite/backend/test/audit-satellite-buttons.test.ts`) runs the audit and is wired into the suite (skips cleanly in CI if the frontend repo isn't checked out alongside).
- `npm run audit-buttons` works in BOTH repos.
- F3: cost.html "View parts ->" lands on a correctly-filtered parts.html.
- F4: auth/callback.html confirmed/fixed (exchange + redirect + readable error).
- F5: instance.html explains the instance_index>1 empty-WO state with a link to instance #1.
- Zero backend route changes; ButtonAudit + EndpointCoverage requirements met.
</success_criteria>

<output>
After completion, create `.planning/phases/29-ui-workflow-e2e-uat-fixes/29-01-SUMMARY.md` documenting: the audit script's design (how it parses app.ts, what it allowlists for onclick), the route count it found, the auth/callback.html finding (no-change vs fixed-what), the parts.html ordering fix, the instance.html hint, and any deviations (e.g. test skip-in-CI behavior, frontend wrapper = re-export vs copy).
</output>
