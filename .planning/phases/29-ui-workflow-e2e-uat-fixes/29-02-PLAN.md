---
phase: 29-ui-workflow-e2e-uat-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/turion-space-demo/satellite/bom.html
  - /Users/jeet/turion-satellite/backend/src/routes/integration.ts
autonomous: true
requirements: [ButtonAudit, EndpointCoverage]

must_haves:
  truths:
    - "bom.html has a '+ Add BOM line' button that opens a modal (parent-instance picker + child-instance picker + qty + ref-designator) and POSTs to the existing POST /api/satellites/:satId/bom route"
    - "Submitting the Add-BOM-line modal creates a bom_lines row in Postgres and the tree reflects the new line after location.reload()"
    - "The modal validates: parent != child, qty is a positive integer, both pickers populated — and shows an inline error instead of submitting on a bad input"
    - "The 4 POST /api/integration/sync-* routes are explicitly documented as API-only (batch backfill, not per-user UI) in a code comment at their definition AND noted in the Phase 29 SUMMARY — no 'Sync now' button is added"
    - "No new backend route is created — F1 reuses the existing bom.ts POST /; the integration.ts change is a comment only"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/satellite/bom.html"
      provides: "Recursive BOM tree page + an Add-BOM-line modal wired to POST /api/satellites/:satId/bom"
      contains: "satelliteApi.post"
    - path: "/Users/jeet/turion-satellite/backend/src/routes/integration.ts"
      provides: "4 sync-* routes with an API-only documentation comment block"
      contains: "API-only"
  key_links:
    - from: "/Users/jeet/turion-space-demo/satellite/bom.html"
      to: "POST /api/satellites/:satId/bom"
      via: "modal #modalSave -> window.satelliteApi.post(`/api/satellites/${satId}/bom`, {parent_part_instance_id, child_part_instance_id, quantity, reference_designator}) -> location.reload()"
      pattern: "satelliteApi\\.post\\(`?/api/satellites/.*?/bom`?"
    - from: "/Users/jeet/turion-space-demo/satellite/bom.html"
      to: "GET /api/satellites/:satId/instances"
      via: "modal populates the parent/child instance pickers from the satellite's instance list (or reuses the already-fetched tree data)"
      pattern: "/api/satellites/.*?/instances|bom/tree"
---

<objective>
Close the one "edit BOM line" gap the Phase 29 ROADMAP goal explicitly calls out: `POST /api/satellites/:satId/bom` exists (bom.ts:156, INSERT INTO bom_lines) but no UI calls it. Add a minimal "+ Add BOM line" modal to `bom.html` following the existing modal pattern (work-orders.html / sat.html / part.html) — zero backend change. And formally document the four `/api/integration/sync-*` endpoints (Phase 25-02 cross-system batch backfills) as API-only so the EndpointCoverage requirement is satisfied without bolting a per-user "Sync now" button onto a batch job.

Purpose: ROADMAP says "verify ... edit BOM line, etc." Per the locked decision, F1 is **add-only** (no edit-qty PATCH route — keeping it zero-backend-change). The integration sync routes are batch/cron-shaped, not per-user — documenting beats wiring.

Output:
- `turion-space-demo/satellite/bom.html` — `#addBomLineBtn` + modal -> `POST /api/satellites/:satId/bom` -> reload
- `turion-satellite/backend/src/routes/integration.ts` — an "API-only / batch backfill — no UI surface by design (Phase 29)" comment block above the 4 sync routes
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-ui-workflow-e2e-uat-fixes/29-RESEARCH.md

# Key source files (read before editing):
# /Users/jeet/turion-space-demo/satellite/bom.html            — current recursive <details> tree (Phase 28-04); 150->204 lines
# /Users/jeet/turion-space-demo/satellite/work-orders.html    — the modal pattern to copy (#newWoModal / #modalSave -> satelliteApi.post -> location.reload)
# /Users/jeet/turion-space-demo/satellite/sat.html            — also has the modal pattern (#addInstanceBtn / openAddInstanceModal)
# /Users/jeet/turion-space-demo/satellite/satellite-shell.css — .modal-backdrop / .modal CSS already exists (used 4x)
# /Users/jeet/turion-space-demo/satellite/satellite-render.js — escapeHtml, getQueryParam, toast
# /Users/jeet/turion-space-demo/satellite/satellite-api.js    — window.satelliteApi.{get,post}
# /Users/jeet/turion-satellite/backend/src/routes/bom.ts      — GET /api/satellites/:satId/bom (line ~1) + GET .../bom/tree + POST / (line ~156) — CONFIRM the POST body field names it expects (parent/child instance ids, qty, ref designator) and use those EXACTLY
# /Users/jeet/turion-satellite/backend/src/routes/integration.ts — the 4 sync-* routes (F2 doc target)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the "+ Add BOM line" modal to bom.html</name>
  <files>/Users/jeet/turion-space-demo/satellite/bom.html</files>
  <action>
First, read `/Users/jeet/turion-satellite/backend/src/routes/bom.ts` and find the `POST /` handler (around line 156). **Confirm the exact request-body field names it expects** — the research says "parent instance picker + child instance picker + qty + ref-designator" but the actual column/body names might be `parent_part_instance_id` / `child_part_instance_id` / `quantity` / `reference_designator` (or `parent_instance_id` / `child_instance_id` / `qty` / `ref_designator`). USE THE EXACT NAMES from the handler — do not guess. Also note any validation the handler does (e.g. does it reject parent==child? does it require quantity>0?) so the client-side validation mirrors it.

Then, in `/Users/jeet/turion-space-demo/satellite/bom.html`:
1. Add a `<button class="btn-primary" id="addBomLineBtn">+ Add BOM line</button>` to the page header area (next to the existing `#expandAll` / `#collapseAll` controls). Only show it when there's a `?sat=` param (the page already guards on `?sat=`; if `satId` is falsy, don't render the button).
2. Add `openAddBomLineModal()` following the EXACT pattern in `work-orders.html` (`document.body.insertAdjacentHTML('beforeend', '<div class="modal-backdrop" id="bomLineModal">...</div>')`, `.modal-header` with an `×` close button, `.modal-body` with the form fields, `.modal-footer` with Cancel + a `#modalSave` "Add line" button). Fields:
   - **Parent instance** `<select id="bomParent" required>` — populated from the satellite's instances. SOURCE: either (a) re-use the already-fetched BOM tree data on the page (flatten it to a list of `{id, label}` where label is e.g. `<part_code> #<instance_index> — <subsystem>`), or (b) `GET /api/satellites/${satId}/instances` fresh inside the modal opener. (a) is preferred (no extra fetch); whichever, build human-readable option labels via `r.escapeHtml`.
   - **Child instance** `<select id="bomChild" required>` — same source/list.
   - **Quantity** `<input type="number" id="bomQty" min="1" step="1" value="1" required>`.
   - **Reference designator** `<input type="text" id="bomRef" placeholder="e.g. R1, U3 (optional)">` — optional.
   - An inline error div `<div id="bomLineErr" style="color:var(--red);margin-top:8px;"></div>`.
3. Wire `#modalSave` click handler (async):
   - Validate client-side: `bomParent` and `bomChild` both set; `bomParent !== bomChild` (show "Parent and child must be different"); `Number(bomQty)` is a positive integer (show "Quantity must be a whole number ≥ 1"). On any failure, set `#bomLineErr` text and `return` — do NOT call the API.
   - Call `await window.satelliteApi.post(\`/api/satellites/${encodeURIComponent(satId)}/bom\`, { <exact field names>: ... })`.
   - On success: `location.reload()` (the tree re-fetches `GET /api/satellites/:satId/bom/tree` on load and the new line appears). Optionally show `r.toast('BOM line added')` then `setTimeout(() => location.reload(), 600)` to match the part.html/instance.html toast-then-reload idiom — pick whichever matches the rest of bom.html's idiom (bom.html currently has no toast; a plain `location.reload()` is fine).
   - On `ApiError` (the backend rejects, e.g. would create a cycle, or parent/child invalid): show `e.message` in `#bomLineErr`, leave the modal open.
4. Don't break the existing recursive tree, the `#expandAll`/`#collapseAll`, the row links, or the `?sat=` guard. Keep bom.html vanilla — no framework, no bundler. New code matches the existing modal idiom verbatim.
5. After editing, extract the inline `<script>` (Python regex, not awk — awk over-captures `<script src=...>` tags per the Phase 28 SUMMARY note) and `node --check` it.
  </action>
  <verify>
Read the diff. The `#addBomLineBtn` appears in the bom.html header only when `?sat=` is present. `openAddBomLineModal()` injects a `.modal-backdrop` with parent/child `<select>`s populated with human-readable instance labels, a qty number input, an optional ref text input, and an error div. `#modalSave` validates (parent≠child, qty≥1) BEFORE calling the API, then `satelliteApi.post(\`/api/satellites/${satId}/bom\`, {...exact field names from bom.ts...})` and `location.reload()` on success / inline error on failure. The audit script from Plan 29-01 still reports 0 violations (the new `satelliteApi.post('/api/satellites/:satId/bom')` matches the real `bom.ts POST /` route; the new `onclick` on the close/cancel buttons is `document.getElementById('bomLineModal').remove()` which is allowlisted). `node --check` on the extracted inline script passes.
  </verify>
  <done>bom.html has a working "+ Add BOM line" modal that validates input, POSTs to the existing POST /api/satellites/:satId/bom with the handler's exact field names, and reloads the tree showing the new line; nothing else on the page changed; inline JS parses; audit clean.</done>
</task>

<task type="auto">
  <name>Task 2: Document the /api/integration/sync-* routes as API-only</name>
  <files>/Users/jeet/turion-satellite/backend/src/routes/integration.ts</files>
  <action>
In `/Users/jeet/turion-satellite/backend/src/routes/integration.ts`, add a clear block comment above the four sync route definitions (`POST /sync-sales-order/:salesOrderId`, `POST /sync-ns-invoice/:invoiceId`, `POST /sync-arena-doc`, `POST /sync-mes-work-order` — around lines 37 / 153 / 241 / 329 per research). Something like:

```ts
/**
 * Cross-system sync endpoints (Phase 25-02).
 *
 * API-ONLY by design — these are batch-backfill operations (populate the cross-system
 * FK columns on part_instances / vendor_orders from the legacy `turion` schema), NOT
 * per-user actions. There is intentionally NO "Sync now" button in the satellite UI:
 * the satellite pages only *display* the cross-system FKs (renderIntegrationsPanel).
 * Invoke these from a cron/admin job or curl. (Decided in Phase 29 — EndpointCoverage:
 * "surface backend routes with no UI → wire or document"; chose: document.)
 *
 * Note: `ns_invoice_id` is NULL on all SAT-003 instances today (Phase 26-04 never wired
 * NetSuite invoices) — see .planning/phases/28-*/deferred-items.md #2.
 */
```
Place ONE such block above the router (or a one-liner above each route if the routes are far apart — but one block at the top of the file is cleanest). Do NOT change any route behavior, signature, or auth. This is a comment-only change. Confirm `tsc` still compiles clean (`cd /Users/jeet/turion-satellite/backend && npx tsc --noEmit`).
  </action>
  <verify>
`grep -n "API-ONLY by design" /Users/jeet/turion-satellite/backend/src/routes/integration.ts` shows the comment. `cd /Users/jeet/turion-satellite/backend && npx tsc --noEmit` exits 0 (no new errors). `git diff --stat` shows only comment lines added to integration.ts (no logic changes). The audit script from Plan 29-01 is unaffected (no route added/removed).
  </verify>
  <done>integration.ts carries an "API-only / batch backfill — no UI by design (Phase 29)" comment block above the 4 sync routes; tsc clean; zero behavior change.</done>
</task>

</tasks>

<verification>
- `cd /Users/jeet/turion-space-demo && node scripts/audit-satellite-buttons.mjs` (the wrapper from Plan 29-01) → exit 0, `violations: 0` — the new bom.html `satelliteApi.post('/api/satellites/:satId/bom')` resolves to a real route.
- `node --check` on the extracted inline `<script>` of bom.html → no syntax errors.
- `grep -n "API-ONLY by design" /Users/jeet/turion-satellite/backend/src/routes/integration.ts` → found.
- `cd /Users/jeet/turion-satellite/backend && npx tsc --noEmit` → exit 0.
- The Add-BOM-line modal's POST body field names match `bom.ts`'s `POST /` handler exactly (verified by reading the handler, not guessing).
- No new backend route; the integration.ts change is comment-only.
- (Live persistence proof of an actual Add-BOM-line submission is part of Plan 29-03's UAT — this plan only ships the code.)
</verification>

<success_criteria>
- bom.html has a "+ Add BOM line" affordance (button + modal) that validates input client-side, POSTs to the existing `POST /api/satellites/:satId/bom`, and reloads the tree to show the new line — zero backend change.
- The 4 `/api/integration/sync-*` routes are explicitly documented in `integration.ts` as API-only batch backfills with no UI surface by design — satisfying EndpointCoverage's "wire or document" without adding a per-user button.
- tsc clean; audit script (from 29-01) still reports 0 violations; bom.html inline JS parses.
- ButtonAudit + EndpointCoverage requirements met for the BOM-line and integration-sync gaps.
</success_criteria>

<output>
After completion, create `.planning/phases/29-ui-workflow-e2e-uat-fixes/29-02-SUMMARY.md` documenting: the exact `POST /api/satellites/:satId/bom` body field names used (as read from bom.ts), the modal's validation rules, the option-label format for the instance pickers, the integration.ts comment block, and any deviations (e.g. if the bom.ts handler validates differently than expected).
</output>
