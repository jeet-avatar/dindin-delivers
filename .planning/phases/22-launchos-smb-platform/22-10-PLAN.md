---
phase: 22-launchos-smb-platform
plan: 10
type: execute
wave: 3
depends_on:
  - 22-09
files_modified:
  - apps/techcloudpro/src/pages/LaunchOS.tsx
  - apps/techcloudpro/src/App.tsx
  - apps/techcloudpro/scripts/prerender.mjs
autonomous: true
requirements:
  - LOS-09
  - LOS-10

must_haves:
  truths:
    - "techcloudpro.com/launchos renders the LaunchOS landing page"
    - "Page has hero section with tagline and primary CTA linking to BrandMonkz signup"
    - "Automation flow section visually shows the DAY 1 → WEEK 1 → WEEK 2 → ONGOING → CLOSE journey"
    - "Pricing section shows 3 tiers (Starter $79, Growth $149, Scale $249) with feature lists"
    - "Consolidation calculator is embedded in the pricing section"
    - "Page pre-renders to static HTML (appears in dist/) before Hostinger deploy"
    - "Page deploys to Hostinger at techcloudpro.com/launchos"
  artifacts:
    - path: "apps/techcloudpro/src/pages/LaunchOS.tsx"
      provides: "Full LaunchOS landing page"
      min_lines: 150
    - path: "apps/techcloudpro/src/App.tsx"
      provides: "/launchos route registration"
      contains: "LaunchOS"
    - path: "apps/techcloudpro/scripts/prerender.mjs"
      provides: "/launchos in prerender list"
      contains: "/launchos"
  key_links:
    - from: "apps/techcloudpro/src/pages/LaunchOS.tsx"
      to: "apps/techcloudpro/src/components/launchos/ConsolidationCalculator.tsx"
      via: "import and render ConsolidationCalculator in pricing section"
      pattern: "ConsolidationCalculator"
    - from: "apps/techcloudpro/scripts/prerender.mjs"
      to: "apps/techcloudpro/dist/launchos/index.html"
      via: "npm run build triggers prerender, creates static HTML for /launchos"
      pattern: "/launchos"
---

<objective>
Build and deploy the LaunchOS landing page at techcloudpro.com/launchos — the primary acquisition channel for LaunchOS. This integrates the consolidation calculator from 22-09 into a full-page marketing experience.

Purpose: The public face of LaunchOS. Everything points here — SEO, ads, word of mouth. Must be persuasive, fast (static HTML), and have a clear CTA.

Output:
- LaunchOS.tsx: hero, automation flow, competitive comparison, pricing tiers, calculator, footer CTA
- Route added to App.tsx
- /launchos added to prerender list
- Deployed to Hostinger via scp
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-launchos-smb-platform/22-RESEARCH.md
@docs/superpowers/specs/2026-04-04-techcloudpro-launchos-design.md
@apps/techcloudpro/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: LaunchOS landing page — all sections</name>
  <files>
    apps/techcloudpro/src/pages/LaunchOS.tsx
  </files>
  <action>
Read `apps/techcloudpro/src/App.tsx` first to understand the project's existing page structure, layout components, and Tailwind class patterns used across the site. Match the design language.

Create `apps/techcloudpro/src/pages/LaunchOS.tsx`. The page has 6 sections:

**Section 1 — Hero:**
```tsx
<section className="min-h-screen bg-gradient-to-br from-slate-900 via-blue-950 to-slate-900 text-white flex flex-col items-center justify-center text-center px-4 py-24">
  <div className="inline-flex items-center bg-blue-500/20 border border-blue-400/30 rounded-full px-4 py-2 text-sm text-blue-300 mb-6">
    Sub-product of TechCloudPro
  </div>
  <h1 className="text-5xl md:text-7xl font-black mb-6 max-w-4xl leading-tight">
    Your entire marketing team.<br />
    <span className="text-blue-400">One platform. One price.</span>
  </h1>
  <p className="text-xl text-slate-300 max-w-2xl mb-10">
    LaunchOS replaces 5–6 disconnected SaaS tools with a single integrated system that autonomously executes 
    your full go-to-market stack: CRM, AI video, social publishing, email campaigns, and video meetings.
  </p>
  <div className="flex flex-col sm:flex-row gap-4">
    <a href="https://brandmonkz.com/signup?utm_source=launchos&utm_medium=hero"
       className="bg-blue-500 hover:bg-blue-400 text-white font-bold px-8 py-4 rounded-xl text-lg transition-colors">
      Start Free 30-Day Trial →
    </a>
    <a href="#pricing"
       className="border border-white/30 text-white font-semibold px-8 py-4 rounded-xl text-lg hover:bg-white/5 transition-colors">
      See Pricing
    </a>
  </div>
  <p className="text-slate-400 text-sm mt-4">No credit card required. Cancel anytime.</p>
</section>
```

**Section 2 — The "aha moment" story:**
```tsx
<section className="py-20 px-4 bg-slate-50">
  <div className="max-w-4xl mx-auto">
    <h2 className="text-3xl font-bold text-center mb-4">The Aha Moment</h2>
    <div className="bg-white rounded-2xl p-8 shadow-lg border-l-4 border-blue-500">
      <p className="text-xl text-gray-700 italic leading-relaxed">
        "I wrote one paragraph about my new service. LaunchOS drafted a 5-email campaign, 
        generated a 60-second marketing video, posted it to LinkedIn and Instagram, emailed 
        my 800 contacts with personalised follow-ups, scored the leads who opened it, and 
        booked three discovery calls — while I slept."
      </p>
    </div>
  </div>
</section>
```

**Section 3 — Automation flow (from spec §5):**
Show the 5-stage flow (DAY 1 ONBOARD → WEEK 1 CREATE → WEEK 2 LAUNCH → ONGOING FOLLOW-UP → CLOSE) as horizontal steps on desktop, vertical on mobile. Each step has an icon, title, and 2-3 bullet points from the spec.

**Section 4 — Competitive comparison table:**
Use the table from spec §3. HTML table with LaunchOS highlighted column. Rows: AI Video, AI Workers, Native Meetings, SEO, Price. Show checkmarks (✓) and crosses (✗) per competitor.

**Section 5 — Pricing (3 tiers + calculator):**
```tsx
<section id="pricing" className="py-20 px-4 bg-white">
  <div className="max-w-6xl mx-auto">
    <h2 className="text-3xl font-bold text-center mb-4">Simple Pricing</h2>
    <p className="text-center text-gray-500 mb-12">Replaces $231–$341/month in tools. 30-day free trial.</p>
    
    {/* 3-column pricing cards */}
    <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-16">
      {/* Starter $79 */}
      {/* Growth $149 — highlighted as Most Popular */}
      {/* Scale $249 */}
    </div>
    
    {/* Consolidation calculator */}
    <h3 className="text-2xl font-bold text-center mb-8">Calculate Your Savings</h3>
    <ConsolidationCalculator />
  </div>
</section>
```

For each pricing tier, include: price, billing period, "Replaces: X + Y = $Z" line, key feature list from spec §6, and CTA button linking to `https://brandmonkz.com/signup?plan={tier}&utm_source=launchos`.

**Section 6 — Footer CTA:**
```tsx
<section className="bg-blue-600 text-white py-20 px-4 text-center">
  <h2 className="text-4xl font-bold mb-4">Ready to replace 5 tools with one?</h2>
  <p className="text-blue-100 text-lg mb-8">Join businesses saving $93+ per month with LaunchOS.</p>
  <a href="https://brandmonkz.com/signup?utm_source=launchos&utm_medium=footer"
     className="bg-white text-blue-600 font-bold px-10 py-4 rounded-xl text-lg hover:bg-blue-50 transition-colors">
    Start Free 30-Day Trial
  </a>
  <p className="text-blue-200 text-sm mt-4">No credit card. No commitment. Full access for 30 days.</p>
</section>
```

Import ConsolidationCalculator in the file:
```tsx
import ConsolidationCalculator from '../components/launchos/ConsolidationCalculator';
```
  </action>
  <verify>
```bash
# Build check
cd /Users/jeet/doordash-p2p/apps/techcloudpro && npm run build 2>&1 | tail -5
# Expect: no errors (LaunchOS.tsx must be valid TSX)

# Grep: ConsolidationCalculator imported and used
grep -n "ConsolidationCalculator" apps/techcloudpro/src/pages/LaunchOS.tsx

# Grep: all 3 pricing tiers
grep -n "\$79\|\$149\|\$249\|Starter\|Growth\|Scale" \
  apps/techcloudpro/src/pages/LaunchOS.tsx

# Grep: CTA links point to brandmonkz.com
grep -n "brandmonkz.com/signup" apps/techcloudpro/src/pages/LaunchOS.tsx
```
  </verify>
  <done>LaunchOS.tsx has all 6 sections. ConsolidationCalculator embedded in pricing. Hero CTA, all 3 tier CTAs, and footer CTA link to brandmonkz.com/signup with UTM params. TechCloudPro npm run build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire route, prerender, and deploy to Hostinger</name>
  <files>
    apps/techcloudpro/src/App.tsx
    apps/techcloudpro/scripts/prerender.mjs
  </files>
  <action>
**Step 1: Read App.tsx** — understand existing lazy import pattern and Routes structure.

**Add to `apps/techcloudpro/src/App.tsx`:**

Following the existing lazy import pattern (from RESEARCH.md Pattern 6):
```tsx
const LaunchOS = lazy(() => import('./pages/LaunchOS'));
// Inside Routes block:
<Route path="/launchos" element={<Suspense fallback={<div />}><LaunchOS /></Suspense>} />
```

**Step 2: Read `apps/techcloudpro/scripts/prerender.mjs`** — understand existing prerender entries.

Add `/launchos` to the prerender route list. The existing format should be an array of routes — add `/launchos` to the list (check exact format and follow it precisely).

**Step 3: Build and deploy:**
```bash
cd /Users/jeet/doordash-p2p/apps/techcloudpro

# Build (triggers prerender via vite-plugin-ssr or similar — check existing build script)
npm run build

# Verify /launchos was pre-rendered
ls dist/launchos/index.html || ls dist/launchos.html
# Expect: file exists

# Check file has actual content (not empty shell)
wc -l dist/launchos/index.html 2>/dev/null || wc -l dist/launchos.html 2>/dev/null

# Deploy to Hostinger (from RESEARCH.md Pattern 6 + MEMORY.md)
scp -P 65002 -r dist/* \
  u350621741@147.93.101.51:/home/u350621741/domains/techcloudpro.com/public_html/
```

**Step 4: Verify live:**
```bash
curl -s -o /dev/null -w "%{http_code}" https://techcloudpro.com/launchos
# Expect: 200

curl -s https://techcloudpro.com/launchos | grep -i "LaunchOS\|marketing team\|One platform"
# Expect: content from landing page
```

**Important:** TechCloudPro has no backend — the CTA buttons redirect to brandmonkz.com (external). The calculator is entirely client-side. No PHP changes needed.
  </action>
  <verify>
```bash
# Route registered in App.tsx
grep -n "LaunchOS\|launchos" apps/techcloudpro/src/App.tsx

# Prerender includes /launchos
grep -n "launchos" apps/techcloudpro/scripts/prerender.mjs

# Build passes
cd /Users/jeet/doordash-p2p/apps/techcloudpro && npm run build 2>&1 | tail -3

# Hostinger live check
curl -s -o /dev/null -w "%{http_code}" https://techcloudpro.com/launchos
# Expect: 200

# Content check
curl -s https://techcloudpro.com/launchos | grep -c "LaunchOS"
# Expect: >= 3 (appears multiple times in the page)
```
  </verify>
  <done>/launchos route lazy-imported and registered in App.tsx. Prerender includes /launchos entry. Static HTML deployed to Hostinger. Live URL returns 200 with LaunchOS content.</done>
</task>

</tasks>

<verification>
```bash
# Full deployment verification
curl -v https://techcloudpro.com/launchos 2>&1 | grep -E "< HTTP|< Content"
# Expect: 200 OK, Content-Type: text/html

# Page contains expected content
curl -s https://techcloudpro.com/launchos | grep -E "LaunchOS|Free Trial|149|ConsolidationCalculator" | head -5

# Check calculator embedded (source mentions the component)
grep -n "ConsolidationCalculator" apps/techcloudpro/src/pages/LaunchOS.tsx
```
</verification>

<success_criteria>
- techcloudpro.com/launchos returns 200 with LaunchOS content
- Page has all 6 sections: hero, aha moment, automation flow, comparison table, pricing, footer CTA
- All 3 pricing tiers shown with correct prices ($79/$149/$249)
- ConsolidationCalculator component embedded in pricing section
- All CTAs link to brandmonkz.com/signup with UTM params
- Pre-rendered static HTML in dist/launchos/
- App.tsx has lazy import for LaunchOS
- prerender.mjs has /launchos entry
</success_criteria>

<output>
After completion, create `.planning/phases/22-launchos-smb-platform/22-10-SUMMARY.md`
</output>
