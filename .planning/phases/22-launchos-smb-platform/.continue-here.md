---
phase: 22-launchos-smb-platform
task: 0
total_tasks: 11
status: replanning
last_updated: 2026-04-16T05:29:50.231Z
---

<current_state>
Major strategic pivot decided this session. Phase 22 was originally called "LaunchOS SMB Platform" 
but the product has been renamed to **Zietra** (using zietra.com domain, already owned). 
The architecture has been fully debated and decided — we are NOT starting from scratch, 
we are stitching the best of existing products into one unified Zietra platform.

No Phase 22 tasks have been executed yet. The landing page (22-09, 22-10) and consolidation 
calculator were built on techcloudpro.com/launchos but need to be updated to say "Zietra" 
and point to zietra.com.
</current_state>

<completed_work>

### This session (pre-execution, design & decisions):
- ✅ Built ConsolidationCalculator.tsx (apps/techcloudpro/src/components/launchos/)
- ✅ LaunchOS landing page live at techcloudpro.com/launchos (22-09, 22-10 done)
- ✅ Fixed free trial messaging → "CRM free / card for AI features"
- ✅ Audited full existing infrastructure — what's live, what's built, what's missing
- ✅ Decided product name: **Zietra** (was LaunchOS)
- ✅ Decided architecture: Option B — unified frontend, shared auth, separate backends
- ✅ Confirmed domain: zietra.com (GoDaddy, owned)
- ✅ Confirmed all existing running services

### Existing infrastructure confirmed running:
- BrandMonkz CRM → EC2 100.24.213.224 (t3.medium) — PostgreSQL, Node.js/Express
- SocialFlow → EC2 54.81.246.138 (t3.2xlarge) — MongoDB, Python/FastAPI
- ArthaBuild → EC2 44.194.34.223 (g5.xlarge)
- Zietra Meet → /apps/zoom/ — built locally, NOT deployed, ECS-ready (deploy.sh exists)
- Entitlement service → apps/launchos-entitlement/ — built locally, NOT deployed
- Video render server → apps/launchos-video-server/ — built locally, NOT deployed

### IMPORTANT — BrandMonkz production incident this session:
- Accidentally deployed LaunchOS-branded frontend to brandmonkz.com ← REVERTED
- Accidentally updated email service to say "LaunchOS" ← REVERTED
- Production is fully restored — original bundle (index-DUfHtym2.js) being served
- Email service restored from March 20 backup — BrandMonkz branding intact
- Rule established: DO NOT touch brandmonkz.com production without explicit instruction
</completed_work>

<remaining_work>

### All Phase 22 plans still pending execution:
- 22-01: Entitlement service → deploy to ECS (Stripe + Redis quotas)
- 22-02: Video render server → deploy to ECS (Remotion + ElevenLabs TTS)
- 22-03: Video button in BrandMonkz campaigns → wire to render server
- 22-04: Watch-time feedback loop → video views → lead score
- 22-05: Zietra Meet → BrandMonkz deal notes (AI summary)
- 22-06: AI Strategy Bot (10-question GTM plan)
- 22-07: Unified Zietra dashboard + SSO (tab inside CRM app)
- 22-08: ActiveCampaign migration importer
- 22-11: End-to-end smoke test

### Additional work decided this session (not in original plans):
- Rename all "LaunchOS" → "Zietra" across all plan files
- Update techcloudpro.com/launchos → rebrand to Zietra
- Update ConsolidationCalculator CTA → zietra.com/signup (not brandmonkz.com)
- Set up DNS on zietra.com (GoDaddy):
  - app.zietra.com → 100.24.213.224 (Zietra CRM)
  - social.zietra.com → 54.81.246.138 (Zietra Social)
  - meet.zietra.com → TBD (Zietra Meet, once deployed)
- New EC2 NOT needed — use existing infrastructure
</remaining_work>

<decisions_made>

1. **Product name: Zietra** (not LaunchOS)
   - Why: They own zietra.com, it's their company brand (Zietra Technologies inc), cleaner
   - LaunchOS was invented name under TechCloudPro — unnecessary extra brand

2. **Architecture: Option B — Unified frontend, shared auth, separate backends**
   - NOT a hub/redirect model (Option A)
   - NOT a full codebase merge/rewrite (true Option B)
   - Hybrid: One React frontend, one JWT across all backends, each service keeps its tech stack
   - BrandMonkz (Node.js + PostgreSQL) → Zietra CRM + Campaigns + Leads
   - SocialFlow (Python + MongoDB) → Zietra Social
   - Zietra Meet (Node.js + PostgreSQL) → Zietra Meetings

3. **Best-of-breed from each product:**
   - Lead generation → BrandMonkz (better job per user)
   - Campaigns + follow-ups → BrandMonkz (tried and tested)
   - Video creation → BrandMonkz HeyGen integration
   - Social posting → SocialFlow (better job per user, 1-click all platforms)
   - Meetings → Zietra Meet

4. **No new EC2 needed** — existing servers cover everything

5. **BrandMonkz stays internal** — never customer-facing, powers Zietra CRM behind the scenes

6. **Build order confirmed:**
   - DNS setup on zietra.com first
   - Deploy Zietra Meet to ECS (script ready)
   - Entitlement service (22-01)
   - Unified Zietra dashboard + shared auth (22-07) ← most important
   - Wire video server (22-02/03)
   - AI Strategy Bot (22-06)
   - Smoke test (22-11)
</decisions_made>

<blockers>

- **Zietra Meet IP unknown** — need to deploy to ECS first before DNS record can be added
- **SocialFlow SSH key** — don't have SSH key for 54.81.246.138, need to check what key it uses
- **zietra.com root A record** — currently points to 198.12.249.3 (old Apache placeholder, safe to replace once new EC2 or service is ready)
- **Shared auth design** — need to decide: single user DB in BrandMonkz PostgreSQL, or new auth service. This is the foundation of 22-07 and must be decided before execution.
</blockers>

<context>
The vision is clear: Zietra is one platform — CRM, social, meetings, video, AI strategy — 
all unified under zietra.com. The user has spent months building each piece separately. 
Now we stitch them together with a shared JWT and a unified React frontend.

The Phase 22 plans were written for "LaunchOS" but the architecture is identical — 
just rename everything to Zietra and use zietra.com instead of techcloudpro.com/launchos.

The 22-07 plan (unified dashboard) is the KEY deliverable — it's what makes Zietra feel 
like one product. Everything else hangs off it. Start there after DNS is set up.

The entitlement service (22-01) is the billing foundation — Stripe webhooks set tier in Redis, 
all other services check quota before executing. Must exist before any paid features go live.
</context>

<next_action>
1. Add DNS records in GoDaddy:
   - A record: app → 100.24.213.224
   - A record: social → 54.81.246.138
   
2. Deploy Zietra Meet to ECS:
   cd /Users/jeet/doordash-p2p/apps/zoom
   # Review deploy/deploy.sh then trigger via CI/CD (NOT manual docker commands)
   
3. Execute 22-01 (entitlement service):
   /gsd:execute-phase 22-01
   
4. Execute 22-07 (unified Zietra dashboard + shared auth):
   /gsd:execute-phase 22-07

Resume with: /gsd:resume-work
</next_action>
