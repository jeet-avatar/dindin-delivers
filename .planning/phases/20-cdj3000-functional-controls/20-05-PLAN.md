---
phase: 20-cdj3000-functional-controls
plan: 05
type: execute
wave: 5
depends_on: ["20-04"]
files_modified:
  - apps/mixmind/frontend/src/components/dj/DJDeck.tsx
  - apps/mixmind/frontend/src/components/DJWaveformView.tsx
autonomous: true
requirements: [CDJ-05]

must_haves:
  truths:
    - "Clicking GRID LEFT nudges all beat grid lines earlier by ~1ms visually"
    - "Clicking GRID RIGHT nudges all beat grid lines later by ~1ms visually"
    - "Beat grid offset persists while the track is loaded (resets on new track)"
    - "Waveform beat grid lines visually shift when grid is nudged"
  artifacts:
    - path: "apps/mixmind/frontend/src/components/DJWaveformView.tsx"
      provides: "Accepts gridOffsetMs prop and applies it to beat_grid time_ms values during rendering"
      contains: "gridOffsetMs"
    - path: "apps/mixmind/frontend/src/components/dj/DJDeck.tsx"
      provides: "Passes gridOffset state to DJWaveformView as gridOffsetMs prop"
      contains: "gridOffsetMs={gridOffset}"
  key_links:
    - from: "DJDeck.tsx gridOffset state"
      to: "DJWaveformView.tsx rendering"
      via: "gridOffsetMs prop applied to beat_grid time_ms during canvas draw"
      pattern: "gridOffsetMs"
---

<objective>
Wire the beat grid nudge buttons to visually shift the beat grid on the waveform display.

Purpose: `gridOffset` state is already set by the nudge buttons in DJDeck.tsx (line 157-159) but is NEVER passed to DJWaveformView. The waveform beat grid lines don't move when you click the nudge buttons.
Output: Beat grid lines on waveform shift left/right when grid nudge buttons are clicked.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/mixmind/frontend/src/components/dj/DJDeck.tsx
@apps/mixmind/frontend/src/components/DJWaveformView.tsx
@.planning/phases/20-cdj3000-functional-controls/20-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass gridOffset to DJWaveformView and apply to beat grid rendering</name>
  <files>
    apps/mixmind/frontend/src/components/dj/DJDeck.tsx
    apps/mixmind/frontend/src/components/DJWaveformView.tsx
  </files>
  <action>
**DJDeck.tsx changes:**

1. Update the DJWaveformView call to pass `gridOffsetMs`:
```tsx
<DJWaveformView
  track={track}
  currentTime={audio.currentTime}
  duration={audio.duration}
  onSeek={actions.seek}
  hotCues={hotCues.filter((c): c is HotCueEntry => c !== null)}
  gridOffsetMs={gridOffset}
/>
```

2. Reset gridOffset when track changes — add to the existing ANLZ fetch useEffect (from Plan 01):
```typescript
// Inside the track change effect, at the beginning:
setGridOffset(0);
```

**DJWaveformView.tsx changes:**

1. Add `gridOffsetMs?: number` to `DJWaveformViewProps` interface (around line 388-394):
```typescript
interface DJWaveformViewProps {
  track: Track;
  currentTime: number;
  duration: number;
  onSeek: (sec: number) => void;
  analysisVersion?: number;
  hotCues?: HotCueEntry[];
  gridOffsetMs?: number;  // beat grid nudge offset in ms
}
```

2. Destructure in function signature.

3. Find the beat grid drawing code. There are TWO places where beat grid lines are drawn:

**Overview canvas** — search for the beat grid drawing section (look for `anlz.beat_grid` iteration drawing vertical lines). The code iterates `anlz.beat_grid` and draws lines using `msToX(b.time_ms)`. Change to `msToX(b.time_ms + (gridOffsetMs ?? 0))`.

**Zoomed canvas** — same pattern, find the beat grid iteration in the zoomed canvas draw function. Apply the same offset: `msToX(b.time_ms + (gridOffsetMs ?? 0))`.

The key change is simple: everywhere that reads `b.time_ms` for beat grid rendering, add `(gridOffsetMs ?? 0)` to the value. This shifts all beat grid lines by the offset amount.

**Do NOT modify:**
- Hot cue positions (those are absolute, not grid-relative)
- Section positions
- Waveform amplitude data
- Playhead position
- Any other rendering logic

**Anti-hallucination:** Beat grid entries have fields `time_ms`, `beat` (1-4), and `bpm`. The field is `beat` NOT `beat_number`.
  </action>
  <verify>
1. `cd apps/mixmind/frontend && npx tsc --noEmit` — no TypeScript errors
2. Load a track with visible beat grid lines on the waveform
3. Click "GRID RIGHT" 5 times — beat grid lines should visibly shift right
4. Click "GRID LEFT" 10 times — beat grid lines should shift left past original position
5. Load a different track — grid offset should reset to 0
6. Hot cue triangles should NOT move when grid is nudged (they're absolute positions)
  </verify>
  <done>
Beat grid nudge buttons visually shift all beat grid lines on the waveform. Offset resets on track load. Hot cues and other elements are unaffected.
  </done>
</task>

</tasks>

<verification>
- Grid nudge buttons shift beat grid lines visually on waveform
- Offset accumulates with multiple clicks
- New track load resets offset to 0
- Hot cues stay fixed when grid is nudged
- TypeScript compiles cleanly
</verification>

<success_criteria>
Beat grid nudge is wired end-to-end from DJDeck buttons through to DJWaveformView canvas rendering. Grid lines shift. No TypeScript errors.
</success_criteria>

<output>
After completion, create `.planning/phases/20-cdj3000-functional-controls/20-05-SUMMARY.md`
</output>
