---
phase: 20-cdj3000-functional-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mixmind/frontend/src/components/dj/DJDeck.tsx
  - apps/mixmind/frontend/src/components/DJWaveformView.tsx
autonomous: true
requirements: [CDJ-06, CDJ-09]

must_haves:
  truths:
    - "Hot cues from Rekordbox appear on pads immediately when a track loads"
    - "User-set hot cues appear as colored triangles on the waveform"
    - "Tapping an occupied pad jumps to that cue position"
    - "Right-clicking a pad deletes the cue and removes its waveform marker"
  artifacts:
    - path: "apps/mixmind/frontend/src/components/dj/DJDeck.tsx"
      provides: "Hot cue loading from ANLZ data + merge with user cues"
      contains: "anlzData"
    - path: "apps/mixmind/frontend/src/components/DJWaveformView.tsx"
      provides: "Accepts external hotCues prop for user-set cues overlay"
      contains: "hotCues"
  key_links:
    - from: "DJDeck.tsx"
      to: "DJWaveformView.tsx"
      via: "hotCues prop passed to waveform for overlay rendering"
      pattern: "hotCues=\\{hotCues\\}"
    - from: "DJDeck.tsx"
      to: "/api/tracks/{content_id}/anlz"
      via: "sidecarGet fetch on track load"
      pattern: "sidecarGet.*anlz"
---

<objective>
Load hot cues from Rekordbox ANLZ data when a track is loaded, merge with user-set cues, and display all cues on the waveform.

Purpose: Currently hotCues starts as an empty array and user-set cues never appear on the waveform. This makes the performance pads useless for tracks with existing Rekordbox cues.
Output: Working hot cue pads that auto-populate from Rekordbox data and display on waveform.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/mixmind/frontend/src/components/dj/DJDeck.tsx
@apps/mixmind/frontend/src/components/DJWaveformView.tsx
@apps/mixmind/frontend/src/types/track.ts
@apps/mixmind/frontend/src/hooks/useAudioEngine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load Rekordbox hot cues on track load + pass user cues to waveform</name>
  <files>
    apps/mixmind/frontend/src/components/dj/DJDeck.tsx
    apps/mixmind/frontend/src/components/DJWaveformView.tsx
  </files>
  <action>
**DJDeck.tsx changes:**

1. Add import: `import { sidecarGet } from '../../hooks/useSidecar';` and `import { DualAnlzData } from '../../types/track';`

2. Add useEffect that fetches ANLZ data when track changes (after the existing useState declarations around line 47):
```
const [anlzData, setAnlzData] = useState<DualAnlzData | null>(null);

useEffect(() => {
  if (!track?.content_id) { setAnlzData(null); setHotCues(Array(8).fill(null)); return; }
  sidecarGet<DualAnlzData>(`/api/tracks/${track.content_id}/anlz`)
    .then(data => {
      setAnlzData(data);
      // Load hot cues from rekordbox data (or mixmind auto_cues)
      const source = data.rekordbox ?? data.mixmind;
      if (source) {
        const rbCues = ('hot_cues' in source ? source.hot_cues : []) as HotCueEntry[];
        const padState: (HotCueEntry | null)[] = Array(8).fill(null);
        for (const cue of rbCues) {
          const idx = SLOTS.indexOf(cue.slot);
          if (idx >= 0 && idx < 8) padState[idx] = cue;
        }
        setHotCues(padState);
      }
    })
    .catch(() => setAnlzData(null));
}, [track?.content_id]);
```

3. Add a `hotCues` prop to the DJWaveformView call (currently at line 254). Change from:
```
<DJWaveformView track={track} currentTime={audio.currentTime} duration={audio.duration} onSeek={actions.seek} />
```
to:
```
<DJWaveformView track={track} currentTime={audio.currentTime} duration={audio.duration} onSeek={actions.seek} hotCues={hotCues.filter((c): c is HotCueEntry => c !== null)} />
```

**DJWaveformView.tsx changes:**

1. Add `hotCues?: HotCueEntry[]` to the `DJWaveformViewProps` interface (line 388-394). This is an OPTIONAL prop -- when provided, these cues are drawn as an overlay IN ADDITION to the cues from the internally-fetched ANLZ data.

2. In the component function signature (line 396), destructure the new prop.

3. In the `drawWaveform` function (find the "Hot cue triangles" section around lines 290-294 for the overview canvas, and lines 364-368 for the zoomed canvas): AFTER drawing the ANLZ hot cues, also draw the `hotCues` prop cues. Only draw prop cues that are NOT already in `anlz.hot_cues` (to avoid double-drawing). Compare by `slot` field:
```
// User-set cues overlay (from DJDeck state)
if (hotCuesOverlay) {
  const anlzSlots = new Set(anlz.hot_cues.map(c => c.slot));
  for (const hc of hotCuesOverlay) {
    if (anlzSlots.has(hc.slot)) continue; // already drawn from ANLZ
    const x = msToX(hc.time_ms);
    ctx.fillStyle = hc.color_hex;
    ctx.beginPath(); ctx.moveTo(x, 0); ctx.lineTo(x - 5, 9); ctx.lineTo(x + 5, 9); ctx.closePath(); ctx.fill();
  }
}
```
Do this for BOTH the overview canvas draw (around line 290) and the zoomed canvas draw (around line 364).

**IMPORTANT anti-hallucination notes:**
- HotCueEntry fields are: `slot`, `time_ms`, `color_hex`, `label`, `is_loop`, `loop_out_ms` (NOT `time` or `color`)
- DualAnlzData has `rekordbox: TrackAnlzData | null` and `mixmind: MixMindAnalysis | null`
- TrackAnlzData has `hot_cues: HotCueEntry[]`
- MixMindAnalysis has `auto_cues: AutoCueEntry[]` (different type -- has `reason` field instead of `is_loop`/`loop_out_ms`)
- SLOTS constant is already defined as `['A','B','C','D','E','F','G','H']` at line 30
  </action>
  <verify>
1. `cd apps/mixmind/frontend && npx tsc --noEmit` — no TypeScript errors
2. Load a track that has Rekordbox hot cues — pads should light up with colors
3. Set a new hot cue on an empty pad — colored triangle should appear on waveform
4. Delete a hot cue via right-click — triangle should disappear from waveform
  </verify>
  <done>
Tracks with Rekordbox hot cues populate pads A-H on load. User-set cues appear as triangles on the waveform. No double-drawing of cues that exist in both ANLZ and local state.
  </done>
</task>

</tasks>

<verification>
- Load track with known Rekordbox hot cues (e.g. 3 cues on A, B, C) — all 3 pads show colored
- Set new cue on pad D — waveform shows new triangle at current position
- Delete cue B — pad B goes empty, triangle disappears from waveform
- TypeScript compiles cleanly
</verification>

<success_criteria>
Hot cues load from Rekordbox on track load. User-set cues merge into the same state. All cues visible on waveform. No TypeScript errors.
</success_criteria>

<output>
After completion, create `.planning/phases/20-cdj3000-functional-controls/20-01-SUMMARY.md`
</output>
