---
phase: 30-interactive-webgl-3d-part-viewer
plan: 03
type: execute
wave: 3
depends_on: ["30-01", "30-02"]
files_modified: []
autonomous: false
requirements: [ThreeJSViewer, WebGLFallback]

must_haves:
  truths:
    - "The F6 deploy-hygiene pre-flight runs before deploy-frontend.sh: any dirty file outside satellite/ in turion-space-demo (about-this-demo.html / agent-sales-cash.html / dashboard-cio.html / .superpowers/ / backend WIP) is git-stashed (or moved aside) so it does NOT ride along on `aws s3 sync . --delete`, then restored after"
    - "All Phase 30 satellite/ changes (satellite-3d.js, 3d-test.html, part.html, instance.html, bom.html) are committed under jm@techcloudpro.com / jeet-avatar and pushed to turion-space-demo main BEFORE the deploy"
    - "`bash deploy-frontend.sh` runs (regenerates satellite-config.js, aws s3 sync to turion-demo-static, CloudFront invalidation on E37R9PT8IL44L2) and the invalidation is polled to Status=Completed"
    - "Post-deploy smoke-check: the deployed part.html + instance.html serve the import map + reference satellite-3d.js; satellite-3d.js is reachable at /satellite/satellite-3d.js with JS content-type; bom.html serves the view=3d deep-link; 3d-test.html is reachable; the static SVG fallback path (frame-svg / cadCenter) is still present in the deployed HTML; HEAD-check the jsDelivr three@0.184.0 URLs return 200 with Access-Control-Allow-Origin: *"
    - "A human visually confirms (via the live site + /satellite/3d-test.html) that the 3D viewer renders, drag-rotates, and that all 8 part families look distinct — and that toggling to 2D shows the SVG"
  artifacts: []
  key_links:
    - from: "turion-space-demo main"
      to: "S3 turion-demo-static + CloudFront E37R9PT8IL44L2"
      via: "bash deploy-frontend.sh"
      pattern: "deploy-frontend\\.sh"
---

<objective>
Ship Phase 30 to production: run the F6 deploy-hygiene pre-flight (stash unrelated dirty WIP so it doesn't ride along on `deploy-frontend.sh`'s `aws s3 sync . --delete`), commit + push all `satellite/` changes, run `deploy-frontend.sh`, poll the CloudFront invalidation, smoke-check the deployed pages + the jsDelivr Three.js URLs, then have a human visually confirm the 3D viewer on the live site.

Purpose: This is the final wave — the only one that touches production. The Phase 29 deploy already hit the `aws s3 sync` hygiene trap (unrelated ERP-demo HTML + `.superpowers/` would ride along), so the same pre-flight is mandatory. The visual checkpoint is the requirement's real acceptance gate (the 3D viewer is inherently visual; headless smoke can only prove the wiring, not the rendering).

Output: Phase 30 live at https://turionspace.zietra.com/satellite/part.html (and instance.html, bom.html, 3d-test.html); a SUMMARY recording the deploy + smoke + visual sign-off.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-interactive-webgl-3d-part-viewer/30-RESEARCH.md
@.planning/phases/30-interactive-webgl-3d-part-viewer/30-01-SUMMARY.md
@.planning/phases/30-interactive-webgl-3d-part-viewer/30-02-SUMMARY.md

# Deploy script: /Users/jeet/turion-space-demo/deploy-frontend.sh
#   → bash scripts/generate-satellite-config.sh
#   → aws s3 sync . s3://turion-demo-static --exclude ".git/*" --exclude "backend/*" --exclude "*.md" --exclude "*.sh" --exclude "deploy-*" --include "*.html" --include "*.js" --include "*.css" --include "*.svg" --include "*.jpg" --include "*.png" --delete --region us-east-1
#   → aws cloudfront create-invalidation --distribution-id E37R9PT8IL44L2 --paths "/*"
# Commit identity: -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar"
# Known dirty-but-unrelated set in turion-space-demo (from `git status`): about-this-demo.html, agent-sales-cash.html, dashboard-cio.html, backend/* (excluded by deploy script), .superpowers/ (untracked — would ride along since it contains .html)
</context>

<tasks>

<task type="auto">
  <name>Task 1: F6 pre-flight + commit + push the Phase 30 satellite/ changes</name>
  <files>/Users/jeet/turion-space-demo/satellite/satellite-3d.js, /Users/jeet/turion-space-demo/satellite/3d-test.html, /Users/jeet/turion-space-demo/satellite/part.html, /Users/jeet/turion-space-demo/satellite/instance.html, /Users/jeet/turion-space-demo/satellite/bom.html</files>
  <action>
1. `cd /Users/jeet/turion-space-demo && git status --short` — inventory the dirty set. Phase 30's new/modified files should be: `satellite/satellite-3d.js` (new), `satellite/3d-test.html` (new), `satellite/part.html` (modified), `satellite/instance.html` (modified), `satellite/bom.html` (modified). Everything else dirty (`about-this-demo.html`, `agent-sales-cash.html`, `dashboard-cio.html`, `backend/*`, `.superpowers/`) is UNRELATED WIP.

2. Commit ONLY the Phase 30 `satellite/` files (do not `git add -A`):
   ```bash
   git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" add satellite/satellite-3d.js satellite/3d-test.html satellite/part.html satellite/instance.html satellite/bom.html
   git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" commit -m "$(cat <<'EOF'
feat(satellite): interactive WebGL 3D part viewer (Phase 30)

Add satellite/satellite-3d.js — a Three.js viewer + procedural mesh
generator (8 part families mirroring the Phase 27 dispatch, sized from
specifications.dimensions_mm, colored by the subsystem palette) loaded
via a jsDelivr import map + dynamic import (no bundler). Wire it into
part.html + instance.html (.cad-frame, default 3D when WebGL is
available, 2D/3D toggle keeping the existing SVG as the 2D/fallback
view, auto-rotate checkbox, ?view= param) and add a per-row "view in 3D"
deep-link to bom.html. New 3d-test.html standalone harness covers all
8 families. No backend route, no DB migration.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
   ```
   (If a pre-commit hook fails, fix the issue and create a NEW commit — do not `--amend`.)

3. Push: `git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" push origin main`. Confirm the push succeeded (`git log --oneline -1` shows the new commit; `git status` shows the unrelated WIP still dirty and uncommitted).

4. Pre-flight stash (do this AFTER the commit/push, BEFORE the deploy in Task 2): any file still dirty outside `satellite/` would ride along on `deploy-frontend.sh`'s `aws s3 sync` (it includes `*.html` repo-wide, only excluding `.git/`, `backend/`, `*.md`, `*.sh`, `deploy-*`). So:
   ```bash
   git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" stash push -m "phase30-deploy-preflight" -- about-this-demo.html agent-sales-cash.html dashboard-cio.html
   # (backend/* is excluded by the deploy script — no need to stash; but if backend/*.html exists and is dirty, stash it too)
   [ -d .superpowers ] && mv .superpowers /tmp/phase30-superpowers-stash
   git status --short   # confirm: only backend/* (excluded) remains dirty, or nothing
   ```
   Record exactly what was stashed/moved in the SUMMARY (Task 2 restores it).

DO NOT `git add -A` / `git commit -a`. DO NOT push the unrelated ERP-demo HTML. DO NOT skip hooks. DO NOT touch `main` on any other repo.
  </action>
  <verify>
`cd /Users/jeet/turion-space-demo && git log --oneline -1` shows the Phase 30 commit authored by `jeet-avatar <jm@techcloudpro.com>` (`git log -1 --format='%an <%ae>'`). `git show --stat HEAD` lists ONLY the 5 `satellite/` files. `git status --short` after the stash shows the unrelated HTML gone from the dirty list and `.superpowers/` no longer present (moved to `/tmp`); `backend/*` may still show (it's deploy-excluded — fine). `git log origin/main --oneline -1` matches the local HEAD (push succeeded).
  </verify>
  <done>
The 5 Phase 30 `satellite/` files are committed (jm@techcloudpro.com / jeet-avatar) and pushed to turion-space-demo main; the unrelated dirty WIP (3 ERP HTML files + `.superpowers/`) is stashed/moved aside so it won't ride the deploy; what was stashed is recorded for restoration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run deploy-frontend.sh, poll CloudFront invalidation, smoke-check deployed pages + jsDelivr URLs, restore stashed WIP</name>
  <files>(deploy + verification — no repo files modified beyond the Task-1 commit)</files>
  <action>
1. `cd /Users/jeet/turion-space-demo && bash deploy-frontend.sh` — this regenerates the gitignored `satellite-config.js`, runs `aws s3 sync . s3://turion-demo-static ... --delete`, and creates a CloudFront invalidation on `E37R9PT8IL44L2`. Capture the printed `invalidation:` ID. Confirm the `aws s3 sync` output lists `satellite/satellite-3d.js`, `satellite/3d-test.html`, `satellite/part.html`, `satellite/instance.html`, `satellite/bom.html` (uploaded) and does NOT list `about-this-demo.html` / `agent-sales-cash.html` / `dashboard-cio.html` / `.superpowers/*` (they were stashed/moved in Task 1). If any unrelated file appears in the sync output → STOP, the pre-flight was incomplete; re-stash and re-run.

2. Poll the invalidation to completion: `aws cloudfront get-invalidation --distribution-id E37R9PT8IL44L2 --id <invalidation-id> --query 'Invalidation.Status' --output text` — repeat until it prints `Completed`.

3. Smoke-check the DEPLOYED pages (curl `https://turionspace.zietra.com/satellite/...` — these are public static assets, no auth needed for the HTML/JS bytes):
   - `curl -s https://turionspace.zietra.com/satellite/satellite-3d.js | head -c 200` → contains `import * as THREE from 'three'` and `mount3DViewer`; `curl -sI https://turionspace.zietra.com/satellite/satellite-3d.js` → 200, `content-type` is `application/javascript` (or `text/javascript`).
   - `curl -s https://turionspace.zietra.com/satellite/part.html` → contains `type="importmap"`, `cdn.jsdelivr.net/npm/three@0.184.0`, `satellite-3d.js`, `viewer3d`, AND still contains `frame-svg` + `cadCenter` (the 2D/fallback SVG path intact).
   - `curl -s https://turionspace.zietra.com/satellite/instance.html` → contains `type="importmap"`, `satellite-3d.js`, `viewer3d`, AND `frame-svg` + `cadCenter`.
   - `curl -s https://turionspace.zietra.com/satellite/bom.html` → contains `view=3d` and `part.html?id=`.
   - `curl -sI https://turionspace.zietra.com/satellite/3d-test.html` → 200.
   - HEAD-check the jsDelivr Three.js URLs: `curl -sI https://cdn.jsdelivr.net/npm/three@0.184.0/build/three.module.min.js` → 200 with `access-control-allow-origin: *`; `curl -sI https://cdn.jsdelivr.net/npm/three@0.184.0/examples/jsm/controls/OrbitControls.js` → 200 with `access-control-allow-origin: *`.
   - Sanity: `curl -sI https://turionspace.zietra.com/satellite/login.html` → 200 (the rest of the satellite app wasn't broken by a stale `--delete`).

4. Restore the stashed/moved WIP (regardless of smoke outcome — leave the working tree as it was found):
   ```bash
   [ -d /tmp/phase30-superpowers-stash ] && mv /tmp/phase30-superpowers-stash .superpowers
   git -c user.email="jm@techcloudpro.com" -c user.name="jeet-avatar" stash pop   # restores about-this-demo.html / agent-sales-cash.html / dashboard-cio.html
   git status --short   # confirm the pre-Phase-30 dirty set is back
   ```
   (If `stash pop` conflicts — unlikely since nothing else touched those files — resolve in favour of the stashed content.)

DO NOT run manual `aws s3 cp` / `docker` / `aws ecs` — only `deploy-frontend.sh`. DO NOT redeploy the turion-satellite Lambda (no backend change in Phase 30). DO NOT leave the working tree in a stashed state.
  </action>
  <verify>
The `aws s3 sync` output (captured) lists the 5 `satellite/` files and NONE of the unrelated ERP HTML or `.superpowers/`. `aws cloudfront get-invalidation ... --query 'Invalidation.Status'` printed `Completed`. All curl checks pass: `satellite-3d.js` 200 + JS content-type + contains `mount3DViewer`; `part.html` & `instance.html` contain `importmap` + `satellite-3d.js` + `viewer3d` + `frame-svg` + `cadCenter`; `bom.html` contains `view=3d`; `3d-test.html` 200; jsDelivr `three.module.min.js` & `OrbitControls.js` both 200 with `access-control-allow-origin: *`; `login.html` 200. `git status --short` shows the working tree restored to its pre-deploy state (the 3 ERP HTML + `.superpowers/` back, the Phase 30 commit on `origin/main`).
  </verify>
  <done>
Phase 30 is deployed: `deploy-frontend.sh` synced the 5 `satellite/` files (nothing unrelated rode along), the CloudFront invalidation completed, all smoke-checks pass (deployed pages serve the import map + `satellite-3d.js`, the SVG fallback path is intact, jsDelivr URLs are 200 + CORS-OK), and the stashed WIP is restored.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification of the live 3D viewer</name>
  <files>(none — visual verification of the deployed site)</files>
  <action>
PAUSE for the user to visually verify the deployed 3D viewer. Present the "how to verify" steps below. Do not proceed until the user types "approved" or reports an issue.

What was built and is now live on the satellite frontend:
- `part.html` and `instance.html` show a Three.js canvas in the `.cad-frame` by default (WebGL permitting) — drag to rotate, scroll to zoom, right-drag to pan; a "view: 3D / view: 2D drawing" toggle chip flips to the existing isometric SVG; an "auto-rotate" checkbox spins the part.
- `bom.html` keeps its SVG thumbnails and has a per-row "3D" badge that deep-links to `part.html?id=<pdId>&view=3d`.
- `satellite/3d-test.html` is a standalone harness rendering one sample part per family (8 cells) — no auth needed.
- WebGL-unavailable browsers fall back to the static SVG (no toggle chip shown).
  </action>
  <verify>
The user confirms (per the steps below) that the 3D viewer renders + drag-rotates on part.html / instance.html / 3d-test.html, the 2D/3D toggle works, the auto-rotate checkbox works, the bom.html "3D" deep-link works (without also triggering the row's instance link), and the 8 families look visibly distinct.
  </verify>
  <how-to-verify>
1. Open `https://turionspace.zietra.com/satellite/3d-test.html` (no login needed). Confirm 8 cells each render a visibly DISTINCT 3D mesh: fastener = hex-head bolt (6-sided head + shaft); assembly/subassembly = box (subassembly also has tiny glowing LED dots + a connector pad); cylindrical = cylinder with seam rings (the thruster one has a flared nozzle); solar = thin panel with a cell-grid texture; lens = stacked concentric discs + dark aperture; antenna = parabolic dish + feed boom + horn; plate = thin slab. Drag in a cell — the mesh should rotate smoothly. Tick a cell's "auto-rotate" — it should start spinning.
2. Open `https://turionspace.zietra.com/satellite/part.html?id=<any part_definition_id>&sat=24587565-b15b-42ce-b590-87ecf9b6bb99` (sign in via the magic-link if prompted). The `.cad-frame` should show a rotatable 3D part by default. Click "view: 3D" -> it should switch to "view: 2D drawing" and show the old isometric SVG (with the Phase-27 BOM-child callouts if that part has children). Click again -> back to 3D. Tick "auto-rotate" -> the part spins. Append `&view=2d` to the URL and reload -> it should open in 2D (SVG) mode.
3. Open `https://turionspace.zietra.com/satellite/instance.html?inst=<any instance_id>&sat=24587565-b15b-42ce-b590-87ecf9b6bb99`. Same: 3D by default, toggle to 2D works, auto-rotate works, the `.cad-frame` is the smaller (~320px) one.
4. Open `https://turionspace.zietra.com/satellite/bom.html?sat=24587565-b15b-42ce-b590-87ecf9b6bb99`. Each tree row should have a small "3D" badge; clicking it opens `part.html` for that part in 3D mode (and does NOT also navigate via the row's instance link).
5. (Optional) In the browser devtools, disable WebGL (or use a browser without it) and reload `part.html` — it should fall back to the static SVG with no 3D toggle chip and no console crash.
  </how-to-verify>
  <done>
The user has confirmed the 3D viewer renders + rotates on all pages, the 2D toggle / auto-rotate / bom deep-link work, and the 8 families look distinct — or has reported issues that were then fixed.
  </done>
  <resume-signal>Type "approved" if the 3D viewer renders + rotates on all pages and the 2D toggle / auto-rotate / bom deep-link work, or describe the issue (e.g. "lens family looks like a box", "toggle chip overlaps the labels chip", "3D badge also triggers row navigation") so it can be fixed.</resume-signal>
</task>

</tasks>

<verification>
- F6 pre-flight ran: unrelated dirty WIP (3 ERP HTML + `.superpowers/`) stashed/moved before deploy, restored after; only the 5 `satellite/` files were committed + synced.
- Phase 30 commit pushed to turion-space-demo main under jm@techcloudpro.com / jeet-avatar.
- `deploy-frontend.sh` ran; CloudFront invalidation on E37R9PT8IL44L2 polled to `Completed`.
- Deployed-page smoke: `satellite-3d.js` reachable (JS content-type, contains `mount3DViewer`); `part.html` + `instance.html` serve the import map + reference `satellite-3d.js` + still contain the SVG fallback (`frame-svg`/`cadCenter`); `bom.html` serves the `view=3d` deep-link; `3d-test.html` reachable; jsDelivr `three@0.184.0` `three.module.min.js` + `OrbitControls.js` 200 with `access-control-allow-origin: *`; `login.html` 200 (app not broken by `--delete`).
- Human visually confirmed the 3D viewer renders + rotates on part.html / instance.html / 3d-test.html, the 2D toggle + auto-rotate work, the bom.html 3D deep-link works.
- No backend route, no DB migration, no Lambda redeploy.
</verification>

<success_criteria>
- The interactive WebGL 3D part viewer is live at https://turionspace.zietra.com/satellite/part.html (and instance.html, bom.html deep-link, 3d-test.html).
- The static SVG is preserved as the 2D / WebGL-fallback view; the Phase-27 callouts still work on the 2D view.
- The deploy carried only Phase 30's `satellite/` changes — no unrelated ERP-demo WIP rode along.
- jsDelivr Three.js URLs are 200 + CORS-OK from the CloudFront origin.
- A human has confirmed the 3D rendering visually.
</success_criteria>

<output>
After completion, create `.planning/phases/30-interactive-webgl-3d-part-viewer/30-03-SUMMARY.md` recording: the F6 pre-flight (what was stashed/moved), the commit hash + push, the deploy + CloudFront invalidation ID, the full smoke-check results, the human visual sign-off (or the issues raised), and any deviations.
</output>
