---
phase: 65.3-tenant-data-driven-erp-detail-pages
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - /Users/jeet/turion-space-demo/dashboard-programs.html
  - /Users/jeet/turion-space-demo/dashboard-cto.html
  - /Users/jeet/turion-space-demo/dashboard-cio.html
  - /Users/jeet/turion-space-demo/dashboard-mfg.html
  - /Users/jeet/turion-space-demo/dashboard-cfo.html
  - /Users/jeet/turion-space-demo/dashboard-ceo.html
  - /Users/jeet/turion-space-demo/dashboard-sfhead.html
  - /Users/jeet/turion-space-demo/executive-cockpit.html
  - /Users/jeet/turion-space-demo/integration-vendor-ns.html
  - /Users/jeet/turion-space-demo/integration-mes-ns.html
  - /Users/jeet/turion-space-demo/integration-arena-ns.html
  - /Users/jeet/turion-space-demo/integration-bank-siem.html
  - /Users/jeet/turion-space-demo/architecture.html
  - /Users/jeet/turion-space-demo/index.html
  - /Users/jeet/turion-space-demo/admin-index.html
  - /Users/jeet/turion-space-demo/backend/src/routes/dashboards.ts
autonomous: true
requirements:
  - PageRefactorBatchC

must_haves:
  truths:
    - "All 8 dashboard pages (programs/cto/cio/mfg/cfo/ceo/sfhead/executive-cockpit) render KPI values via [data-bind] attributes resolved by dashboard-live-bind.js against /api/dashboards/<role>; the inline Turion KPI strip is removed"
    - "All 4 integration pages (vendor-ns/mes-ns/arena-ns/bank-siem) render sync-points + sync-runs tables from /api/integration/<source>/sync-points + /api/integration/<source>/sync-runs (already covered routes per RESEARCH §API Coverage Map)"
    - "All 4 platform/marketing pages (architecture/index/admin-index — workflow-e2e moved to Plan 03) carry [data-z-tenant-name] populator attributes wherever brand text appears; NO hardcoded 'Turion Space' string remains outside <span data-z-tenant-name> wrappers and the populator's fallback"
    - "Backend route GET /api/dashboards/sfhead exists (was missing per RESEARCH §Pattern summary) and returns role-specific KPIs with the same shape as the other 11 dashboards.ts routes"
    - "Cross-tenant safety: rendering dashboard-cto as solobrands tenant returns Solo Brands' KPI numbers (items=109+, sales_orders=4+) NOT Turion's; verified via runner Lambda DB-direct query matching dashboard's data-bind keys"
    - "Turion preservation: every refactored page, when rendered as turion tenant, produces a Puppeteer snapshot whose bodyText shares >= 80% line overlap with the Plan 01 PRE snapshot (Turion sees what Turion saw)"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/backend/src/routes/dashboards.ts"
      provides: "Adds GET /api/dashboards/sfhead route mirroring the existing 11 role-based GETs"
      contains: "r.get('/sfhead'"
    - path: "/Users/jeet/turion-space-demo/dashboard-cto.html"
      provides: "Turion KPI strip JS removed; all KPI numerics use data-bind attrs resolved by dashboard-live-bind.js"
      contains: "data-bind"
    - path: "/Users/jeet/turion-space-demo/executive-cockpit.html"
      provides: "Multi-subsidiary hardcoded table replaced with tenant-aware single-subsidiary view bound to /api/dashboards/executive"
      contains: "data-bind"
    - path: "/Users/jeet/turion-space-demo/integration-vendor-ns.html"
      provides: "Sync-points + sync-runs rendered from /api/integration/vendor/sync-points + sync-runs; empty-state inline render fn included"
      contains: "erpApi.get('/integration/vendor/sync-points')"
  key_links:
    - from: "dashboard-*.html"
      to: "/api/dashboards/<role>"
      via: "dashboard-live-bind.js [data-bind] harness (Phase 62 wiring)"
      pattern: "data-bind=\""
    - from: "integration-*.html"
      to: "/api/integration/<source>/sync-{points,runs}"
      via: "window.erpApi.get fetch + map-render with inline empty-state"
      pattern: "erpApi\\.get\\(['\"\\`]/integration"
    - from: "architecture.html, index.html, admin-index.html"
      to: "GET /api/tenants/current"
      via: "[data-z-tenant-name] populator (app-shell.js Phase 63)"
      pattern: "data-z-tenant-name"
    - from: "dashboard-sfhead.html"
      to: "/api/dashboards/sfhead (NEW backend route in this plan)"
      via: "dashboard-live-bind.js"
      pattern: "data-bind=\""
---

<objective>
Refactor Batch C: 12 dashboard + integration + executive + 3 platform pages from hardcoded Turion HTML to tenant-data-driven rendering. Batch C goes FIRST in Wave 2 because Phase 62 already wired `/api/dashboards/<role>` and `dashboard-live-bind.js` — this is the lowest-risk batch and proves the harness end-to-end for the heavier Plans 03 and 04.

Purpose: Solo Brands sees Solo Brands KPIs on every dashboard. New tenants see empty states. Turion sees Turion KPIs (sourced from the same `/api/dashboards/<role>` routes Turion has been seeded for). Brand text on platform/marketing pages swaps dynamically via the `[data-z-tenant-name]` populator from Phase 63.

Output: 15 modified HTML files (8 dashboards + 4 integrations + 3 platform), 1 backend file (dashboards.ts +1 route), zero new dependencies, all changes vanilla JS following the Phase 62/63 patterns.

NOTE on `workflow-e2e.html`: moved to Plan 03 alongside Arena/QMS pages (it is a quality/lifecycle narrative; co-locating it with arena pages keeps the Arena-domain knowledge in one plan).
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-RESEARCH.md
@/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-SUMMARY.md
@/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-AUDIT.json
@/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-TURION-SNAPSHOT-PRE.json

# Existing patterns to mirror (DO NOT modify these)
@/Users/jeet/turion-space-demo/dashboard-live-bind.js
@/Users/jeet/turion-space-demo/app-shell.js
@/Users/jeet/turion-space-demo/erp-api.js
@/Users/jeet/turion-space-demo/data-loader.js
@/Users/jeet/turion-space-demo/backend/src/routes/dashboards.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GET /api/dashboards/sfhead backend route + refactor 8 dashboard pages to use [data-bind]</name>
  <files>/Users/jeet/turion-space-demo/backend/src/routes/dashboards.ts, /Users/jeet/turion-space-demo/dashboard-programs.html, /Users/jeet/turion-space-demo/dashboard-cto.html, /Users/jeet/turion-space-demo/dashboard-cio.html, /Users/jeet/turion-space-demo/dashboard-mfg.html, /Users/jeet/turion-space-demo/dashboard-cfo.html, /Users/jeet/turion-space-demo/dashboard-ceo.html, /Users/jeet/turion-space-demo/dashboard-sfhead.html, /Users/jeet/turion-space-demo/executive-cockpit.html</files>
  <action>
**Step 1.1 — Add /api/dashboards/sfhead route.** Open `backend/src/routes/dashboards.ts` and add a new GET handler INSIDE the existing `r.use(tenantContext, requireAuth)` scope, mirroring the existing 11 handlers' shape. Look at `r.get('/cro', ...)` (Sales — closest analog to Sales Force Head) as the structural template. The SF Head dashboard's KPIs SHOULD include: pipeline value (sum of `turion.opportunities.amount`), qualified leads (count of opportunities WHERE stage IN ('qualified','proposal')), team quota attainment (sum of closed-won amount / sum of team quota — if no quota table, omit), recent activity counts (count of `turion.activities` last 30d). Use the same `withTenantClient(req, async (client) => ...)` pattern. Return JSON `{ pipeline_value, qualified_leads, opportunities_open, accounts_active, activities_30d, last_updated }`.

Compile-check: `cd backend && npx tsc --noEmit` MUST exit 0.

**Step 1.2 — Refactor each of the 8 dashboards.** For EACH of the 8 files (`dashboard-programs.html`, `dashboard-cto.html`, `dashboard-cio.html`, `dashboard-mfg.html`, `dashboard-cfo.html`, `dashboard-ceo.html`, `dashboard-sfhead.html`, `executive-cockpit.html`):

1. **Cross-reference `65.3-01-AUDIT.json` for the violations in this file** — the line numbers + token excerpts tell you exactly which `<tr>`/`<td>`/`<div>` to refactor.
2. **Open the file**. Identify the KPI strip / KPI cards / overview tiles that show numeric values today (e.g., `<div class="value">42</div>`, `<strong>$2.3M</strong>`).
3. **Replace the hardcoded numeric with `data-bind="<key>"`** matching the keys returned by the corresponding `/api/dashboards/<role>` JSON. E.g., `<div class="value" data-bind="ncrs">—</div>` (the `—` is the fallback per Phase 62 pattern).
4. **REMOVE any inline `<script>` that sets KPI strip values from hardcoded `Turion`/`SAT-001`/`ADCS` literals.** The audit JSON points to these lines. DO NOT remove scripts that hydrate from `window.X_DATA` globals (those are correct — they read from data-loader's API hydration).
5. **Verify `dashboard-live-bind.js` is loaded** in the page's `<head>` or `<body>`. If not present, add `<script src="dashboard-live-bind.js"></script>` BEFORE the closing `</body>` (after `data-loader.js`).
6. **For executive-cockpit.html specifically:** the multi-subsidiary 8-row hardcoded table — replace with a single-subsidiary view bound to the calling tenant via `data-z-tenant-name` + `data-z-tenant-instance` (Phase 63). If the page renders "S-01 / S-02 / ... / S-08 subsidiaries", reduce to one row using `data-z-tenant-instance` for the subsidiary code. This is per RESEARCH §"executive-cockpit.html — pure Turion narrative" — single-tenant scope.
7. **Quick links / navigation:** the audit catches `PROJ-ANDROMEDA-SAT-001` hardcoded in dashboard-cto.html's Quick Links (per Pitfall 10). Replace the literal href with a dynamic `<a id="qlProjectLink">` and inline JS that picks the first project from `window.PROJECT_DATA || {}` (data-loader hydrates this) — if empty, hide the link (`display:none`). DO NOT leave a broken /records/project/PROJ-ANDROMEDA-SAT-001 href.

8. **DO NOT remove the static `*-data.js` fallbacks** (`enterprise-data.js`, `qms-data.js`, `mes-data.js`, etc.) per RESEARCH §"Anti-Patterns to Avoid" — they are the offline fallback. Only remove the INLINE `<script>` blocks that hardcode Turion KPI strip values bypassing the API.

After all 8 pages refactored, RUN the audit script and confirm dashboard-page violation count dropped by >= 80%:
```
cd /Users/jeet/turion-space-demo
node scripts/audit-hardcoded-erp.mjs > /tmp/post-batch-c-dashboards.json
node -e "
const before = require('/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-AUDIT.json');
const after = require('/tmp/post-batch-c-dashboards.json');
const dashFiles = Object.keys(before.by_file).filter(f => f.match(/^(dashboard|executive-cockpit)/));
const dropped = dashFiles.reduce((sum, f) => sum + (before.by_file[f]?.length || 0) - (after.by_file[f]?.length || 0), 0);
const beforeTot = dashFiles.reduce((sum, f) => sum + (before.by_file[f]?.length || 0), 0);
console.log('dashboard files: dropped', dropped, '/', beforeTot, 'violations');
if (dropped / beforeTot < 0.80) process.exit(1);
"
```
  </action>
  <verify>
1. `grep -n "r.get('/sfhead'" /Users/jeet/turion-space-demo/backend/src/routes/dashboards.ts` returns at least 1 line
2. `cd /Users/jeet/turion-space-demo/backend && npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 0
3. `grep -l "data-bind=" /Users/jeet/turion-space-demo/dashboard-{programs,cto,cio,mfg,cfo,ceo,sfhead}.html /Users/jeet/turion-space-demo/executive-cockpit.html | wc -l` returns 8 (all 8 files use data-bind)
4. `grep -c "PROJ-ANDROMEDA-SAT-001\|SAT-001" /Users/jeet/turion-space-demo/dashboard-cto.html` returns 0 (Pitfall 10 closed)
5. The post-batch audit comparison shows dashboard-file violation count dropped by >= 80%
6. `grep -l "dashboard-live-bind.js" /Users/jeet/turion-space-demo/dashboard-{programs,cto,cio,mfg,cfo,ceo,sfhead}.html | wc -l` returns 7 (sfhead may need the script include added; executive-cockpit may use a different harness)
  </verify>
  <done>
- `/api/dashboards/sfhead` route exists and returns valid JSON shape (compile-clean TypeScript).
- All 8 dashboard pages have `[data-bind]` attrs on their KPI numerics.
- Hardcoded Turion KPI strip JS removed; data-bind harness loaded.
- Dashboard-cto Quick Links no longer point to PROJ-ANDROMEDA-SAT-001 (dynamic from PROJECT_DATA or hidden).
- Post-batch audit confirms >= 80% violation reduction on dashboard files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor 4 integration pages + 3 platform pages; capture mid-batch Turion preservation snapshot</name>
  <files>/Users/jeet/turion-space-demo/integration-vendor-ns.html, /Users/jeet/turion-space-demo/integration-mes-ns.html, /Users/jeet/turion-space-demo/integration-arena-ns.html, /Users/jeet/turion-space-demo/integration-bank-siem.html, /Users/jeet/turion-space-demo/architecture.html, /Users/jeet/turion-space-demo/index.html, /Users/jeet/turion-space-demo/admin-index.html</files>
  <action>
**Step 2.1 — Refactor 4 integration pages.** For each of `integration-vendor-ns.html`, `integration-mes-ns.html`, `integration-arena-ns.html`, `integration-bank-siem.html`:

1. Cross-reference `65.3-01-AUDIT.json` for the specific Turion-token lines in this file.
2. Identify the sync-points + sync-runs tables (typically `<tbody id="syncPointsTable">` + `<tbody id="syncRunsTable">` or similar).
3. Replace the hardcoded Turion-row literals with the data-driven recipe from RESEARCH §"Existing Pattern: List-page refactor recipe":
```javascript
window.addEventListener('turion-data-ready', () => {
  window.erpApi.get('/integration/vendor/sync-points').then(points => {
    const tbody = document.getElementById('syncPointsTable');
    if (!points || points.length === 0) {
      tbody.innerHTML = renderEmptyState({ module: 'integration sync points' });
      return;
    }
    tbody.innerHTML = points.map(p => `
      <tr>
        <td>${esc(p.source || '—')}</td>
        <td>${esc(p.target || '—')}</td>
        <td>${esc(p.status || '—')}</td>
        <td>${esc(p.last_synced_at || '—')}</td>
      </tr>`).join('');
  }).catch(() => {
    // Graceful degradation — show empty state, do not crash
    document.getElementById('syncPointsTable').innerHTML = renderEmptyState({ module: 'integration sync points', cta: '/onboarding/migrate' });
  });
});
```
Use the per-page integration source slug: `vendor` for integration-vendor-ns.html, `mes` for integration-mes-ns.html, `arena` for integration-arena-ns.html, `bank` for integration-bank-siem.html (verified from existing /api/integration/* route mounts; if a slug differs, READ `backend/src/routes/integration.ts` to confirm the actual path).

4. Define `esc()` and `renderEmptyState()` once per page (RESEARCH §"Code Examples" copy-paste-able blocks). XSS guard is non-negotiable per Pitfall 5.

**Step 2.2 — Apply Phase 63 populator to 3 platform pages.** For `architecture.html`, `index.html`, `admin-index.html`: these are platform showcase pages, NOT data-driven (per RESEARCH §"Existing Pattern: Platform/marketing pages" + §"Page Anatomy"). The refactor is:

1. Identify every place the literal "Turion Space" appears in headings, paragraphs, button labels, etc.
2. Wrap each with `<span data-z-tenant-name>Turion Space</span>` (the literal stays as fallback for offline + SEO; the populator swaps to tenant name at runtime).
3. For any subsidiary code mentions (e.g., "S-01"), wrap with `<span data-z-tenant-instance>S-01</span>`.
4. Confirm `app-shell.js` is loaded on these pages (it usually is via the chrome injector; if not, add `<script src="app-shell.js"></script>` before `</body>`).
5. **DO NOT** add data-bind, /api/data/all, or full data-driven render to these 3 pages — they're showcase pages whose CONTENT is the narrative, not tenant data (per RESEARCH §"Page Anatomy" + ROADMAP scope-exclusion "platform pages get Phase 63 populator ONLY, NOT data refactor").

**Step 2.3 — Mid-batch Turion preservation snapshot.** Capture a Puppeteer snapshot of the 15 pages refactored so far and compare against the Plan 01 baseline:
```bash
cd /Users/jeet/turion-space-demo
node scripts/snapshot-turion-erp-views.mjs turion > /tmp/turion-snapshot-after-batch-c.json
node -e "
const before = require('/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-TURION-SNAPSHOT-PRE.json');
const after = require('/tmp/turion-snapshot-after-batch-c.json');
const pages = ['dashboard-cto','dashboard-cfo','dashboard-ceo','dashboard-programs','dashboard-mfg','executive-cockpit','integration-vendor-ns','integration-mes-ns','integration-arena-ns','integration-bank-siem'];
for (const p of pages) {
  const b = before.pages[p]; const a = after.pages[p];
  if (!b || !a) { console.error('missing snapshot for', p); process.exit(1); }
  const beforeLines = new Set(b.bodyText.split('\n').map(s => s.trim()).filter(s => s.length > 4));
  const afterLines  = new Set(a.bodyText.split('\n').map(s => s.trim()).filter(s => s.length > 4));
  const overlap = [...beforeLines].filter(l => afterLines.has(l)).length;
  const pct = (overlap / beforeLines.size) * 100;
  console.log(p, 'overlap:', pct.toFixed(1) + '%');
  if (pct < 80) { console.error('TURION PRESERVATION FAIL on', p); process.exit(1); }
}
"
```

If any page falls below 80% line overlap with the PRE snapshot, ROLL BACK that page's refactor and investigate (likely Pitfall 1 — bound to wrong key) before continuing. The 80% threshold accounts for dynamic timestamps and tenant-name string swaps; semantic content must still be there.
  </action>
  <verify>
1. `grep -l "erpApi.get\|window.erpApi" /Users/jeet/turion-space-demo/integration-{vendor-ns,mes-ns,arena-ns,bank-siem}.html | wc -l` returns 4
2. `grep -l "data-z-tenant-name" /Users/jeet/turion-space-demo/architecture.html /Users/jeet/turion-space-demo/index.html /Users/jeet/turion-space-demo/admin-index.html | wc -l` returns 3
3. `grep -c "SAT-001\|PROJ-ANDROMEDA\|ADCS-RW-MEDIUM\|Blue Canyon\|BCT-RW-" /Users/jeet/turion-space-demo/integration-{vendor-ns,mes-ns,arena-ns,bank-siem}.html` returns 0 across all 4
4. The mid-batch Turion preservation script exits 0 (every checked page >= 80% line overlap with PRE snapshot)
5. `grep -c "function esc\|escape(s)" /Users/jeet/turion-space-demo/integration-{vendor-ns,mes-ns,arena-ns,bank-siem}.html` returns 4 (XSS guard present on each)
  </verify>
  <done>
- 4 integration pages render sync-points + sync-runs from /api/integration/<source>/* with empty-state fallback and XSS-safe interpolation.
- 3 platform pages use data-z-tenant-name populator wrappers, NO inline data refactor.
- Mid-batch Turion preservation snapshot diff passes (>= 80% line overlap on 10 checked pages).
- All Turion-specific hardcoded tokens removed from these 7 files.
  </done>
</task>

</tasks>

<verification>
- Audit re-run on Batch C files (`dashboard-*`, `executive-cockpit`, `integration-*`, `architecture`, `index`, `admin-index`) shows >= 80% violation reduction.
- Turion preservation snapshot diff passes (>= 80% line overlap per refactored page).
- Backend compiles cleanly with the new /api/dashboards/sfhead route.
- Files edited: 15 HTML + 1 backend TS = 16 files total (well under the "more than 5 files = too large" per-task limit because the work is mechanical and follows one recipe).
</verification>

<success_criteria>
- `PageRefactorBatchC` requirement satisfied: 15 pages converted using one of two patterns (data-bind for dashboards/integrations OR Phase 63 populator for platform pages).
- Mid-batch snapshot diff is the safety net: Plan 03/04 can iterate without re-snapshotting if Plan 02's diff passed.
- Backend route gap closed (sfhead).
- No backend API leaks (all new code goes through `withTenantClient(req, ...)` — verified by compile + spot-grep).
</success_criteria>

<output>
After completion, create `.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-02-SUMMARY.md` containing:
- Per-file: violations before, violations after, lines changed
- Turion preservation diff results (10 pages × overlap pct)
- The new /api/dashboards/sfhead route signature + sample response shape (Solo Brands + Turion side-by-side)
- Any rolled-back refactors (with diagnosis) — if zero, state "no rollbacks"
- Hand-off to Plan 05: which pages had hardcoded-token incidence >0 in the PRE snapshot that should now be 0 in the POST snapshot (test asserts for smoke harness)
</output>
