---
phase: 65.3-tenant-data-driven-erp-detail-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jeet/turion-space-demo/scripts/audit-hardcoded-erp.mjs
  - /Users/jeet/turion-space-demo/scripts/snapshot-turion-erp-views.mjs
  - /Users/jeet/turion-space-demo/backend/migrations/039_turion_data_completeness_seed.sql
  - /Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-AUDIT.json
  - /Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-TURION-SNAPSHOT-PRE.json
autonomous: true
requirements:
  - HardcodeAudit
  - TurionDataPreservedCheck

must_haves:
  truths:
    - "An executable Node CLI exists at scripts/audit-hardcoded-erp.mjs that scans every *.html in turion-space-demo and exits 1 if ANY of the 8 Turion-specific tokens appears outside the satellite/, backend/, node_modules/, .git/, .superpowers/, onboarding/ paths"
    - "An executable Node CLI exists at scripts/snapshot-turion-erp-views.mjs that drives Puppeteer over the 34 in-scope pages as Turion tenant and captures rendered DOM text + table-row counts to JSON"
    - "A Puppeteer-captured Turion baseline snapshot exists at .planning/phases/65.3-.../65.3-01-TURION-SNAPSHOT-PRE.json for all 34 pages and shows non-empty bodyText for the 8 finance/dashboard/arena pages known to contain Turion data today"
    - "An additive Turion-data seed migration exists at backend/migrations/039_turion_data_completeness_seed.sql that uses INSERT ... ON CONFLICT DO NOTHING ONLY (no UPDATE, no DELETE on turion.* rows) for any gap surfaced by the audit; if no gaps, the migration is a documented no-op with a comment block"
    - "The audit JSON report at .planning/phases/65.3-.../65.3-01-AUDIT.json lists every (file, token, line) violation found across the 91 *.html files in the demo"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/scripts/audit-hardcoded-erp.mjs"
      provides: "Dependency-free Node ESM CLI scanning for 8 Turion tokens; exits 1 on violation"
      contains: "TOKENS"
      min_lines: 70
    - path: "/Users/jeet/turion-space-demo/scripts/snapshot-turion-erp-views.mjs"
      provides: "Puppeteer DOM-text + row-count snapshot tool; one JSON per tenant"
      contains: "puppeteer"
      min_lines: 60
    - path: "/Users/jeet/turion-space-demo/backend/migrations/039_turion_data_completeness_seed.sql"
      provides: "Additive-only Turion seed (or documented no-op) for gaps surfaced by audit"
      contains: "ON CONFLICT DO NOTHING"
      min_lines: 10
    - path: "/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-AUDIT.json"
      provides: "Audit report consumed by Plans 02/03/04 as the refactor targeting list"
      contains: "violations"
    - path: "/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-TURION-SNAPSHOT-PRE.json"
      provides: "Turion baseline for the Plan 05 post-flight TurionDataPreservedCheck diff"
      contains: "bodyText"
  key_links:
    - from: "scripts/audit-hardcoded-erp.mjs"
      to: "/Users/jeet/turion-space-demo/*.html"
      via: "fs.readdirSync recursive walk with EXCLUDE list"
      pattern: "readdirSync|walkDir"
    - from: "scripts/snapshot-turion-erp-views.mjs"
      to: "https://turion.zietra.com/<page>.html"
      via: "puppeteer page.goto + page.waitForFunction window.__TURION_DATA_SOURCE__"
      pattern: "puppeteer|__TURION_DATA_SOURCE__"
    - from: "migration 039"
      to: "turion.* tables"
      via: "INSERT ... ON CONFLICT DO NOTHING (additive only)"
      pattern: "ON CONFLICT DO NOTHING"
---

<objective>
Build the audit + snapshot + additive-seed substrate that the three refactor plans (02, 03, 04) depend on, and that the deploy plan (05) uses for post-flight diff.

Purpose: The user's #1 constraint is "Turion data and details should not be deleted at any cost." Before we touch any of the 34 HTML pages, we MUST (a) know exactly which Turion-specific tokens live in which file:line (audit), (b) have a byte-stable Turion DOM baseline to diff against after each refactor batch (snapshot), and (c) close any Turion data gaps surfaced by the audit via additive seed (migration 039). This plan produces no user-facing change; it produces the safety harness.

Output: Two new executable scripts in /Users/jeet/turion-space-demo/scripts/, one new additive migration in backend/migrations/, an AUDIT.json driving plans 02/03/04 targeting, and a TURION-SNAPSHOT-PRE.json that Plan 05 diffs against.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jeet/doordash-p2p/.planning/PROJECT.md
@/Users/jeet/doordash-p2p/.planning/ROADMAP.md
@/Users/jeet/doordash-p2p/.planning/STATE.md
@/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-RESEARCH.md

# Pattern reference — existing audit script
@/Users/jeet/turion-space-demo/scripts/audit-erp-buttons.mjs

# Pattern reference — data-loader signals used by the snapshot tool
@/Users/jeet/turion-space-demo/data-loader.js

# Pattern reference — guarded multi-tenant DB pattern (use ONLY for inserts, never DELETE/UPDATE on Turion rows)
@/Users/jeet/doordash-p2p/.planning/quick/339-delete-6-import-smoke-338-1-test-residue/339-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/audit-hardcoded-erp.mjs token scanner and emit 65.3-01-AUDIT.json</name>
  <files>/Users/jeet/turion-space-demo/scripts/audit-hardcoded-erp.mjs, /Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-AUDIT.json</files>
  <action>
Create `scripts/audit-hardcoded-erp.mjs` modeled after `scripts/audit-erp-buttons.mjs` (existing 522-LOC pattern in the same directory — read first 30 lines for shebang + ESM imports + walkDir structure). Dependency-free Node ESM (uses only `node:fs` + `node:path`). The TOKENS array MUST contain exactly: `SAT-001`, `ADCS-RW-MEDIUM`, `Blue Canyon`, `PROJ-ANDROMEDA`, `CLIN-004`, `Honeywell · Aerospace`, `BCT-RW-`, `Cygnus` (8 tokens per RESEARCH.md §"Code Examples" + ROADMAP §"HardcodeAudit"). EXCLUDE paths: `/satellite/`, `/backend/`, `/node_modules/`, `/.git/`, `/.superpowers/`, `/onboarding/`.

Walk recursively from `/Users/jeet/turion-space-demo` (configurable via `process.argv[2]`). For every `.html` file not under EXCLUDE, read it line-by-line and record any line containing any TOKEN as `{file (relative), token, line, excerpt (first 120 chars trimmed)}`. Emit JSON on stdout: `{ total_files_scanned, total_violations, violations: [...], by_file: { "path": [...] } }`. Exit 0 if violations.length === 0, else exit 1.

Make it executable: `chmod +x scripts/audit-hardcoded-erp.mjs`. Add a `#!/usr/bin/env node` shebang on line 1.

After the script is in place, RUN it once and CAPTURE the output:
```
cd /Users/jeet/turion-space-demo
node scripts/audit-hardcoded-erp.mjs > /Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-AUDIT.json
# Exit code is expected to be 1 (violations present); record exit code in commit message
echo "audit exit: $?"
```

Verify the JSON has the expected structure with `node -e "const r=require('/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-AUDIT.json'); console.log('files:', r.total_files_scanned, 'violations:', r.total_violations, 'in_files:', Object.keys(r.by_file).length)"`.

Do NOT modify any HTML file in this task — this is read-only scanning.
  </action>
  <verify>
1. `[ -x /Users/jeet/turion-space-demo/scripts/audit-hardcoded-erp.mjs ]` returns 0 (file executable)
2. `head -1 /Users/jeet/turion-space-demo/scripts/audit-hardcoded-erp.mjs` shows `#!/usr/bin/env node`
3. `wc -l /Users/jeet/turion-space-demo/scripts/audit-hardcoded-erp.mjs` returns >= 70 lines
4. `grep -c "SAT-001\|ADCS-RW-MEDIUM\|Blue Canyon\|PROJ-ANDROMEDA\|CLIN-004\|Honeywell\|BCT-RW-\|Cygnus" /Users/jeet/turion-space-demo/scripts/audit-hardcoded-erp.mjs` returns >= 8
5. `node -e "const r=require('/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-AUDIT.json'); if(typeof r.total_violations !== 'number' || !Array.isArray(r.violations) || typeof r.by_file !== 'object') process.exit(1)"` exits 0
6. `node -e "const r=require('/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-AUDIT.json'); if(r.total_violations === 0) { console.error('expected violations, got 0'); process.exit(1)}"` exits 0 (we EXPECT violations — that is the whole point)
  </verify>
  <done>
- File `scripts/audit-hardcoded-erp.mjs` exists, executable, dependency-free, contains all 8 tokens, and walks the repo skipping the EXCLUDE list.
- File `65.3-01-AUDIT.json` exists in the phase dir and contains a non-empty `violations` array covering at least 20 of the 34 in-scope HTML files (the research expects ~34 files with hits).
- Audit script exit code 1 (violations present today) — that is the success signal that the audit is working.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build scripts/snapshot-turion-erp-views.mjs Puppeteer harness and capture Turion baseline</name>
  <files>/Users/jeet/turion-space-demo/scripts/snapshot-turion-erp-views.mjs, /Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-TURION-SNAPSHOT-PRE.json</files>
  <action>
Create `scripts/snapshot-turion-erp-views.mjs` modeled on the RESEARCH.md §"Snapshot tool pattern" Code Example. Use the in-repo Puppeteer (transitive dep via Phase 60 PDF Lambda — verify it resolves with `node -e "import('puppeteer').then(()=>console.log('ok'))"` from the turion-space-demo root before writing the script; if it does not resolve, run `npm install puppeteer --no-save` inside the backend/ subdir which already has it).

The PAGES array MUST contain exactly these 34 in-scope page names (no .html suffix) — sourced from ROADMAP §"Phase 65.3 Requirements" PageRefactorBatch A/B/C lists:
- Batch A (11): `netsuite-financials`, `netsuite-tb`, `netsuite-bs`, `netsuite-coa`, `netsuite-arm`, `netsuite-procurement`, `netsuite-new-po`, `netsuite-new-vendor`, `inventory-index`, `manufacturing-index`, `finance-index`
- Batch B (11): `arena-qms`, `arena-new-part`, `arena-new-audit`, `arena-new-document`, `quality-index`, `arena-bom`, `lifecycle-index`, `quickbooks`, `sales-new-cdrl`, `sales-new-case`, `workflow-e2e`
- Batch C (12): `dashboard-programs`, `dashboard-cto`, `dashboard-cio`, `dashboard-mfg`, `dashboard-cfo`, `dashboard-ceo`, `dashboard-sfhead`, `executive-cockpit`, `integration-vendor-ns`, `integration-mes-ns`, `integration-arena-ns`, `integration-bank-siem`

CLI: `node scripts/snapshot-turion-erp-views.mjs <tenant>` where tenant ∈ `turion | solobrands | test65empty`. Reads JWT from env `COGNITO_ID_TOKEN` (REQUIRED — exit 1 with clear message if unset).

For EACH page:
1. `const ctx = await browser.createIncognitoBrowserContext()` (Pitfall 3 — never reuse context across tenants)
2. `await tab.evaluateOnNewDocument((tok) => { localStorage.setItem('cognito-id-token', tok); }, idToken)`
3. `await tab.goto(\`https://${tenant}.zietra.com/${page}.html\`, { waitUntil: 'networkidle2', timeout: 30000 })`
4. `await tab.waitForFunction('window.__TURION_DATA_SOURCE__ === "live-aws"', { timeout: 10000 }).catch(() => null)` (per Pitfall 2; the .catch lets us still capture the snapshot even if data-loader didn't emit — but we record `dataReady: false`)
5. Capture: `{ title: document.title, bodyText: document.body.innerText.slice(0, 50000), rowCounts: { tables: document.querySelectorAll('table tbody tr').length, emptyStates: document.querySelectorAll('.empty-state').length }, hardcodedTokens: ['SAT-001','ADCS-RW-MEDIUM','Blue Canyon','PROJ-ANDROMEDA','CLIN-004','BCT-RW-','Cygnus'].filter(t => document.body.innerText.includes(t)), dataReady: window.__TURION_DATA_SOURCE__ === 'live-aws' }`
6. `await ctx.close()` (Pitfall 3 — fully tear down per page so window globals don't bleed)

Emit `{ tenant, captured_at, pages: { "<page>": { title, bodyText, rowCounts, hardcodedTokens, dataReady } } }` on stdout.

Make executable: `chmod +x scripts/snapshot-turion-erp-views.mjs`. Add `#!/usr/bin/env node` shebang.

Then CAPTURE THE TURION BASELINE — this is the safety net Plan 05 diffs against:

```bash
# Mint a Turion admin Cognito ID token (use the canonical Phase 65.2 pattern;
# if no admin user is provisioned for turion tenant, use the SSO bootstrap flow
# documented in handoff 2026-05-14-zietra-platform-milestone-kickoff.md)
export COGNITO_ID_TOKEN=$(aws cognito-idp admin-initiate-auth \
  --user-pool-id us-east-1_KQuNS85nP \
  --client-id $(aws ssm get-parameter --name /zietra/cognito/client-id --query Parameter.Value --output text 2>/dev/null || echo "REPLACE_WITH_CLIENT_ID") \
  --auth-flow ADMIN_NO_SRP_AUTH \
  --auth-parameters USERNAME=admin@turion.test,PASSWORD=... \
  --query 'AuthenticationResult.IdToken' --output text 2>/dev/null) || \
  # Fallback if admin-initiate-auth isn't usable: pull the most recent Turion ID token
  # from CloudWatch logs of turion-demo-api Lambda's last successful /api/health call
  # documented in Phase 65.2-04 SUMMARY
  echo "WARNING: unable to mint Cognito token automatically; user must provide" >&2

# If COGNITO_ID_TOKEN is empty, halt and ask the user — do NOT proceed with an empty snapshot
[ -z "$COGNITO_ID_TOKEN" ] && { echo "FATAL: COGNITO_ID_TOKEN required"; exit 1; }

cd /Users/jeet/turion-space-demo
node scripts/snapshot-turion-erp-views.mjs turion > /Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-TURION-SNAPSHOT-PRE.json
```

**If Cognito token cannot be minted programmatically (likely for first run):** halt this task with a clear message asking the user to supply `COGNITO_ID_TOKEN` for the turion tenant via env var, then re-invoke the snapshot step. This is the ONLY external-action gate in this plan — do not silently produce an empty snapshot.

Validate the snapshot has data: at minimum, the 6 finance pages (`netsuite-financials`, `netsuite-tb`, `netsuite-bs`, `netsuite-coa`, `netsuite-arm`, `netsuite-procurement`), the 2 arena pages (`arena-qms`, `arena-bom`), and the 4 dashboards (`dashboard-cto`, `dashboard-cfo`, `dashboard-ceo`, `dashboard-programs`) MUST have `dataReady: true` AND `bodyText.length > 500` (because Turion has real data in those tables today per Phase 36 SUMMARY).
  </action>
  <verify>
1. `[ -x /Users/jeet/turion-space-demo/scripts/snapshot-turion-erp-views.mjs ]` returns 0
2. `head -1 /Users/jeet/turion-space-demo/scripts/snapshot-turion-erp-views.mjs` shows `#!/usr/bin/env node`
3. `grep -c "createIncognitoBrowserContext\|__TURION_DATA_SOURCE__\|hardcodedTokens" /Users/jeet/turion-space-demo/scripts/snapshot-turion-erp-views.mjs` returns >= 3
4. `node -e "const r=require('/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-TURION-SNAPSHOT-PRE.json'); console.log('pages:', Object.keys(r.pages).length); if(Object.keys(r.pages).length !== 34) process.exit(1)"` exits 0 (all 34 pages captured)
5. `node -e "const r=require('/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-TURION-SNAPSHOT-PRE.json'); const fin = r.pages['netsuite-financials']; if(!fin || fin.bodyText.length < 500) { console.error('financials bodyText too small:', fin && fin.bodyText.length); process.exit(1)}"` exits 0
6. `node -e "const r=require('/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-TURION-SNAPSHOT-PRE.json'); const dataReady = Object.values(r.pages).filter(p => p.dataReady === true).length; if(dataReady < 25) { console.error('expected >=25 pages dataReady, got', dataReady); process.exit(1) }"` exits 0
  </verify>
  <done>
- File `scripts/snapshot-turion-erp-views.mjs` exists, executable, drives Puppeteer over the 34 in-scope pages using per-page incognito contexts.
- File `65.3-01-TURION-SNAPSHOT-PRE.json` exists in the phase dir with all 34 pages captured; the 6 finance, 2 arena, and 4 dashboard pages all show non-empty bodyText and `dataReady: true`.
- Turion still sees the hardcoded tokens in its snapshot (`hardcodedTokens` non-empty on most pages) — that is the EXPECTED state pre-refactor; Plan 05 diff will assert the SEMANTIC content is preserved post-refactor even though the source moves from HTML to API.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write additive migration 039 for any Turion data gaps surfaced by audit; apply via runner Lambda</name>
  <files>/Users/jeet/turion-space-demo/backend/migrations/039_turion_data_completeness_seed.sql</files>
  <action>
Inspect `65.3-01-AUDIT.json` from Task 1 and the Turion snapshot from Task 2 to determine if Turion is missing rows in any `turion.*` table that a soon-to-be-refactored page reads. Per RESEARCH.md §"Turion Data Completeness Audit" + §"Migration 039 plan", the most likely candidates are:
- `turion.gl_accounts` for CoA page (verify via runner Lambda: `SELECT count(*) FROM turion.gl_accounts WHERE tenant_id = '00000000-0000-0000-0000-000000000001'::uuid`)
- `turion.journal_entries` aggregator-backing for TB/BS/PL pages (verify count > 100 for the current period `2026-04` per Phase 36)
- `turion.arm_schedule` for ARM page (verify count > 0)
- `turion.mes_stages` for MES shop-floor (verify count >= 8)
- `turion.ncrs / capas / audits / qms_docs` for Arena pages (verify counts > 0 each)

For ANY of those queries returning 0 OR less than what the snapshot's `bodyText` suggests is rendered, ADD an additive INSERT block to `backend/migrations/039_turion_data_completeness_seed.sql`. **Constraints:**
- ONLY `INSERT INTO turion.<table> (...) VALUES (...) ON CONFLICT (id) DO NOTHING` — no UPDATE, no DELETE, ever
- Every INSERT must specify `tenant_id = '00000000-0000-0000-0000-000000000001'::uuid` explicitly
- The data inserted MUST reflect what the current hardcoded HTML shows (preserve the Turion narrative — e.g., if `netsuite-financials.html` shows "$2.3M AR / $3.1M AP", the additive seed creates the underlying invoices/bills/JEs that AGGREGATE to those numbers)
- Wrap the entire migration in `BEGIN; ... COMMIT;`
- Top of file: comment block stating "ADDITIVE ONLY. NEVER UPDATE OR DELETE TURION ROWS. See Phase 65.3 RESEARCH §Turion Data Completeness Audit." + the audit fingerprint timestamp from Task 1.

If the audit + snapshot together prove Turion already has parity (Phase 36 SUMMARY: 53 keys real data — likely outcome), then 039 is a DOCUMENTED NO-OP: a single-statement migration with a comment block explaining "Audit revealed Turion has parity; this migration intentionally contains no INSERT statements. Future audits may add rows additively here." The no-op MUST still be a valid runnable SQL file (e.g., `SELECT 'turion_data_completeness_seed_noop_039' AS status;`).

APPLY the migration via `zietra-rls-runner-55-05` Lambda as `zietra_admin` (cluster-master secret `rds!cluster-16d5e38c-...` per Phase 65.2/Quick 339 — NOT `zietra-aurora/admin-bypass-role`). Capture the runner output and confirm:
- migration applied without error
- Turion row counts AFTER >= row counts BEFORE for every table touched
- Solo Brands row counts BEFORE === Solo Brands row counts AFTER (zero collateral)

Use the Quick-339 guarded pattern: BEGIN; pre-flight count assert (must be exactly the Turion-only counts); INSERT; post-flight count assert (must be > pre-flight if rows added, == if no-op); COMMIT.

If the apply fails any guard, ROLLBACK (the transaction wrapping does this automatically) and HALT — do not push the migration file as committed if it doesn't apply cleanly.
  </action>
  <verify>
1. `[ -f /Users/jeet/turion-space-demo/backend/migrations/039_turion_data_completeness_seed.sql ]` returns 0
2. `head -10 /Users/jeet/turion-space-demo/backend/migrations/039_turion_data_completeness_seed.sql | grep -c "ADDITIVE ONLY"` returns 1
3. `grep -c "DELETE\|UPDATE turion" /Users/jeet/turion-space-demo/backend/migrations/039_turion_data_completeness_seed.sql` returns 0 (forbidden statements)
4. `grep -c "BEGIN\|COMMIT" /Users/jeet/turion-space-demo/backend/migrations/039_turion_data_completeness_seed.sql` returns >= 2
5. Runner Lambda output captured shows `status: applied` (or `status: noop` for the no-op variant) and `solobrands_collateral: 0`
6. Post-apply: Turion `turion.items` count exactly equal to pre-apply count (we are NOT modifying items — quick sanity: items is the most-watched table)
  </verify>
  <done>
- Migration 039 file exists with the additive-only guarantee in a top comment.
- Migration applied successfully via runner Lambda (or documented no-op recorded in commit message).
- Zero collateral on Solo Brands or any non-Turion tenant.
- If 039 is a no-op, the commit message MUST say so explicitly so Plan 05's diff doesn't expect new rows.
  </done>
</task>

</tasks>

<verification>
- All 3 new artifacts exist on disk: `scripts/audit-hardcoded-erp.mjs`, `scripts/snapshot-turion-erp-views.mjs`, `backend/migrations/039_turion_data_completeness_seed.sql`.
- Phase-dir artifacts exist: `65.3-01-AUDIT.json` (non-empty violations list) and `65.3-01-TURION-SNAPSHOT-PRE.json` (34 pages captured).
- No HTML page was modified by this plan.
- Migration 039 applied cleanly (or documented no-op) with zero collateral.
- Snapshot tool's Turion baseline shows the 6 finance + 2 arena + 4 dashboard pages have non-empty bodyText AND `dataReady: true` — proving the snapshot harness sees post-hydration DOM (avoids Pitfall 2).
</verification>

<success_criteria>
- `HardcodeAudit` requirement satisfied: a re-runnable CLI emits JSON; Plans 02/03/04 can `node scripts/audit-hardcoded-erp.mjs` at any time to see remaining work.
- `TurionDataPreservedCheck` substrate ready: PRE snapshot captured; Plan 05 will run POST snapshot and diff.
- Migration 039 either added the additive rows or documented why no seed is needed — Turion's view is data-backed in either case.
- No regression: Turion tenant's row counts in `turion.items`, `turion.sales_orders`, `turion.customers`, `turion.vendors` are byte-equal before vs after this plan (verified via runner Lambda).
</success_criteria>

<output>
After completion, create `.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-SUMMARY.md` containing:
- Audit results: total files scanned, total violations, by_file breakdown (top 10)
- Snapshot results: pages captured, total bodyText bytes, hardcoded-token incidence per page (this drives Plan 05's pre-vs-post diff strategy)
- Migration 039 verdict: applied (with row deltas per table) or no-op
- The Cognito ID-token minting method used (so Plans 02-05 reuse the same approach)
- Hand-off note for Plans 02/03/04: which page-set each plan should consume from the audit
</output>
