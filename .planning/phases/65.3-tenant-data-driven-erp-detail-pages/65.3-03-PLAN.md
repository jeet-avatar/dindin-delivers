---
phase: 65.3-tenant-data-driven-erp-detail-pages
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - /Users/jeet/turion-space-demo/arena-qms.html
  - /Users/jeet/turion-space-demo/arena-new-part.html
  - /Users/jeet/turion-space-demo/arena-new-audit.html
  - /Users/jeet/turion-space-demo/arena-new-document.html
  - /Users/jeet/turion-space-demo/arena-bom.html
  - /Users/jeet/turion-space-demo/quality-index.html
  - /Users/jeet/turion-space-demo/lifecycle-index.html
  - /Users/jeet/turion-space-demo/quickbooks.html
  - /Users/jeet/turion-space-demo/sales-new-cdrl.html
  - /Users/jeet/turion-space-demo/sales-new-case.html
  - /Users/jeet/turion-space-demo/workflow-e2e.html
autonomous: true
requirements:
  - PageRefactorBatchB
  - EmptyStatesAcrossPages

must_haves:
  truths:
    - "arena-qms.html title no longer contains the literal 'SAT-001' — title is `Arena QMS · Quality Management Workspace` (the SAT-specific suffix is removed; per-tenant tab content is bound to /api/arena/* endpoints already available)"
    - "All 4 arena landing/list pages (arena-qms, arena-bom, quality-index, lifecycle-index) render their data tables (NCRs, CAPAs, audits, BOM, ECOs) from existing /api/arena/* + /api/extras/* endpoints; inline Turion-only `<tr>` literals are replaced by data-loader-fed `Object.values(window.X_DATA).map(...)`"
    - "All 4 'new' create-form pages (arena-new-part, arena-new-audit, arena-new-document, sales-new-cdrl, sales-new-case) have their `<select>`/`<option>` dropdowns populated from tenant-scoped APIs (parts, projects, contracts) — NO hardcoded `PROJ-ANDROMEDA` / `SAT-001` options remain"
    - "quickbooks.html (QB → NS migration landing) renders the tenant's actual QB migration rows from /api/quickbooks/migration-runs (existing route per Phase 37); NO hardcoded Turion CUST-001 / RMP-TXN-44012 rows"
    - "workflow-e2e.html uses Phase 63 `[data-z-tenant-name]` populator wrappers (it is a narrative page per RESEARCH §Page Anatomy classified as 'Platform/marketing'); SAT-001 mentions replaced with first-item-id from window.ITEM_DATA OR hidden if empty"
    - "Every refactored list page in this batch has an inline `renderEmptyState(...)` fallback rendered when the bound API returns [] — verified via 5th synthetic test tenant smoke; CTA links to /onboarding/migrate"
    - "Turion preservation: every refactored page, when rendered as turion tenant, produces a Puppeteer snapshot whose bodyText shares >= 80% line overlap with the Plan 01 PRE snapshot"
  artifacts:
    - path: "/Users/jeet/turion-space-demo/arena-qms.html"
      provides: "QMS workspace — 6+ tabs each binding to /api/arena/{ncrs,capas,audits,procedures,persons,tools,qms-docs}; KPI strip uses window.NCR_DATA + window.CAPA_DATA from data-loader; empty-state inline"
      contains: "renderEmptyState"
    - path: "/Users/jeet/turion-space-demo/arena-bom.html"
      provides: "BOM tree render from /api/arena/bom; no hardcoded SAT-001 BOM"
      contains: "erpApi.get('/arena/bom')"
    - path: "/Users/jeet/turion-space-demo/quality-index.html"
      provides: "Quality index counts from /api/dashboards/cto (already covers NCR/CAPA/ECO counts per Phase 62)"
      contains: "data-bind"
    - path: "/Users/jeet/turion-space-demo/workflow-e2e.html"
      provides: "Phase 63 populator + dynamic first-item-id substitution for SAT-001 narrative refs"
      contains: "data-z-tenant-name"
  key_links:
    - from: "arena-*.html"
      to: "/api/arena/{ncrs,capas,audits,procedures,tools,qms-docs,bom}"
      via: "window.erpApi.get OR window.<X>_DATA hydrated by data-loader.js"
      pattern: "erpApi\\.get\\(['\"\\`]/arena|window\\.(NCR|CAPA|AUDIT|BOM)_DATA"
    - from: "quality-index.html, lifecycle-index.html"
      to: "/api/dashboards/<role>"
      via: "[data-bind] harness"
      pattern: "data-bind=\""
    - from: "quickbooks.html"
      to: "/api/quickbooks/migration-runs (Phase 37) + /api/data/all globals"
      via: "erpApi.get"
      pattern: "erpApi\\.get|quickbooks"
    - from: "sales-new-cdrl.html, sales-new-case.html"
      to: "/api/salesforce/customers + /api/netsuite/projects + /api/netsuite/contracts"
      via: "<select> populated by erpApi.get on page load"
      pattern: "erpApi\\.get|<option"
---

<objective>
Refactor Batch B: 11 Arena/QMS/lifecycle/quality/workflow pages from hardcoded Turion HTML to tenant-data-driven rendering. Runs IN PARALLEL with Plan 02 (Batch C dashboards) — these two plans modify disjoint file sets so they can land independently.

Purpose: Solo Brands users opening arena-qms.html see THEIR NCRs/CAPAs/audits (or empty state if none). Turion users continue to see the existing SAT-001 quality workspace because `/api/arena/*` endpoints are already populated for Turion (per Phase 36 §"Turion DB-direct E2E walk … 4 sats / 165 part_definitions … intact"). New tenants get empty states with import-wizard CTAs.

Output: 11 modified HTML files, zero backend changes (every endpoint these pages need already exists per RESEARCH §"API Coverage Map" — already covered list).
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-RESEARCH.md
@/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-SUMMARY.md
@/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-AUDIT.json
@/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-TURION-SNAPSHOT-PRE.json

# Existing patterns to mirror
@/Users/jeet/turion-space-demo/data-loader.js
@/Users/jeet/turion-space-demo/erp-api.js
@/Users/jeet/turion-space-demo/backend/src/routes/arena.ts
@/Users/jeet/turion-space-demo/backend/src/routes/extras.ts
@/Users/jeet/turion-space-demo/page-template.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor 4 Arena list pages (arena-qms, arena-bom, quality-index, lifecycle-index) + workflow-e2e narrative page</name>
  <files>/Users/jeet/turion-space-demo/arena-qms.html, /Users/jeet/turion-space-demo/arena-bom.html, /Users/jeet/turion-space-demo/quality-index.html, /Users/jeet/turion-space-demo/lifecycle-index.html, /Users/jeet/turion-space-demo/workflow-e2e.html</files>
  <action>
**Read 65.3-01-AUDIT.json first** to get exact (file, token, line) violations for each of these 5 files. The audit lines are your refactor targeting list.

**arena-qms.html (heaviest):**
1. Title change: replace `Arena QMS · SAT-001 · Quality Management Workspace` with `Arena QMS · Quality Management Workspace` (drop the SAT-001 segment — per RESEARCH §"arena-qms.html — Verdict"). The Phase 63 populator already swaps "Turion Space" via `data-z-tenant-name`.
2. The 6+ tabs (Documents, NCRs, CAPAs, Audits, Procedures, Tools, QMS Docs) — each tab's table should bind to the corresponding `/api/arena/*` endpoint. Per RESEARCH §"arena-qms.html" verdict: "most binding is already done; just need to (a) strip the SAT-001 literal from title, (b) ensure empty state when data is empty". So this is largely surgical.
3. For each tab table, replace any inline `<tr>` with hardcoded Turion-token content using the data-driven recipe from RESEARCH §"Existing Pattern: List-page refactor recipe". Bind to `Object.values(window.NCR_DATA || {})`, `window.CAPA_DATA`, etc. (data-loader hydrates these from `/api/data/all`).
4. Wrap each render in `window.addEventListener('turion-data-ready', () => {...})`.
5. Add `renderEmptyState({module: 'NCRs'})` (or 'CAPAs', etc.) inline.

**arena-bom.html:**
1. Replace hardcoded BOM tree literal with render from `/api/arena/bom` via `erpApi.get('/arena/bom')`. Existing endpoint per `backend/src/routes/arena.ts:r.get('/bom', ...)`.
2. Empty state: "No BOM yet. Run the import wizard →"

**quality-index.html and lifecycle-index.html (module landings):**
These are count-display pages. Use Phase 62 `[data-bind]` harness against `/api/dashboards/cto` (which includes NCR/CAPA/ECO/MES counts per RESEARCH §"Already covered" list). Add `data-bind="ncrs"`, `data-bind="capas"`, `data-bind="ecos"`, etc. on the relevant `<span>`/`<div>` tags. Ensure `<script src="dashboard-live-bind.js"></script>` is loaded.

**workflow-e2e.html (narrative platform page):**
1. Per ROADMAP scope note ("workflow-e2e: marketing/showcase pages get Phase 63 tenant-name populator ONLY, NOT data refactor"). 
2. Wrap every "Turion Space" literal with `<span data-z-tenant-name>Turion Space</span>`.
3. The narrative mentions `SAT-001` ~10+ times in the "follow a satellite through the ERP" story. Per RESEARCH §"workflow-e2e.html — Strategy": "replace 'SAT-001' → first item id from Solo Brands". Pattern: wrap each `SAT-001` in `<span class="z-first-item-id">SAT-001</span>` and add a tiny inline script at end-of-body:
```javascript
window.addEventListener('turion-data-ready', () => {
  const firstItemId = Object.keys(window.ITEM_DATA || {})[0];
  if (firstItemId) {
    document.querySelectorAll('.z-first-item-id').forEach(el => el.textContent = firstItemId);
  }
  // If no items, leave SAT-001 as fallback (workflow-e2e is a demo narrative; an unbacked tenant
  // still sees a coherent example, just one based on Turion's data)
});
```
4. DO NOT remove the narrative structure — workflow-e2e is intentionally a story, not a list page.

**Mid-task verification:** Run audit and snapshot diff on these 5 pages:
```bash
cd /Users/jeet/turion-space-demo
node scripts/audit-hardcoded-erp.mjs > /tmp/post-batch-b-arena.json
node -e "
const before = require('/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-AUDIT.json');
const after = require('/tmp/post-batch-b-arena.json');
const files = ['arena-qms.html','arena-bom.html','quality-index.html','lifecycle-index.html','workflow-e2e.html'];
for (const f of files) {
  const b = (before.by_file[f]||[]).length;
  const a = (after.by_file[f]||[]).length;
  console.log(f, 'before:', b, 'after:', a);
}
"
```
Workflow-e2e.html may retain SAT-001 as fallback text (acceptable — it's wrapped in `<span class=z-first-item-id>` and gets replaced at runtime for tenants WITH items). The audit script will still flag it; that's expected. Document this exception in the SUMMARY.
  </action>
  <verify>
1. `grep -c "Arena QMS · SAT-001\|SAT-001 · Quality" /Users/jeet/turion-space-demo/arena-qms.html` returns 0
2. `grep -l "renderEmptyState\|empty-state" /Users/jeet/turion-space-demo/arena-{qms,bom}.html | wc -l` returns 2
3. `grep -l "data-bind" /Users/jeet/turion-space-demo/quality-index.html /Users/jeet/turion-space-demo/lifecycle-index.html | wc -l` returns 2
4. `grep -c "data-z-tenant-name" /Users/jeet/turion-space-demo/workflow-e2e.html` returns >= 3 (at least 3 brand-text wrappers)
5. `grep -c "z-first-item-id" /Users/jeet/turion-space-demo/workflow-e2e.html` returns >= 5 (multiple SAT-001 mentions wrapped)
6. Turion preservation diff (subset of Plan 01 snapshot check for these 5 pages) passes >= 80% line overlap
  </verify>
  <done>
- arena-qms title cleaned; tabs bound to /api/arena/* via data-loader globals + erpApi fallback.
- arena-bom renders /api/arena/bom with empty-state.
- quality-index + lifecycle-index use [data-bind] harness.
- workflow-e2e uses Phase 63 populator + dynamic first-item-id substitution; SAT-001 fallback retained as last-resort narrative text (documented exception).
- Turion preservation diff passes on all 5 pages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor 4 create-form pages (arena-new-part/audit/document, sales-new-cdrl/case) + quickbooks landing</name>
  <files>/Users/jeet/turion-space-demo/arena-new-part.html, /Users/jeet/turion-space-demo/arena-new-audit.html, /Users/jeet/turion-space-demo/arena-new-document.html, /Users/jeet/turion-space-demo/sales-new-cdrl.html, /Users/jeet/turion-space-demo/sales-new-case.html, /Users/jeet/turion-space-demo/quickbooks.html</files>
  <action>
**Read 65.3-01-AUDIT.json** first for the (file, token, line) targeting list. Open Q3 from RESEARCH §"Open Questions": sales-new-* may be empty forms with no Turion data; arena-new-* may have hardcoded `<option>` dropdowns. Audit tells you which.

**Per page recipe:**

1. **Identify hardcoded `<option>` values** that reference Turion-specific IDs (e.g., `<option value="PROJ-ANDROMEDA-SAT-001">PROJ-ANDROMEDA</option>`). Replace with empty `<select id="projectSelect"></select>` and populate from API on page load:
```javascript
window.addEventListener('turion-data-ready', () => {
  const projects = Object.values(window.PROJECT_DATA || {});
  const sel = document.getElementById('projectSelect');
  if (!projects.length) {
    sel.innerHTML = '<option value="">No projects — please create one first</option>';
    sel.disabled = true;
    return;
  }
  sel.innerHTML = '<option value="">— Select a project —</option>' + 
    projects.map(p => `<option value="${esc(p.id)}">${esc(p.name || p.id)}</option>`).join('');
});
```

2. **Per-page specifics** (consult audit for each):
   - `arena-new-part.html`: parent-part dropdown → `/api/arena/parts` (or `window.PART_DATA`); subsystem dropdown → `/api/lookups/subsystems` (already exists per Phase 36 lookup endpoints)
   - `arena-new-audit.html`: auditor dropdown → `window.PERSON_DATA`; scope dropdown → `/api/arena/qms-docs` (procedures)
   - `arena-new-document.html`: doc-type dropdown → static enum (acceptable to hardcode the enum since it's not tenant data, but verify with audit)
   - `sales-new-cdrl.html`: contract dropdown → `/api/netsuite/contracts` (or `window.CONTRACT_DATA`); CLIN dropdown depends on selected contract — `change` handler
   - `sales-new-case.html`: customer dropdown → `/api/salesforce/customers` (already covered); contact dropdown → filtered by customer
   - `quickbooks.html`: Q3 from RESEARCH — replace hardcoded migration sample rows with `/api/quickbooks/migration-runs` (exists per Phase 37). Empty state: "No QuickBooks migration runs yet. Start with a sample migration →" linking to the QB type-picker.

3. **For every dropdown, include the `esc()` XSS guard** (Pitfall 5).

4. **For every `<select>` that depends on another `<select>`** (CLIN depending on contract): add a `change` handler that re-fetches and re-populates the dependent dropdown.

5. **Forms themselves (the `<input>` fields)** — do NOT touch unless they have hardcoded Turion defaults (e.g., `<input type="text" value="PROJ-ANDROMEDA">`). If they do, change to empty value `<input type="text" value="">`.

**Quickbooks special case:** quickbooks.html may not have hardcoded Turion tokens directly (audit will tell you). If audit shows 0 violations, this file gets ONLY a Phase 63 populator pass (replace any literal "Turion Space" with `<span data-z-tenant-name>`). No data refactor needed.

**Verify after each page:** re-run audit on these 6 files only:
```bash
cd /Users/jeet/turion-space-demo
for f in arena-new-part.html arena-new-audit.html arena-new-document.html sales-new-cdrl.html sales-new-case.html quickbooks.html; do
  beforeCount=$(node -e "const r=require('/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-AUDIT.json'); console.log((r.by_file['$f']||[]).length)")
  afterCount=$(grep -cE "SAT-001|ADCS-RW-MEDIUM|Blue Canyon|PROJ-ANDROMEDA|CLIN-004|Honeywell|BCT-RW-|Cygnus" "$f" || echo 0)
  echo "$f: before=$beforeCount after=$afterCount"
done
```

**Turion preservation snapshot for these 6 pages:** capture and diff:
```bash
node scripts/snapshot-turion-erp-views.mjs turion > /tmp/turion-snapshot-after-batch-b.json
node -e "
const before = require('/Users/jeet/doordash-p2p/.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-01-TURION-SNAPSHOT-PRE.json');
const after = require('/tmp/turion-snapshot-after-batch-b.json');
const pages = ['arena-new-part','arena-new-audit','arena-new-document','sales-new-cdrl','sales-new-case','quickbooks'];
for (const p of pages) {
  const b = before.pages[p]; const a = after.pages[p];
  if (!b || !a) { console.error('missing snapshot for', p); process.exit(1); }
  const beforeLines = new Set(b.bodyText.split('\n').map(s => s.trim()).filter(s => s.length > 4));
  const afterLines  = new Set(a.bodyText.split('\n').map(s => s.trim()).filter(s => s.length > 4));
  const overlap = [...beforeLines].filter(l => afterLines.has(l)).length;
  const pct = beforeLines.size > 0 ? (overlap / beforeLines.size) * 100 : 100;
  console.log(p, 'overlap:', pct.toFixed(1) + '%');
  // create-form pages have less content overlap because dropdowns now lazy-populate;
  // accept >=60% for forms, >=80% for quickbooks list
  const threshold = p.startsWith('arena-new-') || p.startsWith('sales-new-') ? 60 : 80;
  if (pct < threshold) { console.error('TURION PRESERVATION FAIL on', p, 'pct:', pct.toFixed(1)); process.exit(1); }
}
"
```

**Cross-tenant safety check:** Snapshot AS SOLO BRANDS and assert NO Turion tokens in the rendered DOM:
```bash
# (Uses Solo Brands COGNITO_ID_TOKEN — Plan 01 SUMMARY documents how to mint)
export COGNITO_ID_TOKEN=$SB_TOKEN
node scripts/snapshot-turion-erp-views.mjs solobrands > /tmp/sb-snapshot-after-batch-b.json
node -e "
const sb = require('/tmp/sb-snapshot-after-batch-b.json');
const pages = ['arena-qms','arena-bom','arena-new-part','quality-index','lifecycle-index','quickbooks','sales-new-cdrl','sales-new-case'];
for (const p of pages) {
  const view = sb.pages[p];
  if (view.hardcodedTokens.length > 0) {
    console.error('SOLO BRANDS LEAK on', p, '— tokens:', view.hardcodedTokens);
    process.exit(1);
  }
}
console.log('cross-tenant safety: PASS');
"
```
  </action>
  <verify>
1. `grep -c "PROJ-ANDROMEDA\|SAT-001\|CLIN-004\|ADCS-RW-MEDIUM" /Users/jeet/turion-space-demo/arena-new-part.html /Users/jeet/turion-space-demo/arena-new-audit.html /Users/jeet/turion-space-demo/sales-new-cdrl.html /Users/jeet/turion-space-demo/sales-new-case.html` returns 0 across all 4
2. `grep -l "erpApi.get\|window\\.\\(PROJECT\\|PART\\|PERSON\\|CONTRACT\\|CUSTOMER\\)_DATA" /Users/jeet/turion-space-demo/arena-new-part.html /Users/jeet/turion-space-demo/arena-new-audit.html /Users/jeet/turion-space-demo/arena-new-document.html /Users/jeet/turion-space-demo/sales-new-cdrl.html /Users/jeet/turion-space-demo/sales-new-case.html | wc -l` returns >= 4
3. Turion preservation diff exit 0 (60-80% threshold per page type)
4. Cross-tenant safety check exit 0 (zero Turion tokens leaked into Solo Brands rendering of these pages)
5. `grep -c "function esc\|escape(s)" /Users/jeet/turion-space-demo/arena-new-part.html /Users/jeet/turion-space-demo/sales-new-cdrl.html /Users/jeet/turion-space-demo/sales-new-case.html` returns 3
  </verify>
  <done>
- 5 create-form pages have dynamic dropdowns populated from tenant-scoped APIs; no hardcoded Turion `<option>` values remain.
- quickbooks.html either uses Phase 63 populator (if audit showed no violations) or list-page refactor recipe (if it had hardcoded migration rows).
- Turion preservation diff passes (60% threshold for forms, 80% for list pages).
- Cross-tenant safety verified: Solo Brands sees zero Turion tokens on any of the 11 Batch B pages.
  </done>
</task>

</tasks>

<verification>
- Audit re-run on Batch B files shows >= 80% reduction in violations across the 11 files (workflow-e2e retains SAT-001 as `<span class=z-first-item-id>` fallback — documented exception).
- Turion preservation snapshot diff passes for all 11 pages at the threshold appropriate to page type.
- Cross-tenant safety snapshot proves Solo Brands sees zero Turion tokens on any Batch B page.
- Zero backend changes (every endpoint already exists per RESEARCH §"API Coverage Map").
</verification>

<success_criteria>
- `PageRefactorBatchB` requirement satisfied: 11 pages converted using the appropriate pattern (list-page recipe / dropdown-from-API / Phase 63 populator / data-bind).
- `EmptyStatesAcrossPages` partial credit: every refactored list page in this batch has inline `renderEmptyState(...)` fallback (full requirement coverage lands in Plan 05 smoke harness 5th-tenant assertions).
- Turion's QMS workspace is byte-functionally preserved.
- Solo Brands users opening any Batch B page see ZERO Turion-aerospace tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/65.3-tenant-data-driven-erp-detail-pages/65.3-03-SUMMARY.md` containing:
- Per-file: violations before, violations after, lines changed, page type (list / create-form / narrative)
- Turion preservation diff results (11 pages × overlap pct)
- Cross-tenant safety result (Solo Brands snapshot — zero Turion tokens confirmed)
- workflow-e2e.html documented exception: SAT-001 retained as `<span class=z-first-item-id>` fallback for unbacked tenants
- Any rolled-back refactors (with diagnosis)
- Hand-off to Plan 05: list of pages that should appear empty in the 5th-tenant smoke test
</output>
