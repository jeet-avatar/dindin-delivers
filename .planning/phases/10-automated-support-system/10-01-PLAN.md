---
phase: 10-automated-support-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ios/restaurant/eatffairrestaurant/Views/AIEmployeesView.swift
  - apps/ios/restaurant/eatffairrestaurant/Views/RestaurantSettingsView.swift
  - apps/ios/restaurant/eatffairrestaurant/Views/EnhancedDashboardView.swift
  - apps/ios/customer/eatfaircustomer/Views/OrderChatView.swift
  - apps/ios/customer/eatfaircustomer/Views/HelpSupportView.swift
  - apps/ios/delivery/eatffairdelivery/Views/OrderChatView.swift
  - apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift
autonomous: true
requirements:
  - SUPPORT-01
  - SUPPORT-03

must_haves:
  truths:
    - "Restaurant app compiles and runs without showing AI Employees, AI Feature Toggles, AI Workforce, or AI Suggestion Banner"
    - "Restaurant app still shows AI Insights tab (Tab 3) with real analytics data"
    - "Customer app shows OrderChatView when navigating to chat from an active food delivery order"
    - "Driver app shows OrderChatView when navigating to chat from an active delivery"
    - "HelpSupportView Call Us button dials +18003655671 (1-800-DOLLOR)"
    - "Order chat messages send and receive via REST polling (3s interval)"
  artifacts:
    - path: "apps/ios/restaurant/eatffairrestaurant/Views/AIEmployeesView.swift"
      provides: "AI Employees view wrapped in #if ENABLE_AI_EMPLOYEES"
      contains: "#if ENABLE_AI_EMPLOYEES"
    - path: "apps/ios/restaurant/eatffairrestaurant/Views/RestaurantSettingsView.swift"
      provides: "AI Features and AI Workforce sections wrapped in #if ENABLE_AI_EMPLOYEES"
      contains: "#if ENABLE_AI_EMPLOYEES"
    - path: "apps/ios/restaurant/eatffairrestaurant/Views/EnhancedDashboardView.swift"
      provides: "AI Suggestion Banner wrapped in #if ENABLE_AI_EMPLOYEES"
      contains: "#if ENABLE_AI_EMPLOYEES"
    - path: "apps/ios/customer/eatfaircustomer/Views/OrderChatView.swift"
      provides: "Food delivery order chat for customer app"
      min_lines: 150
    - path: "apps/ios/delivery/eatffairdelivery/Views/OrderChatView.swift"
      provides: "Food delivery order chat for driver app"
      min_lines: 150
    - path: "apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift"
      provides: "fetchOrderChatMessages and sendOrderChatMessage API methods"
      contains: "fetchOrderChatMessages"
  key_links:
    - from: "apps/ios/customer/eatfaircustomer/Views/OrderChatView.swift"
      to: "/api/customer/orders/{order_id}/chat"
      via: "P2PAPIService.shared.fetchOrderChatMessages / sendOrderChatMessage"
      pattern: "orders.*chat"
    - from: "apps/ios/delivery/eatffairdelivery/Views/OrderChatView.swift"
      to: "/api/customer/orders/{order_id}/chat"
      via: "P2PAPIService.shared"
      pattern: "orders.*chat"
    - from: "apps/ios/customer/eatfaircustomer/Views/HelpSupportView.swift"
      to: "tel:+18003655671"
      via: "AppConfig.shared.supportPhone"
      pattern: "supportPhone"
---

<objective>
Hide aspirational AI features from Restaurant app, build OrderChatView for food delivery, fix HelpSupportView phone number.

Purpose: SUPPORT-01 removes confusing placeholder AI features from the restaurant app. SUPPORT-03 adds functional order-driver chat and fixes the broken support phone number.
Output: Restaurant app compiles without AI Employees/Toggles/Workforce/Banner visible. Customer and Driver apps have working OrderChatView for food delivery. HelpSupportView dials correct 1-800-DOLLOR number.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-automated-support-system/10-CONTEXT.md
@.planning/phases/10-automated-support-system/10-RESEARCH.md

# Source files to reference/clone
@apps/ios/restaurant/eatffairrestaurant/Views/AIEmployeesView.swift
@apps/ios/restaurant/eatffairrestaurant/Views/RestaurantSettingsView.swift
@apps/ios/restaurant/eatffairrestaurant/Views/EnhancedDashboardView.swift
@apps/ios/customer/eatfaircustomer/Views/DriverChatView.swift
@apps/ios/customer/eatfaircustomer/Views/HelpSupportView.swift
@apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap aspirational AI features with compile-time flag in Restaurant app</name>
  <files>
    apps/ios/restaurant/eatffairrestaurant/Views/AIEmployeesView.swift
    apps/ios/restaurant/eatffairrestaurant/Views/RestaurantSettingsView.swift
    apps/ios/restaurant/eatffairrestaurant/Views/EnhancedDashboardView.swift
  </files>
  <action>
    Apply `#if ENABLE_AI_EMPLOYEES` / `#endif` compile-time feature flag to 4 locations in the Restaurant app. Since `ENABLE_AI_EMPLOYEES` is NOT defined in Active Compilation Conditions, all wrapped code is compiled out by default.

    1. **AIEmployeesView.swift** (entire file, 1152 lines):
       - Wrap the entire file contents (all structs, view models, preview providers) with `#if ENABLE_AI_EMPLOYEES` / `#endif`
       - Keep the `import` statements outside the flag so the file compiles cleanly
       - This hides: AIEmployeesView, CreateAIEmployeeView, AIEmployeeDetailView, AITaskQueueView, AIAuditLogView, and all view models
       - Before wrapping, check if any other Restaurant app file references these types (grep for `AIEmployeesView`, `AIEmployeesViewModel`, `AITaskQueueView`, `AIAuditLogView` in the restaurant app directory). If references exist outside this file, wrap those NavigationLink/references too.

    2. **RestaurantSettingsView.swift** -- AI Features section (lines ~338-392):
       - Find the `Section("AI Features")` block containing the three toggles (aiDemandPrediction, aiPrepTimeOptimization, aiMenuSuggestions)
       - Wrap the entire Section with `#if ENABLE_AI_EMPLOYEES` / `#endif`

    3. **RestaurantSettingsView.swift** -- AI Workforce section (lines ~394-415):
       - Find the `Section("AI Workforce")` or similar section containing `NavigationLink(destination: AIEmployeesView())`
       - Wrap the entire Section with `#if ENABLE_AI_EMPLOYEES` / `#endif`

    4. **EnhancedDashboardView.swift** -- AI Suggestion Banner (lines ~91-93):
       - Find `if let suggestion = ordersVM.aiSuggestion { AISuggestionBanner(...) }` block
       - Wrap with `#if ENABLE_AI_EMPLOYEES` / `#endif`
       - Also wrap the `AISuggestionBanner` struct definition (line ~332) with the same flag

    CRITICAL: Do NOT touch AIInsightsView or AIInsightsViewModel -- these are functional Tab 3 views with real backend data and must remain untouched.
    CRITICAL: Do NOT modify shared models in eatfair-ios-shared -- only flag Restaurant app views and navigation links.
    CRITICAL: Leave the backend mock endpoint `/api/erp/analytics/ai-employees` as-is (harmless).
  </action>
  <verify>
    Build the Restaurant app to confirm it compiles without ENABLE_AI_EMPLOYEES defined:
    ```
    xcodebuild -workspace apps/ios/EatFair.xcworkspace -scheme eatffairrestaurant -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5
    ```
    Should complete with BUILD SUCCEEDED and zero errors. Grep the build output for "ENABLE_AI_EMPLOYEES" to confirm it is NOT in the active flags.
  </verify>
  <done>
    Restaurant app compiles without errors. AIEmployeesView, AI Feature Toggles, AI Workforce section, and AI Suggestion Banner are all compiled out. AI Insights tab (Tab 3) is unaffected and still shows analytics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build OrderChatView for Customer and Driver apps, fix phone number</name>
  <files>
    apps/ios/customer/eatfaircustomer/Views/OrderChatView.swift
    apps/ios/delivery/eatffairdelivery/Views/OrderChatView.swift
    apps/ios/customer/eatfaircustomer/Views/HelpSupportView.swift
    apps/ios/eatfair-ios-shared/Sources/EatFairShared/Services/P2PAPIService.swift
  </files>
  <action>
    **Part A: Add order chat API methods to P2PAPIService.swift**

    Add two new methods following the existing network call pattern (see DriverChatView's `fetchRideChatMessages` / `sendRideChatMessage` as reference):

    1. `fetchOrderChatMessages(orderId: Int, completion: @escaping (Result<[OrderChatMessage], Error>) -> Void)`:
       - GET `/api/customer/orders/{orderId}/chat`
       - Auth: customer token OR driver token (check which token is available)
       - Response: array of chat message objects with `id`, `sender_type`, `message`, `created_at`
       - Define `OrderChatMessage` struct: `Codable, Identifiable, Sendable` with fields matching backend response

    2. `sendOrderChatMessage(orderId: Int, message: String, senderType: String, completion: @escaping (Result<Bool, Error>) -> Void)`:
       - POST `/api/customer/orders/{orderId}/chat`
       - Body: `{"message": "...", "sender_type": "customer"|"driver"}`
       - Auth: customer token OR driver token
       - Returns success/failure

    Before implementing, verify the exact backend response format:
    ```
    grep -A 30 "orders.*chat" apps/web/p2p-platform/backend/main_new.py | head -60
    ```

    **Part B: Create OrderChatView.swift for Customer app**

    Clone the structure of `DriverChatView.swift` (318 lines) to create `OrderChatView.swift` for the Customer app.

    Key elements to replicate from DriverChatView:
    - Properties: `orderId: Int`, `driverName: String`, `driverPhone: String?`
    - `@State private var messages: [OrderChatMessage] = []`
    - `@State private var messageText = ""`
    - Timer-based REST polling every 3 seconds (`Timer.scheduledTimer`)
    - Quick message buttons in horizontal ScrollView: "Where is my order?", "How long will it take?", "I'm at the door", "Please leave at the door", "Can you call me?", "Thank you!"
    - Message input bar with TextField and send button
    - Chat bubble component (replicate the RideChatBubbleCustomer pattern but for orders)
    - `onAppear` starts polling, `onDisappear` stops polling
    - Uses `P2PAPIService.shared.fetchOrderChatMessages` / `sendOrderChatMessage`

    Do NOT implement WebSocket upgrade (deferred per user decision) -- use REST polling only.

    **Part C: Create OrderChatView.swift for Driver app**

    Similar to Part B but for the Driver app:
    - Properties: `orderId: Int`, `customerName: String`
    - Quick messages: "On my way!", "Arrived at restaurant", "Picked up your order", "Almost there!", "I'm at the door", "Delivered!"
    - Uses driver token for auth
    - sender_type = "driver" when sending

    **Part D: Fix HelpSupportView.swift phone number**

    In `HelpSupportView.swift` (line ~218):
    - Find the hardcoded `tel:+18001234567`
    - Replace with dynamic phone from AppConfig: `tel:\(AppConfig.shared.supportPhone.replacingOccurrences(of: "-", with: ""))`
    - Reference pattern: `PlaceholderViews.swift:121` uses this exact approach
    - The result should dial `+18003655671` (1-800-DOLLOR)
  </action>
  <verify>
    Build all three iOS apps to confirm they compile:
    ```
    xcodebuild -workspace apps/ios/EatFair.xcworkspace -scheme eatfaircustomer -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5
    xcodebuild -workspace apps/ios/EatFair.xcworkspace -scheme eatffairdelivery -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5
    ```
    Both should BUILD SUCCEEDED. Grep for `fetchOrderChatMessages` in P2PAPIService.swift to confirm the API method exists. Grep for `18001234567` in HelpSupportView.swift to confirm the old number is removed.
  </verify>
  <done>
    Customer app has OrderChatView with polling, quick messages, and input bar. Driver app has OrderChatView with driver-specific quick messages. P2PAPIService has fetchOrderChatMessages and sendOrderChatMessage methods. HelpSupportView dials +18003655671.
  </done>
</task>

</tasks>

<verification>
1. Restaurant app builds without AI Employees, AI Feature Toggles, AI Workforce, or AI Suggestion Banner visible
2. AI Insights tab (Tab 3) is untouched and compiles normally
3. Customer app builds with OrderChatView available
4. Driver app builds with OrderChatView available
5. HelpSupportView uses AppConfig.shared.supportPhone (not hardcoded)
6. P2PAPIService has order chat API methods
7. No deferred features implemented (no WebSocket upgrade, no restaurant chat)
</verification>

<success_criteria>
- All 3 iOS apps compile without errors
- `#if ENABLE_AI_EMPLOYEES` wraps exactly 4 locations in Restaurant app
- OrderChatView exists in both Customer and Driver apps with REST polling
- HelpSupportView phone number is dynamic from AppConfig
- Zero references to WebSocket upgrade in chat code
</success_criteria>

<output>
After completion, create `.planning/phases/10-automated-support-system/10-01-SUMMARY.md`
</output>
