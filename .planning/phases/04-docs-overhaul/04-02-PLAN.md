---
phase: 04-docs-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/docs/GROUND_TRUTH.md
  - .claude/docs/API_ENDPOINTS.md
  - .claude/agents/QA_KNOWLEDGE_BASE.md
  - docs/TIER2-ANDROID-IMPLEMENTATION-GUIDE.md
autonomous: true
requirements: [DOC-04, DOC-05, DOC-06]

must_haves:
  truths:
    - "GROUND_TRUTH line numbers match actual code positions in main_new.py and models.py"
    - "GROUND_TRUTH staging URL says d34u5ixl0bulv4.cloudfront.net"
    - "API_ENDPOINTS staging URL says d34u5ixl0bulv4.cloudfront.net"
    - "API_ENDPOINTS auth column reflects post-Phase-02 requirements"
    - "QA_KNOWLEDGE_BASE uses correct staging URL"
    - "TIER2 guide uses :driver not :orderapp for driver module"
  artifacts:
    - path: ".claude/docs/GROUND_TRUTH.md"
      provides: "Backend-verified facts with correct file:line references"
      contains: "d34u5ixl0bulv4"
    - path: ".claude/docs/API_ENDPOINTS.md"
      provides: "API reference with correct auth requirements"
      contains: "d34u5ixl0bulv4"
    - path: ".claude/agents/QA_KNOWLEDGE_BASE.md"
      provides: "QA agent knowledge with correct staging URL"
      contains: "d34u5ixl0bulv4"
    - path: "docs/TIER2-ANDROID-IMPLEMENTATION-GUIDE.md"
      provides: "Android guide with correct module names"
      contains: ":driver"
  key_links:
    - from: ".claude/docs/GROUND_TRUTH.md"
      to: "apps/web/p2p-platform/backend/main_new.py"
      via: "file:line references"
      pattern: "main_new\\.py:\\d+"
    - from: ".claude/docs/GROUND_TRUTH.md"
      to: "apps/web/p2p-platform/backend/models.py"
      via: "file:line references"
      pattern: "models\\.py:\\d+"
---

<objective>
Re-verify all GROUND_TRUTH.md line number references against current code, fix staging URLs in API_ENDPOINTS.md and QA_KNOWLEDGE_BASE.md, update API auth requirements, and fix stale module names in TIER2 guide.

Purpose: GROUND_TRUTH.md is the anti-hallucination reference. Phase 02 added ~450+ lines to main_new.py, shifting every line reference. Wrong line numbers cause Claude to look at wrong code and make incorrect claims. API_ENDPOINTS.md and QA_KNOWLEDGE_BASE.md still reference the old staging URL. The TIER2 guide references `:orderapp` which is the old driver module name (now `:driver`).

Output: GROUND_TRUTH.md with verified line numbers, API_ENDPOINTS.md with correct staging URL and auth info, QA_KNOWLEDGE_BASE.md with correct staging URL, TIER2 guide with correct module name.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/docs/GROUND_TRUTH.md
@.claude/docs/API_ENDPOINTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify and fix all GROUND_TRUTH.md line references</name>
  <files>.claude/docs/GROUND_TRUTH.md</files>
  <action>
Phase 02 added auth middleware, auth_utils.py imports, and Depends() calls throughout main_new.py, shifting line numbers by 400-700+ lines (non-uniform shift). Every `main_new.py:NNNN` reference in GROUND_TRUTH.md is now wrong.

For EACH line reference in GROUND_TRUTH.md, grep the actual code to find the current line number and update it. Here is the mapping of what to search for:

**Section 1 — Registration Fields:**
- `main_new.py:2622` -> grep for `@app.post("/api/auth/customer/register")` (now ~3078)
- `main_new.py:2677` -> grep for customer name split logic near register endpoint
- `main_new.py:2079` -> grep for `@app.post("/api/auth/driver/register")` (now ~2538)
- `main_new.py:1469` -> grep for `@app.post("/api/auth/vendor/register")` (now ~1915)
- `main_new.py:1522` -> grep for vendor `contact_name` assignment near register

**Section 2 — Active Status:**
- `models.py:624` -> grep for `is_active` on Customer model (now ~636)
- `main_new.py:2594` -> grep for `not customer.is_active` login check (now ~3054)
- `main_new.py:2695` -> grep for customer `is_active=True` at registration
- `models.py:564` -> grep for `class CustomerStatus` (now ~572)
- `models.py:690-696` -> grep for `class DriverStatus` (now ~702)
- `main_new.py:2022` -> grep for `DriverStatus.SUSPENDED` login check (now ~2481)
- `main_new.py:2111` -> grep for `DriverStatus.PENDING` at registration (now ~2581)
- `models.py:27-32` -> grep for `class VendorStatus` (still ~27)
- `main_new.py:1314` -> grep for vendor `not approved` login check (now ~1764)

**Section 3 — Vehicle Fields:**
- `models.py:721-726` -> grep for `vehicle_make` etc. (now ~734-739)

**Section 6 — Order Status:**
- `models.py:383-403` -> grep for `class OrderStatus` (now ~385)

**Section 7 — Ride Request Status:**
- `models.py:1248-1255` -> grep for `class RideRequestStatus` (now ~1260)

**Section 8 — Bid Status:**
- `models.py:1258-1264` -> grep for `class BidStatus` (now ~1270)

**Section 9 — Login Endpoints:** No line refs, skip.

**Section 10 — Rideshare Bidding Flow:**
- `main_new.py:14495` -> grep for `/api/rides/available` definition
- `main_new.py:4618` -> grep for `complete-and-pay` (now ~5403)
- `main_new.py:14373` -> grep for `/api/rides/{ride_id}/rate`
- `main_new.py:14595` -> grep for `ride-requests/{id}/chat` GET
- `main_new.py:14609` -> grep for `ride-requests/{id}/chat` POST
- `main_new.py:2498` -> grep for `driver/location` PUT with latitude
- `main_new.py:19066` -> grep for `/api/driver/location` POST

**Section 11 — RideRequest Model:**
- `models.py:1267` -> grep for `class RideRequest` (now ~1279)

**Section 14 — Stripe:**
- `main_new.py:4091` -> grep for driver Stripe Express/4121 (now ~4797)
- `main_new.py:4412` -> grep for vendor Stripe/5812
- `main_new.py:16654` -> grep for `erp/payments/intent`
- `main_new.py:4117` -> grep for driver onboarding-link
- `main_new.py:4179` -> grep for driver stripe status
- `main_new.py:4438` -> grep for vendor onboarding-link
- `main_new.py:4288` -> grep for `webhooks/stripe-connect`

**Section 15 — Admin Security:**
- `main_new.py:167` -> grep for `admin_auth_middleware` definition (now ~196)
- `main_new.py:182-199` -> grep for JWT Bearer in admin middleware
- `main_new.py:204-208` -> grep for ADMIN_SECRET_KEY query param
- `main_new.py:165-166` -> grep for admin exempt paths
- `main_new.py:175` -> grep for OPTIONS preflight
- `main_new.py:155` -> grep for `Vary: Origin`
- `main_new.py:157` -> grep for credentials stripping

**Section 17 — Edge Case Fixes:**
- `main_new.py:14527` -> grep for tip_amount body reading
- `main_new.py:14512` -> grep for rating 1-5 validation
- `main_new.py:14341` -> grep for cancel completed ride check
- `main_new.py:3425-3460` -> grep for ride request validation (empty dict, null coords)

**Section 4 — Pricing:** `order_flow.py` and `rideshare_payments.py` line numbers may also have shifted. Check:
- `order_flow.py:400-401` -> grep for CUSTOMER_SERVICE_FEE (now ~421-422)
- `order_flow.py:405-409` -> grep for base fee constants
- `rideshare_payments.py:36-43` -> grep for fare tier definitions (likely stable)
- `main_new.py:6286` -> grep for driver payout = delivery_fee + tip
- `main_new.py:6083` -> grep for FREEDELIVERY promo code (now ~6821)
- `order_flow.py:568` -> grep for DEFAULT_TAX_RATE
- `main_new.py:1071` -> grep for `/api/config` tax rate

**Section 13 — iOS xcconfig staging URL (line 302):**
Change `d3kuu45w6kl8hr.cloudfront.net` to `d34u5ixl0bulv4.cloudfront.net` in the Environment URLs table.

**Also update GROUND_TRUTH metadata:**
- Change `Last verified: February 16, 2026` to `Last verified: February 20, 2026`

**IMPORTANT:** For each reference, use `grep -n "PATTERN" FILE` to find the EXACT current line number. Do NOT estimate or apply a fixed offset -- the shift is non-uniform because code was added at multiple points in the file.
  </action>
  <verify>
Spot-check 5 critical references after update:
1. Verify `main_new.py` line for customer register matches: `grep -n 'post.*auth/customer/register' apps/web/p2p-platform/backend/main_new.py | head -1`
2. Verify `models.py` line for Customer is_active: `grep -n 'is_active.*Boolean.*True' apps/web/p2p-platform/backend/models.py | head -1`
3. Verify `main_new.py` line for admin_auth_middleware: `grep -n 'admin_auth_middleware' apps/web/p2p-platform/backend/main_new.py | head -1`
4. Verify `main_new.py` line for complete-and-pay: `grep -n 'complete-and-pay' apps/web/p2p-platform/backend/main_new.py | head -1`
5. Verify `models.py` line for RideRequest: `grep -n 'class RideRequest' apps/web/p2p-platform/backend/models.py`
6. `grep "d3kuu45w6kl8hr" .claude/docs/GROUND_TRUTH.md` returns NO results
7. `grep "February 20, 2026" .claude/docs/GROUND_TRUTH.md` returns 1 result
  </verify>
  <done>
Every `file:line` reference in GROUND_TRUTH.md matches the actual current code position. The staging URL in section 13 is correct. The "Last verified" date is current.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix API_ENDPOINTS.md staging URL + auth info</name>
  <files>.claude/docs/API_ENDPOINTS.md</files>
  <action>
Read `.claude/docs/API_ENDPOINTS.md` and make these changes:

**1. Fix staging URL (line 14):**
Change `https://d3kuu45w6kl8hr.cloudfront.net` to `https://d34u5ixl0bulv4.cloudfront.net`

**2. Update "Last Updated" (line 6):**
Change `February 3, 2026` to `February 20, 2026`

**3. Update auth requirements in endpoint tables:**
After Phase 02, most endpoints now require JWT auth. Update the Auth column for these key changes:

In the Customer App table:
- `List Vendors /api/vendors/published` — Auth stays `No` (public, on allowlist)
- `Vendor Menu /api/vendors/{id}/menu` — Auth stays `No` (public, on allowlist)
- All other customer endpoints already show `Bearer` — correct

In the Restaurant App table:
- `Get Orders /api/erp/orders/vendor/{id}` — Change `No*` to `Bearer` (now requires auth per Phase 02)
- `Menu Items /api/vendors/{id}/menu` — Auth stays `No` (public endpoint)
- `Vendor Profile /api/vendors/{id}` GET — Auth stays `No` (public endpoint)
- Remove the `*Note` footnote since vendor orders now require auth

**4. Add note about global auth middleware:**
After the "Authentication Pattern" section, add:
```
> **Security Note (Feb 2026):** A global auth middleware (`require_auth_middleware` in `main_new.py:367`) blocks unauthenticated requests to any endpoint not on the public allowlist. See `auth_utils.py` for role-specific auth dependencies.
```

**5. Update Deprecated Endpoints:**
Remove `/api/rides/estimate` from deprecated list — it is still active and used by iOS.
  </action>
  <verify>
1. `grep "d3kuu45w6kl8hr" .claude/docs/API_ENDPOINTS.md` returns NO results
2. `grep "d34u5ixl0bulv4" .claude/docs/API_ENDPOINTS.md` returns 1 result
3. `grep "February 20, 2026" .claude/docs/API_ENDPOINTS.md` returns 1 result
4. `grep "require_auth_middleware" .claude/docs/API_ENDPOINTS.md` returns 1 result
  </verify>
  <done>
API_ENDPOINTS.md has correct staging URL, current date, accurate auth requirements reflecting Phase 02 changes, and global auth middleware noted.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix QA_KNOWLEDGE_BASE.md and TIER2 guide</name>
  <files>.claude/agents/QA_KNOWLEDGE_BASE.md, docs/TIER2-ANDROID-IMPLEMENTATION-GUIDE.md</files>
  <action>
**QA_KNOWLEDGE_BASE.md:**
Read `.claude/agents/QA_KNOWLEDGE_BASE.md` and fix:
1. Replace ALL occurrences of `d3kuu45w6kl8hr.cloudfront.net` with `d34u5ixl0bulv4.cloudfront.net`
2. Search for any other wrong staging URLs and fix them

**TIER2-ANDROID-IMPLEMENTATION-GUIDE.md:**
Read `docs/TIER2-ANDROID-IMPLEMENTATION-GUIDE.md` and fix:
1. Replace ALL occurrences of `:orderapp` with `:driver` (the correct Android driver module name)
2. Check for `orderapp` in any other context (package references, paths) and update
3. The package name `ai.dollor.driver` is correct — do NOT change that, only the Gradle module reference
  </action>
  <verify>
1. `grep "d3kuu45w6kl8hr" .claude/agents/QA_KNOWLEDGE_BASE.md` returns NO results
2. `grep "d34u5ixl0bulv4" .claude/agents/QA_KNOWLEDGE_BASE.md` returns at least 1 result
3. `grep ":orderapp" docs/TIER2-ANDROID-IMPLEMENTATION-GUIDE.md` returns NO results
4. `grep ":driver" docs/TIER2-ANDROID-IMPLEMENTATION-GUIDE.md` returns at least 1 result
  </verify>
  <done>
QA_KNOWLEDGE_BASE.md has correct staging URL. TIER2 guide uses `:driver` module name instead of the old `:orderapp` name.
  </done>
</task>

</tasks>

<verification>
- `grep -r "d3kuu45w6kl8hr" .claude/docs/ .claude/agents/QA_KNOWLEDGE_BASE.md docs/TIER2-ANDROID-IMPLEMENTATION-GUIDE.md` returns NO matches
- `grep ":orderapp" docs/TIER2-ANDROID-IMPLEMENTATION-GUIDE.md` returns NO matches
- Spot-check 3 random GROUND_TRUTH line references against actual code — all match
</verification>

<success_criteria>
1. Every `main_new.py:NNNN` reference in GROUND_TRUTH.md verified against actual code
2. Every `models.py:NNN` reference in GROUND_TRUTH.md verified against actual code
3. Staging URL correct in API_ENDPOINTS.md, QA_KNOWLEDGE_BASE.md, and GROUND_TRUTH.md
4. API auth requirements updated for Phase 02 changes
5. TIER2 guide uses `:driver` not `:orderapp`
</success_criteria>

<output>
After completion, create `.planning/phases/04-docs-overhaul/04-02-SUMMARY.md`
</output>
