---
phase: 03-android-api-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-android-api-verification/03-01-REPORT-CUSTOMER.md
autonomous: true
requirements:
  - API-04

must_haves:
  truths:
    - "Every Retrofit endpoint in DollorApiService.kt used by the Customer app has been verified against backend routes"
    - "Every OkHttp URL in CustomerRideshareApiService.kt has been verified against backend routes"
    - "Each API call's HTTP method, path, and auth header has been cross-checked against backend route registrations"
    - "All mismatches are documented with severity (critical/medium/low) and fix approach (backend alias or client fix)"
    - "Dead code endpoints identified and categorized separately from actionable mismatches"
  artifacts:
    - path: ".planning/phases/03-android-api-verification/03-01-REPORT-CUSTOMER.md"
      provides: "Complete Android Customer app API verification report"
      contains: "## Summary"
  key_links:
    - from: "/Users/jeet/StudioProjects/eatfair-android/shared/src/main/java/ai/dollor/shared/data/remote/DollorApiService.kt"
      to: "apps/web/p2p-platform/backend/main_new.py"
      via: "Retrofit path matching against @app.get/@app.post route decorators"
      pattern: '@(GET|POST|PUT|DELETE|PATCH)\("'
    - from: "/Users/jeet/StudioProjects/eatfair-android/app/src/main/java/ai/dollor/customer/data/CustomerRideshareApiService.kt"
      to: "apps/web/p2p-platform/backend/main_new.py"
      via: "OkHttp URL string matching against backend routes"
      pattern: '\.url\("'
---

<objective>
Verify every API call made by the Android Customer app against actual backend routes. This covers:
1. Customer-facing sections of DollorApiService.kt (Retrofit): Public, Customer Auth, Customer Orders, Customer Addresses, Customer Favorites, Customer Rideshare, Customer Payment Cards, Promotions, Payments, Order Tracking, Legal, Push Notifications
2. CustomerRideshareApiService.kt (OkHttp): 24 direct URL calls for rideshare functionality

Purpose: Ensure the Android Customer app will not hit 404s, wrong methods, or parse failures when talking to the production backend. This is a prerequisite for Phase 05 (Android Distribution).

Output: A structured verification report (03-01-REPORT-CUSTOMER.md) documenting every API call, its verification status, and any mismatches with severity and fix approach.
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/API_REGISTRY.md
@/Users/jeet/StudioProjects/eatfair-android/shared/src/main/java/ai/dollor/shared/data/remote/DollorApiService.kt
@/Users/jeet/StudioProjects/eatfair-android/app/src/main/java/ai/dollor/customer/data/CustomerRideshareApiService.kt
@apps/web/p2p-platform/backend/main_new.py
@apps/web/p2p-platform/backend/bid_routes.py
@apps/web/p2p-platform/backend/order_flow.py
@apps/web/p2p-platform/backend/chat_routes.py
@apps/web/p2p-platform/backend/matchmaking_routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Customer-facing Retrofit endpoints in DollorApiService.kt</name>
  <files>.planning/phases/03-android-api-verification/03-01-REPORT-CUSTOMER.md</files>
  <action>
Extract every Retrofit-annotated endpoint from DollorApiService.kt that is used by the Customer app. Customer-facing sections are:
- PUBLIC ENDPOINTS (lines ~15-32): `vendors/published`, `public/restaurants/{id}`
- CUSTOMER AUTHENTICATION (lines ~34-96): login, register, google, apple-auth, password reset, profile, demo-login, delete
- CUSTOMER ORDERS (lines ~98-217): orders, active-orders, track, create, confirm-payment, tip-driver, cancel, refund-status, modification, rate-driver, rate-restaurant, chat
- CUSTOMER ADDRESSES (lines ~219-264): CRUD for addresses, default
- CUSTOMER FAVORITES (lines ~265-296): favorites CRUD
- CUSTOMER RIDESHARE (lines ~297-355): ride request, history, track, cancel, rate, estimate, negotiate, accept-fare
- CUSTOMER PAYMENT CARDS (lines ~357-403): cards CRUD, default
- PROMOTIONS (lines ~405-432): active, featured, apply
- PAYMENTS (line ~1319): create-intent
- ORDER TRACKING (lines ~1325-1337): full-tracking, driver-location
- LEGAL (lines ~1339-1347): tos, privacy-policy
- PUSH NOTIFICATIONS (lines ~1379-1398): register-token
- TAX API (lines ~897-915): calculate, estimate

For each endpoint:
1. Extract HTTP method and path from the Retrofit annotation (e.g., `@GET("vendors/published")`)
2. Prepend `/api/` to the path (Retrofit base URL ends with `/api/`)
3. Search backend files for matching route: `grep -rn "the/path" apps/web/p2p-platform/backend/*.py`
4. Record: method, android path, backend route (or "NOT FOUND"), status (OK/MISMATCH/DEAD)
5. For mismatches, determine severity:
   - CRITICAL: Active feature, will cause 404/500 in production
   - MEDIUM: Used feature with wrong method or path variant
   - LOW: Minor inconsistency, backend accepts both
   - DEAD: Endpoint exists in code but feature not actively used

**IMPORTANT**: The Retrofit base URL uses `/api/` prefix. So `@GET("vendors/published")` maps to `GET /api/vendors/published`. Verify this by checking AppConfig.kt for the base URL pattern.

Also check AppConfig.kt to confirm the API base URL pattern:
```bash
grep -n "apiBaseUrl\|API_BASE_URL\|baseUrl" /Users/jeet/StudioProjects/eatfair-android/shared/src/main/java/ai/dollor/shared/config/AppConfig.kt
```

Write the report with this structure:
```markdown
# Android Customer App API Verification Report

## Build Baseline
- Package: ai.dollor.customer
- Last Firebase build: vC=25, v1.0.24 (Feb 23, 2026)
- API Base URL: [from AppConfig]

## Summary
- Total endpoints verified: N
- OK: N
- Mismatches: N (N critical, N medium, N low)
- Dead code: N

## Verification Results

### Public Endpoints
| # | Method | Android Path | Backend Route | Status | Notes |
|---|--------|-------------|---------------|--------|-------|

### Customer Authentication
[same table format]

[...repeat for each section...]

## Mismatches Detail
[For each mismatch: severity, impact, fix approach]

## Dead Code
[List endpoints that exist in DollorApiService but are not called by any Customer ViewModel/Screen]
```
  </action>
  <verify>
- Report file exists at `.planning/phases/03-android-api-verification/03-01-REPORT-CUSTOMER.md`
- Report contains "## Summary" with total counts
- Every `@GET/@POST/@PUT/@DELETE/@PATCH` annotation in customer-facing sections of DollorApiService.kt has a corresponding row in the report
- Each backend route verification was done via `grep` against actual backend `*.py` files
  </verify>
  <done>Every customer-facing Retrofit endpoint in DollorApiService.kt has been verified against backend routes and documented in the report</done>
</task>

<task type="auto">
  <name>Task 2: Verify CustomerRideshareApiService.kt OkHttp endpoints and produce final counts</name>
  <files>.planning/phases/03-android-api-verification/03-01-REPORT-CUSTOMER.md</files>
  <action>
Extract every `.url(` call from CustomerRideshareApiService.kt. There are ~24 OkHttp URL constructions. Each uses `$BASE_URL/api/...` pattern.

Known endpoints from the file:
1. `POST $BASE_URL/api/rides/request` - Create ride request
2. `GET $BASE_URL/api/rides/customer/$customerId/requests` - Get customer ride requests
3. `GET $BASE_URL/api/rides/request/$rideRequestId/bids` - Get bids for ride
4. `POST $BASE_URL/api/rides/bid/$bidId/respond` - Respond to bid (accept/reject/counter) - appears 3 times
5. `POST $BASE_URL/api/erp/rides/$rideId/customer-negotiate` - Customer negotiate
6. `POST $BASE_URL/api/erp/rides/$rideId/customer-accept-fare` - Accept fare
7. `GET $BASE_URL/api/erp/rides/$rideId/negotiation-status` - Negotiation status
8. `POST $BASE_URL/api/rides/request/$rideRequestId/cancel` - Cancel ride
9. `GET $BASE_URL/api/rides/$rideRequestId/track` - Track ride
10. `GET $BASE_URL/api/p2p/ride-requests/$rideRequestId/chat` - Get chat
11. `POST $BASE_URL/api/p2p/ride-requests/$rideRequestId/chat` - Send chat
12. `POST $BASE_URL/api/rides/$rideId/rate` - Rate ride
13. `POST $BASE_URL/api/rides/$rideId/tip` - Tip driver
14. `POST $BASE_URL/api/payments/ride/create-intent` - Create payment intent
15. `GET $BASE_URL/api/rides/request/$rideRequestId/receipt` - Get receipt
16. `POST $BASE_URL/api/rides/request/$rideRequestId/email-receipt` - Email receipt
17. `POST $BASE_URL/api/rides/dispute` - Create dispute
18. `GET $BASE_URL/api/rides/customer/$customerId/disputes` - Get disputes
19. `POST/GET $BASE_URL/api/rides/customer/$customerId/recurring-rides` - Recurring rides
20. `DELETE $BASE_URL/api/rides/recurring-rides/$id` - Delete recurring ride
21. `POST $BASE_URL/api/rides/estimate` - Fare estimate

For each:
1. Extract HTTP method (GET/POST/DELETE) and full URL path
2. Strip `$BASE_URL` prefix to get `/api/...` path
3. Verify against backend: `grep -rn "the/path" apps/web/p2p-platform/backend/*.py`
4. Record status

Append these results to the CustomerRideshareApiService section of the report.

Then update the Summary section with final totals across BOTH files (DollorApiService customer sections + CustomerRideshareApiService).

**Cross-reference with DollorApiService overlap**: Some rideshare endpoints exist in BOTH DollorApiService.kt (Retrofit) and CustomerRideshareApiService.kt (OkHttp). Note any duplicates -- both should point to the same backend route.
  </action>
  <verify>
- Report has a "### CustomerRideshareApiService (OkHttp)" section with all ~24 URL verifications
- Summary totals include both DollorApiService and CustomerRideshareApiService counts
- Any duplicates between the two files are noted
- Each endpoint was verified via grep against backend *.py files
  </verify>
  <done>All 24 CustomerRideshareApiService.kt OkHttp URLs verified, report has final summary with combined totals, duplicates noted</done>
</task>

</tasks>

<verification>
- Report exists and has complete summary with total counts
- Every customer-facing endpoint across both files has a verification row
- Mismatches (if any) have severity, impact, and fix approach documented
- No endpoint was skipped or left unverified
- Backend route verification used actual grep against *.py files, not memory
</verification>

<success_criteria>
- 100% of Android Customer app Retrofit endpoints verified against backend routes
- 100% of CustomerRideshareApiService OkHttp endpoints verified against backend routes
- All mismatches documented with severity and fix plan
- Report is actionable: someone can read it and fix every mismatch without interpretation
</success_criteria>

<output>
After completion, create `.planning/phases/03-android-api-verification/03-01-SUMMARY.md`
</output>
