---
phase: 54.6-enterprise-hardening-starter-pack-vpc-rds-proxy-waf-guardduty-close-sg
plan: 01
subsystem: infra
tags: [vpc, aurora, networking, nat, security-groups, paused, checkpoint]

requires:
  - phase: 54.5-aurora-postgres-migration-leave-supabase
    provides: "54.5-03 — Aurora 16.4 cluster zietra-aurora-prod LIVE in default VPC, 4 Lambdas migrated, 0/0:5432 SG still open (closed in this phase 54.6)"

provides:
  - "zietra-prod-vpc (vpc-012ab4500dcd4ee41) provisioned: 10.0.0.0/16 + DNS hostnames + DNS support"
  - "Internet Gateway zietra-prod-igw (igw-0f7bb813b6b5edb0d) attached"
  - "4 subnets across 2 AZs: public-1a/1b + private-1a/1b with correct CIDRs (10.0.0.0/24 .. 10.0.11.0/24)"
  - "Idempotent provisioning script scripts/setup-vpc-and-private-aurora.sh (275 lines) — safe to re-run; will resume Task 1.b after EIP unblock"
  - "Live runbook .planning/runbooks/aurora-vpc-migration-54-6-01.md (~120 lines) with resource IDs + EIP-quota decision tree"

affects:
  - "54.6-02 (RDS Proxy + Lambda VPC-attach + SG close) — gated on Task 1.b completion (NAT + SGs)"
  - "54.6-03 (WAF + GuardDuty + Security Hub + Config) — independent of VPC, can start in parallel"
  - "54.6-04 (security trust page + smoke matrix) — gated on 54.6-02 + 54.6-03"

tech-stack:
  added: []
  patterns:
    - "Idempotent AWS CLI provisioning via Name-tag lookups — every create is preceded by a describe with Name filter; existing resource ID returned on re-run, new resource created only if absent"
    - "EIP soft-quota ceiling is not exposed via service-quotas API for vpc-domain EIPs — describe-addresses count vs allocate-address response is the only ground truth"

key-files:
  created:
    - "scripts/setup-vpc-and-private-aurora.sh (275 lines, idempotent VPC+IGW+subnets+NAT+route-tables+3 SGs provisioner)"
    - ".planning/runbooks/aurora-vpc-migration-54-6-01.md (live cutover record, partial T+0 captures, EIP-quota decision tree)"
    - ".planning/phases/54.6-.../54.6-01-SUMMARY.md (this file — PARTIAL summary, plan PAUSED at Rule-4 architectural checkpoint)"
  modified: []

key-decisions:
  - "Script idempotency via Name-tag describe-then-create pattern: lets us re-run after partial failure without duplicate resource creation. Critical because Task 1 needed to halt mid-execution and we don't want re-run to spawn a second VPC/IGW/subnet set."
  - "Partial VPC state LEFT IN PLACE on EIP block: no cost ($0/hr — VPC/IGW/subnets are free) and idempotent re-run will complete cleanly. Cheaper than tearing down and rebuilding."
  - "Stopped at Rule-4 architectural checkpoint (EIP quota) — required operator decision among 4 unblock options (reclaim NAT-A, reclaim NAT-B, quota increase, NAT instance). Per deviation rules, Rule 4 = STOP, ask, do not guess."

requirements-completed: []   # PARTIAL — VpcIsolation in progress, AuroraPrivate not started

duration: 1min 49sec   # halted at EIP block
completed: 2026-05-15   # PARTIAL — checkpoint reached, not closed
---

# Phase 54.6 Plan 01: Aurora VPC Migration — Wave 1 (PAUSED at Rule-4 checkpoint)

**Idempotent VPC provisioning script committed; VPC + IGW + 4 subnets created successfully; execution halted at EIP allocation step (AWS `AddressLimitExceeded`, account at 21/21 EIPs); 4 unblock paths documented; awaiting operator decision before Task 1.b (NAT + SGs) and Task 2 (GO/NO-GO before snapshot).**

## Performance

- **Duration:** 1 min 49 sec wall-clock
- **Started:** 2026-05-15T06:53:59Z
- **Halted:** 2026-05-15T06:55:48Z (Rule-4 architectural checkpoint)
- **Tasks executed:** 1 (Task 1.a — VPC fabric)
- **Tasks blocked:** Task 1.b (NAT + SGs), Task 2 (GO/NO-GO), Tasks 3-5
- **Files created:** 3 (script, runbook, this PARTIAL summary)
- **Files modified:** 0
- **Commits:** 1 (`677b2111` — partial Task 1)

## Accomplishments (Task 1.a)

### VPC + IGW + 4 subnets

| Resource | Name | ID | Detail |
|----------|------|----|---------|
| VPC | zietra-prod-vpc | `vpc-012ab4500dcd4ee41` | 10.0.0.0/16, DNS hostnames + support enabled |
| IGW | zietra-prod-igw | `igw-0f7bb813b6b5edb0d` | attached to vpc-012ab4500dcd4ee41 |
| Subnet | zietra-prod-public-1a | `subnet-0b5e4abedde216d3a` | 10.0.0.0/24, us-east-1a, tier=public |
| Subnet | zietra-prod-public-1b | `subnet-02f98e40f1b4afef7` | 10.0.1.0/24, us-east-1b, tier=public |
| Subnet | zietra-prod-private-1a | `subnet-052ed80f6904b9fe7` | 10.0.10.0/24, us-east-1a, tier=private |
| Subnet | zietra-prod-private-1b | `subnet-07893035668f1b015` | 10.0.11.0/24, us-east-1b, tier=private |

### Idempotent script committed

`scripts/setup-vpc-and-private-aurora.sh` (275 lines) is safe to re-run from a blank or partial state. Each create is preceded by a Name-tag describe; existing IDs are returned, new resources are created only if absent.

## Task Commits

1. **Task 1 (partial — VPC + IGW + 4 subnets, EIP block recorded):** `677b2111`

## Deviations from Plan

### Architectural Checkpoint (Rule 4)

**1. [Rule 4 - Architectural] EIP quota exhausted, blocking NAT Gateway creation**
- **Found during:** Task 1.b (EIP allocation)
- **Issue:** `aws ec2 allocate-address --domain vpc` returned `AddressLimitExceeded`. Account has 21/21 vpc-domain EIPs currently allocated. `service-quotas` API reports the default soft quota of `5`, but actual allowed ceiling is at 21 (account-level adjustment already applied). Cannot allocate the 22nd EIP for the new NAT Gateway.
- **Impact:** Task 1.b (NAT + SGs) cannot proceed → Task 2 (GO/NO-GO) cannot be reached → Tasks 3-5 (snapshot, restore, parity, rollback) cannot proceed.
- **Per deviation Rule 4** (architectural changes require user decision), execution STOPPED and a structured checkpoint is being returned to the orchestrator. **No auto-fix attempted** — choice among reclaim/quota-increase/NAT-instance is too consequential.
- **Unblock options documented in runbook:**
  - **A** — Reclaim `nat-0a41bcbe59eba13a8` (`dollor-nat-gateway`, pre-zietra demo VPC) + its EIP `eipalloc-0d501a89b5b0f0b2e`
  - **B** — Reclaim `nat-0edf6d0ff7ec26b80` (`dollor-staging-nat-1`, dollor-staging VPC) + its EIP `eipalloc-0737b35f5bcab1059`
  - **C** — File AWS support ticket to raise EIP quota 21 → 25 (no risk, takes hours-days)
  - **D** — Use a NAT instance (EC2 with public-IP ENI) instead of NAT Gateway (cheaper but lower HA / our maintenance burden)

## Authentication Gates

None. AWS API calls succeeded; the only failure was the resource-limit business error from `AllocateAddress`.

## Issues Encountered

EIP quota block — described above as Rule-4 architectural checkpoint, awaiting operator decision.

## Next Phase Readiness

**Phase 54.6-01 is PAUSED, not complete.** To resume:

1. Operator picks one of options A / B / C / D (see runbook).
2. If A or B: `aws ec2 delete-nat-gateway --nat-gateway-id <chosen-id>` → wait ~5 min for `state=deleted` (EIP returns to pool).
3. If C: file AWS support ticket → wait for quota approval.
4. If D: edit script to use NAT instance pattern instead of NAT Gateway (separate plan recommended).
5. Re-run: `bash scripts/setup-vpc-and-private-aurora.sh > /tmp/vpc-provision.log 2>&1`. Script will skip already-created resources (VPC + IGW + 4 subnets) and create the EIP + NAT GW + route tables + 3 SGs.
6. Resume at Task 2 (GO/NO-GO checkpoint before snapshot cutover).

**State after resume completion of Task 1.b:** all VPC fabric in place, ready for the snapshot+restore cutover. Aurora `zietra-aurora-prod` STILL LIVE in default VPC serving all 4 Lambdas (untouched). Rollback for what landed today = zero work (the VPC + 4 subnets + IGW are inert and cost $0; can be torn down via reverse-order deletes if the phase is abandoned).

## Self-Check: PASSED

- Files claimed → on disk: 3/3
  - `scripts/setup-vpc-and-private-aurora.sh` (275 lines, executable, syntax-valid `bash -n` clean)
  - `.planning/runbooks/aurora-vpc-migration-54-6-01.md` (created, captures all 6 resource IDs from Task 1.a)
  - `.planning/phases/54.6-.../54.6-01-SUMMARY.md` (this file)
- Commit claimed → in `git log`: 1/1
  - `677b2111` ✓ (verified via `git rev-parse --short HEAD`)
- AWS state verified directly: VPC `vpc-012ab4500dcd4ee41` State=available, 4 subnets across 2 AZs with correct CIDRs, IGW `igw-0f7bb813b6b5edb0d` attached
- EIP block reproduced and recorded: 21 EIPs in `describe-addresses`, `AllocateAddress` returned `AddressLimitExceeded` with the script's `set -euo pipefail` halting cleanly

---
*Phase: 54.6-enterprise-hardening-starter-pack-vpc-rds-proxy-waf-guardduty-close-sg*
*Halted: 2026-05-15T06:55Z (Rule-4 architectural checkpoint)*
*Resume on operator decision among A / B / C / D (see runbook)*
